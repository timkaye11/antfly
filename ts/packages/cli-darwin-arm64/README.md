# @antfly/cli-darwin-arm64

Platform package for `@antfly/cli` on macOS arm64.

Install through `npm install -g @antfly/cli`, or outside npm with
`brew install antflydb/taps/antfly` or
`curl -fsSL https://releases.antfly.io/antfly/latest/install.sh | sh`.
