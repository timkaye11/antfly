# @antfly/cli-linux-arm64

glibc platform package for `@antfly/cli` on Linux arm64.

For musl systems, install the portable archive with `curl -fsSL https://releases.antfly.io/antfly/latest/install.sh | sh` instead.
