# Model Explorer sharing review

Reviewed 2026-09-09 against `26e332ed8c335d75eecd875548c6c36bcb47d183`, confirmed as GitHub `main` at the start of this review. The local app branch remains `model_explorer` at merge commit `edad9b743a99413f1be8926eb361b33ef73f487c`; it is unpublished. All inference/tokenizer source files read by the generator match the published source revision. The explorer links to that published revision, so the app does not require publishing its local merge commit to make its code references work.

GitHub `main` advanced during the final check to `aa44bddd1dd8befb5d0aec8bdb6c304054b89149` (PR #629, 2026-09-09 21:57 UTC). The additional commit was reviewed separately: it adds bounded cross-request embedding/GLiNER and compatible tensor-stage scheduling, cached Metal provider execution leases, and cancellation/admission integration. The tokenizer change adds allocation-failure cleanup without changing tokenization output. The selected model architecture and math explanations remain applicable. A dated note on the runtime page links the newer batching and provider contracts directly. Generated diagrams, indexes and snippets retain the source revision matching this checkout; the explorer does not claim they were generated from the newer main commit. No inference merge was made.

Three parallel agents reviewed Gemma4, GLiNER2/Qwen3, and data integrity. The integrating review covered shared runtime explanations, visual semantics, accessibility, browser behavior, static serving, and the final build. All changes are local; no commit, push, PR comment, or deployment was made.

## Material findings resolved

| Area | Correction and reason | Evidence |
| --- | --- | --- |
| Gemma variants | E2B uses a 4:1 sliding/global attention pattern with 12 sliding and 3 global KV owners. E4B uses 5:1 with 20 sliding and 4 global owners. Graph overrides, diagrams, and explanations now distinguish them. | `models/gpt.zig`, `data/curated/gemma4.model.json` |
| Gemma PLE | Described the token and normalized context branches, corrected residual/fusion explanations, and separated activation precision from weight quantization. E4B staging savings are explicitly an approximately 25.8 MB tensor-size calculation. | `architectures/gpt.zig`, `content/gemma4/` |
| Gemma optional paths | LM-head Q4_K repacking is opt-in candidate selection with Q6_K retained. Device sampling, top-8 refinement, host fallback, and MTP are separate eligible paths. Dense E2B/E4B and related A4B MoE material are distinguished. | `backends/metal_runtime.zig`, `pipelines/generation.zig` |
| GLiNER2 | Corrected relative-position buckets, label-count costs, serving first-subtoken pooling, span overlap filtering, and multi-kernel gradient descriptions. Entity extraction is the illustrated task. | `architectures/gliner_head.zig`, `architectures/deberta.zig`, `content/gliner2/` |
| Qwen3 Embedding | Corrected query/document task formatting, last-active-token pooling, normalization, EOS handling, graph execution and temporary attention tensors. Removed unsupported headline performance claims. | `pipelines/embedding.zig`, `registry/qwen3_embedding_catalog.zig`, shared encoder graph helpers, `content/qwen3-embedding/` |
| Qwen3-VL | Updated artifact/role/backend admission, removed implied video support and calibrated-score claims, and separated prefill from decode. Corrected QKV widths, residuals, image replacement length, DeepStack placement and representative 2B geometry. | `MODEL_COMPATIBILITY.md`, Qwen VL generation/vision paths, `content/qwen3-vl/` |
| Shared runtime | Replaced the claim of one identical pipeline with linked runtime topics. Clarified resource admission, cancellation, cache residency, strict backend policies, optional fallback, command encoding, and generated/handwritten/JIT kernel routes. | `server/server.zig`, `execution_control.zig`, `architectures/session_factory.zig`, `graph/backend_contracts.zig` |
| KV teaching | Distinguished logical KV ownership, standard full-history packing, and eligible split SWA rings. Embeddings and extraction do not pretend to allocate a persistent paged decode cache. Corrected polar4's int8-style values and allocation-boundary examples. | `pipelines/generation.zig`, `runtime/kv/pool.zig`, `app/systems/kv/` |
| Performance evidence | Labelled historical experiments and their timing boundaries. Removed an additive tok/s waterfall and unsupported competitor hypotheses. Roofline now correctly states bandwidth divided by bytes/token; recorded strict benchmarks retain protocol and build limitations. | `GEMMA4_PERF_PLAN.md`, `content/perf.ts`, `app/systems/perf/` |
| Frame diagrams | Marked representative scopes, equal widths, and overlapping lanes as schematics. Census counts have historical provenance and do not imply captured GPU timings or a matched before/after run. | `data/curated/frames/`, `app/systems/timeline/` |
| Source integrity | Refreshed stale anchors and added exact source-byte verification, published-revision selection, graph/Sankey checks, frame/barrier checks, and complete schedule parsing. The snapshot indexes 72 op enum members, 417 Metal kernels, 25 schedule routes, 1,463 textual environment-name references, and 114 snippets. | `generator/`, `data/generated/manifest.json` |
| Visual semantics | Unknown activation dtypes no longer use a low-bit quantization color. Stage sizes are distinguished from a representative node's repetitions. Environment names and kernel families are labelled as source/name-derived indexes. | `components/primitives/chips.tsx`, DAG inspector, legend and systems pages |
| Browser correctness | Fixed Gemma hydration caused by a multi-child SVG title, stabilized locale formatting, corrected mobile navigation and grid overflow, and fixed reduced-motion inline figures and hidden-scene focus. | `components/viz/sankey-flow.tsx`, `components/scrollytelling/scrolly.tsx`, `app/globals.css` |
| Accessibility | Replaced invalid panel-less tabs with labelled selection buttons, labelled the actual range control, added keyboard figure interactions and unique SVG IDs, bound missing SVG theme fill/stroke styles, and improved text contrast in both themes without fading informative diagram labels. | `components/primitives/choice-group.tsx`, glyphs and figure controls |
| Static sharing | Replaced incompatible `next start` with a static preview server. Deep links resolve to exported files; missing routes return 404. Added page titles, a source footer, and build/maintenance instructions. | `scripts/preview.ts`, `README.md`, page metadata |

Inference paths in the evidence column are relative to `zig/pkg/inference/src/` unless they are documentation or app paths. The Qwen geometry was also checked against the official [Embedding 0.6B configuration](https://huggingface.co/Qwen/Qwen3-Embedding-0.6B/raw/main/config.json) and [VL 2B configuration](https://huggingface.co/Qwen/Qwen3-VL-2B-Instruct/raw/main/config.json). Static-host behavior follows the [Next.js static export contract](https://nextjs.org/docs/app/guides/static-exports).

## Validation

- Generated snapshot: all 13 files match; all referenced source bytes match the published revision.
- Regression tests: 9 pass, covering graph references, Sankey cycles, frame/barrier validity, source bounds, enum locations, schedule extraction, and static path resolution.
- TypeScript and full application Biome lint pass.
- Production build exports 21 pages with bundled local fonts.
- Exported HTML: 319 internal links checked, no missing pages/fragments or duplicate IDs.
- Browser checks: 11 interaction scenarios pass, covering model navigation, graph search/inspection, URL state and malformed-value fallback, KV replay/keyboard range/model reset, filters and empty states, timeline selection, mobile navigation, reduced-motion layout, themes, and 404 behavior.
- Accessibility and hydration: all 18 public routes return HTTP 200 with no axe WCAG 2 A/AA or 2.1 AA violations in the tested light/dark initial page states, no page/hydration errors, and no horizontal overflow at 390px. Screenshot review additionally checked SVG labels that axe does not evaluate. A separate rendered-style check verifies 987 themed SVG labels per theme across six representative pages (including duplicate inline/sticky figures). These automated checks are not a complete WCAG conformance audit.

The local build used Node 25.2.1 and the workspace-managed pnpm 11.10.0. The workspace recommends Node 24.16.0; use the pinned versions for team/CI reproduction. Validation set `pnpm_config_verify_deps_before_run=false` to reuse the installed workspace dependencies without pnpm attempting a reinstall; dependencies and the lockfile were not changed. Browser validation used installed Chromium through Playwright 1.61.0 and axe-core 4.12.1; mobile checks are viewport emulation, not physical-device Safari tests. Turbopack and local preview/browser workers required execution outside the filesystem sandbox because its local-port restrictions prevented CSS processing and browser startup. The application itself needed no inference server, model weights, API keys, or font downloads.

## Scope and sharing

The app is an educational companion with selected representative paths. It is not a live runtime trace, exhaustive model compatibility catalog, or new numerical/performance qualification. Separate Qwen3 text-generation/text-reranker walkthroughs and GLiNER classification/relation diagrams remain outside this tour; the compatibility link makes the broader serving policy available.

No inference kernels or model weights changed, and no model workloads or hardware benchmarks were run. Historical performance values remain historical. For local review, run `pnpm preview` after building; for sharing, publish the generated `out/` directory with the static routing behavior documented in `README.md`. This branch already tracks the export directory, so its generated output is refreshed locally along with source changes.

## Addendum — 14 September 2026

The GLiNER2.5 (gliner_boundary) pages, the merged `gliner2pointfive` sources, and the subsequent review-fix rounds postdate this review; the counts and page totals above describe the 9 September state. The current snapshot is pinned to local merge commit `05dc2b75e20e0bfca168746f94639713568be9d0` and indexes 72 op enum members, 431 Metal kernels, 25 schedule routes, 1,475 textual environment-name references, and 154 snippets, exporting 23 pages. The GLiNER2.5 chapters were fact-checked against the gliner_boundary implementation in that merge, but are not covered by the 9 September accessibility and hydration sweep.
