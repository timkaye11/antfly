// Copyright 2026 Antfly, Inc.
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

#import <Foundation/Foundation.h>
#import <CoreFoundation/CoreFoundation.h>
#import <Metal/Metal.h>
#import <MetalPerformanceShaders/MetalPerformanceShaders.h>
#import <MetalPerformanceShadersGraph/MetalPerformanceShadersGraph.h>
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>
#include <math.h>
#include <limits.h>
#include <unistd.h>

#define TERMITE_METAL_LAYER_NORM_SLOT_CAPACITY 256
#define TERMITE_METAL_RMS_NORM_SLOT_CAPACITY 512
#define TERMITE_METAL_LINEAR_SLOT_CAPACITY 512
#define TERMITE_METAL_ATTENTION_SPAN_SLOT_CAPACITY 256
#define TERMITE_METAL_SCRATCH_POOL_CAPACITY 16
#define TERMITE_METAL_GRAPH_PLAN_SLOT_CAPACITY 29
#define TERMITE_METAL_GRAPH_PLAN_PROJECTION_SLOT_BASE 0
#define TERMITE_METAL_GRAPH_PLAN_DIRECT_HIDDEN_SLOT_BASE 6
#define TERMITE_METAL_GRAPH_PLAN_DIRECT_Q_SLOT 10
#define TERMITE_METAL_GRAPH_PLAN_SAMPLE_LOGITS_SLOT 11
#define TERMITE_METAL_GRAPH_PLAN_SAMPLE_TOPK_VALUES_SLOT 12
#define TERMITE_METAL_GRAPH_PLAN_SAMPLE_TOPK_IDS_SLOT 13
#define TERMITE_METAL_GRAPH_PLAN_SAMPLE_PROBS_SLOT 14
#define TERMITE_METAL_GRAPH_PLAN_ATTENTION_SPAN_KEY_SLOT 15
#define TERMITE_METAL_GRAPH_PLAN_ATTENTION_SPAN_VALUE_SLOT 16
#define TERMITE_METAL_GRAPH_PLAN_HOT_HIDDEN_SLOT_BASE 17
#define TERMITE_METAL_GRAPH_PLAN_HOT_INTERMEDIATE_SLOT_BASE 21
#define TERMITE_METAL_GRAPH_PLAN_DEBERTA_CC_SCORE_SLOT 26
#define TERMITE_METAL_GRAPH_PLAN_DEBERTA_C2P_SCORE_SLOT 27
#define TERMITE_METAL_GRAPH_PLAN_DEBERTA_P2C_SCORE_SLOT 28
#define TERMITE_METAL_DENSE_QKV_PACK_CACHE_CAPACITY TERMITE_METAL_LINEAR_SLOT_CAPACITY
#define TERMITE_METAL_GENERIC_EMBEDDING_CACHE_CAPACITY 8
#define TERMITE_METAL_DIRECT_BLOCK_HIDDEN_BUFFER_COUNT 4
#define TERMITE_METAL_DIRECT_BLOCK_SHARED_BUFFER_COUNT 3
#define TERMITE_METAL_BLIT_COUNTER_COUNT 7
#define TERMITE_METAL_COMPUTE_COUNTER_COUNT 13
#define TERMITE_METAL_COMPUTE_REGION_COUNTER_COUNT 9
#define TERMITE_METAL_Q8_0_LINEAR_FAMILY_COUNT 12
#define TERMITE_METAL_Q8_0_LINEAR_DISPATCH_COUNT 4
#define TERMITE_METAL_PLAN_OP_COUNTER_COUNT 32
#define TERMITE_METAL_OPERATOR_COUNTER_COUNT 16
#define TERMITE_METAL_QUANT_DISPATCH_COUNTER_COUNT 4

#define TERMITE_METAL_PLAN_OP_UNKNOWN 0u
#define TERMITE_METAL_PLAN_OP_DECODE_ATTENTION_PRE_NORM 1u
#define TERMITE_METAL_PLAN_OP_DECODE_Q_OR_QKV_LINEAR 2u
#define TERMITE_METAL_PLAN_OP_DECODE_Q_HEAD_NORM_ROPE 3u
#define TERMITE_METAL_PLAN_OP_DECODE_K_HEAD_NORM_ROPE 4u
#define TERMITE_METAL_PLAN_OP_DECODE_V_NORM 5u
#define TERMITE_METAL_PLAN_OP_DECODE_KV_SEED 6u
#define TERMITE_METAL_PLAN_OP_DECODE_ATTENTION 7u
#define TERMITE_METAL_PLAN_OP_DECODE_ATTENTION_OUTPUT_LINEAR 8u
#define TERMITE_METAL_PLAN_OP_DECODE_ATTENTION_POST_NORM_RESIDUAL 9u
#define TERMITE_METAL_PLAN_OP_DECODE_FFN_PRE_NORM_SCALE 10u
#define TERMITE_METAL_PLAN_OP_DECODE_FFN_GATE_UP_ACTIVATION 11u
#define TERMITE_METAL_PLAN_OP_DECODE_FFN_DOWN_LINEAR 12u
#define TERMITE_METAL_PLAN_OP_DECODE_FFN_POST_NORM_RESIDUAL 13u
#define TERMITE_METAL_PLAN_OP_DECODE_PLE_GATE_ACTIVATION 14u
#define TERMITE_METAL_PLAN_OP_DECODE_PLE_PROJECTION 15u
#define TERMITE_METAL_PLAN_OP_DECODE_PLE_POST_NORM_RESIDUAL 16u
#define TERMITE_METAL_PLAN_OP_DECODE_TAIL_FINAL_NORM 17u
#define TERMITE_METAL_PLAN_OP_DECODE_TAIL_LM_HEAD 18u
#define TERMITE_METAL_PLAN_OP_DECODE_TAIL_ARGMAX 19u
#define TERMITE_METAL_PLAN_OP_QUANT_GET_ROWS 20u
#define TERMITE_METAL_PLAN_OP_QUANT_SET_ROWS 21u
#define TERMITE_METAL_PLAN_OP_QUANT_COPY_Q_TO_F32 22u
#define TERMITE_METAL_PLAN_OP_QUANT_COPY_F32_TO_Q 23u
#define TERMITE_METAL_PLAN_OP_ATTENTION_FLASH 24u
#define TERMITE_METAL_PLAN_OP_ATTENTION_PAGED 25u
#define TERMITE_METAL_PLAN_OP_ATTENTION_QUANTIZED_KV 26u

#define TERMITE_METAL_OPERATOR_FALLBACK 0u
#define TERMITE_METAL_OPERATOR_MUL_MV 1u
#define TERMITE_METAL_OPERATOR_MUL_MV_EXT 2u
#define TERMITE_METAL_OPERATOR_MUL_MM 3u
#define TERMITE_METAL_OPERATOR_GET_ROWS 4u
#define TERMITE_METAL_OPERATOR_SET_ROWS 5u
#define TERMITE_METAL_OPERATOR_CPY_Q_TO_F32 6u
#define TERMITE_METAL_OPERATOR_CPY_F32_TO_Q 7u
#define TERMITE_METAL_OPERATOR_ATTENTION_FLASH 8u
#define TERMITE_METAL_OPERATOR_ATTENTION_PAGED 9u
#define TERMITE_METAL_OPERATOR_ATTENTION_QUANTIZED_KV 10u
#define TERMITE_METAL_OPERATOR_UNKNOWN 255u

#define TERMITE_METAL_GRAPH_QUANT_FORMAT_Q4_0 1u
#define TERMITE_METAL_GRAPH_QUANT_FORMAT_Q4_1 2u
#define TERMITE_METAL_GRAPH_QUANT_FORMAT_Q5_0 3u
#define TERMITE_METAL_GRAPH_QUANT_FORMAT_Q5_1 4u
#define TERMITE_METAL_GRAPH_QUANT_FORMAT_Q8_0 5u
#define TERMITE_METAL_GRAPH_QUANT_FORMAT_Q8_1 6u
#define TERMITE_METAL_GRAPH_QUANT_FORMAT_Q2_K 7u
#define TERMITE_METAL_GRAPH_QUANT_FORMAT_Q3_K 8u
#define TERMITE_METAL_GRAPH_QUANT_FORMAT_Q4_K 9u
#define TERMITE_METAL_GRAPH_QUANT_FORMAT_Q5_K 10u
#define TERMITE_METAL_GRAPH_QUANT_FORMAT_Q6_K 11u
#define TERMITE_METAL_GRAPH_QUANT_FORMAT_Q8_K 12u
#define TERMITE_METAL_GRAPH_QUANT_FORMAT_IQ2_XS 16u
#define TERMITE_METAL_GRAPH_QUANT_FORMAT_IQ4_NL 20u
#define TERMITE_METAL_GRAPH_QUANT_FORMAT_IQ4_XS 21u
#define TERMITE_METAL_GRAPH_QUANT_FORMAT_I2_S 24u
#define TERMITE_METAL_GRAPH_QUANT_FORMAT_I8_S 25u
#define TERMITE_METAL_GRAPH_QUANT_FORMAT_TL1 26u
#define TERMITE_METAL_GRAPH_QUANT_FORMAT_TL2 27u
#define TERMITE_METAL_GRAPH_QUANT_FORMAT_MXFP4 28u
#define TERMITE_METAL_GRAPH_QUANT_FORMAT_NVFP4 29u
#define TERMITE_METAL_GRAPH_QUANT_FORMAT_Q1_0 30u
#define TERMITE_METAL_GRAPH_ATTENTION_KV_FORMAT_F32 0u
#define TERMITE_METAL_GRAPH_ATTENTION_KV_FORMAT_POLAR4 1u
#define TERMITE_METAL_GRAPH_ATTENTION_KV_FORMAT_TURBO3 2u
#define TERMITE_METAL_GRAPH_ATTENTION_KV_FORMAT_QUANTIZED 3u
#define TERMITE_METAL_GRAPH_FORMAT_UNKNOWN 255u

typedef struct {
    uint16_t kind;
    uint8_t barrier_before;
    uint8_t quant_dispatch;
    uint8_t op;
    uint8_t format;
    uint8_t input_dtype;
    uint8_t output_dtype;
    uint32_t source;
    uint32_t region;
    uint32_t scope_index;
    size_t resource_start;
    size_t resource_count;
} termite_metal_runtime_command_op;

typedef struct {
    const uint16_t *ops;
    size_t op_count;
    const uint8_t *barriers;
    size_t barrier_count;
    const uint8_t *quant_dispatches;
    size_t quant_dispatch_count;
    const termite_metal_runtime_command_op *command_ops;
    size_t command_op_count;
    size_t start_index;
} termite_metal_planned_layer_contract;

typedef enum termite_metal_quant_matmul_activation_dtype {
    TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32 = 0,
    TERMITE_METAL_QUANT_MATMUL_ACTIVATION_BF16 = 1,
    TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F16 = 2,
} termite_metal_quant_matmul_activation_dtype;

typedef struct {
    struct termite_metal_decode_runtime *runtime;
    termite_metal_planned_layer_contract contract;
    size_t index;
    bool use_ops;
    bool use_barriers;
    bool use_quant_dispatches;
    bool use_command_ops;
    uint8_t last_quant_dispatch;
    uint8_t last_operator;
    uint8_t last_format;
    uint8_t last_input_dtype;
    uint8_t last_output_dtype;
    int failure_code;
} termite_metal_planned_layer_cursor;

typedef enum {
    TERMITE_METAL_Q80_F32_KV_LAYER_STEP_ATTENTION = 0,
    TERMITE_METAL_Q80_F32_KV_LAYER_STEP_ATTENTION_OUTPUT_LINEAR,
    TERMITE_METAL_Q80_F32_KV_LAYER_STEP_ATTENTION_POST_NORM_RESIDUAL,
    TERMITE_METAL_Q80_F32_KV_LAYER_STEP_FFN_PRE_NORM_SCALE,
    TERMITE_METAL_Q80_F32_KV_LAYER_STEP_FFN_GATE_UP_ACTIVATION,
    TERMITE_METAL_Q80_F32_KV_LAYER_STEP_FFN_DOWN_LINEAR,
    TERMITE_METAL_Q80_F32_KV_LAYER_STEP_FFN_POST_NORM_RESIDUAL,
    TERMITE_METAL_Q80_F32_KV_LAYER_STEP_PLE_GATE_ACTIVATION,
    TERMITE_METAL_Q80_F32_KV_LAYER_STEP_PLE_PROJECTION,
    TERMITE_METAL_Q80_F32_KV_LAYER_STEP_PLE_POST_NORM_RESIDUAL,
    TERMITE_METAL_Q80_F32_KV_LAYER_STEP_COUNT
} termite_metal_q80_f32_kv_layer_step;

static const uint16_t TERMITE_METAL_Q80_F32_KV_LAYER_STEP_KINDS[TERMITE_METAL_Q80_F32_KV_LAYER_STEP_COUNT] = {
    TERMITE_METAL_PLAN_OP_DECODE_ATTENTION,
    TERMITE_METAL_PLAN_OP_DECODE_ATTENTION_OUTPUT_LINEAR,
    TERMITE_METAL_PLAN_OP_DECODE_ATTENTION_POST_NORM_RESIDUAL,
    TERMITE_METAL_PLAN_OP_DECODE_FFN_PRE_NORM_SCALE,
    TERMITE_METAL_PLAN_OP_DECODE_FFN_GATE_UP_ACTIVATION,
    TERMITE_METAL_PLAN_OP_DECODE_FFN_DOWN_LINEAR,
    TERMITE_METAL_PLAN_OP_DECODE_FFN_POST_NORM_RESIDUAL,
    TERMITE_METAL_PLAN_OP_DECODE_PLE_GATE_ACTIVATION,
    TERMITE_METAL_PLAN_OP_DECODE_PLE_PROJECTION,
    TERMITE_METAL_PLAN_OP_DECODE_PLE_POST_NORM_RESIDUAL,
};

typedef enum {
    TERMITE_METAL_TAIL_STEP_FINAL_NORM = 0,
    TERMITE_METAL_TAIL_STEP_LM_HEAD,
    TERMITE_METAL_TAIL_STEP_ARGMAX,
    TERMITE_METAL_TAIL_STEP_COUNT
} termite_metal_tail_step;

static const uint16_t TERMITE_METAL_TAIL_STEP_KINDS[TERMITE_METAL_TAIL_STEP_COUNT] = {
    TERMITE_METAL_PLAN_OP_DECODE_TAIL_FINAL_NORM,
    TERMITE_METAL_PLAN_OP_DECODE_TAIL_LM_HEAD,
    TERMITE_METAL_PLAN_OP_DECODE_TAIL_ARGMAX,
};

#define TERMITE_METAL_QUANT_FORMAT_UNSUPPORTED 0u
#define TERMITE_METAL_QUANT_FORMAT_Q1_0 1u
#define TERMITE_METAL_QUANT_FORMAT_I2_S 2u
#define TERMITE_METAL_QUANT_FORMAT_I8_S 3u
#define TERMITE_METAL_QUANT_FORMAT_Q2_K 4u
#define TERMITE_METAL_QUANT_FORMAT_Q3_K 5u
#define TERMITE_METAL_QUANT_FORMAT_Q4_0 6u
#define TERMITE_METAL_QUANT_FORMAT_Q4_1 7u
#define TERMITE_METAL_QUANT_FORMAT_Q4_K 8u
#define TERMITE_METAL_QUANT_FORMAT_Q5_0 9u
#define TERMITE_METAL_QUANT_FORMAT_Q5_1 10u
#define TERMITE_METAL_QUANT_FORMAT_Q5_K 11u
#define TERMITE_METAL_QUANT_FORMAT_Q6_K 12u
#define TERMITE_METAL_QUANT_FORMAT_Q8_0 13u
#define TERMITE_METAL_QUANT_FORMAT_Q8_1 14u
#define TERMITE_METAL_QUANT_FORMAT_Q8_K 15u
#define TERMITE_METAL_QUANT_FORMAT_IQ2_XS 19u
#define TERMITE_METAL_QUANT_FORMAT_IQ4_NL 23u
#define TERMITE_METAL_QUANT_FORMAT_IQ4_XS 24u
#define TERMITE_METAL_QUANT_FORMAT_MXFP4 27u
#define TERMITE_METAL_QUANT_FORMAT_NVFP4 28u
#define TERMITE_METAL_QUANT_FORMAT_TL1 29u
#define TERMITE_METAL_QUANT_FORMAT_TL2 30u

#define TERMITE_METAL_DENSE_LINEAR_DTYPE_F32 0u
#define TERMITE_METAL_DENSE_LINEAR_DTYPE_BF16 1u

static id<MTLComputeCommandEncoder> termite_metal_tracked_compute_command_encoder_at(id<MTLCommandBuffer> command_buffer, const char *func, int line);
static id<MTLComputeCommandEncoder> termite_metal_tracked_compute_command_encoder_for(id<MTLCommandBuffer> command_buffer, size_t source);
static id<MTLBlitCommandEncoder> termite_metal_tracked_blit_command_encoder(id<MTLCommandBuffer> command_buffer);
static void termite_metal_record_active_frame_blit_source(id<MTLCommandBuffer> command_buffer, size_t source);

#define termite_metal_tracked_compute_command_encoder(command_buffer) termite_metal_tracked_compute_command_encoder_at((command_buffer), __func__, __LINE__)

typedef struct termite_metal_embedding_table_cache_entry {
    id<MTLBuffer> buffer;
    size_t bytes;
    size_t rows;
    size_t dim;
    uintptr_t source_id;
    size_t source_offset;
} termite_metal_embedding_table_cache_entry;

typedef struct termite_metal_mpsgraph_ffn_cache {
    MPSGraph *graph;
    MPSGraphTensor *input_tensor;
    MPSGraphTensor *residual_tensor;
    MPSGraphTensor *w1_tensor;
    MPSGraphTensor *b1_tensor;
    MPSGraphTensor *w2_tensor;
    MPSGraphTensor *b2_tensor;
    MPSGraphTensor *ln_w_tensor;
    MPSGraphTensor *ln_b_tensor;
    MPSGraphTensor *output_tensor;
    size_t rows;
    size_t hidden_size;
    size_t intermediate_size;
    uint32_t activation_kind;
} termite_metal_mpsgraph_ffn_cache;

typedef struct termite_metal_provider {
    id<MTLDevice> device;
    id<MTLCommandQueue> queue;
    id<MTLComputePipelineState> q4_0_pipeline;
    id<MTLComputePipelineState> q4_1_pipeline;
    id<MTLComputePipelineState> q5_0_pipeline;
    id<MTLComputePipelineState> q5_1_pipeline;
    id<MTLComputePipelineState> q8_0_pipeline;
    id<MTLComputePipelineState> q8_1_pipeline;
    id<MTLComputePipelineState> q8_0_mmv_pipeline;
    id<MTLComputePipelineState> q8_0_small_batch_r2_pipeline;
    id<MTLComputePipelineState> q8_0_small_batch_r3_pipeline;
    id<MTLComputePipelineState> q8_0_small_batch_r4_pipeline;
    id<MTLComputePipelineState> q8_0_small_batch_pipeline;
    id<MTLComputePipelineState> q8_0_mm_pipeline;
    id<MTLComputePipelineState> q2_k_pipeline;
    id<MTLComputePipelineState> q3_k_pipeline;
    id<MTLComputePipelineState> q4_k_pipeline;
    id<MTLComputePipelineState> q5_k_pipeline;
    id<MTLComputePipelineState> q6_k_pipeline;
    id<MTLComputePipelineState> q8_k_pipeline;
    id<MTLComputePipelineState> iq4_nl_pipeline;
    id<MTLComputePipelineState> iq4_xs_pipeline;
    id<MTLComputePipelineState> mxfp4_pipeline;
    id<MTLComputePipelineState> i2_s_pipeline;
    id<MTLComputePipelineState> i2_s_pair_pipeline;
    id<MTLComputePipelineState> tl1_pipeline;
    id<MTLComputePipelineState> tl2_pipeline;
    id<MTLComputePipelineState> polar4_key_scores_pipeline;
    id<MTLComputePipelineState> turbo3_key_scores_pipeline;
} termite_metal_provider;

#define TERMITE_METAL_PLANNED_RANGE_CAPACITY 256u

typedef enum termite_metal_planned_range_type {
    TERMITE_METAL_PLANNED_RANGE_READ = 0,
    TERMITE_METAL_PLANNED_RANGE_WRITE = 1,
} termite_metal_planned_range_type;

typedef struct termite_metal_planned_encoder_range {
    id<MTLBuffer> buffer;
    size_t begin;
    size_t end;
    termite_metal_planned_range_type type;
} termite_metal_planned_encoder_range;

typedef struct termite_metal_decode_runtime {
    id<MTLDevice> device;
    id<MTLCommandQueue> queue;
    id<MTLLibrary> library;
    id<MTLLibrary> precise_library;
    id<MTLComputePipelineState> embed_absolute_position_pipeline;
    id<MTLComputePipelineState> embedding_lookup_pipeline;
    id<MTLComputePipelineState> embedding_lookup_bf16_pipeline;
    id<MTLComputePipelineState> q4_0_get_rows_pipeline;
    id<MTLComputePipelineState> q4_0_set_rows_pipeline;
    id<MTLComputePipelineState> q4_0_cpy_q_to_f32_pipeline;
    id<MTLComputePipelineState> q4_0_cpy_f32_to_q_pipeline;
    id<MTLComputePipelineState> q4_1_get_rows_pipeline;
    id<MTLComputePipelineState> q4_1_set_rows_pipeline;
    id<MTLComputePipelineState> q4_1_cpy_q_to_f32_pipeline;
    id<MTLComputePipelineState> q4_1_cpy_f32_to_q_pipeline;
    id<MTLComputePipelineState> q5_0_get_rows_pipeline;
    id<MTLComputePipelineState> q5_0_set_rows_pipeline;
    id<MTLComputePipelineState> q5_0_cpy_q_to_f32_pipeline;
    id<MTLComputePipelineState> q5_0_cpy_f32_to_q_pipeline;
    id<MTLComputePipelineState> q5_1_get_rows_pipeline;
    id<MTLComputePipelineState> q5_1_set_rows_pipeline;
    id<MTLComputePipelineState> q5_1_cpy_q_to_f32_pipeline;
    id<MTLComputePipelineState> q5_1_cpy_f32_to_q_pipeline;
    id<MTLComputePipelineState> q4_k_get_rows_pipeline;
    id<MTLComputePipelineState> q4_k_set_rows_pipeline;
    id<MTLComputePipelineState> q4_k_cpy_q_to_f32_pipeline;
    id<MTLComputePipelineState> q4_k_cpy_f32_to_q_pipeline;
    id<MTLComputePipelineState> q5_k_get_rows_pipeline;
    id<MTLComputePipelineState> q5_k_set_rows_pipeline;
    id<MTLComputePipelineState> q5_k_cpy_q_to_f32_pipeline;
    id<MTLComputePipelineState> q5_k_cpy_f32_to_q_pipeline;
    id<MTLComputePipelineState> q6_k_get_rows_pipeline;
    id<MTLComputePipelineState> q6_k_set_rows_pipeline;
    id<MTLComputePipelineState> q6_k_cpy_q_to_f32_pipeline;
    id<MTLComputePipelineState> q6_k_cpy_f32_to_q_pipeline;
    id<MTLComputePipelineState> q8_0_get_rows_pipeline;
    id<MTLComputePipelineState> q8_0_set_rows_pipeline;
    id<MTLComputePipelineState> q8_0_cpy_q_to_f32_pipeline;
    id<MTLComputePipelineState> q8_0_cpy_f32_to_q_pipeline;
    id<MTLComputePipelineState> q8_1_get_rows_pipeline;
    id<MTLComputePipelineState> q8_1_set_rows_pipeline;
    id<MTLComputePipelineState> q8_1_cpy_q_to_f32_pipeline;
    id<MTLComputePipelineState> q8_1_cpy_f32_to_q_pipeline;
    id<MTLComputePipelineState> rope_pipeline;
    id<MTLComputePipelineState> head_rms_rope_pipeline;
    id<MTLComputePipelineState> attention_f32_pipeline;
    id<MTLComputePipelineState> attention_f32_prefill_pipeline;
    id<MTLComputePipelineState> attention_paged_pipeline;
    id<MTLComputePipelineState> compressed_attention_store_local_pipeline;
    id<MTLComputePipelineState> compressed_attention_store_component_pipeline;
    id<MTLComputePipelineState> compressed_attention_update_component_pipeline;
    id<MTLComputePipelineState> compressed_attention_select_compressed_pipeline;
    id<MTLComputePipelineState> compressed_attention_hybrid_attention_pipeline;
    id<MTLComputePipelineState> paged_f32_kv_seed_pipeline;
    id<MTLComputePipelineState> paged_f16_kv_seed_pipeline;
    id<MTLComputePipelineState> paged_f32_v_seed_pipeline;
    id<MTLComputePipelineState> slice_last_dim_f32_2d_pipeline;
    id<MTLComputePipelineState> gather_axis0_f32_2d_pipeline;
    id<MTLComputePipelineState> concat_lastdim_f32_2d_pipeline;
    id<MTLComputePipelineState> deberta_embeddings_f32_pipeline;
    id<MTLComputePipelineState> gliner_word_embeddings_f32_pipeline;
    id<MTLComputePipelineState> gliner_gru_combine_f32_pipeline;
    id<MTLComputePipelineState> argmax_axis_f32_pipeline;
    id<MTLComputePipelineState> convert_dtype_f32_pipeline;
    id<MTLComputePipelineState> sdpa_f32_pipeline;
    id<MTLComputePipelineState> disentangled_relative_attention_f32_pipeline;
    id<MTLComputePipelineState> disentangled_relative_attention_f32_tg_pipeline;
    id<MTLComputePipelineState> disentangled_relative_attention_f32_flash4_pipeline;
    id<MTLComputePipelineState> deberta_relative_score_gemm_f32_pipeline;
    id<MTLComputePipelineState> deberta_relative_score_softmax_pv_f32_pipeline;
    id<MTLComputePipelineState> transpose_f32_pipeline;
    id<MTLComputePipelineState> dot_general_2d_f32_pipeline;
    id<MTLComputePipelineState> dot_general_batched_f32_pipeline;
    id<MTLComputePipelineState> conv1d_f32_pipeline;
    id<MTLComputePipelineState> conv2d_f32_pipeline;
    id<MTLComputePipelineState> layer_norm_pipeline;
    id<MTLComputePipelineState> layer_norm_rows_pipeline;
    id<MTLComputePipelineState> layer_norm_add_pipeline;
    id<MTLComputePipelineState> layer_norm_add_rows_pipeline;
    id<MTLComputePipelineState> bias_activation_pipeline;
    id<MTLComputePipelineState> bias_add_layer_norm_pipeline;
    id<MTLComputePipelineState> bias_add_layer_norm_rows_pipeline;
    id<MTLComputePipelineState> dense_qkv_split_pipeline;
    id<MTLComputePipelineState> dense_pair_split_pipeline;
    id<MTLComputePipelineState> dense_qkv_split_bias_pipeline;
    id<MTLComputePipelineState> dense_pair_split_bias_pipeline;
    id<MTLComputePipelineState> dense_qkv_direct_pipeline;
    id<MTLComputePipelineState> dense_pair_direct_pipeline;
    id<MTLComputePipelineState> rms_norm_pipeline;
    id<MTLComputePipelineState> rms_norm_reduce_pipeline;
    id<MTLComputePipelineState> rms_inv_scale_pipeline;
    id<MTLComputePipelineState> rms_norm_rows_pipeline;
    id<MTLComputePipelineState> rms_norm_add_pipeline;
    id<MTLComputePipelineState> rms_norm_add_scale_pipeline;
    id<MTLComputePipelineState> rms_norm_add_scale_rows_pipeline;
    id<MTLComputePipelineState> linear_pipeline;
    id<MTLComputePipelineState> linear_reduce_pipeline;
    id<MTLComputePipelineState> linear_bf16_pipeline;
    id<MTLComputePipelineState> linear_bf16_reduce_pipeline;
    id<MTLComputePipelineState> linear_bf16_multi_row_pipeline;
    id<MTLComputePipelineState> linear_bf16_multi_row_reduce_pipeline;
    id<MTLComputePipelineState> linear_bf16_multi_row_shared_reduce_pipeline;
    id<MTLComputePipelineState> linear_pair_reduce_pipeline;
    id<MTLComputePipelineState> linear_multi_row_pipeline;
    id<MTLComputePipelineState> linear_multi_row_reduce_pipeline;
    id<MTLComputePipelineState> linear_multi_row_tiled_f32_pipeline;
    id<MTLComputePipelineState> linear_multi_row_tiled_activation_f32_pipeline;
    id<MTLComputePipelineState> linear_multi_row_tiled32_f32_pipeline;
    id<MTLComputePipelineState> linear_multi_row_tiled32_activation_f32_pipeline;
    id<MTLComputePipelineState> linear_multi_row_tiled32_m16_f32_pipeline;
    id<MTLComputePipelineState> linear_multi_row_tiled32_m16_activation_f32_pipeline;
    id<MTLComputePipelineState> linear_bias_pipeline;
    id<MTLComputePipelineState> argmax_logits_pipeline;
    id<MTLComputePipelineState> argmax_logits_partials_pipeline;
    id<MTLComputePipelineState> argmax_logits_reduce_pipeline;
    id<MTLComputePipelineState> sample_logits_pipeline;
    id<MTLComputePipelineState> sample_topk_partials_pipeline;
    id<MTLComputePipelineState> sample_topk_reduce_pipeline;
    id<MTLComputePipelineState> activation_pipeline;
    id<MTLComputePipelineState> activation_multiply_pipeline;
    id<MTLComputePipelineState> softmax_pipeline;
    id<MTLComputePipelineState> reduce_last_dim_pipeline;
    id<MTLComputePipelineState> reduce_axis_f32_pipeline;
    id<MTLComputePipelineState> multiply_reduce_last_dim_pipeline;
    id<MTLComputePipelineState> broadcast_last_dim_pipeline;
    id<MTLComputePipelineState> broadcast_f32_pipeline;
    id<MTLComputePipelineState> multiply_pipeline;
    id<MTLComputePipelineState> scale_pipeline;
    id<MTLComputePipelineState> add_pipeline;
    id<MTLComputePipelineState> add_scale_pipeline;
    id<MTLComputePipelineState> scaled_add_scale_pipeline;
    id<MTLComputePipelineState> training_accumulate_pipeline;
    id<MTLComputePipelineState> training_adamw_pipeline;
    id<MTLComputePipelineState> training_sumsq_pipeline;
    id<MTLComputePipelineState> subtract_pipeline;
    id<MTLComputePipelineState> divide_pipeline;
    id<MTLComputePipelineState> less_than_pipeline;
    id<MTLComputePipelineState> where_select_pipeline;
    id<MTLComputePipelineState> i2_s_quantize_pipeline;
    id<MTLComputePipelineState> q1_0_pipeline;
    id<MTLComputePipelineState> i8_s_pipeline;
    id<MTLComputePipelineState> q2_k_pipeline;
    id<MTLComputePipelineState> q3_k_pipeline;
    id<MTLComputePipelineState> q4_k_pipeline;
    id<MTLComputePipelineState> q4_k_reduce_pipeline;
    id<MTLComputePipelineState> q4_k_pair_pipeline;
    id<MTLComputePipelineState> q4_0_pipeline;
    id<MTLComputePipelineState> q4_0_pair_pipeline;
    id<MTLComputePipelineState> q4_0_reduce_pipeline;
    id<MTLComputePipelineState> q4_1_pipeline;
    id<MTLComputePipelineState> q4_1_reduce_pipeline;
    id<MTLComputePipelineState> q5_0_pipeline;
    id<MTLComputePipelineState> q5_0_reduce_pipeline;
    id<MTLComputePipelineState> q5_1_pipeline;
    id<MTLComputePipelineState> q5_1_reduce_pipeline;
    id<MTLComputePipelineState> q8_0_pipeline;
    id<MTLComputePipelineState> q8_0_pair_pipeline;
    id<MTLComputePipelineState> q8_0_mmv_pipeline;
    id<MTLComputePipelineState> q8_0_mmv_f16_input_pipeline;
    id<MTLComputePipelineState> q8_0_rms_scale_mmv_pipeline;
    id<MTLComputePipelineState> q8_0_small_batch_r2_pipeline;
    id<MTLComputePipelineState> q8_0_small_batch_r3_pipeline;
    id<MTLComputePipelineState> q8_0_small_batch_r4_pipeline;
    id<MTLComputePipelineState> q8_0_small_batch_pipeline;
    id<MTLComputePipelineState> q8_0_mm_pipeline;
    id<MTLComputePipelineState> q8_0_mm_f16_input_pipeline;
    id<MTLComputePipelineState> q8_0_mm_sg_pipeline;
    id<MTLComputePipelineState> q8_0_pair_mmv_pipeline;
    id<MTLComputePipelineState> q8_0_pair_small_batch_r2_pipeline;
    id<MTLComputePipelineState> q8_0_pair_small_batch_r3_pipeline;
    id<MTLComputePipelineState> q8_0_pair_small_batch_r4_pipeline;
    id<MTLComputePipelineState> q8_0_pair_small_batch_pipeline;
    id<MTLComputePipelineState> q8_0_qkv_mmv_pipeline;
    id<MTLComputePipelineState> q8_0_pair_activation_reduce_pipeline;
    id<MTLComputePipelineState> q8_0_pair_activation_mmv_pipeline;
    id<MTLComputePipelineState> q8_0_pair_activation_rms_scale_mmv_pipeline;
    id<MTLComputePipelineState> q8_0_pair_activation_rms_scale_mmv_f16_output_pipeline;
    id<MTLComputePipelineState> q8_0_pair_activation_small_batch_r2_pipeline;
    id<MTLComputePipelineState> q8_0_pair_activation_small_batch_r3_pipeline;
    id<MTLComputePipelineState> q8_0_pair_activation_small_batch_r4_pipeline;
    id<MTLComputePipelineState> q8_0_pair_activation_small_batch_pipeline;
    id<MTLComputePipelineState> q8_0_pair_activation_mm_pipeline;
    id<MTLComputePipelineState> q8_0_pair_activation_mm_f16_output_pipeline;
    id<MTLComputePipelineState> q8_0_activation_multiply_reduce_pipeline;
    id<MTLComputePipelineState> q8_0_activation_multiply_mmv_pipeline;
    id<MTLComputePipelineState> q8_1_pipeline;
    id<MTLComputePipelineState> q8_1_reduce_pipeline;
    id<MTLComputePipelineState> q5_k_pipeline;
    id<MTLComputePipelineState> q5_k_reduce_pipeline;
    id<MTLComputePipelineState> q6_k_pipeline;
    id<MTLComputePipelineState> q6_k_reduce_pipeline;
    id<MTLComputePipelineState> q6_k_pair_pipeline;
    id<MTLComputePipelineState> q8_k_pipeline;
    id<MTLComputePipelineState> q8_k_reduce_pipeline;
    id<MTLComputePipelineState> iq4_nl_pipeline;
    id<MTLComputePipelineState> iq4_nl_reduce_pipeline;
    id<MTLComputePipelineState> iq4_xs_pipeline;
    id<MTLComputePipelineState> iq4_xs_reduce_pipeline;
    id<MTLComputePipelineState> mxfp4_pipeline;
    id<MTLComputePipelineState> mxfp4_reduce_pipeline;
    id<MTLComputePipelineState> nvfp4_pipeline;
    id<MTLComputePipelineState> nvfp4_reduce_pipeline;
    id<MTLComputePipelineState> iq2_xs_pipeline;
    id<MTLComputePipelineState> iq2_xs_reduce_pipeline;
    id<MTLComputePipelineState> i2_s_pipeline;
    id<MTLComputePipelineState> i2_s_pair_pipeline;
    id<MTLComputePipelineState> i2_s_linear_i8_pipeline;
    id<MTLComputePipelineState> i2_s_pair_i8_pipeline;
    id<MTLComputePipelineState> tl1_pipeline;
    id<MTLComputePipelineState> tl2_pipeline;
    id<MTLComputePipelineState> encode_polar4_key_pipeline;
    id<MTLComputePipelineState> encode_turbo3_key_pipeline;
    id<MTLComputePipelineState> encode_f16_convert_pipeline;
    id<MTLComputePipelineState> encode_int8_per_head_pipeline;
    id<MTLComputePipelineState> polar4_attention_span_pipeline;
    id<MTLComputePipelineState> turbo3_attention_span_pipeline;
    id<MTLBuffer> token_embedding_table_buffer;
    id<MTLBuffer> position_embedding_table_buffer;
    id<MTLBuffer> generic_embedding_table_buffer;
    termite_metal_embedding_table_cache_entry generic_embedding_cache[TERMITE_METAL_GENERIC_EMBEDDING_CACHE_CAPACITY];
    id<MTLBuffer> layer_norm_weight_buffers[TERMITE_METAL_LAYER_NORM_SLOT_CAPACITY];
    id<MTLBuffer> layer_norm_bias_buffers[TERMITE_METAL_LAYER_NORM_SLOT_CAPACITY];
    size_t layer_norm_hidden_sizes[TERMITE_METAL_LAYER_NORM_SLOT_CAPACITY];
    uint8_t layer_norm_slot_prepared[TERMITE_METAL_LAYER_NORM_SLOT_CAPACITY];
    id<MTLBuffer> rms_norm_weight_buffers[TERMITE_METAL_RMS_NORM_SLOT_CAPACITY];
    size_t rms_norm_hidden_sizes[TERMITE_METAL_RMS_NORM_SLOT_CAPACITY];
    uint8_t rms_norm_slot_prepared[TERMITE_METAL_RMS_NORM_SLOT_CAPACITY];
    id<MTLBuffer> linear_weight_buffers[TERMITE_METAL_LINEAR_SLOT_CAPACITY];
    id<MTLBuffer> linear_bias_buffers[TERMITE_METAL_LINEAR_SLOT_CAPACITY];
    MPSMatrixMultiplication *linear_mps_mm[TERMITE_METAL_LINEAR_SLOT_CAPACITY];
    size_t linear_mps_mm_rows[TERMITE_METAL_LINEAR_SLOT_CAPACITY];
    id<MTLBuffer> dense_qkv_packed_weight_buffers[TERMITE_METAL_DENSE_QKV_PACK_CACHE_CAPACITY];
    id<MTLBuffer> dense_qkv_packed_bias_buffers[TERMITE_METAL_DENSE_QKV_PACK_CACHE_CAPACITY];
    MPSMatrixMultiplication *dense_qkv_packed_mps_mm[TERMITE_METAL_DENSE_QKV_PACK_CACHE_CAPACITY];
    size_t dense_qkv_packed_k_slots[TERMITE_METAL_DENSE_QKV_PACK_CACHE_CAPACITY];
    size_t dense_qkv_packed_v_slots[TERMITE_METAL_DENSE_QKV_PACK_CACHE_CAPACITY];
    size_t dense_qkv_packed_in_dims[TERMITE_METAL_DENSE_QKV_PACK_CACHE_CAPACITY];
    size_t dense_qkv_packed_q_out_dims[TERMITE_METAL_DENSE_QKV_PACK_CACHE_CAPACITY];
    size_t dense_qkv_packed_kv_out_dims[TERMITE_METAL_DENSE_QKV_PACK_CACHE_CAPACITY];
    size_t dense_qkv_packed_mps_rows[TERMITE_METAL_DENSE_QKV_PACK_CACHE_CAPACITY];
    id<MTLBuffer> q8_qkv_packed_weight_buffers[TERMITE_METAL_DENSE_QKV_PACK_CACHE_CAPACITY];
    size_t q8_qkv_packed_k_slots[TERMITE_METAL_DENSE_QKV_PACK_CACHE_CAPACITY];
    size_t q8_qkv_packed_v_slots[TERMITE_METAL_DENSE_QKV_PACK_CACHE_CAPACITY];
    size_t q8_qkv_packed_in_dims[TERMITE_METAL_DENSE_QKV_PACK_CACHE_CAPACITY];
    size_t q8_qkv_packed_q_out_dims[TERMITE_METAL_DENSE_QKV_PACK_CACHE_CAPACITY];
    size_t q8_qkv_packed_kv_out_dims[TERMITE_METAL_DENSE_QKV_PACK_CACHE_CAPACITY];
    size_t q8_qkv_packed_row_stride_bytes[TERMITE_METAL_DENSE_QKV_PACK_CACHE_CAPACITY];
    id<MTLBuffer> dense_pair_packed_weight_buffers[TERMITE_METAL_LINEAR_SLOT_CAPACITY];
    id<MTLBuffer> dense_pair_packed_bias_buffers[TERMITE_METAL_LINEAR_SLOT_CAPACITY];
    MPSMatrixMultiplication *dense_pair_packed_mps_mm[TERMITE_METAL_LINEAR_SLOT_CAPACITY];
    size_t dense_pair_packed_b_slots[TERMITE_METAL_LINEAR_SLOT_CAPACITY];
    size_t dense_pair_packed_in_dims[TERMITE_METAL_LINEAR_SLOT_CAPACITY];
    size_t dense_pair_packed_out_dims[TERMITE_METAL_LINEAR_SLOT_CAPACITY];
    size_t dense_pair_packed_mps_rows[TERMITE_METAL_LINEAR_SLOT_CAPACITY];
    size_t linear_in_dims[TERMITE_METAL_LINEAR_SLOT_CAPACITY];
    size_t linear_out_dims[TERMITE_METAL_LINEAR_SLOT_CAPACITY];
    uint8_t linear_slot_prepared[TERMITE_METAL_LINEAR_SLOT_CAPACITY];
    uint8_t linear_weight_dtypes[TERMITE_METAL_LINEAR_SLOT_CAPACITY];
    uint32_t quant_linear_formats[TERMITE_METAL_LINEAR_SLOT_CAPACITY];
    id<MTLBuffer> quant_linear_weight_buffers[TERMITE_METAL_LINEAR_SLOT_CAPACITY];
    size_t quant_linear_in_dims[TERMITE_METAL_LINEAR_SLOT_CAPACITY];
    size_t quant_linear_out_dims[TERMITE_METAL_LINEAR_SLOT_CAPACITY];
    uint8_t quant_linear_slot_prepared[TERMITE_METAL_LINEAR_SLOT_CAPACITY];
    size_t quant_linear_values_per_block[TERMITE_METAL_LINEAR_SLOT_CAPACITY];
    size_t quant_linear_bytes_per_block[TERMITE_METAL_LINEAR_SLOT_CAPACITY];
    size_t quant_linear_row_blocks[TERMITE_METAL_LINEAR_SLOT_CAPACITY];
    size_t quant_linear_row_stride_bytes[TERMITE_METAL_LINEAR_SLOT_CAPACITY];
    uint32_t quant_linear_tl1_packed_len[TERMITE_METAL_LINEAR_SLOT_CAPACITY];
    uint32_t quant_linear_tl1_bm[TERMITE_METAL_LINEAR_SLOT_CAPACITY];
    uint32_t quant_linear_tl1_cfg_by[TERMITE_METAL_LINEAR_SLOT_CAPACITY];
    uint32_t quant_linear_tl1_bmm[TERMITE_METAL_LINEAR_SLOT_CAPACITY];
    uint32_t quant_linear_tl2_scale_off[TERMITE_METAL_LINEAR_SLOT_CAPACITY];
    uint32_t quant_linear_tl2_three_value_len[TERMITE_METAL_LINEAR_SLOT_CAPACITY];
    uint32_t quant_linear_tl2_three_sign_len[TERMITE_METAL_LINEAR_SLOT_CAPACITY];
    uint32_t quant_linear_tl2_bm[TERMITE_METAL_LINEAR_SLOT_CAPACITY];
    uint32_t quant_linear_tl2_cfg_by[TERMITE_METAL_LINEAR_SLOT_CAPACITY];
    uint32_t quant_linear_tl2_bmm[TERMITE_METAL_LINEAR_SLOT_CAPACITY];
    uint32_t quant_linear_tl2_three_cols[TERMITE_METAL_LINEAR_SLOT_CAPACITY];
    uint32_t quant_linear_tl2_two_cols[TERMITE_METAL_LINEAR_SLOT_CAPACITY];
    id<MTLBuffer> scratch_buffer;
    id<MTLBuffer> host_staging_buffer;
    id<MTLBuffer> active_frame_host_staging_buffer;
    id<MTLBuffer> active_frame_host_staging_retained_buffer;
    id<MTLBuffer> token_buffer;
    id<MTLBuffer> sample_logits_buffer;
    id<MTLBuffer> sample_topk_values_buffer;
    id<MTLBuffer> sample_topk_ids_buffer;
    id<MTLBuffer> hot_hidden_buffers[4];
    id<MTLBuffer> hot_intermediate_buffers[5];
    id<MTLBuffer> direct_block_hidden_buffers[TERMITE_METAL_DIRECT_BLOCK_HIDDEN_BUFFER_COUNT];
    id<MTLBuffer> direct_block_shared_buffers[TERMITE_METAL_DIRECT_BLOCK_SHARED_BUFFER_COUNT];
    id<MTLBuffer> direct_block_q_buffer;
    id<MTLBuffer> active_layer_projection_buffers[6];
    id<MTLBuffer> i2s_qinput_buffer;
    id<MTLBuffer> i2s_scale_buffer;
    id<MTLBuffer> attention_span_encoded_key_buffer;
    id<MTLBuffer> attention_span_v_buffer;
    id<MTLBuffer> attention_span_encoded_key_buffers[TERMITE_METAL_ATTENTION_SPAN_SLOT_CAPACITY];
    id<MTLBuffer> attention_span_v_buffers[TERMITE_METAL_ATTENTION_SPAN_SLOT_CAPACITY];
    size_t token_embedding_table_bytes;
    size_t position_embedding_table_bytes;
    size_t generic_embedding_table_bytes;
    size_t resident_vocab_size;
    size_t resident_max_position_embeddings;
    size_t resident_hidden_size;
    size_t generic_embedding_rows;
    size_t generic_embedding_dim;
    uintptr_t generic_embedding_source_id;
    size_t generic_embedding_source_offset;
    size_t generic_embedding_cache_next;
    size_t scratch_capacity;
    size_t host_staging_capacity;
    size_t active_frame_host_staging_capacity;
    size_t active_frame_host_staging_offset;
    size_t token_capacity;
    size_t sample_logits_capacity;
    size_t sample_topk_capacity;
    size_t hot_hidden_capacity;
    size_t hot_intermediate_capacity;
    size_t direct_block_hidden_capacity;
    size_t direct_block_shared_capacities[TERMITE_METAL_DIRECT_BLOCK_SHARED_BUFFER_COUNT];
    size_t direct_block_q_capacity;
    size_t active_layer_projection_capacities[6];
    size_t i2s_qinput_capacity;
    size_t i2s_scale_capacity;
    size_t attention_span_encoded_key_capacity;
    size_t attention_span_v_capacity;
    size_t attention_span_tokens;
    size_t attention_span_key_row_bytes;
    size_t attention_span_v_row_stride;
    size_t attention_span_position_offset;
    size_t attention_span_encoded_key_capacities[TERMITE_METAL_ATTENTION_SPAN_SLOT_CAPACITY];
    size_t attention_span_v_capacities[TERMITE_METAL_ATTENTION_SPAN_SLOT_CAPACITY];
    size_t attention_span_slot_tokens[TERMITE_METAL_ATTENTION_SPAN_SLOT_CAPACITY];
    size_t attention_span_slot_key_row_bytes[TERMITE_METAL_ATTENTION_SPAN_SLOT_CAPACITY];
    size_t attention_span_slot_v_row_stride[TERMITE_METAL_ATTENTION_SPAN_SLOT_CAPACITY];
    size_t attention_span_slot_position_offset[TERMITE_METAL_ATTENTION_SPAN_SLOT_CAPACITY];
    // Phase 2 — activation scratch pool (Private storage). Without an active
    // frame, release returns the slot to the pool immediately. With an active
    // or submitted frame, release retires the slot until the frame finishes so
    // later encodes cannot overwrite a buffer that queued GPU work still reads.
    id<MTLBuffer> scratch_pool_buffers[TERMITE_METAL_SCRATCH_POOL_CAPACITY];
    size_t scratch_pool_capacities[TERMITE_METAL_SCRATCH_POOL_CAPACITY];
    uint8_t scratch_pool_in_use[TERMITE_METAL_SCRATCH_POOL_CAPACITY];
    uint8_t scratch_pool_pending_frame_release[TERMITE_METAL_SCRATCH_POOL_CAPACITY];
    // Phase 2 — device-side hidden-state ping-pong buffers. Sized to hold
    // `max_prefill_rows * hidden_size * sizeof(f32)` so the same pair serves
    // both single-token decode (rows=1) and batched prefill (rows=chunk).
    id<MTLBuffer> hidden_state_front;
    id<MTLBuffer> hidden_state_back;
    size_t hidden_state_capacity;
    size_t hidden_state_max_rows;
    size_t hidden_state_hidden_size;
    // Phase 4 — active frame command buffer. When non-nil, device-op
    // entrypoints encode into this shared CB and skip commit/wait, so
    // multiple ops batch into a single submission. Set by
    // `begin_frame`, committed by `submit_frame`, waited and cleared by
    // `wait_frame`. A post-commit CB remains assigned so `wait_frame`
    // can await it; `submitted_frame_cb` holds the committed CB during
    // that window.
    id<MTLCommandBuffer> active_frame_cb;
    id<MTLCommandBuffer> submitted_frame_cb;
    CFMutableArrayRef active_frame_retained_resources;
    CFMutableArrayRef submitted_frame_retained_resources;
    uint64_t frame_cb_count;
    uint64_t last_frame_gpu_nanos;
    uint64_t compute_encoder_count;
    uint64_t blit_encoder_count;
    uint64_t active_frame_compute_encoder_count;
    uint64_t active_frame_blit_encoder_count;
    uint64_t active_frame_planned_compute_scope_count;
    uint64_t active_frame_planned_barrier_count;
    uint64_t active_frame_mps_dense_linear_count;
    uint64_t submitted_frame_compute_encoder_count;
    uint64_t submitted_frame_blit_encoder_count;
    uint64_t submitted_frame_planned_compute_scope_count;
    uint64_t submitted_frame_planned_barrier_count;
    uint64_t submitted_frame_mps_dense_linear_count;
    uint64_t last_frame_compute_encoder_count;
    uint64_t last_frame_blit_encoder_count;
    uint64_t last_frame_planned_compute_scope_count;
    uint64_t last_frame_planned_barrier_count;
    uint64_t last_frame_mps_dense_linear_count;
    uint64_t active_frame_planned_command_op_count;
    uint64_t submitted_frame_planned_command_op_count;
    uint64_t last_frame_planned_command_op_count;
    id<MTLComputeCommandEncoder> active_planned_compute_encoder;
    bool active_planned_compute_scope_closed_for_encoder_transition;
    uint32_t planned_compute_barrier_suppression_depth;
    size_t active_planned_compute_source;
    size_t active_planned_compute_region;
    termite_metal_planned_encoder_range active_planned_compute_ranges[TERMITE_METAL_PLANNED_RANGE_CAPACITY];
    size_t active_planned_compute_range_count;
    uint64_t active_frame_compute_source_counts[TERMITE_METAL_COMPUTE_COUNTER_COUNT];
    uint64_t submitted_frame_compute_source_counts[TERMITE_METAL_COMPUTE_COUNTER_COUNT];
    uint64_t last_frame_compute_source_counts[TERMITE_METAL_COMPUTE_COUNTER_COUNT];
    size_t active_compute_region;
    uint64_t active_frame_compute_region_counts[TERMITE_METAL_COMPUTE_REGION_COUNTER_COUNT];
    uint64_t submitted_frame_compute_region_counts[TERMITE_METAL_COMPUTE_REGION_COUNTER_COUNT];
    uint64_t last_frame_compute_region_counts[TERMITE_METAL_COMPUTE_REGION_COUNTER_COUNT];
    uint64_t active_frame_compute_region_source_counts[TERMITE_METAL_COMPUTE_REGION_COUNTER_COUNT][TERMITE_METAL_COMPUTE_COUNTER_COUNT];
    uint64_t submitted_frame_compute_region_source_counts[TERMITE_METAL_COMPUTE_REGION_COUNTER_COUNT][TERMITE_METAL_COMPUTE_COUNTER_COUNT];
    uint64_t last_frame_compute_region_source_counts[TERMITE_METAL_COMPUTE_REGION_COUNTER_COUNT][TERMITE_METAL_COMPUTE_COUNTER_COUNT];
    uint64_t active_frame_planned_command_op_kind_counts[TERMITE_METAL_PLAN_OP_COUNTER_COUNT];
    uint64_t submitted_frame_planned_command_op_kind_counts[TERMITE_METAL_PLAN_OP_COUNTER_COUNT];
    uint64_t last_frame_planned_command_op_kind_counts[TERMITE_METAL_PLAN_OP_COUNTER_COUNT];
    uint64_t active_frame_planned_command_operator_counts[TERMITE_METAL_OPERATOR_COUNTER_COUNT];
    uint64_t submitted_frame_planned_command_operator_counts[TERMITE_METAL_OPERATOR_COUNTER_COUNT];
    uint64_t last_frame_planned_command_operator_counts[TERMITE_METAL_OPERATOR_COUNTER_COUNT];
    uint64_t active_frame_planned_command_quant_dispatch_counts[TERMITE_METAL_QUANT_DISPATCH_COUNTER_COUNT];
    uint64_t submitted_frame_planned_command_quant_dispatch_counts[TERMITE_METAL_QUANT_DISPATCH_COUNTER_COUNT];
    uint64_t last_frame_planned_command_quant_dispatch_counts[TERMITE_METAL_QUANT_DISPATCH_COUNTER_COUNT];
    uint64_t active_frame_blit_source_counts[TERMITE_METAL_BLIT_COUNTER_COUNT];
    uint64_t submitted_frame_blit_source_counts[TERMITE_METAL_BLIT_COUNTER_COUNT];
    uint64_t last_frame_blit_source_counts[TERMITE_METAL_BLIT_COUNTER_COUNT];
    // Graph-planned scratch slots. These are persistent runtime-owned buffers
    // sized before encoding a frame/layer family, so helpers can borrow stable
    // device storage without allocating or retaining per call.
    id<MTLBuffer> graph_plan_buffers[TERMITE_METAL_GRAPH_PLAN_SLOT_CAPACITY];
    size_t graph_plan_capacities[TERMITE_METAL_GRAPH_PLAN_SLOT_CAPACITY];
    size_t graph_plan_requested_bytes[TERMITE_METAL_GRAPH_PLAN_SLOT_CAPACITY];
    uint8_t graph_plan_active;
    uint64_t graph_plan_count;
    uint64_t graph_plan_allocations;
    uint64_t graph_plan_reuses;
    uint64_t mps_dense_linear_standalone_calls;
    uint64_t mps_dense_linear_active_frame_calls;
    uint64_t mps_dense_linear_standalone_wait_nanos;
    uint64_t mps_dense_linear_standalone_gpu_nanos;
    uint64_t dense_qkv_packed_calls;
    uint64_t dense_qkv_packed_fallbacks;
    uint64_t dense_pair_packed_calls;
    uint64_t dense_pair_packed_fallbacks;
    uint64_t deberta_ffn_fused_calls;
    uint64_t deberta_ffn_fused_mps_matmuls;
    uint64_t deberta_ffn_fused_fallbacks;
    uint64_t deberta_attention_flash_calls;
    uint64_t deberta_attention_legacy_calls;
    uint64_t deberta_attention_gemm_calls;
    uint64_t deberta_attention_gemm_fallbacks;
    termite_metal_mpsgraph_ffn_cache mpsgraph_ffn_cache;
    uint64_t mpsgraph_ffn_calls;
    uint64_t mpsgraph_ffn_fallbacks;
    uint64_t mpsgraph_ffn_compiles;
    uint64_t mpsgraph_ffn_cache_hits;
    uint64_t q8_0_linear_dispatch_scalar;
    uint64_t q8_0_linear_dispatch_mmv;
    uint64_t q8_0_linear_dispatch_small_batch;
    uint64_t q8_0_linear_dispatch_mm;
    uint64_t q8_0_linear_rows_1;
    uint64_t q8_0_linear_rows_2_8;
    uint64_t q8_0_linear_rows_9_64;
    uint64_t q8_0_linear_rows_65_plus;
    uint64_t q8_0_pair_activation_mm_f16_output;
    uint64_t q8_0_linear_mm_f16_input;
    uint64_t q8_0_pair_activation_rms_scale_mmv_f16_output;
    uint64_t q8_0_linear_mmv_f16_input;
    uint64_t q8_0_linear_family_dispatch_counts[TERMITE_METAL_Q8_0_LINEAR_FAMILY_COUNT][TERMITE_METAL_Q8_0_LINEAR_DISPATCH_COUNT];
} termite_metal_decode_runtime;

typedef struct termite_metal_attention_gated_block_timing {
    uint64_t replace_span_nanos;
    uint64_t attention_span_nanos;
    uint64_t attention_prefix_nanos;
    uint64_t gated_ffn_residual_nanos;
    uint64_t command_wait_nanos;
    uint64_t gpu_nanos;
    uint32_t failure_stage;
    int32_t failure_code;
    uint64_t attention_f32_kernels;
    uint64_t q8_0_linear_kernels;
    uint64_t q8_0_attention_linear_kernels;
    uint64_t q8_0_ffn_down_linear_kernels;
    uint64_t q8_0_ple_linear_kernels;
    uint64_t q8_0_pair_activation_kernels;
    uint64_t rms_norm_kernels;
    uint64_t rms_norm_add_kernels;
    uint64_t layer_norm_kernels;
    uint64_t add_kernels;
    uint64_t blit_copies;
} termite_metal_attention_gated_block_timing;

typedef enum termite_metal_q8_0_linear_dispatch_kind {
    TERMITE_METAL_Q8_0_LINEAR_DISPATCH_SCALAR = 0,
    TERMITE_METAL_Q8_0_LINEAR_DISPATCH_MMV = 1,
    TERMITE_METAL_Q8_0_LINEAR_DISPATCH_SMALL_BATCH = 2,
    TERMITE_METAL_Q8_0_LINEAR_DISPATCH_MM = 3,
} termite_metal_q8_0_linear_dispatch_kind;

typedef enum termite_metal_quant_matmul_dispatch_kind {
    TERMITE_METAL_QUANT_MATMUL_DISPATCH_SCALAR = 0,
    TERMITE_METAL_QUANT_MATMUL_DISPATCH_MMV = 1,
    TERMITE_METAL_QUANT_MATMUL_DISPATCH_SMALL_BATCH = 2,
    TERMITE_METAL_QUANT_MATMUL_DISPATCH_MM = 3,
} termite_metal_quant_matmul_dispatch_kind;

typedef enum termite_metal_quant_matmul_primitive_kind {
    TERMITE_METAL_QUANT_MATMUL_PRIMITIVE_SCALAR = 0,
    TERMITE_METAL_QUANT_MATMUL_PRIMITIVE_QGEMV = 1,
    TERMITE_METAL_QUANT_MATMUL_PRIMITIVE_QGEMV_BATCH = 2,
    TERMITE_METAL_QUANT_MATMUL_PRIMITIVE_QMATMUL = 3,
} termite_metal_quant_matmul_primitive_kind;

typedef enum termite_metal_q8_0_linear_family_kind {
    TERMITE_METAL_Q8_0_LINEAR_FAMILY_NONE = 0,
    TERMITE_METAL_Q8_0_LINEAR_FAMILY_PAIR_ACTIVATION = 1,
    TERMITE_METAL_Q8_0_LINEAR_FAMILY_PAIR_ACTIVATION_RMS = 2,
    TERMITE_METAL_Q8_0_LINEAR_FAMILY_ACTIVATION_RHS = 3,
    TERMITE_METAL_Q8_0_LINEAR_FAMILY_PAIR = 4,
    TERMITE_METAL_Q8_0_LINEAR_FAMILY_QKV = 5,
    TERMITE_METAL_Q8_0_LINEAR_FAMILY_RMS_SCALE = 6,
    TERMITE_METAL_Q8_0_LINEAR_FAMILY_ATTENTION_OUTPUT = 7,
    TERMITE_METAL_Q8_0_LINEAR_FAMILY_FFN_DOWN = 8,
    TERMITE_METAL_Q8_0_LINEAR_FAMILY_PLE_PROJECTION = 9,
    TERMITE_METAL_Q8_0_LINEAR_FAMILY_TAIL = 10,
    TERMITE_METAL_Q8_0_LINEAR_FAMILY_PLE_GATE = 11,
} termite_metal_q8_0_linear_family_kind;

#define TERMITE_METAL_QUANT_MATMUL_DISPATCH_UNKNOWN 255u

typedef enum termite_metal_blit_source_kind {
    TERMITE_METAL_BLIT_SOURCE_BUFFER_UPLOAD = 0,
    TERMITE_METAL_BLIT_SOURCE_BUFFER_COPY = 1,
    TERMITE_METAL_BLIT_SOURCE_BUFFER_SLICE = 2,
    TERMITE_METAL_BLIT_SOURCE_ATTENTION_SPAN = 3,
    TERMITE_METAL_BLIT_SOURCE_FFN_COPY = 4,
    TERMITE_METAL_BLIT_SOURCE_EMBEDDING = 5,
    TERMITE_METAL_BLIT_SOURCE_OTHER = 6,
} termite_metal_blit_source_kind;

typedef enum termite_metal_compute_source_kind {
    TERMITE_METAL_COMPUTE_SOURCE_QUANT_LINEAR = 0,
    TERMITE_METAL_COMPUTE_SOURCE_QUANT_QKV = 1,
    TERMITE_METAL_COMPUTE_SOURCE_QUANT_PAIR_ACT = 2,
    TERMITE_METAL_COMPUTE_SOURCE_ATTENTION = 3,
    TERMITE_METAL_COMPUTE_SOURCE_RMS_NORM = 4,
    TERMITE_METAL_COMPUTE_SOURCE_HEAD_ROPE = 5,
    TERMITE_METAL_COMPUTE_SOURCE_FFN = 6,
    TERMITE_METAL_COMPUTE_SOURCE_PLE = 7,
    TERMITE_METAL_COMPUTE_SOURCE_TAIL = 8,
    TERMITE_METAL_COMPUTE_SOURCE_EMBEDDING = 9,
    TERMITE_METAL_COMPUTE_SOURCE_DENSE_LINEAR = 10,
    TERMITE_METAL_COMPUTE_SOURCE_LAYER = 11,
    TERMITE_METAL_COMPUTE_SOURCE_OTHER = 12,
} termite_metal_compute_source_kind;

typedef enum termite_metal_compute_region_kind {
    TERMITE_METAL_COMPUTE_REGION_ATTENTION = 0,
    TERMITE_METAL_COMPUTE_REGION_ATTENTION_PROJECT = 1,
    TERMITE_METAL_COMPUTE_REGION_FFN_NORM = 2,
    TERMITE_METAL_COMPUTE_REGION_FFN = 3,
    TERMITE_METAL_COMPUTE_REGION_PLE = 4,
    TERMITE_METAL_COMPUTE_REGION_TAIL = 5,
    TERMITE_METAL_COMPUTE_REGION_EMBEDDING = 6,
    TERMITE_METAL_COMPUTE_REGION_LAYER = 7,
    TERMITE_METAL_COMPUTE_REGION_OTHER = 8,
} termite_metal_compute_region_kind;

typedef struct termite_metal_decode_runtime_memory_stats {
    uint64_t buffer_count;
    uint64_t total_bytes;
    uint64_t embedding_bytes;
    uint64_t norm_bytes;
    uint64_t dense_linear_bytes;
    uint64_t dense_linear_buffer_count;
    uint64_t dense_linear_largest_slot;
    uint64_t dense_linear_largest_bytes;
    uint64_t dense_linear_largest_in_dim;
    uint64_t dense_linear_largest_out_dim;
    uint64_t dense_linear_weight_bytes;
    uint64_t dense_linear_f32_weight_bytes;
    uint64_t dense_linear_bf16_weight_bytes;
    uint64_t dense_linear_f32_slots;
    uint64_t dense_linear_bf16_slots;
    uint64_t quant_linear_bytes;
    uint64_t scratch_bytes;
    uint64_t scratch_pool_bytes;
    uint64_t scratch_pool_slots;
    uint64_t scratch_pool_in_use_slots;
    uint64_t scratch_pool_pending_slots;
    uint64_t attention_span_bytes;
    uint64_t hidden_state_bytes;
    uint64_t frame_retained_bytes;
    uint64_t graph_plan_bytes;
    uint64_t graph_plan_slots;
    uint64_t graph_plan_active;
    uint64_t graph_plan_count;
    uint64_t graph_plan_allocations;
    uint64_t graph_plan_reuses;
    uint64_t mps_dense_linear_standalone_calls;
    uint64_t mps_dense_linear_active_frame_calls;
    uint64_t mps_dense_linear_standalone_wait_nanos;
    uint64_t mps_dense_linear_standalone_gpu_nanos;
    uint64_t last_frame_mps_dense_linear_count;
    uint64_t dense_qkv_packed_calls;
    uint64_t dense_qkv_packed_fallbacks;
    uint64_t dense_pair_packed_calls;
    uint64_t dense_pair_packed_fallbacks;
    uint64_t deberta_ffn_fused_calls;
    uint64_t deberta_ffn_fused_mps_matmuls;
    uint64_t deberta_ffn_fused_fallbacks;
    uint64_t deberta_attention_flash_calls;
    uint64_t deberta_attention_legacy_calls;
    uint64_t deberta_attention_gemm_calls;
    uint64_t deberta_attention_gemm_fallbacks;
    uint64_t mpsgraph_ffn_calls;
    uint64_t mpsgraph_ffn_fallbacks;
    uint64_t mpsgraph_ffn_compiles;
    uint64_t mpsgraph_ffn_cache_hits;
    uint64_t compute_encoder_count;
    uint64_t blit_encoder_count;
    uint64_t last_frame_compute_encoder_count;
    uint64_t last_frame_blit_encoder_count;
    uint64_t last_frame_planned_compute_scope_count;
    uint64_t last_frame_planned_barrier_count;
    uint64_t last_frame_compute_quant_linear_count;
    uint64_t last_frame_compute_quant_qkv_count;
    uint64_t last_frame_compute_quant_pair_act_count;
    uint64_t last_frame_compute_attention_count;
    uint64_t last_frame_compute_rms_norm_count;
    uint64_t last_frame_compute_head_rope_count;
    uint64_t last_frame_compute_ffn_count;
    uint64_t last_frame_compute_ple_count;
    uint64_t last_frame_compute_tail_count;
    uint64_t last_frame_compute_embedding_count;
    uint64_t last_frame_compute_dense_linear_count;
    uint64_t last_frame_compute_layer_count;
    uint64_t last_frame_compute_other_count;
    uint64_t last_frame_compute_region_attention_count;
    uint64_t last_frame_compute_region_attention_project_count;
    uint64_t last_frame_compute_region_ffn_norm_count;
    uint64_t last_frame_compute_region_ffn_count;
    uint64_t last_frame_compute_region_ple_count;
    uint64_t last_frame_compute_region_tail_count;
    uint64_t last_frame_compute_region_embedding_count;
    uint64_t last_frame_compute_region_layer_count;
    uint64_t last_frame_compute_region_other_count;
    uint64_t last_frame_planned_command_op_count;
    uint64_t last_frame_planned_command_op_kind_counts[TERMITE_METAL_PLAN_OP_COUNTER_COUNT];
    uint64_t last_frame_planned_command_operator_counts[TERMITE_METAL_OPERATOR_COUNTER_COUNT];
    uint64_t last_frame_planned_command_quant_dispatch_counts[TERMITE_METAL_QUANT_DISPATCH_COUNTER_COUNT];
    uint64_t last_frame_blit_buffer_upload_count;
    uint64_t last_frame_blit_buffer_copy_count;
    uint64_t last_frame_blit_buffer_slice_count;
    uint64_t last_frame_blit_attention_span_count;
    uint64_t last_frame_blit_ffn_copy_count;
    uint64_t last_frame_blit_embedding_count;
    uint64_t last_frame_blit_other_count;
    uint64_t q8_0_linear_dispatch_scalar;
    uint64_t q8_0_linear_dispatch_mmv;
    uint64_t q8_0_linear_dispatch_small_batch;
    uint64_t q8_0_linear_dispatch_mm;
    uint64_t q8_0_linear_rows_1;
    uint64_t q8_0_linear_rows_2_8;
    uint64_t q8_0_linear_rows_9_64;
    uint64_t q8_0_linear_rows_65_plus;
    uint64_t q8_0_pair_activation_mm_f16_output;
    uint64_t q8_0_linear_mm_f16_input;
    uint64_t q8_0_pair_activation_rms_scale_mmv_f16_output;
    uint64_t q8_0_linear_mmv_f16_input;
    uint64_t q8_0_linear_family_dispatch_counts[TERMITE_METAL_Q8_0_LINEAR_FAMILY_COUNT][TERMITE_METAL_Q8_0_LINEAR_DISPATCH_COUNT];
} termite_metal_decode_runtime_memory_stats;

static NSUInteger termite_metal_thread_width(id<MTLComputePipelineState> pipeline, size_t dim);
static NSUInteger termite_metal_threadgroup_memory_16(NSUInteger bytes);
static uint64_t termite_metal_clock_monotonic_nanos(void);
static id<MTLDevice> termite_metal_shared_device(void);
static bool termite_metal_debug_q80_block_finite(size_t layer_index);
static bool termite_metal_debug_direct_block_finite(size_t layer_index);
static void termite_metal_debug_dump_buffer_finite(termite_metal_decode_runtime *runtime, const char *label, size_t layer_index, id<MTLBuffer> buffer, size_t offset, size_t elems);
static termite_metal_quant_matmul_dispatch_kind termite_metal_select_quant_matmul_dispatch(uint32_t format, size_t rows, size_t in_dim, size_t out_dim, size_t values_per_block);
static termite_metal_quant_matmul_dispatch_kind termite_metal_select_quant_matmul_dispatch_preferred(uint32_t format, size_t rows, size_t in_dim, size_t out_dim, size_t values_per_block, uint8_t preferred_dispatch);
static termite_metal_q8_0_linear_dispatch_kind termite_metal_q8_0_dispatch_from_quant_dispatch(termite_metal_quant_matmul_dispatch_kind dispatch_kind);
static id<MTLComputePipelineState> termite_metal_decode_runtime_quant_get_rows_pipeline(termite_metal_decode_runtime *runtime, uint32_t format);
static id<MTLComputePipelineState> termite_metal_decode_runtime_quant_set_rows_pipeline(termite_metal_decode_runtime *runtime, uint32_t format);
static id<MTLComputePipelineState> termite_metal_decode_runtime_quant_cpy_q_to_f32_pipeline(termite_metal_decode_runtime *runtime, uint32_t format);
static id<MTLComputePipelineState> termite_metal_decode_runtime_quant_cpy_f32_to_q_pipeline(termite_metal_decode_runtime *runtime, uint32_t format);
static void termite_metal_record_q8_0_linear_dispatch(termite_metal_decode_runtime *runtime, termite_metal_q8_0_linear_dispatch_kind dispatch_kind, size_t rows, termite_metal_q8_0_linear_family_kind family);
static id<MTLCommandBuffer> termite_metal_new_command_buffer(id<MTLCommandQueue> queue, const char *label_cstr);
static void termite_metal_log_command_buffer_failure(id<MTLCommandBuffer> command_buffer, const char *context);
static int termite_metal_commit_and_wait(id<MTLCommandBuffer> command_buffer, const char *context, int failure_code);
static const char *termite_metal_compute_source_name(size_t source);
static const char *termite_metal_compute_region_name(size_t region);
static void termite_metal_maybe_trace_last_frame(termite_metal_decode_runtime *runtime);
static const char *termite_metal_dense_linear_variant(bool use_bf16, bool use_shared_row_reduce, bool use_tiled32_m16, bool use_tiled32, bool use_tiled, bool use_reduce, bool activation_fused);
static void termite_metal_maybe_trace_dense_linear_dispatch(const char *site, size_t slot, size_t rows, size_t in_dim, size_t out_dim, const char *variant, bool active_frame, bool planned_encoder, bool packed_weight, bool activation_fused, bool mps);
static int termite_metal_encode_q8_0_linear(termite_metal_decode_runtime *runtime, id<MTLCommandBuffer> command_buffer, id<MTLBuffer> input_buffer, size_t input_offset, id<MTLBuffer> weight_buffer, id<MTLBuffer> output_buffer, size_t output_offset, size_t rows, size_t in_dim, size_t out_dim, int failure_code);
static int termite_metal_encode_q8_0_linear_family(termite_metal_decode_runtime *runtime, id<MTLCommandBuffer> command_buffer, id<MTLBuffer> input_buffer, size_t input_offset, id<MTLBuffer> weight_buffer, id<MTLBuffer> output_buffer, size_t output_offset, size_t rows, size_t in_dim, size_t out_dim, termite_metal_q8_0_linear_family_kind q8_family, int failure_code);
static int termite_metal_encode_q8_0_linear_raw(termite_metal_decode_runtime *runtime, id<MTLCommandBuffer> command_buffer, id<MTLBuffer> input_buffer, size_t input_offset, id<MTLBuffer> weight_buffer, id<MTLBuffer> output_buffer, size_t output_offset, size_t rows, size_t in_dim, size_t out_dim, int failure_code);
static int termite_metal_encode_q8_0_pair_activation_multiply_on_encoder(termite_metal_decode_runtime *runtime, id<MTLComputeCommandEncoder> encoder, id<MTLBuffer> input_buffer, size_t input_offset, id<MTLBuffer> gate_weight_buffer, id<MTLBuffer> up_weight_buffer, id<MTLBuffer> output_buffer, size_t output_offset, size_t rows, size_t in_dim, size_t out_dim, uint32_t activation_kind, int failure_code);
static int termite_metal_encode_q8_0_pair_activation_multiply_on_encoder_planned(termite_metal_decode_runtime *runtime, id<MTLComputeCommandEncoder> encoder, id<MTLBuffer> input_buffer, size_t input_offset, id<MTLBuffer> gate_weight_buffer, id<MTLBuffer> up_weight_buffer, id<MTLBuffer> output_buffer, size_t output_offset, size_t rows, size_t in_dim, size_t out_dim, uint32_t activation_kind, uint8_t planned_dispatch, int failure_code);
static int termite_metal_encode_q8_0_pair_activation_multiply_rms_scale_1x_on_encoder(termite_metal_decode_runtime *runtime, id<MTLComputeCommandEncoder> encoder, id<MTLBuffer> input_buffer, size_t input_offset, size_t norm_slot, id<MTLBuffer> inv_scale_buffer, size_t inv_scale_offset, id<MTLBuffer> gate_weight_buffer, id<MTLBuffer> up_weight_buffer, id<MTLBuffer> output_buffer, size_t output_offset, size_t in_dim, size_t out_dim, uint32_t activation_kind, int failure_code);
static int termite_metal_encode_q8_0_pair_activation_multiply_rms_scale_1x_on_encoder_planned(termite_metal_decode_runtime *runtime, id<MTLComputeCommandEncoder> encoder, id<MTLBuffer> input_buffer, size_t input_offset, size_t norm_slot, id<MTLBuffer> inv_scale_buffer, size_t inv_scale_offset, id<MTLBuffer> gate_weight_buffer, id<MTLBuffer> up_weight_buffer, id<MTLBuffer> output_buffer, size_t output_offset, size_t in_dim, size_t out_dim, uint32_t activation_kind, uint8_t planned_dispatch, int failure_code);
static int termite_metal_encode_q8_0_activation_multiply(termite_metal_decode_runtime *runtime, id<MTLCommandBuffer> command_buffer, id<MTLBuffer> input_buffer, size_t input_offset, id<MTLBuffer> weight_buffer, id<MTLBuffer> rhs_buffer, size_t rhs_offset, id<MTLBuffer> output_buffer, size_t output_offset, size_t rows, size_t in_dim, size_t out_dim, uint32_t activation_kind, int failure_code);
static int termite_metal_encode_q8_0_activation_multiply_on_encoder(termite_metal_decode_runtime *runtime, id<MTLComputeCommandEncoder> encoder, id<MTLBuffer> input_buffer, size_t input_offset, id<MTLBuffer> weight_buffer, id<MTLBuffer> rhs_buffer, size_t rhs_offset, id<MTLBuffer> output_buffer, size_t output_offset, size_t rows, size_t in_dim, size_t out_dim, uint32_t activation_kind, int failure_code);
static int termite_metal_encode_q8_0_activation_multiply_on_encoder_planned(termite_metal_decode_runtime *runtime, id<MTLComputeCommandEncoder> encoder, id<MTLBuffer> input_buffer, size_t input_offset, id<MTLBuffer> weight_buffer, id<MTLBuffer> rhs_buffer, size_t rhs_offset, id<MTLBuffer> output_buffer, size_t output_offset, size_t rows, size_t in_dim, size_t out_dim, uint32_t activation_kind, uint8_t planned_dispatch, int failure_code);
static int termite_metal_encode_q8_0_linear_raw_on_encoder(termite_metal_decode_runtime *runtime, id<MTLComputeCommandEncoder> encoder, id<MTLBuffer> input_buffer, size_t input_offset, id<MTLBuffer> weight_buffer, id<MTLBuffer> output_buffer, size_t output_offset, size_t rows, size_t in_dim, size_t out_dim, int failure_code);
static int termite_metal_encode_q8_0_linear_raw_on_encoder_planned(termite_metal_decode_runtime *runtime, id<MTLComputeCommandEncoder> encoder, id<MTLBuffer> input_buffer, size_t input_offset, id<MTLBuffer> weight_buffer, id<MTLBuffer> output_buffer, size_t output_offset, size_t rows, size_t in_dim, size_t out_dim, uint8_t planned_dispatch, int failure_code);
static int termite_metal_encode_q8_0_linear_raw_on_encoder_planned_family(termite_metal_decode_runtime *runtime, id<MTLComputeCommandEncoder> encoder, id<MTLBuffer> input_buffer, size_t input_offset, id<MTLBuffer> weight_buffer, id<MTLBuffer> output_buffer, size_t output_offset, size_t rows, size_t in_dim, size_t out_dim, uint8_t planned_dispatch, termite_metal_q8_0_linear_family_kind q8_family, int failure_code);
static int termite_metal_encode_argmax_logits_on_encoder(termite_metal_decode_runtime *runtime, id<MTLComputeCommandEncoder> encoder, id<MTLBuffer> logits_buffer, size_t logits_offset, size_t out_dim, int failure_code);
static int termite_metal_encode_head_rms_rope_on_encoder(termite_metal_decode_runtime *runtime, id<MTLComputeCommandEncoder> encoder, id<MTLBuffer> input_buffer, size_t input_offset, size_t norm_slot, size_t total_heads, size_t head_dim, size_t rope_dim, size_t position, float theta, float freq_scale, float eps, float value_scale, uint32_t consecutive_pairs, size_t heads_per_row, size_t position_period, id<MTLBuffer> output_buffer, size_t output_offset, int failure_code);
static int termite_metal_encode_attention_f32_on_encoder(termite_metal_decode_runtime *runtime, id<MTLComputeCommandEncoder> encoder, id<MTLBuffer> q_buffer, size_t q_offset, id<MTLBuffer> k_buffer, size_t k_offset, id<MTLBuffer> v_buffer, size_t v_offset, id<MTLBuffer> output_buffer, size_t output_offset, size_t q_len, size_t kv_tokens, size_t num_heads, size_t num_kv_heads, size_t head_dim, size_t query_position_offset, size_t kv_position_offset, size_t sliding_window, size_t total_sequence_len, int failure_code);
static int termite_metal_encode_dense_linear_on_encoder(termite_metal_decode_runtime *runtime, id<MTLComputeCommandEncoder> encoder, size_t slot, id<MTLBuffer> input_buffer, size_t input_offset, size_t in_dim, size_t out_dim, id<MTLBuffer> output_buffer, size_t output_offset, int failure_code);
static int termite_metal_decode_runtime_encode_attention_paged_update_from_f32_key_device_slot(termite_metal_decode_runtime *runtime, id<MTLCommandBuffer> command_buffer, size_t slot, uint32_t format, void *k_handle, size_t k_offset, void *v_handle, size_t v_offset, size_t total_tokens, size_t suffix_tokens, size_t num_kv_heads, size_t head_dim, size_t key_row_bytes, size_t base_key_row_bytes, size_t v_row_stride, size_t kv_position_offset, const uint32_t *block_table, size_t block_count, size_t page_size);
static int termite_metal_encode_paged_quantized_kv_seed_on_encoder(termite_metal_decode_runtime *runtime, id<MTLComputeCommandEncoder> encoder, size_t slot, uint32_t format, id<MTLBuffer> k_buffer, size_t k_offset, id<MTLBuffer> v_buffer, size_t v_offset, size_t total_tokens, size_t suffix_tokens, size_t num_kv_heads, size_t head_dim, size_t key_row_bytes, size_t base_key_row_bytes, size_t v_row_stride, size_t kv_position_offset, const uint32_t *block_table, size_t block_count, size_t page_size, int failure_code);
static int termite_metal_encode_paged_f32_kv_seed_on_encoder(termite_metal_decode_runtime *runtime, id<MTLComputeCommandEncoder> encoder, size_t slot, id<MTLBuffer> k_buffer, size_t k_offset, id<MTLBuffer> v_buffer, size_t v_offset, size_t total_tokens, size_t suffix_tokens, size_t num_kv_heads, size_t head_dim, size_t key_row_bytes, size_t v_row_stride, size_t kv_position_offset, const uint32_t *block_table, size_t block_count, size_t page_size, int failure_code);
static int termite_metal_encode_rms_norm_add_on_encoder(termite_metal_decode_runtime *runtime, id<MTLComputeCommandEncoder> encoder, id<MTLBuffer> input_buffer, size_t input_offset, size_t norm_slot, id<MTLBuffer> residual_buffer, size_t residual_offset, id<MTLBuffer> output_buffer, size_t output_offset, size_t hidden_size, float eps, int failure_code);
static int termite_metal_encode_rms_norm_add_scale_on_encoder(termite_metal_decode_runtime *runtime, id<MTLComputeCommandEncoder> encoder, id<MTLBuffer> input_buffer, size_t input_offset, size_t norm_slot, id<MTLBuffer> residual_buffer, size_t residual_offset, id<MTLBuffer> output_buffer, size_t output_offset, size_t hidden_size, float eps, float scale, int failure_code);
static int termite_metal_encode_rms_inv_scale_1x_on_encoder(termite_metal_decode_runtime *runtime, id<MTLComputeCommandEncoder> encoder, id<MTLBuffer> input_buffer, size_t input_offset, id<MTLBuffer> output_buffer, size_t output_offset, size_t hidden_size, float eps, int failure_code);
static int termite_metal_encode_rms_norm_rows(termite_metal_decode_runtime *runtime, id<MTLCommandBuffer> command_buffer, id<MTLBuffer> input_buffer, size_t input_offset, size_t norm_slot, id<MTLBuffer> output_buffer, size_t output_offset, size_t rows, size_t hidden_size, float eps, int failure_code);
static int termite_metal_encode_add_for(termite_metal_decode_runtime *runtime, id<MTLCommandBuffer> command_buffer, id<MTLBuffer> lhs_buffer, size_t lhs_offset, id<MTLBuffer> rhs_buffer, size_t rhs_offset, id<MTLBuffer> output_buffer, size_t output_offset, size_t elems, size_t source, int failure_code);
static int termite_metal_encode_add(termite_metal_decode_runtime *runtime, id<MTLCommandBuffer> command_buffer, id<MTLBuffer> lhs_buffer, size_t lhs_offset, id<MTLBuffer> rhs_buffer, size_t rhs_offset, id<MTLBuffer> output_buffer, size_t output_offset, size_t elems, int failure_code);
static int termite_metal_decode_runtime_ensure_active_layer_projection_buffer(termite_metal_decode_runtime *runtime, size_t index, size_t bytes);
static void termite_metal_decode_runtime_clear_frame_resources(CFMutableArrayRef *resources);
static int termite_metal_decode_runtime_retain_frame_resource(termite_metal_decode_runtime *runtime, id resource);
static void termite_metal_decode_runtime_memory_add_buffer(
    termite_metal_decode_runtime_memory_stats *snapshot,
    id<MTLBuffer> buffer,
    uint64_t *category
);
static void termite_metal_decode_runtime_memory_add_resources(
    termite_metal_decode_runtime_memory_stats *snapshot,
    CFMutableArrayRef resources,
    uint64_t *category
);
static int termite_metal_decode_runtime_ensure_hot_buffers(
    termite_metal_decode_runtime *runtime,
    size_t hidden_bytes,
    size_t intermediate_bytes
);
static int termite_metal_decode_runtime_ensure_i2s_quantize_buffers(
    termite_metal_decode_runtime *runtime,
    size_t qinput_bytes,
    size_t scale_bytes
);
static int termite_metal_decode_runtime_ensure_direct_block_hidden_buffers(
    termite_metal_decode_runtime *runtime,
    size_t hidden_bytes
);
static int termite_metal_decode_runtime_ensure_direct_block_shared_buffer(
    termite_metal_decode_runtime *runtime,
    size_t index,
    size_t bytes
);
static int termite_metal_decode_runtime_ensure_direct_block_q_buffer(
    termite_metal_decode_runtime *runtime,
    size_t q_bytes
);
static int termite_metal_decode_runtime_encode_replace_attention_span(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    const uint8_t *encoded_key,
    const float *v,
    size_t kv_tokens,
    size_t key_row_bytes,
    size_t v_row_stride,
    size_t position_offset
);
static int termite_metal_decode_runtime_encode_replace_attention_span_slot(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    size_t slot,
    const uint8_t *encoded_key,
    const float *v,
    size_t kv_tokens,
    size_t key_row_bytes,
    size_t v_row_stride,
    size_t position_offset
);
static int termite_metal_decode_runtime_encode_replace_attention_span_from_f32_key_slot(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    size_t slot,
    uint32_t format,
    const float *k,
    const float *v,
    size_t kv_tokens,
    size_t num_kv_heads,
    size_t head_dim,
    size_t key_row_bytes,
    size_t base_key_row_bytes,
    size_t v_row_stride,
    size_t position_offset
);

static int termite_metal_decode_runtime_encode_replace_attention_span_from_f32_key_device_slot(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    size_t slot,
    uint32_t format,
    void *k_handle,
    size_t k_offset,
    void *v_handle,
    size_t v_offset,
    size_t kv_tokens,
    size_t num_kv_heads,
    size_t head_dim,
    size_t key_row_bytes,
    size_t base_key_row_bytes,
    size_t v_row_stride,
    size_t position_offset
);
static int termite_metal_decode_runtime_encode_attention_span(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    uint32_t format,
    id<MTLBuffer> q_buffer,
    size_t q_offset,
    size_t kv_tokens,
    size_t num_heads,
    size_t num_kv_heads,
    size_t head_dim,
    size_t key_row_bytes,
    size_t base_key_row_bytes,
    size_t query_position,
    size_t kv_position_offset,
    size_t sliding_window,
    id<MTLBuffer> output_buffer,
    size_t output_offset
);
static int termite_metal_decode_runtime_encode_attention_span_slot(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    size_t slot,
    uint32_t format,
    id<MTLBuffer> q_buffer,
    size_t q_offset,
    size_t kv_tokens,
    size_t num_heads,
    size_t num_kv_heads,
    size_t head_dim,
    size_t key_row_bytes,
    size_t base_key_row_bytes,
    size_t query_position,
    size_t kv_position_offset,
    size_t sliding_window,
    id<MTLBuffer> output_buffer,
    size_t output_offset
);
static int termite_metal_decode_runtime_encode_sample_from_logits_buffer(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    id<MTLBuffer> logits_buffer,
    size_t out_dim,
    float temperature,
    size_t top_k,
    float top_p,
    float min_p,
    float repetition_penalty,
    float frequency_penalty,
    float presence_penalty,
    const uint32_t *penalty_token_ids,
    const uint32_t *penalty_counts,
    size_t penalty_count,
    uint32_t seed
);
int termite_metal_decode_runtime_reserve(termite_metal_decode_runtime *runtime, size_t scratch_bytes, size_t token_bytes);
int termite_metal_decode_runtime_flush_active_frame(termite_metal_decode_runtime *runtime);
int termite_metal_decode_runtime_active_frame_has_work(termite_metal_decode_runtime *runtime);
int termite_metal_decode_runtime_submit_frame(termite_metal_decode_runtime *runtime);
int termite_metal_decode_runtime_cancel_frame(termite_metal_decode_runtime *runtime);
int termite_metal_decode_runtime_wait_frame(termite_metal_decode_runtime *runtime);
static bool termite_metal_decode_runtime_active_frame_empty(termite_metal_decode_runtime *runtime);
static void termite_metal_decode_runtime_close_planned_compute_encoder_for_transition(termite_metal_decode_runtime *runtime);

void *termite_metal_buffer_alloc(termite_metal_decode_runtime *runtime, size_t length, int storage_mode);
void termite_metal_buffer_release(void *handle);
void *termite_metal_buffer_contents(void *handle);
int termite_metal_buffer_upload(
    termite_metal_decode_runtime *runtime,
    void *handle,
    size_t offset,
    const void *src,
    size_t length
);
int termite_metal_buffer_download(
    termite_metal_decode_runtime *runtime,
    void *handle,
    size_t offset,
    void *dst,
    size_t length
);
int termite_metal_buffer_slice_last_dim_2d(
    termite_metal_decode_runtime *runtime,
    void *src_handle,
    size_t src_offset,
    size_t rows,
    size_t cols,
    size_t start,
    size_t out_cols,
    void *dst_handle,
    size_t dst_offset
);

int termite_metal_decode_runtime_reserve_hidden_state(
    termite_metal_decode_runtime *runtime,
    size_t max_prefill_rows,
    size_t hidden_size
);
void *termite_metal_decode_runtime_hidden_state_buffer(
    termite_metal_decode_runtime *runtime,
    int which
);
size_t termite_metal_decode_runtime_hidden_state_capacity(
    termite_metal_decode_runtime *runtime
);
size_t termite_metal_decode_runtime_hidden_state_max_rows(
    termite_metal_decode_runtime *runtime
);
void *termite_metal_decode_runtime_acquire_scratch(
    termite_metal_decode_runtime *runtime,
    size_t bytes
);
void termite_metal_decode_runtime_release_scratch(
    termite_metal_decode_runtime *runtime,
    void *handle
);

int termite_metal_decode_runtime_apply_attention_block_prefix(
    termite_metal_decode_runtime *runtime,
    const float *attention_input,
    size_t attention_pre_linear_rms_norm_slot,
    size_t attention_linear_slot,
    size_t attention_post_linear_rms_norm_slot,
    const float *residual,
    size_t attention_input_size,
    size_t hidden_size,
    float eps,
    size_t ffn_layer_norm_slot,
    size_t ffn_rms_norm_slot,
    float *attn_added,
    float *ffn_input
);

static NSUInteger termite_metal_thread_width(id<MTLComputePipelineState> pipeline, size_t dim);
static NSUInteger termite_metal_threadgroup_memory_16(NSUInteger bytes);

static uint64_t termite_metal_clock_monotonic_nanos(void) {
    struct timespec ts;
    if (clock_gettime(CLOCK_MONOTONIC, &ts) != 0) return 0;
    return ((uint64_t)ts.tv_sec * 1000000000ull) + (uint64_t)ts.tv_nsec;
}

static bool termite_metal_debug_q80_block_finite(size_t layer_index) {
    const char *enabled = getenv("TERMITE_METAL_DEBUG_QUANT_BLOCK_FINITE");
    if (enabled == NULL) enabled = getenv("TERMITE_METAL_DEBUG_Q80_BLOCK_FINITE");
    if (enabled == NULL || enabled[0] == '\0' || strcmp(enabled, "0") == 0) return false;
    const char *layer_env = getenv("TERMITE_METAL_DEBUG_QUANT_BLOCK_LAYER");
    if (layer_env == NULL) layer_env = getenv("TERMITE_METAL_DEBUG_Q80_BLOCK_LAYER");
    if (layer_env == NULL || layer_env[0] == '\0') return true;
    char *end = NULL;
    unsigned long long target = strtoull(layer_env, &end, 10);
    if (end == layer_env) return true;
    return (size_t)target == layer_index;
}

static bool termite_metal_trace_q80_block(void) {
    const char *enabled = getenv("TERMITE_METAL_TRACE_QUANT_BLOCK");
    if (enabled == NULL) enabled = getenv("TERMITE_METAL_TRACE_Q80_BLOCK");
    return enabled != NULL && enabled[0] != '\0' && strcmp(enabled, "0") != 0;
}

static bool termite_metal_debug_direct_block_finite(size_t layer_index) {
    const char *enabled = getenv("TERMITE_METAL_DEBUG_DIRECT_BLOCK_FINITE");
    if (enabled == NULL || enabled[0] == '\0' || strcmp(enabled, "0") == 0) return false;
    const char *layer_env = getenv("TERMITE_METAL_DEBUG_DIRECT_BLOCK_LAYER");
    if (layer_env == NULL || layer_env[0] == '\0') return true;
    char *end = NULL;
    unsigned long long target = strtoull(layer_env, &end, 10);
    if (end == layer_env) return true;
    return (size_t)target == layer_index;
}

static void termite_metal_debug_dump_buffer_finite(
    termite_metal_decode_runtime *runtime,
    const char *label,
    size_t layer_index,
    id<MTLBuffer> buffer,
    size_t offset,
    size_t elems
) {
    if (runtime == NULL || buffer == nil || elems == 0) return;
    const size_t bytes = elems * sizeof(float);
    if (offset + bytes > buffer.length) {
        fprintf(stderr, "metal-quant-block-finite label=%s layer=%zu elems=%zu out_of_bounds=1 offset=%zu length=%zu\n", label, layer_index, elems, offset, (size_t)buffer.length);
        return;
    }

    const float *values = NULL;
    id<MTLBuffer> staging = nil;
    if (buffer.storageMode == MTLStorageModeShared) {
        values = (const float *)((const uint8_t *)buffer.contents + offset);
    } else {
        staging = [runtime->device newBufferWithLength:bytes options:MTLResourceStorageModeShared];
        if (staging == nil) {
            fprintf(stderr, "metal-quant-block-finite label=%s layer=%zu elems=%zu staging_failed=1\n", label, layer_index, elems);
            return;
        }
        id<MTLCommandBuffer> cb = termite_metal_new_command_buffer(runtime->queue, "termite_metal_debug_dump_buffer_finite");
        if (cb == nil) {
            fprintf(stderr, "metal-quant-block-finite label=%s layer=%zu elems=%zu command_failed=1\n", label, layer_index, elems);
            return;
        }
        termite_metal_record_active_frame_blit_source(cb, TERMITE_METAL_BLIT_SOURCE_OTHER);
        id<MTLBlitCommandEncoder> blit = termite_metal_tracked_blit_command_encoder(cb);
        if (blit == nil) {
            fprintf(stderr, "metal-quant-block-finite label=%s layer=%zu elems=%zu blit_failed=1\n", label, layer_index, elems);
            return;
        }
        [blit copyFromBuffer:buffer sourceOffset:offset toBuffer:staging destinationOffset:0 size:bytes];
        [blit endEncoding];
        if (termite_metal_commit_and_wait(cb, "termite_metal_debug_dump_buffer_finite", -1) != 0) {
            fprintf(stderr, "metal-quant-block-finite label=%s layer=%zu elems=%zu download_failed=1\n", label, layer_index, elems);
            return;
        }
        values = (const float *)staging.contents;
    }

    size_t nonfinite = 0;
    size_t nan_count = 0;
    size_t inf_count = 0;
    size_t first_bad = SIZE_MAX;
    float first_bad_value = 0.0f;
    float min_value = 0.0f;
    float max_value = 0.0f;
    float max_abs = 0.0f;
    bool have_finite = false;
    for (size_t i = 0; i < elems; ++i) {
        const float v = values[i];
        if (!isfinite(v)) {
            if (first_bad == SIZE_MAX) {
                first_bad = i;
                first_bad_value = v;
            }
            nonfinite += 1;
            if (isnan(v)) nan_count += 1;
            else inf_count += 1;
            continue;
        }
        if (!have_finite) {
            min_value = v;
            max_value = v;
            have_finite = true;
        } else {
            if (v < min_value) min_value = v;
            if (v > max_value) max_value = v;
        }
        const float av = fabsf(v);
        if (av > max_abs) max_abs = av;
    }
    fprintf(
        stderr,
        "metal-quant-block-finite label=%s layer=%zu elems=%zu nonfinite=%zu nan=%zu inf=%zu first_bad=%lld first_bad_value=%e min=%e max=%e max_abs=%e\n",
        label,
        layer_index,
        elems,
        nonfinite,
        nan_count,
        inf_count,
        first_bad == SIZE_MAX ? -1ll : (long long)first_bad,
        first_bad_value,
        have_finite ? min_value : 0.0f,
        have_finite ? max_value : 0.0f,
        max_abs
    );
}

int termite_metal_device_available(void) {
    @autoreleasepool {
        return termite_metal_shared_device() != nil ? 1 : 0;
    }
}

typedef struct termite_metal_linear_params {
    uint32_t rows;
    uint32_t in_dim;
    uint32_t out_dim;
    uint32_t row_blocks;
} termite_metal_linear_params;

typedef struct termite_metal_qkv_linear_params {
    uint32_t rows;
    uint32_t in_dim;
    uint32_t q_out_dim;
    uint32_t kv_out_dim;
    uint32_t row_blocks;
} termite_metal_qkv_linear_params;

static const size_t TERMITE_METAL_Q8_0_PAIR_ACTIVATION_SPLIT_MM_SG_MIN_ROWS = 129u;

typedef struct termite_metal_key_scores_params {
    uint32_t q_len;
    uint32_t block_tokens;
    uint32_t num_heads;
    uint32_t num_kv_heads;
    uint32_t head_dim;
    uint32_t key_row_bytes;
} termite_metal_key_scores_params;

typedef struct termite_metal_tl1_params {
    uint32_t rows;
    uint32_t in_dim;
    uint32_t out_dim;
    uint32_t packed_len;
    uint32_t bm;
    uint32_t cfg_by;
    uint32_t bmm;
} termite_metal_tl1_params;

typedef struct termite_metal_tl2_params {
    uint32_t rows;
    uint32_t in_dim;
    uint32_t out_dim;
    uint32_t scale_off;
    uint32_t three_value_len;
    uint32_t three_sign_len;
    uint32_t bm;
    uint32_t cfg_by;
    uint32_t bmm;
    uint32_t three_cols;
    uint32_t two_cols;
} termite_metal_tl2_params;

typedef struct termite_metal_embed_absolute_position_params {
    uint32_t token_id;
    uint32_t position_id;
    uint32_t hidden_size;
} termite_metal_embed_absolute_position_params;

typedef struct termite_metal_embedding_lookup_params {
    uint32_t total;
    uint32_t dim;
} termite_metal_embedding_lookup_params;

typedef struct termite_metal_quant_rows_params {
    uint32_t total;
    uint32_t dim;
    uint32_t source_rows;
    uint32_t row_offset;
    float scale;
} termite_metal_quant_rows_params;

typedef struct termite_metal_rope_params {
    uint32_t total_chunks;
    uint32_t head_dim;
    uint32_t rope_dim;
    float theta;
    float freq_scale;
    uint32_t consecutive_pairs;
} termite_metal_rope_params;

typedef struct termite_metal_head_rms_rope_params {
    uint32_t total_heads;
    uint32_t head_dim;
    uint32_t rope_dim;
    uint32_t position;
    float theta;
    float freq_scale;
    float eps;
    float value_scale;
    uint32_t consecutive_pairs;
    uint32_t heads_per_row;
    uint32_t position_period;
} termite_metal_head_rms_rope_params;

typedef struct termite_metal_attention_f32_params {
    uint32_t q_len;
    uint32_t kv_len;
    uint32_t num_heads;
    uint32_t num_kv_heads;
    uint32_t head_dim;
    uint32_t query_position_offset;
    uint32_t kv_position_offset;
    uint32_t sliding_window;
    uint32_t total_sequence_len;
    uint32_t has_bias;
    uint32_t has_mask;
} termite_metal_attention_f32_params;

typedef struct termite_metal_compressed_attention_store_local_params {
    uint32_t query_rows;
    uint32_t query_abs_start;
    uint32_t head_dim;
    uint32_t reserved;
} termite_metal_compressed_attention_store_local_params;

typedef struct termite_metal_compressed_attention_component_params {
    uint32_t query_rows;
    uint32_t query_abs_start;
    uint32_t total_tokens;
    uint32_t compress_rate;
    uint32_t row_dim;
    uint32_t gate_width;
    uint32_t row_count;
    uint32_t rope_dim;
    float theta;
    float freq_scale;
    float eps;
    uint32_t consecutive_pairs;
    uint32_t bias_rows;
    uint32_t reserved0;
    uint32_t reserved1;
    uint32_t reserved2;
} termite_metal_compressed_attention_component_params;

typedef struct termite_metal_compressed_attention_params {
    uint32_t query_abs_start;
    uint32_t query_rows;
    uint32_t token_count;
    uint32_t compressed_rows;
    uint32_t num_heads;
    uint32_t head_dim;
    uint32_t sliding_window;
    uint32_t top_k;
    uint32_t has_indexer;
    uint32_t index_rows;
    uint32_t index_heads;
    uint32_t index_head_dim;
    uint32_t has_sinks;
    uint32_t reserved0;
    float scale;
    float reserved1;
} termite_metal_compressed_attention_params;

typedef struct termite_metal_apply_layer_norm_params {
    uint32_t hidden_size;
    float eps;
} termite_metal_apply_layer_norm_params;

typedef struct termite_metal_apply_layer_norm_scale_params {
    uint32_t hidden_size;
    float eps;
    float scale;
    uint32_t reserved;
} termite_metal_apply_layer_norm_scale_params;

typedef struct termite_metal_apply_row_norm_params {
    uint32_t rows;
    uint32_t hidden_size;
    float eps;
} termite_metal_apply_row_norm_params;

typedef struct termite_metal_apply_row_norm_scale_params {
    uint32_t rows;
    uint32_t hidden_size;
    float eps;
    float scale;
} termite_metal_apply_row_norm_scale_params;

typedef struct termite_metal_apply_linear_params {
    uint32_t in_dim;
    uint32_t out_dim;
} termite_metal_apply_linear_params;

typedef struct termite_metal_quantize_rows_params {
    uint32_t rows;
    uint32_t dim;
} termite_metal_quantize_rows_params;

typedef struct termite_metal_sample_logits_params {
    uint32_t out_dim;
    float temperature;
    uint32_t top_k;
    float top_p;
    float min_p;
    float repetition_penalty;
    float frequency_penalty;
    float presence_penalty;
    uint32_t penalty_count;
    uint32_t seed;
} termite_metal_sample_logits_params;

typedef struct termite_metal_penalty_entry {
    uint32_t token_id;
    uint32_t count;
} termite_metal_penalty_entry;

typedef struct termite_metal_topk_reduce_params {
    uint32_t candidate_count;
    uint32_t top_k;
    float top_p;
    float min_p;
    uint32_t seed;
} termite_metal_topk_reduce_params;

typedef struct termite_metal_apply_activation_params {
    uint32_t activation_kind;
    uint32_t rows;
    uint32_t dim;
} termite_metal_apply_activation_params;

typedef struct termite_metal_apply_add_params {
    uint32_t rows;
    uint32_t dim;
    uint32_t flags;
    uint32_t reserved;
} termite_metal_apply_add_params;

typedef struct termite_metal_training_accumulate_params {
    uint32_t elem_count;
    float scale;
    uint32_t first;
    uint32_t reserved;
} termite_metal_training_accumulate_params;

typedef struct termite_metal_training_adamw_params {
    uint32_t elem_count;
    float lr;
    float beta1;
    float beta2;
    float eps;
    float weight_decay;
    float bias_correction1;
    float bias_correction2;
    float grad_scale;
    uint32_t reserved0;
    uint32_t reserved1;
    uint32_t reserved2;
} termite_metal_training_adamw_params;

typedef struct termite_metal_training_sumsq_params {
    uint32_t elem_count;
    uint32_t reserved0;
    uint32_t reserved1;
    uint32_t reserved2;
} termite_metal_training_sumsq_params;

typedef struct termite_metal_apply_softmax_params {
    uint32_t rows;
    uint32_t dim;
    uint32_t log_softmax;
    uint32_t reserved;
} termite_metal_apply_softmax_params;

typedef struct termite_metal_reduce_last_dim_params {
    uint32_t rows;
    uint32_t dim;
    uint32_t kind;
    uint32_t reserved;
} termite_metal_reduce_last_dim_params;

typedef struct termite_metal_broadcast_f32_params {
    uint32_t rank;
    uint32_t total;
    uint32_t reserved0;
    uint32_t reserved1;
    uint32_t out_strides[8];
    uint32_t input_strides_for_out[8];
} termite_metal_broadcast_f32_params;

typedef struct termite_metal_reduce_axis_f32_params {
    uint32_t rank;
    uint32_t out_total;
    uint32_t reduce_dim;
    uint32_t reduce_stride;
    uint32_t kind;
    uint32_t reserved0;
    uint32_t reserved1;
    uint32_t reserved2;
    uint32_t out_strides[8];
    uint32_t input_strides_for_out[8];
} termite_metal_reduce_axis_f32_params;

typedef struct termite_metal_broadcast_last_dim_params {
    uint32_t rows;
    uint32_t in_dim;
    uint32_t out_dim;
    uint32_t reserved;
} termite_metal_broadcast_last_dim_params;

typedef struct termite_metal_attention_span_params {
    uint32_t kv_tokens;
    uint32_t num_heads;
    uint32_t num_kv_heads;
    uint32_t head_dim;
    uint32_t key_row_bytes;
    uint32_t base_key_row_bytes;
    uint32_t query_position;
    uint32_t kv_position_offset;
    uint32_t sliding_window;
    uint32_t v_row_stride;
} termite_metal_attention_span_params;

typedef struct termite_metal_paged_attention_params {
    uint32_t q_len;
    uint32_t kv_tokens;
    uint32_t num_heads;
    uint32_t num_kv_heads;
    uint32_t head_dim;
    uint32_t key_row_bytes;
    uint32_t base_key_row_bytes;
    uint32_t query_position_offset;
    uint32_t kv_position_offset;
    uint32_t sliding_window;
    uint32_t v_row_stride;
    uint32_t page_size;
    uint32_t block_count;
    uint32_t format;
    uint32_t v_element_bytes;
    uint32_t has_sinks;
    float softcap;
} termite_metal_paged_attention_params;

typedef struct termite_metal_encode_key_params {
    uint32_t rows;
    uint32_t num_kv_heads;
    uint32_t head_dim;
    uint32_t key_row_bytes;
    uint32_t base_key_row_bytes;
} termite_metal_encode_key_params;

typedef struct termite_metal_paged_f32_kv_seed_params {
    uint32_t total_tokens;
    uint32_t suffix_tokens;
    uint32_t values_per_row;
    uint32_t page_size;
    uint32_t key_row_floats;
    uint32_t v_row_stride;
} termite_metal_paged_f32_kv_seed_params;

typedef struct termite_metal_slice_last_dim_f32_2d_params {
    uint32_t rows;
    uint32_t cols;
    uint32_t start;
    uint32_t out_cols;
} termite_metal_slice_last_dim_f32_2d_params;

typedef struct termite_metal_gather_axis0_f32_2d_params {
    uint32_t rows;
    uint32_t cols;
    uint32_t index_count;
    uint32_t reserved;
} termite_metal_gather_axis0_f32_2d_params;

typedef struct termite_metal_concat_lastdim_f32_2d_params {
    uint32_t rows;
    uint32_t dim_a;
    uint32_t dim_b;
    uint32_t reserved;
} termite_metal_concat_lastdim_f32_2d_params;

typedef struct termite_metal_dense_qkv_split_bias_params {
    uint32_t rows;
    uint32_t q_out_dim;
    uint32_t kv_out_dim;
    uint32_t total_out_dim;
} termite_metal_dense_qkv_split_bias_params;

typedef struct termite_metal_dense_pair_split_bias_params {
    uint32_t rows;
    uint32_t out_dim;
    uint32_t total_out_dim;
    uint32_t reserved;
} termite_metal_dense_pair_split_bias_params;

typedef struct termite_metal_dense_qkv_direct_params {
    uint32_t rows;
    uint32_t in_dim;
    uint32_t q_out_dim;
    uint32_t kv_out_dim;
} termite_metal_dense_qkv_direct_params;

typedef struct termite_metal_dense_pair_direct_params {
    uint32_t rows;
    uint32_t in_dim;
    uint32_t out_dim;
    uint32_t reserved;
} termite_metal_dense_pair_direct_params;

typedef struct termite_metal_deberta_embeddings_f32_params {
    uint32_t total;
    uint32_t rows;
    uint32_t dim;
    float eps;
} termite_metal_deberta_embeddings_f32_params;

typedef struct termite_metal_gliner_word_embeddings_f32_params {
    uint32_t batch;
    uint32_t seq_len;
    uint32_t hidden_size;
    uint32_t num_words;
} termite_metal_gliner_word_embeddings_f32_params;

typedef struct termite_metal_gliner_gru_combine_f32_params {
    uint32_t rows;
    uint32_t dim;
    uint32_t reserved0;
    uint32_t reserved1;
} termite_metal_gliner_gru_combine_f32_params;

typedef struct termite_metal_argmax_axis_f32_params {
    uint32_t outer;
    uint32_t axis_dim;
    uint32_t inner;
    uint32_t reserved;
} termite_metal_argmax_axis_f32_params;

typedef struct termite_metal_convert_dtype_f32_params {
    uint32_t elem_count;
    uint32_t kind;
    uint32_t reserved0;
    uint32_t reserved1;
} termite_metal_convert_dtype_f32_params;

typedef struct termite_metal_sdpa_f32_params {
    uint32_t batch;
    uint32_t seq_len;
    uint32_t num_heads;
    uint32_t head_dim;
    uint32_t bias_mode;
    uint32_t has_mask;
    uint32_t reserved0;
    uint32_t reserved1;
} termite_metal_sdpa_f32_params;

typedef struct termite_metal_disentangled_relative_attention_f32_params {
    uint32_t batch;
    uint32_t seq_len;
    uint32_t num_heads;
    uint32_t head_dim;
    uint32_t has_mask;
    uint32_t reserved0;
    uint32_t reserved1;
    uint32_t reserved2;
} termite_metal_disentangled_relative_attention_f32_params;

typedef struct termite_metal_deberta_relative_score_gemm_f32_params {
    uint32_t batch;
    uint32_t seq_len;
    uint32_t rel_len;
    uint32_t num_heads;
    uint32_t head_dim;
    uint32_t mode;
    uint32_t reserved0;
    uint32_t reserved1;
} termite_metal_deberta_relative_score_gemm_f32_params;

typedef struct termite_metal_transpose_f32_params {
    uint32_t rank;
    uint32_t total;
    uint32_t reserved0;
    uint32_t reserved1;
    uint32_t dims[8];
    uint32_t in_strides[8];
    uint32_t out_strides[8];
    uint32_t perm[8];
} termite_metal_transpose_f32_params;

typedef struct termite_metal_dot_general_2d_f32_params {
    uint32_t m;
    uint32_t n;
    uint32_t k;
    uint32_t rhs_contract_axis;
} termite_metal_dot_general_2d_f32_params;

typedef struct termite_metal_dot_general_batched_f32_params {
    uint32_t batch_count;
    uint32_t m;
    uint32_t n;
    uint32_t k;
    uint32_t rhs_contract_axis;
    uint32_t reserved0;
    uint32_t reserved1;
    uint32_t reserved2;
} termite_metal_dot_general_batched_f32_params;

typedef struct termite_metal_conv1d_f32_params {
    uint32_t batch;
    uint32_t in_channels;
    uint32_t out_channels;
    uint32_t time_steps;
    uint32_t kernel_size;
    uint32_t stride;
    uint32_t padding;
    uint32_t out_time;
    uint32_t has_bias;
    uint32_t reserved0;
    uint32_t reserved1;
    uint32_t reserved2;
} termite_metal_conv1d_f32_params;

typedef struct termite_metal_conv2d_f32_params {
    uint32_t batch;
    uint32_t in_channels;
    uint32_t out_channels;
    uint32_t height;
    uint32_t width;
    uint32_t kernel_h;
    uint32_t kernel_w;
    uint32_t stride_h;
    uint32_t stride_w;
    uint32_t padding_h;
    uint32_t padding_w;
    uint32_t groups;
    uint32_t out_h;
    uint32_t out_w;
    uint32_t has_bias;
    uint32_t reserved0;
} termite_metal_conv2d_f32_params;

static NSString *termite_metal_shader_source(void) {
    return @"#include <metal_stdlib>\n"
           "using namespace metal;\n"
           "struct termite_metal_linear_params { uint rows; uint in_dim; uint out_dim; uint row_blocks; };\n"
           "struct termite_metal_qkv_linear_params { uint rows; uint in_dim; uint q_out_dim; uint kv_out_dim; uint row_blocks; };\n"
           "struct termite_metal_key_scores_params { uint q_len; uint block_tokens; uint num_heads; uint num_kv_heads; uint head_dim; uint key_row_bytes; };\n"
           "struct termite_metal_head_rms_rope_params { uint total_heads; uint head_dim; uint rope_dim; uint position; float theta; float freq_scale; float eps; float value_scale; uint consecutive_pairs; uint heads_per_row; uint position_period; };\n"
           "struct termite_metal_tl1_params { uint rows; uint in_dim; uint out_dim; uint packed_len; uint bm; uint cfg_by; uint bmm; };\n"
           "struct termite_metal_tl2_params { uint rows; uint in_dim; uint out_dim; uint scale_off; uint three_value_len; uint three_sign_len; uint bm; uint cfg_by; uint bmm; uint three_cols; uint two_cols; };\n"
           "struct termite_metal_embed_absolute_position_params { uint token_id; uint position_id; uint hidden_size; };\n"
           "struct termite_metal_embedding_lookup_params { uint total; uint dim; };\n"
           "struct termite_metal_deberta_embeddings_f32_params { uint total; uint rows; uint dim; float eps; };\n"
           "struct termite_metal_quant_rows_params { uint total; uint dim; uint source_rows; uint row_offset; float scale; };\n"
           "struct termite_metal_rope_params { uint total_chunks; uint head_dim; uint rope_dim; float theta; float freq_scale; uint consecutive_pairs; };\n"
           "struct termite_metal_apply_layer_norm_params { uint hidden_size; float eps; };\n"
           "struct termite_metal_apply_layer_norm_scale_params { uint hidden_size; float eps; float scale; uint reserved; };\n"
           "struct termite_metal_apply_row_norm_params { uint rows; uint hidden_size; float eps; };\n"
           "struct termite_metal_apply_row_norm_scale_params { uint rows; uint hidden_size; float eps; float scale; };\n"
           "struct termite_metal_apply_linear_params { uint in_dim; uint out_dim; };\n"
           "struct termite_metal_sample_logits_params { uint out_dim; float temperature; uint top_k; float top_p; float min_p; float repetition_penalty; float frequency_penalty; float presence_penalty; uint penalty_count; uint seed; };\n"
           "struct termite_metal_penalty_entry { uint token_id; uint count; };\n"
           "struct termite_metal_topk_reduce_params { uint candidate_count; uint top_k; float top_p; float min_p; uint seed; };\n"
           "struct termite_metal_apply_activation_params { uint activation_kind; uint rows; uint dim; };\n"
           "struct termite_metal_apply_add_params { uint rows; uint dim; uint flags; uint reserved; };\n"
           "struct termite_metal_training_accumulate_params { uint elem_count; float scale; uint first; uint reserved; };\n"
           "struct termite_metal_training_adamw_params { uint elem_count; float lr; float beta1; float beta2; float eps; float weight_decay; float bias_correction1; float bias_correction2; float grad_scale; uint reserved0; uint reserved1; uint reserved2; };\n"
           "struct termite_metal_training_sumsq_params { uint elem_count; uint reserved0; uint reserved1; uint reserved2; };\n"
           "struct termite_metal_apply_softmax_params { uint rows; uint dim; uint log_softmax; uint reserved; };\n"
           "struct termite_metal_reduce_last_dim_params { uint rows; uint dim; uint kind; uint reserved; };\n"
           "struct termite_metal_broadcast_f32_params { uint rank; uint total; uint reserved0; uint reserved1; uint out_strides[8]; uint input_strides_for_out[8]; };\n"
           "struct termite_metal_reduce_axis_f32_params { uint rank; uint out_total; uint reduce_dim; uint reduce_stride; uint kind; uint reserved0; uint reserved1; uint reserved2; uint out_strides[8]; uint input_strides_for_out[8]; };\n"
           "struct termite_metal_broadcast_last_dim_params { uint rows; uint in_dim; uint out_dim; uint reserved; };\n"
           "struct termite_metal_quantize_rows_params { uint rows; uint dim; };\n"
           "struct termite_metal_attention_f32_params { uint q_len; uint kv_len; uint num_heads; uint num_kv_heads; uint head_dim; uint query_position_offset; uint kv_position_offset; uint sliding_window; uint total_sequence_len; uint has_bias; uint has_mask; };\n"
           "struct termite_metal_compressed_attention_store_local_params { uint query_rows; uint query_abs_start; uint head_dim; uint reserved; };\n"
           "struct termite_metal_compressed_attention_component_params { uint query_rows; uint query_abs_start; uint total_tokens; uint compress_rate; uint row_dim; uint gate_width; uint row_count; uint rope_dim; float theta; float freq_scale; float eps; uint consecutive_pairs; uint bias_rows; uint reserved0; uint reserved1; uint reserved2; };\n"
           "struct termite_metal_compressed_attention_params { uint query_abs_start; uint query_rows; uint token_count; uint compressed_rows; uint num_heads; uint head_dim; uint sliding_window; uint top_k; uint has_indexer; uint index_rows; uint index_heads; uint index_head_dim; uint has_sinks; uint reserved0; float scale; float reserved1; };\n"
           "struct termite_metal_attention_span_params { uint kv_tokens; uint num_heads; uint num_kv_heads; uint head_dim; uint key_row_bytes; uint base_key_row_bytes; uint query_position; uint kv_position_offset; uint sliding_window; uint v_row_stride; };\n"
           "struct termite_metal_paged_attention_params { uint q_len; uint kv_tokens; uint num_heads; uint num_kv_heads; uint head_dim; uint key_row_bytes; uint base_key_row_bytes; uint query_position_offset; uint kv_position_offset; uint sliding_window; uint v_row_stride; uint page_size; uint block_count; uint format; uint v_element_bytes; uint has_sinks; float softcap; };\n"
           "struct termite_metal_encode_key_params { uint rows; uint num_kv_heads; uint head_dim; uint key_row_bytes; uint base_key_row_bytes; };\n"
           "struct termite_metal_paged_f32_kv_seed_params { uint total_tokens; uint suffix_tokens; uint values_per_row; uint page_size; uint key_row_floats; uint v_row_stride; };\n"
           "struct termite_metal_slice_last_dim_f32_2d_params { uint rows; uint cols; uint start; uint out_cols; };\n"
           "struct termite_metal_gather_axis0_f32_2d_params { uint rows; uint cols; uint index_count; uint reserved; };\n"
           "struct termite_metal_concat_lastdim_f32_2d_params { uint rows; uint dim_a; uint dim_b; uint reserved; };\n"
           "struct termite_metal_dense_qkv_split_bias_params { uint rows; uint q_out_dim; uint kv_out_dim; uint total_out_dim; };\n"
           "struct termite_metal_dense_pair_split_bias_params { uint rows; uint out_dim; uint total_out_dim; uint reserved; };\n"
           "struct termite_metal_dense_qkv_direct_params { uint rows; uint in_dim; uint q_out_dim; uint kv_out_dim; };\n"
           "struct termite_metal_dense_pair_direct_params { uint rows; uint in_dim; uint out_dim; uint reserved; };\n"
           "struct termite_metal_gliner_word_embeddings_f32_params { uint batch; uint seq_len; uint hidden_size; uint num_words; };\n"
           "struct termite_metal_gliner_gru_combine_f32_params { uint rows; uint dim; uint reserved0; uint reserved1; };\n"
           "struct termite_metal_argmax_axis_f32_params { uint outer; uint axis_dim; uint inner; uint reserved; };\n"
           "struct termite_metal_convert_dtype_f32_params { uint elem_count; uint kind; uint reserved0; uint reserved1; };\n"
           "struct termite_metal_sdpa_f32_params { uint batch; uint seq_len; uint num_heads; uint head_dim; uint bias_mode; uint has_mask; uint reserved0; uint reserved1; };\n"
           "struct termite_metal_disentangled_relative_attention_f32_params { uint batch; uint seq_len; uint num_heads; uint head_dim; uint has_mask; uint reserved0; uint reserved1; uint reserved2; };\n"
           "struct termite_metal_deberta_relative_score_gemm_f32_params { uint batch; uint seq_len; uint rel_len; uint num_heads; uint head_dim; uint mode; uint reserved0; uint reserved1; };\n"
           "struct termite_metal_transpose_f32_params { uint rank; uint total; uint reserved0; uint reserved1; uint dims[8]; uint in_strides[8]; uint out_strides[8]; uint perm[8]; };\n"
           "struct termite_metal_dot_general_2d_f32_params { uint m; uint n; uint k; uint rhs_contract_axis; };\n"
           "struct termite_metal_dot_general_batched_f32_params { uint batch_count; uint m; uint n; uint k; uint rhs_contract_axis; uint reserved0; uint reserved1; uint reserved2; };\n"
           "struct termite_metal_conv1d_f32_params { uint batch; uint in_channels; uint out_channels; uint time_steps; uint kernel_size; uint stride; uint padding; uint out_time; uint has_bias; uint reserved0; uint reserved1; uint reserved2; };\n"
           "struct termite_metal_conv2d_f32_params { uint batch; uint in_channels; uint out_channels; uint height; uint width; uint kernel_h; uint kernel_w; uint stride_h; uint stride_w; uint padding_h; uint padding_w; uint groups; uint out_h; uint out_w; uint has_bias; uint reserved0; };\n"
           "inline float termite_erf_approx(float x) {\n"
           "    float sign = x < 0.0f ? -1.0f : 1.0f;\n"
           "    float ax = fabs(x);\n"
           "    float t = 1.0f / (1.0f + 0.3275911f * ax);\n"
           "    float poly = (((((1.061405429f * t - 1.453152027f) * t) + 1.421413741f) * t - 0.284496736f) * t + 0.254829592f) * t;\n"
           "    return sign * (1.0f - poly * exp(-(ax * ax)));\n"
           "}\n"
           "inline float termite_gelu_tanh(float x) {\n"
           "    if (!isfinite(x)) return 0.0f;\n"
           "    float inner = 0.7978845608f * (x + 0.044715f * x * x * x);\n"
           "    if (inner > 10.0f) return x;\n"
           "    if (inner < -10.0f) return 0.0f;\n"
           "    float y = 0.5f * x * (1.0f + tanh(inner));\n"
           "    return isfinite(y) ? y : 0.0f;\n"
           "}\n"
           "inline float termite_silu(float x) {\n"
           "    if (!isfinite(x)) return 0.0f;\n"
           "    if (x > 20.0f) return x;\n"
           "    if (x < -20.0f) return 0.0f;\n"
           "    float y = x / (1.0f + exp(-x));\n"
           "    return isfinite(y) ? y : 0.0f;\n"
           "}\n"
           "inline float termite_exp_approx(float x_in) {\n"
           "    if (isnan(x_in)) return NAN;\n"
           "    if (x_in > 88.7228f) return INFINITY;\n"
           "    if (x_in < -87.34f) return 0.0f;\n"
           "    const float ln2 = 0.6931471805599453f;\n"
           "    const float inv_ln2 = 1.4426950408889634f;\n"
           "    int k = int(x_in * inv_ln2 + (x_in < 0.0f ? -0.5f : 0.5f));\n"
           "    float r = x_in - float(k) * ln2;\n"
           "    float p = 0.0013888889f;\n"
           "    p = fma(p, r, 0.008333334f);\n"
           "    p = fma(p, r, 0.041666668f);\n"
           "    p = fma(p, r, 0.16666667f);\n"
           "    p = fma(p, r, 0.5f);\n"
           "    p = fma(p, r, 1.0f);\n"
           "    p = fma(p, r, 1.0f);\n"
           "    uint pow2_bits = uint((k + 127) << 23);\n"
           "    return p * as_type<float>(pow2_bits);\n"
           "}\n"
           "inline float termite_quick_gelu(float x) {\n"
           "    if (!isfinite(x)) return 0.0f;\n"
           "    float y = x / (1.0f + termite_exp_approx(-1.702f * x));\n"
           "    return isfinite(y) ? y : 0.0f;\n"
           "}\n"
           "inline float termite_apply_activation_scalar(float x, uint activation_kind) {\n"
           "    switch (activation_kind) {\n"
           "        case 0u: return termite_gelu_tanh(x);\n"
           "        case 1u: return termite_gelu_tanh(x);\n"
           "        case 2u: return termite_silu(x);\n"
           "        case 3u: return max(x, 0.0f);\n"
           "        case 4u: return termite_quick_gelu(x);\n"
           "        case 5u: { float relu_x = max(x, 0.0f); return relu_x * relu_x; }\n"
           "        case 6u: return -x;\n"
           "        case 7u: return sqrt(x);\n"
           "        case 8u: return rsqrt(x);\n"
           "        case 9u: return exp(x);\n"
           "        case 10u: return log(x);\n"
           "        case 11u: return sin(x);\n"
           "        case 12u: return cos(x);\n"
           "        case 13u: return tanh(x);\n"
           "        case 14u: return termite_erf_approx(x);\n"
           "        case 15u: return fabs(x);\n"
           "        case 16u: return 1.0f / (1.0f + exp(-x));\n"
           "        default: return x;\n"
           "    }\n"
           "}\n"
           "inline float termite_gated_activation_product(float gate, float up, uint activation_kind) {\n"
           "    float activated = termite_apply_activation_scalar(gate, activation_kind);\n"
           "    return activated == 0.0f ? 0.0f : activated * up;\n"
           "}\n"
           "inline void unpack_q4k_scale_mins(const device uchar *scales, thread float scs[8], thread float mins[8]) {\n"
           "    for (uint j = 0; j < 4; ++j) {\n"
           "        scs[j] = float(scales[j] & 63u);\n"
           "        mins[j] = float(scales[j + 4] & 63u);\n"
           "    }\n"
           "    for (uint j = 4; j < 8; ++j) {\n"
           "        scs[j] = float((scales[j + 4] & 0x0Fu) | ((scales[j - 4] >> 6) << 4));\n"
           "        mins[j] = float((scales[j + 4] >> 4) | ((scales[j] >> 6) << 4));\n"
           "    }\n"
           "}\n"
           "inline void pack_q4k_scale_mins(device uchar *dst, thread uint scs[8], thread uint mins[8]) {\n"
           "    dst[0] = uchar((scs[0] & 63u) | ((scs[4] >> 4) << 6));\n"
           "    dst[1] = uchar((scs[1] & 63u) | ((scs[5] >> 4) << 6));\n"
           "    dst[2] = uchar((scs[2] & 63u) | ((scs[6] >> 4) << 6));\n"
           "    dst[3] = uchar((scs[3] & 63u) | ((scs[7] >> 4) << 6));\n"
           "    dst[4] = uchar((mins[0] & 63u) | ((mins[4] >> 4) << 6));\n"
           "    dst[5] = uchar((mins[1] & 63u) | ((mins[5] >> 4) << 6));\n"
           "    dst[6] = uchar((mins[2] & 63u) | ((mins[6] >> 4) << 6));\n"
           "    dst[7] = uchar((mins[3] & 63u) | ((mins[7] >> 4) << 6));\n"
           "    dst[8] = uchar((scs[4] & 0x0Fu) | ((mins[4] & 0x0Fu) << 4));\n"
           "    dst[9] = uchar((scs[5] & 0x0Fu) | ((mins[5] & 0x0Fu) << 4));\n"
           "    dst[10] = uchar((scs[6] & 0x0Fu) | ((mins[6] & 0x0Fu) << 4));\n"
           "    dst[11] = uchar((scs[7] & 0x0Fu) | ((mins[7] & 0x0Fu) << 4));\n"
           "}\n"
           "kernel void termite_q1_0_linear(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.out_dim; if (gid >= total) return; uint r = gid / p.out_dim; uint o = gid % p.out_dim; uint row_offset = o * p.row_blocks * 18u; float acc = 0.0f;\n"
           "    for (uint b = 0; b < p.row_blocks; ++b) { uint off = row_offset + b * 18u; ushort bits = (ushort(weight[off + 1]) << 8) | ushort(weight[off]); float d = float(as_type<half>(bits)); float signed_sum = 0.0f; uint in_off = r * p.in_dim + b * 128u; for (uint by = 0; by < 16u; ++by) { uchar sbits = weight[off + 2u + by]; uint base = in_off + by * 8u; signed_sum += (sbits & 0x01u) != 0u ? input[base] : -input[base]; signed_sum += (sbits & 0x02u) != 0u ? input[base + 1u] : -input[base + 1u]; signed_sum += (sbits & 0x04u) != 0u ? input[base + 2u] : -input[base + 2u]; signed_sum += (sbits & 0x08u) != 0u ? input[base + 3u] : -input[base + 3u]; signed_sum += (sbits & 0x10u) != 0u ? input[base + 4u] : -input[base + 4u]; signed_sum += (sbits & 0x20u) != 0u ? input[base + 5u] : -input[base + 5u]; signed_sum += (sbits & 0x40u) != 0u ? input[base + 6u] : -input[base + 6u]; signed_sum += (sbits & 0x80u) != 0u ? input[base + 7u] : -input[base + 7u]; } acc += d * signed_sum; }\n"
           "    output[gid] = acc;\n"
           "}\n"
           "kernel void termite_i8_s_linear(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.out_dim; if (gid >= total) return; uint r = gid / p.out_dim; uint o = gid % p.out_dim; uint row_offset = o * p.row_blocks; float acc = 0.0f;\n"
           "    for (uint b = 0; b < p.row_blocks; ++b) { char q = as_type<char>(weight[row_offset + b]); acc += input[r * p.in_dim + b] * float(q); }\n"
           "    output[gid] = acc;\n"
           "}\n"
           "kernel void termite_q4_0_linear(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.out_dim; if (gid >= total) return; uint r = gid / p.out_dim; uint o = gid % p.out_dim; uint row_offset = o * p.row_blocks * 18u; float acc = 0.0f;\n"
           "    for (uint b = 0; b < p.row_blocks; ++b) { uint off = row_offset + b * 18u; ushort bits = (ushort(weight[off + 1]) << 8) | ushort(weight[off]); float d = float(as_type<half>(bits)); const device uchar *qs = weight + off + 2u; uint in_off = r * p.in_dim + b * 32u; for (uint i = 0; i < 16; ++i) { uchar packed = qs[i]; float lo = float(int(packed & 0x0Fu) - 8); acc += input[in_off + i] * (d * lo); } for (uint i = 0; i < 16; ++i) { uchar packed = qs[i]; float hi = float(int((packed >> 4) & 0x0Fu) - 8); acc += input[in_off + 16u + i] * (d * hi); } }\n"
           "    output[gid] = acc;\n"
           "}\n"
           "kernel void termite_q4_0_pair_linear(device const float *input [[buffer(0)]], device const uchar *weight_a [[buffer(1)]], device const uchar *weight_b [[buffer(2)]], device float *output_a [[buffer(3)]], device float *output_b [[buffer(4)]], constant termite_metal_linear_params &p [[buffer(5)]], uint2 tid [[thread_position_in_grid]]) {\n"
           "    uint o = tid.x; uint r = tid.y; if (o >= p.out_dim || r >= p.rows) return; uint row_offset = o * p.row_blocks * 18u; float acc_a = 0.0f; float acc_b = 0.0f;\n"
           "    for (uint b = 0; b < p.row_blocks; ++b) { uint off = row_offset + b * 18u; ushort bits_a = (ushort(weight_a[off + 1]) << 8) | ushort(weight_a[off]); ushort bits_b = (ushort(weight_b[off + 1]) << 8) | ushort(weight_b[off]); float d_a = float(as_type<half>(bits_a)); float d_b = float(as_type<half>(bits_b)); const device uchar *qs_a = weight_a + off + 2u; const device uchar *qs_b = weight_b + off + 2u; uint in_off = r * p.in_dim + b * 32u; for (uint i = 0; i < 16; ++i) { float x_lo = input[in_off + i]; float x_hi = input[in_off + 16u + i]; uchar packed_a = qs_a[i]; uchar packed_b = qs_b[i]; float lo_a = float(int(packed_a & 0x0Fu) - 8); float hi_a = float(int((packed_a >> 4) & 0x0Fu) - 8); float lo_b = float(int(packed_b & 0x0Fu) - 8); float hi_b = float(int((packed_b >> 4) & 0x0Fu) - 8); acc_a += x_lo * (d_a * lo_a) + x_hi * (d_a * hi_a); acc_b += x_lo * (d_b * lo_b) + x_hi * (d_b * hi_b); } }\n"
           "    output_a[r * p.out_dim + o] = acc_a;\n"
           "    output_b[r * p.out_dim + o] = acc_b;\n"
           "}\n"
           "kernel void termite_q4_0_linear_1x_reduce(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], threadgroup float *shmem [[threadgroup(0)]], ushort lane [[thread_index_in_simdgroup]], ushort sgitg [[simdgroup_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) {\n"
           "    const uint NR0 = 4u; const uint NSG = 2u; const uint NQ = 8u; const uint NW = 32u; uint first_o = tg.x * NR0; uint r = tg.y; if (r >= p.rows || lane >= NW) return;\n"
           "    uint ix = lane / (NW / NQ); uint il = lane - ix * (NW / NQ); uint ib0 = sgitg * NQ + ix; uint in_row = r * p.in_dim; float acc0 = 0.0f; float acc1 = 0.0f; float acc2 = 0.0f; float acc3 = 0.0f;\n"
           "    for (uint b = ib0; b < p.row_blocks; b += NSG * NQ) { float x[8]; uint qj0 = il * NQ; uint in_off = in_row + b * 32u + qj0; for (uint i = 0; i < NQ; ++i) { x[i] = input[in_off + i]; } for (uint row = 0; row < NR0; ++row) { uint o = first_o + row; if (o >= p.out_dim) continue; uint off = o * p.row_blocks * 18u + b * 18u; ushort bits = (ushort(weight[off + 1]) << 8) | ushort(weight[off]); float d = float(as_type<half>(bits)); const device uchar *qs = weight + off + 2u; float block = 0.0f; for (uint i = 0; i < NQ; ++i) { uint j = qj0 + i; uchar packed = qs[j & 15u]; float q = j < 16u ? float(int(packed & 0x0Fu) - 8) : float(int((packed >> 4) & 0x0Fu) - 8); block += x[i] * q; } if (row == 0u) acc0 += d * block; else if (row == 1u) acc1 += d * block; else if (row == 2u) acc2 += d * block; else acc3 += d * block; } }\n"
           "    if (sgitg == 0u) { shmem[lane] = 0.0f; shmem[32u + lane] = 0.0f; shmem[64u + lane] = 0.0f; shmem[96u + lane] = 0.0f; } acc0 = simd_sum(acc0); acc1 = simd_sum(acc1); acc2 = simd_sum(acc2); acc3 = simd_sum(acc3); threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    if (lane == 0u) { shmem[sgitg] = acc0; shmem[32u + sgitg] = acc1; shmem[64u + sgitg] = acc2; shmem[96u + sgitg] = acc3; } threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    float total0 = simd_sum(shmem[lane]); float total1 = simd_sum(shmem[32u + lane]); float total2 = simd_sum(shmem[64u + lane]); float total3 = simd_sum(shmem[96u + lane]); if (lane == 0u && sgitg == 0u) { uint idx = r * p.out_dim + first_o; if (first_o < p.out_dim) output[idx] = total0; if (first_o + 1u < p.out_dim) output[idx + 1u] = total1; if (first_o + 2u < p.out_dim) output[idx + 2u] = total2; if (first_o + 3u < p.out_dim) output[idx + 3u] = total3; }\n"
           "}\n"
           "kernel void termite_q4_1_linear(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.out_dim; if (gid >= total) return; uint r = gid / p.out_dim; uint o = gid % p.out_dim; uint row_offset = o * p.row_blocks * 20u; float acc = 0.0f;\n"
           "    for (uint b = 0; b < p.row_blocks; ++b) { uint off = row_offset + b * 20u; ushort d_bits = (ushort(weight[off + 1]) << 8) | ushort(weight[off]); ushort m_bits = (ushort(weight[off + 3]) << 8) | ushort(weight[off + 2]); float d = float(as_type<half>(d_bits)); float m = float(as_type<half>(m_bits)); const device uchar *qs = weight + off + 4u; uint in_off = r * p.in_dim + b * 32u; for (uint i = 0; i < 16; ++i) { uchar packed = qs[i]; float lo = float(int(packed & 0x0Fu)); acc += input[in_off + i] * (d * lo + m); } for (uint i = 0; i < 16; ++i) { uchar packed = qs[i]; float hi = float(int((packed >> 4) & 0x0Fu)); acc += input[in_off + 16u + i] * (d * hi + m); } }\n"
           "    output[gid] = acc;\n"
           "}\n"
           "kernel void termite_q4_1_linear_1x_reduce(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], threadgroup float *shmem [[threadgroup(0)]], ushort lane [[thread_index_in_simdgroup]], ushort sgitg [[simdgroup_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) {\n"
           "    const uint NR0 = 4u; const uint NSG = 2u; const uint NQ = 8u; const uint NW = 32u; uint first_o = tg.x * NR0; uint r = tg.y; if (r >= p.rows || lane >= NW) return;\n"
           "    uint ix = lane / (NW / NQ); uint il = lane - ix * (NW / NQ); uint ib0 = uint(sgitg) * NQ + ix; uint in_row = r * p.in_dim; float acc0 = 0.0f; float acc1 = 0.0f; float acc2 = 0.0f; float acc3 = 0.0f;\n"
           "    for (uint b = ib0; b < p.row_blocks; b += NSG * NQ) { float x[8]; uint qj0 = il * NQ; uint in_off = in_row + b * 32u + qj0; for (uint i = 0u; i < NQ; ++i) { x[i] = input[in_off + i]; } for (uint row = 0u; row < NR0; ++row) { uint o = first_o + row; if (o >= p.out_dim) continue; uint off = o * p.row_blocks * 20u + b * 20u; ushort d_bits = (ushort(weight[off + 1u]) << 8) | ushort(weight[off]); ushort m_bits = (ushort(weight[off + 3u]) << 8) | ushort(weight[off + 2u]); float d = float(as_type<half>(d_bits)); float m = float(as_type<half>(m_bits)); const device uchar *qs = weight + off + 4u; float block = 0.0f; for (uint i = 0u; i < NQ; ++i) { uint j = qj0 + i; uchar packed = qs[j & 15u]; float q = j < 16u ? float(int(packed & 0x0Fu)) : float(int((packed >> 4) & 0x0Fu)); block += x[i] * (d * q + m); } if (row == 0u) acc0 += block; else if (row == 1u) acc1 += block; else if (row == 2u) acc2 += block; else acc3 += block; } }\n"
           "    if (sgitg == 0u) { shmem[lane] = 0.0f; shmem[32u + lane] = 0.0f; shmem[64u + lane] = 0.0f; shmem[96u + lane] = 0.0f; } acc0 = simd_sum(acc0); acc1 = simd_sum(acc1); acc2 = simd_sum(acc2); acc3 = simd_sum(acc3); threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    if (lane == 0u) { shmem[sgitg] = acc0; shmem[32u + sgitg] = acc1; shmem[64u + sgitg] = acc2; shmem[96u + sgitg] = acc3; } threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    float total0 = simd_sum(shmem[lane]); float total1 = simd_sum(shmem[32u + lane]); float total2 = simd_sum(shmem[64u + lane]); float total3 = simd_sum(shmem[96u + lane]); if (lane == 0u && sgitg == 0u) { uint idx = r * p.out_dim + first_o; if (first_o < p.out_dim) output[idx] = total0; if (first_o + 1u < p.out_dim) output[idx + 1u] = total1; if (first_o + 2u < p.out_dim) output[idx + 2u] = total2; if (first_o + 3u < p.out_dim) output[idx + 3u] = total3; }\n"
           "}\n"
           "kernel void termite_q5_0_linear(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.out_dim; if (gid >= total) return; uint r = gid / p.out_dim; uint o = gid % p.out_dim; uint row_offset = o * p.row_blocks * 22u; float acc = 0.0f;\n"
           "    for (uint b = 0; b < p.row_blocks; ++b) { uint off = row_offset + b * 22u; ushort bits = (ushort(weight[off + 1]) << 8) | ushort(weight[off]); float d = float(as_type<half>(bits)); uint qh = uint(weight[off + 2]) | (uint(weight[off + 3]) << 8) | (uint(weight[off + 4]) << 16) | (uint(weight[off + 5]) << 24); const device uchar *qs = weight + off + 6u; uint in_off = r * p.in_dim + b * 32u; for (uint i = 0; i < 16; ++i) { int lo4 = int(qs[i] & 0x0Fu); int hi1 = int((qh >> i) & 1u); acc += input[in_off + i] * (d * float((lo4 | (hi1 << 4)) - 16)); } for (uint i = 0; i < 16; ++i) { int lo4 = int(qs[i] >> 4); int hi1 = int((qh >> (i + 16u)) & 1u); acc += input[in_off + 16u + i] * (d * float((lo4 | (hi1 << 4)) - 16)); } }\n"
           "    output[gid] = acc;\n"
           "}\n"
           "kernel void termite_q5_0_linear_1x_reduce(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], threadgroup float *shmem [[threadgroup(0)]], ushort lane [[thread_index_in_simdgroup]], ushort sgitg [[simdgroup_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) {\n"
           "    const uint NR0 = 2u; const uint NSG = 4u; const uint NQ = 8u; const uint NW = 32u; uint first_o = tg.x * NR0; uint r = tg.y; if (r >= p.rows || lane >= NW) return;\n"
           "    uint ix = lane / (NW / NQ); uint il = lane - ix * (NW / NQ); uint ib0 = uint(sgitg) * NQ + ix; uint in_row = r * p.in_dim; float acc0 = 0.0f; float acc1 = 0.0f;\n"
           "    for (uint b = ib0; b < p.row_blocks; b += NSG * NQ) { float x[8]; uint qj0 = il * NQ; uint in_off = in_row + b * 32u + qj0; for (uint i = 0u; i < NQ; ++i) { x[i] = input[in_off + i]; } for (uint row = 0u; row < NR0; ++row) { uint o = first_o + row; if (o >= p.out_dim) continue; uint off = o * p.row_blocks * 22u + b * 22u; ushort bits = (ushort(weight[off + 1u]) << 8) | ushort(weight[off]); float d = float(as_type<half>(bits)); uint qh = uint(weight[off + 2u]) | (uint(weight[off + 3u]) << 8) | (uint(weight[off + 4u]) << 16) | (uint(weight[off + 5u]) << 24); const device uchar *qs = weight + off + 6u; float block = 0.0f; for (uint i = 0u; i < NQ; ++i) { uint j = qj0 + i; uchar packed = qs[j & 15u]; int lo4 = j < 16u ? int(packed & 0x0Fu) : int(packed >> 4); int hi1 = int((qh >> j) & 1u); block += x[i] * float((lo4 | (hi1 << 4)) - 16); } if (row == 0u) acc0 += d * block; else acc1 += d * block; } }\n"
           "    if (sgitg == 0u) { shmem[lane] = 0.0f; shmem[32u + lane] = 0.0f; } acc0 = simd_sum(acc0); acc1 = simd_sum(acc1); threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    if (lane == 0u) { shmem[sgitg] = acc0; shmem[32u + sgitg] = acc1; } threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    float total0 = simd_sum(shmem[lane]); float total1 = simd_sum(shmem[32u + lane]); if (lane == 0u && sgitg == 0u) { uint idx = r * p.out_dim + first_o; if (first_o < p.out_dim) output[idx] = total0; if (first_o + 1u < p.out_dim) output[idx + 1u] = total1; }\n"
           "}\n"
           "kernel void termite_q5_1_linear(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.out_dim; if (gid >= total) return; uint r = gid / p.out_dim; uint o = gid % p.out_dim; uint row_offset = o * p.row_blocks * 24u; float acc = 0.0f;\n"
           "    for (uint b = 0; b < p.row_blocks; ++b) { uint off = row_offset + b * 24u; ushort d_bits = (ushort(weight[off + 1]) << 8) | ushort(weight[off]); ushort m_bits = (ushort(weight[off + 3]) << 8) | ushort(weight[off + 2]); float d = float(as_type<half>(d_bits)); float m = float(as_type<half>(m_bits)); uint qh = uint(weight[off + 4]) | (uint(weight[off + 5]) << 8) | (uint(weight[off + 6]) << 16) | (uint(weight[off + 7]) << 24); const device uchar *qs = weight + off + 8u; uint in_off = r * p.in_dim + b * 32u; for (uint i = 0; i < 16; ++i) { int lo4 = int(qs[i] & 0x0Fu); int hi1 = int((qh >> i) & 1u); acc += input[in_off + i] * (d * float(lo4 | (hi1 << 4)) + m); } for (uint i = 0; i < 16; ++i) { int lo4 = int(qs[i] >> 4); int hi1 = int((qh >> (i + 16u)) & 1u); acc += input[in_off + 16u + i] * (d * float(lo4 | (hi1 << 4)) + m); } }\n"
           "    output[gid] = acc;\n"
           "}\n"
           "kernel void termite_q5_1_linear_1x_reduce(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], threadgroup float *shmem [[threadgroup(0)]], ushort lane [[thread_index_in_simdgroup]], ushort sgitg [[simdgroup_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) {\n"
           "    const uint NR0 = 2u; const uint NSG = 4u; const uint NQ = 8u; const uint NW = 32u; uint first_o = tg.x * NR0; uint r = tg.y; if (r >= p.rows || lane >= NW) return;\n"
           "    uint ix = lane / (NW / NQ); uint il = lane - ix * (NW / NQ); uint ib0 = uint(sgitg) * NQ + ix; uint in_row = r * p.in_dim; float acc0 = 0.0f; float acc1 = 0.0f;\n"
           "    for (uint b = ib0; b < p.row_blocks; b += NSG * NQ) { float x[8]; uint qj0 = il * NQ; uint in_off = in_row + b * 32u + qj0; for (uint i = 0u; i < NQ; ++i) { x[i] = input[in_off + i]; } for (uint row = 0u; row < NR0; ++row) { uint o = first_o + row; if (o >= p.out_dim) continue; uint off = o * p.row_blocks * 24u + b * 24u; ushort d_bits = (ushort(weight[off + 1u]) << 8) | ushort(weight[off]); ushort m_bits = (ushort(weight[off + 3u]) << 8) | ushort(weight[off + 2u]); float d = float(as_type<half>(d_bits)); float m = float(as_type<half>(m_bits)); uint qh = uint(weight[off + 4u]) | (uint(weight[off + 5u]) << 8) | (uint(weight[off + 6u]) << 16) | (uint(weight[off + 7u]) << 24); const device uchar *qs = weight + off + 8u; float block = 0.0f; for (uint i = 0u; i < NQ; ++i) { uint j = qj0 + i; uchar packed = qs[j & 15u]; int lo4 = j < 16u ? int(packed & 0x0Fu) : int(packed >> 4); int hi1 = int((qh >> j) & 1u); block += x[i] * (d * float(lo4 | (hi1 << 4)) + m); } if (row == 0u) acc0 += block; else acc1 += block; } }\n"
           "    if (sgitg == 0u) { shmem[lane] = 0.0f; shmem[32u + lane] = 0.0f; } acc0 = simd_sum(acc0); acc1 = simd_sum(acc1); threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    if (lane == 0u) { shmem[sgitg] = acc0; shmem[32u + sgitg] = acc1; } threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    float total0 = simd_sum(shmem[lane]); float total1 = simd_sum(shmem[32u + lane]); if (lane == 0u && sgitg == 0u) { uint idx = r * p.out_dim + first_o; if (first_o < p.out_dim) output[idx] = total0; if (first_o + 1u < p.out_dim) output[idx + 1u] = total1; }\n"
           "}\n"
           "inline float termite_q4_0_block_scale(device const uchar *weight, uint off) { ushort bits = (ushort(weight[off + 1u]) << 8) | ushort(weight[off]); return float(as_type<half>(bits)); }\n"
           "inline float termite_q4_0_block_value(device const uchar *weight, uint off, uint i) { const device uchar *qs = weight + off + 2u; uchar packed = qs[i & 15u]; return i < 16u ? float(int(packed & 0x0Fu) - 8) : float(int((packed >> 4) & 0x0Fu) - 8); }\n"
           "inline float termite_q4_1_block_scale(device const uchar *weight, uint off) { ushort bits = (ushort(weight[off + 1u]) << 8) | ushort(weight[off]); return float(as_type<half>(bits)); }\n"
           "inline float termite_q4_1_block_min(device const uchar *weight, uint off) { ushort bits = (ushort(weight[off + 3u]) << 8) | ushort(weight[off + 2u]); return float(as_type<half>(bits)); }\n"
           "inline float termite_q4_1_block_value(device const uchar *weight, uint off, uint i) { const device uchar *qs = weight + off + 4u; uchar packed = qs[i & 15u]; float q = i < 16u ? float(packed & 0x0Fu) : float((packed >> 4) & 0x0Fu); return termite_q4_1_block_scale(weight, off) * q + termite_q4_1_block_min(weight, off); }\n"
           "inline float termite_q5_0_block_scale(device const uchar *weight, uint off) { ushort bits = (ushort(weight[off + 1u]) << 8) | ushort(weight[off]); return float(as_type<half>(bits)); }\n"
           "inline float termite_q5_0_block_value(device const uchar *weight, uint off, uint i) { uint qh = uint(weight[off + 2u]) | (uint(weight[off + 3u]) << 8) | (uint(weight[off + 4u]) << 16) | (uint(weight[off + 5u]) << 24); const device uchar *qs = weight + off + 6u; uchar packed = qs[i & 15u]; int lo4 = i < 16u ? int(packed & 0x0Fu) : int(packed >> 4); int hi1 = int((qh >> i) & 1u); return float((lo4 | (hi1 << 4)) - 16); }\n"
           "inline float termite_q5_1_block_scale(device const uchar *weight, uint off) { ushort bits = (ushort(weight[off + 1u]) << 8) | ushort(weight[off]); return float(as_type<half>(bits)); }\n"
           "inline float termite_q5_1_block_min(device const uchar *weight, uint off) { ushort bits = (ushort(weight[off + 3u]) << 8) | ushort(weight[off + 2u]); return float(as_type<half>(bits)); }\n"
           "inline float termite_q5_1_block_value(device const uchar *weight, uint off, uint i) { uint qh = uint(weight[off + 4u]) | (uint(weight[off + 5u]) << 8) | (uint(weight[off + 6u]) << 16) | (uint(weight[off + 7u]) << 24); const device uchar *qs = weight + off + 8u; uchar packed = qs[i & 15u]; int lo4 = i < 16u ? int(packed & 0x0Fu) : int(packed >> 4); int hi1 = int((qh >> i) & 1u); return termite_q5_1_block_scale(weight, off) * float(lo4 | (hi1 << 4)) + termite_q5_1_block_min(weight, off); }\n"
           "inline float termite_q4_k_block_value(device const uchar *weight, uint off, uint i) { ushort d_bits = (ushort(weight[off + 1u]) << 8) | ushort(weight[off]); ushort dmin_bits = (ushort(weight[off + 3u]) << 8) | ushort(weight[off + 2u]); float d = float(as_type<half>(d_bits)); float dmin = float(as_type<half>(dmin_bits)); const device uchar *scales = weight + off + 4u; const device uchar *qs = weight + off + 16u; thread float scs[8]; thread float mins[8]; unpack_q4k_scale_mins(scales, scs, mins); uint sub = i / 32u; uint lane = i & 31u; uint chunk = sub >> 1; bool high = (sub & 1u) != 0u; uchar packed = qs[chunk * 32u + lane]; float q = high ? float(packed >> 4) : float(packed & 0x0Fu); return d * scs[sub] * q - dmin * mins[sub]; }\n"
           "inline float termite_q5_k_block_value(device const uchar *weight, uint off, uint i) { ushort d_bits = (ushort(weight[off + 1u]) << 8) | ushort(weight[off]); ushort dmin_bits = (ushort(weight[off + 3u]) << 8) | ushort(weight[off + 2u]); float d = float(as_type<half>(d_bits)); float dmin = float(as_type<half>(dmin_bits)); const device uchar *scales = weight + off + 4u; const device uchar *qh = weight + off + 16u; const device uchar *ql = weight + off + 48u; thread float scs[8]; thread float mins[8]; unpack_q4k_scale_mins(scales, scs, mins); uint sub = i / 32u; uint lane = i & 31u; uint chunk = sub >> 1; bool high = (sub & 1u) != 0u; uchar low = high ? (ql[chunk * 32u + lane] >> 4) : (ql[chunk * 32u + lane] & 0x0Fu); int q = int(low) + int((qh[lane] >> sub) & 1u) * 16; return d * scs[sub] * float(q) - dmin * mins[sub]; }\n"
           "inline float termite_q6_k_block_value(device const uchar *weight, uint off, uint i) { const device uchar *ql = weight + off; const device uchar *qh = weight + off + 128u; const device char *scales = reinterpret_cast<const device char *>(weight + off + 192u); ushort d_bits = (ushort(weight[off + 209u]) << 8) | ushort(weight[off + 208u]); float d = float(as_type<half>(d_bits)); uint sub = i / 16u; uint j = i & 15u; uint half_idx = sub / 8u; uint group = (sub % 8u) / 2u; uint l = (sub % 2u) * 16u + j; uint ql_off = half_idx * 64u + (group & 1u) * 32u; uint qh_off = half_idx * 32u; uint qh_shift = group * 2u; uint nibble_shift = (group / 2u) * 4u; int low4 = int((ql[ql_off + l] >> nibble_shift) & 0x0Fu); int high2 = int((qh[qh_off + l] >> qh_shift) & 0x03u); int q = (low4 | (high2 << 4)) - 32; return d * float(scales[sub]) * float(q); }\n"
           "inline float termite_q8_0_block_scale(device const uchar *weight, uint off) { ushort bits = (ushort(weight[off + 1u]) << 8) | ushort(weight[off]); return float(as_type<half>(bits)); }\n"
           "inline float termite_q8_0_block_value(device const uchar *weight, uint off, uint i) { return float(as_type<char>(weight[off + 2u + i])); }\n"
           "inline float termite_q8_1_block_scale(device const uchar *weight, uint off) { ushort bits = (ushort(weight[off + 1u]) << 8) | ushort(weight[off]); return float(as_type<half>(bits)); }\n"
           "inline float termite_q8_1_block_value(device const uchar *weight, uint off, uint i) { return float(as_type<char>(weight[off + 4u + i])); }\n"
           "kernel void termite_q8_0_linear(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.out_dim; if (gid >= total) return; uint r = gid / p.out_dim; uint o = gid % p.out_dim; uint row_offset = o * p.row_blocks * 34u; float acc = 0.0f;\n"
           "    for (uint b = 0; b < p.row_blocks; ++b) { uint off = row_offset + b * 34u; float d = termite_q8_0_block_scale(weight, off); float block_acc = 0.0f; uint in_off = r * p.in_dim + b * 32u; for (uint i = 0; i < 32; ++i) { block_acc += input[in_off + i] * termite_q8_0_block_value(weight, off, i); } acc += d * block_acc; }\n"
           "    output[gid] = acc;\n"
           "}\n"
           "kernel void termite_apply_linear_bias_2d(device float *output [[buffer(0)]], device const float *bias [[buffer(1)]], constant termite_metal_linear_params &p [[buffer(2)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.out_dim; if (gid >= total || p.out_dim == 0u) return;\n"
           "    uint col = gid - (gid / p.out_dim) * p.out_dim;\n"
           "    output[gid] += bias[col];\n"
           "}\n"
           "kernel void termite_q8_0_pair_linear(device const float *input [[buffer(0)]], device const uchar *weight_a [[buffer(1)]], device const uchar *weight_b [[buffer(2)]], device float *output_a [[buffer(3)]], device float *output_b [[buffer(4)]], constant termite_metal_linear_params &p [[buffer(5)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.out_dim; if (gid >= total) return; uint r = gid / p.out_dim; uint o = gid % p.out_dim; uint row_offset = o * p.row_blocks * 34u; float acc_a = 0.0f; float acc_b = 0.0f;\n"
           "    for (uint b = 0; b < p.row_blocks; ++b) { uint off = row_offset + b * 34u; float d_a = termite_q8_0_block_scale(weight_a, off); float d_b = termite_q8_0_block_scale(weight_b, off); float block_acc_a = 0.0f; float block_acc_b = 0.0f; uint in_off = r * p.in_dim + b * 32u; for (uint i = 0; i < 32; ++i) { float x = input[in_off + i]; block_acc_a += x * termite_q8_0_block_value(weight_a, off, i); block_acc_b += x * termite_q8_0_block_value(weight_b, off, i); } acc_a += d_a * block_acc_a; acc_b += d_b * block_acc_b; }\n"
           "    output_a[gid] = acc_a; output_b[gid] = acc_b;\n"
           "}\n"
           "kernel void termite_q8_1_linear(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.out_dim; if (gid >= total) return; uint r = gid / p.out_dim; uint o = gid % p.out_dim; uint row_offset = o * p.row_blocks * 36u; float acc = 0.0f;\n"
           "    for (uint b = 0; b < p.row_blocks; ++b) { uint off = row_offset + b * 36u; ushort bits = (ushort(weight[off + 1]) << 8) | ushort(weight[off]); float d = float(as_type<half>(bits)); float block_acc = 0.0f; uint in_off = r * p.in_dim + b * 32u; for (uint i = 0; i < 32; ++i) { char q = as_type<char>(weight[off + 4u + i]); block_acc += input[in_off + i] * float(q); } acc += d * block_acc; }\n"
           "    output[gid] = acc;\n"
           "}\n"
           "kernel void termite_q8_1_linear_1x_reduce(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], threadgroup float *shmem [[threadgroup(0)]], ushort lane [[thread_index_in_simdgroup]], ushort sgitg [[simdgroup_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) {\n"
           "    const uint NR0 = 2u; const uint NSG = 4u; const uint NQ = 8u; const uint NW = 32u; uint first_o = tg.x * NR0; uint r = tg.y; if (r >= p.rows || lane >= NW) return;\n"
           "    uint ix = lane / (NW / NQ); uint il = lane - ix * (NW / NQ); uint ib0 = uint(sgitg) * NQ + ix; uint in_row = r * p.in_dim; float acc0 = 0.0f; float acc1 = 0.0f;\n"
           "    for (uint b = ib0; b < p.row_blocks; b += NSG * NQ) { float x[8]; uint qj0 = il * NQ; uint in_off = in_row + b * 32u + qj0; for (uint i = 0u; i < NQ; ++i) { x[i] = input[in_off + i]; } for (uint row = 0u; row < NR0; ++row) { uint o = first_o + row; if (o >= p.out_dim) continue; uint off = o * p.row_blocks * 36u + b * 36u; ushort bits = (ushort(weight[off + 1u]) << 8) | ushort(weight[off]); float d = float(as_type<half>(bits)); float block = 0.0f; for (uint i = 0u; i < NQ; ++i) { char q = as_type<char>(weight[off + 4u + qj0 + i]); block += x[i] * float(q); } if (row == 0u) acc0 += d * block; else acc1 += d * block; } }\n"
           "    if (sgitg == 0u) { shmem[lane] = 0.0f; shmem[32u + lane] = 0.0f; } acc0 = simd_sum(acc0); acc1 = simd_sum(acc1); threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    if (lane == 0u) { shmem[sgitg] = acc0; shmem[32u + sgitg] = acc1; } threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    float total0 = simd_sum(shmem[lane]); float total1 = simd_sum(shmem[32u + lane]); if (lane == 0u && sgitg == 0u) { uint idx = r * p.out_dim + first_o; if (first_o < p.out_dim) output[idx] = total0; if (first_o + 1u < p.out_dim) output[idx + 1u] = total1; }\n"
           "}\n"
           "kernel void termite_q8_0_linear_mmv(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], threadgroup float *shmem [[threadgroup(0)]], ushort lane [[thread_index_in_simdgroup]], ushort sgitg [[simdgroup_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) {\n"
           "    const uint NR0 = 2u; const uint NSG = 4u; const uint NQ = 8u; const uint NW = 32u; uint first_o = tg.x * NR0; uint r = tg.y; if (r >= p.rows || lane >= NW) return;\n"
           "    uint ix = lane / (NW / NQ); uint il = lane - ix * (NW / NQ); uint ib0 = uint(sgitg) * NQ + ix; uint in_row = r * p.in_dim; float acc0 = 0.0f; float acc1 = 0.0f;\n"
           "    for (uint b = ib0; b < p.row_blocks; b += NSG * NQ) { float x[8]; uint in_off = in_row + b * 32u + il * NQ; for (uint i = 0u; i < NQ; ++i) { x[i] = input[in_off + i]; } for (uint row = 0u; row < NR0; ++row) { uint o = first_o + row; if (o >= p.out_dim) continue; uint off = o * p.row_blocks * 34u + b * 34u; ushort bits = (ushort(weight[off + 1u]) << 8) | ushort(weight[off]); float d = float(as_type<half>(bits)); float block_acc = 0.0f; for (uint i = 0u; i < NQ; ++i) { char q = as_type<char>(weight[off + 2u + il * NQ + i]); block_acc += x[i] * float(q); } if (row == 0u) acc0 += d * block_acc; else acc1 += d * block_acc; } }\n"
           "    if (sgitg == 0u) { shmem[lane] = 0.0f; shmem[32u + lane] = 0.0f; } acc0 = simd_sum(acc0); acc1 = simd_sum(acc1); threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    if (lane == 0u) { shmem[sgitg] = acc0; shmem[32u + sgitg] = acc1; } threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    float total0 = simd_sum(shmem[lane]); float total1 = simd_sum(shmem[32u + lane]); if (lane == 0u && sgitg == 0u) { uint idx = r * p.out_dim + first_o; if (first_o < p.out_dim) output[idx] = total0; if (first_o + 1u < p.out_dim) output[idx + 1u] = total1; }\n"
           "}\n"
           "kernel void termite_q8_0_linear_mmv_in_f16(device const half *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], threadgroup float *shmem [[threadgroup(0)]], ushort lane [[thread_index_in_simdgroup]], ushort sgitg [[simdgroup_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) {\n"
           "    const uint NR0 = 2u; const uint NSG = 4u; const uint NQ = 8u; const uint NW = 32u; uint first_o = tg.x * NR0; uint r = tg.y; if (r >= p.rows || lane >= NW) return;\n"
           "    uint ix = lane / (NW / NQ); uint il = lane - ix * (NW / NQ); uint ib0 = uint(sgitg) * NQ + ix; uint in_row = r * p.in_dim; float acc0 = 0.0f; float acc1 = 0.0f;\n"
           "    for (uint b = ib0; b < p.row_blocks; b += NSG * NQ) { float x[8]; uint in_off = in_row + b * 32u + il * NQ; for (uint i = 0u; i < NQ; ++i) { x[i] = float(input[in_off + i]); } for (uint row = 0u; row < NR0; ++row) { uint o = first_o + row; if (o >= p.out_dim) continue; uint off = o * p.row_blocks * 34u + b * 34u; ushort bits = (ushort(weight[off + 1u]) << 8) | ushort(weight[off]); float d = float(as_type<half>(bits)); float block_acc = 0.0f; for (uint i = 0u; i < NQ; ++i) { char q = as_type<char>(weight[off + 2u + il * NQ + i]); block_acc += x[i] * float(q); } if (row == 0u) acc0 += d * block_acc; else acc1 += d * block_acc; } }\n"
           "    if (sgitg == 0u) { shmem[lane] = 0.0f; shmem[32u + lane] = 0.0f; } acc0 = simd_sum(acc0); acc1 = simd_sum(acc1); threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    if (lane == 0u) { shmem[sgitg] = acc0; shmem[32u + sgitg] = acc1; } threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    float total0 = simd_sum(shmem[lane]); float total1 = simd_sum(shmem[32u + lane]); if (lane == 0u && sgitg == 0u) { uint idx = r * p.out_dim + first_o; if (first_o < p.out_dim) output[idx] = total0; if (first_o + 1u < p.out_dim) output[idx + 1u] = total1; }\n"
           "}\n"
           "kernel void termite_q8_0_linear_rms_scale_mmv(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], device const float *norm_weight [[buffer(4)]], device const float *inv_scale [[buffer(5)]], threadgroup float *shmem [[threadgroup(0)]], ushort lane [[thread_index_in_simdgroup]], ushort sgitg [[simdgroup_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) {\n"
           "    const uint NR0 = 2u; const uint NSG = 4u; const uint NQ = 8u; const uint NW = 32u; uint first_o = tg.x * NR0; uint r = tg.y; if (r >= p.rows || lane >= NW) return;\n"
           "    uint ix = lane / (NW / NQ); uint il = lane - ix * (NW / NQ); uint ib0 = uint(sgitg) * NQ + ix; uint in_row = r * p.in_dim; float scale = inv_scale[r]; float acc0 = 0.0f; float acc1 = 0.0f;\n"
           "    for (uint b = ib0; b < p.row_blocks; b += NSG * NQ) { float x[8]; uint in_off = in_row + b * 32u + il * NQ; uint norm_off = b * 32u + il * NQ; for (uint i = 0u; i < NQ; ++i) { x[i] = input[in_off + i] * norm_weight[norm_off + i] * scale; } for (uint row = 0u; row < NR0; ++row) { uint o = first_o + row; if (o >= p.out_dim) continue; uint off = o * p.row_blocks * 34u + b * 34u; ushort bits = (ushort(weight[off + 1u]) << 8) | ushort(weight[off]); float d = float(as_type<half>(bits)); float block_acc = 0.0f; for (uint i = 0u; i < NQ; ++i) { char q = as_type<char>(weight[off + 2u + il * NQ + i]); block_acc += x[i] * float(q); } if (row == 0u) acc0 += d * block_acc; else acc1 += d * block_acc; } }\n"
           "    if (sgitg == 0u) { shmem[lane] = 0.0f; shmem[32u + lane] = 0.0f; } acc0 = simd_sum(acc0); acc1 = simd_sum(acc1); threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    if (lane == 0u) { shmem[sgitg] = acc0; shmem[32u + sgitg] = acc1; } threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    float total0 = simd_sum(shmem[lane]); float total1 = simd_sum(shmem[32u + lane]); if (lane == 0u && sgitg == 0u) { uint idx = r * p.out_dim + first_o; if (first_o < p.out_dim) output[idx] = total0; if (first_o + 1u < p.out_dim) output[idx + 1u] = total1; }\n"
           "}\n"
           "inline void termite_q8_0_linear_r_ext_impl(device const float *input, device const uchar *weight, device float *output, constant termite_metal_linear_params &p, ushort lane, ushort sgitg, uint3 tg, uint RPTG) {\n"
           "    const uint NX = 8u; const uint NY = 4u; const uint NSG = 4u; const uint CHPT = 4u; uint tx = uint(lane) & 7u; uint ty = uint(lane) >> 3; uint o = tg.x * (NY * NSG) + uint(sgitg) * NY + ty; uint first_r = tg.y * RPTG; if (o >= p.out_dim) return; float acc[5]; for (uint i = 0u; i < 5u; ++i) acc[i] = 0.0f; uint row_offset = o * p.row_blocks * 34u; uint total_chunks = p.row_blocks * 8u;\n"
           "    for (uint chunk = tx; chunk < total_chunks; chunk += CHPT * NX) { for (uint ch = 0u; ch < CHPT; ++ch) { uint c = chunk + ch * NX; if (c >= total_chunks) continue; uint b = c >> 3; uint ci = c & 7u; uint off = row_offset + b * 34u; ushort bits = (ushort(weight[off + 1u]) << 8) | ushort(weight[off]); float d = float(as_type<half>(bits)); float4 w = float4(float(as_type<char>(weight[off + 2u + ci * 4u])), float(as_type<char>(weight[off + 3u + ci * 4u])), float(as_type<char>(weight[off + 4u + ci * 4u])), float(as_type<char>(weight[off + 5u + ci * 4u]))) * d; uint in_col = c * 4u; for (uint rr = 0u; rr < RPTG; ++rr) { uint r = first_r + rr; if (r >= p.rows) continue; uint in_off = r * p.in_dim + in_col; float4 x = float4(input[in_off], input[in_off + 1u], input[in_off + 2u], input[in_off + 3u]); acc[rr] += dot(x, w); } } }\n"
           "    for (uint rr = 0u; rr < RPTG; ++rr) { float total = acc[rr]; total += simd_shuffle_down(total, 4u); total += simd_shuffle_down(total, 2u); total += simd_shuffle_down(total, 1u); uint r = first_r + rr; if (tx == 0u && r < p.rows) output[r * p.out_dim + o] = total; }\n"
           "}\n"
           "kernel void termite_q8_0_linear_r2_ext(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], ushort lane [[thread_index_in_simdgroup]], ushort sgitg [[simdgroup_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) { termite_q8_0_linear_r_ext_impl(input, weight, output, p, lane, sgitg, tg, 2u); }\n"
           "kernel void termite_q8_0_linear_r3_ext(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], ushort lane [[thread_index_in_simdgroup]], ushort sgitg [[simdgroup_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) { termite_q8_0_linear_r_ext_impl(input, weight, output, p, lane, sgitg, tg, 3u); }\n"
           "kernel void termite_q8_0_linear_r4_ext(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], ushort lane [[thread_index_in_simdgroup]], ushort sgitg [[simdgroup_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) { termite_q8_0_linear_r_ext_impl(input, weight, output, p, lane, sgitg, tg, 4u); }\n"
           "kernel void termite_q8_0_linear_r5_ext(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], ushort lane [[thread_index_in_simdgroup]], ushort sgitg [[simdgroup_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) { termite_q8_0_linear_r_ext_impl(input, weight, output, p, lane, sgitg, tg, 5u); }\n"
           "struct termite_block_q8_0 { half d; char qs[32]; };\n"
           "inline void termite_dequantize_q8_0_4x4(device const termite_block_q8_0 *xb, short il, thread float4x4 &reg) {\n"
           "    float d = float(xb->d); for (short i = 0; i < 16; ++i) reg[i / 4][i % 4] = float(xb->qs[i + 16 * il]) * d;\n"
           "}\n"
           "inline void termite_dequantize_q8_0_4x4_half(device const termite_block_q8_0 *xb, short il, thread half4x4 &reg) {\n"
           "    float d = float(xb->d); for (short i = 0; i < 16; ++i) reg[i / 4][i % 4] = half(float(xb->qs[i + 16 * il]) * d);\n"
           "}\n"
           "kernel void termite_q8_0_linear_mm_sg(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], threadgroup char *shmem [[threadgroup(0)]], ushort tiitg [[thread_index_in_threadgroup]], ushort sgitg [[simdgroup_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) {\n"
           "    threadgroup half *sa = (threadgroup half *)(shmem); threadgroup half *sb = (threadgroup half *)(shmem + 4096u);\n"
           "    const short NR0 = 64; const short NR1 = 32; const short NK = 32; const short NL0 = 2; const short NL1 = 4;\n"
           "    const int r0 = int(tg.x) * NR0; const int r1 = int(tg.y) * NR1; if (r0 >= int(p.out_dim) || r1 >= int(p.rows)) return;\n"
           "    const short nr0 = short(min(uint(NR0), p.out_dim - uint(r0))); const short nr1 = short(min(uint(NR1), p.rows - uint(r1)));\n"
           "    const short lr0 = short(min(int(tiitg) / int(NL0), int(nr0) - 1)); const short lr1 = short(min(int(tiitg) / int(NL1), int(nr1) - 1)); const short il0 = short(tiitg) % NL0; short il = il0;\n"
           "    device const termite_block_q8_0 *x = (device const termite_block_q8_0 *)(weight + (uint(r0 + lr0) * p.row_blocks * 34u)); const short iy = 8 * (short(tiitg) % NL1);\n"
           "    simdgroup_half8x8 ma[4]; simdgroup_half8x8 mb[2]; simdgroup_float8x8 mc[8];\n"
           "    for (short i = 0; i < 8; ++i) mc[i] = make_filled_simdgroup_matrix<float, 8>(0.0f);\n"
           "    for (uint loop_k = 0u; loop_k < p.in_dim; loop_k += uint(NK)) {\n"
           "        half4x4 temp_a; termite_dequantize_q8_0_4x4_half(x, il, temp_a); threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "        for (short i = 0; i < 16; ++i) { const short sx = 2 * il0 + i / 8; const short sy = (short(tiitg) / NL0) / 8; const short lx = (short(tiitg) / NL0) % 8; const short ly = i % 8; const short ib = 8 * sx + sy; *(sa + 64 * ib + 8 * ly + lx) = temp_a[i / 4][i % 4]; }\n"
           "        for (short i = 0; i < 8; ++i) { const short sx = short(tiitg) % NL1; const short sy = (short(tiitg) / NL1) / 8; const short lx = i; const short ly = (short(tiitg) / NL1) % 8; const short ib = 4 * sx + sy; uint k = loop_k + uint(iy + i); *(sb + 64 * ib + 8 * ly + lx) = k < p.in_dim ? half(input[uint(r1 + lr1) * p.in_dim + k]) : half(0.0f); }\n"
           "        il = (il + 2 < 2) ? il + 2 : il % 2; x = (il < 2) ? x + 1 : x; threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "        threadgroup const half *lsma = sa + 4 * 64 * (uint(sgitg) % 2u); threadgroup const half *lsmb = sb + 2 * 64 * (uint(sgitg) / 2u);\n"
           "        for (short ik = 0; ik < NK / 8; ++ik) { simdgroup_barrier(mem_flags::mem_none); for (short i = 0; i < 4; ++i) simdgroup_load(ma[i], lsma + 64 * i, 8, 0, false); simdgroup_barrier(mem_flags::mem_none); for (short i = 0; i < 2; ++i) simdgroup_load(mb[i], lsmb + 64 * i, 8, 0, false); simdgroup_barrier(mem_flags::mem_none); for (short i = 0; i < 8; ++i) simdgroup_multiply_accumulate(mc[i], mb[i / 4], ma[i % 4], mc[i]); lsma += 8 * 64; lsmb += 4 * 64; }\n"
           "    }\n"
           "    if (r0 + NR0 <= int(p.out_dim) && r1 + NR1 <= int(p.rows)) { device float *C = output + uint(r1) * p.out_dim + uint(r0) + 32u * (uint(sgitg) & 1u) + 16u * (uint(sgitg) >> 1u) * p.out_dim; for (short i = 0; i < 8; ++i) simdgroup_store(mc[i], C + 8u * uint(i % 4) + 8u * p.out_dim * uint(i / 4), p.out_dim, 0, false); }\n"
           "    else { threadgroup_barrier(mem_flags::mem_threadgroup); threadgroup float *temp_str = ((threadgroup float *)shmem) + 32u * (uint(sgitg) & 1u) + 16u * (uint(sgitg) >> 1u) * uint(NR0); for (short i = 0; i < 8; ++i) simdgroup_store(mc[i], temp_str + 8u * uint(i % 4) + 8u * uint(NR0) * uint(i / 4), uint(NR0), 0, false); threadgroup_barrier(mem_flags::mem_threadgroup); if (sgitg == 0) { for (int j = int(tiitg); j < int(nr1); j += NR1) { device float *D = output + uint(r1 + j) * p.out_dim + uint(r0); threadgroup float *C = ((threadgroup float *)shmem) + uint(j) * uint(NR0); uint i = 0u; for (; i < uint(nr0); ++i) D[i] = C[i]; } } }\n"
           "}\n"
           "kernel void termite_q8_0_linear_mm(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], ushort lane [[thread_index_in_simdgroup]], ushort sgitg [[simdgroup_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) {\n"
           "    const uint NX = 8u; const uint NY = 4u; const uint NSG = 4u; const uint RPTG = 8u; const uint CHPT = 4u; const uint OPG = 2u;\n"
           "    uint tx = uint(lane) & 7u; uint ty = uint(lane) >> 3; uint first_o = (tg.x * (NY * NSG) + uint(sgitg) * NY + ty) * OPG; uint first_r = tg.y * RPTG; if (first_o >= p.out_dim) return;\n"
           "    float acc0[8]; float acc1[8]; for (uint i = 0u; i < RPTG; ++i) { acc0[i] = 0.0f; acc1[i] = 0.0f; }\n"
           "    uint row_offset0 = first_o * p.row_blocks * 34u; uint row_offset1 = row_offset0 + p.row_blocks * 34u; uint total_chunks = p.row_blocks * 8u; bool has_o1 = first_o + 1u < p.out_dim;\n"
           "    for (uint chunk = tx; chunk < total_chunks; chunk += CHPT * NX) { for (uint ch = 0u; ch < CHPT; ++ch) { uint c = chunk + ch * NX; if (c >= total_chunks) continue; uint b = c >> 3; uint ci = c & 7u; uint off0 = row_offset0 + b * 34u; ushort bits0 = (ushort(weight[off0 + 1u]) << 8) | ushort(weight[off0]); float d0 = float(as_type<half>(bits0)); float4 w0 = float4(float(as_type<char>(weight[off0 + 2u + ci * 4u])), float(as_type<char>(weight[off0 + 3u + ci * 4u])), float(as_type<char>(weight[off0 + 4u + ci * 4u])), float(as_type<char>(weight[off0 + 5u + ci * 4u]))) * d0; float4 w1 = float4(0.0f); if (has_o1) { uint off1 = row_offset1 + b * 34u; ushort bits1 = (ushort(weight[off1 + 1u]) << 8) | ushort(weight[off1]); float d1 = float(as_type<half>(bits1)); w1 = float4(float(as_type<char>(weight[off1 + 2u + ci * 4u])), float(as_type<char>(weight[off1 + 3u + ci * 4u])), float(as_type<char>(weight[off1 + 4u + ci * 4u])), float(as_type<char>(weight[off1 + 5u + ci * 4u]))) * d1; } uint in_col = c * 4u; for (uint rr = 0u; rr < RPTG; ++rr) { uint r = first_r + rr; if (r >= p.rows) continue; uint in_off = r * p.in_dim + in_col; float4 x = float4(input[in_off], input[in_off + 1u], input[in_off + 2u], input[in_off + 3u]); acc0[rr] += dot(x, w0); if (has_o1) acc1[rr] += dot(x, w1); } } }\n"
           "    for (uint rr = 0u; rr < RPTG; ++rr) { float total0 = acc0[rr]; float total1 = acc1[rr]; total0 += simd_shuffle_down(total0, 4u); total1 += simd_shuffle_down(total1, 4u); total0 += simd_shuffle_down(total0, 2u); total1 += simd_shuffle_down(total1, 2u); total0 += simd_shuffle_down(total0, 1u); total1 += simd_shuffle_down(total1, 1u); uint r = first_r + rr; if (tx == 0u && r < p.rows) { uint idx = r * p.out_dim + first_o; output[idx] = total0; if (has_o1) output[idx + 1u] = total1; } }\n"
           "}\n"
           "kernel void termite_q8_0_linear_mm_in_f16(device const half *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], ushort lane [[thread_index_in_simdgroup]], ushort sgitg [[simdgroup_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) {\n"
           "    const uint NX = 8u; const uint NY = 4u; const uint NSG = 4u; const uint RPTG = 8u; const uint CHPT = 4u; const uint OPG = 2u;\n"
           "    uint tx = uint(lane) & 7u; uint ty = uint(lane) >> 3; uint first_o = (tg.x * (NY * NSG) + uint(sgitg) * NY + ty) * OPG; uint first_r = tg.y * RPTG; if (first_o >= p.out_dim) return;\n"
           "    float acc0[8]; float acc1[8]; for (uint i = 0u; i < RPTG; ++i) { acc0[i] = 0.0f; acc1[i] = 0.0f; }\n"
           "    uint row_offset0 = first_o * p.row_blocks * 34u; uint row_offset1 = row_offset0 + p.row_blocks * 34u; uint total_chunks = p.row_blocks * 8u; bool has_o1 = first_o + 1u < p.out_dim;\n"
           "    for (uint chunk = tx; chunk < total_chunks; chunk += CHPT * NX) { for (uint ch = 0u; ch < CHPT; ++ch) { uint c = chunk + ch * NX; if (c >= total_chunks) continue; uint b = c >> 3; uint ci = c & 7u; uint off0 = row_offset0 + b * 34u; ushort bits0 = (ushort(weight[off0 + 1u]) << 8) | ushort(weight[off0]); float d0 = float(as_type<half>(bits0)); float4 w0 = float4(float(as_type<char>(weight[off0 + 2u + ci * 4u])), float(as_type<char>(weight[off0 + 3u + ci * 4u])), float(as_type<char>(weight[off0 + 4u + ci * 4u])), float(as_type<char>(weight[off0 + 5u + ci * 4u]))) * d0; float4 w1 = float4(0.0f); if (has_o1) { uint off1 = row_offset1 + b * 34u; ushort bits1 = (ushort(weight[off1 + 1u]) << 8) | ushort(weight[off1]); float d1 = float(as_type<half>(bits1)); w1 = float4(float(as_type<char>(weight[off1 + 2u + ci * 4u])), float(as_type<char>(weight[off1 + 3u + ci * 4u])), float(as_type<char>(weight[off1 + 4u + ci * 4u])), float(as_type<char>(weight[off1 + 5u + ci * 4u]))) * d1; } uint in_col = c * 4u; for (uint rr = 0u; rr < RPTG; ++rr) { uint r = first_r + rr; if (r >= p.rows) continue; uint in_off = r * p.in_dim + in_col; float4 x = float4(float(input[in_off]), float(input[in_off + 1u]), float(input[in_off + 2u]), float(input[in_off + 3u])); acc0[rr] += dot(x, w0); if (has_o1) acc1[rr] += dot(x, w1); } } }\n"
           "    for (uint rr = 0u; rr < RPTG; ++rr) { float total0 = acc0[rr]; float total1 = acc1[rr]; total0 += simd_shuffle_down(total0, 4u); total1 += simd_shuffle_down(total1, 4u); total0 += simd_shuffle_down(total0, 2u); total1 += simd_shuffle_down(total1, 2u); total0 += simd_shuffle_down(total0, 1u); total1 += simd_shuffle_down(total1, 1u); uint r = first_r + rr; if (tx == 0u && r < p.rows) { uint idx = r * p.out_dim + first_o; output[idx] = total0; if (has_o1) output[idx + 1u] = total1; } }\n"
           "}\n"
           "kernel void termite_q8_0_pair_activation_multiply_mm(device const float *input [[buffer(0)]], device const uchar *weight_gate [[buffer(1)]], device const uchar *weight_up [[buffer(2)]], device float *output [[buffer(3)]], constant termite_metal_linear_params &p [[buffer(4)]], constant termite_metal_apply_activation_params &ap [[buffer(5)]], ushort lane [[thread_index_in_simdgroup]], ushort sgitg [[simdgroup_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) {\n"
           "    const uint NX = 8u; const uint NY = 4u; const uint NSG = 4u; const uint RPTG = 8u; const uint CHPT = 4u; const uint OPG = 2u;\n"
           "    uint tx = uint(lane) & 7u; uint ty = uint(lane) >> 3; uint first_o = (tg.x * (NY * NSG) + uint(sgitg) * NY + ty) * OPG; uint first_r = tg.y * RPTG; if (first_o >= p.out_dim) return;\n"
           "    float gate0[8]; float up0[8]; float gate1[8]; float up1[8]; for (uint i = 0u; i < RPTG; ++i) { gate0[i] = 0.0f; up0[i] = 0.0f; gate1[i] = 0.0f; up1[i] = 0.0f; }\n"
           "    uint row_offset0 = first_o * p.row_blocks * 34u; uint row_offset1 = row_offset0 + p.row_blocks * 34u; uint total_chunks = p.row_blocks * 8u; bool has_o1 = first_o + 1u < p.out_dim;\n"
           "    for (uint chunk = tx; chunk < total_chunks; chunk += CHPT * NX) { for (uint ch = 0u; ch < CHPT; ++ch) { uint c = chunk + ch * NX; if (c >= total_chunks) continue; uint b = c >> 3; uint ci = c & 7u; uint off0 = row_offset0 + b * 34u; ushort bits_g0 = (ushort(weight_gate[off0 + 1u]) << 8) | ushort(weight_gate[off0]); ushort bits_u0 = (ushort(weight_up[off0 + 1u]) << 8) | ushort(weight_up[off0]); float dg0 = float(as_type<half>(bits_g0)); float du0 = float(as_type<half>(bits_u0)); float4 wg0 = float4(float(as_type<char>(weight_gate[off0 + 2u + ci * 4u])), float(as_type<char>(weight_gate[off0 + 3u + ci * 4u])), float(as_type<char>(weight_gate[off0 + 4u + ci * 4u])), float(as_type<char>(weight_gate[off0 + 5u + ci * 4u]))) * dg0; float4 wu0 = float4(float(as_type<char>(weight_up[off0 + 2u + ci * 4u])), float(as_type<char>(weight_up[off0 + 3u + ci * 4u])), float(as_type<char>(weight_up[off0 + 4u + ci * 4u])), float(as_type<char>(weight_up[off0 + 5u + ci * 4u]))) * du0; float4 wg1 = float4(0.0f); float4 wu1 = float4(0.0f); if (has_o1) { uint off1 = row_offset1 + b * 34u; ushort bits_g1 = (ushort(weight_gate[off1 + 1u]) << 8) | ushort(weight_gate[off1]); ushort bits_u1 = (ushort(weight_up[off1 + 1u]) << 8) | ushort(weight_up[off1]); float dg1 = float(as_type<half>(bits_g1)); float du1 = float(as_type<half>(bits_u1)); wg1 = float4(float(as_type<char>(weight_gate[off1 + 2u + ci * 4u])), float(as_type<char>(weight_gate[off1 + 3u + ci * 4u])), float(as_type<char>(weight_gate[off1 + 4u + ci * 4u])), float(as_type<char>(weight_gate[off1 + 5u + ci * 4u]))) * dg1; wu1 = float4(float(as_type<char>(weight_up[off1 + 2u + ci * 4u])), float(as_type<char>(weight_up[off1 + 3u + ci * 4u])), float(as_type<char>(weight_up[off1 + 4u + ci * 4u])), float(as_type<char>(weight_up[off1 + 5u + ci * 4u]))) * du1; } uint in_col = c * 4u; for (uint rr = 0u; rr < RPTG; ++rr) { uint r = first_r + rr; if (r >= p.rows) continue; uint in_off = r * p.in_dim + in_col; float4 x = float4(input[in_off], input[in_off + 1u], input[in_off + 2u], input[in_off + 3u]); gate0[rr] += dot(x, wg0); up0[rr] += dot(x, wu0); if (has_o1) { gate1[rr] += dot(x, wg1); up1[rr] += dot(x, wu1); } } } }\n"
           "    for (uint rr = 0u; rr < RPTG; ++rr) { float total_gate0 = gate0[rr]; float total_up0 = up0[rr]; float total_gate1 = gate1[rr]; float total_up1 = up1[rr]; total_gate0 += simd_shuffle_down(total_gate0, 4u); total_up0 += simd_shuffle_down(total_up0, 4u); total_gate1 += simd_shuffle_down(total_gate1, 4u); total_up1 += simd_shuffle_down(total_up1, 4u); total_gate0 += simd_shuffle_down(total_gate0, 2u); total_up0 += simd_shuffle_down(total_up0, 2u); total_gate1 += simd_shuffle_down(total_gate1, 2u); total_up1 += simd_shuffle_down(total_up1, 2u); total_gate0 += simd_shuffle_down(total_gate0, 1u); total_up0 += simd_shuffle_down(total_up0, 1u); total_gate1 += simd_shuffle_down(total_gate1, 1u); total_up1 += simd_shuffle_down(total_up1, 1u); uint r = first_r + rr; if (tx == 0u && r < p.rows) { uint idx = r * p.out_dim + first_o; output[idx] = termite_gated_activation_product(total_gate0, total_up0, ap.activation_kind); if (has_o1) output[idx + 1u] = termite_gated_activation_product(total_gate1, total_up1, ap.activation_kind); } }\n"
           "}\n"
           "kernel void termite_q8_0_pair_activation_multiply_mm_out_f16(device const float *input [[buffer(0)]], device const uchar *weight_gate [[buffer(1)]], device const uchar *weight_up [[buffer(2)]], device half *output [[buffer(3)]], constant termite_metal_linear_params &p [[buffer(4)]], constant termite_metal_apply_activation_params &ap [[buffer(5)]], ushort lane [[thread_index_in_simdgroup]], ushort sgitg [[simdgroup_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) {\n"
           "    const uint NX = 8u; const uint NY = 4u; const uint NSG = 4u; const uint RPTG = 8u; const uint CHPT = 4u; const uint OPG = 2u;\n"
           "    uint tx = uint(lane) & 7u; uint ty = uint(lane) >> 3; uint first_o = (tg.x * (NY * NSG) + uint(sgitg) * NY + ty) * OPG; uint first_r = tg.y * RPTG; if (first_o >= p.out_dim) return;\n"
           "    float gate0[8]; float up0[8]; float gate1[8]; float up1[8]; for (uint i = 0u; i < RPTG; ++i) { gate0[i] = 0.0f; up0[i] = 0.0f; gate1[i] = 0.0f; up1[i] = 0.0f; }\n"
           "    uint row_offset0 = first_o * p.row_blocks * 34u; uint row_offset1 = row_offset0 + p.row_blocks * 34u; uint total_chunks = p.row_blocks * 8u; bool has_o1 = first_o + 1u < p.out_dim;\n"
           "    for (uint chunk = tx; chunk < total_chunks; chunk += CHPT * NX) { for (uint ch = 0u; ch < CHPT; ++ch) { uint c = chunk + ch * NX; if (c >= total_chunks) continue; uint b = c >> 3; uint ci = c & 7u; uint off0 = row_offset0 + b * 34u; ushort bits_g0 = (ushort(weight_gate[off0 + 1u]) << 8) | ushort(weight_gate[off0]); ushort bits_u0 = (ushort(weight_up[off0 + 1u]) << 8) | ushort(weight_up[off0]); float dg0 = float(as_type<half>(bits_g0)); float du0 = float(as_type<half>(bits_u0)); float4 wg0 = float4(float(as_type<char>(weight_gate[off0 + 2u + ci * 4u])), float(as_type<char>(weight_gate[off0 + 3u + ci * 4u])), float(as_type<char>(weight_gate[off0 + 4u + ci * 4u])), float(as_type<char>(weight_gate[off0 + 5u + ci * 4u]))) * dg0; float4 wu0 = float4(float(as_type<char>(weight_up[off0 + 2u + ci * 4u])), float(as_type<char>(weight_up[off0 + 3u + ci * 4u])), float(as_type<char>(weight_up[off0 + 4u + ci * 4u])), float(as_type<char>(weight_up[off0 + 5u + ci * 4u]))) * du0; float4 wg1 = float4(0.0f); float4 wu1 = float4(0.0f); if (has_o1) { uint off1 = row_offset1 + b * 34u; ushort bits_g1 = (ushort(weight_gate[off1 + 1u]) << 8) | ushort(weight_gate[off1]); ushort bits_u1 = (ushort(weight_up[off1 + 1u]) << 8) | ushort(weight_up[off1]); float dg1 = float(as_type<half>(bits_g1)); float du1 = float(as_type<half>(bits_u1)); wg1 = float4(float(as_type<char>(weight_gate[off1 + 2u + ci * 4u])), float(as_type<char>(weight_gate[off1 + 3u + ci * 4u])), float(as_type<char>(weight_gate[off1 + 4u + ci * 4u])), float(as_type<char>(weight_gate[off1 + 5u + ci * 4u]))) * dg1; wu1 = float4(float(as_type<char>(weight_up[off1 + 2u + ci * 4u])), float(as_type<char>(weight_up[off1 + 3u + ci * 4u])), float(as_type<char>(weight_up[off1 + 4u + ci * 4u])), float(as_type<char>(weight_up[off1 + 5u + ci * 4u]))) * du1; } uint in_col = c * 4u; for (uint rr = 0u; rr < RPTG; ++rr) { uint r = first_r + rr; if (r >= p.rows) continue; uint in_off = r * p.in_dim + in_col; float4 x = float4(input[in_off], input[in_off + 1u], input[in_off + 2u], input[in_off + 3u]); gate0[rr] += dot(x, wg0); up0[rr] += dot(x, wu0); if (has_o1) { gate1[rr] += dot(x, wg1); up1[rr] += dot(x, wu1); } } } }\n"
           "    for (uint rr = 0u; rr < RPTG; ++rr) { float total_gate0 = gate0[rr]; float total_up0 = up0[rr]; float total_gate1 = gate1[rr]; float total_up1 = up1[rr]; total_gate0 += simd_shuffle_down(total_gate0, 4u); total_up0 += simd_shuffle_down(total_up0, 4u); total_gate1 += simd_shuffle_down(total_gate1, 4u); total_up1 += simd_shuffle_down(total_up1, 4u); total_gate0 += simd_shuffle_down(total_gate0, 2u); total_up0 += simd_shuffle_down(total_up0, 2u); total_gate1 += simd_shuffle_down(total_gate1, 2u); total_up1 += simd_shuffle_down(total_up1, 2u); total_gate0 += simd_shuffle_down(total_gate0, 1u); total_up0 += simd_shuffle_down(total_up0, 1u); total_gate1 += simd_shuffle_down(total_gate1, 1u); total_up1 += simd_shuffle_down(total_up1, 1u); uint r = first_r + rr; if (tx == 0u && r < p.rows) { uint idx = r * p.out_dim + first_o; output[idx] = half(termite_gated_activation_product(total_gate0, total_up0, ap.activation_kind)); if (has_o1) output[idx + 1u] = half(termite_gated_activation_product(total_gate1, total_up1, ap.activation_kind)); } }\n"
           "}\n"
           "kernel void termite_q8_0_pair_linear_mmv(device const float *input [[buffer(0)]], device const uchar *weight_a [[buffer(1)]], device const uchar *weight_b [[buffer(2)]], device float *output_a [[buffer(3)]], device float *output_b [[buffer(4)]], constant termite_metal_linear_params &p [[buffer(5)]], threadgroup float *shmem [[threadgroup(0)]], ushort lane [[thread_index_in_simdgroup]], ushort sgitg [[simdgroup_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) {\n"
           "    const uint NR0 = 2u; const uint NSG = 4u; const uint NQ = 8u; const uint NW = 32u; uint first_o = tg.x * NR0; uint r = tg.y; if (r >= p.rows || lane >= NW) return;\n"
           "    uint ix = lane / (NW / NQ); uint il = lane - ix * (NW / NQ); uint ib0 = sgitg * NQ + ix; uint in_row = r * p.in_dim; float acc_a0 = 0.0f; float acc_b0 = 0.0f; float acc_a1 = 0.0f; float acc_b1 = 0.0f;\n"
           "    for (uint b = ib0; b < p.row_blocks; b += NSG * NQ) { float x[8]; uint in_off = in_row + b * 32u + il * NQ; for (uint i = 0; i < NQ; ++i) { x[i] = input[in_off + i]; } uint o0 = first_o; if (o0 < p.out_dim) { uint off = o0 * p.row_blocks * 34u + b * 34u; ushort bits_a = (ushort(weight_a[off + 1]) << 8) | ushort(weight_a[off]); ushort bits_b = (ushort(weight_b[off + 1]) << 8) | ushort(weight_b[off]); float d_a = float(as_type<half>(bits_a)); float d_b = float(as_type<half>(bits_b)); float block_a = 0.0f; float block_b = 0.0f; for (uint i = 0; i < NQ; ++i) { float xv = x[i]; block_a += xv * float(as_type<char>(weight_a[off + 2u + il * NQ + i])); block_b += xv * float(as_type<char>(weight_b[off + 2u + il * NQ + i])); } acc_a0 += d_a * block_a; acc_b0 += d_b * block_b; } uint o1 = first_o + 1u; if (o1 < p.out_dim) { uint off = o1 * p.row_blocks * 34u + b * 34u; ushort bits_a = (ushort(weight_a[off + 1]) << 8) | ushort(weight_a[off]); ushort bits_b = (ushort(weight_b[off + 1]) << 8) | ushort(weight_b[off]); float d_a = float(as_type<half>(bits_a)); float d_b = float(as_type<half>(bits_b)); float block_a = 0.0f; float block_b = 0.0f; for (uint i = 0; i < NQ; ++i) { float xv = x[i]; block_a += xv * float(as_type<char>(weight_a[off + 2u + il * NQ + i])); block_b += xv * float(as_type<char>(weight_b[off + 2u + il * NQ + i])); } acc_a1 += d_a * block_a; acc_b1 += d_b * block_b; } }\n"
           "    if (sgitg == 0u) { shmem[lane] = 0.0f; shmem[32u + lane] = 0.0f; shmem[64u + lane] = 0.0f; shmem[96u + lane] = 0.0f; } acc_a0 = simd_sum(acc_a0); acc_b0 = simd_sum(acc_b0); acc_a1 = simd_sum(acc_a1); acc_b1 = simd_sum(acc_b1); threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    if (lane == 0u) { shmem[sgitg] = acc_a0; shmem[32u + sgitg] = acc_b0; shmem[64u + sgitg] = acc_a1; shmem[96u + sgitg] = acc_b1; } threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    float total_a0 = simd_sum(shmem[lane]); float total_b0 = simd_sum(shmem[32u + lane]); float total_a1 = simd_sum(shmem[64u + lane]); float total_b1 = simd_sum(shmem[96u + lane]); if (lane == 0u && sgitg == 0u) { uint idx = r * p.out_dim + first_o; if (first_o < p.out_dim) { output_a[idx] = total_a0; output_b[idx] = total_b0; } if (first_o + 1u < p.out_dim) { output_a[idx + 1u] = total_a1; output_b[idx + 1u] = total_b1; } }\n"
           "}\n"
           "inline void termite_q8_0_pair_linear_r_ext_impl(device const float *input, device const uchar *weight_a, device const uchar *weight_b, device float *output_a, device float *output_b, constant termite_metal_linear_params &p, ushort lane, ushort sgitg, uint3 tg, uint RPTG) {\n"
           "    const uint NX = 8u; const uint NY = 4u; const uint NSG = 4u; const uint CHPT = 4u; uint tx = uint(lane) & 7u; uint ty = uint(lane) >> 3; uint o = tg.x * (NY * NSG) + uint(sgitg) * NY + ty; uint first_r = tg.y * RPTG; if (o >= p.out_dim) return; float acc_a[5]; float acc_b[5]; for (uint i = 0u; i < 5u; ++i) { acc_a[i] = 0.0f; acc_b[i] = 0.0f; } uint row_offset = o * p.row_blocks * 34u; uint total_chunks = p.row_blocks * 8u;\n"
           "    for (uint chunk = tx; chunk < total_chunks; chunk += CHPT * NX) { for (uint ch = 0u; ch < CHPT; ++ch) { uint c = chunk + ch * NX; if (c >= total_chunks) continue; uint b = c >> 3; uint ci = c & 7u; uint off = row_offset + b * 34u; ushort bits_a = (ushort(weight_a[off + 1u]) << 8) | ushort(weight_a[off]); ushort bits_b = (ushort(weight_b[off + 1u]) << 8) | ushort(weight_b[off]); float da = float(as_type<half>(bits_a)); float db = float(as_type<half>(bits_b)); float4 wa = float4(float(as_type<char>(weight_a[off + 2u + ci * 4u])), float(as_type<char>(weight_a[off + 3u + ci * 4u])), float(as_type<char>(weight_a[off + 4u + ci * 4u])), float(as_type<char>(weight_a[off + 5u + ci * 4u]))) * da; float4 wb = float4(float(as_type<char>(weight_b[off + 2u + ci * 4u])), float(as_type<char>(weight_b[off + 3u + ci * 4u])), float(as_type<char>(weight_b[off + 4u + ci * 4u])), float(as_type<char>(weight_b[off + 5u + ci * 4u]))) * db; uint in_col = c * 4u; for (uint rr = 0u; rr < RPTG; ++rr) { uint r = first_r + rr; if (r >= p.rows) continue; uint in_off = r * p.in_dim + in_col; float4 x = float4(input[in_off], input[in_off + 1u], input[in_off + 2u], input[in_off + 3u]); acc_a[rr] += dot(x, wa); acc_b[rr] += dot(x, wb); } } }\n"
           "    for (uint rr = 0u; rr < RPTG; ++rr) { float total_a = acc_a[rr]; float total_b = acc_b[rr]; total_a += simd_shuffle_down(total_a, 4u); total_b += simd_shuffle_down(total_b, 4u); total_a += simd_shuffle_down(total_a, 2u); total_b += simd_shuffle_down(total_b, 2u); total_a += simd_shuffle_down(total_a, 1u); total_b += simd_shuffle_down(total_b, 1u); uint r = first_r + rr; if (tx == 0u && r < p.rows) { uint idx = r * p.out_dim + o; output_a[idx] = total_a; output_b[idx] = total_b; } }\n"
           "}\n"
           "kernel void termite_q8_0_pair_linear_r2_ext(device const float *input [[buffer(0)]], device const uchar *weight_a [[buffer(1)]], device const uchar *weight_b [[buffer(2)]], device float *output_a [[buffer(3)]], device float *output_b [[buffer(4)]], constant termite_metal_linear_params &p [[buffer(5)]], ushort lane [[thread_index_in_simdgroup]], ushort sgitg [[simdgroup_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) { termite_q8_0_pair_linear_r_ext_impl(input, weight_a, weight_b, output_a, output_b, p, lane, sgitg, tg, 2u); }\n"
           "kernel void termite_q8_0_pair_linear_r3_ext(device const float *input [[buffer(0)]], device const uchar *weight_a [[buffer(1)]], device const uchar *weight_b [[buffer(2)]], device float *output_a [[buffer(3)]], device float *output_b [[buffer(4)]], constant termite_metal_linear_params &p [[buffer(5)]], ushort lane [[thread_index_in_simdgroup]], ushort sgitg [[simdgroup_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) { termite_q8_0_pair_linear_r_ext_impl(input, weight_a, weight_b, output_a, output_b, p, lane, sgitg, tg, 3u); }\n"
           "kernel void termite_q8_0_pair_linear_r4_ext(device const float *input [[buffer(0)]], device const uchar *weight_a [[buffer(1)]], device const uchar *weight_b [[buffer(2)]], device float *output_a [[buffer(3)]], device float *output_b [[buffer(4)]], constant termite_metal_linear_params &p [[buffer(5)]], ushort lane [[thread_index_in_simdgroup]], ushort sgitg [[simdgroup_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) { termite_q8_0_pair_linear_r_ext_impl(input, weight_a, weight_b, output_a, output_b, p, lane, sgitg, tg, 4u); }\n"
           "kernel void termite_q8_0_pair_linear_r5_ext(device const float *input [[buffer(0)]], device const uchar *weight_a [[buffer(1)]], device const uchar *weight_b [[buffer(2)]], device float *output_a [[buffer(3)]], device float *output_b [[buffer(4)]], constant termite_metal_linear_params &p [[buffer(5)]], ushort lane [[thread_index_in_simdgroup]], ushort sgitg [[simdgroup_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) { termite_q8_0_pair_linear_r_ext_impl(input, weight_a, weight_b, output_a, output_b, p, lane, sgitg, tg, 5u); }\n"
           "kernel void termite_q8_0_qkv_linear_mmv(device const float *input [[buffer(0)]], device const uchar *weight_q [[buffer(1)]], device const uchar *weight_k [[buffer(2)]], device const uchar *weight_v [[buffer(3)]], device float *output_q [[buffer(4)]], device float *output_k [[buffer(5)]], device float *output_v [[buffer(6)]], constant termite_metal_qkv_linear_params &p [[buffer(7)]], threadgroup float *shmem [[threadgroup(0)]], ushort lane [[thread_index_in_simdgroup]], ushort sgitg [[simdgroup_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) {\n"
           "    const uint NR0 = 2u; const uint NSG = 4u; const uint NQ = 8u; const uint NW = 32u; uint q_groups = (p.q_out_dim + 1u) / 2u; uint first_o = tg.x < q_groups ? tg.x * NR0 : (tg.x - q_groups) * NR0; uint r = tg.y; if (r >= p.rows || lane >= NW) return;\n"
           "    uint ix = lane / (NW / NQ); uint il = lane - ix * (NW / NQ); uint ib0 = sgitg * NQ + ix; uint in_row = r * p.in_dim;\n"
           "    if (tg.x < q_groups) {\n"
           "        float acc0 = 0.0f; float acc1 = 0.0f;\n"
           "        for (uint b = ib0; b < p.row_blocks; b += NSG * NQ) { float x[8]; uint in_off = in_row + b * 32u + il * NQ; for (uint i = 0; i < NQ; ++i) { x[i] = input[in_off + i]; } uint o0 = first_o; if (o0 < p.q_out_dim) { uint off = o0 * p.row_blocks * 34u + b * 34u; ushort bits = (ushort(weight_q[off + 1]) << 8) | ushort(weight_q[off]); float d = float(as_type<half>(bits)); float block = 0.0f; for (uint i = 0; i < NQ; ++i) block += x[i] * float(as_type<char>(weight_q[off + 2u + il * NQ + i])); acc0 += d * block; } uint o1 = first_o + 1u; if (o1 < p.q_out_dim) { uint off = o1 * p.row_blocks * 34u + b * 34u; ushort bits = (ushort(weight_q[off + 1]) << 8) | ushort(weight_q[off]); float d = float(as_type<half>(bits)); float block = 0.0f; for (uint i = 0; i < NQ; ++i) block += x[i] * float(as_type<char>(weight_q[off + 2u + il * NQ + i])); acc1 += d * block; } }\n"
           "        if (sgitg == 0u) { shmem[lane] = 0.0f; shmem[32u + lane] = 0.0f; } acc0 = simd_sum(acc0); acc1 = simd_sum(acc1); threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "        if (lane == 0u) { shmem[sgitg] = acc0; shmem[32u + sgitg] = acc1; } threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "        float total0 = simd_sum(shmem[lane]); float total1 = simd_sum(shmem[32u + lane]); if (lane == 0u && sgitg == 0u) { uint idx = r * p.q_out_dim + first_o; if (first_o < p.q_out_dim) output_q[idx] = total0; if (first_o + 1u < p.q_out_dim) output_q[idx + 1u] = total1; }\n"
           "    } else {\n"
           "        float acc_k0 = 0.0f; float acc_v0 = 0.0f; float acc_k1 = 0.0f; float acc_v1 = 0.0f;\n"
           "        for (uint b = ib0; b < p.row_blocks; b += NSG * NQ) { float x[8]; uint in_off = in_row + b * 32u + il * NQ; for (uint i = 0; i < NQ; ++i) { x[i] = input[in_off + i]; } uint o0 = first_o; if (o0 < p.kv_out_dim) { uint off = o0 * p.row_blocks * 34u + b * 34u; ushort bits_k = (ushort(weight_k[off + 1]) << 8) | ushort(weight_k[off]); ushort bits_v = (ushort(weight_v[off + 1]) << 8) | ushort(weight_v[off]); float dk = float(as_type<half>(bits_k)); float dv = float(as_type<half>(bits_v)); float block_k = 0.0f; float block_v = 0.0f; for (uint i = 0; i < NQ; ++i) { float xv = x[i]; block_k += xv * float(as_type<char>(weight_k[off + 2u + il * NQ + i])); block_v += xv * float(as_type<char>(weight_v[off + 2u + il * NQ + i])); } acc_k0 += dk * block_k; acc_v0 += dv * block_v; } uint o1 = first_o + 1u; if (o1 < p.kv_out_dim) { uint off = o1 * p.row_blocks * 34u + b * 34u; ushort bits_k = (ushort(weight_k[off + 1]) << 8) | ushort(weight_k[off]); ushort bits_v = (ushort(weight_v[off + 1]) << 8) | ushort(weight_v[off]); float dk = float(as_type<half>(bits_k)); float dv = float(as_type<half>(bits_v)); float block_k = 0.0f; float block_v = 0.0f; for (uint i = 0; i < NQ; ++i) { float xv = x[i]; block_k += xv * float(as_type<char>(weight_k[off + 2u + il * NQ + i])); block_v += xv * float(as_type<char>(weight_v[off + 2u + il * NQ + i])); } acc_k1 += dk * block_k; acc_v1 += dv * block_v; } }\n"
           "        if (sgitg == 0u) { shmem[lane] = 0.0f; shmem[32u + lane] = 0.0f; shmem[64u + lane] = 0.0f; shmem[96u + lane] = 0.0f; } acc_k0 = simd_sum(acc_k0); acc_v0 = simd_sum(acc_v0); acc_k1 = simd_sum(acc_k1); acc_v1 = simd_sum(acc_v1); threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "        if (lane == 0u) { shmem[sgitg] = acc_k0; shmem[32u + sgitg] = acc_v0; shmem[64u + sgitg] = acc_k1; shmem[96u + sgitg] = acc_v1; } threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "        float total_k0 = simd_sum(shmem[lane]); float total_v0 = simd_sum(shmem[32u + lane]); float total_k1 = simd_sum(shmem[64u + lane]); float total_v1 = simd_sum(shmem[96u + lane]); if (lane == 0u && sgitg == 0u) { uint idx = r * p.kv_out_dim + first_o; if (first_o < p.kv_out_dim) { output_k[idx] = total_k0; output_v[idx] = total_v0; } if (first_o + 1u < p.kv_out_dim) { output_k[idx + 1u] = total_k1; output_v[idx + 1u] = total_v1; } }\n"
           "    }\n"
           "}\n"
           "kernel void termite_q8_0_pair_activation_multiply_1x_reduce4(device const float *input [[buffer(0)]], device const uchar *weight_gate [[buffer(1)]], device const uchar *weight_up [[buffer(2)]], device float *output [[buffer(3)]], constant termite_metal_linear_params &p [[buffer(4)]], constant termite_metal_apply_activation_params &ap [[buffer(5)]], threadgroup float *shmem [[threadgroup(0)]], ushort lane [[thread_index_in_simdgroup]], ushort sgitg [[simdgroup_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) {\n"
           "    const uint NR0 = 4u; const uint NSG = 4u; const uint NQ = 8u; const uint NW = 32u; uint first_o = tg.x * NR0; uint r = tg.y; if (r >= p.rows || lane >= NW) return;\n"
           "    uint ix = lane / (NW / NQ); uint il = lane - ix * (NW / NQ); uint ib0 = sgitg * NQ + ix; uint in_row = r * p.in_dim; float gate0 = 0.0f; float up0 = 0.0f; float gate1 = 0.0f; float up1 = 0.0f; float gate2 = 0.0f; float up2 = 0.0f; float gate3 = 0.0f; float up3 = 0.0f;\n"
           "    for (uint b = ib0; b < p.row_blocks; b += NSG * NQ) { float x[8]; uint in_off = in_row + b * 32u + il * NQ; for (uint i = 0; i < NQ; ++i) { x[i] = input[in_off + i]; } for (uint row = 0; row < NR0; ++row) { uint o = first_o + row; if (o >= p.out_dim) continue; uint off = o * p.row_blocks * 34u + b * 34u; ushort bits_g = (ushort(weight_gate[off + 1]) << 8) | ushort(weight_gate[off]); ushort bits_u = (ushort(weight_up[off + 1]) << 8) | ushort(weight_up[off]); float dg = float(as_type<half>(bits_g)); float du = float(as_type<half>(bits_u)); float block_g = 0.0f; float block_u = 0.0f; for (uint i = 0; i < NQ; ++i) { float xv = x[i]; block_g += xv * float(as_type<char>(weight_gate[off + 2u + il * NQ + i])); block_u += xv * float(as_type<char>(weight_up[off + 2u + il * NQ + i])); } if (row == 0u) { gate0 += dg * block_g; up0 += du * block_u; } else if (row == 1u) { gate1 += dg * block_g; up1 += du * block_u; } else if (row == 2u) { gate2 += dg * block_g; up2 += du * block_u; } else { gate3 += dg * block_g; up3 += du * block_u; } } }\n"
           "    if (sgitg == 0u) { shmem[lane] = 0.0f; shmem[32u + lane] = 0.0f; shmem[64u + lane] = 0.0f; shmem[96u + lane] = 0.0f; shmem[128u + lane] = 0.0f; shmem[160u + lane] = 0.0f; shmem[192u + lane] = 0.0f; shmem[224u + lane] = 0.0f; } gate0 = simd_sum(gate0); up0 = simd_sum(up0); gate1 = simd_sum(gate1); up1 = simd_sum(up1); gate2 = simd_sum(gate2); up2 = simd_sum(up2); gate3 = simd_sum(gate3); up3 = simd_sum(up3); threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    if (lane == 0u) { shmem[sgitg] = gate0; shmem[32u + sgitg] = up0; shmem[64u + sgitg] = gate1; shmem[96u + sgitg] = up1; shmem[128u + sgitg] = gate2; shmem[160u + sgitg] = up2; shmem[192u + sgitg] = gate3; shmem[224u + sgitg] = up3; } threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    float total_gate0 = simd_sum(shmem[lane]); float total_up0 = simd_sum(shmem[32u + lane]); float total_gate1 = simd_sum(shmem[64u + lane]); float total_up1 = simd_sum(shmem[96u + lane]); float total_gate2 = simd_sum(shmem[128u + lane]); float total_up2 = simd_sum(shmem[160u + lane]); float total_gate3 = simd_sum(shmem[192u + lane]); float total_up3 = simd_sum(shmem[224u + lane]); if (lane == 0u && sgitg == 0u) { uint idx = r * p.out_dim + first_o; if (first_o < p.out_dim) output[idx] = termite_gated_activation_product(total_gate0, total_up0, ap.activation_kind); if (first_o + 1u < p.out_dim) output[idx + 1u] = termite_gated_activation_product(total_gate1, total_up1, ap.activation_kind); if (first_o + 2u < p.out_dim) output[idx + 2u] = termite_gated_activation_product(total_gate2, total_up2, ap.activation_kind); if (first_o + 3u < p.out_dim) output[idx + 3u] = termite_gated_activation_product(total_gate3, total_up3, ap.activation_kind); }\n"
           "}\n"
           "kernel void termite_q8_0_pair_activation_multiply_1r_ext(device const float *input [[buffer(0)]], device const uchar *weight_gate [[buffer(1)]], device const uchar *weight_up [[buffer(2)]], device float *output [[buffer(3)]], constant termite_metal_linear_params &p [[buffer(4)]], constant termite_metal_apply_activation_params &ap [[buffer(5)]], ushort lane [[thread_index_in_simdgroup]], ushort sgitg [[simdgroup_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) {\n"
           "    const uint NX = 8u; const uint NY = 4u; const uint NSG = 4u; const uint CHPT = 4u; uint tx = uint(lane) & 7u; uint ty = uint(lane) >> 3; uint o = tg.x * (NY * NSG) + uint(sgitg) * NY + ty; uint r = tg.y; if (r >= p.rows || o >= p.out_dim) return; uint row_offset = o * p.row_blocks * 34u; uint total_chunks = p.row_blocks * 8u; uint input_row = r * p.in_dim; float gate = 0.0f; float up = 0.0f;\n"
           "    for (uint chunk = tx; chunk < total_chunks; chunk += CHPT * NX) { for (uint ch = 0u; ch < CHPT; ++ch) { uint c = chunk + ch * NX; if (c >= total_chunks) continue; uint b = c >> 3; uint ci = c & 7u; uint off = row_offset + b * 34u; ushort bits_g = (ushort(weight_gate[off + 1u]) << 8) | ushort(weight_gate[off]); ushort bits_u = (ushort(weight_up[off + 1u]) << 8) | ushort(weight_up[off]); float dg = float(as_type<half>(bits_g)); float du = float(as_type<half>(bits_u)); float4 wg = float4(float(as_type<char>(weight_gate[off + 2u + ci * 4u])), float(as_type<char>(weight_gate[off + 3u + ci * 4u])), float(as_type<char>(weight_gate[off + 4u + ci * 4u])), float(as_type<char>(weight_gate[off + 5u + ci * 4u]))) * dg; float4 wu = float4(float(as_type<char>(weight_up[off + 2u + ci * 4u])), float(as_type<char>(weight_up[off + 3u + ci * 4u])), float(as_type<char>(weight_up[off + 4u + ci * 4u])), float(as_type<char>(weight_up[off + 5u + ci * 4u]))) * du; uint in_col = input_row + c * 4u; float4 x = float4(input[in_col], input[in_col + 1u], input[in_col + 2u], input[in_col + 3u]); gate += dot(x, wg); up += dot(x, wu); } }\n"
           "    gate += simd_shuffle_down(gate, 4u); up += simd_shuffle_down(up, 4u); gate += simd_shuffle_down(gate, 2u); up += simd_shuffle_down(up, 2u); gate += simd_shuffle_down(gate, 1u); up += simd_shuffle_down(up, 1u); if (tx == 0u) output[r * p.out_dim + o] = termite_gated_activation_product(gate, up, ap.activation_kind);\n"
           "}\n"
           "kernel void termite_q8_0_pair_activation_multiply_rms_scale_1r_ext(device const float *input [[buffer(0)]], device const uchar *weight_gate [[buffer(1)]], device const uchar *weight_up [[buffer(2)]], device float *output [[buffer(3)]], constant termite_metal_linear_params &p [[buffer(4)]], constant termite_metal_apply_activation_params &ap [[buffer(5)]], device const float *norm_weight [[buffer(6)]], device const float *inv_scale [[buffer(7)]], ushort lane [[thread_index_in_simdgroup]], ushort sgitg [[simdgroup_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) {\n"
           "    const uint NX = 8u; const uint NY = 4u; const uint NSG = 4u; const uint CHPT = 4u; uint tx = uint(lane) & 7u; uint ty = uint(lane) >> 3; uint o = tg.x * (NY * NSG) + uint(sgitg) * NY + ty; uint r = tg.y; if (r >= p.rows || o >= p.out_dim) return; uint row_offset = o * p.row_blocks * 34u; uint total_chunks = p.row_blocks * 8u; uint input_row = r * p.in_dim; float scale = inv_scale[r]; float gate = 0.0f; float up = 0.0f;\n"
           "    for (uint chunk = tx; chunk < total_chunks; chunk += CHPT * NX) { for (uint ch = 0u; ch < CHPT; ++ch) { uint c = chunk + ch * NX; if (c >= total_chunks) continue; uint b = c >> 3; uint ci = c & 7u; uint off = row_offset + b * 34u; ushort bits_g = (ushort(weight_gate[off + 1u]) << 8) | ushort(weight_gate[off]); ushort bits_u = (ushort(weight_up[off + 1u]) << 8) | ushort(weight_up[off]); float dg = float(as_type<half>(bits_g)); float du = float(as_type<half>(bits_u)); float4 wg = float4(float(as_type<char>(weight_gate[off + 2u + ci * 4u])), float(as_type<char>(weight_gate[off + 3u + ci * 4u])), float(as_type<char>(weight_gate[off + 4u + ci * 4u])), float(as_type<char>(weight_gate[off + 5u + ci * 4u]))) * dg; float4 wu = float4(float(as_type<char>(weight_up[off + 2u + ci * 4u])), float(as_type<char>(weight_up[off + 3u + ci * 4u])), float(as_type<char>(weight_up[off + 4u + ci * 4u])), float(as_type<char>(weight_up[off + 5u + ci * 4u]))) * du; uint in_col = input_row + c * 4u; uint norm_col = c * 4u; float4 x = float4(input[in_col] * norm_weight[norm_col], input[in_col + 1u] * norm_weight[norm_col + 1u], input[in_col + 2u] * norm_weight[norm_col + 2u], input[in_col + 3u] * norm_weight[norm_col + 3u]) * scale; gate += dot(x, wg); up += dot(x, wu); } }\n"
           "    gate += simd_shuffle_down(gate, 4u); up += simd_shuffle_down(up, 4u); gate += simd_shuffle_down(gate, 2u); up += simd_shuffle_down(up, 2u); gate += simd_shuffle_down(gate, 1u); up += simd_shuffle_down(up, 1u); if (tx == 0u) output[r * p.out_dim + o] = termite_gated_activation_product(gate, up, ap.activation_kind);\n"
           "}\n"
           "kernel void termite_q8_0_pair_activation_multiply_rms_scale_1r_ext_out_f16(device const float *input [[buffer(0)]], device const uchar *weight_gate [[buffer(1)]], device const uchar *weight_up [[buffer(2)]], device half *output [[buffer(3)]], constant termite_metal_linear_params &p [[buffer(4)]], constant termite_metal_apply_activation_params &ap [[buffer(5)]], device const float *norm_weight [[buffer(6)]], device const float *inv_scale [[buffer(7)]], ushort lane [[thread_index_in_simdgroup]], ushort sgitg [[simdgroup_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) {\n"
           "    const uint NX = 8u; const uint NY = 4u; const uint NSG = 4u; const uint CHPT = 4u; uint tx = uint(lane) & 7u; uint ty = uint(lane) >> 3; uint o = tg.x * (NY * NSG) + uint(sgitg) * NY + ty; uint r = tg.y; if (r >= p.rows || o >= p.out_dim) return; uint row_offset = o * p.row_blocks * 34u; uint total_chunks = p.row_blocks * 8u; uint input_row = r * p.in_dim; float scale = inv_scale[r]; float gate = 0.0f; float up = 0.0f;\n"
           "    for (uint chunk = tx; chunk < total_chunks; chunk += CHPT * NX) { for (uint ch = 0u; ch < CHPT; ++ch) { uint c = chunk + ch * NX; if (c >= total_chunks) continue; uint b = c >> 3; uint ci = c & 7u; uint off = row_offset + b * 34u; ushort bits_g = (ushort(weight_gate[off + 1u]) << 8) | ushort(weight_gate[off]); ushort bits_u = (ushort(weight_up[off + 1u]) << 8) | ushort(weight_up[off]); float dg = float(as_type<half>(bits_g)); float du = float(as_type<half>(bits_u)); float4 wg = float4(float(as_type<char>(weight_gate[off + 2u + ci * 4u])), float(as_type<char>(weight_gate[off + 3u + ci * 4u])), float(as_type<char>(weight_gate[off + 4u + ci * 4u])), float(as_type<char>(weight_gate[off + 5u + ci * 4u]))) * dg; float4 wu = float4(float(as_type<char>(weight_up[off + 2u + ci * 4u])), float(as_type<char>(weight_up[off + 3u + ci * 4u])), float(as_type<char>(weight_up[off + 4u + ci * 4u])), float(as_type<char>(weight_up[off + 5u + ci * 4u]))) * du; uint in_col = input_row + c * 4u; uint norm_col = c * 4u; float4 x = float4(input[in_col] * norm_weight[norm_col], input[in_col + 1u] * norm_weight[norm_col + 1u], input[in_col + 2u] * norm_weight[norm_col + 2u], input[in_col + 3u] * norm_weight[norm_col + 3u]) * scale; gate += dot(x, wg); up += dot(x, wu); } }\n"
           "    gate += simd_shuffle_down(gate, 4u); up += simd_shuffle_down(up, 4u); gate += simd_shuffle_down(gate, 2u); up += simd_shuffle_down(up, 2u); gate += simd_shuffle_down(gate, 1u); up += simd_shuffle_down(up, 1u); if (tx == 0u) output[r * p.out_dim + o] = half(termite_gated_activation_product(gate, up, ap.activation_kind));\n"
           "}\n"
           "inline void termite_q8_0_pair_activation_multiply_r_ext_impl(device const float *input, device const uchar *weight_gate, device const uchar *weight_up, device float *output, constant termite_metal_linear_params &p, constant termite_metal_apply_activation_params &ap, ushort lane, ushort sgitg, uint3 tg, uint RPTG) {\n"
           "    const uint NX = 8u; const uint NY = 4u; const uint NSG = 4u; const uint CHPT = 4u; uint tx = uint(lane) & 7u; uint ty = uint(lane) >> 3; uint o = tg.x * (NY * NSG) + uint(sgitg) * NY + ty; uint first_r = tg.y * RPTG; if (o >= p.out_dim) return; float gate[5]; float up[5]; for (uint i = 0u; i < 5u; ++i) { gate[i] = 0.0f; up[i] = 0.0f; } uint row_offset = o * p.row_blocks * 34u; uint total_chunks = p.row_blocks * 8u;\n"
           "    for (uint chunk = tx; chunk < total_chunks; chunk += CHPT * NX) { for (uint ch = 0u; ch < CHPT; ++ch) { uint c = chunk + ch * NX; if (c >= total_chunks) continue; uint b = c >> 3; uint ci = c & 7u; uint off = row_offset + b * 34u; ushort bits_g = (ushort(weight_gate[off + 1u]) << 8) | ushort(weight_gate[off]); ushort bits_u = (ushort(weight_up[off + 1u]) << 8) | ushort(weight_up[off]); float dg = float(as_type<half>(bits_g)); float du = float(as_type<half>(bits_u)); float4 wg = float4(float(as_type<char>(weight_gate[off + 2u + ci * 4u])), float(as_type<char>(weight_gate[off + 3u + ci * 4u])), float(as_type<char>(weight_gate[off + 4u + ci * 4u])), float(as_type<char>(weight_gate[off + 5u + ci * 4u]))) * dg; float4 wu = float4(float(as_type<char>(weight_up[off + 2u + ci * 4u])), float(as_type<char>(weight_up[off + 3u + ci * 4u])), float(as_type<char>(weight_up[off + 4u + ci * 4u])), float(as_type<char>(weight_up[off + 5u + ci * 4u]))) * du; uint in_col = c * 4u; for (uint rr = 0u; rr < RPTG; ++rr) { uint r = first_r + rr; if (r >= p.rows) continue; uint in_off = r * p.in_dim + in_col; float4 x = float4(input[in_off], input[in_off + 1u], input[in_off + 2u], input[in_off + 3u]); gate[rr] += dot(x, wg); up[rr] += dot(x, wu); } } }\n"
           "    for (uint rr = 0u; rr < RPTG; ++rr) { float total_gate = gate[rr]; float total_up = up[rr]; total_gate += simd_shuffle_down(total_gate, 4u); total_up += simd_shuffle_down(total_up, 4u); total_gate += simd_shuffle_down(total_gate, 2u); total_up += simd_shuffle_down(total_up, 2u); total_gate += simd_shuffle_down(total_gate, 1u); total_up += simd_shuffle_down(total_up, 1u); uint r = first_r + rr; if (tx == 0u && r < p.rows) output[r * p.out_dim + o] = termite_gated_activation_product(total_gate, total_up, ap.activation_kind); }\n"
           "}\n"
           "kernel void termite_q8_0_pair_activation_multiply_r2_ext(device const float *input [[buffer(0)]], device const uchar *weight_gate [[buffer(1)]], device const uchar *weight_up [[buffer(2)]], device float *output [[buffer(3)]], constant termite_metal_linear_params &p [[buffer(4)]], constant termite_metal_apply_activation_params &ap [[buffer(5)]], ushort lane [[thread_index_in_simdgroup]], ushort sgitg [[simdgroup_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) { termite_q8_0_pair_activation_multiply_r_ext_impl(input, weight_gate, weight_up, output, p, ap, lane, sgitg, tg, 2u); }\n"
           "kernel void termite_q8_0_pair_activation_multiply_r3_ext(device const float *input [[buffer(0)]], device const uchar *weight_gate [[buffer(1)]], device const uchar *weight_up [[buffer(2)]], device float *output [[buffer(3)]], constant termite_metal_linear_params &p [[buffer(4)]], constant termite_metal_apply_activation_params &ap [[buffer(5)]], ushort lane [[thread_index_in_simdgroup]], ushort sgitg [[simdgroup_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) { termite_q8_0_pair_activation_multiply_r_ext_impl(input, weight_gate, weight_up, output, p, ap, lane, sgitg, tg, 3u); }\n"
           "kernel void termite_q8_0_pair_activation_multiply_r4_ext(device const float *input [[buffer(0)]], device const uchar *weight_gate [[buffer(1)]], device const uchar *weight_up [[buffer(2)]], device float *output [[buffer(3)]], constant termite_metal_linear_params &p [[buffer(4)]], constant termite_metal_apply_activation_params &ap [[buffer(5)]], ushort lane [[thread_index_in_simdgroup]], ushort sgitg [[simdgroup_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) { termite_q8_0_pair_activation_multiply_r_ext_impl(input, weight_gate, weight_up, output, p, ap, lane, sgitg, tg, 4u); }\n"
           "kernel void termite_q8_0_pair_activation_multiply_r5_ext(device const float *input [[buffer(0)]], device const uchar *weight_gate [[buffer(1)]], device const uchar *weight_up [[buffer(2)]], device float *output [[buffer(3)]], constant termite_metal_linear_params &p [[buffer(4)]], constant termite_metal_apply_activation_params &ap [[buffer(5)]], ushort lane [[thread_index_in_simdgroup]], ushort sgitg [[simdgroup_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) { termite_q8_0_pair_activation_multiply_r_ext_impl(input, weight_gate, weight_up, output, p, ap, lane, sgitg, tg, 5u); }\n"
           "kernel void termite_q8_0_activation_multiply_1r_ext(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device const float *rhs [[buffer(2)]], device float *output [[buffer(3)]], constant termite_metal_linear_params &p [[buffer(4)]], constant termite_metal_apply_activation_params &ap [[buffer(5)]], ushort lane [[thread_index_in_simdgroup]], ushort sgitg [[simdgroup_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) {\n"
           "    const uint NX = 8u; const uint NY = 4u; const uint NSG = 4u; const uint CHPT = 4u; uint tx = uint(lane) & 7u; uint ty = uint(lane) >> 3; uint o = tg.x * (NY * NSG) + uint(sgitg) * NY + ty; uint r = tg.y; if (r >= p.rows || o >= p.out_dim) return; uint row_offset = o * p.row_blocks * 34u; uint total_chunks = p.row_blocks * 8u; uint input_row = r * p.in_dim; float acc = 0.0f;\n"
           "    for (uint chunk = tx; chunk < total_chunks; chunk += CHPT * NX) { for (uint ch = 0u; ch < CHPT; ++ch) { uint c = chunk + ch * NX; if (c >= total_chunks) continue; uint b = c >> 3; uint ci = c & 7u; uint off = row_offset + b * 34u; ushort bits = (ushort(weight[off + 1u]) << 8) | ushort(weight[off]); float d = float(as_type<half>(bits)); float4 w = float4(float(as_type<char>(weight[off + 2u + ci * 4u])), float(as_type<char>(weight[off + 3u + ci * 4u])), float(as_type<char>(weight[off + 4u + ci * 4u])), float(as_type<char>(weight[off + 5u + ci * 4u]))) * d; uint in_col = input_row + c * 4u; float4 x = float4(input[in_col], input[in_col + 1u], input[in_col + 2u], input[in_col + 3u]); acc += dot(x, w); } }\n"
           "    acc += simd_shuffle_down(acc, 4u); acc += simd_shuffle_down(acc, 2u); acc += simd_shuffle_down(acc, 1u); if (tx == 0u) { uint idx = r * p.out_dim + o; output[idx] = termite_gated_activation_product(acc, rhs[idx], ap.activation_kind); }\n"
           "}\n"
           "kernel void termite_q8_0_activation_multiply_1x_reduce4(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device const float *rhs [[buffer(2)]], device float *output [[buffer(3)]], constant termite_metal_linear_params &p [[buffer(4)]], constant termite_metal_apply_activation_params &ap [[buffer(5)]], threadgroup float *shmem [[threadgroup(0)]], ushort lane [[thread_index_in_simdgroup]], ushort sgitg [[simdgroup_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) {\n"
           "    const uint NR0 = 4u; const uint NSG = 4u; const uint NQ = 8u; const uint NW = 32u; uint first_o = tg.x * NR0; uint r = tg.y; if (r >= p.rows || lane >= NW) return;\n"
           "    uint ix = lane / (NW / NQ); uint il = lane - ix * (NW / NQ); uint ib0 = sgitg * NQ + ix; uint in_row = r * p.in_dim; float acc0 = 0.0f; float acc1 = 0.0f; float acc2 = 0.0f; float acc3 = 0.0f;\n"
           "    for (uint b = ib0; b < p.row_blocks; b += NSG * NQ) { float x[8]; uint in_off = in_row + b * 32u + il * NQ; for (uint i = 0; i < NQ; ++i) { x[i] = input[in_off + i]; } for (uint row = 0; row < NR0; ++row) { uint o = first_o + row; if (o >= p.out_dim) continue; uint off = o * p.row_blocks * 34u + b * 34u; ushort bits = (ushort(weight[off + 1]) << 8) | ushort(weight[off]); float d = float(as_type<half>(bits)); float block = 0.0f; for (uint i = 0; i < NQ; ++i) block += x[i] * float(as_type<char>(weight[off + 2u + il * NQ + i])); if (row == 0u) acc0 += d * block; else if (row == 1u) acc1 += d * block; else if (row == 2u) acc2 += d * block; else acc3 += d * block; } }\n"
           "    if (sgitg == 0u) { shmem[lane] = 0.0f; shmem[32u + lane] = 0.0f; shmem[64u + lane] = 0.0f; shmem[96u + lane] = 0.0f; } acc0 = simd_sum(acc0); acc1 = simd_sum(acc1); acc2 = simd_sum(acc2); acc3 = simd_sum(acc3); threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    if (lane == 0u) { shmem[sgitg] = acc0; shmem[32u + sgitg] = acc1; shmem[64u + sgitg] = acc2; shmem[96u + sgitg] = acc3; } threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    float total0 = simd_sum(shmem[lane]); float total1 = simd_sum(shmem[32u + lane]); float total2 = simd_sum(shmem[64u + lane]); float total3 = simd_sum(shmem[96u + lane]); if (lane == 0u && sgitg == 0u) { uint idx = r * p.out_dim + first_o; if (first_o < p.out_dim) output[idx] = termite_gated_activation_product(total0, rhs[idx], ap.activation_kind); if (first_o + 1u < p.out_dim) output[idx + 1u] = termite_gated_activation_product(total1, rhs[idx + 1u], ap.activation_kind); if (first_o + 2u < p.out_dim) output[idx + 2u] = termite_gated_activation_product(total2, rhs[idx + 2u], ap.activation_kind); if (first_o + 3u < p.out_dim) output[idx + 3u] = termite_gated_activation_product(total3, rhs[idx + 3u], ap.activation_kind); }\n"
           "}\n"
           "kernel void termite_q4_0_get_rows(device const uchar *weight [[buffer(0)]], device const uint *ids [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_quant_rows_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total_values = p.total * p.dim; if (gid >= total_values) return; uint row = gid / p.dim; uint col = gid % p.dim; uint source_row = ids[row]; if (source_row >= p.source_rows || (p.dim & 31u) != 0u) { output[gid] = 0.0f; return; } uint row_blocks = p.dim / 32u; uint block = col / 32u; uint inner = col - block * 32u; uint off = (source_row * row_blocks + block) * 18u; float d = termite_q4_0_block_scale(weight, off); output[gid] = d * termite_q4_0_block_value(weight, off, inner) * p.scale;\n"
           "}\n"
           "kernel void termite_q4_0_set_rows(device const float *input [[buffer(0)]], device const uint *ids [[buffer(1)]], device uchar *weight [[buffer(2)]], constant termite_metal_quant_rows_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    if ((p.dim & 31u) != 0u) return; uint row_blocks = p.dim / 32u; uint total_blocks = p.total * row_blocks; if (gid >= total_blocks) return; uint row = gid / row_blocks; uint block = gid - row * row_blocks; uint target_row = ids[row]; if (target_row >= p.source_rows) return; uint input_base = row * p.dim + block * 32u; float max_abs = 0.0f; for (uint i = 0u; i < 32u; ++i) max_abs = fmax(max_abs, fabs(input[input_base + i] * p.scale)); float d = max_abs == 0.0f ? 1.0f : max_abs / 8.0f; float inv = max_abs == 0.0f ? 0.0f : 1.0f / d; uint off = (target_row * row_blocks + block) * 18u; ushort bits = as_type<ushort>(half(d)); weight[off] = uchar(bits & 0xffu); weight[off + 1u] = uchar((bits >> 8) & 0xffu); for (uint i = 0u; i < 16u; ++i) { uint lo = uint(clamp(round(input[input_base + i] * p.scale * inv + 8.0f), 0.0f, 15.0f)); uint hi = uint(clamp(round(input[input_base + 16u + i] * p.scale * inv + 8.0f), 0.0f, 15.0f)); weight[off + 2u + i] = uchar(lo | (hi << 4)); }\n"
           "}\n"
           "kernel void termite_q4_0_cpy_q_to_f32(device const uchar *weight [[buffer(0)]], device float *output [[buffer(1)]], constant termite_metal_quant_rows_params &p [[buffer(2)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total_values = p.total * p.dim; if (gid >= total_values) return; uint row = gid / p.dim; uint col = gid % p.dim; uint source_row = p.row_offset + row; if (source_row >= p.source_rows || (p.dim & 31u) != 0u) { output[gid] = 0.0f; return; } uint row_blocks = p.dim / 32u; uint block = col / 32u; uint inner = col - block * 32u; uint off = (source_row * row_blocks + block) * 18u; float d = termite_q4_0_block_scale(weight, off); output[gid] = d * termite_q4_0_block_value(weight, off, inner) * p.scale;\n"
           "}\n"
           "kernel void termite_q4_0_cpy_f32_to_q(device const float *input [[buffer(0)]], device uchar *weight [[buffer(1)]], constant termite_metal_quant_rows_params &p [[buffer(2)]], uint gid [[thread_position_in_grid]]) {\n"
           "    if ((p.dim & 31u) != 0u) return; uint row_blocks = p.dim / 32u; uint total_blocks = p.total * row_blocks; if (gid >= total_blocks) return; uint row = gid / row_blocks; uint block = gid - row * row_blocks; uint target_row = p.row_offset + row; if (target_row >= p.source_rows) return; uint input_base = row * p.dim + block * 32u; float max_abs = 0.0f; for (uint i = 0u; i < 32u; ++i) max_abs = fmax(max_abs, fabs(input[input_base + i] * p.scale)); float d = max_abs == 0.0f ? 1.0f : max_abs / 8.0f; float inv = max_abs == 0.0f ? 0.0f : 1.0f / d; uint off = (target_row * row_blocks + block) * 18u; ushort bits = as_type<ushort>(half(d)); weight[off] = uchar(bits & 0xffu); weight[off + 1u] = uchar((bits >> 8) & 0xffu); for (uint i = 0u; i < 16u; ++i) { uint lo = uint(clamp(round(input[input_base + i] * p.scale * inv + 8.0f), 0.0f, 15.0f)); uint hi = uint(clamp(round(input[input_base + 16u + i] * p.scale * inv + 8.0f), 0.0f, 15.0f)); weight[off + 2u + i] = uchar(lo | (hi << 4)); }\n"
           "}\n"
           "kernel void termite_q4_1_get_rows(device const uchar *weight [[buffer(0)]], device const uint *ids [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_quant_rows_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total_values = p.total * p.dim; if (gid >= total_values) return; uint row = gid / p.dim; uint col = gid % p.dim; uint source_row = ids[row]; if (source_row >= p.source_rows || (p.dim & 31u) != 0u) { output[gid] = 0.0f; return; } uint row_blocks = p.dim / 32u; uint block = col / 32u; uint inner = col - block * 32u; uint off = (source_row * row_blocks + block) * 20u; output[gid] = termite_q4_1_block_value(weight, off, inner) * p.scale;\n"
           "}\n"
           "kernel void termite_q4_1_set_rows(device const float *input [[buffer(0)]], device const uint *ids [[buffer(1)]], device uchar *weight [[buffer(2)]], constant termite_metal_quant_rows_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    if ((p.dim & 31u) != 0u) return; uint row_blocks = p.dim / 32u; uint total_blocks = p.total * row_blocks; if (gid >= total_blocks) return; uint row = gid / row_blocks; uint block = gid - row * row_blocks; uint target_row = ids[row]; if (target_row >= p.source_rows) return; uint input_base = row * p.dim + block * 32u; float min_v = input[input_base] * p.scale; float max_v = min_v; for (uint i = 1u; i < 32u; ++i) { float v = input[input_base + i] * p.scale; min_v = fmin(min_v, v); max_v = fmax(max_v, v); } float span = max_v - min_v; float d = span == 0.0f ? 1.0f : span / 15.0f; float inv = span == 0.0f ? 0.0f : 1.0f / d; uint off = (target_row * row_blocks + block) * 20u; ushort d_bits = as_type<ushort>(half(d)); ushort m_bits = as_type<ushort>(half(min_v)); weight[off] = uchar(d_bits & 0xffu); weight[off + 1u] = uchar((d_bits >> 8) & 0xffu); weight[off + 2u] = uchar(m_bits & 0xffu); weight[off + 3u] = uchar((m_bits >> 8) & 0xffu); for (uint i = 0u; i < 16u; ++i) { uint lo = uint(clamp(round((input[input_base + i] * p.scale - min_v) * inv), 0.0f, 15.0f)); uint hi = uint(clamp(round((input[input_base + 16u + i] * p.scale - min_v) * inv), 0.0f, 15.0f)); weight[off + 4u + i] = uchar(lo | (hi << 4)); }\n"
           "}\n"
           "kernel void termite_q4_1_cpy_q_to_f32(device const uchar *weight [[buffer(0)]], device float *output [[buffer(1)]], constant termite_metal_quant_rows_params &p [[buffer(2)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total_values = p.total * p.dim; if (gid >= total_values) return; uint row = gid / p.dim; uint col = gid % p.dim; uint source_row = p.row_offset + row; if (source_row >= p.source_rows || (p.dim & 31u) != 0u) { output[gid] = 0.0f; return; } uint row_blocks = p.dim / 32u; uint block = col / 32u; uint inner = col - block * 32u; uint off = (source_row * row_blocks + block) * 20u; output[gid] = termite_q4_1_block_value(weight, off, inner) * p.scale;\n"
           "}\n"
           "kernel void termite_q4_1_cpy_f32_to_q(device const float *input [[buffer(0)]], device uchar *weight [[buffer(1)]], constant termite_metal_quant_rows_params &p [[buffer(2)]], uint gid [[thread_position_in_grid]]) {\n"
           "    if ((p.dim & 31u) != 0u) return; uint row_blocks = p.dim / 32u; uint total_blocks = p.total * row_blocks; if (gid >= total_blocks) return; uint row = gid / row_blocks; uint block = gid - row * row_blocks; uint target_row = p.row_offset + row; if (target_row >= p.source_rows) return; uint input_base = row * p.dim + block * 32u; float min_v = input[input_base] * p.scale; float max_v = min_v; for (uint i = 1u; i < 32u; ++i) { float v = input[input_base + i] * p.scale; min_v = fmin(min_v, v); max_v = fmax(max_v, v); } float span = max_v - min_v; float d = span == 0.0f ? 1.0f : span / 15.0f; float inv = span == 0.0f ? 0.0f : 1.0f / d; uint off = (target_row * row_blocks + block) * 20u; ushort d_bits = as_type<ushort>(half(d)); ushort m_bits = as_type<ushort>(half(min_v)); weight[off] = uchar(d_bits & 0xffu); weight[off + 1u] = uchar((d_bits >> 8) & 0xffu); weight[off + 2u] = uchar(m_bits & 0xffu); weight[off + 3u] = uchar((m_bits >> 8) & 0xffu); for (uint i = 0u; i < 16u; ++i) { uint lo = uint(clamp(round((input[input_base + i] * p.scale - min_v) * inv), 0.0f, 15.0f)); uint hi = uint(clamp(round((input[input_base + 16u + i] * p.scale - min_v) * inv), 0.0f, 15.0f)); weight[off + 4u + i] = uchar(lo | (hi << 4)); }\n"
           "}\n"
           "kernel void termite_q5_0_get_rows(device const uchar *weight [[buffer(0)]], device const uint *ids [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_quant_rows_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total_values = p.total * p.dim; if (gid >= total_values) return; uint row = gid / p.dim; uint col = gid % p.dim; uint source_row = ids[row]; if (source_row >= p.source_rows || (p.dim & 31u) != 0u) { output[gid] = 0.0f; return; } uint row_blocks = p.dim / 32u; uint block = col / 32u; uint inner = col - block * 32u; uint off = (source_row * row_blocks + block) * 22u; float d = termite_q5_0_block_scale(weight, off); output[gid] = d * termite_q5_0_block_value(weight, off, inner) * p.scale;\n"
           "}\n"
           "kernel void termite_q5_0_set_rows(device const float *input [[buffer(0)]], device const uint *ids [[buffer(1)]], device uchar *weight [[buffer(2)]], constant termite_metal_quant_rows_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    if ((p.dim & 31u) != 0u) return; uint row_blocks = p.dim / 32u; uint total_blocks = p.total * row_blocks; if (gid >= total_blocks) return; uint row = gid / row_blocks; uint block = gid - row * row_blocks; uint target_row = ids[row]; if (target_row >= p.source_rows) return; uint input_base = row * p.dim + block * 32u; float max_abs = 0.0f; for (uint i = 0u; i < 32u; ++i) max_abs = fmax(max_abs, fabs(input[input_base + i] * p.scale)); float d = max_abs == 0.0f ? 1.0f : max_abs / 16.0f; float inv = max_abs == 0.0f ? 0.0f : 1.0f / d; uint off = (target_row * row_blocks + block) * 22u; ushort bits = as_type<ushort>(half(d)); weight[off] = uchar(bits & 0xffu); weight[off + 1u] = uchar((bits >> 8) & 0xffu); uint qh = 0u; for (uint i = 0u; i < 16u; ++i) { uint lo = uint(clamp(round(input[input_base + i] * p.scale * inv + 16.0f), 0.0f, 31.0f)); uint hi = uint(clamp(round(input[input_base + 16u + i] * p.scale * inv + 16.0f), 0.0f, 31.0f)); weight[off + 6u + i] = uchar((lo & 0x0fu) | ((hi & 0x0fu) << 4)); qh |= ((lo >> 4) & 1u) << i; qh |= ((hi >> 4) & 1u) << (i + 16u); } weight[off + 2u] = uchar(qh & 0xffu); weight[off + 3u] = uchar((qh >> 8) & 0xffu); weight[off + 4u] = uchar((qh >> 16) & 0xffu); weight[off + 5u] = uchar((qh >> 24) & 0xffu);\n"
           "}\n"
           "kernel void termite_q5_0_cpy_q_to_f32(device const uchar *weight [[buffer(0)]], device float *output [[buffer(1)]], constant termite_metal_quant_rows_params &p [[buffer(2)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total_values = p.total * p.dim; if (gid >= total_values) return; uint row = gid / p.dim; uint col = gid % p.dim; uint source_row = p.row_offset + row; if (source_row >= p.source_rows || (p.dim & 31u) != 0u) { output[gid] = 0.0f; return; } uint row_blocks = p.dim / 32u; uint block = col / 32u; uint inner = col - block * 32u; uint off = (source_row * row_blocks + block) * 22u; float d = termite_q5_0_block_scale(weight, off); output[gid] = d * termite_q5_0_block_value(weight, off, inner) * p.scale;\n"
           "}\n"
           "kernel void termite_q5_0_cpy_f32_to_q(device const float *input [[buffer(0)]], device uchar *weight [[buffer(1)]], constant termite_metal_quant_rows_params &p [[buffer(2)]], uint gid [[thread_position_in_grid]]) {\n"
           "    if ((p.dim & 31u) != 0u) return; uint row_blocks = p.dim / 32u; uint total_blocks = p.total * row_blocks; if (gid >= total_blocks) return; uint row = gid / row_blocks; uint block = gid - row * row_blocks; uint target_row = p.row_offset + row; if (target_row >= p.source_rows) return; uint input_base = row * p.dim + block * 32u; float max_abs = 0.0f; for (uint i = 0u; i < 32u; ++i) max_abs = fmax(max_abs, fabs(input[input_base + i] * p.scale)); float d = max_abs == 0.0f ? 1.0f : max_abs / 16.0f; float inv = max_abs == 0.0f ? 0.0f : 1.0f / d; uint off = (target_row * row_blocks + block) * 22u; ushort bits = as_type<ushort>(half(d)); weight[off] = uchar(bits & 0xffu); weight[off + 1u] = uchar((bits >> 8) & 0xffu); uint qh = 0u; for (uint i = 0u; i < 16u; ++i) { uint lo = uint(clamp(round(input[input_base + i] * p.scale * inv + 16.0f), 0.0f, 31.0f)); uint hi = uint(clamp(round(input[input_base + 16u + i] * p.scale * inv + 16.0f), 0.0f, 31.0f)); weight[off + 6u + i] = uchar((lo & 0x0fu) | ((hi & 0x0fu) << 4)); qh |= ((lo >> 4) & 1u) << i; qh |= ((hi >> 4) & 1u) << (i + 16u); } weight[off + 2u] = uchar(qh & 0xffu); weight[off + 3u] = uchar((qh >> 8) & 0xffu); weight[off + 4u] = uchar((qh >> 16) & 0xffu); weight[off + 5u] = uchar((qh >> 24) & 0xffu);\n"
           "}\n"
           "kernel void termite_q5_1_get_rows(device const uchar *weight [[buffer(0)]], device const uint *ids [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_quant_rows_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total_values = p.total * p.dim; if (gid >= total_values) return; uint row = gid / p.dim; uint col = gid % p.dim; uint source_row = ids[row]; if (source_row >= p.source_rows || (p.dim & 31u) != 0u) { output[gid] = 0.0f; return; } uint row_blocks = p.dim / 32u; uint block = col / 32u; uint inner = col - block * 32u; uint off = (source_row * row_blocks + block) * 24u; output[gid] = termite_q5_1_block_value(weight, off, inner) * p.scale;\n"
           "}\n"
           "kernel void termite_q5_1_set_rows(device const float *input [[buffer(0)]], device const uint *ids [[buffer(1)]], device uchar *weight [[buffer(2)]], constant termite_metal_quant_rows_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    if ((p.dim & 31u) != 0u) return; uint row_blocks = p.dim / 32u; uint total_blocks = p.total * row_blocks; if (gid >= total_blocks) return; uint row = gid / row_blocks; uint block = gid - row * row_blocks; uint target_row = ids[row]; if (target_row >= p.source_rows) return; uint input_base = row * p.dim + block * 32u; float min_v = input[input_base] * p.scale; float max_v = min_v; for (uint i = 1u; i < 32u; ++i) { float v = input[input_base + i] * p.scale; min_v = fmin(min_v, v); max_v = fmax(max_v, v); } float span = max_v - min_v; float d = span == 0.0f ? 1.0f : span / 31.0f; float inv = span == 0.0f ? 0.0f : 1.0f / d; uint off = (target_row * row_blocks + block) * 24u; ushort d_bits = as_type<ushort>(half(d)); ushort m_bits = as_type<ushort>(half(min_v)); weight[off] = uchar(d_bits & 0xffu); weight[off + 1u] = uchar((d_bits >> 8) & 0xffu); weight[off + 2u] = uchar(m_bits & 0xffu); weight[off + 3u] = uchar((m_bits >> 8) & 0xffu); uint qh = 0u; for (uint i = 0u; i < 16u; ++i) { uint lo = uint(clamp(round((input[input_base + i] * p.scale - min_v) * inv), 0.0f, 31.0f)); uint hi = uint(clamp(round((input[input_base + 16u + i] * p.scale - min_v) * inv), 0.0f, 31.0f)); weight[off + 8u + i] = uchar((lo & 0x0fu) | ((hi & 0x0fu) << 4)); qh |= ((lo >> 4) & 1u) << i; qh |= ((hi >> 4) & 1u) << (i + 16u); } weight[off + 4u] = uchar(qh & 0xffu); weight[off + 5u] = uchar((qh >> 8) & 0xffu); weight[off + 6u] = uchar((qh >> 16) & 0xffu); weight[off + 7u] = uchar((qh >> 24) & 0xffu);\n"
           "}\n"
           "kernel void termite_q5_1_cpy_q_to_f32(device const uchar *weight [[buffer(0)]], device float *output [[buffer(1)]], constant termite_metal_quant_rows_params &p [[buffer(2)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total_values = p.total * p.dim; if (gid >= total_values) return; uint row = gid / p.dim; uint col = gid % p.dim; uint source_row = p.row_offset + row; if (source_row >= p.source_rows || (p.dim & 31u) != 0u) { output[gid] = 0.0f; return; } uint row_blocks = p.dim / 32u; uint block = col / 32u; uint inner = col - block * 32u; uint off = (source_row * row_blocks + block) * 24u; output[gid] = termite_q5_1_block_value(weight, off, inner) * p.scale;\n"
           "}\n"
           "kernel void termite_q5_1_cpy_f32_to_q(device const float *input [[buffer(0)]], device uchar *weight [[buffer(1)]], constant termite_metal_quant_rows_params &p [[buffer(2)]], uint gid [[thread_position_in_grid]]) {\n"
           "    if ((p.dim & 31u) != 0u) return; uint row_blocks = p.dim / 32u; uint total_blocks = p.total * row_blocks; if (gid >= total_blocks) return; uint row = gid / row_blocks; uint block = gid - row * row_blocks; uint target_row = p.row_offset + row; if (target_row >= p.source_rows) return; uint input_base = row * p.dim + block * 32u; float min_v = input[input_base] * p.scale; float max_v = min_v; for (uint i = 1u; i < 32u; ++i) { float v = input[input_base + i] * p.scale; min_v = fmin(min_v, v); max_v = fmax(max_v, v); } float span = max_v - min_v; float d = span == 0.0f ? 1.0f : span / 31.0f; float inv = span == 0.0f ? 0.0f : 1.0f / d; uint off = (target_row * row_blocks + block) * 24u; ushort d_bits = as_type<ushort>(half(d)); ushort m_bits = as_type<ushort>(half(min_v)); weight[off] = uchar(d_bits & 0xffu); weight[off + 1u] = uchar((d_bits >> 8) & 0xffu); weight[off + 2u] = uchar(m_bits & 0xffu); weight[off + 3u] = uchar((m_bits >> 8) & 0xffu); uint qh = 0u; for (uint i = 0u; i < 16u; ++i) { uint lo = uint(clamp(round((input[input_base + i] * p.scale - min_v) * inv), 0.0f, 31.0f)); uint hi = uint(clamp(round((input[input_base + 16u + i] * p.scale - min_v) * inv), 0.0f, 31.0f)); weight[off + 8u + i] = uchar((lo & 0x0fu) | ((hi & 0x0fu) << 4)); qh |= ((lo >> 4) & 1u) << i; qh |= ((hi >> 4) & 1u) << (i + 16u); } weight[off + 4u] = uchar(qh & 0xffu); weight[off + 5u] = uchar((qh >> 8) & 0xffu); weight[off + 6u] = uchar((qh >> 16) & 0xffu); weight[off + 7u] = uchar((qh >> 24) & 0xffu);\n"
           "}\n"
           "kernel void termite_q4_k_get_rows(device const uchar *weight [[buffer(0)]], device const uint *ids [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_quant_rows_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total_values = p.total * p.dim; if (gid >= total_values) return; uint row = gid / p.dim; uint col = gid % p.dim; uint source_row = ids[row]; if (source_row >= p.source_rows || (p.dim & 255u) != 0u) { output[gid] = 0.0f; return; } uint row_blocks = p.dim / 256u; uint block = col / 256u; uint inner = col - block * 256u; uint off = (source_row * row_blocks + block) * 144u; output[gid] = termite_q4_k_block_value(weight, off, inner) * p.scale;\n"
           "}\n"
           "kernel void termite_q4_k_set_rows(device const float *input [[buffer(0)]], device const uint *ids [[buffer(1)]], device uchar *weight [[buffer(2)]], constant termite_metal_quant_rows_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    if ((p.dim & 255u) != 0u) return; uint row_blocks = p.dim / 256u; uint total_blocks = p.total * row_blocks; if (gid >= total_blocks) return; uint row = gid / row_blocks; uint block = gid - row * row_blocks; uint target_row = ids[row]; if (target_row >= p.source_rows) return; uint input_base = row * p.dim + block * 256u; uint off = (target_row * row_blocks + block) * 144u; for (uint i = 0u; i < 144u; ++i) weight[off + i] = 0u; float sub_scales[8]; float sub_mins[8]; float max_scale = 0.0f; float max_min = 0.0f; for (uint sub = 0u; sub < 8u; ++sub) { float min_v = input[input_base + sub * 32u] * p.scale; float max_v = min_v; for (uint i = 1u; i < 32u; ++i) { float v = input[input_base + sub * 32u + i] * p.scale; min_v = fmin(min_v, v); max_v = fmax(max_v, v); } float min_mag = fmax(-min_v, 0.0f); float sc = (max_v + min_mag) == 0.0f ? 1.0f : (max_v + min_mag) / 15.0f; sub_scales[sub] = sc; sub_mins[sub] = min_mag; max_scale = fmax(max_scale, sc); max_min = fmax(max_min, min_mag); } float d = max_scale == 0.0f ? 1.0f : max_scale / 63.0f; float dmin = max_min == 0.0f ? 1.0f : max_min / 63.0f; ushort d_bits = as_type<ushort>(half(d)); ushort dmin_bits = as_type<ushort>(half(dmin)); weight[off] = uchar(d_bits & 0xffu); weight[off + 1u] = uchar((d_bits >> 8) & 0xffu); weight[off + 2u] = uchar(dmin_bits & 0xffu); weight[off + 3u] = uchar((dmin_bits >> 8) & 0xffu); uint scs[8]; uint mins[8]; for (uint sub = 0u; sub < 8u; ++sub) { scs[sub] = max_scale == 0.0f ? 0u : uint(clamp(round(sub_scales[sub] / d), 0.0f, 63.0f)); mins[sub] = max_min == 0.0f ? 0u : uint(clamp(round(sub_mins[sub] / dmin), 0.0f, 63.0f)); } pack_q4k_scale_mins(weight + off + 4u, scs, mins); for (uint chunk = 0u; chunk < 4u; ++chunk) { uint sub0 = chunk * 2u; uint sub1 = sub0 + 1u; float inv0 = sub_scales[sub0] == 0.0f ? 0.0f : 1.0f / sub_scales[sub0]; float inv1 = sub_scales[sub1] == 0.0f ? 0.0f : 1.0f / sub_scales[sub1]; uint q_off = off + 16u + chunk * 32u; for (uint i = 0u; i < 32u; ++i) { uint q0 = uint(clamp(round((input[input_base + sub0 * 32u + i] * p.scale + sub_mins[sub0]) * inv0), 0.0f, 15.0f)); uint q1 = uint(clamp(round((input[input_base + sub1 * 32u + i] * p.scale + sub_mins[sub1]) * inv1), 0.0f, 15.0f)); weight[q_off + i] = uchar(q0 | (q1 << 4)); } }\n"
           "}\n"
           "kernel void termite_q4_k_cpy_q_to_f32(device const uchar *weight [[buffer(0)]], device float *output [[buffer(1)]], constant termite_metal_quant_rows_params &p [[buffer(2)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total_values = p.total * p.dim; if (gid >= total_values) return; uint row = gid / p.dim; uint col = gid % p.dim; uint source_row = p.row_offset + row; if (source_row >= p.source_rows || (p.dim & 255u) != 0u) { output[gid] = 0.0f; return; } uint row_blocks = p.dim / 256u; uint block = col / 256u; uint inner = col - block * 256u; uint off = (source_row * row_blocks + block) * 144u; output[gid] = termite_q4_k_block_value(weight, off, inner) * p.scale;\n"
           "}\n"
           "kernel void termite_q4_k_cpy_f32_to_q(device const float *input [[buffer(0)]], device uchar *weight [[buffer(1)]], constant termite_metal_quant_rows_params &p [[buffer(2)]], uint gid [[thread_position_in_grid]]) {\n"
           "    if ((p.dim & 255u) != 0u) return; uint row_blocks = p.dim / 256u; uint total_blocks = p.total * row_blocks; if (gid >= total_blocks) return; uint row = gid / row_blocks; uint block = gid - row * row_blocks; uint target_row = p.row_offset + row; if (target_row >= p.source_rows) return; uint input_base = row * p.dim + block * 256u; uint off = (target_row * row_blocks + block) * 144u; for (uint i = 0u; i < 144u; ++i) weight[off + i] = 0u; float sub_scales[8]; float sub_mins[8]; float max_scale = 0.0f; float max_min = 0.0f; for (uint sub = 0u; sub < 8u; ++sub) { float min_v = input[input_base + sub * 32u] * p.scale; float max_v = min_v; for (uint i = 1u; i < 32u; ++i) { float v = input[input_base + sub * 32u + i] * p.scale; min_v = fmin(min_v, v); max_v = fmax(max_v, v); } float min_mag = fmax(-min_v, 0.0f); float sc = (max_v + min_mag) == 0.0f ? 1.0f : (max_v + min_mag) / 15.0f; sub_scales[sub] = sc; sub_mins[sub] = min_mag; max_scale = fmax(max_scale, sc); max_min = fmax(max_min, min_mag); } float d = max_scale == 0.0f ? 1.0f : max_scale / 63.0f; float dmin = max_min == 0.0f ? 1.0f : max_min / 63.0f; ushort d_bits = as_type<ushort>(half(d)); ushort dmin_bits = as_type<ushort>(half(dmin)); weight[off] = uchar(d_bits & 0xffu); weight[off + 1u] = uchar((d_bits >> 8) & 0xffu); weight[off + 2u] = uchar(dmin_bits & 0xffu); weight[off + 3u] = uchar((dmin_bits >> 8) & 0xffu); uint scs[8]; uint mins[8]; for (uint sub = 0u; sub < 8u; ++sub) { scs[sub] = max_scale == 0.0f ? 0u : uint(clamp(round(sub_scales[sub] / d), 0.0f, 63.0f)); mins[sub] = max_min == 0.0f ? 0u : uint(clamp(round(sub_mins[sub] / dmin), 0.0f, 63.0f)); } pack_q4k_scale_mins(weight + off + 4u, scs, mins); for (uint chunk = 0u; chunk < 4u; ++chunk) { uint sub0 = chunk * 2u; uint sub1 = sub0 + 1u; float inv0 = sub_scales[sub0] == 0.0f ? 0.0f : 1.0f / sub_scales[sub0]; float inv1 = sub_scales[sub1] == 0.0f ? 0.0f : 1.0f / sub_scales[sub1]; uint q_off = off + 16u + chunk * 32u; for (uint i = 0u; i < 32u; ++i) { uint q0 = uint(clamp(round((input[input_base + sub0 * 32u + i] * p.scale + sub_mins[sub0]) * inv0), 0.0f, 15.0f)); uint q1 = uint(clamp(round((input[input_base + sub1 * 32u + i] * p.scale + sub_mins[sub1]) * inv1), 0.0f, 15.0f)); weight[q_off + i] = uchar(q0 | (q1 << 4)); } }\n"
           "}\n"
           "kernel void termite_q5_k_get_rows(device const uchar *weight [[buffer(0)]], device const uint *ids [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_quant_rows_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total_values = p.total * p.dim; if (gid >= total_values) return; uint row = gid / p.dim; uint col = gid % p.dim; uint source_row = ids[row]; if (source_row >= p.source_rows || (p.dim & 255u) != 0u) { output[gid] = 0.0f; return; } uint row_blocks = p.dim / 256u; uint block = col / 256u; uint inner = col - block * 256u; uint off = (source_row * row_blocks + block) * 176u; output[gid] = termite_q5_k_block_value(weight, off, inner) * p.scale;\n"
           "}\n"
           "kernel void termite_q5_k_set_rows(device const float *input [[buffer(0)]], device const uint *ids [[buffer(1)]], device uchar *weight [[buffer(2)]], constant termite_metal_quant_rows_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    if ((p.dim & 255u) != 0u) return; uint row_blocks = p.dim / 256u; uint total_blocks = p.total * row_blocks; if (gid >= total_blocks) return; uint row = gid / row_blocks; uint block = gid - row * row_blocks; uint target_row = ids[row]; if (target_row >= p.source_rows) return; uint input_base = row * p.dim + block * 256u; uint off = (target_row * row_blocks + block) * 176u; for (uint i = 0u; i < 176u; ++i) weight[off + i] = 0u; float sub_scales[8]; float sub_mins[8]; float max_scale = 0.0f; float max_min = 0.0f; for (uint sub = 0u; sub < 8u; ++sub) { float min_v = input[input_base + sub * 32u] * p.scale; float max_v = min_v; for (uint i = 1u; i < 32u; ++i) { float v = input[input_base + sub * 32u + i] * p.scale; min_v = fmin(min_v, v); max_v = fmax(max_v, v); } float min_mag = fmax(-min_v, 0.0f); float sc = (max_v + min_mag) == 0.0f ? 1.0f : (max_v + min_mag) / 31.0f; sub_scales[sub] = sc; sub_mins[sub] = min_mag; max_scale = fmax(max_scale, sc); max_min = fmax(max_min, min_mag); } float d = max_scale == 0.0f ? 1.0f : max_scale / 63.0f; float dmin = max_min == 0.0f ? 1.0f : max_min / 63.0f; ushort d_bits = as_type<ushort>(half(d)); ushort dmin_bits = as_type<ushort>(half(dmin)); weight[off] = uchar(d_bits & 0xffu); weight[off + 1u] = uchar((d_bits >> 8) & 0xffu); weight[off + 2u] = uchar(dmin_bits & 0xffu); weight[off + 3u] = uchar((dmin_bits >> 8) & 0xffu); uint scs[8]; uint mins[8]; for (uint sub = 0u; sub < 8u; ++sub) { scs[sub] = max_scale == 0.0f ? 0u : uint(clamp(round(sub_scales[sub] / d), 0.0f, 63.0f)); mins[sub] = max_min == 0.0f ? 0u : uint(clamp(round(sub_mins[sub] / dmin), 0.0f, 63.0f)); } pack_q4k_scale_mins(weight + off + 4u, scs, mins); for (uint sub = 0u; sub < 8u; ++sub) { uint chunk = sub / 2u; bool high = (sub & 1u) != 0u; uint ql_off = off + 48u + chunk * 32u; float inv = sub_scales[sub] == 0.0f ? 0.0f : 1.0f / sub_scales[sub]; for (uint i = 0u; i < 32u; ++i) { uint q = uint(clamp(round((input[input_base + sub * 32u + i] * p.scale + sub_mins[sub]) * inv), 0.0f, 31.0f)); if (high) weight[ql_off + i] |= uchar((q & 0x0fu) << 4); else weight[ql_off + i] |= uchar(q & 0x0fu); weight[off + 16u + i] |= uchar(((q >> 4) & 1u) << sub); } }\n"
           "}\n"
           "kernel void termite_q5_k_cpy_q_to_f32(device const uchar *weight [[buffer(0)]], device float *output [[buffer(1)]], constant termite_metal_quant_rows_params &p [[buffer(2)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total_values = p.total * p.dim; if (gid >= total_values) return; uint row = gid / p.dim; uint col = gid % p.dim; uint source_row = p.row_offset + row; if (source_row >= p.source_rows || (p.dim & 255u) != 0u) { output[gid] = 0.0f; return; } uint row_blocks = p.dim / 256u; uint block = col / 256u; uint inner = col - block * 256u; uint off = (source_row * row_blocks + block) * 176u; output[gid] = termite_q5_k_block_value(weight, off, inner) * p.scale;\n"
           "}\n"
           "kernel void termite_q5_k_cpy_f32_to_q(device const float *input [[buffer(0)]], device uchar *weight [[buffer(1)]], constant termite_metal_quant_rows_params &p [[buffer(2)]], uint gid [[thread_position_in_grid]]) {\n"
           "    if ((p.dim & 255u) != 0u) return; uint row_blocks = p.dim / 256u; uint total_blocks = p.total * row_blocks; if (gid >= total_blocks) return; uint row = gid / row_blocks; uint block = gid - row * row_blocks; uint target_row = p.row_offset + row; if (target_row >= p.source_rows) return; uint input_base = row * p.dim + block * 256u; uint off = (target_row * row_blocks + block) * 176u; for (uint i = 0u; i < 176u; ++i) weight[off + i] = 0u; float sub_scales[8]; float sub_mins[8]; float max_scale = 0.0f; float max_min = 0.0f; for (uint sub = 0u; sub < 8u; ++sub) { float min_v = input[input_base + sub * 32u] * p.scale; float max_v = min_v; for (uint i = 1u; i < 32u; ++i) { float v = input[input_base + sub * 32u + i] * p.scale; min_v = fmin(min_v, v); max_v = fmax(max_v, v); } float min_mag = fmax(-min_v, 0.0f); float sc = (max_v + min_mag) == 0.0f ? 1.0f : (max_v + min_mag) / 31.0f; sub_scales[sub] = sc; sub_mins[sub] = min_mag; max_scale = fmax(max_scale, sc); max_min = fmax(max_min, min_mag); } float d = max_scale == 0.0f ? 1.0f : max_scale / 63.0f; float dmin = max_min == 0.0f ? 1.0f : max_min / 63.0f; ushort d_bits = as_type<ushort>(half(d)); ushort dmin_bits = as_type<ushort>(half(dmin)); weight[off] = uchar(d_bits & 0xffu); weight[off + 1u] = uchar((d_bits >> 8) & 0xffu); weight[off + 2u] = uchar(dmin_bits & 0xffu); weight[off + 3u] = uchar((dmin_bits >> 8) & 0xffu); uint scs[8]; uint mins[8]; for (uint sub = 0u; sub < 8u; ++sub) { scs[sub] = max_scale == 0.0f ? 0u : uint(clamp(round(sub_scales[sub] / d), 0.0f, 63.0f)); mins[sub] = max_min == 0.0f ? 0u : uint(clamp(round(sub_mins[sub] / dmin), 0.0f, 63.0f)); } pack_q4k_scale_mins(weight + off + 4u, scs, mins); for (uint sub = 0u; sub < 8u; ++sub) { uint chunk = sub / 2u; bool high = (sub & 1u) != 0u; uint ql_off = off + 48u + chunk * 32u; float inv = sub_scales[sub] == 0.0f ? 0.0f : 1.0f / sub_scales[sub]; for (uint i = 0u; i < 32u; ++i) { uint q = uint(clamp(round((input[input_base + sub * 32u + i] * p.scale + sub_mins[sub]) * inv), 0.0f, 31.0f)); if (high) weight[ql_off + i] |= uchar((q & 0x0fu) << 4); else weight[ql_off + i] |= uchar(q & 0x0fu); weight[off + 16u + i] |= uchar(((q >> 4) & 1u) << sub); } }\n"
           "}\n"
           "kernel void termite_q6_k_get_rows(device const uchar *weight [[buffer(0)]], device const uint *ids [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_quant_rows_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total_values = p.total * p.dim; if (gid >= total_values) return; uint row = gid / p.dim; uint col = gid % p.dim; uint source_row = ids[row]; if (source_row >= p.source_rows || (p.dim & 255u) != 0u) { output[gid] = 0.0f; return; } uint row_blocks = p.dim / 256u; uint block = col / 256u; uint inner = col - block * 256u; uint off = (source_row * row_blocks + block) * 210u; output[gid] = termite_q6_k_block_value(weight, off, inner) * p.scale;\n"
           "}\n"
           "kernel void termite_q6_k_set_rows(device const float *input [[buffer(0)]], device const uint *ids [[buffer(1)]], device uchar *weight [[buffer(2)]], constant termite_metal_quant_rows_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    if ((p.dim & 255u) != 0u) return; uint row_blocks = p.dim / 256u; uint total_blocks = p.total * row_blocks; if (gid >= total_blocks) return; uint row = gid / row_blocks; uint block = gid - row * row_blocks; uint target_row = ids[row]; if (target_row >= p.source_rows) return; uint input_base = row * p.dim + block * 256u; uint off = (target_row * row_blocks + block) * 210u; for (uint i = 0u; i < 210u; ++i) weight[off + i] = 0u; float sub_scales[16]; float max_scale = 0.0f; for (uint sub = 0u; sub < 16u; ++sub) { float sub_max = 0.0f; for (uint i = 0u; i < 16u; ++i) sub_max = fmax(sub_max, fabs(input[input_base + sub * 16u + i] * p.scale)); float sc = sub_max == 0.0f ? 1.0f : sub_max / 31.0f; sub_scales[sub] = sc; max_scale = fmax(max_scale, sc); } float d = max_scale == 0.0f ? 1.0f : max_scale / 127.0f; ushort d_bits = as_type<ushort>(half(d)); weight[off + 208u] = uchar(d_bits & 0xffu); weight[off + 209u] = uchar((d_bits >> 8) & 0xffu); for (uint sub = 0u; sub < 16u; ++sub) { uint scaled = max_scale == 0.0f ? 0u : uint(clamp(round(sub_scales[sub] / d), 0.0f, 127.0f)); weight[off + 192u + sub] = as_type<uchar>(char(scaled)); uint half_idx = sub / 8u; uint group = (sub % 8u) / 2u; uint l_base = (sub % 2u) * 16u; uint ql_off = half_idx * 64u + (group & 1u) * 32u; uint qh_off = half_idx * 32u; uint qh_shift = group * 2u; uint nibble_shift = (group / 2u) * 4u; float inv = sub_scales[sub] == 0.0f ? 0.0f : 1.0f / sub_scales[sub]; for (uint i = 0u; i < 16u; ++i) { float v = input[input_base + sub * 16u + i] * p.scale; uint q = uint(clamp(round(v * inv + 32.0f), 0.0f, 63.0f)); uint l = l_base + i; weight[off + ql_off + l] |= uchar((q & 0x0fu) << nibble_shift); weight[off + 128u + qh_off + l] |= uchar(((q >> 4) & 0x03u) << qh_shift); } }\n"
           "}\n"
           "kernel void termite_q6_k_cpy_q_to_f32(device const uchar *weight [[buffer(0)]], device float *output [[buffer(1)]], constant termite_metal_quant_rows_params &p [[buffer(2)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total_values = p.total * p.dim; if (gid >= total_values) return; uint row = gid / p.dim; uint col = gid % p.dim; uint source_row = p.row_offset + row; if (source_row >= p.source_rows || (p.dim & 255u) != 0u) { output[gid] = 0.0f; return; } uint row_blocks = p.dim / 256u; uint block = col / 256u; uint inner = col - block * 256u; uint off = (source_row * row_blocks + block) * 210u; output[gid] = termite_q6_k_block_value(weight, off, inner) * p.scale;\n"
           "}\n"
           "kernel void termite_q6_k_cpy_f32_to_q(device const float *input [[buffer(0)]], device uchar *weight [[buffer(1)]], constant termite_metal_quant_rows_params &p [[buffer(2)]], uint gid [[thread_position_in_grid]]) {\n"
           "    if ((p.dim & 255u) != 0u) return; uint row_blocks = p.dim / 256u; uint total_blocks = p.total * row_blocks; if (gid >= total_blocks) return; uint row = gid / row_blocks; uint block = gid - row * row_blocks; uint target_row = p.row_offset + row; if (target_row >= p.source_rows) return; uint input_base = row * p.dim + block * 256u; uint off = (target_row * row_blocks + block) * 210u; for (uint i = 0u; i < 210u; ++i) weight[off + i] = 0u; float sub_scales[16]; float max_scale = 0.0f; for (uint sub = 0u; sub < 16u; ++sub) { float sub_max = 0.0f; for (uint i = 0u; i < 16u; ++i) sub_max = fmax(sub_max, fabs(input[input_base + sub * 16u + i] * p.scale)); float sc = sub_max == 0.0f ? 1.0f : sub_max / 31.0f; sub_scales[sub] = sc; max_scale = fmax(max_scale, sc); } float d = max_scale == 0.0f ? 1.0f : max_scale / 127.0f; ushort d_bits = as_type<ushort>(half(d)); weight[off + 208u] = uchar(d_bits & 0xffu); weight[off + 209u] = uchar((d_bits >> 8) & 0xffu); for (uint sub = 0u; sub < 16u; ++sub) { uint scaled = max_scale == 0.0f ? 0u : uint(clamp(round(sub_scales[sub] / d), 0.0f, 127.0f)); weight[off + 192u + sub] = as_type<uchar>(char(scaled)); uint half_idx = sub / 8u; uint group = (sub % 8u) / 2u; uint l_base = (sub % 2u) * 16u; uint ql_off = half_idx * 64u + (group & 1u) * 32u; uint qh_off = half_idx * 32u; uint qh_shift = group * 2u; uint nibble_shift = (group / 2u) * 4u; float inv = sub_scales[sub] == 0.0f ? 0.0f : 1.0f / sub_scales[sub]; for (uint i = 0u; i < 16u; ++i) { float v = input[input_base + sub * 16u + i] * p.scale; uint q = uint(clamp(round(v * inv + 32.0f), 0.0f, 63.0f)); uint l = l_base + i; weight[off + ql_off + l] |= uchar((q & 0x0fu) << nibble_shift); weight[off + 128u + qh_off + l] |= uchar(((q >> 4) & 0x03u) << qh_shift); } }\n"
           "}\n"
           "kernel void termite_q8_0_get_rows(device const uchar *weight [[buffer(0)]], device const uint *ids [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_quant_rows_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total_values = p.total * p.dim; if (gid >= total_values) return; uint row = gid / p.dim; uint col = gid % p.dim; uint source_row = ids[row]; if (source_row >= p.source_rows || (p.dim & 31u) != 0u) { output[gid] = 0.0f; return; } uint row_blocks = p.dim / 32u; uint block = col / 32u; uint inner = col - block * 32u; uint off = (source_row * row_blocks + block) * 34u; float d = termite_q8_0_block_scale(weight, off); output[gid] = d * termite_q8_0_block_value(weight, off, inner) * p.scale;\n"
           "}\n"
           "kernel void termite_q8_0_set_rows(device const float *input [[buffer(0)]], device const uint *ids [[buffer(1)]], device uchar *weight [[buffer(2)]], constant termite_metal_quant_rows_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    if ((p.dim & 31u) != 0u) return; uint row_blocks = p.dim / 32u; uint total_blocks = p.total * row_blocks; if (gid >= total_blocks) return; uint row = gid / row_blocks; uint block = gid - row * row_blocks; uint target_row = ids[row]; if (target_row >= p.source_rows) return; uint input_base = row * p.dim + block * 32u; float max_abs = 0.0f; for (uint i = 0u; i < 32u; ++i) max_abs = fmax(max_abs, fabs(input[input_base + i] * p.scale)); float d = max_abs == 0.0f ? 0.0f : max_abs / 127.0f; float inv = d == 0.0f ? 0.0f : 1.0f / d; uint off = (target_row * row_blocks + block) * 34u; ushort bits = as_type<ushort>(half(d)); weight[off] = uchar(bits & 0xffu); weight[off + 1u] = uchar((bits >> 8) & 0xffu); for (uint i = 0u; i < 32u; ++i) { float scaled = input[input_base + i] * p.scale; int q = int(round(clamp(scaled * inv, -127.0f, 127.0f))); weight[off + 2u + i] = as_type<uchar>(char(q)); }\n"
           "}\n"
           "kernel void termite_q8_0_cpy_q_to_f32(device const uchar *weight [[buffer(0)]], device float *output [[buffer(1)]], constant termite_metal_quant_rows_params &p [[buffer(2)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total_values = p.total * p.dim; if (gid >= total_values) return; uint row = gid / p.dim; uint col = gid % p.dim; uint source_row = p.row_offset + row; if (source_row >= p.source_rows || (p.dim & 31u) != 0u) { output[gid] = 0.0f; return; } uint row_blocks = p.dim / 32u; uint block = col / 32u; uint inner = col - block * 32u; uint off = (source_row * row_blocks + block) * 34u; float d = termite_q8_0_block_scale(weight, off); output[gid] = d * termite_q8_0_block_value(weight, off, inner) * p.scale;\n"
           "}\n"
           "kernel void termite_q8_0_cpy_f32_to_q(device const float *input [[buffer(0)]], device uchar *weight [[buffer(1)]], constant termite_metal_quant_rows_params &p [[buffer(2)]], uint gid [[thread_position_in_grid]]) {\n"
           "    if ((p.dim & 31u) != 0u) return; uint row_blocks = p.dim / 32u; uint total_blocks = p.total * row_blocks; if (gid >= total_blocks) return; uint row = gid / row_blocks; uint block = gid - row * row_blocks; uint target_row = p.row_offset + row; if (target_row >= p.source_rows) return; uint input_base = row * p.dim + block * 32u; float max_abs = 0.0f; for (uint i = 0u; i < 32u; ++i) max_abs = fmax(max_abs, fabs(input[input_base + i] * p.scale)); float d = max_abs == 0.0f ? 0.0f : max_abs / 127.0f; float inv = d == 0.0f ? 0.0f : 1.0f / d; uint off = (target_row * row_blocks + block) * 34u; ushort bits = as_type<ushort>(half(d)); weight[off] = uchar(bits & 0xffu); weight[off + 1u] = uchar((bits >> 8) & 0xffu); for (uint i = 0u; i < 32u; ++i) { float scaled = input[input_base + i] * p.scale; int q = int(round(clamp(scaled * inv, -127.0f, 127.0f))); weight[off + 2u + i] = as_type<uchar>(char(q)); }\n"
           "}\n"
           "kernel void termite_q8_1_get_rows(device const uchar *weight [[buffer(0)]], device const uint *ids [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_quant_rows_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total_values = p.total * p.dim; if (gid >= total_values) return; uint row = gid / p.dim; uint col = gid % p.dim; uint source_row = ids[row]; if (source_row >= p.source_rows || (p.dim & 31u) != 0u) { output[gid] = 0.0f; return; } uint row_blocks = p.dim / 32u; uint block = col / 32u; uint inner = col - block * 32u; uint off = (source_row * row_blocks + block) * 36u; output[gid] = termite_q8_1_block_scale(weight, off) * termite_q8_1_block_value(weight, off, inner) * p.scale;\n"
           "}\n"
           "kernel void termite_q8_1_set_rows(device const float *input [[buffer(0)]], device const uint *ids [[buffer(1)]], device uchar *weight [[buffer(2)]], constant termite_metal_quant_rows_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    if ((p.dim & 31u) != 0u) return; uint row_blocks = p.dim / 32u; uint total_blocks = p.total * row_blocks; if (gid >= total_blocks) return; uint row = gid / row_blocks; uint block = gid - row * row_blocks; uint target_row = ids[row]; if (target_row >= p.source_rows) return; uint input_base = row * p.dim + block * 32u; float max_abs = 0.0f; for (uint i = 0u; i < 32u; ++i) max_abs = fmax(max_abs, fabs(input[input_base + i] * p.scale)); float d = max_abs == 0.0f ? 1.0f : max_abs / 127.0f; float inv = max_abs == 0.0f ? 0.0f : 1.0f / d; uint off = (target_row * row_blocks + block) * 36u; ushort bits = as_type<ushort>(half(d)); weight[off] = uchar(bits & 0xffu); weight[off + 1u] = uchar((bits >> 8) & 0xffu); int qsum = 0; for (uint i = 0u; i < 32u; ++i) { float scaled = input[input_base + i] * p.scale; int q = int(round(clamp(scaled * inv, -127.0f, 127.0f))); qsum += q; weight[off + 4u + i] = as_type<uchar>(char(q)); } ushort sum_bits = as_type<ushort>(half(d * float(qsum))); weight[off + 2u] = uchar(sum_bits & 0xffu); weight[off + 3u] = uchar((sum_bits >> 8) & 0xffu);\n"
           "}\n"
           "kernel void termite_q8_1_cpy_q_to_f32(device const uchar *weight [[buffer(0)]], device float *output [[buffer(1)]], constant termite_metal_quant_rows_params &p [[buffer(2)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total_values = p.total * p.dim; if (gid >= total_values) return; uint row = gid / p.dim; uint col = gid % p.dim; uint source_row = p.row_offset + row; if (source_row >= p.source_rows || (p.dim & 31u) != 0u) { output[gid] = 0.0f; return; } uint row_blocks = p.dim / 32u; uint block = col / 32u; uint inner = col - block * 32u; uint off = (source_row * row_blocks + block) * 36u; output[gid] = termite_q8_1_block_scale(weight, off) * termite_q8_1_block_value(weight, off, inner) * p.scale;\n"
           "}\n"
           "kernel void termite_q8_1_cpy_f32_to_q(device const float *input [[buffer(0)]], device uchar *weight [[buffer(1)]], constant termite_metal_quant_rows_params &p [[buffer(2)]], uint gid [[thread_position_in_grid]]) {\n"
           "    if ((p.dim & 31u) != 0u) return; uint row_blocks = p.dim / 32u; uint total_blocks = p.total * row_blocks; if (gid >= total_blocks) return; uint row = gid / row_blocks; uint block = gid - row * row_blocks; uint target_row = p.row_offset + row; if (target_row >= p.source_rows) return; uint input_base = row * p.dim + block * 32u; float max_abs = 0.0f; for (uint i = 0u; i < 32u; ++i) max_abs = fmax(max_abs, fabs(input[input_base + i] * p.scale)); float d = max_abs == 0.0f ? 1.0f : max_abs / 127.0f; float inv = max_abs == 0.0f ? 0.0f : 1.0f / d; uint off = (target_row * row_blocks + block) * 36u; ushort bits = as_type<ushort>(half(d)); weight[off] = uchar(bits & 0xffu); weight[off + 1u] = uchar((bits >> 8) & 0xffu); int qsum = 0; for (uint i = 0u; i < 32u; ++i) { float scaled = input[input_base + i] * p.scale; int q = int(round(clamp(scaled * inv, -127.0f, 127.0f))); qsum += q; weight[off + 4u + i] = as_type<uchar>(char(q)); } ushort sum_bits = as_type<ushort>(half(d * float(qsum))); weight[off + 2u] = uchar(sum_bits & 0xffu); weight[off + 3u] = uchar((sum_bits >> 8) & 0xffu);\n"
           "}\n"
           "kernel void termite_q8_k_linear(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.out_dim; if (gid >= total) return; uint r = gid / p.out_dim; uint o = gid % p.out_dim; uint row_offset = o * p.row_blocks * 292u; float acc = 0.0f;\n"
           "    for (uint b = 0; b < p.row_blocks; ++b) { uint off = row_offset + b * 292u; uint bits = uint(weight[off]) | (uint(weight[off + 1]) << 8) | (uint(weight[off + 2]) << 16) | (uint(weight[off + 3]) << 24); float d = as_type<float>(bits); float block_acc = 0.0f; uint in_off = r * p.in_dim + b * 256u; for (uint i = 0; i < 256; ++i) { char q = as_type<char>(weight[off + 4u + i]); block_acc += input[in_off + i] * float(q); } acc += d * block_acc; }\n"
           "    output[gid] = acc;\n"
           "}\n"
           "kernel void termite_q8_k_linear_1x_reduce(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], threadgroup float *shmem [[threadgroup(0)]], ushort lane [[thread_index_in_simdgroup]], ushort sgitg [[simdgroup_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) {\n"
           "    const uint NR0 = 2u; const uint NSG = 2u; const uint NW = 32u; uint first_o = tg.x * NR0; uint r = tg.y; if (r >= p.rows || lane >= NW) return;\n"
           "    uint ix = lane / 4u; uint il = lane - ix * 4u; uint chunk0 = uint(sgitg) * 8u + ix; uint in_row = r * p.in_dim; uint total_chunks = p.row_blocks * 64u; float acc0 = 0.0f; float acc1 = 0.0f;\n"
           "    for (uint chunk = chunk0; chunk < total_chunks; chunk += NSG * 8u) { uint b = chunk >> 6; uint qj = ((chunk & 63u) << 2) + il; float x = input[in_row + b * 256u + qj]; for (uint row = 0u; row < NR0; ++row) { uint o = first_o + row; if (o >= p.out_dim) continue; uint off = o * p.row_blocks * 292u + b * 292u; uint bits = uint(weight[off]) | (uint(weight[off + 1u]) << 8) | (uint(weight[off + 2u]) << 16) | (uint(weight[off + 3u]) << 24); float d = as_type<float>(bits); char q = as_type<char>(weight[off + 4u + qj]); float block = x * (d * float(q)); if (row == 0u) acc0 += block; else acc1 += block; } }\n"
           "    if (sgitg == 0u) { shmem[lane] = 0.0f; shmem[32u + lane] = 0.0f; } acc0 = simd_sum(acc0); acc1 = simd_sum(acc1); threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    if (lane == 0u) { shmem[sgitg] = acc0; shmem[32u + sgitg] = acc1; } threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    float total0 = simd_sum(shmem[lane]); float total1 = simd_sum(shmem[32u + lane]); if (lane == 0u && sgitg == 0u) { uint idx = r * p.out_dim + first_o; if (first_o < p.out_dim) output[idx] = total0; if (first_o + 1u < p.out_dim) output[idx + 1u] = total1; }\n"
           "}\n"
           "kernel void termite_i2_s_linear(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.out_dim; if (gid >= total) return; uint r = gid / p.out_dim; uint o = gid % p.out_dim; float abs_max = 0.0f; uint row_base = r * p.in_dim; for (uint i = 0; i < p.in_dim; ++i) { abs_max = fmax(abs_max, fabs(input[row_base + i])); } float act_scale = abs_max == 0.0f ? 1.0f : abs_max / 127.0f; float acc = 0.0f; uint row_offset = o * p.row_blocks * 32u; for (uint b = 0; b < p.row_blocks; ++b) { uint off = row_offset + b * 32u; uint in_base = row_base + b * 128u; for (uint group = 0; group < 32u; ++group) { uchar packed = weight[off + group]; uint code0 = uint((packed >> 6) & 0x03u); uint code1 = uint((packed >> 4) & 0x03u); uint code2 = uint((packed >> 2) & 0x03u); uint code3 = uint(packed & 0x03u); float w0 = code0 == 0u ? -1.0f : (code0 == 2u ? 1.0f : 0.0f); float w1 = code1 == 0u ? -1.0f : (code1 == 2u ? 1.0f : 0.0f); float w2 = code2 == 0u ? -1.0f : (code2 == 2u ? 1.0f : 0.0f); float w3 = code3 == 0u ? -1.0f : (code3 == 2u ? 1.0f : 0.0f); float x0 = clamp(round(input[in_base + group] / act_scale), -127.0f, 127.0f) * act_scale; float x1 = clamp(round(input[in_base + 32u + group] / act_scale), -127.0f, 127.0f) * act_scale; float x2 = clamp(round(input[in_base + 64u + group] / act_scale), -127.0f, 127.0f) * act_scale; float x3 = clamp(round(input[in_base + 96u + group] / act_scale), -127.0f, 127.0f) * act_scale; acc += x0 * w0 + x1 * w1 + x2 * w2 + x3 * w3; } }\n"
           "    output[gid] = acc;\n"
           "}\n"
           "kernel void termite_i2_s_pair_linear(device const float *input [[buffer(0)]], device const uchar *weight_a [[buffer(1)]], device const uchar *weight_b [[buffer(2)]], device float *output_a [[buffer(3)]], device float *output_b [[buffer(4)]], constant termite_metal_linear_params &p [[buffer(5)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.out_dim; if (gid >= total) return; uint r = gid / p.out_dim; uint o = gid % p.out_dim; float abs_max = 0.0f; uint row_base = r * p.in_dim; for (uint i = 0; i < p.in_dim; ++i) { abs_max = fmax(abs_max, fabs(input[row_base + i])); } float act_scale = abs_max == 0.0f ? 1.0f : abs_max / 127.0f; float acc_a = 0.0f; float acc_b = 0.0f; uint row_offset = o * p.row_blocks * 32u; for (uint b = 0; b < p.row_blocks; ++b) { uint off = row_offset + b * 32u; uint in_base = row_base + b * 128u; for (uint group = 0; group < 32u; ++group) { uchar packed_a = weight_a[off + group]; uchar packed_b = weight_b[off + group]; uint code_a0 = uint((packed_a >> 6) & 0x03u); uint code_a1 = uint((packed_a >> 4) & 0x03u); uint code_a2 = uint((packed_a >> 2) & 0x03u); uint code_a3 = uint(packed_a & 0x03u); uint code_b0 = uint((packed_b >> 6) & 0x03u); uint code_b1 = uint((packed_b >> 4) & 0x03u); uint code_b2 = uint((packed_b >> 2) & 0x03u); uint code_b3 = uint(packed_b & 0x03u); float wa0 = code_a0 == 0u ? -1.0f : (code_a0 == 2u ? 1.0f : 0.0f); float wa1 = code_a1 == 0u ? -1.0f : (code_a1 == 2u ? 1.0f : 0.0f); float wa2 = code_a2 == 0u ? -1.0f : (code_a2 == 2u ? 1.0f : 0.0f); float wa3 = code_a3 == 0u ? -1.0f : (code_a3 == 2u ? 1.0f : 0.0f); float wb0 = code_b0 == 0u ? -1.0f : (code_b0 == 2u ? 1.0f : 0.0f); float wb1 = code_b1 == 0u ? -1.0f : (code_b1 == 2u ? 1.0f : 0.0f); float wb2 = code_b2 == 0u ? -1.0f : (code_b2 == 2u ? 1.0f : 0.0f); float wb3 = code_b3 == 0u ? -1.0f : (code_b3 == 2u ? 1.0f : 0.0f); float x0 = clamp(round(input[in_base + group] / act_scale), -127.0f, 127.0f) * act_scale; float x1 = clamp(round(input[in_base + 32u + group] / act_scale), -127.0f, 127.0f) * act_scale; float x2 = clamp(round(input[in_base + 64u + group] / act_scale), -127.0f, 127.0f) * act_scale; float x3 = clamp(round(input[in_base + 96u + group] / act_scale), -127.0f, 127.0f) * act_scale; acc_a += x0 * wa0 + x1 * wa1 + x2 * wa2 + x3 * wa3; acc_b += x0 * wb0 + x1 * wb1 + x2 * wb2 + x3 * wb3; } }\n"
           "    output_a[gid] = acc_a; output_b[gid] = acc_b;\n"
           "}\n"
           "kernel void termite_quantize_rows_i8(device const float *input [[buffer(0)]], device char *qinput [[buffer(1)]], device float *scales [[buffer(2)]], constant termite_metal_quantize_rows_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    if (gid >= p.rows) return; uint row = gid; uint row_base = row * p.dim; float abs_max = 0.0f; for (uint i = 0; i < p.dim; ++i) { abs_max = fmax(abs_max, fabs(input[row_base + i])); } float scale = abs_max == 0.0f ? 1.0f : abs_max / 127.0f; scales[row] = scale; float inv = scale == 0.0f ? 0.0f : 1.0f / scale; for (uint i = 0; i < p.dim; ++i) { float q = clamp(round(input[row_base + i] * inv), -127.0f, 127.0f); qinput[row_base + i] = char(int(q)); }\n"
           "}\n"
           "kernel void termite_i2_s_linear_i8(device const char *qinput [[buffer(0)]], device const uchar *weight [[buffer(1)]], device const float *scales [[buffer(2)]], device float *output [[buffer(3)]], constant termite_metal_linear_params &p [[buffer(4)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.out_dim; if (gid >= total) return; uint r = gid / p.out_dim; uint o = gid % p.out_dim; float scale = scales[r]; float acc = 0.0f; uint row_base = r * p.in_dim; uint row_offset = o * p.row_blocks * 32u; for (uint b = 0; b < p.row_blocks; ++b) { uint off = row_offset + b * 32u; uint in_base = row_base + b * 128u; for (uint group = 0; group < 32u; ++group) { uchar packed = weight[off + group]; uint code0 = uint((packed >> 6) & 0x03u); uint code1 = uint((packed >> 4) & 0x03u); uint code2 = uint((packed >> 2) & 0x03u); uint code3 = uint(packed & 0x03u); float w0 = code0 == 0u ? -1.0f : (code0 == 2u ? 1.0f : 0.0f); float w1 = code1 == 0u ? -1.0f : (code1 == 2u ? 1.0f : 0.0f); float w2 = code2 == 0u ? -1.0f : (code2 == 2u ? 1.0f : 0.0f); float w3 = code3 == 0u ? -1.0f : (code3 == 2u ? 1.0f : 0.0f); float x0 = float(qinput[in_base + group]); float x1 = float(qinput[in_base + 32u + group]); float x2 = float(qinput[in_base + 64u + group]); float x3 = float(qinput[in_base + 96u + group]); acc += x0 * w0 + x1 * w1 + x2 * w2 + x3 * w3; } } output[gid] = scale * acc;\n"
           "}\n"
           "kernel void termite_i2_s_pair_linear_i8(device const char *qinput [[buffer(0)]], device const uchar *weight_a [[buffer(1)]], device const uchar *weight_b [[buffer(2)]], device const float *scales [[buffer(3)]], device float *output_a [[buffer(4)]], device float *output_b [[buffer(5)]], constant termite_metal_linear_params &p [[buffer(6)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.out_dim; if (gid >= total) return; uint r = gid / p.out_dim; uint o = gid % p.out_dim; float scale = scales[r]; float acc_a = 0.0f; float acc_b = 0.0f; uint row_base = r * p.in_dim; uint row_offset = o * p.row_blocks * 32u; for (uint b = 0; b < p.row_blocks; ++b) { uint off = row_offset + b * 32u; uint in_base = row_base + b * 128u; for (uint group = 0; group < 32u; ++group) { uchar packed_a = weight_a[off + group]; uchar packed_b = weight_b[off + group]; uint code_a0 = uint((packed_a >> 6) & 0x03u); uint code_a1 = uint((packed_a >> 4) & 0x03u); uint code_a2 = uint((packed_a >> 2) & 0x03u); uint code_a3 = uint(packed_a & 0x03u); uint code_b0 = uint((packed_b >> 6) & 0x03u); uint code_b1 = uint((packed_b >> 4) & 0x03u); uint code_b2 = uint((packed_b >> 2) & 0x03u); uint code_b3 = uint(packed_b & 0x03u); float wa0 = code_a0 == 0u ? -1.0f : (code_a0 == 2u ? 1.0f : 0.0f); float wa1 = code_a1 == 0u ? -1.0f : (code_a1 == 2u ? 1.0f : 0.0f); float wa2 = code_a2 == 0u ? -1.0f : (code_a2 == 2u ? 1.0f : 0.0f); float wa3 = code_a3 == 0u ? -1.0f : (code_a3 == 2u ? 1.0f : 0.0f); float wb0 = code_b0 == 0u ? -1.0f : (code_b0 == 2u ? 1.0f : 0.0f); float wb1 = code_b1 == 0u ? -1.0f : (code_b1 == 2u ? 1.0f : 0.0f); float wb2 = code_b2 == 0u ? -1.0f : (code_b2 == 2u ? 1.0f : 0.0f); float wb3 = code_b3 == 0u ? -1.0f : (code_b3 == 2u ? 1.0f : 0.0f); float x0 = float(qinput[in_base + group]); float x1 = float(qinput[in_base + 32u + group]); float x2 = float(qinput[in_base + 64u + group]); float x3 = float(qinput[in_base + 96u + group]); acc_a += x0 * wa0 + x1 * wa1 + x2 * wa2 + x3 * wa3; acc_b += x0 * wb0 + x1 * wb1 + x2 * wb2 + x3 * wb3; } } output_a[gid] = scale * acc_a; output_b[gid] = scale * acc_b;\n"
           "}\n"
	           "kernel void termite_apply_layer_norm_1x(device const float *input [[buffer(0)]], device const float *gamma [[buffer(1)]], device const float *beta [[buffer(2)]], device float *output [[buffer(3)]], constant termite_metal_apply_layer_norm_params &p [[buffer(4)]], uint gid [[thread_position_in_grid]]) {\n"
	           "    if (p.hidden_size == 0) return;\n"
	           "    uint row_base = gid * p.hidden_size;\n"
	           "    float mean = 0.0f;\n"
	           "    for (uint i = 0; i < p.hidden_size; ++i) mean += input[row_base + i];\n"
           "    mean /= float(p.hidden_size);\n"
           "    float variance = 0.0f;\n"
           "    for (uint i = 0; i < p.hidden_size; ++i) { float centered = input[row_base + i] - mean; variance += centered * centered; }\n"
           "    variance /= float(p.hidden_size);\n"
	           "    float inv_std = rsqrt(variance + p.eps);\n"
	           "    for (uint i = 0; i < p.hidden_size; ++i) output[row_base + i] = ((input[row_base + i] - mean) * inv_std) * gamma[i] + beta[i];\n"
	           "}\n"
	           "kernel void termite_apply_layer_norm_rows(device const float *input [[buffer(0)]], device const float *gamma [[buffer(1)]], device const float *beta [[buffer(2)]], device float *output [[buffer(3)]], constant termite_metal_apply_layer_norm_params &p [[buffer(4)]], threadgroup float *scratch [[threadgroup(0)]], uint tid [[thread_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]], uint3 threads_per_tg [[threads_per_threadgroup]]) {\n"
	           "    uint row = tg.x; uint width = threads_per_tg.x; if (p.hidden_size == 0u || width == 0u || tid >= 256u) return; uint row_base = row * p.hidden_size; threadgroup float *sums = scratch; threadgroup float *sqs = scratch + 256u;\n"
	           "    float sum = 0.0f; float sq = 0.0f; for (uint i = tid; i < p.hidden_size; i += width) { float v = input[row_base + i]; sum += v; sq += v * v; }\n"
	           "    sums[tid] = sum; sqs[tid] = sq; threadgroup_barrier(mem_flags::mem_threadgroup);\n"
	           "    for (uint stride = width >> 1; stride > 0u; stride >>= 1u) { if (tid < stride && tid + stride < 256u) { sums[tid] += sums[tid + stride]; sqs[tid] += sqs[tid + stride]; } threadgroup_barrier(mem_flags::mem_threadgroup); }\n"
	           "    float mean = sums[0] / float(p.hidden_size); float variance = max(sqs[0] / float(p.hidden_size) - mean * mean, 0.0f); float inv_std = rsqrt(variance + p.eps);\n"
	           "    for (uint i = tid; i < p.hidden_size; i += width) { float v = input[row_base + i]; output[row_base + i] = ((v - mean) * inv_std) * gamma[i] + beta[i]; }\n"
	           "}\n"
	           "kernel void termite_apply_add_layer_norm_1x(device const float *a [[buffer(0)]], device const float *b [[buffer(1)]], device const float *gamma [[buffer(2)]], device const float *beta [[buffer(3)]], device float *output [[buffer(4)]], constant termite_metal_apply_layer_norm_params &p [[buffer(5)]], uint gid [[thread_position_in_grid]]) {\n"
	           "    if (p.hidden_size == 0) return;\n"
	           "    uint row_base = gid * p.hidden_size;\n"
	           "    float mean = 0.0f;\n"
	           "    for (uint i = 0; i < p.hidden_size; ++i) mean += a[row_base + i] + b[row_base + i];\n"
           "    mean /= float(p.hidden_size);\n"
           "    float variance = 0.0f;\n"
           "    for (uint i = 0; i < p.hidden_size; ++i) { float value = a[row_base + i] + b[row_base + i]; float centered = value - mean; variance += centered * centered; }\n"
           "    variance /= float(p.hidden_size);\n"
	           "    float inv_std = rsqrt(variance + p.eps);\n"
	           "    for (uint i = 0; i < p.hidden_size; ++i) { float value = a[row_base + i] + b[row_base + i]; output[row_base + i] = ((value - mean) * inv_std) * gamma[i] + beta[i]; }\n"
	           "}\n"
	           "kernel void termite_apply_add_layer_norm_rows(device const float *a [[buffer(0)]], device const float *b [[buffer(1)]], device const float *gamma [[buffer(2)]], device const float *beta [[buffer(3)]], device float *output [[buffer(4)]], constant termite_metal_apply_layer_norm_params &p [[buffer(5)]], threadgroup float *scratch [[threadgroup(0)]], uint tid [[thread_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]], uint3 threads_per_tg [[threads_per_threadgroup]]) {\n"
	           "    uint row = tg.x; uint width = threads_per_tg.x; if (p.hidden_size == 0u || width == 0u || tid >= 256u) return; uint row_base = row * p.hidden_size; threadgroup float *sums = scratch; threadgroup float *sqs = scratch + 256u;\n"
	           "    float sum = 0.0f; float sq = 0.0f; for (uint i = tid; i < p.hidden_size; i += width) { float v = a[row_base + i] + b[row_base + i]; sum += v; sq += v * v; }\n"
	           "    sums[tid] = sum; sqs[tid] = sq; threadgroup_barrier(mem_flags::mem_threadgroup);\n"
	           "    for (uint stride = width >> 1; stride > 0u; stride >>= 1u) { if (tid < stride && tid + stride < 256u) { sums[tid] += sums[tid + stride]; sqs[tid] += sqs[tid + stride]; } threadgroup_barrier(mem_flags::mem_threadgroup); }\n"
	           "    float mean = sums[0] / float(p.hidden_size); float variance = max(sqs[0] / float(p.hidden_size) - mean * mean, 0.0f); float inv_std = rsqrt(variance + p.eps);\n"
	           "    for (uint i = tid; i < p.hidden_size; i += width) { float v = a[row_base + i] + b[row_base + i]; output[row_base + i] = ((v - mean) * inv_std) * gamma[i] + beta[i]; }\n"
	           "}\n"
	           "kernel void termite_apply_bias_add_layer_norm_1x(device const float *projected [[buffer(0)]], device const float *bias [[buffer(1)]], device const float *residual [[buffer(2)]], device const float *gamma [[buffer(3)]], device const float *beta [[buffer(4)]], device float *output [[buffer(5)]], constant termite_metal_apply_layer_norm_params &p [[buffer(6)]], uint gid [[thread_position_in_grid]]) {\n"
	           "    if (p.hidden_size == 0) return;\n"
	           "    uint row_base = gid * p.hidden_size;\n"
	           "    float mean = 0.0f;\n"
	           "    for (uint i = 0; i < p.hidden_size; ++i) mean += projected[row_base + i] + bias[i] + residual[row_base + i];\n"
           "    mean /= float(p.hidden_size);\n"
           "    float variance = 0.0f;\n"
           "    for (uint i = 0; i < p.hidden_size; ++i) { float value = projected[row_base + i] + bias[i] + residual[row_base + i]; float centered = value - mean; variance += centered * centered; }\n"
           "    variance /= float(p.hidden_size);\n"
	           "    float inv_std = rsqrt(variance + p.eps);\n"
	           "    for (uint i = 0; i < p.hidden_size; ++i) { float value = projected[row_base + i] + bias[i] + residual[row_base + i]; output[row_base + i] = ((value - mean) * inv_std) * gamma[i] + beta[i]; }\n"
	           "}\n"
	           "kernel void termite_apply_bias_add_layer_norm_rows(device const float *projected [[buffer(0)]], device const float *bias [[buffer(1)]], device const float *residual [[buffer(2)]], device const float *gamma [[buffer(3)]], device const float *beta [[buffer(4)]], device float *output [[buffer(5)]], constant termite_metal_apply_layer_norm_params &p [[buffer(6)]], threadgroup float *scratch [[threadgroup(0)]], uint tid [[thread_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]], uint3 threads_per_tg [[threads_per_threadgroup]]) {\n"
	           "    uint row = tg.x; uint width = threads_per_tg.x; if (p.hidden_size == 0u || width == 0u || tid >= 256u) return; uint row_base = row * p.hidden_size; threadgroup float *sums = scratch; threadgroup float *sqs = scratch + 256u;\n"
	           "    float sum = 0.0f; float sq = 0.0f; for (uint i = tid; i < p.hidden_size; i += width) { float v = projected[row_base + i] + bias[i] + residual[row_base + i]; sum += v; sq += v * v; }\n"
	           "    sums[tid] = sum; sqs[tid] = sq; threadgroup_barrier(mem_flags::mem_threadgroup);\n"
	           "    for (uint stride = width >> 1; stride > 0u; stride >>= 1u) { if (tid < stride && tid + stride < 256u) { sums[tid] += sums[tid + stride]; sqs[tid] += sqs[tid + stride]; } threadgroup_barrier(mem_flags::mem_threadgroup); }\n"
	           "    float mean = sums[0] / float(p.hidden_size); float variance = max(sqs[0] / float(p.hidden_size) - mean * mean, 0.0f); float inv_std = rsqrt(variance + p.eps);\n"
	           "    for (uint i = tid; i < p.hidden_size; i += width) { float v = projected[row_base + i] + bias[i] + residual[row_base + i]; output[row_base + i] = ((v - mean) * inv_std) * gamma[i] + beta[i]; }\n"
	           "}\n"
	           "kernel void termite_apply_rms_norm_1x(device const float *input [[buffer(0)]], device const float *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_apply_layer_norm_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    if (gid != 0) return;\n"
           "    if (p.hidden_size == 0) return;\n"
           "    float mean_square = 0.0f;\n"
           "    for (uint i = 0; i < p.hidden_size; ++i) { float x = input[i]; mean_square += x * x; }\n"
           "    mean_square /= float(p.hidden_size);\n"
           "    float inv_rms = rsqrt(mean_square + p.eps);\n"
           "    for (uint i = 0; i < p.hidden_size; ++i) output[i] = input[i] * inv_rms * weight[i];\n"
           "}\n"
           "kernel void termite_apply_rms_norm_1x_reduce(device const float *input [[buffer(0)]], device const float *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_apply_layer_norm_params &p [[buffer(3)]], threadgroup float *shmem [[threadgroup(0)]], uint tid [[thread_index_in_threadgroup]], uint tpg [[threads_per_threadgroup]]) {\n"
           "    if (p.hidden_size == 0) return;\n"
           "    float mean_square = 0.0f;\n"
           "    for (uint i = tid; i < p.hidden_size; i += tpg) { float x = input[i]; mean_square += x * x; }\n"
           "    shmem[tid] = mean_square;\n"
           "    threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    for (uint stride = tpg >> 1; stride > 0; stride >>= 1) { if (tid < stride) shmem[tid] += shmem[tid + stride]; threadgroup_barrier(mem_flags::mem_threadgroup); }\n"
           "    float inv_rms = rsqrt(shmem[0] / float(p.hidden_size) + p.eps);\n"
           "    for (uint i = tid; i < p.hidden_size; i += tpg) output[i] = input[i] * inv_rms * weight[i];\n"
           "}\n"
           "kernel void termite_compute_rms_inv_scale_1x(device const float *input [[buffer(0)]], device float *inv_scale [[buffer(1)]], constant termite_metal_apply_layer_norm_params &p [[buffer(2)]], threadgroup float *shmem [[threadgroup(0)]], uint tid [[thread_index_in_threadgroup]], uint tpg [[threads_per_threadgroup]]) {\n"
           "    if (p.hidden_size == 0) return;\n"
           "    float mean_square = 0.0f;\n"
           "    for (uint i = tid; i < p.hidden_size; i += tpg) { float x = input[i]; mean_square += x * x; }\n"
           "    shmem[tid] = mean_square;\n"
           "    threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    for (uint stride = tpg >> 1; stride > 0; stride >>= 1) { if (tid < stride) shmem[tid] += shmem[tid + stride]; threadgroup_barrier(mem_flags::mem_threadgroup); }\n"
           "    if (tid == 0u) inv_scale[0] = rsqrt(shmem[0] / float(p.hidden_size) + p.eps);\n"
           "}\n"
           "kernel void termite_apply_rms_norm_rows(device const float *input [[buffer(0)]], device const float *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_apply_row_norm_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    if (gid >= p.rows) return;\n"
           "    if (p.hidden_size == 0) return;\n"
           "    uint row_base = gid * p.hidden_size;\n"
           "    float mean_square = 0.0f;\n"
           "    for (uint i = 0; i < p.hidden_size; ++i) { float x = input[row_base + i]; mean_square += x * x; }\n"
           "    mean_square /= float(p.hidden_size);\n"
           "    float inv_rms = rsqrt(mean_square + p.eps);\n"
           "    for (uint i = 0; i < p.hidden_size; ++i) output[row_base + i] = input[row_base + i] * inv_rms * weight[i];\n"
           "}\n"
           "kernel void termite_apply_head_rms_rope(device const float *input [[buffer(0)]], device const float *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_head_rms_rope_params &p [[buffer(3)]], threadgroup float *shmem [[threadgroup(0)]], uint tid [[thread_index_in_threadgroup]], uint tpg [[threads_per_threadgroup]], uint head [[threadgroup_position_in_grid]]) {\n"
           "    if (head >= p.total_heads || p.head_dim == 0u || p.rope_dim == 0u) return;\n"
           "    uint base = head * p.head_dim;\n"
           "    float mean_square = 0.0f;\n"
           "    for (uint i = tid; i < p.head_dim; i += tpg) { float x = input[base + i]; mean_square += x * x; }\n"
           "    shmem[tid] = mean_square;\n"
           "    threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    for (uint stride = tpg >> 1; stride > 0; stride >>= 1) { if (tid < stride) shmem[tid] += shmem[tid + stride]; threadgroup_barrier(mem_flags::mem_threadgroup); }\n"
           "    float inv_rms = rsqrt(shmem[0] / float(p.head_dim) + p.eps);\n"
           "    uint rope_half = p.rope_dim / 2u;\n"
           "    uint head_half = p.head_dim / 2u;\n"
           "    for (uint d = tid; d < p.head_dim; d += tpg) {\n"
           "        float value = input[base + d] * inv_rms * weight[d] * p.value_scale;\n"
           "        if (p.rope_dim < 2u) { output[base + d] = value; continue; }\n"
           "        uint idx0 = 0u; uint idx1 = 0u; uint j = 0u; bool apply = true;\n"
           "        if (p.consecutive_pairs != 0u) { if (d >= p.rope_dim) apply = false; else { j = d / 2u; idx0 = 2u * j; idx1 = idx0 + 1u; } }\n"
           "        else { if (d < rope_half) { j = d; idx0 = d; idx1 = head_half + d; } else if (d >= head_half && d < head_half + rope_half) { j = d - head_half; idx0 = j; idx1 = d; } else apply = false; }\n"
           "        if (!apply) { output[base + d] = value; continue; }\n"
           "        float x0 = input[base + idx0] * inv_rms * weight[idx0] * p.value_scale;\n"
           "        float x1 = input[base + idx1] * inv_rms * weight[idx1] * p.value_scale;\n"
           "        float freq = 1.0f / pow(p.theta, float(2u * j) / float(p.rope_dim));\n"
           "        uint row = p.heads_per_row == 0u ? 0u : head / p.heads_per_row;\n"
           "        if (p.position_period != 0u) row = row % p.position_period;\n"
           "        float angle = float(p.position + row) * p.freq_scale * freq;\n"
           "        float c = cos(angle); float s = sin(angle);\n"
           "        if (d == idx0) output[base + d] = x0 * c - x1 * s;\n"
           "        else if (d == idx1) output[base + d] = x0 * s + x1 * c;\n"
           "        else output[base + d] = value;\n"
           "    }\n"
           "}\n"
           "kernel void termite_apply_rms_norm_add_1x(device const float *input [[buffer(0)]], device const float *weight [[buffer(1)]], device const float *residual [[buffer(2)]], device float *output [[buffer(3)]], constant termite_metal_apply_layer_norm_params &p [[buffer(4)]], threadgroup float *shmem [[threadgroup(0)]], uint tid [[thread_index_in_threadgroup]], uint tpg [[threads_per_threadgroup]]) {\n"
           "    if (p.hidden_size == 0) return;\n"
           "    float mean_square = 0.0f;\n"
           "    for (uint i = tid; i < p.hidden_size; i += tpg) { float x = input[i]; mean_square += x * x; }\n"
           "    shmem[tid] = mean_square;\n"
           "    threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    for (uint stride = tpg >> 1; stride > 0; stride >>= 1) { if (tid < stride) shmem[tid] += shmem[tid + stride]; threadgroup_barrier(mem_flags::mem_threadgroup); }\n"
           "    mean_square = shmem[0] / float(p.hidden_size);\n"
           "    float inv_rms = rsqrt(mean_square + p.eps);\n"
           "    for (uint i = tid; i < p.hidden_size; i += tpg) output[i] = input[i] * inv_rms * weight[i] + residual[i];\n"
           "}\n"
           "kernel void termite_apply_rms_norm_add_scale_1x(device const float *input [[buffer(0)]], device const float *weight [[buffer(1)]], device const float *residual [[buffer(2)]], device float *output [[buffer(3)]], constant termite_metal_apply_layer_norm_scale_params &p [[buffer(4)]], threadgroup float *shmem [[threadgroup(0)]], uint tid [[thread_index_in_threadgroup]], uint tpg [[threads_per_threadgroup]]) {\n"
           "    if (p.hidden_size == 0) return;\n"
           "    float mean_square = 0.0f;\n"
           "    for (uint i = tid; i < p.hidden_size; i += tpg) { float x = input[i]; mean_square += x * x; }\n"
           "    shmem[tid] = mean_square;\n"
           "    threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    for (uint stride = tpg >> 1; stride > 0; stride >>= 1) { if (tid < stride) shmem[tid] += shmem[tid + stride]; threadgroup_barrier(mem_flags::mem_threadgroup); }\n"
           "    mean_square = shmem[0] / float(p.hidden_size);\n"
           "    float inv_rms = rsqrt(mean_square + p.eps);\n"
           "    for (uint i = tid; i < p.hidden_size; i += tpg) output[i] = (input[i] * inv_rms * weight[i] + residual[i]) * p.scale;\n"
           "}\n"
           "kernel void termite_apply_rms_norm_add_scale_rows(device const float *input [[buffer(0)]], device const float *weight [[buffer(1)]], device const float *residual [[buffer(2)]], device float *output [[buffer(3)]], constant termite_metal_apply_row_norm_scale_params &p [[buffer(4)]], threadgroup float *shmem [[threadgroup(0)]], uint tid [[thread_index_in_threadgroup]], uint row [[threadgroup_position_in_grid]], uint tpg [[threads_per_threadgroup]]) {\n"
           "    if (row >= p.rows || p.hidden_size == 0) return;\n"
           "    uint base = row * p.hidden_size;\n"
           "    float mean_square = 0.0f;\n"
           "    for (uint i = tid; i < p.hidden_size; i += tpg) { float x = input[base + i]; mean_square += x * x; }\n"
           "    shmem[tid] = mean_square;\n"
           "    threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    for (uint stride = tpg >> 1; stride > 0; stride >>= 1) { if (tid < stride) shmem[tid] += shmem[tid + stride]; threadgroup_barrier(mem_flags::mem_threadgroup); }\n"
           "    mean_square = shmem[0] / float(p.hidden_size);\n"
           "    float inv_rms = rsqrt(mean_square + p.eps);\n"
           "    for (uint i = tid; i < p.hidden_size; i += tpg) output[base + i] = (input[base + i] * inv_rms * weight[i] + residual[base + i]) * p.scale;\n"
           "}\n"
           "kernel void termite_apply_linear_1x(device const float *input [[buffer(0)]], device const float *weight [[buffer(1)]], device const float *bias [[buffer(2)]], device float *output [[buffer(3)]], constant termite_metal_apply_linear_params &p [[buffer(4)]], uint gid [[thread_position_in_grid]]) {\n"
           "    if (gid >= p.out_dim) return;\n"
           "    float acc = bias[gid];\n"
           "    uint row_base = gid * p.in_dim;\n"
           "    for (uint i = 0; i < p.in_dim; ++i) acc += input[i] * weight[row_base + i];\n"
           "    output[gid] = acc;\n"
           "}\n"
           "kernel void termite_apply_linear_1x_reduce(device const float *input [[buffer(0)]], device const float *weight [[buffer(1)]], device const float *bias [[buffer(2)]], device float *output [[buffer(3)]], constant termite_metal_apply_linear_params &p [[buffer(4)]], uint lane [[thread_index_in_threadgroup]], uint group [[threadgroup_position_in_grid]]) {\n"
           "    if (group >= p.out_dim) return;\n"
           "    threadgroup float partials[32];\n"
           "    float acc = lane == 0u ? bias[group] : 0.0f;\n"
           "    uint row_base = group * p.in_dim;\n"
           "    for (uint i = lane; i < p.in_dim; i += 32u) acc += input[i] * weight[row_base + i];\n"
           "    partials[lane] = acc;\n"
           "    threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    for (uint stride = 16u; stride > 0u; stride >>= 1u) {\n"
           "        if (lane < stride) partials[lane] += partials[lane + stride];\n"
           "        threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    }\n"
           "    if (lane == 0u) output[group] = partials[0];\n"
           "}\n"
           "static inline float termite_bf16_to_f32(ushort bits) { return as_type<float>((uint(bits) << 16u)); }\n"
           "kernel void termite_apply_linear_bf16_1x(device const float *input [[buffer(0)]], device const ushort *weight [[buffer(1)]], device const float *bias [[buffer(2)]], device float *output [[buffer(3)]], constant termite_metal_apply_linear_params &p [[buffer(4)]], uint gid [[thread_position_in_grid]]) {\n"
           "    if (gid >= p.out_dim) return;\n"
           "    float acc = bias[gid];\n"
           "    uint row_base = gid * p.in_dim;\n"
           "    for (uint i = 0; i < p.in_dim; ++i) acc += input[i] * termite_bf16_to_f32(weight[row_base + i]);\n"
           "    output[gid] = acc;\n"
           "}\n"
           "kernel void termite_apply_linear_bf16_1x_reduce(device const float *input [[buffer(0)]], device const ushort *weight [[buffer(1)]], device const float *bias [[buffer(2)]], device float *output [[buffer(3)]], constant termite_metal_apply_linear_params &p [[buffer(4)]], uint lane [[thread_index_in_threadgroup]], uint group [[threadgroup_position_in_grid]]) {\n"
           "    if (group >= p.out_dim) return;\n"
           "    threadgroup float partials[32];\n"
           "    float acc = lane == 0u ? bias[group] : 0.0f;\n"
           "    uint row_base = group * p.in_dim;\n"
           "    for (uint i = lane; i < p.in_dim; i += 32u) acc += input[i] * termite_bf16_to_f32(weight[row_base + i]);\n"
           "    partials[lane] = acc;\n"
           "    threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    for (uint stride = 16u; stride > 0u; stride >>= 1u) {\n"
           "        if (lane < stride) partials[lane] += partials[lane + stride];\n"
           "        threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    }\n"
           "    if (lane == 0u) output[group] = partials[0];\n"
           "}\n"
           "kernel void termite_apply_linear_pair_1x_reduce(device const float *input [[buffer(0)]], device const float *weight_a [[buffer(1)]], device const float *weight_b [[buffer(2)]], device const float *bias_a [[buffer(3)]], device const float *bias_b [[buffer(4)]], device float *output_a [[buffer(5)]], device float *output_b [[buffer(6)]], constant termite_metal_apply_linear_params &p [[buffer(7)]], uint lane [[thread_index_in_threadgroup]], uint group [[threadgroup_position_in_grid]]) {\n"
           "    if (group >= p.out_dim) return;\n"
           "    threadgroup float partials_a[32];\n"
           "    threadgroup float partials_b[32];\n"
           "    float acc_a = lane == 0u ? bias_a[group] : 0.0f;\n"
           "    float acc_b = lane == 0u ? bias_b[group] : 0.0f;\n"
           "    uint row_base = group * p.in_dim;\n"
           "    for (uint i = lane; i < p.in_dim; i += 32u) {\n"
           "        float x = input[i];\n"
           "        acc_a += x * weight_a[row_base + i];\n"
           "        acc_b += x * weight_b[row_base + i];\n"
           "    }\n"
           "    partials_a[lane] = acc_a;\n"
           "    partials_b[lane] = acc_b;\n"
           "    threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    for (uint stride = 16u; stride > 0u; stride >>= 1u) {\n"
           "        if (lane < stride) {\n"
           "            partials_a[lane] += partials_a[lane + stride];\n"
           "            partials_b[lane] += partials_b[lane + stride];\n"
           "        }\n"
           "        threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    }\n"
           "    if (lane == 0u) { output_a[group] = partials_a[0]; output_b[group] = partials_b[0]; }\n"
           "}\n"
           "kernel void termite_apply_linear_multi_row(device const float *input [[buffer(0)]], device const float *weight [[buffer(1)]], device const float *bias [[buffer(2)]], device float *output [[buffer(3)]], constant termite_metal_linear_params &p [[buffer(4)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.out_dim;\n"
           "    if (gid >= total) return;\n"
           "    uint r = gid / p.out_dim;\n"
           "    uint o = gid % p.out_dim;\n"
           "    float acc = bias[o];\n"
           "    uint in_base = r * p.in_dim;\n"
           "    uint w_base = o * p.in_dim;\n"
           "    for (uint i = 0; i < p.in_dim; ++i) acc += input[in_base + i] * weight[w_base + i];\n"
           "    output[gid] = acc;\n"
           "}\n"
           "kernel void termite_apply_linear_multi_row_reduce(device const float *input [[buffer(0)]], device const float *weight [[buffer(1)]], device const float *bias [[buffer(2)]], device float *output [[buffer(3)]], constant termite_metal_linear_params &p [[buffer(4)]], threadgroup float *partials [[threadgroup(0)]], ushort lane [[thread_index_in_threadgroup]], uint2 tg [[threadgroup_position_in_grid]]) {\n"
           "    uint o = tg.x;\n"
           "    uint r = tg.y;\n"
           "    if (r >= p.rows || o >= p.out_dim) return;\n"
           "    float acc = lane == 0u ? bias[o] : 0.0f;\n"
           "    uint in_base = r * p.in_dim;\n"
           "    uint w_base = o * p.in_dim;\n"
           "    for (uint i = lane; i < p.in_dim; i += 32u) acc += input[in_base + i] * weight[w_base + i];\n"
           "    partials[lane] = acc;\n"
           "    threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    for (uint stride = 16u; stride > 0u; stride >>= 1u) {\n"
           "        if (lane < stride) partials[lane] += partials[lane + stride];\n"
           "        threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    }\n"
           "    if (lane == 0u) output[r * p.out_dim + o] = partials[0];\n"
           "}\n"
           "kernel void termite_apply_linear_multi_row_tiled_f32(device const float *input [[buffer(0)]], device const float *weight [[buffer(1)]], device const float *bias [[buffer(2)]], device float *output [[buffer(3)]], constant termite_metal_linear_params &p [[buffer(4)]], ushort2 tid [[thread_position_in_threadgroup]], uint2 tg [[threadgroup_position_in_grid]]) {\n"
           "    constexpr uint tile_m = 8u;\n"
           "    constexpr uint tile_n = 16u;\n"
           "    constexpr uint tile_k = 32u;\n"
           "    threadgroup float a_tile[8][32];\n"
           "    threadgroup float b_tile[16][32];\n"
           "    uint row = tg.y * tile_m + uint(tid.y);\n"
           "    uint col0 = tg.x * tile_n + uint(tid.x);\n"
           "    uint col1 = col0 + 8u;\n"
           "    uint lane = uint(tid.y) * 8u + uint(tid.x);\n"
           "    constexpr uint load_stride = 64u;\n"
           "    float acc0 = (row < p.rows && col0 < p.out_dim) ? bias[col0] : 0.0f;\n"
           "    float acc1 = (row < p.rows && col1 < p.out_dim) ? bias[col1] : 0.0f;\n"
           "    for (uint k0 = 0u; k0 < p.in_dim; k0 += tile_k) {\n"
           "        for (uint idx = lane; idx < tile_m * tile_k; idx += load_stride) {\n"
           "            uint ar = idx / tile_k;\n"
           "            uint ak = idx - ar * tile_k;\n"
           "            uint g_row = tg.y * tile_m + ar;\n"
           "            uint g_k = k0 + ak;\n"
           "            a_tile[ar][ak] = (g_row < p.rows && g_k < p.in_dim) ? input[g_row * p.in_dim + g_k] : 0.0f;\n"
           "        }\n"
           "        for (uint idx = lane; idx < tile_n * tile_k; idx += load_stride) {\n"
           "            uint bc = idx / tile_k;\n"
           "            uint bk = idx - bc * tile_k;\n"
           "            uint g_col = tg.x * tile_n + bc;\n"
           "            uint g_k = k0 + bk;\n"
           "            b_tile[bc][bk] = (g_col < p.out_dim && g_k < p.in_dim) ? weight[g_col * p.in_dim + g_k] : 0.0f;\n"
           "        }\n"
           "        threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "        for (uint kk = 0u; kk < tile_k; ++kk) { float a = a_tile[uint(tid.y)][kk]; acc0 += a * b_tile[uint(tid.x)][kk]; acc1 += a * b_tile[uint(tid.x) + 8u][kk]; }\n"
           "        threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    }\n"
           "    if (row < p.rows && col0 < p.out_dim) output[row * p.out_dim + col0] = acc0;\n"
           "    if (row < p.rows && col1 < p.out_dim) output[row * p.out_dim + col1] = acc1;\n"
           "}\n"
           "kernel void termite_apply_linear_multi_row_tiled_activation_f32(device const float *input [[buffer(0)]], device const float *weight [[buffer(1)]], device const float *bias [[buffer(2)]], device float *output [[buffer(3)]], constant termite_metal_linear_params &p [[buffer(4)]], constant termite_metal_apply_activation_params &ap [[buffer(5)]], ushort2 tid [[thread_position_in_threadgroup]], uint2 tg [[threadgroup_position_in_grid]]) {\n"
           "    constexpr uint tile_m = 8u;\n"
           "    constexpr uint tile_n = 16u;\n"
           "    constexpr uint tile_k = 32u;\n"
           "    threadgroup float a_tile[8][32];\n"
           "    threadgroup float b_tile[16][32];\n"
           "    uint row = tg.y * tile_m + uint(tid.y);\n"
           "    uint col0 = tg.x * tile_n + uint(tid.x);\n"
           "    uint col1 = col0 + 8u;\n"
           "    uint lane = uint(tid.y) * 8u + uint(tid.x);\n"
           "    constexpr uint load_stride = 64u;\n"
           "    float acc0 = (row < p.rows && col0 < p.out_dim) ? bias[col0] : 0.0f;\n"
           "    float acc1 = (row < p.rows && col1 < p.out_dim) ? bias[col1] : 0.0f;\n"
           "    for (uint k0 = 0u; k0 < p.in_dim; k0 += tile_k) {\n"
           "        for (uint idx = lane; idx < tile_m * tile_k; idx += load_stride) {\n"
           "            uint ar = idx / tile_k;\n"
           "            uint ak = idx - ar * tile_k;\n"
           "            uint g_row = tg.y * tile_m + ar;\n"
           "            uint g_k = k0 + ak;\n"
           "            a_tile[ar][ak] = (g_row < p.rows && g_k < p.in_dim) ? input[g_row * p.in_dim + g_k] : 0.0f;\n"
           "        }\n"
           "        for (uint idx = lane; idx < tile_n * tile_k; idx += load_stride) {\n"
           "            uint bc = idx / tile_k;\n"
           "            uint bk = idx - bc * tile_k;\n"
           "            uint g_col = tg.x * tile_n + bc;\n"
           "            uint g_k = k0 + bk;\n"
           "            b_tile[bc][bk] = (g_col < p.out_dim && g_k < p.in_dim) ? weight[g_col * p.in_dim + g_k] : 0.0f;\n"
           "        }\n"
           "        threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "        for (uint kk = 0u; kk < tile_k; ++kk) { float a = a_tile[uint(tid.y)][kk]; acc0 += a * b_tile[uint(tid.x)][kk]; acc1 += a * b_tile[uint(tid.x) + 8u][kk]; }\n"
           "        threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    }\n"
           "    if (row < p.rows && col0 < p.out_dim) output[row * p.out_dim + col0] = termite_apply_activation_scalar(acc0, ap.activation_kind);\n"
           "    if (row < p.rows && col1 < p.out_dim) output[row * p.out_dim + col1] = termite_apply_activation_scalar(acc1, ap.activation_kind);\n"
           "}\n"
           "kernel void termite_apply_linear_multi_row_tiled32_f32(device const float *input [[buffer(0)]], device const float *weight [[buffer(1)]], device const float *bias [[buffer(2)]], device float *output [[buffer(3)]], constant termite_metal_linear_params &p [[buffer(4)]], ushort2 tid [[thread_position_in_threadgroup]], uint2 tg [[threadgroup_position_in_grid]]) {\n"
           "    constexpr uint tile_m = 8u;\n"
           "    constexpr uint tile_n = 32u;\n"
           "    constexpr uint tile_k = 32u;\n"
           "    threadgroup float a_tile[8][32];\n"
           "    threadgroup float b_tile[32][32];\n"
           "    uint row = tg.y * tile_m + uint(tid.y);\n"
           "    uint col0 = tg.x * tile_n + uint(tid.x);\n"
           "    uint col1 = col0 + 8u;\n"
           "    uint col2 = col0 + 16u;\n"
           "    uint col3 = col0 + 24u;\n"
           "    uint lane = uint(tid.y) * 8u + uint(tid.x);\n"
           "    constexpr uint load_stride = 64u;\n"
           "    float acc0 = (row < p.rows && col0 < p.out_dim) ? bias[col0] : 0.0f;\n"
           "    float acc1 = (row < p.rows && col1 < p.out_dim) ? bias[col1] : 0.0f;\n"
           "    float acc2 = (row < p.rows && col2 < p.out_dim) ? bias[col2] : 0.0f;\n"
           "    float acc3 = (row < p.rows && col3 < p.out_dim) ? bias[col3] : 0.0f;\n"
           "    for (uint k0 = 0u; k0 < p.in_dim; k0 += tile_k) {\n"
           "        for (uint idx = lane; idx < tile_m * tile_k; idx += load_stride) {\n"
           "            uint ar = idx / tile_k; uint ak = idx - ar * tile_k; uint g_row = tg.y * tile_m + ar; uint g_k = k0 + ak;\n"
           "            a_tile[ar][ak] = (g_row < p.rows && g_k < p.in_dim) ? input[g_row * p.in_dim + g_k] : 0.0f;\n"
           "        }\n"
           "        for (uint idx = lane; idx < tile_n * tile_k; idx += load_stride) {\n"
           "            uint bc = idx / tile_k; uint bk = idx - bc * tile_k; uint g_col = tg.x * tile_n + bc; uint g_k = k0 + bk;\n"
           "            b_tile[bc][bk] = (g_col < p.out_dim && g_k < p.in_dim) ? weight[g_col * p.in_dim + g_k] : 0.0f;\n"
           "        }\n"
           "        threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "        for (uint kk = 0u; kk < tile_k; ++kk) { float a = a_tile[uint(tid.y)][kk]; uint tx = uint(tid.x); acc0 += a * b_tile[tx][kk]; acc1 += a * b_tile[tx + 8u][kk]; acc2 += a * b_tile[tx + 16u][kk]; acc3 += a * b_tile[tx + 24u][kk]; }\n"
           "        threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    }\n"
           "    if (row < p.rows && col0 < p.out_dim) output[row * p.out_dim + col0] = acc0;\n"
           "    if (row < p.rows && col1 < p.out_dim) output[row * p.out_dim + col1] = acc1;\n"
           "    if (row < p.rows && col2 < p.out_dim) output[row * p.out_dim + col2] = acc2;\n"
           "    if (row < p.rows && col3 < p.out_dim) output[row * p.out_dim + col3] = acc3;\n"
           "}\n"
           "kernel void termite_apply_linear_multi_row_tiled32_activation_f32(device const float *input [[buffer(0)]], device const float *weight [[buffer(1)]], device const float *bias [[buffer(2)]], device float *output [[buffer(3)]], constant termite_metal_linear_params &p [[buffer(4)]], constant termite_metal_apply_activation_params &ap [[buffer(5)]], ushort2 tid [[thread_position_in_threadgroup]], uint2 tg [[threadgroup_position_in_grid]]) {\n"
           "    constexpr uint tile_m = 8u;\n"
           "    constexpr uint tile_n = 32u;\n"
           "    constexpr uint tile_k = 32u;\n"
           "    threadgroup float a_tile[8][32];\n"
           "    threadgroup float b_tile[32][32];\n"
           "    uint row = tg.y * tile_m + uint(tid.y);\n"
           "    uint col0 = tg.x * tile_n + uint(tid.x);\n"
           "    uint col1 = col0 + 8u;\n"
           "    uint col2 = col0 + 16u;\n"
           "    uint col3 = col0 + 24u;\n"
           "    uint lane = uint(tid.y) * 8u + uint(tid.x);\n"
           "    constexpr uint load_stride = 64u;\n"
           "    float acc0 = (row < p.rows && col0 < p.out_dim) ? bias[col0] : 0.0f;\n"
           "    float acc1 = (row < p.rows && col1 < p.out_dim) ? bias[col1] : 0.0f;\n"
           "    float acc2 = (row < p.rows && col2 < p.out_dim) ? bias[col2] : 0.0f;\n"
           "    float acc3 = (row < p.rows && col3 < p.out_dim) ? bias[col3] : 0.0f;\n"
           "    for (uint k0 = 0u; k0 < p.in_dim; k0 += tile_k) {\n"
           "        for (uint idx = lane; idx < tile_m * tile_k; idx += load_stride) { uint ar = idx / tile_k; uint ak = idx - ar * tile_k; uint g_row = tg.y * tile_m + ar; uint g_k = k0 + ak; a_tile[ar][ak] = (g_row < p.rows && g_k < p.in_dim) ? input[g_row * p.in_dim + g_k] : 0.0f; }\n"
           "        for (uint idx = lane; idx < tile_n * tile_k; idx += load_stride) { uint bc = idx / tile_k; uint bk = idx - bc * tile_k; uint g_col = tg.x * tile_n + bc; uint g_k = k0 + bk; b_tile[bc][bk] = (g_col < p.out_dim && g_k < p.in_dim) ? weight[g_col * p.in_dim + g_k] : 0.0f; }\n"
           "        threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "        for (uint kk = 0u; kk < tile_k; ++kk) { float a = a_tile[uint(tid.y)][kk]; uint tx = uint(tid.x); acc0 += a * b_tile[tx][kk]; acc1 += a * b_tile[tx + 8u][kk]; acc2 += a * b_tile[tx + 16u][kk]; acc3 += a * b_tile[tx + 24u][kk]; }\n"
           "        threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    }\n"
           "    if (row < p.rows && col0 < p.out_dim) output[row * p.out_dim + col0] = termite_apply_activation_scalar(acc0, ap.activation_kind);\n"
	           "    if (row < p.rows && col1 < p.out_dim) output[row * p.out_dim + col1] = termite_apply_activation_scalar(acc1, ap.activation_kind);\n"
	           "    if (row < p.rows && col2 < p.out_dim) output[row * p.out_dim + col2] = termite_apply_activation_scalar(acc2, ap.activation_kind);\n"
	           "    if (row < p.rows && col3 < p.out_dim) output[row * p.out_dim + col3] = termite_apply_activation_scalar(acc3, ap.activation_kind);\n"
	           "}\n"
	           "kernel void termite_apply_linear_multi_row_tiled32_m16_f32(device const float *input [[buffer(0)]], device const float *weight [[buffer(1)]], device const float *bias [[buffer(2)]], device float *output [[buffer(3)]], constant termite_metal_linear_params &p [[buffer(4)]], ushort2 tid [[thread_position_in_threadgroup]], uint2 tg [[threadgroup_position_in_grid]]) {\n"
	           "    constexpr uint tile_m = 16u;\n"
	           "    constexpr uint tile_n = 32u;\n"
	           "    constexpr uint tile_k = 32u;\n"
	           "    threadgroup float a_tile[16][32];\n"
	           "    threadgroup float b_tile[32][32];\n"
	           "    uint row = tg.y * tile_m + uint(tid.y);\n"
	           "    uint col0 = tg.x * tile_n + uint(tid.x);\n"
	           "    uint col1 = col0 + 8u;\n"
	           "    uint col2 = col0 + 16u;\n"
	           "    uint col3 = col0 + 24u;\n"
	           "    uint lane = uint(tid.y) * 8u + uint(tid.x);\n"
	           "    constexpr uint load_stride = 128u;\n"
	           "    float acc0 = (row < p.rows && col0 < p.out_dim) ? bias[col0] : 0.0f;\n"
	           "    float acc1 = (row < p.rows && col1 < p.out_dim) ? bias[col1] : 0.0f;\n"
	           "    float acc2 = (row < p.rows && col2 < p.out_dim) ? bias[col2] : 0.0f;\n"
	           "    float acc3 = (row < p.rows && col3 < p.out_dim) ? bias[col3] : 0.0f;\n"
	           "    for (uint k0 = 0u; k0 < p.in_dim; k0 += tile_k) {\n"
	           "        for (uint idx = lane; idx < tile_m * tile_k; idx += load_stride) { uint ar = idx / tile_k; uint ak = idx - ar * tile_k; uint g_row = tg.y * tile_m + ar; uint g_k = k0 + ak; a_tile[ar][ak] = (g_row < p.rows && g_k < p.in_dim) ? input[g_row * p.in_dim + g_k] : 0.0f; }\n"
	           "        for (uint idx = lane; idx < tile_n * tile_k; idx += load_stride) { uint bc = idx / tile_k; uint bk = idx - bc * tile_k; uint g_col = tg.x * tile_n + bc; uint g_k = k0 + bk; b_tile[bc][bk] = (g_col < p.out_dim && g_k < p.in_dim) ? weight[g_col * p.in_dim + g_k] : 0.0f; }\n"
	           "        threadgroup_barrier(mem_flags::mem_threadgroup);\n"
	           "        for (uint kk = 0u; kk < tile_k; ++kk) { float a = a_tile[uint(tid.y)][kk]; uint tx = uint(tid.x); acc0 += a * b_tile[tx][kk]; acc1 += a * b_tile[tx + 8u][kk]; acc2 += a * b_tile[tx + 16u][kk]; acc3 += a * b_tile[tx + 24u][kk]; }\n"
	           "        threadgroup_barrier(mem_flags::mem_threadgroup);\n"
	           "    }\n"
	           "    if (row < p.rows && col0 < p.out_dim) output[row * p.out_dim + col0] = acc0;\n"
	           "    if (row < p.rows && col1 < p.out_dim) output[row * p.out_dim + col1] = acc1;\n"
	           "    if (row < p.rows && col2 < p.out_dim) output[row * p.out_dim + col2] = acc2;\n"
	           "    if (row < p.rows && col3 < p.out_dim) output[row * p.out_dim + col3] = acc3;\n"
	           "}\n"
	           "kernel void termite_apply_linear_multi_row_tiled32_m16_activation_f32(device const float *input [[buffer(0)]], device const float *weight [[buffer(1)]], device const float *bias [[buffer(2)]], device float *output [[buffer(3)]], constant termite_metal_linear_params &p [[buffer(4)]], constant termite_metal_apply_activation_params &ap [[buffer(5)]], ushort2 tid [[thread_position_in_threadgroup]], uint2 tg [[threadgroup_position_in_grid]]) {\n"
	           "    constexpr uint tile_m = 16u;\n"
	           "    constexpr uint tile_n = 32u;\n"
	           "    constexpr uint tile_k = 32u;\n"
	           "    threadgroup float a_tile[16][32];\n"
	           "    threadgroup float b_tile[32][32];\n"
	           "    uint row = tg.y * tile_m + uint(tid.y);\n"
	           "    uint col0 = tg.x * tile_n + uint(tid.x);\n"
	           "    uint col1 = col0 + 8u;\n"
	           "    uint col2 = col0 + 16u;\n"
	           "    uint col3 = col0 + 24u;\n"
	           "    uint lane = uint(tid.y) * 8u + uint(tid.x);\n"
	           "    constexpr uint load_stride = 128u;\n"
	           "    float acc0 = (row < p.rows && col0 < p.out_dim) ? bias[col0] : 0.0f;\n"
	           "    float acc1 = (row < p.rows && col1 < p.out_dim) ? bias[col1] : 0.0f;\n"
	           "    float acc2 = (row < p.rows && col2 < p.out_dim) ? bias[col2] : 0.0f;\n"
	           "    float acc3 = (row < p.rows && col3 < p.out_dim) ? bias[col3] : 0.0f;\n"
	           "    for (uint k0 = 0u; k0 < p.in_dim; k0 += tile_k) {\n"
	           "        for (uint idx = lane; idx < tile_m * tile_k; idx += load_stride) { uint ar = idx / tile_k; uint ak = idx - ar * tile_k; uint g_row = tg.y * tile_m + ar; uint g_k = k0 + ak; a_tile[ar][ak] = (g_row < p.rows && g_k < p.in_dim) ? input[g_row * p.in_dim + g_k] : 0.0f; }\n"
	           "        for (uint idx = lane; idx < tile_n * tile_k; idx += load_stride) { uint bc = idx / tile_k; uint bk = idx - bc * tile_k; uint g_col = tg.x * tile_n + bc; uint g_k = k0 + bk; b_tile[bc][bk] = (g_col < p.out_dim && g_k < p.in_dim) ? weight[g_col * p.in_dim + g_k] : 0.0f; }\n"
	           "        threadgroup_barrier(mem_flags::mem_threadgroup);\n"
	           "        for (uint kk = 0u; kk < tile_k; ++kk) { float a = a_tile[uint(tid.y)][kk]; uint tx = uint(tid.x); acc0 += a * b_tile[tx][kk]; acc1 += a * b_tile[tx + 8u][kk]; acc2 += a * b_tile[tx + 16u][kk]; acc3 += a * b_tile[tx + 24u][kk]; }\n"
	           "        threadgroup_barrier(mem_flags::mem_threadgroup);\n"
	           "    }\n"
	           "    if (row < p.rows && col0 < p.out_dim) output[row * p.out_dim + col0] = termite_apply_activation_scalar(acc0, ap.activation_kind);\n"
	           "    if (row < p.rows && col1 < p.out_dim) output[row * p.out_dim + col1] = termite_apply_activation_scalar(acc1, ap.activation_kind);\n"
	           "    if (row < p.rows && col2 < p.out_dim) output[row * p.out_dim + col2] = termite_apply_activation_scalar(acc2, ap.activation_kind);\n"
	           "    if (row < p.rows && col3 < p.out_dim) output[row * p.out_dim + col3] = termite_apply_activation_scalar(acc3, ap.activation_kind);\n"
	           "}\n"
	           "kernel void termite_apply_linear_bf16_multi_row(device const float *input [[buffer(0)]], device const ushort *weight [[buffer(1)]], device const float *bias [[buffer(2)]], device float *output [[buffer(3)]], constant termite_metal_linear_params &p [[buffer(4)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.out_dim;\n"
           "    if (gid >= total) return;\n"
           "    uint r = gid / p.out_dim;\n"
           "    uint o = gid % p.out_dim;\n"
           "    float acc = bias[o];\n"
           "    uint in_base = r * p.in_dim;\n"
           "    uint w_base = o * p.in_dim;\n"
           "    for (uint i = 0; i < p.in_dim; ++i) acc += input[in_base + i] * termite_bf16_to_f32(weight[w_base + i]);\n"
           "    output[gid] = acc;\n"
           "}\n"
           "kernel void termite_apply_linear_bf16_multi_row_reduce(device const float *input [[buffer(0)]], device const ushort *weight [[buffer(1)]], device const float *bias [[buffer(2)]], device float *output [[buffer(3)]], constant termite_metal_linear_params &p [[buffer(4)]], threadgroup float *partials [[threadgroup(0)]], ushort lane [[thread_index_in_threadgroup]], uint2 tg [[threadgroup_position_in_grid]]) {\n"
           "    uint o = tg.x;\n"
           "    uint r = tg.y;\n"
           "    if (r >= p.rows || o >= p.out_dim) return;\n"
           "    float acc = lane == 0u ? bias[o] : 0.0f;\n"
           "    uint in_base = r * p.in_dim;\n"
           "    uint w_base = o * p.in_dim;\n"
           "    for (uint i = lane; i < p.in_dim; i += 32u) acc += input[in_base + i] * termite_bf16_to_f32(weight[w_base + i]);\n"
           "    partials[lane] = acc;\n"
           "    threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    for (uint stride = 16u; stride > 0u; stride >>= 1u) {\n"
           "        if (lane < stride) partials[lane] += partials[lane + stride];\n"
           "        threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    }\n"
           "    if (lane == 0u) output[r * p.out_dim + o] = partials[0];\n"
           "}\n"
           "kernel void termite_apply_linear_bf16_multi_row_shared_reduce(device const float *input [[buffer(0)]], device const ushort *weight [[buffer(1)]], device const float *bias [[buffer(2)]], device float *output [[buffer(3)]], constant termite_metal_linear_params &p [[buffer(4)]], threadgroup float *partials [[threadgroup(0)]], ushort lane [[thread_index_in_threadgroup]], uint o [[threadgroup_position_in_grid]]) {\n"
           "    if (o >= p.out_dim || p.rows == 0u || p.rows > 64u) return;\n"
           "    for (uint r = 0u; r < p.rows; ++r) partials[r * 32u + lane] = lane == 0u ? bias[o] : 0.0f;\n"
           "    threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    uint w_base = o * p.in_dim;\n"
           "    for (uint i = lane; i < p.in_dim; i += 32u) {\n"
           "        float w = termite_bf16_to_f32(weight[w_base + i]);\n"
           "        for (uint r = 0u; r < p.rows; ++r) partials[r * 32u + lane] += input[r * p.in_dim + i] * w;\n"
           "    }\n"
           "    threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    for (uint stride = 16u; stride > 0u; stride >>= 1u) {\n"
           "        if (lane < stride) {\n"
           "            for (uint r = 0u; r < p.rows; ++r) partials[r * 32u + lane] += partials[r * 32u + lane + stride];\n"
           "        }\n"
           "        threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    }\n"
           "    if (lane == 0u) {\n"
           "        for (uint r = 0u; r < p.rows; ++r) output[r * p.out_dim + o] = partials[r * 32u];\n"
           "    }\n"
           "}\n"
           "kernel void termite_apply_linear_argmax_1x(device const float *input [[buffer(0)]], device const float *weight [[buffer(1)]], device const float *bias [[buffer(2)]], device uint *output [[buffer(3)]], constant termite_metal_apply_linear_params &p [[buffer(4)]], uint gid [[thread_position_in_grid]]) {\n"
           "    if (gid != 0) return;\n"
           "    float best = -INFINITY;\n"
           "    uint best_idx = 0u;\n"
           "    for (uint o = 0; o < p.out_dim; ++o) {\n"
           "        float acc = bias[o];\n"
           "        uint row_base = o * p.in_dim;\n"
           "        for (uint i = 0; i < p.in_dim; ++i) acc += input[i] * weight[row_base + i];\n"
           "        if (acc > best) { best = acc; best_idx = o; }\n"
           "    }\n"
           "    output[0] = best_idx;\n"
           "}\n"
           "kernel void termite_argmax_logits_1x(device const float *logits [[buffer(0)]], device uint *output [[buffer(1)]], constant uint &out_dim [[buffer(2)]], uint gid [[thread_position_in_grid]]) {\n"
           "    if (gid != 0) return;\n"
           "    if (out_dim == 0u) { output[0] = 0u; return; }\n"
           "    float best = logits[0];\n"
           "    uint best_idx = 0u;\n"
           "    for (uint i = 1u; i < out_dim; ++i) {\n"
           "        float value = logits[i];\n"
           "        if (value > best) { best = value; best_idx = i; }\n"
           "    }\n"
           "    output[0] = best_idx;\n"
           "}\n"
           "kernel void termite_argmax_logits_partials(device const float *logits [[buffer(0)]], device float *partial_values [[buffer(1)]], device uint *partial_ids [[buffer(2)]], constant uint &out_dim [[buffer(3)]], threadgroup float *sh_vals [[threadgroup(0)]], threadgroup uint *sh_ids [[threadgroup(1)]], ushort tid [[thread_index_in_threadgroup]], ushort tg_size [[threads_per_threadgroup]], uint block [[threadgroup_position_in_grid]]) {\n"
           "    const uint block_size = 1024u;\n"
           "    uint start = block * block_size;\n"
           "    uint end = min(start + block_size, out_dim);\n"
           "    float best = -INFINITY;\n"
           "    uint best_idx = 0u;\n"
           "    for (uint i = start + uint(tid); i < end; i += uint(tg_size)) {\n"
           "        float value = logits[i];\n"
           "        if (value > best || (value == best && i < best_idx)) { best = value; best_idx = i; }\n"
           "    }\n"
           "    sh_vals[tid] = best;\n"
           "    sh_ids[tid] = best_idx;\n"
           "    threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    for (uint stride = uint(tg_size) >> 1u; stride > 0u; stride >>= 1u) {\n"
           "        if (uint(tid) < stride) {\n"
           "            float other = sh_vals[uint(tid) + stride];\n"
           "            uint other_idx = sh_ids[uint(tid) + stride];\n"
           "            if (other > sh_vals[tid] || (other == sh_vals[tid] && other_idx < sh_ids[tid])) { sh_vals[tid] = other; sh_ids[tid] = other_idx; }\n"
           "        }\n"
           "        threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    }\n"
           "    if (tid == 0u) { partial_values[block] = sh_vals[0]; partial_ids[block] = sh_ids[0]; }\n"
           "}\n"
           "kernel void termite_argmax_logits_reduce(device const float *partial_values [[buffer(0)]], device const uint *partial_ids [[buffer(1)]], device uint *output [[buffer(2)]], constant uint &partial_count [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    if (gid != 0u) return;\n"
           "    if (partial_count == 0u) { output[0] = 0u; return; }\n"
           "    float best = partial_values[0];\n"
           "    uint best_idx = partial_ids[0];\n"
           "    for (uint i = 1u; i < partial_count; ++i) {\n"
           "        float value = partial_values[i];\n"
           "        uint idx = partial_ids[i];\n"
           "        if (value > best || (value == best && idx < best_idx)) { best = value; best_idx = idx; }\n"
           "    }\n"
           "    output[0] = best_idx;\n"
           "}\n"
           "inline float termite_apply_sampling_penalties(float logit, uint token_id, const device termite_metal_penalty_entry *entries, uint entry_count, constant termite_metal_sample_logits_params &p) {\n"
           "    if (entry_count == 0u) return logit;\n"
           "    for (uint i = 0u; i < entry_count; ++i) {\n"
           "        if (entries[i].token_id != token_id) continue;\n"
           "        float adjusted = logit;\n"
           "        if (p.repetition_penalty != 1.0f) {\n"
           "            adjusted = adjusted > 0.0f ? (adjusted / p.repetition_penalty) : (adjusted * p.repetition_penalty);\n"
           "        }\n"
           "        if (p.frequency_penalty != 0.0f) adjusted -= p.frequency_penalty * float(entries[i].count);\n"
           "        if (p.presence_penalty != 0.0f) adjusted -= p.presence_penalty;\n"
           "        return adjusted;\n"
           "    }\n"
           "    return logit;\n"
           "}\n"
           "kernel void termite_sample_logits_1x(device const float *logits [[buffer(0)]], device float *probs [[buffer(1)]], device uint *output [[buffer(2)]], constant termite_metal_sample_logits_params &p [[buffer(3)]], device const termite_metal_penalty_entry *penalties [[buffer(4)]], uint gid [[thread_position_in_grid]]) {\n"
           "    if (gid != 0) return;\n"
           "    if (p.out_dim == 0u) { output[0] = 0u; return; }\n"
           "    const uint max_top_k = 256u;\n"
           "    thread float top_vals[256];\n"
           "    thread uint top_idx[256];\n"
           "    uint candidate_count = 0u;\n"
           "    if (p.top_k > 0u && p.top_k <= max_top_k) {\n"
           "        candidate_count = min(p.top_k, p.out_dim);\n"
           "    } else if (p.top_p > 0.0f && p.top_p < 1.0f && p.out_dim <= max_top_k) {\n"
           "        candidate_count = p.out_dim;\n"
           "    }\n"
           "    float cutoff = -INFINITY;\n"
           "    if (candidate_count != 0u) {\n"
           "        for (uint i = 0u; i < candidate_count; ++i) { top_vals[i] = -INFINITY; top_idx[i] = 0u; }\n"
           "        for (uint i = 0u; i < p.out_dim; ++i) {\n"
           "            float adjusted = termite_apply_sampling_penalties(logits[i], i, penalties, p.penalty_count, p);\n"
           "            float scaled = (p.temperature > 0.0f) ? (adjusted / p.temperature) : adjusted;\n"
           "            if (scaled <= top_vals[candidate_count - 1u]) continue;\n"
           "            uint insert_at = candidate_count - 1u;\n"
           "            while (insert_at > 0u && scaled > top_vals[insert_at - 1u]) {\n"
           "                top_vals[insert_at] = top_vals[insert_at - 1u];\n"
           "                top_idx[insert_at] = top_idx[insert_at - 1u];\n"
           "                insert_at -= 1u;\n"
           "            }\n"
           "            top_vals[insert_at] = scaled;\n"
           "            top_idx[insert_at] = i;\n"
           "        }\n"
           "        cutoff = top_vals[candidate_count - 1u];\n"
           "    }\n"
           "    float best_scaled = -INFINITY;\n"
           "    uint best_idx = 0u;\n"
           "    for (uint i = 0u; i < p.out_dim; ++i) {\n"
           "        float adjusted = termite_apply_sampling_penalties(logits[i], i, penalties, p.penalty_count, p);\n"
           "        float scaled = (p.temperature > 0.0f) ? (adjusted / p.temperature) : adjusted;\n"
           "        if (candidate_count != 0u && scaled < cutoff) continue;\n"
           "        if (scaled > best_scaled) { best_scaled = scaled; best_idx = i; }\n"
           "    }\n"
           "    if (!(best_scaled > -INFINITY)) { output[0] = 0u; return; }\n"
           "    if (p.temperature <= 0.0f) { output[0] = best_idx; return; }\n"
           "    float sum = 0.0f;\n"
           "    float max_prob = 0.0f;\n"
           "    if (candidate_count != 0u) {\n"
           "        for (uint i = 0u; i < candidate_count; ++i) {\n"
           "            float prob = exp(top_vals[i] - best_scaled);\n"
           "            probs[i] = prob;\n"
           "            sum += prob;\n"
           "            if (prob > max_prob) max_prob = prob;\n"
           "        }\n"
           "        if (p.top_p > 0.0f && p.top_p < 1.0f) {\n"
           "            float threshold = p.top_p * sum;\n"
           "            float cumulative = 0.0f;\n"
           "            sum = 0.0f;\n"
           "            for (uint i = 0u; i < candidate_count; ++i) {\n"
           "                cumulative += probs[i];\n"
           "                if (cumulative > threshold && i > 0u) probs[i] = 0.0f;\n"
           "                sum += probs[i];\n"
           "            }\n"
           "        }\n"
           "    } else {\n"
           "        for (uint i = 0u; i < p.out_dim; ++i) {\n"
           "            float adjusted = termite_apply_sampling_penalties(logits[i], i, penalties, p.penalty_count, p);\n"
           "            float scaled = adjusted / p.temperature;\n"
           "            float prob = exp(scaled - best_scaled);\n"
           "            probs[i] = prob;\n"
           "            sum += prob;\n"
           "            if (prob > max_prob) max_prob = prob;\n"
           "        }\n"
           "    }\n"
           "    if (p.min_p > 0.0f && p.min_p < 1.0f) {\n"
           "        float threshold = max_prob * p.min_p;\n"
           "        sum = 0.0f;\n"
           "        uint limit = candidate_count != 0u ? candidate_count : p.out_dim;\n"
           "        for (uint i = 0u; i < limit; ++i) {\n"
           "            float prob = probs[i];\n"
           "            if (prob < threshold) { probs[i] = 0.0f; } else { sum += prob; }\n"
           "        }\n"
           "    }\n"
           "    if (sum <= 0.0f) { output[0] = best_idx; return; }\n"
           "    uint state = p.seed ^ 0x9e3779b9u;\n"
           "    state ^= state << 13; state ^= state >> 17; state ^= state << 5;\n"
           "    float target = (float(state) / 4294967296.0f) * sum;\n"
           "    float cumulative = 0.0f;\n"
           "    uint limit = candidate_count != 0u ? candidate_count : p.out_dim;\n"
           "    for (uint i = 0u; i < limit; ++i) {\n"
           "        cumulative += probs[i];\n"
           "        if (cumulative >= target) { output[0] = (candidate_count != 0u ? top_idx[i] : i); return; }\n"
           "    }\n"
           "    output[0] = best_idx;\n"
           "}\n"
           "kernel void termite_sample_topk_partials(device const float *logits [[buffer(0)]], device float *partial_values [[buffer(1)]], device uint *partial_ids [[buffer(2)]], constant termite_metal_sample_logits_params &p [[buffer(3)]], device const termite_metal_penalty_entry *penalties [[buffer(4)]], uint block [[thread_position_in_grid]]) {\n"
           "    const uint block_size = 512u;\n"
           "    const uint max_top_k = 64u;\n"
           "    uint top_k = min(p.top_k, max_top_k);\n"
           "    if (top_k == 0u) return;\n"
           "    thread float top_vals[64];\n"
           "    thread uint top_idx[64];\n"
           "    for (uint i = 0u; i < top_k; ++i) { top_vals[i] = -INFINITY; top_idx[i] = 0u; }\n"
           "    uint start = block * block_size;\n"
           "    uint end = min(start + block_size, p.out_dim);\n"
           "    for (uint i = start; i < end; ++i) {\n"
           "        float adjusted = termite_apply_sampling_penalties(logits[i], i, penalties, p.penalty_count, p);\n"
           "        float scaled = (p.temperature > 0.0f) ? (adjusted / p.temperature) : adjusted;\n"
           "        if (scaled <= top_vals[top_k - 1u]) continue;\n"
           "        uint insert_at = top_k - 1u;\n"
           "        while (insert_at > 0u && scaled > top_vals[insert_at - 1u]) {\n"
           "            top_vals[insert_at] = top_vals[insert_at - 1u];\n"
           "            top_idx[insert_at] = top_idx[insert_at - 1u];\n"
           "            insert_at -= 1u;\n"
           "        }\n"
           "        top_vals[insert_at] = scaled;\n"
           "        top_idx[insert_at] = i;\n"
           "    }\n"
           "    uint base = block * top_k;\n"
           "    for (uint i = 0u; i < top_k; ++i) {\n"
           "        partial_values[base + i] = top_vals[i];\n"
           "        partial_ids[base + i] = top_idx[i];\n"
           "    }\n"
           "}\n"
           "kernel void termite_sample_topk_reduce(device const float *partial_values [[buffer(0)]], device const uint *partial_ids [[buffer(1)]], device float *probs [[buffer(2)]], device uint *output [[buffer(3)]], constant termite_metal_topk_reduce_params &p [[buffer(4)]], uint gid [[thread_position_in_grid]]) {\n"
           "    if (gid != 0u) return;\n"
           "    if (p.candidate_count == 0u || p.top_k == 0u) { output[0] = 0u; return; }\n"
           "    const uint max_top_k = 64u;\n"
           "    uint top_k = min(p.top_k, max_top_k);\n"
           "    thread float top_vals[64];\n"
           "    thread uint top_idx[64];\n"
           "    for (uint i = 0u; i < top_k; ++i) { top_vals[i] = -INFINITY; top_idx[i] = 0u; }\n"
           "    for (uint i = 0u; i < p.candidate_count; ++i) {\n"
           "        float v = partial_values[i];\n"
           "        if (v <= top_vals[top_k - 1u]) continue;\n"
           "        uint insert_at = top_k - 1u;\n"
           "        while (insert_at > 0u && v > top_vals[insert_at - 1u]) {\n"
           "            top_vals[insert_at] = top_vals[insert_at - 1u];\n"
           "            top_idx[insert_at] = top_idx[insert_at - 1u];\n"
           "            insert_at -= 1u;\n"
           "        }\n"
           "        top_vals[insert_at] = v;\n"
           "        top_idx[insert_at] = partial_ids[i];\n"
           "    }\n"
           "    float best_scaled = top_vals[0];\n"
           "    uint best_idx = top_idx[0];\n"
           "    if (!(best_scaled > -INFINITY)) { output[0] = 0u; return; }\n"
           "    float sum = 0.0f;\n"
           "    float max_prob = 0.0f;\n"
           "    for (uint i = 0u; i < top_k; ++i) {\n"
           "        float prob = exp(top_vals[i] - best_scaled);\n"
           "        probs[i] = prob;\n"
           "        sum += prob;\n"
           "        if (prob > max_prob) max_prob = prob;\n"
           "    }\n"
           "    if (p.top_p > 0.0f && p.top_p < 1.0f) {\n"
           "        float threshold = p.top_p * sum;\n"
           "        float cumulative = 0.0f;\n"
           "        sum = 0.0f;\n"
           "        for (uint i = 0u; i < top_k; ++i) {\n"
           "            cumulative += probs[i];\n"
           "            if (cumulative > threshold && i > 0u) probs[i] = 0.0f;\n"
           "            sum += probs[i];\n"
           "        }\n"
           "    }\n"
           "    if (p.min_p > 0.0f && p.min_p < 1.0f) {\n"
           "        float threshold = max_prob * p.min_p;\n"
           "        sum = 0.0f;\n"
           "        for (uint i = 0u; i < top_k; ++i) {\n"
           "            float prob = probs[i];\n"
           "            if (prob < threshold) { probs[i] = 0.0f; } else { sum += prob; }\n"
           "        }\n"
           "    }\n"
           "    if (sum <= 0.0f) { output[0] = best_idx; return; }\n"
           "    uint state = p.seed ^ 0x9e3779b9u;\n"
           "    state ^= state << 13; state ^= state >> 17; state ^= state << 5;\n"
           "    float target = (float(state) / 4294967296.0f) * sum;\n"
           "    float cumulative = 0.0f;\n"
           "    for (uint i = 0u; i < top_k; ++i) {\n"
           "        cumulative += probs[i];\n"
           "        if (cumulative >= target) { output[0] = top_idx[i]; return; }\n"
           "    }\n"
           "    output[0] = best_idx;\n"
           "}\n"
           "kernel void termite_apply_activation_1x(device const float *input [[buffer(0)]], device float *output [[buffer(1)]], constant termite_metal_apply_activation_params &p [[buffer(2)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.dim; if (gid >= total) return;\n"
           "    output[gid] = termite_apply_activation_scalar(input[gid], p.activation_kind);\n"
           "}\n"
           "kernel void termite_apply_bias_activation_2d(device const float *input [[buffer(0)]], device const float *bias [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_apply_activation_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.dim; if (gid >= total || p.dim == 0u) return;\n"
           "    uint col = gid % p.dim;\n"
           "    output[gid] = termite_apply_activation_scalar(input[gid] + bias[col], p.activation_kind);\n"
           "}\n"
           "kernel void termite_apply_activation_multiply_1x(device const float *gate [[buffer(0)]], device const float *up [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_apply_activation_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.dim; if (gid >= total) return;\n"
           "    float y = termite_apply_activation_scalar(gate[gid], p.activation_kind);\n"
           "    output[gid] = y == 0.0f ? 0.0f : y * up[gid];\n"
           "}\n"
           "kernel void termite_apply_softmax_rows(device const float *input [[buffer(0)]], device float *output [[buffer(1)]], constant termite_metal_apply_softmax_params &p [[buffer(2)]], threadgroup float *shmem [[threadgroup(0)]], uint tid [[thread_index_in_threadgroup]], uint row [[threadgroup_position_in_grid]]) {\n"
           "    if (row >= p.rows || p.dim == 0u) return;\n"
           "    const uint threads = 256u;\n"
           "    const uint base = row * p.dim;\n"
           "    float local_max = -3.4028234663852886e+38f;\n"
           "    for (uint col = tid; col < p.dim; col += threads) local_max = max(local_max, input[base + col]);\n"
           "    shmem[tid] = local_max;\n"
           "    threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    for (uint stride = threads >> 1u; stride > 0u; stride >>= 1u) {\n"
           "        if (tid < stride) shmem[tid] = max(shmem[tid], shmem[tid + stride]);\n"
           "        threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    }\n"
           "    const float row_max = shmem[0];\n"
           "    float local_sum = 0.0f;\n"
           "    for (uint col = tid; col < p.dim; col += threads) local_sum += exp(input[base + col] - row_max);\n"
           "    shmem[tid] = local_sum;\n"
           "    threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    for (uint stride = threads >> 1u; stride > 0u; stride >>= 1u) {\n"
           "        if (tid < stride) shmem[tid] += shmem[tid + stride];\n"
           "        threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    }\n"
           "    const float row_sum = max(shmem[0], 1.0e-30f);\n"
           "    const float log_sum = log(row_sum);\n"
           "    for (uint col = tid; col < p.dim; col += threads) {\n"
           "        const float shifted = input[base + col] - row_max;\n"
           "        output[base + col] = p.log_softmax != 0u ? shifted - log_sum : exp(shifted) / row_sum;\n"
           "    }\n"
           "}\n"
           "kernel void termite_reduce_last_dim_rows(device const float *input [[buffer(0)]], device float *output [[buffer(1)]], constant termite_metal_reduce_last_dim_params &p [[buffer(2)]], threadgroup float *shmem [[threadgroup(0)]], uint tid [[thread_index_in_threadgroup]], uint row [[threadgroup_position_in_grid]]) {\n"
           "    if (row >= p.rows || p.dim == 0u) return;\n"
           "    const uint threads = 256u;\n"
           "    const uint base = row * p.dim;\n"
           "    float local = p.kind == 1u ? -3.4028234663852886e+38f : 0.0f;\n"
           "    if (p.kind == 1u) {\n"
           "        for (uint col = tid; col < p.dim; col += threads) local = max(local, input[base + col]);\n"
           "    } else {\n"
           "        for (uint col = tid; col < p.dim; col += threads) local += input[base + col];\n"
           "    }\n"
           "    shmem[tid] = local;\n"
           "    threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    for (uint stride = threads >> 1u; stride > 0u; stride >>= 1u) {\n"
           "        if (tid < stride) {\n"
           "            if (p.kind == 1u) shmem[tid] = max(shmem[tid], shmem[tid + stride]);\n"
           "            else shmem[tid] += shmem[tid + stride];\n"
           "        }\n"
           "        threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    }\n"
           "    if (tid == 0u) output[row] = p.kind == 2u ? shmem[0] / float(p.dim) : shmem[0];\n"
           "}\n"
           "kernel void termite_multiply_reduce_last_dim_rows(device const float *lhs [[buffer(0)]], device const float *rhs [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_reduce_last_dim_params &p [[buffer(3)]], threadgroup float *shmem [[threadgroup(0)]], uint tid [[thread_index_in_threadgroup]], uint row [[threadgroup_position_in_grid]]) {\n"
           "    if (row >= p.rows || p.dim == 0u) return;\n"
           "    const uint threads = 256u;\n"
           "    const uint base = row * p.dim;\n"
           "    float local = 0.0f;\n"
           "    for (uint col = tid; col < p.dim; col += threads) local += lhs[base + col] * rhs[base + col];\n"
           "    shmem[tid] = local;\n"
           "    threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    for (uint stride = threads >> 1u; stride > 0u; stride >>= 1u) {\n"
           "        if (tid < stride) shmem[tid] += shmem[tid + stride];\n"
           "        threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    }\n"
           "    if (tid == 0u) output[row] = shmem[0];\n"
           "}\n"
           "kernel void termite_broadcast_last_dim_f32_2d(device const float *input [[buffer(0)]], device float *output [[buffer(1)]], constant termite_metal_broadcast_last_dim_params &p [[buffer(2)]], uint gid [[thread_position_in_grid]]) {\n"
           "    const uint total = p.rows * p.out_dim;\n"
           "    if (gid >= total || p.in_dim == 0u || p.out_dim == 0u) return;\n"
           "    const uint row = gid / p.out_dim;\n"
           "    const uint col = gid - row * p.out_dim;\n"
           "    const uint in_col = p.in_dim == 1u ? 0u : col;\n"
           "    output[gid] = input[row * p.in_dim + in_col];\n"
           "}\n"
           "kernel void termite_broadcast_f32(device const float *input [[buffer(0)]], device float *output [[buffer(1)]], constant termite_metal_broadcast_f32_params &p [[buffer(2)]], uint gid [[thread_position_in_grid]]) {\n"
           "    if (gid >= p.total || p.rank == 0u || p.rank > 8u) return;\n"
           "    uint rem = gid;\n"
           "    uint flat_in = 0u;\n"
           "    for (uint axis = 0u; axis < p.rank; ++axis) {\n"
           "        uint stride = p.out_strides[axis];\n"
           "        uint coord = stride == 0u ? 0u : rem / stride;\n"
           "        if (stride != 0u) rem -= coord * stride;\n"
           "        flat_in += coord * p.input_strides_for_out[axis];\n"
           "    }\n"
           "    output[gid] = input[flat_in];\n"
           "}\n"
           "kernel void termite_reduce_axis_f32(device const float *input [[buffer(0)]], device float *output [[buffer(1)]], constant termite_metal_reduce_axis_f32_params &p [[buffer(2)]], uint gid [[thread_position_in_grid]]) {\n"
           "    if (gid >= p.out_total || p.rank == 0u || p.rank > 8u || p.reduce_dim == 0u) return;\n"
           "    uint rem = gid;\n"
           "    uint base = 0u;\n"
           "    for (uint axis = 0u; axis < p.rank; ++axis) {\n"
           "        uint stride = p.out_strides[axis];\n"
           "        uint coord = stride == 0u ? 0u : rem / stride;\n"
           "        if (stride != 0u) rem -= coord * stride;\n"
           "        base += coord * p.input_strides_for_out[axis];\n"
           "    }\n"
           "    float acc = 0.0f;\n"
           "    for (uint i = 0u; i < p.reduce_dim; ++i) acc += input[base + i * p.reduce_stride];\n"
           "    output[gid] = p.kind == 2u ? acc / float(p.reduce_dim) : acc;\n"
           "}\n"
           "kernel void termite_apply_add_1x(device const float *lhs [[buffer(0)]], device const float *rhs [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_apply_add_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.dim; if (gid >= total) return;\n"
           "    output[gid] = lhs[gid] + rhs[gid];\n"
           "}\n"
           "kernel void termite_apply_add_scale_1x(device const float *lhs [[buffer(0)]], device const float *rhs [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_apply_add_params &p [[buffer(3)]], constant float &scale [[buffer(4)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.dim; if (gid >= total) return;\n"
           "    output[gid] = (lhs[gid] + rhs[gid]) * scale;\n"
           "}\n"
           "kernel void termite_apply_scaled_add_scale_1x(device const float *lhs [[buffer(0)]], device const float *rhs [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_apply_add_params &p [[buffer(3)]], constant float2 &scales [[buffer(4)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.dim; if (gid >= total) return;\n"
           "    output[gid] = (lhs[gid] * scales.x + rhs[gid]) * scales.y;\n"
           "}\n"
           "kernel void termite_training_accumulate_f32(device float *accum [[buffer(0)]], device const float *grad [[buffer(1)]], constant termite_metal_training_accumulate_params &p [[buffer(2)]], uint gid [[thread_position_in_grid]]) {\n"
           "    if (gid >= p.elem_count) return;\n"
           "    float g = grad[gid] * p.scale;\n"
           "    accum[gid] = (p.first != 0u) ? g : (accum[gid] + g);\n"
           "}\n"
           "kernel void termite_training_adamw_f32(device float *weight [[buffer(0)]], device float *grad [[buffer(1)]], device float *m [[buffer(2)]], device float *v [[buffer(3)]], constant termite_metal_training_adamw_params &p [[buffer(4)]], uint gid [[thread_position_in_grid]]) {\n"
           "    if (gid >= p.elem_count) return;\n"
           "    float g = grad[gid] * p.grad_scale;\n"
           "    float mv = p.beta1 * m[gid] + (1.0f - p.beta1) * g;\n"
           "    float vv = p.beta2 * v[gid] + (1.0f - p.beta2) * g * g;\n"
           "    m[gid] = mv;\n"
           "    v[gid] = vv;\n"
           "    float mh = mv / p.bias_correction1;\n"
           "    float vh = vv / p.bias_correction2;\n"
           "    float update = mh / (sqrt(vh) + p.eps) + p.weight_decay * weight[gid];\n"
           "    weight[gid] -= p.lr * update;\n"
           "    grad[gid] = 0.0f;\n"
           "}\n"
           "kernel void termite_training_sumsq_f32(device const float *input [[buffer(0)]], device float *output [[buffer(1)]], constant termite_metal_training_sumsq_params &p [[buffer(2)]], uint gid [[thread_position_in_grid]]) {\n"
           "    if (gid != 0u) return;\n"
           "    float sum = 0.0f;\n"
           "    for (uint i = 0u; i < p.elem_count; i += 1u) {\n"
           "        float v = input[i];\n"
           "        sum += v * v;\n"
           "    }\n"
           "    output[0] = sum;\n"
           "}\n"
           "kernel void termite_apply_subtract_1x(device const float *lhs [[buffer(0)]], device const float *rhs [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_apply_add_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.dim; if (gid >= total) return;\n"
           "    float l = lhs[(p.flags & 1u) != 0u ? 0u : gid]; float r = rhs[(p.flags & 2u) != 0u ? 0u : gid];\n"
           "    output[gid] = l - r;\n"
           "}\n"
           "kernel void termite_apply_divide_1x(device const float *lhs [[buffer(0)]], device const float *rhs [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_apply_add_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.dim; if (gid >= total) return;\n"
           "    uint col = p.dim == 0u ? gid : gid % p.dim;\n"
           "    float l = lhs[(p.flags & 1u) != 0u ? 0u : gid]; float r = rhs[(p.flags & 2u) != 0u ? 0u : ((p.flags & 4u) != 0u ? col : gid)];\n"
           "    output[gid] = l / r;\n"
           "}\n"
           "kernel void termite_apply_less_than_1x(device const float *lhs [[buffer(0)]], device const float *rhs [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_apply_add_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.dim; if (gid >= total) return;\n"
           "    float l = lhs[(p.flags & 1u) != 0u ? 0u : gid]; float r = rhs[(p.flags & 2u) != 0u ? 0u : gid];\n"
           "    output[gid] = l < r ? 1.0f : 0.0f;\n"
           "}\n"
           "kernel void termite_apply_multiply_1x(device const float *lhs [[buffer(0)]], device const float *rhs [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_apply_add_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.dim; if (gid >= total) return;\n"
           "    uint col = p.dim == 0u ? gid : gid % p.dim;\n"
           "    float l = lhs[(p.flags & 1u) != 0u ? 0u : gid]; float r = rhs[(p.flags & 2u) != 0u ? 0u : ((p.flags & 4u) != 0u ? col : gid)];\n"
           "    output[gid] = l * r;\n"
           "}\n"
           "kernel void termite_apply_scale_1x(device const float *input [[buffer(0)]], device float *output [[buffer(1)]], constant float &scale [[buffer(2)]], uint gid [[thread_position_in_grid]]) {\n"
           "    output[gid] = input[gid] * scale;\n"
           "}\n"
           "kernel void termite_apply_where_select_1x(device const float *cond [[buffer(0)]], device const float *on_true [[buffer(1)]], device const float *on_false [[buffer(2)]], device float *output [[buffer(3)]], constant termite_metal_apply_add_params &p [[buffer(4)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.dim; if (gid >= total) return;\n"
           "    float c = cond[(p.flags & 1u) != 0u ? 0u : gid]; float t = on_true[(p.flags & 2u) != 0u ? 0u : gid]; float f = on_false[(p.flags & 4u) != 0u ? 0u : gid];\n"
           "    output[gid] = c != 0.0f ? t : f;\n"
           "}\n"
           "kernel void termite_q2_k_linear(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.out_dim; if (gid >= total) return; uint r = gid / p.out_dim; uint o = gid % p.out_dim; uint row_offset = o * p.row_blocks * 84u; float acc = 0.0f;\n"
           "    for (uint b = 0; b < p.row_blocks; ++b) { uint off = row_offset + b * 84u; const device uchar *scales_raw = weight + off; ushort d_bits = (ushort(weight[off + 17]) << 8) | ushort(weight[off + 16]); ushort dmin_bits = (ushort(weight[off + 19]) << 8) | ushort(weight[off + 18]); float d = float(as_type<half>(d_bits)); float dmin = float(as_type<half>(dmin_bits)); const device uchar *qs = weight + off + 20u; for (uint sub = 0; sub < 16; ++sub) { float sc = float(scales_raw[sub] & 0x0Fu); float m = float(scales_raw[sub] >> 4); float dsc = d * sc; float dmn = dmin * m; uint chunk = sub / 8u; uint group = (sub % 8u) / 2u; uint l_base = (sub % 2u) * 16u; uint q_base = chunk * 32u; uint shift = group * 2u; uint input_off = r * p.in_dim + b * 256u + sub * 16u; for (uint i = 0; i < 16; ++i) { int q = int((qs[q_base + l_base + i] >> shift) & 0x03u); acc += input[input_off + i] * (dsc * float(q) - dmn); } } }\n"
           "    output[gid] = acc;\n"
           "}\n"
           "kernel void termite_q3_k_linear(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.out_dim; if (gid >= total) return; uint r = gid / p.out_dim; uint o = gid % p.out_dim; uint row_offset = o * p.row_blocks * 110u; float acc = 0.0f;\n"
           "    for (uint b = 0; b < p.row_blocks; ++b) { uint off = row_offset + b * 110u; const device uchar *hmask = weight + off; const device uchar *qs = weight + off + 32u; const device uchar *scale_data = weight + off + 96u; ushort d_bits = (ushort(weight[off + 109]) << 8) | ushort(weight[off + 108]); float d = float(as_type<half>(d_bits)); int raw_scales[16]; for (uint i = 0; i < 4; ++i) { raw_scales[i] = int(scale_data[i] & 0x0Fu) | (int(scale_data[8 + i] & 0x03u) << 4); raw_scales[i + 4] = int(scale_data[4 + i] & 0x0Fu) | (int((scale_data[8 + i] >> 2) & 0x03u) << 4); raw_scales[i + 8] = int((scale_data[i] >> 4) & 0x0Fu) | (int((scale_data[8 + i] >> 4) & 0x03u) << 4); raw_scales[i + 12] = int((scale_data[4 + i] >> 4) & 0x0Fu) | (int((scale_data[8 + i] >> 6) & 0x03u) << 4); } for (uint sub = 0; sub < 16; ++sub) { float scale = d * float(raw_scales[sub] - 32); uint chunk = sub / 8u; uint group = (sub % 8u) / 2u; uint l_base = (sub % 2u) * 16u; uint q_base = chunk * 32u; uint shift = group * 2u; uint hm_bit = chunk * 4u + group; uint input_off = r * p.in_dim + b * 256u + sub * 16u; for (uint i = 0; i < 16; ++i) { uint l = l_base + i; int low2 = int((qs[q_base + l] >> shift) & 0x03u); int high1 = int((hmask[l] >> hm_bit) & 0x01u); int q = low2 + high1 * 4 - 4; acc += input[input_off + i] * (scale * float(q)); } } }\n"
           "    output[gid] = acc;\n"
           "}\n"
           "kernel void termite_q4_k_linear(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.out_dim; if (gid >= total) return; uint r = gid / p.out_dim; uint o = gid % p.out_dim; uint row_offset = o * p.row_blocks * 144u; float acc = 0.0f;\n"
           "    for (uint b = 0; b < p.row_blocks; ++b) { uint off = row_offset + b * 144u; ushort d_bits = (ushort(weight[off + 1]) << 8) | ushort(weight[off]); ushort dmin_bits = (ushort(weight[off + 3]) << 8) | ushort(weight[off + 2]); float d = float(as_type<half>(d_bits)); float dmin = float(as_type<half>(dmin_bits)); const device uchar *scales = weight + off + 4u; const device uchar *qs = weight + off + 16u; thread float scs[8]; thread float mins[8]; unpack_q4k_scale_mins(scales, scs, mins); uint q_off = 0u; for (uint chunk = 0; chunk < 4; ++chunk) { uint sub = chunk * 2u; float dsc0 = d * scs[sub]; float dmn0 = dmin * mins[sub]; float dsc1 = d * scs[sub + 1u]; float dmn1 = dmin * mins[sub + 1u]; uint input_off = r * p.in_dim + b * 256u + chunk * 64u; for (uint i = 0; i < 32; ++i) { acc += input[input_off + i] * (dsc0 * float(qs[q_off + i] & 0x0Fu) - dmn0); } for (uint i = 0; i < 32; ++i) { acc += input[input_off + 32u + i] * (dsc1 * float(qs[q_off + i] >> 4) - dmn1); } q_off += 32u; } }\n"
           "    output[gid] = acc;\n"
           "}\n"
           "kernel void termite_q4_k_linear_1x_reduce(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], threadgroup float *shmem [[threadgroup(0)]], ushort lane [[thread_index_in_simdgroup]], ushort sgitg [[simdgroup_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) {\n"
           "    const uint NR0 = 2u; const uint NSG = 2u; const uint NQ = 8u; const uint NW = 32u; uint first_o = tg.x * NR0; uint r = tg.y; if (r >= p.rows || lane >= NW) return;\n"
           "    uint ix = lane / (NW / NQ); uint il = lane - ix * (NW / NQ); uint sb0 = sgitg * NQ + ix; uint in_row = r * p.in_dim; uint total_subblocks = p.row_blocks * 8u; float acc0 = 0.0f; float acc1 = 0.0f;\n"
           "    for (uint sb = sb0; sb < total_subblocks; sb += NSG * NQ) { uint b = sb >> 3; uint sub = sb & 7u; uint chunk = sub >> 1; bool high = (sub & 1u) != 0u; uint j0 = il * NQ; uint input_off = in_row + b * 256u + sub * 32u + j0; float x[8]; for (uint i = 0; i < NQ; ++i) { x[i] = input[input_off + i]; } for (uint row = 0; row < NR0; ++row) { uint o = first_o + row; if (o >= p.out_dim) continue; uint off = o * p.row_blocks * 144u + b * 144u; ushort d_bits = (ushort(weight[off + 1]) << 8) | ushort(weight[off]); ushort dmin_bits = (ushort(weight[off + 3]) << 8) | ushort(weight[off + 2]); float d = float(as_type<half>(d_bits)); float dmin = float(as_type<half>(dmin_bits)); const device uchar *scales = weight + off + 4u; const device uchar *qs = weight + off + 16u; thread float scs[8]; thread float mins[8]; unpack_q4k_scale_mins(scales, scs, mins); float dsc = d * scs[sub]; float dmn = dmin * mins[sub]; uint q_off = chunk * 32u + j0; float block = 0.0f; for (uint i = 0; i < NQ; ++i) { uchar packed = qs[q_off + i]; float q = high ? float(packed >> 4) : float(packed & 0x0Fu); block += x[i] * (dsc * q - dmn); } if (row == 0u) acc0 += block; else acc1 += block; } }\n"
           "    if (sgitg == 0u) { shmem[lane] = 0.0f; shmem[32u + lane] = 0.0f; } acc0 = simd_sum(acc0); acc1 = simd_sum(acc1); threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    if (lane == 0u) { shmem[sgitg] = acc0; shmem[32u + sgitg] = acc1; } threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    float total0 = simd_sum(shmem[lane]); float total1 = simd_sum(shmem[32u + lane]); if (lane == 0u && sgitg == 0u) { uint idx = r * p.out_dim + first_o; if (first_o < p.out_dim) output[idx] = total0; if (first_o + 1u < p.out_dim) output[idx + 1u] = total1; }\n"
           "}\n"
           "kernel void termite_q4_k_pair_linear(device const float *input [[buffer(0)]], device const uchar *weight_a [[buffer(1)]], device const uchar *weight_b [[buffer(2)]], device float *output_a [[buffer(3)]], device float *output_b [[buffer(4)]], constant termite_metal_linear_params &p [[buffer(5)]], uint2 tid [[thread_position_in_grid]]) {\n"
           "    uint o = tid.x; uint r = tid.y; if (o >= p.out_dim || r >= p.rows) return; uint row_offset = o * p.row_blocks * 144u; float acc_a = 0.0f; float acc_b = 0.0f;\n"
           "    for (uint b = 0; b < p.row_blocks; ++b) { uint off_a = row_offset + b * 144u; uint off_b = row_offset + b * 144u; ushort d_bits_a = (ushort(weight_a[off_a + 1]) << 8) | ushort(weight_a[off_a]); ushort dmin_bits_a = (ushort(weight_a[off_a + 3]) << 8) | ushort(weight_a[off_a + 2]); ushort d_bits_b = (ushort(weight_b[off_b + 1]) << 8) | ushort(weight_b[off_b]); ushort dmin_bits_b = (ushort(weight_b[off_b + 3]) << 8) | ushort(weight_b[off_b + 2]); float d_a = float(as_type<half>(d_bits_a)); float dmin_a = float(as_type<half>(dmin_bits_a)); float d_b = float(as_type<half>(d_bits_b)); float dmin_b = float(as_type<half>(dmin_bits_b)); const device uchar *scales_a = weight_a + off_a + 4u; const device uchar *scales_b = weight_b + off_b + 4u; const device uchar *qs_a = weight_a + off_a + 16u; const device uchar *qs_b = weight_b + off_b + 16u; thread float scs_a[8]; thread float mins_a[8]; thread float scs_b[8]; thread float mins_b[8]; unpack_q4k_scale_mins(scales_a, scs_a, mins_a); unpack_q4k_scale_mins(scales_b, scs_b, mins_b); uint q_off = 0u; for (uint chunk = 0; chunk < 4; ++chunk) { uint sub = chunk * 2u; float dsc0_a = d_a * scs_a[sub]; float dmn0_a = dmin_a * mins_a[sub]; float dsc1_a = d_a * scs_a[sub + 1u]; float dmn1_a = dmin_a * mins_a[sub + 1u]; float dsc0_b = d_b * scs_b[sub]; float dmn0_b = dmin_b * mins_b[sub]; float dsc1_b = d_b * scs_b[sub + 1u]; float dmn1_b = dmin_b * mins_b[sub + 1u]; uint input_off = r * p.in_dim + b * 256u + chunk * 64u; for (uint i = 0; i < 32; ++i) { uchar packed_a = qs_a[q_off + i]; uchar packed_b = qs_b[q_off + i]; float x0 = input[input_off + i]; float x1 = input[input_off + 32u + i]; acc_a += x0 * (dsc0_a * float(packed_a & 0x0Fu) - dmn0_a); acc_a += x1 * (dsc1_a * float(packed_a >> 4) - dmn1_a); acc_b += x0 * (dsc0_b * float(packed_b & 0x0Fu) - dmn0_b); acc_b += x1 * (dsc1_b * float(packed_b >> 4) - dmn1_b); } q_off += 32u; } }\n"
           "    output_a[r * p.out_dim + o] = acc_a;\n"
           "    output_b[r * p.out_dim + o] = acc_b;\n"
           "}\n"
           "kernel void termite_q5_k_linear(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.out_dim; if (gid >= total) return; uint r = gid / p.out_dim; uint o = gid % p.out_dim; uint row_offset = o * p.row_blocks * 176u; float acc = 0.0f;\n"
           "    for (uint b = 0; b < p.row_blocks; ++b) { uint off = row_offset + b * 176u; ushort d_bits = (ushort(weight[off + 1]) << 8) | ushort(weight[off]); ushort dmin_bits = (ushort(weight[off + 3]) << 8) | ushort(weight[off + 2]); float d = float(as_type<half>(d_bits)); float dmin = float(as_type<half>(dmin_bits)); const device uchar *scales = weight + off + 4u; const device uchar *qh = weight + off + 16u; const device uchar *ql = weight + off + 48u; thread float scs[8]; thread float mins[8]; unpack_q4k_scale_mins(scales, scs, mins); for (uint sub = 0; sub < 8; ++sub) { float dsc = d * scs[sub]; float dmn = dmin * mins[sub]; uint chunk = sub / 2u; bool is_high = (sub & 1u) != 0u; uint ql_off = chunk * 32u; uint hb_shift = sub; uint input_off = r * p.in_dim + b * 256u + sub * 32u; for (uint i = 0; i < 32; ++i) { uchar low = is_high ? (ql[ql_off + i] >> 4) : (ql[ql_off + i] & 0x0Fu); int q = int(low) + int((qh[i] >> hb_shift) & 1u) * 16; acc += input[input_off + i] * (dsc * float(q) - dmn); } } }\n"
           "    output[gid] = acc;\n"
           "}\n"
           "kernel void termite_q5_k_linear_1x_reduce(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], threadgroup float *shmem [[threadgroup(0)]], ushort lane [[thread_index_in_simdgroup]], ushort sgitg [[simdgroup_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) {\n"
           "    const uint NSG = 2u; const uint NQ = 8u; const uint NW = 32u; uint o = tg.x; uint r = tg.y; if (o >= p.out_dim || r >= p.rows || lane >= NW) return;\n"
           "    uint ix = lane / (NW / NQ); uint il = lane - ix * (NW / NQ); uint sb0 = sgitg * NQ + ix; uint in_row = r * p.in_dim; uint total_subblocks = p.row_blocks * 8u; float acc = 0.0f;\n"
           "    for (uint sb = sb0; sb < total_subblocks; sb += NSG * NQ) { uint b = sb >> 3; uint sub = sb & 7u; uint chunk = sub >> 1; bool high = (sub & 1u) != 0u; uint j0 = il * NQ; uint off = o * p.row_blocks * 176u + b * 176u; ushort d_bits = (ushort(weight[off + 1]) << 8) | ushort(weight[off]); ushort dmin_bits = (ushort(weight[off + 3]) << 8) | ushort(weight[off + 2]); float d = float(as_type<half>(d_bits)); float dmin = float(as_type<half>(dmin_bits)); const device uchar *scales = weight + off + 4u; const device uchar *qh = weight + off + 16u; const device uchar *ql = weight + off + 48u; thread float scs[8]; thread float mins[8]; unpack_q4k_scale_mins(scales, scs, mins); float dsc = d * scs[sub]; float dmn = dmin * mins[sub]; uint ql_off = chunk * 32u + j0; uint input_off = in_row + b * 256u + sub * 32u + j0; for (uint i = 0; i < NQ; ++i) { uchar low = high ? (ql[ql_off + i] >> 4) : (ql[ql_off + i] & 0x0Fu); int q = int(low) + int((qh[j0 + i] >> sub) & 1u) * 16; acc += input[input_off + i] * (dsc * float(q) - dmn); } }\n"
           "    if (sgitg == 0u) shmem[lane] = 0.0f; acc = simd_sum(acc); threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    if (lane == 0u) shmem[sgitg] = acc; threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    float total = simd_sum(shmem[lane]); if (lane == 0u && sgitg == 0u) output[r * p.out_dim + o] = total;\n"
           "}\n"
           "kernel void termite_q6_k_linear(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.out_dim; if (gid >= total) return; uint r = gid / p.out_dim; uint o = gid % p.out_dim; uint row_offset = o * p.row_blocks * 210u; float acc = 0.0f;\n"
           "    for (uint b = 0; b < p.row_blocks; ++b) { uint off = row_offset + b * 210u; const device uchar *ql = weight + off; const device uchar *qh = weight + off + 128u; const device char *scales = reinterpret_cast<const device char *>(weight + off + 192u); ushort d_bits = (ushort(weight[off + 209]) << 8) | ushort(weight[off + 208]); float d = float(as_type<half>(d_bits)); for (uint sub = 0; sub < 16; ++sub) { float scale = d * float(scales[sub]); uint half_idx = sub / 8u; uint group = (sub % 8u) / 2u; uint l_base = (sub % 2u) * 16u; uint ql_off = half_idx * 64u + (group & 1u) * 32u; uint qh_off = half_idx * 32u; uint qh_shift = group * 2u; uint nibble_shift = (group / 2u) * 4u; uint input_off = r * p.in_dim + b * 256u + sub * 16u; for (uint i = 0; i < 16; ++i) { uint l = l_base + i; int low4 = int((ql[ql_off + l] >> nibble_shift) & 0x0Fu); int high2 = int((qh[qh_off + l] >> qh_shift) & 0x03u); int q = (low4 | (high2 << 4)) - 32; acc += input[input_off + i] * (scale * float(q)); } } }\n"
           "    output[gid] = acc;\n"
           "}\n"
           "kernel void termite_q6_k_linear_1x_reduce(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], threadgroup float *shmem [[threadgroup(0)]], ushort lane [[thread_index_in_simdgroup]], ushort sgitg [[simdgroup_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) {\n"
           "    const uint NR0 = 2u; const uint NSG = 2u; const uint NW = 32u; uint first_o = tg.x * NR0; uint r = tg.y; if (r >= p.rows || lane >= NW) return;\n"
           "    uint ix = lane / 4u; uint il = lane - ix * 4u; uint sb0 = sgitg * 8u + ix; uint in_row = r * p.in_dim; uint total_subblocks = p.row_blocks * 16u; float acc0 = 0.0f; float acc1 = 0.0f;\n"
           "    for (uint sb = sb0; sb < total_subblocks; sb += NSG * 8u) { uint b = sb >> 4; uint sub = sb & 15u; uint j0 = il * 4u; uint half_idx = sub / 8u; uint group = (sub % 8u) / 2u; uint l_base = (sub % 2u) * 16u; uint ql_off = half_idx * 64u + (group & 1u) * 32u; uint qh_off = half_idx * 32u; uint qh_shift = group * 2u; uint nibble_shift = (group / 2u) * 4u; uint input_off = in_row + b * 256u + sub * 16u + j0; float x[4]; for (uint i = 0; i < 4u; ++i) { x[i] = input[input_off + i]; } for (uint row = 0; row < NR0; ++row) { uint o = first_o + row; if (o >= p.out_dim) continue; uint off = o * p.row_blocks * 210u + b * 210u; const device uchar *ql = weight + off; const device uchar *qh = weight + off + 128u; const device char *scales = reinterpret_cast<const device char *>(weight + off + 192u); ushort d_bits = (ushort(weight[off + 209]) << 8) | ushort(weight[off + 208]); float scale = float(as_type<half>(d_bits)) * float(scales[sub]); float block = 0.0f; for (uint i = 0; i < 4u; ++i) { uint l = l_base + j0 + i; int low4 = int((ql[ql_off + l] >> nibble_shift) & 0x0Fu); int high2 = int((qh[qh_off + l] >> qh_shift) & 0x03u); int q = (low4 | (high2 << 4)) - 32; block += x[i] * (scale * float(q)); } if (row == 0u) acc0 += block; else acc1 += block; } }\n"
           "    if (sgitg == 0u) { shmem[lane] = 0.0f; shmem[32u + lane] = 0.0f; } acc0 = simd_sum(acc0); acc1 = simd_sum(acc1); threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    if (lane == 0u) { shmem[sgitg] = acc0; shmem[32u + sgitg] = acc1; } threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    float total0 = simd_sum(shmem[lane]); float total1 = simd_sum(shmem[32u + lane]); if (lane == 0u && sgitg == 0u) { uint idx = r * p.out_dim + first_o; if (first_o < p.out_dim) output[idx] = total0; if (first_o + 1u < p.out_dim) output[idx + 1u] = total1; }\n"
           "}\n"
           "kernel void termite_q6_k_pair_linear(device const float *input [[buffer(0)]], device const uchar *weight_a [[buffer(1)]], device const uchar *weight_b [[buffer(2)]], device float *output_a [[buffer(3)]], device float *output_b [[buffer(4)]], constant termite_metal_linear_params &p [[buffer(5)]], uint2 tid [[thread_position_in_grid]]) {\n"
           "    uint o = tid.x; uint r = tid.y; if (o >= p.out_dim || r >= p.rows) return; uint row_offset = o * p.row_blocks * 210u; float acc_a = 0.0f; float acc_b = 0.0f;\n"
           "    for (uint b = 0; b < p.row_blocks; ++b) { uint off = row_offset + b * 210u; const device uchar *ql_a = weight_a + off; const device uchar *qh_a = weight_a + off + 128u; const device char *scales_a = reinterpret_cast<const device char *>(weight_a + off + 192u); const device uchar *ql_b = weight_b + off; const device uchar *qh_b = weight_b + off + 128u; const device char *scales_b = reinterpret_cast<const device char *>(weight_b + off + 192u); ushort d_bits_a = (ushort(weight_a[off + 209]) << 8) | ushort(weight_a[off + 208]); ushort d_bits_b = (ushort(weight_b[off + 209]) << 8) | ushort(weight_b[off + 208]); float d_a = float(as_type<half>(d_bits_a)); float d_b = float(as_type<half>(d_bits_b)); for (uint sub = 0; sub < 16; ++sub) { float scale_a = d_a * float(scales_a[sub]); float scale_b = d_b * float(scales_b[sub]); uint half_idx = sub / 8u; uint group = (sub % 8u) / 2u; uint l_base = (sub % 2u) * 16u; uint ql_off = half_idx * 64u + (group & 1u) * 32u; uint qh_off = half_idx * 32u; uint qh_shift = group * 2u; uint nibble_shift = (group / 2u) * 4u; uint input_off = r * p.in_dim + b * 256u + sub * 16u; for (uint i = 0; i < 16; ++i) { uint l = l_base + i; float x = input[input_off + i]; int low4_a = int((ql_a[ql_off + l] >> nibble_shift) & 0x0Fu); int high2_a = int((qh_a[qh_off + l] >> qh_shift) & 0x03u); int q_a = (low4_a | (high2_a << 4)) - 32; acc_a += x * (scale_a * float(q_a)); int low4_b = int((ql_b[ql_off + l] >> nibble_shift) & 0x0Fu); int high2_b = int((qh_b[qh_off + l] >> qh_shift) & 0x03u); int q_b = (low4_b | (high2_b << 4)) - 32; acc_b += x * (scale_b * float(q_b)); } } }\n"
           "    output_a[r * p.out_dim + o] = acc_a;\n"
           "    output_b[r * p.out_dim + o] = acc_b;\n"
           "}\n"
           "kernel void termite_iq4_nl_linear(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.out_dim; if (gid >= total) return; uint r = gid / p.out_dim; uint o = gid % p.out_dim; uint row_offset = o * p.row_blocks * 18u; float acc = 0.0f; constexpr int iq4_values[16] = {-127, -104, -83, -65, -49, -35, -22, -10, 1, 13, 25, 38, 53, 69, 89, 113};\n"
           "    for (uint b = 0; b < p.row_blocks; ++b) { uint off = row_offset + b * 18u; ushort bits = (ushort(weight[off + 1]) << 8) | ushort(weight[off]); float d = float(as_type<half>(bits)); const device uchar *qs = weight + off + 2u; uint in_off = r * p.in_dim + b * 32u; for (uint i = 0; i < 16u; ++i) { uchar packed = qs[i]; acc += input[in_off + i] * (d * float(iq4_values[packed & 0x0Fu])); } for (uint i = 0; i < 16u; ++i) { uchar packed = qs[i]; acc += input[in_off + 16u + i] * (d * float(iq4_values[(packed >> 4) & 0x0Fu])); } }\n"
           "    output[gid] = acc;\n"
           "}\n"
           "kernel void termite_iq4_nl_linear_1x_reduce(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], threadgroup float *shmem [[threadgroup(0)]], ushort lane [[thread_index_in_simdgroup]], ushort sgitg [[simdgroup_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) {\n"
           "    const uint NR0 = 2u; const uint NSG = 4u; const uint NQ = 8u; const uint NW = 32u; constexpr int iq4_values[16] = {-127, -104, -83, -65, -49, -35, -22, -10, 1, 13, 25, 38, 53, 69, 89, 113}; uint first_o = tg.x * NR0; uint r = tg.y; if (r >= p.rows || lane >= NW) return;\n"
           "    uint ix = lane / (NW / NQ); uint il = lane - ix * (NW / NQ); uint ib0 = uint(sgitg) * NQ + ix; uint in_row = r * p.in_dim; float acc0 = 0.0f; float acc1 = 0.0f;\n"
           "    for (uint b = ib0; b < p.row_blocks; b += NSG * NQ) { float x[8]; uint qj0 = il * NQ; uint in_off = in_row + b * 32u + qj0; for (uint i = 0u; i < NQ; ++i) { x[i] = input[in_off + i]; } for (uint row = 0u; row < NR0; ++row) { uint o = first_o + row; if (o >= p.out_dim) continue; uint off = o * p.row_blocks * 18u + b * 18u; ushort bits = (ushort(weight[off + 1u]) << 8) | ushort(weight[off]); float d = float(as_type<half>(bits)); const device uchar *qs = weight + off + 2u; float block = 0.0f; for (uint i = 0u; i < NQ; ++i) { uint j = qj0 + i; uchar packed = qs[j & 15u]; uint code = j < 16u ? uint(packed & 0x0Fu) : uint((packed >> 4) & 0x0Fu); block += x[i] * float(iq4_values[code]); } if (row == 0u) acc0 += d * block; else acc1 += d * block; } }\n"
           "    if (sgitg == 0u) { shmem[lane] = 0.0f; shmem[32u + lane] = 0.0f; } acc0 = simd_sum(acc0); acc1 = simd_sum(acc1); threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    if (lane == 0u) { shmem[sgitg] = acc0; shmem[32u + sgitg] = acc1; } threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    float total0 = simd_sum(shmem[lane]); float total1 = simd_sum(shmem[32u + lane]); if (lane == 0u && sgitg == 0u) { uint idx = r * p.out_dim + first_o; if (first_o < p.out_dim) output[idx] = total0; if (first_o + 1u < p.out_dim) output[idx + 1u] = total1; }\n"
           "}\n"
           "kernel void termite_iq4_xs_linear(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.out_dim; if (gid >= total) return; uint r = gid / p.out_dim; uint o = gid % p.out_dim; uint row_offset = o * p.row_blocks * 136u; float acc = 0.0f;\n"
           "    constexpr int iq4_nl[16] = {-127, -104, -83, -65, -49, -35, -22, -10, 1, 13, 25, 38, 53, 69, 89, 113};\n"
           "    for (uint b = 0; b < p.row_blocks; ++b) { uint off = row_offset + b * 136u; ushort d_bits = (ushort(weight[off + 1]) << 8) | ushort(weight[off]); float d = float(as_type<half>(d_bits)); ushort scales_h = (ushort(weight[off + 3]) << 8) | ushort(weight[off + 2]); const device uchar *scales_l = weight + off + 4u; const device uchar *qs = weight + off + 8u; for (uint sub = 0; sub < 8; ++sub) { uint low = (uint(scales_l[sub >> 1u]) >> ((sub & 1u) * 4u)) & 0x0Fu; uint high = (uint(scales_h) >> (sub * 2u)) & 0x03u; int ls = int(low | (high << 4)); float dl = d * float(ls - 32); uint q_base = sub * 16u; uint input_off = r * p.in_dim + b * 256u + sub * 32u; for (uint j = 0; j < 16u; ++j) { uchar byte = qs[q_base + j]; int lo = iq4_nl[byte & 0x0Fu]; int hi = iq4_nl[byte >> 4]; acc += input[input_off + j] * (dl * float(lo)); acc += input[input_off + 16u + j] * (dl * float(hi)); } } }\n"
           "    output[gid] = acc;\n"
           "}\n"
           "kernel void termite_iq4_xs_linear_1x_reduce(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], threadgroup float *shmem [[threadgroup(0)]], ushort lane [[thread_index_in_simdgroup]], ushort sgitg [[simdgroup_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) {\n"
           "    const uint NR0 = 2u; const uint NSG = 4u; const uint NQ = 8u; const uint NW = 32u; constexpr int iq4_nl[16] = {-127, -104, -83, -65, -49, -35, -22, -10, 1, 13, 25, 38, 53, 69, 89, 113}; uint first_o = tg.x * NR0; uint r = tg.y; if (r >= p.rows || lane >= NW) return;\n"
           "    uint ix = lane / (NW / NQ); uint il = lane - ix * (NW / NQ); uint sb0 = uint(sgitg) * 8u + ix; uint in_row = r * p.in_dim; uint total_subblocks = p.row_blocks * 8u; float acc0 = 0.0f; float acc1 = 0.0f;\n"
           "    for (uint sb = sb0; sb < total_subblocks; sb += NSG * 8u) { uint b = sb >> 3; uint sub = sb & 7u; uint qj0 = il * NQ; uint in_off = in_row + b * 256u + sub * 32u + qj0; float x[8]; for (uint i = 0u; i < NQ; ++i) { x[i] = input[in_off + i]; } for (uint row = 0u; row < NR0; ++row) { uint o = first_o + row; if (o >= p.out_dim) continue; uint off = o * p.row_blocks * 136u + b * 136u; ushort d_bits = (ushort(weight[off + 1u]) << 8) | ushort(weight[off]); float d = float(as_type<half>(d_bits)); ushort scales_h = (ushort(weight[off + 3u]) << 8) | ushort(weight[off + 2u]); const device uchar *scales_l = weight + off + 4u; const device uchar *qs = weight + off + 8u; uint low = (uint(scales_l[sub >> 1u]) >> ((sub & 1u) * 4u)) & 0x0Fu; uint high = (uint(scales_h) >> (sub * 2u)) & 0x03u; float dl = d * float(int(low | (high << 4)) - 32); uint q_base = sub * 16u; float block = 0.0f; for (uint i = 0u; i < NQ; ++i) { uint j = qj0 + i; uchar packed = qs[q_base + (j & 15u)]; uint code = j < 16u ? uint(packed & 0x0Fu) : uint(packed >> 4); block += x[i] * float(iq4_nl[code]); } if (row == 0u) acc0 += dl * block; else acc1 += dl * block; } }\n"
           "    if (sgitg == 0u) { shmem[lane] = 0.0f; shmem[32u + lane] = 0.0f; } acc0 = simd_sum(acc0); acc1 = simd_sum(acc1); threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    if (lane == 0u) { shmem[sgitg] = acc0; shmem[32u + sgitg] = acc1; } threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    float total0 = simd_sum(shmem[lane]); float total1 = simd_sum(shmem[32u + lane]); if (lane == 0u && sgitg == 0u) { uint idx = r * p.out_dim + first_o; if (first_o < p.out_dim) output[idx] = total0; if (first_o + 1u < p.out_dim) output[idx + 1u] = total1; }\n"
           "}\n"
           "kernel void termite_mxfp4_linear(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.out_dim; if (gid >= total) return; uint r = gid / p.out_dim; uint o = gid % p.out_dim; uint row_offset = o * p.row_blocks * 17u; float acc = 0.0f;\n"
           "    constexpr int mxfp4_lut[16] = {0, 1, 2, 3, 4, 6, 8, 12, 0, -1, -2, -3, -4, -6, -8, -12};\n"
           "    for (uint b = 0; b < p.row_blocks; ++b) { uint off = row_offset + b * 17u; uint scale_byte = uint(weight[off]); uint scale_bits = (scale_byte < 2u) ? (0x00200000u << scale_byte) : (uint(scale_byte - 1u) << 23); float d = as_type<float>(scale_bits); uint input_off = r * p.in_dim + b * 32u; const device uchar *qs = weight + off + 1u; for (uint j = 0; j < 16u; ++j) { uchar byte = qs[j]; int lo = mxfp4_lut[byte & 0x0Fu]; int hi = mxfp4_lut[byte >> 4]; acc += input[input_off + j] * (d * float(lo)); acc += input[input_off + 16u + j] * (d * float(hi)); } }\n"
           "    output[gid] = acc;\n"
           "}\n"
           "kernel void termite_mxfp4_linear_1x_reduce(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], threadgroup float *shmem [[threadgroup(0)]], ushort lane [[thread_index_in_simdgroup]], ushort sgitg [[simdgroup_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) {\n"
           "    const uint NR0 = 2u; const uint NSG = 4u; const uint NQ = 8u; const uint NW = 32u; constexpr int mxfp4_lut[16] = {0, 1, 2, 3, 4, 6, 8, 12, 0, -1, -2, -3, -4, -6, -8, -12}; uint first_o = tg.x * NR0; uint r = tg.y; if (r >= p.rows || lane >= NW) return;\n"
           "    uint ix = lane / (NW / NQ); uint il = lane - ix * (NW / NQ); uint ib0 = uint(sgitg) * NQ + ix; uint in_row = r * p.in_dim; float acc0 = 0.0f; float acc1 = 0.0f;\n"
           "    for (uint b = ib0; b < p.row_blocks; b += NSG * NQ) { float x[8]; uint qj0 = il * NQ; uint in_off = in_row + b * 32u + qj0; for (uint i = 0u; i < NQ; ++i) { x[i] = input[in_off + i]; } for (uint row = 0u; row < NR0; ++row) { uint o = first_o + row; if (o >= p.out_dim) continue; uint off = o * p.row_blocks * 17u + b * 17u; uint scale_byte = uint(weight[off]); uint scale_bits = (scale_byte < 2u) ? (0x00200000u << scale_byte) : (uint(scale_byte - 1u) << 23); float d = as_type<float>(scale_bits); const device uchar *qs = weight + off + 1u; float block = 0.0f; for (uint i = 0u; i < NQ; ++i) { uint j = qj0 + i; uchar packed = qs[j & 15u]; uint code = j < 16u ? uint(packed & 0x0Fu) : uint((packed >> 4) & 0x0Fu); block += x[i] * float(mxfp4_lut[code]); } if (row == 0u) acc0 += d * block; else acc1 += d * block; } }\n"
           "    if (sgitg == 0u) { shmem[lane] = 0.0f; shmem[32u + lane] = 0.0f; } acc0 = simd_sum(acc0); acc1 = simd_sum(acc1); threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    if (lane == 0u) { shmem[sgitg] = acc0; shmem[32u + sgitg] = acc1; } threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    float total0 = simd_sum(shmem[lane]); float total1 = simd_sum(shmem[32u + lane]); if (lane == 0u && sgitg == 0u) { uint idx = r * p.out_dim + first_o; if (first_o < p.out_dim) output[idx] = total0; if (first_o + 1u < p.out_dim) output[idx + 1u] = total1; }\n"
           "}\n"
           "inline float termite_nvfp4_scale(uchar value) { if (value == 0u || value == 0x7Fu) return 0.0f; uint e = (uint(value) >> 3) & 0x0Fu; float m = float(uint(value) & 0x07u); float raw = e == 0u ? m * exp2(-9.0f) : (1.0f + m / 8.0f) * exp2(float(int(e) - 7)); return raw * 0.5f; }\n"
           "kernel void termite_nvfp4_linear(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.out_dim; if (gid >= total) return; uint r = gid / p.out_dim; uint o = gid % p.out_dim; uint row_offset = o * p.row_blocks * 36u; float acc = 0.0f; constexpr int lut[16] = {0, 1, 2, 3, 4, 6, 8, 12, 0, -1, -2, -3, -4, -6, -8, -12};\n"
           "    for (uint b = 0; b < p.row_blocks; ++b) { uint off = row_offset + b * 36u; const device uchar *scales = weight + off; const device uchar *qs = weight + off + 4u; uint input_off = r * p.in_dim + b * 64u; for (uint sub = 0; sub < 4u; ++sub) { float d = termite_nvfp4_scale(scales[sub]); uint q_base = sub * 8u; uint x_base = input_off + sub * 16u; for (uint j = 0; j < 8u; ++j) { uchar packed = qs[q_base + j]; acc += input[x_base + j] * (d * float(lut[packed & 0x0Fu])); acc += input[x_base + 8u + j] * (d * float(lut[packed >> 4])); } } }\n"
           "    output[gid] = acc;\n"
           "}\n"
           "constant ushort termite_iq2_xs_grid[512] = {0,2,5,8,10,17,20,22,25,32,34,37,40,65,68,70,73,80,82,85,88,97,100,128,130,133,136,145,148,153,160,257,260,262,265,272,274,277,280,282,289,292,320,322,325,328,337,340,352,360,385,388,400,512,514,517,520,529,532,544,577,580,592,597,640,650,1025,1028,1030,1033,1040,1042,1045,1048,1057,1060,1088,1090,1093,1096,1105,1108,1110,1120,1153,1156,1168,1280,1282,1285,1288,1297,1300,1312,1345,1348,1360,1377,1408,1537,1540,1552,1574,1600,1602,1668,2048,2050,2053,2056,2058,2065,2068,2080,2085,2113,2116,2128,2136,2176,2208,2218,2305,2308,2320,2368,2433,2441,2560,2592,2600,2710,2720,4097,4100,4102,4105,4112,4114,4117,4120,4129,4132,4160,4162,4165,4168,4177,4180,4192,4202,4225,4228,4240,4352,4354,4357,4360,4369,4372,4384,4417,4420,4432,4480,4500,4502,4609,4612,4614,4624,4672,4704,5120,5122,5125,5128,5137,5140,5152,5185,5188,5193,5200,5220,5248,5377,5380,5392,5440,5632,5652,5705,6145,6148,6160,6162,6208,6228,6278,6400,6405,6502,6737,6825,8192,8194,8197,8200,8202,8209,8212,8224,8257,8260,8272,8320,8352,8449,8452,8464,8512,8520,8549,8704,8738,8832,8872,9217,9220,9232,9257,9280,9472,9537,9554,9625,9729,9754,9894,10240,10248,10250,10272,10325,10376,10402,10600,10640,10760,10784,10882,10888,10890,16385,16388,16390,16393,16400,16402,16405,16408,16417,16420,16448,16450,16453,16456,16458,16465,16468,16480,16485,16513,16516,16528,16640,16642,16645,16648,16657,16660,16672,16705,16708,16720,16768,16773,16802,16897,16900,16912,16914,16937,16960,17408,17410,17413,17416,17425,17428,17433,17440,17473,17476,17488,17536,17556,17665,17668,17680,17700,17728,17818,17920,17930,17988,18000,18433,18436,18448,18496,18501,18516,18530,18688,18705,18756,18768,18793,18948,20480,20482,20485,20488,20497,20500,20512,20520,20545,20548,20560,20608,20737,20740,20752,20757,20800,20802,20992,21060,21162,21505,21508,21520,21537,21568,21600,21633,21665,21760,21768,21888,21896,22049,22120,22177,22528,22548,22593,22608,22681,22810,22848,22850,23173,24577,24580,24592,24640,24660,24674,24710,24745,24832,25124,25162,25234,25600,25622,25872,25920,25925,26020,26625,26730,26917,27142,27220,27234,32768,32770,32773,32776,32785,32788,32800,32810,32833,32836,32848,32896,32898,32936,32938,33025,33028,33030,33040,33088,33105,33113,33280,33312,33408,33410,33440,33448,33793,33796,33808,33810,33813,33856,33888,33929,34048,34116,34213,34328,34410,34816,34824,34853,34906,34944,34946,34984,35078,35362,35456,35464,35478,35496,36865,36868,36880,36928,36950,36996,37120,37154,37220,37462,37513,37888,37893,37956,37968,37976,38185,38288,38290,38465,38993,39078,39241,39445,39520,40960,40962,40968,40970,40992,41002,41120,41297,41305,41382,41472,41474,41480,41514,41600,41632,42048,42133,42597,42648,43018,43040,43042,43048,43168,43176,43268,43396,43398,43560,43562,43665,43690};\n"
           "inline uchar termite_iq2_sign_mask(uint sign_index) { uchar low = uchar(sign_index & 0x7Fu); uint x = sign_index & 0x7Fu; uint parity = 0u; for (uint i = 0u; i < 7u; ++i) parity ^= (x >> i) & 1u; return uchar(low | (parity << 7)); }\n"
           "kernel void termite_iq2_xs_linear(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_linear_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.out_dim; if (gid >= total) return; uint r = gid / p.out_dim; uint o = gid % p.out_dim; uint row_offset = o * p.row_blocks * 74u; float acc = 0.0f;\n"
           "    for (uint b = 0; b < p.row_blocks; ++b) { uint off = row_offset + b * 74u; ushort d_bits = (ushort(weight[off + 1u]) << 8) | ushort(weight[off]); float d = float(as_type<half>(d_bits)); const device uchar *qs = weight + off + 2u; const device uchar *scales = weight + off + 66u; uint input_off = r * p.in_dim + b * 256u; for (uint ib32 = 0u; ib32 < 8u; ++ib32) { float db0 = d * (0.5f + float(scales[ib32] & 0x0Fu)) * 0.25f; float db1 = d * (0.5f + float(scales[ib32] >> 4)) * 0.25f; for (uint lane = 0u; lane < 4u; ++lane) { uint qoff = (4u * ib32 + lane) * 2u; ushort packed = ushort(qs[qoff]) | (ushort(qs[qoff + 1u]) << 8); ushort grid = termite_iq2_xs_grid[packed & 0x01FFu]; uchar signs = termite_iq2_sign_mask(uint(packed >> 9)); float dl = lane < 2u ? db0 : db1; uint base = input_off + ib32 * 32u + lane * 8u; for (uint j = 0u; j < 8u; ++j) { float raw = float(2u * ((uint(grid) >> (2u * j)) & 0x03u) + 1u); float sign = ((uint(signs) >> j) & 1u) != 0u ? -1.0f : 1.0f; acc += input[base + j] * (dl * raw * sign); } } } }\n"
           "    output[gid] = acc;\n"
           "}\n"
           "kernel void termite_tl1_linear(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_tl1_params &p [[buffer(3)]], uint2 tid [[thread_position_in_grid]]) {\n"
           "    uint o = tid.x; uint r = tid.y; if (o >= p.out_dim || r >= p.rows) return;\n"
           "    uint input_base = r * p.in_dim; float abs_max = 0.0f;\n"
           "    for (uint i = 0; i < p.in_dim; ++i) abs_max = fmax(abs_max, fabs(input[input_base + i]));\n"
           "    float act_scale = abs_max == 0.0f ? 1.0f : abs_max / 127.0f;\n"
           "    uint scale_bits = uint(weight[p.packed_len]) | (uint(weight[p.packed_len + 1u]) << 8) | (uint(weight[p.packed_len + 2u]) << 16) | (uint(weight[p.packed_len + 3u]) << 24);\n"
           "    float weight_scale = as_type<float>(scale_bits); float acc = 0.0f;\n"
           "    uint row_outer = o / p.bm; uint row_in_bm = o % p.bm; uint bm_block = row_in_bm / p.bmm; uint row_in_bmm = row_in_bm % p.bmm; uint tl_by = 256u / p.bmm; uint row16 = row_in_bmm % 16u; uint bmm16 = row_in_bmm / 16u; uint col_blocks = p.in_dim / p.cfg_by; uint pair_count = p.in_dim / 2u;\n"
           "    for (uint pair_col = 0; pair_col < pair_count; ++pair_col) { uint col_block = pair_col / (p.cfg_by / 2u); uint col_in_block = pair_col % (p.cfg_by / 2u); uint by_block = col_in_block / (tl_by / 2u); uint pair_in_by = col_in_block % (tl_by / 2u); uint by4 = pair_in_by / 2u; uint nibble = pair_in_by % 2u; uint index = ((((((row_outer * col_blocks + col_block) * (p.bm / p.bmm) + bm_block) * (p.cfg_by / tl_by) + by_block) * (p.bmm / 16u) + bmm16) * (tl_by / 4u) + by4) * 16u + row16); uchar packed = weight[index]; uint code = nibble == 0u ? uint(packed >> 4) : uint(packed & 0x0Fu); float w0 = floor(float(code) / 3.0f) - 1.0f; float w1 = float(code % 3u) - 1.0f; uint col = pair_col * 2u; float a0 = clamp(round(input[input_base + col] / act_scale), -127.0f, 127.0f) * act_scale; float a1 = clamp(round(input[input_base + col + 1u] / act_scale), -127.0f, 127.0f) * act_scale; acc += a0 * w0 + a1 * w1; }\n"
           "    output[r * p.out_dim + o] = acc * weight_scale;\n"
           "}\n"
           "kernel void termite_tl2_linear(device const float *input [[buffer(0)]], device const uchar *weight [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_tl2_params &p [[buffer(3)]], uint2 tid [[thread_position_in_grid]]) {\n"
           "    uint o = tid.x; uint r = tid.y; if (o >= p.out_dim || r >= p.rows) return;\n"
           "    uint input_base = r * p.in_dim; float abs_max = 0.0f;\n"
           "    for (uint i = 0; i < p.in_dim; ++i) abs_max = fmax(abs_max, fabs(input[input_base + i]));\n"
           "    float act_scale = abs_max == 0.0f ? 1.0f : abs_max / 127.0f;\n"
           "    uint scale_bits = uint(weight[p.scale_off]) | (uint(weight[p.scale_off + 1u]) << 8) | (uint(weight[p.scale_off + 2u]) << 16) | (uint(weight[p.scale_off + 3u]) << 24);\n"
           "    float weight_scale = as_type<float>(scale_bits); float acc = 0.0f;\n"
           "    uint row_outer = o / p.bm; uint row_in_bm = o % p.bm; uint bm_block = row_in_bm / p.bmm; uint row_in_bmm = row_in_bm % p.bmm; uint row_group = row_in_bmm / 8u; uint row_offset = row_in_bmm % 8u; uint final_group = row_group == 1u ? 2u : (row_group == 2u ? 1u : row_group); uint three_by = 192u / p.bmm; uint col_blocks = p.in_dim / p.cfg_by; uint three_count = p.three_cols / 3u;\n"
           "    for (uint triple_col = 0; triple_col < three_count; ++triple_col) { uint col_block = triple_col / (p.cfg_by / 3u); uint col_in_block = triple_col % (p.cfg_by / 3u); uint by_block = col_in_block / (three_by / 3u); uint triple_in_by = col_in_block % (three_by / 3u); uint value_index = ((((row_outer * col_blocks + col_block) * (p.bm / p.bmm) + bm_block) * (p.cfg_by / three_by) + by_block) * p.bmm + final_group * 8u + row_offset); uchar packed = weight[value_index]; uint code = triple_in_by == 0u ? uint(packed >> 4) : uint(packed & 0x0Fu); uint sub_count = (three_by / 3u) * 4u; uint sign_block = col_in_block / sub_count; uint sign_sub = col_in_block % sub_count; uint line = sign_sub * p.bmm + row_in_bmm; uint bit_lane = line / (p.bmm / 2u); uint half_idx = line % (p.bmm / 2u); uint bit_index = 15u - bit_lane; uint sign_index = ((((row_outer * col_blocks + col_block) * (p.bm / p.bmm) + bm_block) * (p.cfg_by / (three_by * 4u)) + sign_block) * p.bmm + half_idx * 2u + bit_index / 8u); uchar sign_byte = weight[p.three_value_len + sign_index]; bool negative = ((sign_byte >> (bit_index % 8u)) & 1u) != 0u; float w0 = 0.0f; float w1 = 0.0f; float w2 = 0.0f; switch (code) { case 1u: w2 = 1.0f; break; case 2u: w1 = 1.0f; w2 = -1.0f; break; case 3u: w1 = 1.0f; break; case 4u: w1 = 1.0f; w2 = 1.0f; break; case 5u: w0 = 1.0f; w1 = -1.0f; w2 = -1.0f; break; case 6u: w0 = 1.0f; w1 = -1.0f; break; case 7u: w0 = 1.0f; w1 = -1.0f; w2 = 1.0f; break; case 8u: w0 = 1.0f; w2 = -1.0f; break; case 9u: w0 = 1.0f; break; case 10u: w0 = 1.0f; w2 = 1.0f; break; case 11u: w0 = 1.0f; w1 = 1.0f; w2 = -1.0f; break; case 12u: w0 = 1.0f; w1 = 1.0f; break; case 13u: w0 = 1.0f; w1 = 1.0f; w2 = 1.0f; break; default: break; } if (negative) { w0 = -w0; w1 = -w1; w2 = -w2; } uint col = triple_col * 3u; float a0 = clamp(round(input[input_base + col] / act_scale), -127.0f, 127.0f) * act_scale; float a1 = clamp(round(input[input_base + col + 1u] / act_scale), -127.0f, 127.0f) * act_scale; float a2 = clamp(round(input[input_base + col + 2u] / act_scale), -127.0f, 127.0f) * act_scale; acc += a0 * w0 + a1 * w1 + a2 * w2; }\n"
           "    if (p.two_cols != 0u) { uint tail_base = p.three_value_len + p.three_sign_len; uint tail_pair_count = p.two_cols / 2u; uint tail_row_outer = o / 32u; uint tail_row_in_bm = o % 32u; uint tail_row_group = tail_row_in_bm / 8u; uint tail_row_offset = tail_row_in_bm % 8u; uint tail_final_group = tail_row_group == 1u ? 2u : (tail_row_group == 2u ? 1u : tail_row_group); for (uint pair_col = 0; pair_col < tail_pair_count; ++pair_col) { uint col_block = pair_col / 16u; uint col_in_block = pair_col % 16u; uint by_block = col_in_block / 2u; uint pair_in_by = col_in_block % 2u; uint tail_col_blocks = p.two_cols / 32u; uint index = (((tail_row_outer * tail_col_blocks + col_block) * 8u + by_block) * 32u + tail_final_group * 8u + tail_row_offset); uchar packed = weight[tail_base + index]; uint code = pair_in_by == 0u ? uint(packed >> 4) : uint(packed & 0x0Fu); float w0 = floor(float(code) / 3.0f) - 1.0f; float w1 = float(code % 3u) - 1.0f; uint col = p.three_cols + pair_col * 2u; float a0 = clamp(round(input[input_base + col] / act_scale), -127.0f, 127.0f) * act_scale; float a1 = clamp(round(input[input_base + col + 1u] / act_scale), -127.0f, 127.0f) * act_scale; acc += a0 * w0 + a1 * w1; } }\n"
           "    output[r * p.out_dim + o] = acc * weight_scale;\n"
           "}\n"
           "kernel void termite_polar4_key_scores(device const float *q [[buffer(0)]], device const uchar *encoded_key [[buffer(1)]], device float *scores [[buffer(2)]], constant termite_metal_key_scores_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.num_heads * p.q_len * p.block_tokens; if (gid >= total) return; uint ki = gid % p.block_tokens; uint tmp = gid / p.block_tokens; uint qi = tmp % p.q_len; uint h = tmp / p.q_len; uint heads_per_group = p.num_heads / p.num_kv_heads; uint kv_h = h / heads_per_group; uint kv_head_off = kv_h * p.head_dim; uint q_base = qi * p.num_heads * p.head_dim + h * p.head_dim; uint k_base = ki * p.key_row_bytes; float acc = 0.0f;\n"
           "    for (uint d = 0; d < p.head_dim; ++d) { uint value_index = kv_head_off + d; uchar packed = encoded_key[k_base + (value_index >> 1)]; uint code = ((value_index & 1u) == 0u) ? uint(packed & 0x0Fu) : uint((packed >> 4) & 0x0Fu); float k = float(code) / 7.5f - 1.0f; acc += q[q_base + d] * k; }\n"
           "    scores[h * p.q_len * p.block_tokens + qi * p.block_tokens + ki] = acc;\n"
           "}\n"
           "kernel void termite_turbo3_key_scores(device const float *q [[buffer(0)]], device const uchar *encoded_key [[buffer(1)]], device float *scores [[buffer(2)]], constant termite_metal_key_scores_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.num_heads * p.q_len * p.block_tokens; if (gid >= total) return; uint ki = gid % p.block_tokens; uint tmp = gid / p.block_tokens; uint qi = tmp % p.q_len; uint h = tmp / p.q_len; uint heads_per_group = p.num_heads / p.num_kv_heads; uint kv_h = h / heads_per_group; uint kv_head_off = kv_h * p.head_dim; uint q_base = qi * p.num_heads * p.head_dim + h * p.head_dim; uint k_base = ki * p.key_row_bytes; uint base_key_row_bytes = (p.num_kv_heads * p.head_dim * 3u + 7u) / 8u; uint residual_base = k_base + base_key_row_bytes; thread float residual_projection[32];\n"
           "    for (uint projection = 0; projection < 32u; ++projection) { float projected_query = 0.0f; for (uint d = 0; d < p.head_dim; ++d) { ulong x = (ulong(kv_h + 1u) * 0x9e3779b97f4a7c15UL); x ^= (ulong(projection + 1u) * 0xbf58476d1ce4e5b9UL); x ^= (ulong(d + 1u) * 0x94d049bb133111ebUL); x ^= x >> 30; x *= 0xbf58476d1ce4e5b9UL; x ^= x >> 27; x *= 0x94d049bb133111ebUL; x ^= x >> 31; float sign = ((x & 1UL) == 0UL) ? 1.0f : -1.0f; projected_query += sign * q[q_base + d]; } residual_projection[projection] = projected_query; }\n"
           "    float acc = 0.0f; for (uint d = 0; d < p.head_dim; ++d) { uint value_index = kv_head_off + d; uint bit_offset = value_index * 3u; uint byte_index = k_base + (bit_offset >> 3); uint shift = bit_offset & 7u; uint bits = uint(encoded_key[byte_index]) >> shift; if (shift > 5u) bits |= uint(encoded_key[byte_index + 1u]) << (8u - shift); float k = float(bits & 0x07u) / 3.5f - 1.0f; acc += q[q_base + d] * k; }\n"
           "    float residual_acc = 0.0f; for (uint projection = 0; projection < 32u; ++projection) { uint residual_bit = kv_h * 32u + projection; uchar residual_byte = encoded_key[residual_base + (residual_bit >> 3)]; float residual_sign = ((residual_byte >> (residual_bit & 7u)) & 1u) != 0u ? 1.0f : -1.0f; residual_acc += residual_sign * residual_projection[projection]; }\n"
           "    scores[h * p.q_len * p.block_tokens + qi * p.block_tokens + ki] = acc + 0.125f * (residual_acc / 32.0f);\n"
           "}\n"
           "inline uint termite_encode_polar4_scalar(float value) { float clipped = clamp(value, -1.0f, 1.0f); return uint(clamp(round((clipped + 1.0f) * 7.5f), 0.0f, 15.0f)); }\n"
           "inline uint termite_encode_turbo3_scalar(float value) { float clipped = clamp(value, -1.0f, 1.0f); return uint(clamp(round((clipped + 1.0f) * 3.5f), 0.0f, 7.0f)); }\n"
           "inline float termite_decode_turbo3_scalar(uint code) { return float(code & 0x07u) / 3.5f - 1.0f; }\n"
           "inline float termite_random_sign(uint kv_h, uint projection, uint d) { ulong x = (ulong(kv_h + 1u) * 0x9e3779b97f4a7c15UL); x ^= (ulong(projection + 1u) * 0xbf58476d1ce4e5b9UL); x ^= (ulong(d + 1u) * 0x94d049bb133111ebUL); x ^= x >> 30; x *= 0xbf58476d1ce4e5b9UL; x ^= x >> 27; x *= 0x94d049bb133111ebUL; x ^= x >> 31; return ((x & 1UL) == 0UL) ? 1.0f : -1.0f; }\n"
           "kernel void termite_encode_polar4_key(device const float *k [[buffer(0)]], device uchar *encoded_key [[buffer(1)]], constant termite_metal_encode_key_params &p [[buffer(2)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint row_bytes = p.base_key_row_bytes; uint total = p.rows * row_bytes; if (gid >= total) return; uint row = gid / row_bytes; uint byte_in_row = gid - row * row_bytes; uint row_values = p.num_kv_heads * p.head_dim; uint value0 = byte_in_row * 2u; uint k_base = row * row_values; uint lo = value0 < row_values ? termite_encode_polar4_scalar(k[k_base + value0]) : 0u; uint hi = (value0 + 1u) < row_values ? termite_encode_polar4_scalar(k[k_base + value0 + 1u]) : 0u; encoded_key[row * p.key_row_bytes + byte_in_row] = uchar(lo | (hi << 4));\n"
           "}\n"
           "kernel void termite_encode_turbo3_key(device const float *k [[buffer(0)]], device uchar *encoded_key [[buffer(1)]], constant termite_metal_encode_key_params &p [[buffer(2)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint base_bytes = p.base_key_row_bytes; uint residual_bytes = (p.num_kv_heads * 32u + 7u) / 8u; uint row_work = base_bytes + residual_bytes; uint total = p.rows * row_work; if (gid >= total) return; uint row = gid / row_work; uint local = gid - row * row_work; uint row_values = p.num_kv_heads * p.head_dim; uint k_base = row * row_values; uint encoded_base = row * p.key_row_bytes;\n"
           "    if (local < base_bytes) { uint out = 0u; uint bit_base = local * 8u; for (uint bit = 0; bit < 8u; ++bit) { uint packed_bit = bit_base + bit; uint value_index = packed_bit / 3u; if (value_index >= row_values) continue; uint code_bit = packed_bit - value_index * 3u; uint code = termite_encode_turbo3_scalar(k[k_base + value_index]); out |= ((code >> code_bit) & 1u) << bit; } encoded_key[encoded_base + local] = uchar(out); return; }\n"
           "    uint residual_byte = local - base_bytes; uint out = 0u; for (uint bit = 0; bit < 8u; ++bit) { uint residual_bit = residual_byte * 8u + bit; if (residual_bit >= p.num_kv_heads * 32u) continue; uint kv_h = residual_bit / 32u; uint projection = residual_bit - kv_h * 32u; uint head_base = kv_h * p.head_dim; float projected_residual = 0.0f; for (uint d = 0; d < p.head_dim; ++d) { float value = k[k_base + head_base + d]; uint code = termite_encode_turbo3_scalar(value); float decoded = termite_decode_turbo3_scalar(code); projected_residual += termite_random_sign(kv_h, projection, d) * (value - decoded); } if (projected_residual >= 0.0f) out |= 1u << bit; } encoded_key[encoded_base + base_bytes + residual_byte] = uchar(out);\n"
           "}\n"
           "kernel void termite_encode_f16_convert(device const float *in [[buffer(0)]], device half *out [[buffer(1)]], constant termite_metal_encode_key_params &p [[buffer(2)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.num_kv_heads * p.head_dim; if (gid >= total) return; out[gid] = half(in[gid]);\n"
           "}\n"
           "kernel void termite_paged_f32_kv_seed(device const float *src_k [[buffer(0)]], device const float *src_v [[buffer(1)]], device float *dst_k [[buffer(2)]], device float *dst_v [[buffer(3)]], device const uint *block_table [[buffer(4)]], constant termite_metal_paged_f32_kv_seed_params &p [[buffer(5)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint values = p.values_per_row; uint width = max(values, p.v_row_stride); if (values == 0u || width == 0u) return; uint row = gid / width; uint col = gid - row * width; if (row >= p.suffix_tokens) return;\n"
           "    uint logical = p.total_tokens - p.suffix_tokens + row; uint logical_block = logical / p.page_size; uint token_in_block = logical - logical_block * p.page_size; uint physical = block_table[logical_block] + token_in_block;\n"
           "    if (col < values) dst_k[physical * p.key_row_floats + col] = src_k[row * values + col];\n"
           "    if (col < p.v_row_stride) dst_v[physical * p.v_row_stride + col] = src_v[row * p.v_row_stride + col];\n"
           "}\n"
           "kernel void termite_paged_f16_kv_seed(device const float *src_k [[buffer(0)]], device const float *src_v [[buffer(1)]], device half *dst_k [[buffer(2)]], device half *dst_v [[buffer(3)]], device const uint *block_table [[buffer(4)]], constant termite_metal_paged_f32_kv_seed_params &p [[buffer(5)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint values = p.values_per_row; uint width = max(values, p.v_row_stride); if (values == 0u || width == 0u) return; uint row = gid / width; uint col = gid - row * width; if (row >= p.suffix_tokens) return;\n"
           "    uint logical = p.total_tokens - p.suffix_tokens + row; uint logical_block = logical / p.page_size; uint token_in_block = logical - logical_block * p.page_size; uint physical = block_table[logical_block] + token_in_block;\n"
           "    if (col < values) dst_k[physical * p.key_row_floats + col] = half(src_k[row * values + col]);\n"
           "    if (col < p.v_row_stride) dst_v[physical * p.v_row_stride + col] = half(src_v[row * p.v_row_stride + col]);\n"
           "}\n"
           "kernel void termite_paged_f32_v_seed(device const float *src_v [[buffer(0)]], device float *dst_v [[buffer(1)]], device const uint *block_table [[buffer(2)]], constant termite_metal_paged_f32_kv_seed_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint values = p.v_row_stride; if (values == 0u) return; uint row = gid / values; uint col = gid - row * values; if (row >= p.suffix_tokens) return;\n"
           "    uint logical = p.total_tokens - p.suffix_tokens + row; uint logical_block = logical / p.page_size; uint token_in_block = logical - logical_block * p.page_size; uint physical = block_table[logical_block] + token_in_block;\n"
           "    dst_v[physical * p.v_row_stride + col] = src_v[row * p.v_row_stride + col];\n"
           "}\n"
           "kernel void termite_slice_last_dim_f32_2d(device const float *src [[buffer(0)]], device float *dst [[buffer(1)]], constant termite_metal_slice_last_dim_f32_2d_params &p [[buffer(2)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint out_cols = p.out_cols; if (out_cols == 0u) return; uint row = gid / out_cols; uint col = gid - row * out_cols; if (row >= p.rows) return;\n"
           "    dst[row * out_cols + col] = src[row * p.cols + p.start + col];\n"
           "}\n"
           "kernel void termite_gather_axis0_f32_2d(device const float *input [[buffer(0)]], device const float *indices [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_gather_axis0_f32_2d_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.index_count * p.cols; if (gid >= total || p.rows == 0u || p.cols == 0u) return;\n"
           "    uint out_row = gid / p.cols; uint col = gid - out_row * p.cols;\n"
           "    int row = int(indices[out_row]); if (row < 0) row += int(p.rows);\n"
           "    output[gid] = (row >= 0 && uint(row) < p.rows) ? input[uint(row) * p.cols + col] : 0.0f;\n"
           "}\n"
           "kernel void termite_concat_lastdim_f32_2d(device const float *a [[buffer(0)]], device const float *b [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_concat_lastdim_f32_2d_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint out_dim = p.dim_a + p.dim_b; uint total = p.rows * out_dim; if (gid >= total || out_dim == 0u) return;\n"
           "    uint row = gid / out_dim; uint col = gid - row * out_dim;\n"
           "    output[gid] = col < p.dim_a ? a[row * p.dim_a + col] : b[row * p.dim_b + (col - p.dim_a)];\n"
           "}\n"
           "kernel void termite_dense_qkv_split_f32_2d(device const float *packed [[buffer(0)]], device float *q_out [[buffer(1)]], device float *k_out [[buffer(2)]], device float *v_out [[buffer(3)]], constant termite_metal_dense_qkv_split_bias_params &p [[buffer(4)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.total_out_dim; if (gid >= total || p.total_out_dim == 0u) return; uint row = gid / p.total_out_dim; uint col = gid - row * p.total_out_dim; float value = packed[gid];\n"
           "    if (col < p.q_out_dim) { q_out[row * p.q_out_dim + col] = value; return; }\n"
           "    uint k_col = col - p.q_out_dim; if (k_col < p.kv_out_dim) { k_out[row * p.kv_out_dim + k_col] = value; return; }\n"
           "    uint v_col = k_col - p.kv_out_dim; if (v_col < p.kv_out_dim) v_out[row * p.kv_out_dim + v_col] = value;\n"
           "}\n"
           "kernel void termite_dense_qkv_split_bias_f32_2d(device const float *packed [[buffer(0)]], device const float *q_bias [[buffer(1)]], device const float *k_bias [[buffer(2)]], device const float *v_bias [[buffer(3)]], device float *q_out [[buffer(4)]], device float *k_out [[buffer(5)]], device float *v_out [[buffer(6)]], constant termite_metal_dense_qkv_split_bias_params &p [[buffer(7)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.total_out_dim; if (gid >= total || p.total_out_dim == 0u) return; uint row = gid / p.total_out_dim; uint col = gid - row * p.total_out_dim; float value = packed[gid];\n"
           "    if (col < p.q_out_dim) { q_out[row * p.q_out_dim + col] = value + q_bias[col]; return; }\n"
           "    uint k_col = col - p.q_out_dim; if (k_col < p.kv_out_dim) { k_out[row * p.kv_out_dim + k_col] = value + k_bias[k_col]; return; }\n"
           "    uint v_col = k_col - p.kv_out_dim; if (v_col < p.kv_out_dim) v_out[row * p.kv_out_dim + v_col] = value + v_bias[v_col];\n"
           "}\n"
           "kernel void termite_dense_pair_split_f32_2d(device const float *packed [[buffer(0)]], device float *a_out [[buffer(1)]], device float *b_out [[buffer(2)]], constant termite_metal_dense_pair_split_bias_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.total_out_dim; if (gid >= total || p.total_out_dim == 0u) return; uint row = gid / p.total_out_dim; uint col = gid - row * p.total_out_dim; float value = packed[gid];\n"
           "    if (col < p.out_dim) { a_out[row * p.out_dim + col] = value; return; }\n"
           "    uint b_col = col - p.out_dim; if (b_col < p.out_dim) b_out[row * p.out_dim + b_col] = value;\n"
           "}\n"
           "kernel void termite_dense_pair_split_bias_f32_2d(device const float *packed [[buffer(0)]], device const float *a_bias [[buffer(1)]], device const float *b_bias [[buffer(2)]], device float *a_out [[buffer(3)]], device float *b_out [[buffer(4)]], constant termite_metal_dense_pair_split_bias_params &p [[buffer(5)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.rows * p.total_out_dim; if (gid >= total || p.total_out_dim == 0u) return; uint row = gid / p.total_out_dim; uint col = gid - row * p.total_out_dim; float value = packed[gid];\n"
           "    if (col < p.out_dim) { a_out[row * p.out_dim + col] = value + a_bias[col]; return; }\n"
           "    uint b_col = col - p.out_dim; if (b_col < p.out_dim) b_out[row * p.out_dim + b_col] = value + b_bias[b_col];\n"
           "}\n"
           "kernel void termite_dense_qkv_direct_f32_2d(device const float *input [[buffer(0)]], device const float *q_weight [[buffer(1)]], device const float *k_weight [[buffer(2)]], device const float *v_weight [[buffer(3)]], device const float *q_bias [[buffer(4)]], device const float *k_bias [[buffer(5)]], device const float *v_bias [[buffer(6)]], device float *q_out [[buffer(7)]], device float *k_out [[buffer(8)]], device float *v_out [[buffer(9)]], constant termite_metal_dense_qkv_direct_params &p [[buffer(10)]], threadgroup float *partials [[threadgroup(0)]], uint tid [[thread_index_in_threadgroup]], uint2 tg [[threadgroup_position_in_grid]]) {\n"
           "    uint total_out_dim = p.q_out_dim + p.kv_out_dim + p.kv_out_dim; uint col = tg.x; uint row = tg.y; if (row >= p.rows || col >= total_out_dim || p.in_dim == 0u) return;\n"
           "    device const float *weight = q_weight; device const float *bias = q_bias; device float *output = q_out; uint out_dim = p.q_out_dim; uint out_col = col;\n"
           "    if (col >= p.q_out_dim) { uint k_col = col - p.q_out_dim; if (k_col < p.kv_out_dim) { weight = k_weight; bias = k_bias; output = k_out; out_dim = p.kv_out_dim; out_col = k_col; } else { uint v_col = k_col - p.kv_out_dim; weight = v_weight; bias = v_bias; output = v_out; out_dim = p.kv_out_dim; out_col = v_col; } }\n"
           "    float sum = 0.0f; for (uint k = tid; k < p.in_dim; k += 32u) sum += input[row * p.in_dim + k] * weight[out_col * p.in_dim + k];\n"
           "    partials[tid] = sum; threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    for (uint stride = 16u; stride > 0u; stride >>= 1u) { if (tid < stride) partials[tid] += partials[tid + stride]; threadgroup_barrier(mem_flags::mem_threadgroup); }\n"
           "    if (tid == 0u) output[row * out_dim + out_col] = partials[0] + bias[out_col];\n"
           "}\n"
           "kernel void termite_dense_pair_direct_f32_2d(device const float *input [[buffer(0)]], device const float *a_weight [[buffer(1)]], device const float *b_weight [[buffer(2)]], device const float *a_bias [[buffer(3)]], device const float *b_bias [[buffer(4)]], device float *a_out [[buffer(5)]], device float *b_out [[buffer(6)]], constant termite_metal_dense_pair_direct_params &p [[buffer(7)]], threadgroup float *partials [[threadgroup(0)]], uint tid [[thread_index_in_threadgroup]], uint2 tg [[threadgroup_position_in_grid]]) {\n"
           "    uint total_out_dim = p.out_dim + p.out_dim; uint col = tg.x; uint row = tg.y; if (row >= p.rows || col >= total_out_dim || p.in_dim == 0u) return;\n"
           "    bool second = col >= p.out_dim; uint out_col = second ? col - p.out_dim : col; device const float *weight = second ? b_weight : a_weight; device const float *bias = second ? b_bias : a_bias; device float *output = second ? b_out : a_out;\n"
           "    float sum = 0.0f; for (uint k = tid; k < p.in_dim; k += 32u) sum += input[row * p.in_dim + k] * weight[out_col * p.in_dim + k];\n"
           "    partials[tid] = sum; threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    for (uint stride = 16u; stride > 0u; stride >>= 1u) { if (tid < stride) partials[tid] += partials[tid + stride]; threadgroup_barrier(mem_flags::mem_threadgroup); }\n"
           "    if (tid == 0u) output[row * p.out_dim + out_col] = partials[0] + bias[out_col];\n"
           "}\n"
           "kernel void termite_deberta_embeddings_f32(device const float *weight [[buffer(0)]], device const float *gamma [[buffer(1)]], device const float *beta [[buffer(2)]], device const uint *ids [[buffer(3)]], device const uint *mask [[buffer(4)]], device float *output [[buffer(5)]], constant termite_metal_deberta_embeddings_f32_params &p [[buffer(6)]], uint tid [[thread_index_in_threadgroup]], uint3 tg [[threadgroup_position_in_grid]], uint3 threads_per_tg [[threads_per_threadgroup]]) {\n"
           "    threadgroup float sums[256]; threadgroup float sqs[256];\n"
           "    uint row = tg.x; uint width = threads_per_tg.x; if (row >= p.total || p.dim == 0u || width == 0u || tid >= 256u) return;\n"
           "    uint token = ids[row]; float sum = 0.0f; float sq = 0.0f;\n"
           "    if (token < p.rows) { for (uint d = tid; d < p.dim; d += width) { float v = weight[token * p.dim + d]; sum += v; sq += v * v; } }\n"
           "    sums[tid] = sum; sqs[tid] = sq; threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    for (uint stride = width >> 1; stride > 0u; stride >>= 1) { if (tid < stride && tid + stride < 256u) { sums[tid] += sums[tid + stride]; sqs[tid] += sqs[tid + stride]; } threadgroup_barrier(mem_flags::mem_threadgroup); }\n"
           "    float mean = sums[0] / float(p.dim); float var = max(sqs[0] / float(p.dim) - mean * mean, 0.0f); float inv_std = rsqrt(var + p.eps); float m = (mask[row] == 0u || token >= p.rows) ? 0.0f : 1.0f;\n"
           "    for (uint d = tid; d < p.dim; d += width) { float v = token < p.rows ? weight[token * p.dim + d] : 0.0f; output[row * p.dim + d] = ((v - mean) * inv_std * gamma[d] + beta[d]) * m; }\n"
           "}\n"
           "kernel void termite_gliner_word_embeddings_f32(device const float *hidden [[buffer(0)]], device const float *words_mask [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_gliner_word_embeddings_f32_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.batch * p.num_words * p.hidden_size; if (gid >= total || p.seq_len == 0u || p.hidden_size == 0u) return;\n"
           "    uint d = gid % p.hidden_size; uint tmp = gid / p.hidden_size; uint word = tmp % p.num_words; uint b = tmp / p.num_words;\n"
           "    float wanted = float(word + 1u); float sum = 0.0f; uint n = 0u; uint token_base = b * p.seq_len; uint hidden_base = token_base * p.hidden_size + d;\n"
           "    for (uint t = 0u; t < p.seq_len; ++t) { if (words_mask[token_base + t] == wanted) { sum += hidden[hidden_base + t * p.hidden_size]; ++n; } }\n"
           "    output[gid] = n == 0u ? 0.0f : sum / float(n);\n"
           "}\n"
           "kernel void termite_gliner_gru_combine_f32(device const float *label_embeddings [[buffer(0)]], device const float *gi [[buffer(1)]], device const float *gh [[buffer(2)]], device float *output [[buffer(3)]], constant termite_metal_gliner_gru_combine_f32_params &p [[buffer(4)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint count = p.rows * p.dim; if (gid >= count || p.dim == 0u) return;\n"
           "    uint row = gid / p.dim; uint col = gid - row * p.dim; uint gate_base = row * p.dim * 3u + col;\n"
           "    float r = 1.0f / (1.0f + exp(-(gi[gate_base] + gh[gate_base])));\n"
           "    float z = 1.0f / (1.0f + exp(-(gi[gate_base + p.dim] + gh[gate_base + p.dim])));\n"
           "    float n = tanh(gi[gate_base + p.dim * 2u] + r * gh[gate_base + p.dim * 2u]);\n"
           "    float h0 = label_embeddings[gid]; float h1 = (1.0f - z) * n + z * h0; output[gid] = h1 + h0;\n"
           "}\n"
           "kernel void termite_argmax_axis_f32(device const float *input [[buffer(0)]], device float *output [[buffer(1)]], constant termite_metal_argmax_axis_f32_params &p [[buffer(2)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint out_total = p.outer * p.inner; if (gid >= out_total || p.axis_dim == 0u || p.inner == 0u) return;\n"
           "    uint outer_idx = gid / p.inner; uint inner_idx = gid - outer_idx * p.inner;\n"
           "    uint base = outer_idx * p.axis_dim * p.inner + inner_idx;\n"
           "    float best = input[base]; uint best_idx = 0u;\n"
           "    for (uint i = 1u; i < p.axis_dim; ++i) { float candidate = input[base + i * p.inner]; if (candidate > best) { best = candidate; best_idx = i; } }\n"
           "    output[gid] = float(best_idx);\n"
           "}\n"
           "kernel void termite_convert_dtype_f32(device const float *input [[buffer(0)]], device float *output [[buffer(1)]], constant termite_metal_convert_dtype_f32_params &p [[buffer(2)]], uint gid [[thread_position_in_grid]]) {\n"
           "    if (gid >= p.elem_count) return; float value = input[gid];\n"
           "    if (p.kind == 1u) value = round(value); else if (p.kind == 2u) value = value != 0.0f ? 1.0f : 0.0f;\n"
           "    output[gid] = value;\n"
           "}\n"
           "kernel void termite_sdpa_f32(device const float *q [[buffer(0)]], device const float *k [[buffer(1)]], device const float *v [[buffer(2)]], device const float *bias [[buffer(3)]], device const float *mask [[buffer(4)]], device float *output [[buffer(5)]], constant termite_metal_sdpa_f32_params &p [[buffer(6)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.batch * p.num_heads * p.seq_len * p.head_dim; if (gid >= total || p.seq_len == 0u || p.head_dim == 0u) return;\n"
           "    uint d = gid % p.head_dim; uint tmp = gid / p.head_dim; uint qi = tmp % p.seq_len; uint bh = tmp / p.seq_len; uint b = bh / p.num_heads; uint h = bh - b * p.num_heads;\n"
           "    uint q_base = (bh * p.seq_len + qi) * p.head_dim;\n"
           "    float scale = rsqrt(float(p.head_dim)); float best = -3.402823466e+38f;\n"
           "    for (uint ki = 0u; ki < p.seq_len; ++ki) {\n"
           "        bool allowed = p.has_mask == 0u || mask[b * p.seq_len + ki] != 0.0f; if (!allowed) continue;\n"
           "        uint k_base = (bh * p.seq_len + ki) * p.head_dim; float score = 0.0f;\n"
           "        for (uint x = 0u; x < p.head_dim; ++x) score += q[q_base + x] * k[k_base + x];\n"
           "        score *= scale;\n"
           "        if (p.bias_mode == 1u) score += bias[(h * p.seq_len + qi) * p.seq_len + ki];\n"
           "        else if (p.bias_mode == 2u) score += bias[(bh * p.seq_len + qi) * p.seq_len + ki];\n"
           "        else if (p.bias_mode == 3u) score += bias[(b * p.seq_len + qi) * p.seq_len + ki];\n"
           "        best = max(best, score);\n"
           "    }\n"
           "    float sum = 0.0f; float accum = 0.0f;\n"
           "    for (uint ki = 0u; ki < p.seq_len; ++ki) {\n"
           "        bool allowed = p.has_mask == 0u || mask[b * p.seq_len + ki] != 0.0f; if (!allowed) continue;\n"
           "        uint k_base = (bh * p.seq_len + ki) * p.head_dim; float score = 0.0f;\n"
           "        for (uint x = 0u; x < p.head_dim; ++x) score += q[q_base + x] * k[k_base + x];\n"
           "        score *= scale;\n"
           "        if (p.bias_mode == 1u) score += bias[(h * p.seq_len + qi) * p.seq_len + ki];\n"
           "        else if (p.bias_mode == 2u) score += bias[(bh * p.seq_len + qi) * p.seq_len + ki];\n"
           "        else if (p.bias_mode == 3u) score += bias[(b * p.seq_len + qi) * p.seq_len + ki];\n"
           "        float w = exp(score - best); sum += w; accum += w * v[(bh * p.seq_len + ki) * p.head_dim + d];\n"
           "    }\n"
           "    output[gid] = sum > 0.0f ? accum / sum : 0.0f;\n"
           "}\n"
           "kernel void termite_disentangled_relative_attention_f32(device const float *q [[buffer(0)]], device const float *k [[buffer(1)]], device const float *v [[buffer(2)]], device const float *q_r [[buffer(3)]], device const float *k_r [[buffer(4)]], device const float *mask [[buffer(5)]], device float *output [[buffer(6)]], constant termite_metal_disentangled_relative_attention_f32_params &p [[buffer(7)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint hidden = p.num_heads * p.head_dim; uint total = p.batch * p.seq_len * hidden; if (gid >= total || p.seq_len == 0u || p.head_dim == 0u || hidden == 0u) return;\n"
           "    uint d = gid % p.head_dim; uint tmp = gid / p.head_dim; uint h = tmp % p.num_heads; uint token = tmp / p.num_heads; uint qi = token % p.seq_len; uint b = token / p.seq_len; uint head_off = h * p.head_dim;\n"
           "    uint q_base = (b * p.seq_len + qi) * hidden + head_off; float scale = rsqrt(float(p.head_dim) * 3.0f); float best = -3.402823466e+38f;\n"
           "    for (uint ki = 0u; ki < p.seq_len; ++ki) {\n"
           "        bool allowed = p.has_mask == 0u || mask[b * p.seq_len + ki] != 0.0f; if (!allowed) continue;\n"
           "        uint k_base = (b * p.seq_len + ki) * hidden + head_off; uint rel_idx = qi + p.seq_len - 1u - ki; uint rel_base = rel_idx * hidden + head_off; float score = 0.0f;\n"
           "        for (uint x = 0u; x < p.head_dim; ++x) score += q[q_base + x] * k[k_base + x] + q[q_base + x] * k_r[rel_base + x] + q_r[rel_base + x] * k[k_base + x];\n"
           "        score *= scale; best = max(best, score);\n"
           "    }\n"
           "    float sum = 0.0f; float accum = 0.0f;\n"
           "    for (uint ki = 0u; ki < p.seq_len; ++ki) {\n"
           "        bool allowed = p.has_mask == 0u || mask[b * p.seq_len + ki] != 0.0f; if (!allowed) continue;\n"
           "        uint k_base = (b * p.seq_len + ki) * hidden + head_off; uint rel_idx = qi + p.seq_len - 1u - ki; uint rel_base = rel_idx * hidden + head_off; float score = 0.0f;\n"
           "        for (uint x = 0u; x < p.head_dim; ++x) score += q[q_base + x] * k[k_base + x] + q[q_base + x] * k_r[rel_base + x] + q_r[rel_base + x] * k[k_base + x];\n"
           "        score *= scale; float w = exp(score - best); sum += w; accum += w * v[k_base + d];\n"
           "    }\n"
           "    output[gid] = sum > 0.0f ? accum / sum : 0.0f;\n"
           "}\n"
           "kernel void termite_disentangled_relative_attention_f32_tg(device const float *q [[buffer(0)]], device const float *k [[buffer(1)]], device const float *v [[buffer(2)]], device const float *q_r [[buffer(3)]], device const float *k_r [[buffer(4)]], device const float *mask [[buffer(5)]], device float *output [[buffer(6)]], constant termite_metal_disentangled_relative_attention_f32_params &p [[buffer(7)]], threadgroup float *scratch [[threadgroup(0)]], ushort tid [[thread_index_in_threadgroup]], uint3 tpg [[threads_per_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) {\n"
           "    if (p.seq_len == 0u || p.head_dim == 0u || p.num_heads == 0u) return;\n"
           "    uint qi = tg.x; uint h = tg.y; uint b = tg.z; if (qi >= p.seq_len || h >= p.num_heads || b >= p.batch) return;\n"
           "    uint hidden = p.num_heads * p.head_dim; uint head_off = h * p.head_dim; uint q_base = (b * p.seq_len + qi) * hidden + head_off; float scale = rsqrt(float(p.head_dim) * 3.0f);\n"
           "    uint lid = uint(tid); uint width = uint(tpg.x); threadgroup float *scores = scratch; threadgroup float *partials = scratch + p.seq_len;\n"
           "    float local_best = -3.402823466e+38f;\n"
           "    for (uint ki = lid; ki < p.seq_len; ki += width) {\n"
           "        bool allowed = p.has_mask == 0u || mask[b * p.seq_len + ki] != 0.0f; float score = -3.402823466e+38f;\n"
           "        if (allowed) { uint k_base = (b * p.seq_len + ki) * hidden + head_off; uint rel_idx = qi + p.seq_len - 1u - ki; uint rel_base = rel_idx * hidden + head_off; float acc = 0.0f;\n"
           "            if (p.head_dim == 64u) { const device float4 *q4 = reinterpret_cast<const device float4 *>(q + q_base); const device float4 *k4 = reinterpret_cast<const device float4 *>(k + k_base); const device float4 *kr4 = reinterpret_cast<const device float4 *>(k_r + rel_base); const device float4 *qr4 = reinterpret_cast<const device float4 *>(q_r + rel_base); for (uint x4 = 0u; x4 < 16u; ++x4) { float4 qx = q4[x4]; float4 kx = k4[x4]; acc += dot(qx, kx) + dot(qx, kr4[x4]) + dot(qr4[x4], kx); } } else { for (uint x = 0u; x < p.head_dim; ++x) { float qx = q[q_base + x]; acc += qx * k[k_base + x] + qx * k_r[rel_base + x] + q_r[rel_base + x] * k[k_base + x]; } }\n"
           "            score = acc * scale; local_best = max(local_best, score); }\n"
           "        scores[ki] = score;\n"
           "    }\n"
           "    partials[lid] = local_best; threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    for (uint stride = width >> 1u; stride > 0u; stride >>= 1u) { if (lid < stride) partials[lid] = max(partials[lid], partials[lid + stride]); threadgroup_barrier(mem_flags::mem_threadgroup); }\n"
           "    float best = partials[0]; float local_sum = 0.0f;\n"
           "    for (uint ki = lid; ki < p.seq_len; ki += width) { float s = scores[ki]; if (s > -3.0e38f) local_sum += exp(s - best); }\n"
           "    partials[lid] = local_sum; threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    for (uint stride = width >> 1u; stride > 0u; stride >>= 1u) { if (lid < stride) partials[lid] += partials[lid + stride]; threadgroup_barrier(mem_flags::mem_threadgroup); }\n"
           "    float denom = partials[0]; float inv_sum = denom > 0.0f ? 1.0f / denom : 0.0f;\n"
           "    for (uint ki = lid; ki < p.seq_len; ki += width) { float s = scores[ki]; scores[ki] = s > -3.0e38f ? exp(s - best) * inv_sum : 0.0f; } threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    for (uint d = lid; d < p.head_dim; d += width) { float accum = 0.0f; for (uint ki = 0u; ki < p.seq_len; ++ki) { uint k_base = (b * p.seq_len + ki) * hidden + head_off; accum += scores[ki] * v[k_base + d]; } output[(b * p.seq_len + qi) * hidden + head_off + d] = accum; }\n"
           "}\n"
           "kernel void termite_disentangled_relative_attention_f32_flash4(device const float *q [[buffer(0)]], device const float *k [[buffer(1)]], device const float *v [[buffer(2)]], device const float *q_r [[buffer(3)]], device const float *k_r [[buffer(4)]], device const float *mask [[buffer(5)]], device float *output [[buffer(6)]], constant termite_metal_disentangled_relative_attention_f32_params &p [[buffer(7)]], threadgroup float *scratch [[threadgroup(0)]], ushort tid [[thread_index_in_threadgroup]], uint3 tpg [[threads_per_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) {\n"
           "    if (p.seq_len == 0u || p.head_dim == 0u || p.num_heads == 0u) return;\n"
           "    uint q0 = tg.x * 4u; uint h = tg.y; uint b = tg.z; if (q0 >= p.seq_len || h >= p.num_heads || b >= p.batch) return;\n"
           "    uint block_m = min(4u, p.seq_len - q0); uint lid = uint(tid); uint width = uint(tpg.x); uint hidden = p.num_heads * p.head_dim; uint head_off = h * p.head_dim; float scale = rsqrt(float(p.head_dim) * 3.0f);\n"
           "    threadgroup float *scores = scratch; threadgroup float *bests = scratch + 4u * p.seq_len; threadgroup float *partials = bests + 4u;\n"
           "    float local_best0 = -3.402823466e+38f; float local_best1 = -3.402823466e+38f; float local_best2 = -3.402823466e+38f; float local_best3 = -3.402823466e+38f;\n"
           "    for (uint ki = lid; ki < p.seq_len; ki += width) {\n"
           "        bool allowed = p.has_mask == 0u || mask[b * p.seq_len + ki] != 0.0f; float s0 = -3.402823466e+38f; float s1 = -3.402823466e+38f; float s2 = -3.402823466e+38f; float s3 = -3.402823466e+38f;\n"
           "        if (allowed) { uint k_base = (b * p.seq_len + ki) * hidden + head_off;\n"
           "            if (p.head_dim == 64u) { const device float4 *k4 = reinterpret_cast<const device float4 *>(k + k_base); float acc0 = 0.0f; float acc1 = 0.0f; float acc2 = 0.0f; float acc3 = 0.0f;\n"
           "                for (uint x4 = 0u; x4 < 16u; ++x4) { float4 kx = k4[x4];\n"
           "                    if (block_m > 0u) { uint qi = q0; uint q_base = (b * p.seq_len + qi) * hidden + head_off; uint rel_base = (qi + p.seq_len - 1u - ki) * hidden + head_off; const device float4 *q4 = reinterpret_cast<const device float4 *>(q + q_base); const device float4 *kr4 = reinterpret_cast<const device float4 *>(k_r + rel_base); const device float4 *qr4 = reinterpret_cast<const device float4 *>(q_r + rel_base); acc0 += dot(q4[x4], kx) + dot(q4[x4], kr4[x4]) + dot(qr4[x4], kx); }\n"
           "                    if (block_m > 1u) { uint qi = q0 + 1u; uint q_base = (b * p.seq_len + qi) * hidden + head_off; uint rel_base = (qi + p.seq_len - 1u - ki) * hidden + head_off; const device float4 *q4 = reinterpret_cast<const device float4 *>(q + q_base); const device float4 *kr4 = reinterpret_cast<const device float4 *>(k_r + rel_base); const device float4 *qr4 = reinterpret_cast<const device float4 *>(q_r + rel_base); acc1 += dot(q4[x4], kx) + dot(q4[x4], kr4[x4]) + dot(qr4[x4], kx); }\n"
           "                    if (block_m > 2u) { uint qi = q0 + 2u; uint q_base = (b * p.seq_len + qi) * hidden + head_off; uint rel_base = (qi + p.seq_len - 1u - ki) * hidden + head_off; const device float4 *q4 = reinterpret_cast<const device float4 *>(q + q_base); const device float4 *kr4 = reinterpret_cast<const device float4 *>(k_r + rel_base); const device float4 *qr4 = reinterpret_cast<const device float4 *>(q_r + rel_base); acc2 += dot(q4[x4], kx) + dot(q4[x4], kr4[x4]) + dot(qr4[x4], kx); }\n"
           "                    if (block_m > 3u) { uint qi = q0 + 3u; uint q_base = (b * p.seq_len + qi) * hidden + head_off; uint rel_base = (qi + p.seq_len - 1u - ki) * hidden + head_off; const device float4 *q4 = reinterpret_cast<const device float4 *>(q + q_base); const device float4 *kr4 = reinterpret_cast<const device float4 *>(k_r + rel_base); const device float4 *qr4 = reinterpret_cast<const device float4 *>(q_r + rel_base); acc3 += dot(q4[x4], kx) + dot(q4[x4], kr4[x4]) + dot(qr4[x4], kx); }\n"
           "                } s0 = acc0 * scale; s1 = acc1 * scale; s2 = acc2 * scale; s3 = acc3 * scale;\n"
           "            } else { float acc0 = 0.0f; float acc1 = 0.0f; float acc2 = 0.0f; float acc3 = 0.0f; for (uint x = 0u; x < p.head_dim; ++x) { float kx = k[k_base + x];\n"
           "                    if (block_m > 0u) { uint qi = q0; uint q_base = (b * p.seq_len + qi) * hidden + head_off; uint rel_base = (qi + p.seq_len - 1u - ki) * hidden + head_off; float qx = q[q_base + x]; acc0 += qx * kx + qx * k_r[rel_base + x] + q_r[rel_base + x] * kx; }\n"
           "                    if (block_m > 1u) { uint qi = q0 + 1u; uint q_base = (b * p.seq_len + qi) * hidden + head_off; uint rel_base = (qi + p.seq_len - 1u - ki) * hidden + head_off; float qx = q[q_base + x]; acc1 += qx * kx + qx * k_r[rel_base + x] + q_r[rel_base + x] * kx; }\n"
           "                    if (block_m > 2u) { uint qi = q0 + 2u; uint q_base = (b * p.seq_len + qi) * hidden + head_off; uint rel_base = (qi + p.seq_len - 1u - ki) * hidden + head_off; float qx = q[q_base + x]; acc2 += qx * kx + qx * k_r[rel_base + x] + q_r[rel_base + x] * kx; }\n"
           "                    if (block_m > 3u) { uint qi = q0 + 3u; uint q_base = (b * p.seq_len + qi) * hidden + head_off; uint rel_base = (qi + p.seq_len - 1u - ki) * hidden + head_off; float qx = q[q_base + x]; acc3 += qx * kx + qx * k_r[rel_base + x] + q_r[rel_base + x] * kx; }\n"
           "                } s0 = acc0 * scale; s1 = acc1 * scale; s2 = acc2 * scale; s3 = acc3 * scale; }\n"
           "        }\n"
           "        if (block_m > 0u) { scores[ki] = s0; local_best0 = max(local_best0, s0); } if (block_m > 1u) { scores[p.seq_len + ki] = s1; local_best1 = max(local_best1, s1); } if (block_m > 2u) { scores[2u * p.seq_len + ki] = s2; local_best2 = max(local_best2, s2); } if (block_m > 3u) { scores[3u * p.seq_len + ki] = s3; local_best3 = max(local_best3, s3); }\n"
           "    }\n"
           "    partials[lid] = local_best0; partials[width + lid] = local_best1; partials[2u * width + lid] = local_best2; partials[3u * width + lid] = local_best3; threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    for (uint stride = width >> 1u; stride > 0u; stride >>= 1u) { if (lid < stride) { partials[lid] = max(partials[lid], partials[lid + stride]); partials[width + lid] = max(partials[width + lid], partials[width + lid + stride]); partials[2u * width + lid] = max(partials[2u * width + lid], partials[2u * width + lid + stride]); partials[3u * width + lid] = max(partials[3u * width + lid], partials[3u * width + lid + stride]); } threadgroup_barrier(mem_flags::mem_threadgroup); }\n"
           "    if (lid == 0u) { bests[0] = partials[0]; bests[1] = partials[width]; bests[2] = partials[2u * width]; bests[3] = partials[3u * width]; } threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    float sum0 = 0.0f; float sum1 = 0.0f; float sum2 = 0.0f; float sum3 = 0.0f; for (uint ki = lid; ki < p.seq_len; ki += width) { if (block_m > 0u) { float s = scores[ki]; if (s > -3.0e38f) sum0 += exp(s - bests[0]); } if (block_m > 1u) { float s = scores[p.seq_len + ki]; if (s > -3.0e38f) sum1 += exp(s - bests[1]); } if (block_m > 2u) { float s = scores[2u * p.seq_len + ki]; if (s > -3.0e38f) sum2 += exp(s - bests[2]); } if (block_m > 3u) { float s = scores[3u * p.seq_len + ki]; if (s > -3.0e38f) sum3 += exp(s - bests[3]); } }\n"
           "    partials[lid] = sum0; partials[width + lid] = sum1; partials[2u * width + lid] = sum2; partials[3u * width + lid] = sum3; threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    for (uint stride = width >> 1u; stride > 0u; stride >>= 1u) { if (lid < stride) { partials[lid] += partials[lid + stride]; partials[width + lid] += partials[width + lid + stride]; partials[2u * width + lid] += partials[2u * width + lid + stride]; partials[3u * width + lid] += partials[3u * width + lid + stride]; } threadgroup_barrier(mem_flags::mem_threadgroup); }\n"
           "    float inv0 = partials[0] > 0.0f ? 1.0f / partials[0] : 0.0f; float inv1 = partials[width] > 0.0f ? 1.0f / partials[width] : 0.0f; float inv2 = partials[2u * width] > 0.0f ? 1.0f / partials[2u * width] : 0.0f; float inv3 = partials[3u * width] > 0.0f ? 1.0f / partials[3u * width] : 0.0f;\n"
           "    for (uint ki = lid; ki < p.seq_len; ki += width) { if (block_m > 0u) { float s = scores[ki]; scores[ki] = s > -3.0e38f ? exp(s - bests[0]) * inv0 : 0.0f; } if (block_m > 1u) { float s = scores[p.seq_len + ki]; scores[p.seq_len + ki] = s > -3.0e38f ? exp(s - bests[1]) * inv1 : 0.0f; } if (block_m > 2u) { float s = scores[2u * p.seq_len + ki]; scores[2u * p.seq_len + ki] = s > -3.0e38f ? exp(s - bests[2]) * inv2 : 0.0f; } if (block_m > 3u) { float s = scores[3u * p.seq_len + ki]; scores[3u * p.seq_len + ki] = s > -3.0e38f ? exp(s - bests[3]) * inv3 : 0.0f; } } threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    for (uint d = lid; d < p.head_dim; d += width) { float accum0 = 0.0f; float accum1 = 0.0f; float accum2 = 0.0f; float accum3 = 0.0f;\n"
           "        for (uint ki = 0u; ki < p.seq_len; ++ki) { uint v_base = (b * p.seq_len + ki) * hidden + head_off; float vx = v[v_base + d];\n"
           "            if (block_m > 0u) accum0 += scores[ki] * vx; if (block_m > 1u) accum1 += scores[p.seq_len + ki] * vx; if (block_m > 2u) accum2 += scores[2u * p.seq_len + ki] * vx; if (block_m > 3u) accum3 += scores[3u * p.seq_len + ki] * vx; }\n"
           "        if (block_m > 0u) output[(b * p.seq_len + q0) * hidden + head_off + d] = accum0; if (block_m > 1u) output[(b * p.seq_len + q0 + 1u) * hidden + head_off + d] = accum1;\n"
           "        if (block_m > 2u) output[(b * p.seq_len + q0 + 2u) * hidden + head_off + d] = accum2; if (block_m > 3u) output[(b * p.seq_len + q0 + 3u) * hidden + head_off + d] = accum3; }\n"
           "}\n"
           "kernel void termite_deberta_relative_score_gemm_f32(device const float *q [[buffer(0)]], device const float *k [[buffer(1)]], device const float *q_r [[buffer(2)]], device const float *k_r [[buffer(3)]], device float *out [[buffer(4)]], constant termite_metal_deberta_relative_score_gemm_f32_params &p [[buffer(5)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint width = p.mode == 0u ? p.seq_len : p.rel_len; uint rows_per_bh = p.seq_len * width; uint bh_count = p.batch * p.num_heads; uint total = bh_count * rows_per_bh; if (gid >= total || p.head_dim == 0u || width == 0u) return;\n"
           "    uint r = gid % width; uint tmp = gid / width; uint token = tmp % p.seq_len; uint bh = tmp / p.seq_len; uint h = bh % p.num_heads; uint b = bh / p.num_heads; uint hidden = p.num_heads * p.head_dim; uint head_off = h * p.head_dim; uint left_base = (b * p.seq_len + token) * hidden + head_off; uint right_base = 0u;\n"
           "    const device float *left = q; const device float *right = k; if (p.mode == 0u) { right_base = (b * p.seq_len + r) * hidden + head_off; } else if (p.mode == 1u) { right = k_r; right_base = r * hidden + head_off; } else { left = k; right = q_r; right_base = r * hidden + head_off; }\n"
           "    float acc = 0.0f; if (p.head_dim == 64u) { const device float4 *l4 = reinterpret_cast<const device float4 *>(left + left_base); const device float4 *r4 = reinterpret_cast<const device float4 *>(right + right_base); for (uint x4 = 0u; x4 < 16u; ++x4) acc += dot(l4[x4], r4[x4]); } else { for (uint d = 0u; d < p.head_dim; ++d) acc += left[left_base + d] * right[right_base + d]; } out[gid] = acc;\n"
           "}\n"
           "kernel void termite_deberta_relative_score_softmax_pv_f32(device const float *cc [[buffer(0)]], device const float *c2p [[buffer(1)]], device const float *p2c [[buffer(2)]], device const float *v [[buffer(3)]], device const float *mask [[buffer(4)]], device float *output [[buffer(5)]], constant termite_metal_disentangled_relative_attention_f32_params &p [[buffer(6)]], threadgroup float *scratch [[threadgroup(0)]], ushort tid [[thread_index_in_threadgroup]], uint3 tpg [[threads_per_threadgroup]], uint3 tg [[threadgroup_position_in_grid]]) {\n"
           "    uint qi = tg.x; uint h = tg.y; uint b = tg.z; if (p.seq_len == 0u || p.head_dim == 0u || p.num_heads == 0u || qi >= p.seq_len || h >= p.num_heads || b >= p.batch) return;\n"
           "    uint lid = uint(tid); uint width = uint(tpg.x); uint hidden = p.num_heads * p.head_dim; uint rel_len = p.seq_len * 2u - 1u; uint bh = b * p.num_heads + h; uint cc_base = (bh * p.seq_len + qi) * p.seq_len; uint c2p_base = (bh * p.seq_len + qi) * rel_len; float scale = rsqrt(float(p.head_dim) * 3.0f); threadgroup float *scores = scratch; threadgroup float *partials = scratch + p.seq_len;\n"
           "    float local_best = -3.402823466e+38f; for (uint ki = lid; ki < p.seq_len; ki += width) { bool allowed = p.has_mask == 0u || mask[b * p.seq_len + ki] != 0.0f; uint rel_idx = qi + p.seq_len - 1u - ki; uint p2c_idx = (bh * p.seq_len + ki) * rel_len + rel_idx; float score = allowed ? (cc[cc_base + ki] + c2p[c2p_base + rel_idx] + p2c[p2c_idx]) * scale : -3.402823466e+38f; scores[ki] = score; local_best = max(local_best, score); } partials[lid] = local_best; threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    for (uint stride = width >> 1u; stride > 0u; stride >>= 1u) { if (lid < stride) partials[lid] = max(partials[lid], partials[lid + stride]); threadgroup_barrier(mem_flags::mem_threadgroup); } float best = partials[0]; float local_sum = 0.0f; for (uint ki = lid; ki < p.seq_len; ki += width) { float s = scores[ki]; if (s > -3.0e38f) local_sum += exp(s - best); } partials[lid] = local_sum; threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    for (uint stride = width >> 1u; stride > 0u; stride >>= 1u) { if (lid < stride) partials[lid] += partials[lid + stride]; threadgroup_barrier(mem_flags::mem_threadgroup); } float denom = partials[0]; float inv_sum = denom > 0.0f ? 1.0f / denom : 0.0f; for (uint ki = lid; ki < p.seq_len; ki += width) { float s = scores[ki]; scores[ki] = s > -3.0e38f ? exp(s - best) * inv_sum : 0.0f; } threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    uint head_off = h * p.head_dim; for (uint d = lid; d < p.head_dim; d += width) { float accum = 0.0f; for (uint ki = 0u; ki < p.seq_len; ++ki) { uint v_base = (b * p.seq_len + ki) * hidden + head_off; accum += scores[ki] * v[v_base + d]; } output[(b * p.seq_len + qi) * hidden + head_off + d] = accum; }\n"
           "}\n"
           "kernel void termite_transpose_f32(device const float *input [[buffer(0)]], device float *output [[buffer(1)]], constant termite_metal_transpose_f32_params &p [[buffer(2)]], uint gid [[thread_position_in_grid]]) {\n"
           "    if (gid >= p.total || p.rank == 0u || p.rank > 8u) return;\n"
           "    uint rem = gid; uint flat_in = 0u;\n"
           "    for (uint axis = 0u; axis < p.rank; ++axis) { uint stride = p.out_strides[axis]; uint coord = stride == 0u ? 0u : rem / stride; if (stride != 0u) rem -= coord * stride; uint in_axis = p.perm[axis]; if (in_axis >= p.rank) return; flat_in += coord * p.in_strides[in_axis]; }\n"
           "    output[gid] = input[flat_in];\n"
           "}\n"
           "kernel void termite_dot_general_2d_f32(device const float *lhs [[buffer(0)]], device const float *rhs [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_dot_general_2d_f32_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.m * p.n; if (gid >= total || p.k == 0u) return; uint row = gid / p.n; uint col = gid - row * p.n; float acc = 0.0f;\n"
           "    for (uint kk = 0u; kk < p.k; ++kk) { float r = p.rhs_contract_axis == 0u ? rhs[kk * p.n + col] : rhs[col * p.k + kk]; acc += lhs[row * p.k + kk] * r; }\n"
           "    output[gid] = acc;\n"
           "}\n"
           "kernel void termite_dot_general_batched_f32(device const float *lhs [[buffer(0)]], device const float *rhs [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_dot_general_batched_f32_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint per_batch = p.m * p.n; uint total = p.batch_count * per_batch; if (gid >= total || p.k == 0u || per_batch == 0u) return; uint batch = gid / per_batch; uint inner = gid - batch * per_batch; uint row = inner / p.n; uint col = inner - row * p.n; uint lhs_base = batch * p.m * p.k; uint rhs_base = batch * p.n * p.k; float acc = 0.0f;\n"
           "    for (uint kk = 0u; kk < p.k; ++kk) { float r = p.rhs_contract_axis == 0u ? rhs[rhs_base + kk * p.n + col] : rhs[rhs_base + col * p.k + kk]; acc += lhs[lhs_base + row * p.k + kk] * r; }\n"
           "    output[gid] = acc;\n"
           "}\n"
           "kernel void termite_conv1d_f32(device const float *input [[buffer(0)]], device const float *weight [[buffer(1)]], device const float *bias [[buffer(2)]], device float *output [[buffer(3)]], constant termite_metal_conv1d_f32_params &p [[buffer(4)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.batch * p.out_channels * p.out_time; if (gid >= total) return; uint t = gid % p.out_time; uint tmp = gid / p.out_time; uint oc = tmp % p.out_channels; uint b = tmp / p.out_channels; float acc = p.has_bias != 0u ? bias[oc] : 0.0f;\n"
           "    for (uint ic = 0u; ic < p.in_channels; ++ic) { for (uint kk = 0u; kk < p.kernel_size; ++kk) { int in_t = int(t * p.stride + kk) - int(p.padding); if (in_t >= 0 && uint(in_t) < p.time_steps) { uint in_idx = (b * p.in_channels + ic) * p.time_steps + uint(in_t); uint w_idx = (oc * p.in_channels + ic) * p.kernel_size + kk; acc += input[in_idx] * weight[w_idx]; } } }\n"
           "    output[gid] = acc;\n"
           "}\n"
           "kernel void termite_conv2d_f32(device const float *input [[buffer(0)]], device const float *weight [[buffer(1)]], device const float *bias [[buffer(2)]], device float *output [[buffer(3)]], constant termite_metal_conv2d_f32_params &p [[buffer(4)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.batch * p.out_channels * p.out_h * p.out_w; if (gid >= total || p.groups == 0u) return; uint ow = gid % p.out_w; uint tmp0 = gid / p.out_w; uint oh = tmp0 % p.out_h; uint tmp1 = tmp0 / p.out_h; uint oc = tmp1 % p.out_channels; uint b = tmp1 / p.out_channels; uint oc_per_group = p.out_channels / p.groups; uint ic_per_group = p.in_channels / p.groups; uint group = oc / oc_per_group; uint ic_start = group * ic_per_group; float acc = p.has_bias != 0u ? bias[oc] : 0.0f;\n"
           "    for (uint local_ic = 0u; local_ic < ic_per_group; ++local_ic) { uint ic = ic_start + local_ic; for (uint kh = 0u; kh < p.kernel_h; ++kh) { int ih = int(oh * p.stride_h + kh) - int(p.padding_h); if (ih < 0 || uint(ih) >= p.height) continue; for (uint kw = 0u; kw < p.kernel_w; ++kw) { int iw = int(ow * p.stride_w + kw) - int(p.padding_w); if (iw < 0 || uint(iw) >= p.width) continue; uint in_idx = ((b * p.in_channels + ic) * p.height + uint(ih)) * p.width + uint(iw); uint w_idx = ((oc * ic_per_group + local_ic) * p.kernel_h + kh) * p.kernel_w + kw; acc += input[in_idx] * weight[w_idx]; } } }\n"
           "    output[gid] = acc;\n"
           "}\n"
           "kernel void termite_encode_int8_per_head(device const float *in [[buffer(0)]], device uchar *out [[buffer(1)]], constant termite_metal_encode_key_params &p [[buffer(2)]], uint2 gid [[thread_position_in_grid]], uint2 tid [[thread_position_in_threadgroup]], uint2 tg_size [[threads_per_threadgroup]]) {\n"
           "    uint head_dim = p.head_dim; uint num_kv_heads = p.num_kv_heads; uint total_heads = p.rows * num_kv_heads; if (gid.y >= total_heads) return;\n"
           "    uint row = gid.y / num_kv_heads; uint head = gid.y - row * num_kv_heads; uint d = gid.x; uint row_values = num_kv_heads * head_dim; uint in_base = row * row_values + head * head_dim; uint out_base = row * p.key_row_bytes + head * (head_dim + 4u);\n"
           "    float val = (d < head_dim) ? in[in_base + d] : 0.0f; float abs_val = (d < head_dim) ? fabs(val) : 0.0f;\n"
           "    threadgroup float tg_max[256]; tg_max[tid.x] = abs_val; threadgroup_barrier(mem_flags::mem_threadgroup);\n"
           "    for (uint stride = tg_size.x >> 1u; stride > 0u; stride >>= 1u) { if (tid.x < stride) { tg_max[tid.x] = max(tg_max[tid.x], tg_max[tid.x + stride]); } threadgroup_barrier(mem_flags::mem_threadgroup); }\n"
           "    float max_abs = tg_max[0]; float scale = max_abs == 0.0f ? 1.0f : max_abs / 127.0f; float inv_scale = 1.0f / scale;\n"
           "    if (tid.x == 0u) { uint scale_bits = as_type<uint>(scale); out[out_base + 0u] = uchar(scale_bits & 0xffu); out[out_base + 1u] = uchar((scale_bits >> 8u) & 0xffu); out[out_base + 2u] = uchar((scale_bits >> 16u) & 0xffu); out[out_base + 3u] = uchar((scale_bits >> 24u) & 0xffu); }\n"
           "    if (d < head_dim) { int q = int(clamp(round(val * inv_scale), -127.0f, 127.0f)); out[out_base + 4u + d] = uchar(q); }\n"
           "}\n"
           "kernel void termite_embed_absolute_position(device const float *token_embedding [[buffer(0)]], device const float *position_embedding [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_embed_absolute_position_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    if (gid >= p.hidden_size) return;\n"
           "    uint token_base = p.token_id * p.hidden_size;\n"
           "    uint position_base = p.position_id * p.hidden_size;\n"
           "    output[gid] = token_embedding[token_base + gid] + position_embedding[position_base + gid];\n"
           "}\n"
           "kernel void termite_embedding_lookup(device const float *weight [[buffer(0)]], device const uint *ids [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_embedding_lookup_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.total * p.dim;\n"
           "    if (gid >= total) return;\n"
           "    uint row = gid / p.dim;\n"
           "    uint col = gid - row * p.dim;\n"
           "    uint idx = ids[row];\n"
           "    output[gid] = weight[idx * p.dim + col];\n"
           "}\n"
           "static inline float termite_embedding_bf16_to_f32(ushort bits) { return as_type<float>((uint(bits) << 16u)); }\n"
           "kernel void termite_embedding_lookup_bf16(device const ushort *weight [[buffer(0)]], device const uint *ids [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_embedding_lookup_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.total * p.dim;\n"
           "    if (gid >= total) return;\n"
           "    uint row = gid / p.dim;\n"
           "    uint col = gid - row * p.dim;\n"
           "    uint idx = ids[row];\n"
           "    output[gid] = termite_embedding_bf16_to_f32(weight[idx * p.dim + col]);\n"
           "}\n"
           "kernel void termite_apply_rope(device const float *input [[buffer(0)]], device const uint *positions [[buffer(1)]], device float *output [[buffer(2)]], constant termite_metal_rope_params &p [[buffer(3)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.total_chunks * p.head_dim;\n"
           "    if (gid >= total) return;\n"
           "    uint chunk = gid / p.head_dim;\n"
           "    uint d = gid - chunk * p.head_dim;\n"
           "    uint base = chunk * p.head_dim;\n"
           "    if (p.rope_dim < 2u) { output[gid] = input[gid]; return; }\n"
           "    uint rope_half = p.rope_dim / 2u;\n"
           "    uint head_half = p.head_dim / 2u;\n"
           "    uint idx0 = 0u;\n"
           "    uint idx1 = 0u;\n"
           "    uint j = 0u;\n"
           "    if (p.consecutive_pairs != 0u) {\n"
           "        if (d >= p.rope_dim) { output[gid] = input[gid]; return; }\n"
           "        j = d / 2u;\n"
           "        idx0 = 2u * j;\n"
           "        idx1 = idx0 + 1u;\n"
           "    } else {\n"
           "        if (d < rope_half) { j = d; idx0 = d; idx1 = head_half + d; }\n"
           "        else if (d >= head_half && d < head_half + rope_half) { j = d - head_half; idx0 = j; idx1 = d; }\n"
           "        else { output[gid] = input[gid]; return; }\n"
           "    }\n"
           "    float pos = float(positions[chunk]);\n"
           "    float freq = 1.0f / pow(p.theta, float(2u * j) / float(p.rope_dim));\n"
           "    float angle = pos * p.freq_scale * freq;\n"
           "    float cos_val = cos(angle);\n"
           "    float sin_val = sin(angle);\n"
           "    float x0 = input[base + idx0];\n"
           "    float x1 = input[base + idx1];\n"
           "    if (d == idx0) output[gid] = x0 * cos_val - x1 * sin_val;\n"
           "    else if (d == idx1) output[gid] = x0 * sin_val + x1 * cos_val;\n"
           "    else output[gid] = input[gid];\n"
           "}\n"
           "kernel void termite_attention_f32(device const float *q [[buffer(0)]], device const float *k [[buffer(1)]], device const float *v [[buffer(2)]], device float *output [[buffer(3)]], device const float *bias [[buffer(4)]], device const uchar *mask [[buffer(5)]], constant termite_metal_attention_f32_params &p [[buffer(6)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.q_len * p.num_heads;\n"
           "    if (gid >= total) return;\n"
           "    uint qi = gid / p.num_heads;\n"
           "    uint h = gid - qi * p.num_heads;\n"
           "    uint heads_per_group = p.num_heads / p.num_kv_heads;\n"
           "    uint kv_h = h / heads_per_group;\n"
           "    uint q_stride = p.num_heads * p.head_dim;\n"
           "    uint kv_stride = p.num_kv_heads * p.head_dim;\n"
           "    uint q_base = qi * q_stride + h * p.head_dim;\n"
           "    uint kv_head_off = kv_h * p.head_dim;\n"
           "    uint out_base = qi * q_stride + h * p.head_dim;\n"
           "    float best = -3.402823466e+38f;\n"
           "    float scale = rsqrt(float(p.head_dim));\n"
           "    uint query_pos = p.query_position_offset + qi;\n"
           "    for (uint ki = 0u; ki < p.kv_len; ++ki) {\n"
           "        uint key_pos = p.kv_position_offset + ki;\n"
           "        bool allowed = key_pos <= query_pos;\n"
           "        if (!allowed && p.has_mask != 0u && query_pos < p.total_sequence_len && key_pos < p.total_sequence_len) {\n"
           "            uint mask_idx = query_pos * p.total_sequence_len + key_pos;\n"
           "            allowed = mask[mask_idx] != 0u;\n"
           "        }\n"
           "        if (key_pos > query_pos) allowed = false;\n"
           "        if (p.sliding_window != 0u && allowed) allowed = (query_pos - key_pos) < p.sliding_window;\n"
           "        float score = -3.402823466e+38f;\n"
           "        if (allowed) {\n"
           "            uint k_base = ki * kv_stride + kv_head_off;\n"
           "            float acc = 0.0f;\n"
           "            for (uint d = 0u; d < p.head_dim; ++d) acc += q[q_base + d] * k[k_base + d];\n"
           "            score = acc * scale;\n"
           "            if (p.has_bias != 0u) score += bias[h * p.q_len * p.kv_len + qi * p.kv_len + key_pos];\n"
           "        }\n"
           "        if (score > best) best = score;\n"
           "    }\n"
           "    for (uint d = 0u; d < p.head_dim; ++d) output[out_base + d] = 0.0f;\n"
           "    float sum = 0.0f;\n"
           "    for (uint ki = 0u; ki < p.kv_len; ++ki) {\n"
           "        uint key_pos = p.kv_position_offset + ki;\n"
           "        bool allowed = key_pos <= query_pos;\n"
           "        if (!allowed && p.has_mask != 0u && query_pos < p.total_sequence_len && key_pos < p.total_sequence_len) {\n"
           "            uint mask_idx = query_pos * p.total_sequence_len + key_pos;\n"
           "            allowed = mask[mask_idx] != 0u;\n"
           "        }\n"
           "        if (key_pos > query_pos) allowed = false;\n"
           "        if (p.sliding_window != 0u && allowed) allowed = (query_pos - key_pos) < p.sliding_window;\n"
           "        if (!allowed) continue;\n"
           "        uint k_base = ki * kv_stride + kv_head_off;\n"
           "        uint v_base = ki * kv_stride + kv_head_off;\n"
           "        float acc = 0.0f;\n"
           "        for (uint d = 0u; d < p.head_dim; ++d) acc += q[q_base + d] * k[k_base + d];\n"
           "        float score = acc * scale;\n"
           "        if (p.has_bias != 0u) score += bias[h * p.q_len * p.kv_len + qi * p.kv_len + key_pos];\n"
           "        float e = exp(score - best);\n"
           "        sum += e;\n"
           "        for (uint d = 0u; d < p.head_dim; ++d) output[out_base + d] += e * v[v_base + d];\n"
           "    }\n"
           "    float inv_sum = sum > 0.0f ? 1.0f / sum : 0.0f;\n"
           "    for (uint d = 0u; d < p.head_dim; ++d) output[out_base + d] *= inv_sum;\n"
           "}\n"
           "kernel void termite_compressed_attention_store_local(device const float *input [[buffer(0)]], device float *local [[buffer(1)]], constant termite_metal_compressed_attention_store_local_params &p [[buffer(2)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.query_rows * p.head_dim;\n"
           "    if (gid >= total) return;\n"
           "    uint row = gid / p.head_dim;\n"
           "    uint d = gid - row * p.head_dim;\n"
           "    local[(p.query_abs_start + row) * p.head_dim + d] = input[gid];\n"
           "}\n"
           "kernel void termite_compressed_attention_store_component(device const float *projected_in [[buffer(0)]], device const float *gate_in [[buffer(1)]], device float *projected_cache [[buffer(2)]], device float *gate_cache [[buffer(3)]], constant termite_metal_compressed_attention_component_params &p [[buffer(4)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint projected_total = p.query_rows * p.row_dim;\n"
           "    uint gate_total = p.query_rows * p.gate_width;\n"
           "    if (gid < projected_total) { uint r = gid / p.row_dim; uint d = gid - r * p.row_dim; projected_cache[(p.query_abs_start + r) * p.row_dim + d] = projected_in[gid]; }\n"
           "    if (gid < gate_total) { uint r = gid / p.gate_width; uint g = gid - r * p.gate_width; gate_cache[(p.query_abs_start + r) * p.gate_width + g] = gate_in[gid]; }\n"
           "}\n"
           "kernel void termite_compressed_attention_update_component(device const float *bias [[buffer(0)]], device const float *norm [[buffer(1)]], device float *projected_cache [[buffer(2)]], device float *gate_cache [[buffer(3)]], device float *compressed [[buffer(4)]], device uint *positions [[buffer(5)]], constant termite_metal_compressed_attention_component_params &p [[buffer(6)]], uint block [[thread_position_in_grid]]) {\n"
           "    if (block >= p.row_count) return;\n"
           "    uint first_block = p.query_abs_start / p.compress_rate;\n"
           "    uint last_block = (p.total_tokens - 1u) / p.compress_rate;\n"
           "    if (block < first_block || block > last_block) return;\n"
           "    uint start = block * p.compress_rate;\n"
           "    uint end = min(p.total_tokens, start + p.compress_rate);\n"
           "    float denom = 0.0f;\n"
           "    for (uint pos = start; pos < end; ++pos) {\n"
           "        uint gate_slot = (p.gate_width == p.compress_rate) ? (pos - start) : 0u;\n"
           "        denom += 1.0f / (1.0f + exp(-gate_cache[pos * p.gate_width + gate_slot]));\n"
           "    }\n"
           "    float sum_sq = 0.0f;\n"
           "    for (uint d = 0u; d < p.row_dim; ++d) {\n"
           "        float acc = 0.0f;\n"
           "        for (uint pos = start; pos < end; ++pos) {\n"
           "            uint gate_slot = (p.gate_width == p.compress_rate) ? (pos - start) : 0u;\n"
           "            float gate = 1.0f / (1.0f + exp(-gate_cache[pos * p.gate_width + gate_slot]));\n"
           "            float b = 0.0f;\n"
           "            if (p.bias_rows == p.compress_rate) b = bias[(pos - start) * p.row_dim + d]; else b = bias[d];\n"
           "            acc += gate * (projected_cache[pos * p.row_dim + d] + b);\n"
           "        }\n"
           "        float v = denom > 0.0f ? acc / denom : 0.0f;\n"
           "        compressed[block * p.row_dim + d] = v;\n"
           "        sum_sq += v * v;\n"
           "    }\n"
           "    float inv = rsqrt(sum_sq / float(p.row_dim) + p.eps);\n"
           "    uint row_pos = end - 1u;\n"
           "    positions[block] = row_pos;\n"
           "    for (uint d = 0u; d < p.row_dim; ++d) compressed[block * p.row_dim + d] *= inv * norm[d];\n"
           "    if (p.rope_dim >= 2u) {\n"
           "        uint rope_half = p.rope_dim / 2u;\n"
           "        uint rope_start = p.row_dim > p.rope_dim ? p.row_dim - p.rope_dim : 0u;\n"
           "        for (uint j = 0u; j < rope_half; ++j) {\n"
           "            uint idx0 = p.consecutive_pairs != 0u ? rope_start + 2u * j : rope_start + j;\n"
           "            uint idx1 = p.consecutive_pairs != 0u ? idx0 + 1u : rope_start + rope_half + j;\n"
           "            if (idx1 >= p.row_dim) continue;\n"
           "            float freq = 1.0f / pow(p.theta, float(2u * j) / float(p.rope_dim));\n"
           "            float angle = float(row_pos) * p.freq_scale * freq;\n"
           "            float c = cos(angle); float s = sin(angle);\n"
           "            float x0 = compressed[block * p.row_dim + idx0];\n"
           "            float x1 = compressed[block * p.row_dim + idx1];\n"
           "            compressed[block * p.row_dim + idx0] = x0 * c - x1 * s;\n"
           "            compressed[block * p.row_dim + idx1] = x0 * s + x1 * c;\n"
           "        }\n"
           "    }\n"
           "}\n"
           "kernel void termite_compressed_attention_select_compressed(device const float *index_kv [[buffer(0)]], device const uint *index_positions [[buffer(1)]], device const float *index_query [[buffer(2)]], device const float *index_head_weights [[buffer(3)]], device uint *selected_idx [[buffer(4)]], device float *selected_score [[buffer(5)]], constant termite_metal_compressed_attention_params &p [[buffer(6)]], uint qr [[thread_position_in_grid]]) {\n"
           "    if (qr >= p.query_rows) return;\n"
           "    uint limit = min(p.top_k, 512u);\n"
           "    uint query_abs = p.query_abs_start + qr;\n"
           "    for (uint slot = 0u; slot < limit; ++slot) { selected_idx[qr * p.top_k + slot] = 0xffffffffu; selected_score[qr * p.top_k + slot] = -3.402823466e+38f; }\n"
           "    if (limit == 0u) return;\n"
           "    uint selected_count = 0u;\n"
           "    for (uint cr = 0u; cr < p.index_rows; ++cr) {\n"
           "        if (index_positions[cr] > query_abs) continue;\n"
           "        float score = 0.0f;\n"
           "        for (uint ih = 0u; ih < p.index_heads; ++ih) {\n"
           "            float dot = 0.0f;\n"
           "            for (uint d = 0u; d < p.index_head_dim; ++d) dot += index_query[qr * p.index_heads * p.index_head_dim + ih * p.index_head_dim + d] * index_kv[cr * p.index_head_dim + d];\n"
           "            float w = 1.0f / (1.0f + exp(-index_head_weights[qr * p.index_heads + ih]));\n"
           "            score += w * dot * rsqrt(float(p.index_head_dim));\n"
           "        }\n"
           "        uint dst = selected_count;\n"
           "        if (selected_count < limit) selected_count++;\n"
           "        else { dst = 0u; for (uint i = 1u; i < limit; ++i) if (selected_score[qr * p.top_k + i] < selected_score[qr * p.top_k + dst]) dst = i; if (score <= selected_score[qr * p.top_k + dst]) continue; }\n"
           "        selected_idx[qr * p.top_k + dst] = cr;\n"
           "        selected_score[qr * p.top_k + dst] = score;\n"
           "    }\n"
           "}\n"
           "kernel void termite_compressed_attention_hybrid_attention(device const float *q [[buffer(0)]], device const float *local_kv [[buffer(1)]], device const float *compressed [[buffer(2)]], device const uint *positions [[buffer(3)]], device const uint *selected_idx [[buffer(4)]], device const float *sinks [[buffer(5)]], device float *output [[buffer(6)]], constant termite_metal_compressed_attention_params &p [[buffer(7)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.query_rows * p.num_heads;\n"
           "    if (gid >= total) return;\n"
           "    uint qr = gid / p.num_heads;\n"
           "    uint h = gid - qr * p.num_heads;\n"
           "    uint q_width = p.num_heads * p.head_dim;\n"
           "    uint q_base = qr * q_width + h * p.head_dim;\n"
           "    uint query_abs = p.query_abs_start + qr;\n"
           "    uint local_start = (p.sliding_window != 0u && query_abs + 1u > p.sliding_window) ? (query_abs + 1u - p.sliding_window) : 0u;\n"
           "    uint local_end = min(query_abs + 1u, p.token_count);\n"
           "    uint limit = min(p.top_k, 512u);\n"
           "    float best = p.has_sinks != 0u ? sinks[h] : -3.402823466e+38f;\n"
           "    for (uint kr = local_start; kr < local_end; ++kr) { float acc = 0.0f; for (uint d = 0u; d < p.head_dim; ++d) acc += q[q_base + d] * local_kv[kr * p.head_dim + d]; best = max(best, acc * p.scale); }\n"
           "    if (p.has_indexer != 0u) { for (uint si = 0u; si < limit; ++si) { uint cr = selected_idx[qr * p.top_k + si]; if (cr == 0xffffffffu) continue; float acc = 0.0f; for (uint d = 0u; d < p.head_dim; ++d) acc += q[q_base + d] * compressed[cr * p.head_dim + d]; best = max(best, acc * p.scale); } }\n"
           "    else { for (uint cr = 0u; cr < p.compressed_rows; ++cr) { if (positions[cr] > query_abs) continue; float acc = 0.0f; for (uint d = 0u; d < p.head_dim; ++d) acc += q[q_base + d] * compressed[cr * p.head_dim + d]; best = max(best, acc * p.scale); } }\n"
           "    float denom = p.has_sinks != 0u ? exp(sinks[h] - best) : 0.0f;\n"
           "    for (uint kr = local_start; kr < local_end; ++kr) { float acc = 0.0f; for (uint d = 0u; d < p.head_dim; ++d) acc += q[q_base + d] * local_kv[kr * p.head_dim + d]; denom += exp(acc * p.scale - best); }\n"
           "    if (p.has_indexer != 0u) { for (uint si = 0u; si < limit; ++si) { uint cr = selected_idx[qr * p.top_k + si]; if (cr == 0xffffffffu) continue; float acc = 0.0f; for (uint d = 0u; d < p.head_dim; ++d) acc += q[q_base + d] * compressed[cr * p.head_dim + d]; denom += exp(acc * p.scale - best); } }\n"
           "    else { for (uint cr = 0u; cr < p.compressed_rows; ++cr) { if (positions[cr] > query_abs) continue; float acc = 0.0f; for (uint d = 0u; d < p.head_dim; ++d) acc += q[q_base + d] * compressed[cr * p.head_dim + d]; denom += exp(acc * p.scale - best); } }\n"
           "    float inv = denom > 0.0f ? 1.0f / denom : 0.0f;\n"
           "    for (uint d = 0u; d < p.head_dim; ++d) { float sum = 0.0f; for (uint kr = local_start; kr < local_end; ++kr) { float acc = 0.0f; for (uint kd = 0u; kd < p.head_dim; ++kd) acc += q[q_base + kd] * local_kv[kr * p.head_dim + kd]; sum += exp(acc * p.scale - best) * inv * local_kv[kr * p.head_dim + d]; } if (p.has_indexer != 0u) { for (uint si = 0u; si < limit; ++si) { uint cr = selected_idx[qr * p.top_k + si]; if (cr == 0xffffffffu) continue; float acc = 0.0f; for (uint kd = 0u; kd < p.head_dim; ++kd) acc += q[q_base + kd] * compressed[cr * p.head_dim + kd]; sum += exp(acc * p.scale - best) * inv * compressed[cr * p.head_dim + d]; } } else { for (uint cr = 0u; cr < p.compressed_rows; ++cr) { if (positions[cr] > query_abs) continue; float acc = 0.0f; for (uint kd = 0u; kd < p.head_dim; ++kd) acc += q[q_base + kd] * compressed[cr * p.head_dim + kd]; sum += exp(acc * p.scale - best) * inv * compressed[cr * p.head_dim + d]; } } output[qr * q_width + h * p.head_dim + d] = sum; }\n"
           "}\n"
           "kernel void termite_attention_f32_prefill_tiled(device const float *q [[buffer(0)]], device const float *k [[buffer(1)]], device const float *v [[buffer(2)]], device float *output [[buffer(3)]], device const float *bias [[buffer(4)]], device const uchar *mask [[buffer(5)]], constant termite_metal_attention_f32_params &p [[buffer(6)]], threadgroup float *shmem [[threadgroup(0)]], ushort tid [[thread_index_in_threadgroup]], uint2 tg [[threadgroup_position_in_grid]]) {\n"
           "    const uint qi = tg.x; const uint h = tg.y; if (qi >= p.q_len || h >= p.num_heads) return;\n"
           "    const uint NT = 256u; const uint heads_per_group = p.num_heads / p.num_kv_heads; const uint kv_h = h / heads_per_group; const uint q_stride = p.num_heads * p.head_dim; const uint kv_stride = p.num_kv_heads * p.head_dim; const uint q_base = qi * q_stride + h * p.head_dim; const uint kv_head_off = kv_h * p.head_dim; const uint out_base = qi * q_stride + h * p.head_dim; const float scale = rsqrt(float(p.head_dim)); const uint query_pos = p.query_position_offset + qi; threadgroup float *scores = shmem; threadgroup float *partials = shmem + p.kv_len;\n"
           "    float local_best = -3.402823466e+38f;\n"
           "    for (uint ki = 0u; ki < p.kv_len; ++ki) { uint key_pos = p.kv_position_offset + ki; bool allowed = key_pos <= query_pos; if (!allowed && p.has_mask != 0u && query_pos < p.total_sequence_len && key_pos < p.total_sequence_len) { uint mask_idx = query_pos * p.total_sequence_len + key_pos; allowed = mask[mask_idx] != 0u; } if (key_pos > query_pos) allowed = false; if (p.sliding_window != 0u && allowed) allowed = (query_pos - key_pos) < p.sliding_window; float acc = 0.0f; if (allowed) { uint k_base = ki * kv_stride + kv_head_off; for (uint d = tid; d < p.head_dim; d += NT) acc += q[q_base + d] * k[k_base + d]; } partials[tid] = acc; threadgroup_barrier(mem_flags::mem_threadgroup); for (uint stride = NT >> 1u; stride > 0u; stride >>= 1u) { if (tid < stride) partials[tid] += partials[tid + stride]; threadgroup_barrier(mem_flags::mem_threadgroup); } float score = allowed ? partials[0] * scale : -3.402823466e+38f; if (allowed && p.has_bias != 0u) score += bias[h * p.q_len * p.kv_len + qi * p.kv_len + key_pos]; if (tid == 0u) scores[ki] = score; if (score > local_best) local_best = score; threadgroup_barrier(mem_flags::mem_threadgroup); }\n"
           "    if (tid == 0u) { float sum = 0.0f; for (uint ki = 0u; ki < p.kv_len; ++ki) sum += exp(scores[ki] - local_best); partials[0] = sum > 0.0f ? 1.0f / sum : 0.0f; } threadgroup_barrier(mem_flags::mem_threadgroup); const float inv_sum = partials[0];\n"
           "    for (uint d = tid; d < p.head_dim; d += NT) { float value = 0.0f; for (uint ki = 0u; ki < p.kv_len; ++ki) { float e = exp(scores[ki] - local_best) * inv_sum; uint v_base = ki * kv_stride + kv_head_off; value += e * v[v_base + d]; } output[out_base + d] = value; }\n"
           "}\n"
           "inline uint termite_attention_page_token(device const uint *block_table, constant termite_metal_paged_attention_params &p, uint logical_token) {\n"
           "    uint logical_block = logical_token / p.page_size;\n"
           "    uint token_in_block = logical_token - logical_block * p.page_size;\n"
           "    if (logical_block >= p.block_count) return 0xffffffffu;\n"
           "    return block_table[logical_block] + token_in_block;\n"
           "}\n"
           "inline float termite_paged_attention_key_scalar(device const uchar *encoded_key, constant termite_metal_paged_attention_params &p, uint physical_token, uint kv_h, uint d) {\n"
           "    uint value_index = kv_h * p.head_dim + d;\n"
           "    uint k_base = physical_token * p.key_row_bytes;\n"
           "    if (p.format == 0u) { uchar packed = encoded_key[k_base + (value_index >> 1)]; uint code = ((value_index & 1u) == 0u) ? uint(packed & 0x0Fu) : uint((packed >> 4) & 0x0Fu); return float(code) / 7.5f - 1.0f; }\n"
           "    if (p.format == 1u) { uint bit_offset = value_index * 3u; uint byte_index = k_base + (bit_offset >> 3); uint shift = bit_offset & 7u; uint bits = uint(encoded_key[byte_index]) >> shift; if (shift > 5u) bits |= uint(encoded_key[byte_index + 1u]) << (8u - shift); return float(bits & 0x07u) / 3.5f - 1.0f; }\n"
           "    if (p.format == 2u) { const device float *ptr = reinterpret_cast<const device float *>(encoded_key + k_base); return ptr[value_index]; }\n"
           "    if (p.format == 3u) { const device half *ptr = reinterpret_cast<const device half *>(encoded_key + k_base); return float(ptr[value_index]); }\n"
           "    if (p.format == 4u) { uint head_base = k_base + kv_h * (p.head_dim + 4u); const device float *scale_ptr = reinterpret_cast<const device float *>(encoded_key + head_base); const device char *q_ptr = reinterpret_cast<const device char *>(encoded_key + head_base + 4u); return scale_ptr[0] * float(q_ptr[d]); }\n"
           "    return 0.0f;\n"
           "}\n"
           "inline float termite_paged_attention_value_scalar(device const uchar *v_bytes, constant termite_metal_paged_attention_params &p, uint physical_token, uint kv_h, uint d) {\n"
           "    uint value_index = kv_h * p.head_dim + d;\n"
           "    uint byte_base = physical_token * p.v_row_stride * p.v_element_bytes;\n"
           "    if (p.v_element_bytes == 2u) { const device half *ptr = reinterpret_cast<const device half *>(v_bytes + byte_base); return float(ptr[value_index]); }\n"
           "    const device float *ptr = reinterpret_cast<const device float *>(v_bytes + byte_base);\n"
           "    return ptr[value_index];\n"
           "}\n"
           "inline float termite_paged_attention_score(device const float *q, device const uchar *encoded_key, constant termite_metal_paged_attention_params &p, uint q_base, uint physical_token, uint kv_h, thread float residual_projection[32]) {\n"
           "    float acc = 0.0f;\n"
           "    for (uint d = 0u; d < p.head_dim; ++d) acc += q[q_base + d] * termite_paged_attention_key_scalar(encoded_key, p, physical_token, kv_h, d);\n"
           "    if (p.format == 1u) { uint k_base = physical_token * p.key_row_bytes; uint residual_base = k_base + p.base_key_row_bytes; float residual_acc = 0.0f; for (uint projection = 0u; projection < 32u; ++projection) { uint residual_bit = kv_h * 32u + projection; uchar residual_byte = encoded_key[residual_base + (residual_bit >> 3)]; float residual_sign = ((residual_byte >> (residual_bit & 7u)) & 1u) != 0u ? 1.0f : -1.0f; residual_acc += residual_sign * residual_projection[projection]; } acc += 0.125f * (residual_acc / 32.0f); }\n"
           "    return acc * rsqrt(float(p.head_dim));\n"
           "}\n"
           "kernel void termite_paged_attention_kv(device const float *q [[buffer(0)]], device const uchar *encoded_key [[buffer(1)]], device const uchar *v_bytes [[buffer(2)]], device const uint *block_table [[buffer(3)]], device const float *sinks [[buffer(4)]], device float *output [[buffer(5)]], constant termite_metal_paged_attention_params &p [[buffer(6)]], uint gid [[thread_position_in_grid]]) {\n"
           "    uint total = p.q_len * p.num_heads; if (gid >= total || p.page_size == 0u || p.num_kv_heads == 0u) return;\n"
           "    uint qi = gid / p.num_heads; uint h = gid - qi * p.num_heads; uint heads_per_group = p.num_heads / p.num_kv_heads; uint kv_h = h / heads_per_group; uint q_stride = p.num_heads * p.head_dim; uint q_base = qi * q_stride + h * p.head_dim; uint out_base = qi * q_stride + h * p.head_dim; uint query_pos = p.query_position_offset + qi;\n"
           "    thread float residual_projection[32];\n"
           "    if (p.format == 1u) { for (uint projection = 0u; projection < 32u; ++projection) { float projected_query = 0.0f; for (uint d = 0u; d < p.head_dim; ++d) { ulong x = (ulong(kv_h + 1u) * 0x9e3779b97f4a7c15UL); x ^= (ulong(projection + 1u) * 0xbf58476d1ce4e5b9UL); x ^= (ulong(d + 1u) * 0x94d049bb133111ebUL); x ^= x >> 30; x *= 0xbf58476d1ce4e5b9UL; x ^= x >> 27; x *= 0x94d049bb133111ebUL; x ^= x >> 31; float sign = ((x & 1UL) == 0UL) ? 1.0f : -1.0f; projected_query += sign * q[q_base + d]; } residual_projection[projection] = projected_query; } } else { for (uint projection = 0u; projection < 32u; ++projection) residual_projection[projection] = 0.0f; }\n"
           "    float best = -3.402823466e+38f;\n"
           "    for (uint ki = 0u; ki < p.kv_tokens; ++ki) { uint key_pos = p.kv_position_offset + ki; bool allowed = key_pos <= query_pos; if (p.sliding_window != 0u && allowed) allowed = (query_pos - key_pos) < p.sliding_window; if (!allowed) continue; uint physical_token = termite_attention_page_token(block_table, p, ki); if (physical_token == 0xffffffffu) continue; float score = termite_paged_attention_score(q, encoded_key, p, q_base, physical_token, kv_h, residual_projection); if (p.has_sinks != 0u) score += sinks[h]; if (p.softcap > 0.0f) score = tanh(score / p.softcap) * p.softcap; if (!isfinite(score)) score = -3.402823466e+38f; if (score > best) best = score; }\n"
           "    for (uint d = 0u; d < p.head_dim; ++d) output[out_base + d] = 0.0f;\n"
           "    float sum = 0.0f;\n"
           "    for (uint ki = 0u; ki < p.kv_tokens; ++ki) { uint key_pos = p.kv_position_offset + ki; bool allowed = key_pos <= query_pos; if (p.sliding_window != 0u && allowed) allowed = (query_pos - key_pos) < p.sliding_window; if (!allowed) continue; uint physical_token = termite_attention_page_token(block_table, p, ki); if (physical_token == 0xffffffffu) continue; float score = termite_paged_attention_score(q, encoded_key, p, q_base, physical_token, kv_h, residual_projection); if (p.has_sinks != 0u) score += sinks[h]; if (p.softcap > 0.0f) score = tanh(score / p.softcap) * p.softcap; if (!isfinite(score)) continue; float e = exp(score - best); if (!isfinite(e)) continue; sum += e; for (uint d = 0u; d < p.head_dim; ++d) output[out_base + d] += e * termite_paged_attention_value_scalar(v_bytes, p, physical_token, kv_h, d); }\n"
           "    float inv_sum = sum > 0.0f ? 1.0f / sum : 0.0f;\n"
           "    for (uint d = 0u; d < p.head_dim; ++d) output[out_base + d] *= inv_sum;\n"
           "}\n"
           "kernel void termite_polar4_attention_span(device const float *q [[buffer(0)]], device const uchar *encoded_key [[buffer(1)]], device const float *v [[buffer(2)]], device float *output [[buffer(3)]], constant termite_metal_attention_span_params &p [[buffer(4)]], uint h [[thread_position_in_grid]]) {\n"
           "    if (h >= p.num_heads) return;\n"
           "    uint heads_per_group = p.num_heads / p.num_kv_heads;\n"
           "    uint kv_h = h / heads_per_group;\n"
           "    uint kv_head_off = kv_h * p.head_dim;\n"
           "    uint q_base = h * p.head_dim;\n"
           "    float best = -3.402823466e+38f;\n"
           "    float scale = rsqrt(float(p.head_dim));\n"
           "    for (uint ki = 0; ki < p.kv_tokens; ++ki) {\n"
           "        uint key_pos = p.kv_position_offset + ki;\n"
           "        bool allowed = key_pos <= p.query_position;\n"
           "        if (p.sliding_window != 0u && allowed) {\n"
           "            allowed = (p.query_position - key_pos) < p.sliding_window;\n"
           "        }\n"
           "        float score = -3.402823466e+38f;\n"
           "        if (allowed) {\n"
           "            uint k_base = ki * p.key_row_bytes;\n"
           "            float acc = 0.0f;\n"
           "            for (uint d = 0; d < p.head_dim; ++d) {\n"
           "                uint value_index = kv_head_off + d;\n"
           "                uchar packed = encoded_key[k_base + (value_index >> 1)];\n"
           "                uint code = ((value_index & 1u) == 0u) ? uint(packed & 0x0Fu) : uint((packed >> 4) & 0x0Fu);\n"
           "                float k = float(code) / 7.5f - 1.0f;\n"
           "                acc += q[q_base + d] * k;\n"
           "            }\n"
           "            score = acc * scale;\n"
           "            if (!isfinite(score)) score = -3.402823466e+38f;\n"
           "        }\n"
           "        if (score > best) best = score;\n"
           "    }\n"
           "    for (uint d = 0; d < p.head_dim; ++d) {\n"
           "        output[h * p.head_dim + d] = 0.0f;\n"
           "    }\n"
           "    float sum = 0.0f;\n"
           "    for (uint ki = 0; ki < p.kv_tokens; ++ki) {\n"
           "        uint key_pos = p.kv_position_offset + ki;\n"
           "        bool allowed = key_pos <= p.query_position;\n"
           "        if (p.sliding_window != 0u && allowed) {\n"
           "            allowed = (p.query_position - key_pos) < p.sliding_window;\n"
           "        }\n"
           "        if (!allowed) continue;\n"
           "        uint k_base = ki * p.key_row_bytes;\n"
           "        float score = 0.0f;\n"
           "        for (uint d = 0; d < p.head_dim; ++d) {\n"
           "            uint value_index = kv_head_off + d;\n"
           "            uchar packed = encoded_key[k_base + (value_index >> 1)];\n"
           "            uint code = ((value_index & 1u) == 0u) ? uint(packed & 0x0Fu) : uint((packed >> 4) & 0x0Fu);\n"
           "            float k = float(code) / 7.5f - 1.0f;\n"
           "            score += q[q_base + d] * k;\n"
           "        }\n"
           "        score = score * scale;\n"
           "        if (!isfinite(score)) continue;\n"
           "        float e = exp(score - best);\n"
           "        if (!isfinite(e)) continue;\n"
           "        sum += e;\n"
           "        uint v_base = ki * p.v_row_stride + kv_head_off;\n"
           "        for (uint d = 0; d < p.head_dim; ++d) {\n"
           "            output[h * p.head_dim + d] += e * v[v_base + d];\n"
           "        }\n"
           "    }\n"
           "    float inv_sum = sum > 0.0f ? 1.0f / sum : 0.0f;\n"
           "    for (uint d = 0; d < p.head_dim; ++d) {\n"
           "        output[h * p.head_dim + d] *= inv_sum;\n"
           "    }\n"
           "}\n"
           "kernel void termite_turbo3_attention_span(device const float *q [[buffer(0)]], device const uchar *encoded_key [[buffer(1)]], device const float *v [[buffer(2)]], device float *output [[buffer(3)]], constant termite_metal_attention_span_params &p [[buffer(4)]], uint h [[thread_position_in_grid]]) {\n"
           "    if (h >= p.num_heads) return;\n"
           "    uint heads_per_group = p.num_heads / p.num_kv_heads;\n"
           "    uint kv_h = h / heads_per_group;\n"
           "    uint kv_head_off = kv_h * p.head_dim;\n"
           "    uint q_base = h * p.head_dim;\n"
           "    float residual_projection[32];\n"
           "    for (uint projection = 0; projection < 32u; ++projection) {\n"
           "        float projected_query = 0.0f;\n"
           "        for (uint d = 0; d < p.head_dim; ++d) {\n"
           "            ulong x = (ulong(kv_h + 1u) * 0x9e3779b97f4a7c15UL);\n"
           "            x ^= (ulong(projection + 1u) * 0xbf58476d1ce4e5b9UL);\n"
           "            x ^= (ulong(d + 1u) * 0x94d049bb133111ebUL);\n"
           "            x ^= x >> 30;\n"
           "            x *= 0xbf58476d1ce4e5b9UL;\n"
           "            x ^= x >> 27;\n"
           "            x *= 0x94d049bb133111ebUL;\n"
           "            x ^= x >> 31;\n"
           "            float sign = ((x & 1UL) == 0UL) ? 1.0f : -1.0f;\n"
           "            projected_query += sign * q[q_base + d];\n"
           "        }\n"
           "        residual_projection[projection] = projected_query;\n"
           "    }\n"
           "    float best = -3.402823466e+38f;\n"
           "    float scale = rsqrt(float(p.head_dim));\n"
           "    for (uint ki = 0; ki < p.kv_tokens; ++ki) {\n"
           "        uint key_pos = p.kv_position_offset + ki;\n"
           "        bool allowed = key_pos <= p.query_position;\n"
           "        if (p.sliding_window != 0u && allowed) {\n"
           "            allowed = (p.query_position - key_pos) < p.sliding_window;\n"
           "        }\n"
           "        float score = -3.402823466e+38f;\n"
           "        if (allowed) {\n"
           "            uint k_base = ki * p.key_row_bytes;\n"
           "            uint residual_base = k_base + p.base_key_row_bytes;\n"
           "            float acc = 0.0f;\n"
           "            for (uint d = 0; d < p.head_dim; ++d) {\n"
           "                uint value_index = kv_head_off + d;\n"
           "                uint bit_offset = value_index * 3u;\n"
           "                uint byte_index = k_base + (bit_offset >> 3);\n"
           "                uint shift = bit_offset & 7u;\n"
           "                uint bits = uint(encoded_key[byte_index]) >> shift;\n"
           "                if (shift > 5u) bits |= uint(encoded_key[byte_index + 1u]) << (8u - shift);\n"
           "                float k = float(bits & 0x07u) / 3.5f - 1.0f;\n"
           "                acc += q[q_base + d] * k;\n"
           "            }\n"
           "            float residual_acc = 0.0f;\n"
           "            for (uint projection = 0; projection < 32u; ++projection) {\n"
           "                uint residual_bit = kv_h * 32u + projection;\n"
           "                uchar residual_byte = encoded_key[residual_base + (residual_bit >> 3)];\n"
           "                float residual_sign = ((residual_byte >> (residual_bit & 7u)) & 1u) != 0u ? 1.0f : -1.0f;\n"
           "                residual_acc += residual_sign * residual_projection[projection];\n"
           "            }\n"
           "            score = (acc + 0.125f * (residual_acc / 32.0f)) * scale;\n"
           "            if (!isfinite(score)) score = -3.402823466e+38f;\n"
           "        }\n"
           "        if (score > best) best = score;\n"
           "    }\n"
           "    for (uint d = 0; d < p.head_dim; ++d) {\n"
           "        output[h * p.head_dim + d] = 0.0f;\n"
           "    }\n"
           "    float sum = 0.0f;\n"
           "    for (uint ki = 0; ki < p.kv_tokens; ++ki) {\n"
           "        uint key_pos = p.kv_position_offset + ki;\n"
           "        bool allowed = key_pos <= p.query_position;\n"
           "        if (p.sliding_window != 0u && allowed) {\n"
           "            allowed = (p.query_position - key_pos) < p.sliding_window;\n"
           "        }\n"
           "        if (!allowed) continue;\n"
           "        uint k_base = ki * p.key_row_bytes;\n"
           "        uint residual_base = k_base + p.base_key_row_bytes;\n"
           "        float score = 0.0f;\n"
           "        for (uint d = 0; d < p.head_dim; ++d) {\n"
           "            uint value_index = kv_head_off + d;\n"
           "            uint bit_offset = value_index * 3u;\n"
           "            uint byte_index = k_base + (bit_offset >> 3);\n"
           "            uint shift = bit_offset & 7u;\n"
           "            uint bits = uint(encoded_key[byte_index]) >> shift;\n"
           "            if (shift > 5u) bits |= uint(encoded_key[byte_index + 1u]) << (8u - shift);\n"
           "            float k = float(bits & 0x07u) / 3.5f - 1.0f;\n"
           "            score += q[q_base + d] * k;\n"
           "        }\n"
           "        float residual_acc = 0.0f;\n"
           "        for (uint projection = 0; projection < 32u; ++projection) {\n"
           "            uint residual_bit = kv_h * 32u + projection;\n"
           "            uchar residual_byte = encoded_key[residual_base + (residual_bit >> 3)];\n"
           "            float residual_sign = ((residual_byte >> (residual_bit & 7u)) & 1u) != 0u ? 1.0f : -1.0f;\n"
           "            residual_acc += residual_sign * residual_projection[projection];\n"
           "        }\n"
           "        score = (score + 0.125f * (residual_acc / 32.0f)) * scale;\n"
           "        if (!isfinite(score)) continue;\n"
           "        float e = exp(score - best);\n"
           "        if (!isfinite(e)) continue;\n"
           "        sum += e;\n"
           "        uint v_base = ki * p.v_row_stride + kv_head_off;\n"
           "        for (uint d = 0; d < p.head_dim; ++d) {\n"
           "            output[h * p.head_dim + d] += e * v[v_base + d];\n"
           "        }\n"
           "    }\n"
           "    float inv_sum = sum > 0.0f ? 1.0f / sum : 0.0f;\n"
           "    for (uint d = 0; d < p.head_dim; ++d) {\n"
           "        output[h * p.head_dim + d] *= inv_sum;\n"
           "    }\n"
           "}\n";
}

static id<MTLBuffer> termite_metal_make_private_buffer_with_bytes(
    id<MTLDevice> device,
    id<MTLCommandQueue> queue,
    const void *bytes,
    size_t length
) {
    if (device == nil || queue == nil || bytes == NULL || length == 0) return nil;
    @autoreleasepool {
        id<MTLBuffer> staging = [device newBufferWithBytes:bytes length:length options:MTLResourceStorageModeShared];
        if (staging == nil) return nil;
        id<MTLBuffer> dst = [device newBufferWithLength:length options:MTLResourceStorageModePrivate];
        if (dst == nil) return nil;
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(queue, __func__);
        if (command_buffer == nil) return nil;
        termite_metal_record_active_frame_blit_source(command_buffer, TERMITE_METAL_BLIT_SOURCE_OTHER);
        id<MTLBlitCommandEncoder> blit = termite_metal_tracked_blit_command_encoder(command_buffer);
        if (blit == nil) return nil;
        if ([blit respondsToSelector:@selector(setLabel:)]) {
            blit.label = @"private-buffer-upload";
        }
        [blit copyFromBuffer:staging sourceOffset:0 toBuffer:dst destinationOffset:0 size:length];
        [blit endEncoding];
        return termite_metal_commit_and_wait(command_buffer, __func__, -1) == 0 ? dst : nil;
    }
}

static bool termite_metal_can_make_no_copy_buffer(const void *bytes, size_t length) {
    if (bytes == NULL || length == 0) return false;
    long page_size = sysconf(_SC_PAGESIZE);
    if (page_size <= 0) page_size = 4096;
    const uintptr_t ptr = (uintptr_t)bytes;
    const uintptr_t page = (uintptr_t)page_size;
    return (ptr % page) == 0 && (length % (size_t)page_size) == 0;
}

typedef struct termite_metal_host_staging_slice {
    id<MTLBuffer> buffer;
    size_t offset;
} termite_metal_host_staging_slice;

static size_t termite_metal_align_forward_size(size_t value, size_t alignment) {
    if (alignment <= 1) return value;
    const size_t mask = alignment - 1;
    if ((alignment & mask) != 0) return value;
    if (value > SIZE_MAX - mask) return SIZE_MAX;
    return (value + mask) & ~mask;
}

static int termite_metal_decode_runtime_ensure_host_staging_buffer(
    termite_metal_decode_runtime *runtime,
    bool frame_owned,
    size_t required
) {
    if (runtime == NULL || runtime->device == nil || required == 0) return -1;
    id<MTLBuffer> current_buffer = frame_owned ? runtime->host_staging_buffer : runtime->active_frame_host_staging_buffer;
    size_t current_capacity = frame_owned ? runtime->host_staging_capacity : runtime->active_frame_host_staging_capacity;
    if (current_buffer != nil && current_capacity >= required) return 0;

    size_t new_capacity = current_capacity;
    if (new_capacity < 4096) new_capacity = 4096;
    while (new_capacity < required) {
        if (new_capacity > SIZE_MAX / 2) {
            new_capacity = required;
            break;
        }
        new_capacity *= 2;
    }

    id<MTLBuffer> new_buffer = [runtime->device newBufferWithLength:new_capacity options:MTLResourceStorageModeShared];
    if (new_buffer == nil) return -2;
    if (frame_owned) {
        runtime->host_staging_buffer = new_buffer;
        runtime->host_staging_capacity = new_capacity;
    } else {
        runtime->active_frame_host_staging_buffer = new_buffer;
        runtime->active_frame_host_staging_capacity = new_capacity;
    }
    return 0;
}

static int termite_metal_decode_runtime_stage_host_bytes(
    termite_metal_decode_runtime *runtime,
    const void *bytes,
    size_t length,
    bool frame_owned,
    termite_metal_host_staging_slice *slice
) {
    if (runtime == NULL || bytes == NULL || length == 0 || slice == NULL) return -1;
    const size_t alignment = 256;
    size_t offset = 0;
    if (!frame_owned) {
        offset = termite_metal_align_forward_size(runtime->active_frame_host_staging_offset, alignment);
        if (offset == SIZE_MAX || length > SIZE_MAX - offset) return -2;
    }
    const size_t required = offset + length;
    if (termite_metal_decode_runtime_ensure_host_staging_buffer(runtime, frame_owned, required) != 0) return -3;

    id<MTLBuffer> buffer = frame_owned ? runtime->host_staging_buffer : runtime->active_frame_host_staging_buffer;
    if (buffer == nil || buffer.contents == NULL || offset + length > buffer.length) return -4;
    memcpy((uint8_t *)buffer.contents + offset, bytes, length);
    if (buffer.storageMode == MTLStorageModeManaged) {
        [buffer didModifyRange:NSMakeRange(offset, length)];
    }

    if (!frame_owned) {
        runtime->active_frame_host_staging_offset = termite_metal_align_forward_size(required, alignment);
        if (runtime->active_frame_host_staging_offset == SIZE_MAX) return -5;
        if (runtime->active_frame_host_staging_retained_buffer != buffer) {
            if (termite_metal_decode_runtime_retain_frame_resource(runtime, buffer) != 0) return -6;
            runtime->active_frame_host_staging_retained_buffer = buffer;
        }
    }

    slice->buffer = buffer;
    slice->offset = offset;
    return 0;
}

static int termite_metal_upload_private_buffer(
    id<MTLDevice> device,
    id<MTLCommandQueue> queue,
    id<MTLBuffer> dst,
    const void *bytes,
    size_t length
) {
    if (device == nil || queue == nil || dst == nil || bytes == NULL || length == 0) return -1;
    @autoreleasepool {
        if (dst.length < length) return -2;
        id<MTLBuffer> staging = [device newBufferWithBytes:bytes length:length options:MTLResourceStorageModeShared];
        if (staging == nil) return -3;
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(queue, __func__);
        if (command_buffer == nil) return -4;
        termite_metal_record_active_frame_blit_source(command_buffer, TERMITE_METAL_BLIT_SOURCE_OTHER);
        id<MTLBlitCommandEncoder> blit = termite_metal_tracked_blit_command_encoder(command_buffer);
        if (blit == nil) return -5;
        if ([blit respondsToSelector:@selector(setLabel:)]) {
            blit.label = @"private-buffer-upload";
        }
        [blit copyFromBuffer:staging sourceOffset:0 toBuffer:dst destinationOffset:0 size:length];
        [blit endEncoding];
        return termite_metal_commit_and_wait(command_buffer, __func__, -6);
    }
}

static void termite_metal_decode_runtime_memory_add_buffer(
    termite_metal_decode_runtime_memory_stats *snapshot,
    id<MTLBuffer> buffer,
    uint64_t *category
) {
    if (snapshot == NULL || buffer == nil) return;
    const uint64_t bytes = (uint64_t)buffer.length;
    snapshot->buffer_count += 1;
    snapshot->total_bytes += bytes;
    if (category != NULL) {
        *category += bytes;
    }
}

static void termite_metal_decode_runtime_memory_add_resources(
    termite_metal_decode_runtime_memory_stats *snapshot,
    CFMutableArrayRef resources,
    uint64_t *category
) {
    if (snapshot == NULL || resources == NULL) return;
    const CFIndex count = CFArrayGetCount(resources);
    for (CFIndex i = 0; i < count; ++i) {
        id resource = (__bridge id)CFArrayGetValueAtIndex(resources, i);
        if ([resource conformsToProtocol:@protocol(MTLBuffer)]) {
            termite_metal_decode_runtime_memory_add_buffer(snapshot, (id<MTLBuffer>)resource, category);
        }
    }
}

static termite_metal_decode_runtime *termite_metal_active_encoder_runtime = NULL;

static bool termite_metal_trace_unscoped_compute_enabled(void) {
    const char *enabled = getenv("TERMITE_METAL_TRACE_UNSCOPED_COMPUTE");
    return enabled != NULL && enabled[0] != '\0' && strcmp(enabled, "0") != 0;
}

static bool termite_metal_trace_frame_blits_enabled(void) {
    const char *enabled = getenv("TERMITE_METAL_TRACE_FRAME_BLITS");
    return enabled != NULL && enabled[0] != '\0' && strcmp(enabled, "0") != 0;
}

static bool termite_metal_trace_frame_lifecycle_enabled(void) {
    const char *enabled = getenv("TERMITE_METAL_TRACE_FRAME_LIFECYCLE");
    return enabled != NULL && enabled[0] != '\0' && strcmp(enabled, "0") != 0;
}

static bool termite_metal_coalesce_planned_compute_encoders_enabled(void) {
    const char *disabled = getenv("TERMITE_METAL_DISABLE_PLANNED_ENCODER_COALESCING");
    return disabled == NULL || disabled[0] == '\0' || strcmp(disabled, "0") == 0;
}

static bool termite_metal_planned_compute_barriers_enabled(termite_metal_decode_runtime *runtime) {
    const char *disabled = getenv("TERMITE_METAL_DISABLE_PLANNED_COMPUTE_BARRIERS");
    if (disabled != NULL && disabled[0] != '\0' && strcmp(disabled, "0") != 0) return false;
    if (runtime != NULL && runtime->planned_compute_barrier_suppression_depth != 0) return false;
    return true;
}

static void termite_metal_decode_runtime_reset_planned_compute_ranges(termite_metal_decode_runtime *runtime) {
    if (runtime == NULL) return;
    runtime->active_planned_compute_range_count = 0;
}

static bool termite_metal_size_mul(size_t a, size_t b, size_t *out) {
    if (out == NULL) return false;
    if (a != 0 && b > SIZE_MAX / a) return false;
    *out = a * b;
    return true;
}

static bool termite_metal_size_mul3(size_t a, size_t b, size_t c, size_t *out) {
    size_t ab = 0;
    return termite_metal_size_mul(a, b, &ab) && termite_metal_size_mul(ab, c, out);
}

static bool termite_metal_buffer_range_valid(id<MTLBuffer> buffer, size_t offset, size_t byte_count) {
    if (buffer == nil) return false;
    return offset <= buffer.length && byte_count <= buffer.length - offset;
}

static int termite_metal_planned_range_make(
    id<MTLBuffer> buffer,
    size_t offset,
    size_t byte_count,
    termite_metal_planned_range_type type,
    termite_metal_planned_encoder_range *out,
    int failure_code
) {
    if (out == NULL) return failure_code;
    out->buffer = nil;
    out->begin = 0;
    out->end = 0;
    out->type = type;
    if (buffer == nil || byte_count == 0) return 0;
    if (offset > buffer.length || byte_count > buffer.length - offset) return failure_code;
    out->buffer = buffer;
    out->begin = offset;
    out->end = offset + byte_count;
    return 0;
}

static bool termite_metal_planned_ranges_overlap(
    const termite_metal_planned_encoder_range *a,
    const termite_metal_planned_encoder_range *b
) {
    if (a == NULL || b == NULL || a->buffer == nil || b->buffer == nil) return false;
    if (a->buffer != b->buffer) return false;
    if (a->type == TERMITE_METAL_PLANNED_RANGE_READ && b->type == TERMITE_METAL_PLANNED_RANGE_READ) return false;
    return a->begin < b->end && b->begin < a->end;
}

static int termite_metal_decode_runtime_prepare_planned_compute_accesses(
    termite_metal_decode_runtime *runtime,
    const termite_metal_planned_encoder_range *accesses,
    size_t access_count,
    int failure_code
) {
    if (runtime == NULL || accesses == NULL || access_count == 0) return 0;
    if (runtime->active_frame_cb == nil || runtime->active_planned_compute_encoder == nil) return 0;

    bool conflict = false;
    for (size_t i = 0; !conflict && i < access_count; ++i) {
        const termite_metal_planned_encoder_range *access = &accesses[i];
        if (access->buffer == nil) continue;
        for (size_t j = 0; j < runtime->active_planned_compute_range_count; ++j) {
            if (termite_metal_planned_ranges_overlap(access, &runtime->active_planned_compute_ranges[j])) {
                conflict = true;
                break;
            }
        }
    }

    if (conflict || runtime->active_planned_compute_range_count + access_count > TERMITE_METAL_PLANNED_RANGE_CAPACITY) {
        id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
        if (encoder == nil) return failure_code;
        if (termite_metal_planned_compute_barriers_enabled(runtime)) {
            [encoder memoryBarrierWithScope:MTLBarrierScopeBuffers];
            runtime->active_frame_planned_barrier_count += 1;
        }
        termite_metal_decode_runtime_reset_planned_compute_ranges(runtime);
    }

    for (size_t i = 0; i < access_count; ++i) {
        const termite_metal_planned_encoder_range access = accesses[i];
        if (access.buffer == nil) continue;
        if (runtime->active_planned_compute_range_count >= TERMITE_METAL_PLANNED_RANGE_CAPACITY) return failure_code;
        runtime->active_planned_compute_ranges[runtime->active_planned_compute_range_count] = access;
        runtime->active_planned_compute_range_count += 1;
    }
    return 0;
}

static int termite_metal_decode_runtime_prepare_planned_compute_unary_accesses(
    termite_metal_decode_runtime *runtime,
    id<MTLBuffer> input_buffer,
    size_t input_offset,
    size_t input_bytes,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    size_t output_bytes,
    int failure_code
) {
    termite_metal_planned_encoder_range accesses[2];
    if (termite_metal_planned_range_make(input_buffer, input_offset, input_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[0], failure_code) != 0 ||
        termite_metal_planned_range_make(output_buffer, output_offset, output_bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &accesses[1], failure_code) != 0 ||
        termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, accesses, 2, failure_code) != 0)
    {
        return failure_code;
    }
    return 0;
}

static int termite_metal_decode_runtime_prepare_planned_compute_binary_accesses(
    termite_metal_decode_runtime *runtime,
    id<MTLBuffer> lhs_buffer,
    size_t lhs_offset,
    size_t lhs_bytes,
    id<MTLBuffer> rhs_buffer,
    size_t rhs_offset,
    size_t rhs_bytes,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    size_t output_bytes,
    int failure_code
) {
    termite_metal_planned_encoder_range accesses[3];
    if (termite_metal_planned_range_make(lhs_buffer, lhs_offset, lhs_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[0], failure_code) != 0 ||
        termite_metal_planned_range_make(rhs_buffer, rhs_offset, rhs_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[1], failure_code) != 0 ||
        termite_metal_planned_range_make(output_buffer, output_offset, output_bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &accesses[2], failure_code) != 0 ||
        termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, accesses, 3, failure_code) != 0)
    {
        return failure_code;
    }
    return 0;
}

static int termite_metal_decode_runtime_prepare_planned_compute_norm_accesses(
    termite_metal_decode_runtime *runtime,
    id<MTLBuffer> input_buffer,
    size_t input_offset,
    size_t input_bytes,
    id<MTLBuffer> weight_buffer,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    size_t output_bytes,
    int failure_code
) {
    if (weight_buffer == nil) return failure_code;
    termite_metal_planned_encoder_range accesses[3];
    if (termite_metal_planned_range_make(input_buffer, input_offset, input_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[0], failure_code) != 0 ||
        termite_metal_planned_range_make(weight_buffer, 0, weight_buffer.length, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[1], failure_code) != 0 ||
        termite_metal_planned_range_make(output_buffer, output_offset, output_bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &accesses[2], failure_code) != 0 ||
        termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, accesses, 3, failure_code) != 0)
    {
        return failure_code;
    }
    return 0;
}

static int termite_metal_decode_runtime_prepare_planned_compute_weight_norm_accesses(
    termite_metal_decode_runtime *runtime,
    id<MTLBuffer> input_buffer,
    size_t input_offset,
    size_t input_bytes,
    id<MTLBuffer> weight_buffer,
    size_t weight_offset,
    size_t weight_bytes,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    size_t output_bytes,
    int failure_code
) {
    termite_metal_planned_encoder_range accesses[3];
    if (termite_metal_planned_range_make(input_buffer, input_offset, input_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[0], failure_code) != 0 ||
        termite_metal_planned_range_make(weight_buffer, weight_offset, weight_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[1], failure_code) != 0 ||
        termite_metal_planned_range_make(output_buffer, output_offset, output_bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &accesses[2], failure_code) != 0 ||
        termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, accesses, 3, failure_code) != 0)
    {
        return failure_code;
    }
    return 0;
}

static int termite_metal_decode_runtime_prepare_planned_compute_ternary_accesses(
    termite_metal_decode_runtime *runtime,
    id<MTLBuffer> first_buffer,
    size_t first_offset,
    size_t first_bytes,
    id<MTLBuffer> second_buffer,
    size_t second_offset,
    size_t second_bytes,
    id<MTLBuffer> third_buffer,
    size_t third_offset,
    size_t third_bytes,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    size_t output_bytes,
    int failure_code
) {
    termite_metal_planned_encoder_range accesses[4];
    if (termite_metal_planned_range_make(first_buffer, first_offset, first_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[0], failure_code) != 0 ||
        termite_metal_planned_range_make(second_buffer, second_offset, second_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[1], failure_code) != 0 ||
        termite_metal_planned_range_make(third_buffer, third_offset, third_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[2], failure_code) != 0 ||
        termite_metal_planned_range_make(output_buffer, output_offset, output_bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &accesses[3], failure_code) != 0 ||
        termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, accesses, 4, failure_code) != 0)
    {
        return failure_code;
    }
    return 0;
}

static int termite_metal_decode_runtime_prepare_planned_dense_linear_accesses(
    termite_metal_decode_runtime *runtime,
    id<MTLBuffer> input_buffer,
    size_t input_offset,
    size_t input_bytes,
    id<MTLBuffer> weight_buffer,
    id<MTLBuffer> bias_buffer,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    size_t output_bytes,
    int failure_code
) {
    if (weight_buffer == nil || bias_buffer == nil) return failure_code;
    termite_metal_planned_encoder_range accesses[4];
    if (termite_metal_planned_range_make(input_buffer, input_offset, input_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[0], failure_code) != 0 ||
        termite_metal_planned_range_make(weight_buffer, 0, weight_buffer.length, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[1], failure_code) != 0 ||
        termite_metal_planned_range_make(bias_buffer, 0, bias_buffer.length, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[2], failure_code) != 0 ||
        termite_metal_planned_range_make(output_buffer, output_offset, output_bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &accesses[3], failure_code) != 0 ||
        termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, accesses, 4, failure_code) != 0)
    {
        return failure_code;
    }
    return 0;
}

static bool termite_metal_should_coalesce_planned_compute_encoder(termite_metal_decode_runtime *runtime) {
    return runtime != NULL &&
        runtime->active_frame_cb != nil &&
        termite_metal_coalesce_planned_compute_encoders_enabled();
}

static id<MTLComputeCommandEncoder> termite_metal_tracked_compute_command_encoder_at(id<MTLCommandBuffer> command_buffer, const char *func, int line) {
    termite_metal_decode_runtime *runtime = termite_metal_active_encoder_runtime;
    if (runtime != NULL && runtime->active_frame_cb == command_buffer && runtime->active_compute_region == TERMITE_METAL_COMPUTE_REGION_OTHER && termite_metal_trace_unscoped_compute_enabled()) {
        fprintf(stderr, "metal_unscoped_compute: func=%s line=%d\n", func != NULL ? func : "(unknown)", line);
    }
    return termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_OTHER);
}

static size_t termite_metal_enter_compute_region(termite_metal_decode_runtime *runtime, size_t region) {
    if (runtime == NULL) return TERMITE_METAL_COMPUTE_REGION_OTHER;
    if (region >= TERMITE_METAL_COMPUTE_REGION_COUNTER_COUNT) region = TERMITE_METAL_COMPUTE_REGION_OTHER;
    const size_t previous = runtime->active_compute_region;
    runtime->active_compute_region = region;
    return previous;
}

static void termite_metal_restore_compute_region(termite_metal_decode_runtime *runtime, size_t previous) {
    if (runtime == NULL) return;
    if (previous >= TERMITE_METAL_COMPUTE_REGION_COUNTER_COUNT) previous = TERMITE_METAL_COMPUTE_REGION_OTHER;
    runtime->active_compute_region = previous;
}

static size_t termite_metal_default_compute_region_for_source(size_t source) {
    switch (source) {
        case TERMITE_METAL_COMPUTE_SOURCE_QUANT_QKV:
        case TERMITE_METAL_COMPUTE_SOURCE_ATTENTION:
        case TERMITE_METAL_COMPUTE_SOURCE_HEAD_ROPE:
            return TERMITE_METAL_COMPUTE_REGION_ATTENTION;
        case TERMITE_METAL_COMPUTE_SOURCE_QUANT_PAIR_ACT:
        case TERMITE_METAL_COMPUTE_SOURCE_FFN:
            return TERMITE_METAL_COMPUTE_REGION_FFN;
        case TERMITE_METAL_COMPUTE_SOURCE_PLE:
            return TERMITE_METAL_COMPUTE_REGION_PLE;
        case TERMITE_METAL_COMPUTE_SOURCE_TAIL:
            return TERMITE_METAL_COMPUTE_REGION_TAIL;
        case TERMITE_METAL_COMPUTE_SOURCE_EMBEDDING:
            return TERMITE_METAL_COMPUTE_REGION_EMBEDDING;
        case TERMITE_METAL_COMPUTE_SOURCE_LAYER:
            return TERMITE_METAL_COMPUTE_REGION_LAYER;
        case TERMITE_METAL_COMPUTE_SOURCE_DENSE_LINEAR:
            return TERMITE_METAL_COMPUTE_REGION_OTHER;
        default:
            return TERMITE_METAL_COMPUTE_REGION_OTHER;
    }
}

static const char *termite_metal_compute_source_name(size_t source) {
    switch (source) {
        case TERMITE_METAL_COMPUTE_SOURCE_QUANT_LINEAR: return "quant_linear";
        case TERMITE_METAL_COMPUTE_SOURCE_QUANT_QKV: return "quant_qkv";
        case TERMITE_METAL_COMPUTE_SOURCE_QUANT_PAIR_ACT: return "quant_pair_act";
        case TERMITE_METAL_COMPUTE_SOURCE_ATTENTION: return "attention";
        case TERMITE_METAL_COMPUTE_SOURCE_RMS_NORM: return "rms_norm";
        case TERMITE_METAL_COMPUTE_SOURCE_HEAD_ROPE: return "head_rope";
        case TERMITE_METAL_COMPUTE_SOURCE_FFN: return "ffn";
        case TERMITE_METAL_COMPUTE_SOURCE_PLE: return "ple";
        case TERMITE_METAL_COMPUTE_SOURCE_TAIL: return "tail";
        case TERMITE_METAL_COMPUTE_SOURCE_EMBEDDING: return "embedding";
        case TERMITE_METAL_COMPUTE_SOURCE_DENSE_LINEAR: return "dense_linear";
        case TERMITE_METAL_COMPUTE_SOURCE_LAYER: return "layer";
        case TERMITE_METAL_COMPUTE_SOURCE_OTHER: return "other";
        default: return "unknown";
    }
}

static const char *termite_metal_compute_region_name(size_t region) {
    switch (region) {
        case TERMITE_METAL_COMPUTE_REGION_ATTENTION: return "attention";
        case TERMITE_METAL_COMPUTE_REGION_ATTENTION_PROJECT: return "attention_project";
        case TERMITE_METAL_COMPUTE_REGION_FFN_NORM: return "ffn_norm";
        case TERMITE_METAL_COMPUTE_REGION_FFN: return "ffn";
        case TERMITE_METAL_COMPUTE_REGION_PLE: return "ple";
        case TERMITE_METAL_COMPUTE_REGION_TAIL: return "tail";
        case TERMITE_METAL_COMPUTE_REGION_EMBEDDING: return "embedding";
        case TERMITE_METAL_COMPUTE_REGION_LAYER: return "layer";
        case TERMITE_METAL_COMPUTE_REGION_OTHER: return "other";
        default: return "unknown";
    }
}

static const char *termite_metal_plan_op_name(size_t kind) {
    switch (kind) {
        case TERMITE_METAL_PLAN_OP_UNKNOWN: return "unknown";
        case TERMITE_METAL_PLAN_OP_DECODE_ATTENTION_PRE_NORM: return "attention_pre_norm";
        case TERMITE_METAL_PLAN_OP_DECODE_Q_OR_QKV_LINEAR: return "qkv_linear";
        case TERMITE_METAL_PLAN_OP_DECODE_Q_HEAD_NORM_ROPE: return "q_head_norm_rope";
        case TERMITE_METAL_PLAN_OP_DECODE_K_HEAD_NORM_ROPE: return "k_head_norm_rope";
        case TERMITE_METAL_PLAN_OP_DECODE_V_NORM: return "v_norm";
        case TERMITE_METAL_PLAN_OP_DECODE_KV_SEED: return "kv_seed";
        case TERMITE_METAL_PLAN_OP_DECODE_ATTENTION: return "attention";
        case TERMITE_METAL_PLAN_OP_DECODE_ATTENTION_OUTPUT_LINEAR: return "attention_output_linear";
        case TERMITE_METAL_PLAN_OP_DECODE_ATTENTION_POST_NORM_RESIDUAL: return "attention_post_norm_residual";
        case TERMITE_METAL_PLAN_OP_DECODE_FFN_PRE_NORM_SCALE: return "ffn_pre_norm_scale";
        case TERMITE_METAL_PLAN_OP_DECODE_FFN_GATE_UP_ACTIVATION: return "ffn_gate_up_activation";
        case TERMITE_METAL_PLAN_OP_DECODE_FFN_DOWN_LINEAR: return "ffn_down_linear";
        case TERMITE_METAL_PLAN_OP_DECODE_FFN_POST_NORM_RESIDUAL: return "ffn_post_norm_residual";
        case TERMITE_METAL_PLAN_OP_DECODE_PLE_GATE_ACTIVATION: return "ple_gate_activation";
        case TERMITE_METAL_PLAN_OP_DECODE_PLE_PROJECTION: return "ple_projection";
        case TERMITE_METAL_PLAN_OP_DECODE_PLE_POST_NORM_RESIDUAL: return "ple_post_norm_residual";
        case TERMITE_METAL_PLAN_OP_DECODE_TAIL_FINAL_NORM: return "tail_final_norm";
        case TERMITE_METAL_PLAN_OP_DECODE_TAIL_LM_HEAD: return "tail_lm_head";
        case TERMITE_METAL_PLAN_OP_DECODE_TAIL_ARGMAX: return "tail_argmax";
        case TERMITE_METAL_PLAN_OP_QUANT_GET_ROWS: return "quant_get_rows";
        case TERMITE_METAL_PLAN_OP_QUANT_SET_ROWS: return "quant_set_rows";
        case TERMITE_METAL_PLAN_OP_QUANT_COPY_Q_TO_F32: return "quant_copy_q_to_f32";
        case TERMITE_METAL_PLAN_OP_QUANT_COPY_F32_TO_Q: return "quant_copy_f32_to_q";
        case TERMITE_METAL_PLAN_OP_ATTENTION_FLASH: return "attention_flash";
        case TERMITE_METAL_PLAN_OP_ATTENTION_PAGED: return "attention_paged";
        case TERMITE_METAL_PLAN_OP_ATTENTION_QUANTIZED_KV: return "attention_quantized_kv";
        default: return "unknown";
    }
}

static const char *termite_metal_operator_name(size_t op) {
    switch (op) {
        case TERMITE_METAL_OPERATOR_FALLBACK: return "fallback";
        case TERMITE_METAL_OPERATOR_MUL_MV: return "mul_mv";
        case TERMITE_METAL_OPERATOR_MUL_MV_EXT: return "mul_mv_ext";
        case TERMITE_METAL_OPERATOR_MUL_MM: return "mul_mm";
        case TERMITE_METAL_OPERATOR_GET_ROWS: return "get_rows";
        case TERMITE_METAL_OPERATOR_SET_ROWS: return "set_rows";
        case TERMITE_METAL_OPERATOR_CPY_Q_TO_F32: return "cpy_q_to_f32";
        case TERMITE_METAL_OPERATOR_CPY_F32_TO_Q: return "cpy_f32_to_q";
        case TERMITE_METAL_OPERATOR_ATTENTION_FLASH: return "attention_flash";
        case TERMITE_METAL_OPERATOR_ATTENTION_PAGED: return "attention_paged";
        case TERMITE_METAL_OPERATOR_ATTENTION_QUANTIZED_KV: return "attention_quantized_kv";
        default: return "unknown";
    }
}

static void termite_metal_decode_runtime_record_planned_command_op(
    termite_metal_decode_runtime *runtime,
    termite_metal_runtime_command_op op
) {
    if (runtime == NULL || runtime->active_frame_cb == nil) return;
    runtime->active_frame_planned_command_op_count += 1;
    const size_t kind = op.kind < TERMITE_METAL_PLAN_OP_COUNTER_COUNT ? op.kind : TERMITE_METAL_PLAN_OP_UNKNOWN;
    runtime->active_frame_planned_command_op_kind_counts[kind] += 1;
    if (op.op < TERMITE_METAL_OPERATOR_COUNTER_COUNT) {
        runtime->active_frame_planned_command_operator_counts[op.op] += 1;
    }
    if (op.quant_dispatch < TERMITE_METAL_QUANT_DISPATCH_COUNTER_COUNT) {
        runtime->active_frame_planned_command_quant_dispatch_counts[op.quant_dispatch] += 1;
    }
}

static void termite_metal_decode_runtime_record_logical_command_op(
    termite_metal_decode_runtime *runtime,
    uint16_t kind,
    uint8_t op,
    uint8_t quant_dispatch
) {
    termite_metal_runtime_command_op command_op = {
        .kind = kind,
        .barrier_before = 0,
        .quant_dispatch = quant_dispatch,
        .op = op,
        .format = TERMITE_METAL_GRAPH_FORMAT_UNKNOWN,
        .input_dtype = 0u,
        .output_dtype = 0u,
        .source = 0,
        .region = 0,
        .scope_index = 0,
        .resource_start = 0,
        .resource_count = 0,
    };
    termite_metal_decode_runtime_record_planned_command_op(runtime, command_op);
}

static bool termite_metal_planned_layer_contract_find_command_op(
    termite_metal_planned_layer_contract contract,
    uint16_t kind,
    termite_metal_runtime_command_op *out_op
) {
    if (contract.command_ops == NULL || out_op == NULL) return false;
    if (contract.start_index >= contract.command_op_count) return false;
    for (size_t index = contract.start_index; index < contract.command_op_count; index += 1) {
        const termite_metal_runtime_command_op op = contract.command_ops[index];
        if (op.kind == kind) {
            *out_op = op;
            return true;
        }
    }
    return false;
}

static void termite_metal_decode_runtime_record_contract_or_logical_command_op(
    termite_metal_decode_runtime *runtime,
    termite_metal_planned_layer_contract contract,
    uint16_t kind,
    uint8_t op,
    uint8_t quant_dispatch
) {
    termite_metal_runtime_command_op command_op;
    if (termite_metal_planned_layer_contract_find_command_op(contract, kind, &command_op)) {
        termite_metal_decode_runtime_record_planned_command_op(runtime, command_op);
        return;
    }
    termite_metal_decode_runtime_record_logical_command_op(runtime, kind, op, quant_dispatch);
}

static void termite_metal_maybe_trace_last_frame(termite_metal_decode_runtime *runtime) {
    if (runtime == NULL) return;
    const char *enabled = getenv("TERMITE_METAL_TRACE_FRAME");
    if (enabled == NULL || enabled[0] == '\0' || strcmp(enabled, "0") == 0) return;
    const bool trace_all = strcmp(enabled, "all") == 0;
    if (!trace_all && runtime->last_frame_compute_encoder_count < 100 && runtime->last_frame_blit_encoder_count < 100) return;

    fprintf(
        stderr,
        "metal_frame_trace: compute=%llu blit=%llu planned_scopes=%llu planned_barriers=%llu gpu_ms=%.3f\n",
        (unsigned long long)runtime->last_frame_compute_encoder_count,
        (unsigned long long)runtime->last_frame_blit_encoder_count,
        (unsigned long long)runtime->last_frame_planned_compute_scope_count,
        (unsigned long long)runtime->last_frame_planned_barrier_count,
        (double)runtime->last_frame_gpu_nanos / 1000000.0
    );
    for (size_t region = 0; region < TERMITE_METAL_COMPUTE_REGION_COUNTER_COUNT; ++region) {
        const uint64_t region_total = runtime->last_frame_compute_region_counts[region];
        if (region_total == 0) continue;
        fprintf(
            stderr,
            "metal_frame_trace_region: %s total=%llu",
            termite_metal_compute_region_name(region),
            (unsigned long long)region_total
        );
        for (size_t source = 0; source < TERMITE_METAL_COMPUTE_COUNTER_COUNT; ++source) {
            const uint64_t count = runtime->last_frame_compute_region_source_counts[region][source];
            if (count == 0) continue;
            fprintf(
                stderr,
                " %s=%llu",
                termite_metal_compute_source_name(source),
                (unsigned long long)count
            );
        }
        fprintf(stderr, "\n");
    }
    if (runtime->last_frame_planned_command_op_count != 0) {
        fprintf(
            stderr,
            "metal_frame_trace_command_ops: total=%llu",
            (unsigned long long)runtime->last_frame_planned_command_op_count
        );
        for (size_t kind = 0; kind < TERMITE_METAL_PLAN_OP_COUNTER_COUNT; ++kind) {
            const uint64_t count = runtime->last_frame_planned_command_op_kind_counts[kind];
            if (count == 0) continue;
            fprintf(stderr, " %s=%llu", termite_metal_plan_op_name(kind), (unsigned long long)count);
        }
        fprintf(stderr, "\n");
        fprintf(stderr, "metal_frame_trace_command_operators:");
        for (size_t op = 0; op < TERMITE_METAL_OPERATOR_COUNTER_COUNT; ++op) {
            const uint64_t count = runtime->last_frame_planned_command_operator_counts[op];
            if (count == 0) continue;
            fprintf(stderr, " %s=%llu", termite_metal_operator_name(op), (unsigned long long)count);
        }
        fprintf(stderr, "\n");
        fprintf(
            stderr,
            "metal_frame_trace_quant_dispatch: scalar=%llu mmv=%llu small_batch=%llu mm=%llu\n",
            (unsigned long long)runtime->last_frame_planned_command_quant_dispatch_counts[0],
            (unsigned long long)runtime->last_frame_planned_command_quant_dispatch_counts[1],
            (unsigned long long)runtime->last_frame_planned_command_quant_dispatch_counts[2],
            (unsigned long long)runtime->last_frame_planned_command_quant_dispatch_counts[3]
        );
    }
}

static id<MTLComputeCommandEncoder> termite_metal_tracked_compute_command_encoder_for(id<MTLCommandBuffer> command_buffer, size_t source) {
    termite_metal_decode_runtime *runtime = termite_metal_active_encoder_runtime;
    if (runtime != NULL && runtime->active_frame_cb == command_buffer) {
        termite_metal_decode_runtime_close_planned_compute_encoder_for_transition(runtime);
        if (source >= TERMITE_METAL_COMPUTE_COUNTER_COUNT) source = TERMITE_METAL_COMPUTE_SOURCE_OTHER;
        size_t region = runtime->active_compute_region;
        if (region >= TERMITE_METAL_COMPUTE_REGION_COUNTER_COUNT) region = TERMITE_METAL_COMPUTE_REGION_OTHER;
        if (region == TERMITE_METAL_COMPUTE_REGION_OTHER) region = termite_metal_default_compute_region_for_source(source);
        runtime->compute_encoder_count += 1;
        runtime->active_frame_compute_encoder_count += 1;
        runtime->active_frame_compute_source_counts[source] += 1;
        runtime->active_frame_compute_region_counts[region] += 1;
        runtime->active_frame_compute_region_source_counts[region][source] += 1;
    }
    return [command_buffer computeCommandEncoder];
}

static id<MTLComputeCommandEncoder> termite_metal_scoped_compute_encoder_for(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    size_t source,
    BOOL *owned
) {
    if (owned != NULL) *owned = YES;
    if (runtime != NULL &&
        runtime->active_frame_cb == command_buffer &&
        runtime->active_planned_compute_encoder != nil &&
        termite_metal_coalesce_planned_compute_encoders_enabled())
    {
        if (owned != NULL) *owned = NO;
        return runtime->active_planned_compute_encoder;
    }
    return termite_metal_tracked_compute_command_encoder_for(command_buffer, source);
}

static void termite_metal_end_scoped_compute_encoder(id<MTLComputeCommandEncoder> encoder, BOOL owned) {
    if (owned && encoder != nil) [encoder endEncoding];
}

int termite_metal_decode_runtime_begin_planned_compute_scope(termite_metal_decode_runtime *runtime, size_t source, size_t region) {
    if (runtime == NULL) return -1;
    if (runtime->active_frame_cb == nil) return -2;
    if (source >= TERMITE_METAL_COMPUTE_COUNTER_COUNT) source = TERMITE_METAL_COMPUTE_SOURCE_OTHER;
    if (region >= TERMITE_METAL_COMPUTE_REGION_COUNTER_COUNT) region = TERMITE_METAL_COMPUTE_REGION_OTHER;
    if (region == TERMITE_METAL_COMPUTE_REGION_OTHER) region = termite_metal_default_compute_region_for_source(source);

    if (runtime->active_planned_compute_encoder != nil) {
        if (!termite_metal_should_coalesce_planned_compute_encoder(runtime)) return -3;
        runtime->active_planned_compute_scope_closed_for_encoder_transition = false;
        runtime->active_planned_compute_source = source;
        runtime->active_planned_compute_region = region;
        runtime->active_frame_planned_compute_scope_count += 1;
        runtime->active_frame_compute_source_counts[source] += 1;
        runtime->active_frame_compute_region_counts[region] += 1;
        runtime->active_frame_compute_region_source_counts[region][source] += 1;
        return 0;
    }

    id<MTLComputeCommandEncoder> encoder = [runtime->active_frame_cb computeCommandEncoder];
    if (encoder == nil) return -4;
    runtime->active_planned_compute_encoder = encoder;
    runtime->active_planned_compute_scope_closed_for_encoder_transition = false;
    runtime->active_planned_compute_source = source;
    runtime->active_planned_compute_region = region;
    termite_metal_decode_runtime_reset_planned_compute_ranges(runtime);
    runtime->compute_encoder_count += 1;
    runtime->active_frame_compute_encoder_count += 1;
    runtime->active_frame_planned_compute_scope_count += 1;
    runtime->active_frame_compute_source_counts[source] += 1;
    runtime->active_frame_compute_region_counts[region] += 1;
    runtime->active_frame_compute_region_source_counts[region][source] += 1;
    return 0;
}

int termite_metal_decode_runtime_planned_compute_barrier(termite_metal_decode_runtime *runtime) {
    if (runtime == NULL) return -1;
    if (runtime->active_frame_cb == nil) return -2;
    id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
    if (encoder == nil) {
        if (runtime->active_planned_compute_scope_closed_for_encoder_transition) return 0;
        return -3;
    }
    if (termite_metal_planned_compute_barriers_enabled(runtime)) {
        [encoder memoryBarrierWithScope:MTLBarrierScopeBuffers];
        runtime->active_frame_planned_barrier_count += 1;
    }
    termite_metal_decode_runtime_reset_planned_compute_ranges(runtime);
    return 0;
}

int termite_metal_decode_runtime_push_planned_compute_barrier_suppression(termite_metal_decode_runtime *runtime) {
    if (runtime == NULL) return -1;
    if (runtime->planned_compute_barrier_suppression_depth == UINT32_MAX) return -2;
    runtime->planned_compute_barrier_suppression_depth += 1;
    return 0;
}

int termite_metal_decode_runtime_pop_planned_compute_barrier_suppression(termite_metal_decode_runtime *runtime) {
    if (runtime == NULL) return -1;
    if (runtime->planned_compute_barrier_suppression_depth == 0) return -2;
    runtime->planned_compute_barrier_suppression_depth -= 1;
    return 0;
}

static termite_metal_planned_layer_cursor termite_metal_planned_layer_cursor_make(
    termite_metal_decode_runtime *runtime,
    termite_metal_planned_layer_contract contract,
    int failure_code
) {
    return (termite_metal_planned_layer_cursor){
        .runtime = runtime,
        .contract = contract,
        .index = contract.start_index,
        .use_ops = contract.ops != NULL && contract.start_index < contract.op_count,
        .use_barriers = contract.barriers != NULL && contract.start_index < contract.barrier_count,
        .use_quant_dispatches = contract.quant_dispatches != NULL && contract.start_index < contract.quant_dispatch_count,
        .use_command_ops = contract.command_ops != NULL && contract.start_index < contract.command_op_count,
        .last_quant_dispatch = TERMITE_METAL_QUANT_MATMUL_DISPATCH_UNKNOWN,
        .last_operator = TERMITE_METAL_OPERATOR_UNKNOWN,
        .last_format = UINT8_MAX,
        .last_input_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
        .last_output_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
        .failure_code = failure_code,
    };
}

static int termite_metal_planned_layer_cursor_before_op(
    termite_metal_planned_layer_cursor *cursor,
    uint16_t expected_kind
) {
    if (cursor == NULL) return -1;
    if (cursor->use_command_ops) {
        if (cursor->index >= cursor->contract.command_op_count) return cursor->failure_code;
        const termite_metal_runtime_command_op op = cursor->contract.command_ops[cursor->index];
        cursor->last_quant_dispatch = op.quant_dispatch;
        cursor->last_operator = op.op;
        cursor->last_format = op.format;
        cursor->last_input_dtype = op.input_dtype;
        cursor->last_output_dtype = op.output_dtype;
        if (op.kind != expected_kind) return cursor->failure_code;
        termite_metal_decode_runtime_record_planned_command_op(cursor->runtime, op);
        cursor->index += 1;
        return 0;
    }
    cursor->last_quant_dispatch = (cursor->use_quant_dispatches && cursor->index < cursor->contract.quant_dispatch_count)
        ? cursor->contract.quant_dispatches[cursor->index]
        : TERMITE_METAL_QUANT_MATMUL_DISPATCH_UNKNOWN;
    cursor->last_operator = TERMITE_METAL_OPERATOR_UNKNOWN;
    cursor->last_format = UINT8_MAX;
    cursor->last_input_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32;
    cursor->last_output_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32;
    if (cursor->use_ops) {
        if (cursor->index >= cursor->contract.op_count || cursor->contract.ops[cursor->index] != expected_kind) return cursor->failure_code;
    }
    if (cursor->use_barriers || cursor->use_ops) {
        cursor->index += 1;
    }
    return 0;
}

static int termite_metal_planned_layer_cursor_before_step(
    termite_metal_planned_layer_cursor *cursor,
    const uint16_t *step_kinds,
    size_t step_count,
    size_t step_index
) {
    if (cursor == NULL || step_kinds == NULL || step_index >= step_count) return -1;
    return termite_metal_planned_layer_cursor_before_op(cursor, step_kinds[step_index]);
}

static uint8_t termite_metal_planned_layer_cursor_quant_dispatch(
    const termite_metal_planned_layer_cursor *cursor
) {
    return cursor != NULL ? cursor->last_quant_dispatch : TERMITE_METAL_QUANT_MATMUL_DISPATCH_UNKNOWN;
}

static uint8_t termite_metal_planned_layer_cursor_operator(
    const termite_metal_planned_layer_cursor *cursor
) {
    return cursor != NULL ? cursor->last_operator : TERMITE_METAL_OPERATOR_UNKNOWN;
}

static uint8_t termite_metal_planned_layer_cursor_input_dtype(
    const termite_metal_planned_layer_cursor *cursor
) {
    return cursor != NULL ? cursor->last_input_dtype : TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32;
}

static uint8_t termite_metal_planned_layer_cursor_output_dtype(
    const termite_metal_planned_layer_cursor *cursor
) {
    return cursor != NULL ? cursor->last_output_dtype : TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32;
}

static bool termite_metal_runtime_operator_is_quant_matmul(uint8_t op) {
    return op == TERMITE_METAL_OPERATOR_UNKNOWN ||
        op == TERMITE_METAL_OPERATOR_MUL_MV ||
        op == TERMITE_METAL_OPERATOR_MUL_MV_EXT ||
        op == TERMITE_METAL_OPERATOR_MUL_MM;
}

static uint8_t termite_metal_graph_quant_format_for_runtime_format(uint32_t format) {
    switch (format) {
        case TERMITE_METAL_QUANT_FORMAT_Q1_0: return TERMITE_METAL_GRAPH_QUANT_FORMAT_Q1_0;
        case TERMITE_METAL_QUANT_FORMAT_I2_S: return TERMITE_METAL_GRAPH_QUANT_FORMAT_I2_S;
        case TERMITE_METAL_QUANT_FORMAT_I8_S: return TERMITE_METAL_GRAPH_QUANT_FORMAT_I8_S;
        case TERMITE_METAL_QUANT_FORMAT_Q2_K: return TERMITE_METAL_GRAPH_QUANT_FORMAT_Q2_K;
        case TERMITE_METAL_QUANT_FORMAT_Q3_K: return TERMITE_METAL_GRAPH_QUANT_FORMAT_Q3_K;
        case TERMITE_METAL_QUANT_FORMAT_Q4_0: return TERMITE_METAL_GRAPH_QUANT_FORMAT_Q4_0;
        case TERMITE_METAL_QUANT_FORMAT_Q4_1: return TERMITE_METAL_GRAPH_QUANT_FORMAT_Q4_1;
        case TERMITE_METAL_QUANT_FORMAT_Q4_K: return TERMITE_METAL_GRAPH_QUANT_FORMAT_Q4_K;
        case TERMITE_METAL_QUANT_FORMAT_Q5_0: return TERMITE_METAL_GRAPH_QUANT_FORMAT_Q5_0;
        case TERMITE_METAL_QUANT_FORMAT_Q5_1: return TERMITE_METAL_GRAPH_QUANT_FORMAT_Q5_1;
        case TERMITE_METAL_QUANT_FORMAT_Q5_K: return TERMITE_METAL_GRAPH_QUANT_FORMAT_Q5_K;
        case TERMITE_METAL_QUANT_FORMAT_Q6_K: return TERMITE_METAL_GRAPH_QUANT_FORMAT_Q6_K;
        case TERMITE_METAL_QUANT_FORMAT_Q8_0: return TERMITE_METAL_GRAPH_QUANT_FORMAT_Q8_0;
        case TERMITE_METAL_QUANT_FORMAT_Q8_1: return TERMITE_METAL_GRAPH_QUANT_FORMAT_Q8_1;
        case TERMITE_METAL_QUANT_FORMAT_Q8_K: return TERMITE_METAL_GRAPH_QUANT_FORMAT_Q8_K;
        case TERMITE_METAL_QUANT_FORMAT_IQ2_XS: return TERMITE_METAL_GRAPH_QUANT_FORMAT_IQ2_XS;
        case TERMITE_METAL_QUANT_FORMAT_IQ4_NL: return TERMITE_METAL_GRAPH_QUANT_FORMAT_IQ4_NL;
        case TERMITE_METAL_QUANT_FORMAT_IQ4_XS: return TERMITE_METAL_GRAPH_QUANT_FORMAT_IQ4_XS;
        case TERMITE_METAL_QUANT_FORMAT_MXFP4: return TERMITE_METAL_GRAPH_QUANT_FORMAT_MXFP4;
        case TERMITE_METAL_QUANT_FORMAT_NVFP4: return TERMITE_METAL_GRAPH_QUANT_FORMAT_NVFP4;
        case TERMITE_METAL_QUANT_FORMAT_TL1: return TERMITE_METAL_GRAPH_QUANT_FORMAT_TL1;
        case TERMITE_METAL_QUANT_FORMAT_TL2: return TERMITE_METAL_GRAPH_QUANT_FORMAT_TL2;
        default: return TERMITE_METAL_GRAPH_FORMAT_UNKNOWN;
    }
}

static bool termite_metal_runtime_command_format_matches_quant(uint8_t command_format, uint32_t quant_format) {
    const uint8_t graph_format = termite_metal_graph_quant_format_for_runtime_format(quant_format);
    return command_format == TERMITE_METAL_GRAPH_FORMAT_UNKNOWN ||
        (graph_format != TERMITE_METAL_GRAPH_FORMAT_UNKNOWN && command_format == graph_format);
}

static bool termite_metal_runtime_command_format_is_f32_attention(uint8_t format) {
    return format == TERMITE_METAL_GRAPH_FORMAT_UNKNOWN ||
        format == TERMITE_METAL_GRAPH_ATTENTION_KV_FORMAT_F32;
}

static int termite_metal_planned_layer_cursor_fallback_barrier_after_op(
    termite_metal_planned_layer_cursor *cursor
) {
    if (cursor == NULL) return -1;
    (void)cursor;
    return 0;
}

static void termite_metal_decode_runtime_close_planned_compute_encoder_for_transition(termite_metal_decode_runtime *runtime) {
    if (runtime == NULL) return;
    id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
    if (encoder == nil) return;
    [encoder endEncoding];
    runtime->active_planned_compute_encoder = nil;
    termite_metal_decode_runtime_reset_planned_compute_ranges(runtime);
    runtime->active_planned_compute_scope_closed_for_encoder_transition = true;
}

int termite_metal_decode_runtime_end_planned_compute_scope(termite_metal_decode_runtime *runtime) {
    if (runtime == NULL) return -1;
    id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
    if (encoder == nil) {
        if (runtime->active_planned_compute_scope_closed_for_encoder_transition) {
            runtime->active_planned_compute_scope_closed_for_encoder_transition = false;
            runtime->active_planned_compute_source = TERMITE_METAL_COMPUTE_SOURCE_OTHER;
            runtime->active_planned_compute_region = TERMITE_METAL_COMPUTE_REGION_OTHER;
            return 0;
        }
        return -2;
    }
    if (termite_metal_should_coalesce_planned_compute_encoder(runtime)) {
        runtime->active_planned_compute_scope_closed_for_encoder_transition = false;
        runtime->active_planned_compute_source = TERMITE_METAL_COMPUTE_SOURCE_OTHER;
        runtime->active_planned_compute_region = TERMITE_METAL_COMPUTE_REGION_OTHER;
        return 0;
    }
    [encoder endEncoding];
    runtime->active_planned_compute_encoder = nil;
    termite_metal_decode_runtime_reset_planned_compute_ranges(runtime);
    runtime->active_planned_compute_scope_closed_for_encoder_transition = false;
    runtime->active_planned_compute_source = TERMITE_METAL_COMPUTE_SOURCE_OTHER;
    runtime->active_planned_compute_region = TERMITE_METAL_COMPUTE_REGION_OTHER;
    return 0;
}

static id<MTLBlitCommandEncoder> termite_metal_tracked_blit_command_encoder(id<MTLCommandBuffer> command_buffer) {
    termite_metal_decode_runtime *runtime = termite_metal_active_encoder_runtime;
    if (runtime != NULL && runtime->active_frame_cb == command_buffer) {
        termite_metal_decode_runtime_close_planned_compute_encoder_for_transition(runtime);
        runtime->blit_encoder_count += 1;
        runtime->active_frame_blit_encoder_count += 1;
    }
    return [command_buffer blitCommandEncoder];
}

static void termite_metal_record_active_frame_blit_source(id<MTLCommandBuffer> command_buffer, size_t source) {
    termite_metal_decode_runtime *runtime = termite_metal_active_encoder_runtime;
    if (runtime == NULL || runtime->active_frame_cb != command_buffer) return;
    if (source >= TERMITE_METAL_BLIT_COUNTER_COUNT) source = TERMITE_METAL_BLIT_SOURCE_OTHER;
    runtime->active_frame_blit_source_counts[source] += 1;
}

static id<MTLComputePipelineState> termite_metal_make_pipeline(id<MTLDevice> device, id<MTLLibrary> library, NSString *name) {
    NSError *error = nil;
    id<MTLFunction> function = [library newFunctionWithName:name];
    if (function == nil) {
        fprintf(stderr, "metal-make-pipeline missing-function name=%s\n", name.UTF8String);
        return nil;
    }
    id<MTLComputePipelineState> pipeline = [device newComputePipelineStateWithFunction:function error:&error];
    if (pipeline == nil && error != nil) {
        fprintf(stderr, "metal-make-pipeline name=%s error=%s\n", name.UTF8String, error.localizedDescription.UTF8String);
    }
    return pipeline;
}

static MPSMatrixMultiplication *termite_metal_decode_runtime_cached_mps_mm(
    termite_metal_decode_runtime *runtime,
    size_t slot,
    size_t rows,
    size_t in_dim,
    size_t out_dim
) {
    if (runtime == NULL || slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || rows == 0 || in_dim == 0 || out_dim == 0) return nil;
    MPSMatrixMultiplication *cached = runtime->linear_mps_mm[slot];
    if (cached != nil &&
        runtime->linear_mps_mm_rows[slot] == rows &&
        runtime->linear_in_dims[slot] == in_dim &&
        runtime->linear_out_dims[slot] == out_dim)
    {
        return cached;
    }
    MPSMatrixMultiplication *mm = [[MPSMatrixMultiplication alloc] initWithDevice:runtime->device
                                                                    transposeLeft:NO
                                                                   transposeRight:YES
                                                                       resultRows:rows
                                                                    resultColumns:out_dim
                                                                  interiorColumns:in_dim
                                                                            alpha:1.0
                                                                             beta:0.0];
    if (mm == nil) return nil;
    runtime->linear_mps_mm[slot] = mm;
    runtime->linear_mps_mm_rows[slot] = rows;
    return mm;
}

static void termite_metal_decode_runtime_invalidate_dense_qkv_pack_slot(
    termite_metal_decode_runtime *runtime,
    size_t slot
) {
    if (runtime == NULL) return;
    for (size_t i = 0; i < TERMITE_METAL_DENSE_QKV_PACK_CACHE_CAPACITY; ++i) {
        if (i == slot ||
            runtime->dense_qkv_packed_k_slots[i] == slot ||
            runtime->dense_qkv_packed_v_slots[i] == slot)
        {
            runtime->dense_qkv_packed_weight_buffers[i] = nil;
            runtime->dense_qkv_packed_bias_buffers[i] = nil;
            runtime->dense_qkv_packed_mps_mm[i] = nil;
            runtime->dense_qkv_packed_k_slots[i] = SIZE_MAX;
            runtime->dense_qkv_packed_v_slots[i] = SIZE_MAX;
            runtime->dense_qkv_packed_in_dims[i] = 0;
            runtime->dense_qkv_packed_q_out_dims[i] = 0;
            runtime->dense_qkv_packed_kv_out_dims[i] = 0;
            runtime->dense_qkv_packed_mps_rows[i] = 0;
        }
    }
}

static void termite_metal_decode_runtime_invalidate_q8_qkv_pack_slot(
    termite_metal_decode_runtime *runtime,
    size_t slot
) {
    if (runtime == NULL) return;
    for (size_t i = 0; i < TERMITE_METAL_DENSE_QKV_PACK_CACHE_CAPACITY; ++i) {
        if (i == slot ||
            runtime->q8_qkv_packed_k_slots[i] == slot ||
            runtime->q8_qkv_packed_v_slots[i] == slot)
        {
            runtime->q8_qkv_packed_weight_buffers[i] = nil;
            runtime->q8_qkv_packed_k_slots[i] = SIZE_MAX;
            runtime->q8_qkv_packed_v_slots[i] = SIZE_MAX;
            runtime->q8_qkv_packed_in_dims[i] = 0;
            runtime->q8_qkv_packed_q_out_dims[i] = 0;
            runtime->q8_qkv_packed_kv_out_dims[i] = 0;
            runtime->q8_qkv_packed_row_stride_bytes[i] = 0;
        }
    }
}

static void termite_metal_decode_runtime_invalidate_dense_pair_pack_slot(
    termite_metal_decode_runtime *runtime,
    size_t slot
) {
    if (runtime == NULL) return;
    for (size_t i = 0; i < TERMITE_METAL_LINEAR_SLOT_CAPACITY; ++i) {
        if (i == slot || runtime->dense_pair_packed_b_slots[i] == slot) {
            runtime->dense_pair_packed_weight_buffers[i] = nil;
            runtime->dense_pair_packed_bias_buffers[i] = nil;
            runtime->dense_pair_packed_mps_mm[i] = nil;
            runtime->dense_pair_packed_b_slots[i] = SIZE_MAX;
            runtime->dense_pair_packed_in_dims[i] = 0;
            runtime->dense_pair_packed_out_dims[i] = 0;
            runtime->dense_pair_packed_mps_rows[i] = 0;
        }
    }
}

static MPSMatrixMultiplication *termite_metal_decode_runtime_cached_dense_qkv_mps_mm(
    termite_metal_decode_runtime *runtime,
    size_t q_slot,
    size_t rows,
    size_t in_dim,
    size_t total_out_dim
) {
    if (runtime == NULL || q_slot >= TERMITE_METAL_DENSE_QKV_PACK_CACHE_CAPACITY || rows == 0 || in_dim == 0 || total_out_dim == 0) return nil;
    MPSMatrixMultiplication *cached = runtime->dense_qkv_packed_mps_mm[q_slot];
    if (cached != nil &&
        runtime->dense_qkv_packed_mps_rows[q_slot] == rows &&
        runtime->dense_qkv_packed_in_dims[q_slot] == in_dim)
    {
        return cached;
    }
    MPSMatrixMultiplication *mm = [[MPSMatrixMultiplication alloc] initWithDevice:runtime->device
                                                                    transposeLeft:NO
                                                                   transposeRight:YES
                                                                       resultRows:rows
                                                                    resultColumns:total_out_dim
                                                                  interiorColumns:in_dim
                                                                            alpha:1.0
                                                                             beta:0.0];
    if (mm == nil) return nil;
    runtime->dense_qkv_packed_mps_mm[q_slot] = mm;
    runtime->dense_qkv_packed_mps_rows[q_slot] = rows;
    return mm;
}

static int termite_metal_decode_runtime_ensure_dense_qkv_packed_weight(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    size_t q_slot,
    size_t k_slot,
    size_t v_slot,
    size_t in_dim,
    size_t q_out_dim,
    size_t kv_out_dim
) {
    if (runtime == NULL || command_buffer == nil || q_slot >= TERMITE_METAL_DENSE_QKV_PACK_CACHE_CAPACITY) return -1;
    const size_t q_bytes = q_out_dim * in_dim * sizeof(float);
    const size_t kv_bytes = kv_out_dim * in_dim * sizeof(float);
    const size_t packed_bytes = q_bytes + kv_bytes + kv_bytes;
    const size_t q_bias_bytes = q_out_dim * sizeof(float);
    const size_t kv_bias_bytes = kv_out_dim * sizeof(float);
    const size_t packed_bias_bytes = q_bias_bytes + kv_bias_bytes + kv_bias_bytes;
    if (runtime->dense_qkv_packed_weight_buffers[q_slot] != nil &&
        runtime->dense_qkv_packed_weight_buffers[q_slot].length >= packed_bytes &&
        runtime->dense_qkv_packed_bias_buffers[q_slot] != nil &&
        runtime->dense_qkv_packed_bias_buffers[q_slot].length >= packed_bias_bytes &&
        runtime->dense_qkv_packed_k_slots[q_slot] == k_slot &&
        runtime->dense_qkv_packed_v_slots[q_slot] == v_slot &&
        runtime->dense_qkv_packed_in_dims[q_slot] == in_dim &&
        runtime->dense_qkv_packed_q_out_dims[q_slot] == q_out_dim &&
        runtime->dense_qkv_packed_kv_out_dims[q_slot] == kv_out_dim)
    {
        return 0;
    }
    id<MTLBuffer> packed = [runtime->device newBufferWithLength:packed_bytes options:MTLResourceStorageModePrivate];
    id<MTLBuffer> packed_bias = [runtime->device newBufferWithLength:packed_bias_bytes options:MTLResourceStorageModePrivate];
    if (packed == nil || packed_bias == nil) return -2;
    id<MTLBlitCommandEncoder> blit = termite_metal_tracked_blit_command_encoder(command_buffer);
    if (blit == nil) return -3;
    [blit copyFromBuffer:runtime->linear_weight_buffers[q_slot] sourceOffset:0 toBuffer:packed destinationOffset:0 size:q_bytes];
    [blit copyFromBuffer:runtime->linear_weight_buffers[k_slot] sourceOffset:0 toBuffer:packed destinationOffset:q_bytes size:kv_bytes];
    [blit copyFromBuffer:runtime->linear_weight_buffers[v_slot] sourceOffset:0 toBuffer:packed destinationOffset:q_bytes + kv_bytes size:kv_bytes];
    [blit copyFromBuffer:runtime->linear_bias_buffers[q_slot] sourceOffset:0 toBuffer:packed_bias destinationOffset:0 size:q_bias_bytes];
    [blit copyFromBuffer:runtime->linear_bias_buffers[k_slot] sourceOffset:0 toBuffer:packed_bias destinationOffset:q_bias_bytes size:kv_bias_bytes];
    [blit copyFromBuffer:runtime->linear_bias_buffers[v_slot] sourceOffset:0 toBuffer:packed_bias destinationOffset:q_bias_bytes + kv_bias_bytes size:kv_bias_bytes];
    [blit endEncoding];

    runtime->dense_qkv_packed_weight_buffers[q_slot] = packed;
    runtime->dense_qkv_packed_bias_buffers[q_slot] = packed_bias;
    runtime->dense_qkv_packed_mps_mm[q_slot] = nil;
    runtime->dense_qkv_packed_k_slots[q_slot] = k_slot;
    runtime->dense_qkv_packed_v_slots[q_slot] = v_slot;
    runtime->dense_qkv_packed_in_dims[q_slot] = in_dim;
    runtime->dense_qkv_packed_q_out_dims[q_slot] = q_out_dim;
    runtime->dense_qkv_packed_kv_out_dims[q_slot] = kv_out_dim;
    runtime->dense_qkv_packed_mps_rows[q_slot] = 0;
    return 0;
}

static MPSMatrixMultiplication *termite_metal_decode_runtime_cached_dense_pair_mps_mm(
    termite_metal_decode_runtime *runtime,
    size_t a_slot,
    size_t rows,
    size_t in_dim,
    size_t total_out_dim
) {
    if (runtime == NULL || a_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || rows == 0 || in_dim == 0 || total_out_dim == 0) return nil;
    MPSMatrixMultiplication *cached = runtime->dense_pair_packed_mps_mm[a_slot];
    if (cached != nil &&
        runtime->dense_pair_packed_mps_rows[a_slot] == rows &&
        runtime->dense_pair_packed_in_dims[a_slot] == in_dim)
    {
        return cached;
    }
    MPSMatrixMultiplication *mm = [[MPSMatrixMultiplication alloc] initWithDevice:runtime->device
                                                                    transposeLeft:NO
                                                                   transposeRight:YES
                                                                       resultRows:rows
                                                                    resultColumns:total_out_dim
                                                                  interiorColumns:in_dim
                                                                            alpha:1.0
                                                                             beta:0.0];
    if (mm == nil) return nil;
    runtime->dense_pair_packed_mps_mm[a_slot] = mm;
    runtime->dense_pair_packed_mps_rows[a_slot] = rows;
    return mm;
}

static int termite_metal_decode_runtime_ensure_dense_pair_packed_weight(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    size_t a_slot,
    size_t b_slot,
    size_t in_dim,
    size_t out_dim
) {
    if (runtime == NULL || command_buffer == nil || a_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -1;
    const size_t projection_bytes = out_dim * in_dim * sizeof(float);
    const size_t packed_bytes = projection_bytes * 2;
    const size_t bias_bytes = out_dim * sizeof(float);
    const size_t packed_bias_bytes = bias_bytes * 2;
    if (runtime->dense_pair_packed_weight_buffers[a_slot] != nil &&
        runtime->dense_pair_packed_weight_buffers[a_slot].length >= packed_bytes &&
        runtime->dense_pair_packed_bias_buffers[a_slot] != nil &&
        runtime->dense_pair_packed_bias_buffers[a_slot].length >= packed_bias_bytes &&
        runtime->dense_pair_packed_b_slots[a_slot] == b_slot &&
        runtime->dense_pair_packed_in_dims[a_slot] == in_dim &&
        runtime->dense_pair_packed_out_dims[a_slot] == out_dim)
    {
        return 0;
    }
    id<MTLBuffer> packed = [runtime->device newBufferWithLength:packed_bytes options:MTLResourceStorageModePrivate];
    id<MTLBuffer> packed_bias = [runtime->device newBufferWithLength:packed_bias_bytes options:MTLResourceStorageModePrivate];
    if (packed == nil || packed_bias == nil) return -2;
    id<MTLBlitCommandEncoder> blit = termite_metal_tracked_blit_command_encoder(command_buffer);
    if (blit == nil) return -3;
    [blit copyFromBuffer:runtime->linear_weight_buffers[a_slot] sourceOffset:0 toBuffer:packed destinationOffset:0 size:projection_bytes];
    [blit copyFromBuffer:runtime->linear_weight_buffers[b_slot] sourceOffset:0 toBuffer:packed destinationOffset:projection_bytes size:projection_bytes];
    [blit copyFromBuffer:runtime->linear_bias_buffers[a_slot] sourceOffset:0 toBuffer:packed_bias destinationOffset:0 size:bias_bytes];
    [blit copyFromBuffer:runtime->linear_bias_buffers[b_slot] sourceOffset:0 toBuffer:packed_bias destinationOffset:bias_bytes size:bias_bytes];
    [blit endEncoding];

    runtime->dense_pair_packed_weight_buffers[a_slot] = packed;
    runtime->dense_pair_packed_bias_buffers[a_slot] = packed_bias;
    runtime->dense_pair_packed_mps_mm[a_slot] = nil;
    runtime->dense_pair_packed_b_slots[a_slot] = b_slot;
    runtime->dense_pair_packed_in_dims[a_slot] = in_dim;
    runtime->dense_pair_packed_out_dims[a_slot] = out_dim;
    runtime->dense_pair_packed_mps_rows[a_slot] = 0;
    return 0;
}

static id<MTLLibrary> termite_metal_make_library(id<MTLDevice> device, BOOL precise_math) {
    NSError *error = nil;
    MTLCompileOptions *options = [MTLCompileOptions new];
    if (precise_math) {
        if (options != nil) {
            options.mathMode = MTLMathModeSafe;
        }
    }
    id<MTLLibrary> library = [device newLibraryWithSource:termite_metal_shader_source() options:options error:&error];
    if (library == nil && error != nil) {
        fprintf(stderr, "metal-make-library precise=%d error=%s\n", precise_math ? 1 : 0, error.localizedDescription.UTF8String);
    }
    return library;
}

static id<MTLDevice> termite_metal_shared_device(void) {
    static id<MTLDevice> shared_device = nil;
    if (shared_device == nil) {
        shared_device = MTLCreateSystemDefaultDevice();
    }
    return shared_device;
}

static id<MTLCommandBuffer> termite_metal_new_command_buffer(id<MTLCommandQueue> queue, const char *label_cstr) {
    if (queue == nil) return nil;
    @autoreleasepool {
        id<MTLCommandBuffer> command_buffer = nil;
        if ([queue respondsToSelector:@selector(commandBufferWithDescriptor:)]) {
            MTLCommandBufferDescriptor *descriptor = [MTLCommandBufferDescriptor new];
            if (descriptor != nil && [descriptor respondsToSelector:@selector(setErrorOptions:)]) {
                descriptor.errorOptions = MTLCommandBufferErrorOptionEncoderExecutionStatus;
            }
            command_buffer = [queue commandBufferWithDescriptor:descriptor];
        }
        if (command_buffer == nil) {
            command_buffer = [queue commandBuffer];
        }
        if (command_buffer != nil && label_cstr != NULL) {
            NSString *label = [NSString stringWithUTF8String:label_cstr];
            if (label != nil) {
                command_buffer.label = label;
            }
        }
        if (command_buffer != nil) {
            const char *context = label_cstr;
            [command_buffer addCompletedHandler:^(id<MTLCommandBuffer> cb) {
                if (cb.status != MTLCommandBufferStatusCompleted) {
                    termite_metal_log_command_buffer_failure(cb, context);
                }
            }];
        }
        return command_buffer;
    }
}

static void termite_metal_log_command_buffer_failure(id<MTLCommandBuffer> command_buffer, const char *context) {
    if (command_buffer == nil) return;
    NSError *error = command_buffer.error;
    if (command_buffer.status != MTLCommandBufferStatusError && error == nil) return;
    const char *label = command_buffer.label != nil ? command_buffer.label.UTF8String : "(unlabeled)";
    const char *domain = (error != nil && error.domain != nil) ? error.domain.UTF8String : "(none)";
    const char *desc = (error != nil && error.localizedDescription != nil) ? error.localizedDescription.UTF8String : "(none)";
    long code = error != nil ? (long)error.code : 0;
    fprintf(stderr,
        "metal-command-buffer-failed context=%s label=%s status=%ld error_domain=%s error_code=%ld description=%s\n",
        context != NULL ? context : "(unknown)",
        label,
        (long)command_buffer.status,
        domain,
        code,
        desc);
}

static int termite_metal_commit_and_wait(id<MTLCommandBuffer> command_buffer, const char *context, int failure_code) {
    (void)context;
    if (command_buffer == nil) return failure_code;
    [command_buffer commit];
    [command_buffer waitUntilCompleted];
    if (command_buffer.status == MTLCommandBufferStatusCompleted) return 0;
    return failure_code;
}

static uint64_t termite_metal_command_buffer_gpu_elapsed_nanos(id<MTLCommandBuffer> command_buffer) {
    if (command_buffer == nil) return 0;
    if (![command_buffer respondsToSelector:@selector(GPUStartTime)] || ![command_buffer respondsToSelector:@selector(GPUEndTime)]) return 0;
    const CFTimeInterval start = command_buffer.GPUStartTime;
    const CFTimeInterval end = command_buffer.GPUEndTime;
    if (!(end > start) || !isfinite(start) || !isfinite(end)) return 0;
    return (uint64_t)((end - start) * 1000000000.0);
}

static id<MTLCommandBuffer> termite_metal_decode_runtime_command_buffer(termite_metal_decode_runtime *runtime, const char *label_cstr, bool *frame_owned) {
    if (runtime == NULL || frame_owned == NULL) return nil;
    *frame_owned = (runtime->active_frame_cb == nil);
    return *frame_owned ? termite_metal_new_command_buffer(runtime->queue, label_cstr) : runtime->active_frame_cb;
}

static bool termite_metal_dense_mps_linear_enabled(void) {
    return false;
}

static bool termite_metal_direct_packed_dense_enabled(void) {
    return true;
}

static bool termite_metal_direct_deberta_ffn_enabled(void) {
    const char *disabled = getenv("TERMITE_METAL_DISABLE_GLINER_DEBERTA_DIRECT_FFN");
    return disabled == NULL || disabled[0] == '\0' || strcmp(disabled, "0") == 0;
}

static void termite_metal_mpsgraph_ffn_cache_clear(termite_metal_mpsgraph_ffn_cache *cache) {
    if (cache == NULL) return;
    cache->graph = nil;
    cache->input_tensor = nil;
    cache->residual_tensor = nil;
    cache->w1_tensor = nil;
    cache->b1_tensor = nil;
    cache->w2_tensor = nil;
    cache->b2_tensor = nil;
    cache->ln_w_tensor = nil;
    cache->ln_b_tensor = nil;
    cache->output_tensor = nil;
    cache->rows = 0;
    cache->hidden_size = 0;
    cache->intermediate_size = 0;
    cache->activation_kind = UINT32_MAX;
}

static MPSGraphTensor *termite_metal_mpsgraph_gelu_tanh(MPSGraph *graph, MPSGraphTensor *x) {
    MPSGraphTensor *half = [graph constantWithScalar:0.5 dataType:MPSDataTypeFloat32];
    MPSGraphTensor *one = [graph constantWithScalar:1.0 dataType:MPSDataTypeFloat32];
    MPSGraphTensor *coef = [graph constantWithScalar:0.7978845608028654 dataType:MPSDataTypeFloat32];
    MPSGraphTensor *cubic_coef = [graph constantWithScalar:0.044715 dataType:MPSDataTypeFloat32];
    MPSGraphTensor *x2 = [graph multiplicationWithPrimaryTensor:x secondaryTensor:x name:@"gelu_x2"];
    MPSGraphTensor *x3 = [graph multiplicationWithPrimaryTensor:x2 secondaryTensor:x name:@"gelu_x3"];
    MPSGraphTensor *x3_scaled = [graph multiplicationWithPrimaryTensor:x3 secondaryTensor:cubic_coef name:@"gelu_x3_scaled"];
    MPSGraphTensor *inner_sum = [graph additionWithPrimaryTensor:x secondaryTensor:x3_scaled name:@"gelu_inner_sum"];
    MPSGraphTensor *inner = [graph multiplicationWithPrimaryTensor:inner_sum secondaryTensor:coef name:@"gelu_inner"];
    MPSGraphTensor *tanh_inner = [graph tanhWithTensor:inner name:@"gelu_tanh"];
    MPSGraphTensor *cdf = [graph additionWithPrimaryTensor:one secondaryTensor:tanh_inner name:@"gelu_cdf"];
    MPSGraphTensor *xh = [graph multiplicationWithPrimaryTensor:x secondaryTensor:half name:@"gelu_xh"];
    return [graph multiplicationWithPrimaryTensor:xh secondaryTensor:cdf name:@"gelu_out"];
}

static int termite_metal_mpsgraph_ffn_cache_prepare(
    termite_metal_decode_runtime *runtime,
    size_t rows,
    size_t hidden_size,
    size_t intermediate_size,
    uint32_t activation_kind,
    float eps
) {
    if (runtime == NULL) return -1;
    if (activation_kind != 0u && activation_kind != 1u) return -2;
    termite_metal_mpsgraph_ffn_cache *cache = &runtime->mpsgraph_ffn_cache;
    if (cache->graph != nil &&
        cache->rows == rows &&
        cache->hidden_size == hidden_size &&
        cache->intermediate_size == intermediate_size &&
        cache->activation_kind == activation_kind) {
        runtime->mpsgraph_ffn_cache_hits += 1;
        return 0;
    }

    termite_metal_mpsgraph_ffn_cache_clear(cache);
    @autoreleasepool {
        MPSGraph *graph = [MPSGraph new];
        if (graph == nil) return -3;
        MPSShape *hidden_shape = @[ @(rows), @(hidden_size) ];
        MPSShape *intermediate_shape = @[ @(rows), @(intermediate_size) ];
        MPSShape *w1_shape = @[ @(intermediate_size), @(hidden_size) ];
        MPSShape *b1_shape = @[ @(intermediate_size) ];
        MPSShape *w2_shape = @[ @(hidden_size), @(intermediate_size) ];
        MPSShape *b2_shape = @[ @(hidden_size) ];
        MPSGraphTensor *input = [graph placeholderWithShape:hidden_shape dataType:MPSDataTypeFloat32 name:@"input"];
        MPSGraphTensor *residual = [graph placeholderWithShape:hidden_shape dataType:MPSDataTypeFloat32 name:@"residual"];
        MPSGraphTensor *w1 = [graph placeholderWithShape:w1_shape dataType:MPSDataTypeFloat32 name:@"w1"];
        MPSGraphTensor *b1 = [graph placeholderWithShape:b1_shape dataType:MPSDataTypeFloat32 name:@"b1"];
        MPSGraphTensor *w2 = [graph placeholderWithShape:w2_shape dataType:MPSDataTypeFloat32 name:@"w2"];
        MPSGraphTensor *b2 = [graph placeholderWithShape:b2_shape dataType:MPSDataTypeFloat32 name:@"b2"];
        MPSGraphTensor *ln_w = [graph placeholderWithShape:b2_shape dataType:MPSDataTypeFloat32 name:@"ln_w"];
        MPSGraphTensor *ln_b = [graph placeholderWithShape:b2_shape dataType:MPSDataTypeFloat32 name:@"ln_b"];
        if (input == nil || residual == nil || w1 == nil || b1 == nil || w2 == nil || b2 == nil || ln_w == nil || ln_b == nil) return -4;

        MPSGraphTensor *w1_t = [graph transposeTensor:w1 dimension:0 withDimension:1 name:@"w1_t"];
        MPSGraphTensor *h1 = [graph matrixMultiplicationWithPrimaryTensor:input secondaryTensor:w1_t name:@"ffn_up"];
        MPSGraphTensor *h1_bias = [graph additionWithPrimaryTensor:h1 secondaryTensor:b1 name:@"ffn_up_bias"];
        MPSGraphTensor *h1_act = termite_metal_mpsgraph_gelu_tanh(graph, h1_bias);
        MPSGraphTensor *w2_t = [graph transposeTensor:w2 dimension:0 withDimension:1 name:@"w2_t"];
        MPSGraphTensor *h2 = [graph matrixMultiplicationWithPrimaryTensor:h1_act secondaryTensor:w2_t name:@"ffn_down"];
        MPSGraphTensor *h2_bias = [graph additionWithPrimaryTensor:h2 secondaryTensor:b2 name:@"ffn_down_bias"];
        MPSGraphTensor *residual_sum = [graph additionWithPrimaryTensor:h2_bias secondaryTensor:residual name:@"ffn_residual"];
        NSArray<NSNumber *> *axes = @[ @1 ];
        MPSGraphTensor *mean = [graph meanOfTensor:residual_sum axes:axes name:@"ln_mean"];
        MPSGraphTensor *variance = [graph varianceOfTensor:residual_sum meanTensor:mean axes:axes name:@"ln_variance"];
        MPSGraphTensor *output = [graph normalizationWithTensor:residual_sum
                                                     meanTensor:mean
                                                 varianceTensor:variance
                                                    gammaTensor:ln_w
                                                     betaTensor:ln_b
                                                        epsilon:eps
                                                           name:@"ln_out"];
        if (output == nil) return -5;

        cache->graph = graph;
        cache->input_tensor = input;
        cache->residual_tensor = residual;
        cache->w1_tensor = w1;
        cache->b1_tensor = b1;
        cache->w2_tensor = w2;
        cache->b2_tensor = b2;
        cache->ln_w_tensor = ln_w;
        cache->ln_b_tensor = ln_b;
        cache->output_tensor = output;
        cache->rows = rows;
        cache->hidden_size = hidden_size;
        cache->intermediate_size = intermediate_size;
        cache->activation_kind = activation_kind;
        runtime->mpsgraph_ffn_compiles += 1;
        return 0;
    }
}

static int termite_metal_decode_runtime_apply_mpsgraph_ffn_layer_norm_device(
    termite_metal_decode_runtime *runtime,
    size_t first_linear_slot,
    size_t second_linear_slot,
    size_t layer_norm_slot,
    id<MTLBuffer> input_buffer,
    id<MTLBuffer> residual_buffer,
    id<MTLBuffer> output_buffer,
    size_t rows,
    size_t hidden_size,
    size_t intermediate_size,
    uint32_t activation_kind,
    float eps
) {
    if (runtime == NULL || input_buffer == nil || residual_buffer == nil || output_buffer == nil) return -1;
    int cache_rc = termite_metal_mpsgraph_ffn_cache_prepare(runtime, rows, hidden_size, intermediate_size, activation_kind, eps);
    if (cache_rc != 0) return -2;
    termite_metal_mpsgraph_ffn_cache *cache = &runtime->mpsgraph_ffn_cache;
    if (cache->graph == nil || cache->output_tensor == nil) return -3;

    const bool frame_owned = (runtime->active_frame_cb == nil);
    if (!frame_owned && runtime->active_planned_compute_encoder != nil) {
        termite_metal_decode_runtime_close_planned_compute_encoder_for_transition(runtime);
    }
    id<MTLCommandBuffer> command_buffer = frame_owned
        ? termite_metal_new_command_buffer(runtime->queue, __func__)
        : runtime->active_frame_cb;
    if (command_buffer == nil) return -4;
    MPSCommandBuffer *mps_command_buffer = [MPSCommandBuffer commandBufferWithCommandBuffer:command_buffer];
    if (mps_command_buffer == nil) return -5;

    MPSShape *hidden_shape = @[ @(rows), @(hidden_size) ];
    MPSShape *intermediate_shape = @[ @(rows), @(intermediate_size) ];
    MPSShape *w1_shape = @[ @(intermediate_size), @(hidden_size) ];
    MPSShape *b1_shape = @[ @(intermediate_size) ];
    MPSShape *w2_shape = @[ @(hidden_size), @(intermediate_size) ];
    MPSShape *b2_shape = @[ @(hidden_size) ];
    MPSGraphTensorData *input_data = [[MPSGraphTensorData alloc] initWithMTLBuffer:input_buffer shape:hidden_shape dataType:MPSDataTypeFloat32];
    MPSGraphTensorData *residual_data = [[MPSGraphTensorData alloc] initWithMTLBuffer:residual_buffer shape:hidden_shape dataType:MPSDataTypeFloat32];
    MPSGraphTensorData *w1_data = [[MPSGraphTensorData alloc] initWithMTLBuffer:runtime->linear_weight_buffers[first_linear_slot] shape:w1_shape dataType:MPSDataTypeFloat32];
    MPSGraphTensorData *b1_data = [[MPSGraphTensorData alloc] initWithMTLBuffer:runtime->linear_bias_buffers[first_linear_slot] shape:b1_shape dataType:MPSDataTypeFloat32];
    MPSGraphTensorData *w2_data = [[MPSGraphTensorData alloc] initWithMTLBuffer:runtime->linear_weight_buffers[second_linear_slot] shape:w2_shape dataType:MPSDataTypeFloat32];
    MPSGraphTensorData *b2_data = [[MPSGraphTensorData alloc] initWithMTLBuffer:runtime->linear_bias_buffers[second_linear_slot] shape:b2_shape dataType:MPSDataTypeFloat32];
    MPSGraphTensorData *ln_w_data = [[MPSGraphTensorData alloc] initWithMTLBuffer:runtime->layer_norm_weight_buffers[layer_norm_slot] shape:b2_shape dataType:MPSDataTypeFloat32];
    MPSGraphTensorData *ln_b_data = [[MPSGraphTensorData alloc] initWithMTLBuffer:runtime->layer_norm_bias_buffers[layer_norm_slot] shape:b2_shape dataType:MPSDataTypeFloat32];
    MPSGraphTensorData *output_data = [[MPSGraphTensorData alloc] initWithMTLBuffer:output_buffer shape:hidden_shape dataType:MPSDataTypeFloat32];
    if (input_data == nil || residual_data == nil || w1_data == nil || b1_data == nil || w2_data == nil || b2_data == nil || ln_w_data == nil || ln_b_data == nil || output_data == nil) return -6;

    MPSGraphTensorDataDictionary *feeds = @{
        cache->input_tensor: input_data,
        cache->residual_tensor: residual_data,
        cache->w1_tensor: w1_data,
        cache->b1_tensor: b1_data,
        cache->w2_tensor: w2_data,
        cache->b2_tensor: b2_data,
        cache->ln_w_tensor: ln_w_data,
        cache->ln_b_tensor: ln_b_data,
    };
    MPSGraphTensorDataDictionary *results = @{
        cache->output_tensor: output_data,
    };
    if (feeds == nil || results == nil) return -7;
    [cache->graph encodeToCommandBuffer:mps_command_buffer
                                  feeds:feeds
                       targetOperations:nil
                      resultsDictionary:results
                    executionDescriptor:nil];
    if (!frame_owned) {
        runtime->active_frame_cb = mps_command_buffer.rootCommandBuffer;
        if (termite_metal_decode_runtime_retain_frame_resource(runtime, mps_command_buffer) != 0 ||
            termite_metal_decode_runtime_retain_frame_resource(runtime, feeds) != 0 ||
            termite_metal_decode_runtime_retain_frame_resource(runtime, results) != 0) {
            return -8;
        }
        runtime->mpsgraph_ffn_calls += 1;
        return 0;
    }
    const uint64_t wait_start = termite_metal_clock_monotonic_nanos();
    [mps_command_buffer commit];
    [mps_command_buffer waitUntilCompleted];
    const uint64_t wait_end = termite_metal_clock_monotonic_nanos();
    if (wait_end > wait_start) runtime->mps_dense_linear_standalone_wait_nanos += wait_end - wait_start;
    if (mps_command_buffer.status == MTLCommandBufferStatusCompleted) {
        runtime->mps_dense_linear_standalone_gpu_nanos += termite_metal_command_buffer_gpu_elapsed_nanos(mps_command_buffer);
        runtime->mpsgraph_ffn_calls += 1;
        return 0;
    }
    return -9;
}

static int termite_metal_decode_runtime_finish_command_buffer(id<MTLCommandBuffer> command_buffer, bool frame_owned, int failure_code) {
    if (!frame_owned) return 0;
    [command_buffer commit];
    [command_buffer waitUntilCompleted];
    return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : failure_code;
}

static void termite_metal_decode_runtime_set_quant_linear_descriptor(
    termite_metal_decode_runtime *runtime,
    size_t slot,
    uint32_t format,
    size_t values_per_block,
    size_t bytes_per_block,
    size_t in_dim
) {
    if (runtime == NULL || slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || values_per_block == 0) return;
    runtime->quant_linear_formats[slot] = format;
    runtime->quant_linear_values_per_block[slot] = values_per_block;
    runtime->quant_linear_bytes_per_block[slot] = bytes_per_block;
    runtime->quant_linear_row_blocks[slot] = in_dim / values_per_block;
    runtime->quant_linear_row_stride_bytes[slot] = (in_dim / values_per_block) * bytes_per_block;
}

static void termite_metal_decode_runtime_clear_quant_linear_descriptor(termite_metal_decode_runtime *runtime, size_t slot) {
    termite_metal_decode_runtime_set_quant_linear_descriptor(runtime, slot, TERMITE_METAL_QUANT_FORMAT_UNSUPPORTED, 1u, 0u, 0u);
    if (runtime == NULL || slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return;
    termite_metal_decode_runtime_invalidate_q8_qkv_pack_slot(runtime, slot);
    runtime->quant_linear_weight_buffers[slot] = nil;
    runtime->quant_linear_in_dims[slot] = 0;
    runtime->quant_linear_out_dims[slot] = 0;
    runtime->quant_linear_slot_prepared[slot] = 0;
    runtime->quant_linear_tl1_packed_len[slot] = 0;
    runtime->quant_linear_tl1_bm[slot] = 0;
    runtime->quant_linear_tl1_cfg_by[slot] = 0;
    runtime->quant_linear_tl1_bmm[slot] = 0;
    runtime->quant_linear_tl2_scale_off[slot] = 0;
    runtime->quant_linear_tl2_three_value_len[slot] = 0;
    runtime->quant_linear_tl2_three_sign_len[slot] = 0;
    runtime->quant_linear_tl2_bm[slot] = 0;
    runtime->quant_linear_tl2_cfg_by[slot] = 0;
    runtime->quant_linear_tl2_bmm[slot] = 0;
    runtime->quant_linear_tl2_three_cols[slot] = 0;
    runtime->quant_linear_tl2_two_cols[slot] = 0;
}

static int termite_metal_decode_runtime_check_quant_linear_descriptor(
    termite_metal_decode_runtime *runtime,
    size_t slot,
    uint32_t format,
    size_t values_per_block,
    size_t bytes_per_block,
    size_t in_dim
) {
    if (runtime == NULL) return -1;
    if (slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -2;
    if (values_per_block == 0 || in_dim % values_per_block != 0) return -3;
    if (runtime->quant_linear_formats[slot] != format) return -4;
    if (runtime->quant_linear_values_per_block[slot] != values_per_block) return -5;
    if (runtime->quant_linear_bytes_per_block[slot] != bytes_per_block) return -6;
    if (runtime->quant_linear_row_blocks[slot] != in_dim / values_per_block) return -7;
    if (runtime->quant_linear_row_stride_bytes[slot] != (in_dim / values_per_block) * bytes_per_block) return -8;
    return 0;
}

typedef struct termite_metal_quant_linear_slot_view {
    id<MTLComputePipelineState> pipeline;
    id<MTLBuffer> weight_buffer;
    uint8_t prepared;
    size_t in_dim;
    size_t out_dim;
    uint32_t format;
    size_t values_per_block;
    size_t bytes_per_block;
} termite_metal_quant_linear_slot_view;

static int termite_metal_decode_runtime_ensure_q8_qkv_packed_weight(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    size_t q_slot,
    size_t k_slot,
    size_t v_slot,
    size_t in_dim,
    size_t q_out_dim,
    size_t kv_out_dim,
    const termite_metal_quant_linear_slot_view *q_view,
    const termite_metal_quant_linear_slot_view *k_view,
    const termite_metal_quant_linear_slot_view *v_view
) {
    if (runtime == NULL || command_buffer == nil || q_view == NULL || k_view == NULL || v_view == NULL) return -1;
    if (q_slot >= TERMITE_METAL_DENSE_QKV_PACK_CACHE_CAPACITY) return -1;
    if (q_view->weight_buffer == nil || k_view->weight_buffer == nil || v_view->weight_buffer == nil) return -2;
    if (q_view->values_per_block == 0 || q_view->bytes_per_block == 0) return -2;
    if (q_view->values_per_block != k_view->values_per_block ||
        q_view->values_per_block != v_view->values_per_block ||
        q_view->bytes_per_block != k_view->bytes_per_block ||
        q_view->bytes_per_block != v_view->bytes_per_block)
    {
        return -2;
    }
    if (in_dim % q_view->values_per_block != 0) return -2;
    const size_t row_blocks = in_dim / q_view->values_per_block;
    if (row_blocks == 0 || row_blocks > SIZE_MAX / q_view->bytes_per_block) return -2;
    const size_t row_stride_bytes = row_blocks * q_view->bytes_per_block;
    const size_t total_out_dim = q_out_dim + kv_out_dim + kv_out_dim;
    if (total_out_dim == 0 || total_out_dim > SIZE_MAX / row_stride_bytes) return -2;
    const size_t q_bytes = q_out_dim * row_stride_bytes;
    const size_t kv_bytes = kv_out_dim * row_stride_bytes;
    const size_t packed_bytes = total_out_dim * row_stride_bytes;
    if (q_view->weight_buffer.length < q_bytes ||
        k_view->weight_buffer.length < kv_bytes ||
        v_view->weight_buffer.length < kv_bytes)
    {
        return -2;
    }
    if (runtime->q8_qkv_packed_weight_buffers[q_slot] != nil &&
        runtime->q8_qkv_packed_weight_buffers[q_slot].length >= packed_bytes &&
        runtime->q8_qkv_packed_k_slots[q_slot] == k_slot &&
        runtime->q8_qkv_packed_v_slots[q_slot] == v_slot &&
        runtime->q8_qkv_packed_in_dims[q_slot] == in_dim &&
        runtime->q8_qkv_packed_q_out_dims[q_slot] == q_out_dim &&
        runtime->q8_qkv_packed_kv_out_dims[q_slot] == kv_out_dim &&
        runtime->q8_qkv_packed_row_stride_bytes[q_slot] == row_stride_bytes)
    {
        return 0;
    }

    id<MTLBuffer> packed = [runtime->device newBufferWithLength:packed_bytes options:MTLResourceStorageModePrivate];
    if (packed == nil) return -3;
    id<MTLBlitCommandEncoder> blit = termite_metal_tracked_blit_command_encoder(command_buffer);
    if (blit == nil) return -4;
    [blit copyFromBuffer:q_view->weight_buffer sourceOffset:0 toBuffer:packed destinationOffset:0 size:q_bytes];
    [blit copyFromBuffer:k_view->weight_buffer sourceOffset:0 toBuffer:packed destinationOffset:q_bytes size:kv_bytes];
    [blit copyFromBuffer:v_view->weight_buffer sourceOffset:0 toBuffer:packed destinationOffset:q_bytes + kv_bytes size:kv_bytes];
    [blit endEncoding];

    runtime->q8_qkv_packed_weight_buffers[q_slot] = packed;
    runtime->q8_qkv_packed_k_slots[q_slot] = k_slot;
    runtime->q8_qkv_packed_v_slots[q_slot] = v_slot;
    runtime->q8_qkv_packed_in_dims[q_slot] = in_dim;
    runtime->q8_qkv_packed_q_out_dims[q_slot] = q_out_dim;
    runtime->q8_qkv_packed_kv_out_dims[q_slot] = kv_out_dim;
    runtime->q8_qkv_packed_row_stride_bytes[q_slot] = row_stride_bytes;
    return 0;
}

static bool termite_metal_quant_format_block_layout(
    uint32_t format,
    size_t *values_per_block,
    size_t *bytes_per_block
) {
    if (values_per_block == NULL || bytes_per_block == NULL) return false;
    switch (format) {
        case TERMITE_METAL_QUANT_FORMAT_Q1_0: *values_per_block = 128u; *bytes_per_block = 18u; return true;
        case TERMITE_METAL_QUANT_FORMAT_I2_S: *values_per_block = 128u; *bytes_per_block = 32u; return true;
        case TERMITE_METAL_QUANT_FORMAT_I8_S: *values_per_block = 1u; *bytes_per_block = 1u; return true;
        case TERMITE_METAL_QUANT_FORMAT_Q2_K: *values_per_block = 256u; *bytes_per_block = 84u; return true;
        case TERMITE_METAL_QUANT_FORMAT_Q3_K: *values_per_block = 256u; *bytes_per_block = 110u; return true;
        case TERMITE_METAL_QUANT_FORMAT_Q4_0: *values_per_block = 32u; *bytes_per_block = 18u; return true;
        case TERMITE_METAL_QUANT_FORMAT_Q4_1: *values_per_block = 32u; *bytes_per_block = 20u; return true;
        case TERMITE_METAL_QUANT_FORMAT_Q4_K: *values_per_block = 256u; *bytes_per_block = 144u; return true;
        case TERMITE_METAL_QUANT_FORMAT_Q5_0: *values_per_block = 32u; *bytes_per_block = 22u; return true;
        case TERMITE_METAL_QUANT_FORMAT_Q5_1: *values_per_block = 32u; *bytes_per_block = 24u; return true;
        case TERMITE_METAL_QUANT_FORMAT_Q5_K: *values_per_block = 256u; *bytes_per_block = 176u; return true;
        case TERMITE_METAL_QUANT_FORMAT_Q6_K: *values_per_block = 256u; *bytes_per_block = 210u; return true;
        case TERMITE_METAL_QUANT_FORMAT_Q8_0: *values_per_block = 32u; *bytes_per_block = 34u; return true;
        case TERMITE_METAL_QUANT_FORMAT_Q8_1: *values_per_block = 32u; *bytes_per_block = 36u; return true;
        case TERMITE_METAL_QUANT_FORMAT_Q8_K: *values_per_block = 256u; *bytes_per_block = 292u; return true;
        case TERMITE_METAL_QUANT_FORMAT_IQ2_XS: *values_per_block = 256u; *bytes_per_block = 74u; return true;
        case TERMITE_METAL_QUANT_FORMAT_IQ4_NL: *values_per_block = 32u; *bytes_per_block = 18u; return true;
        case TERMITE_METAL_QUANT_FORMAT_IQ4_XS: *values_per_block = 256u; *bytes_per_block = 136u; return true;
        case TERMITE_METAL_QUANT_FORMAT_MXFP4: *values_per_block = 32u; *bytes_per_block = 17u; return true;
        case TERMITE_METAL_QUANT_FORMAT_NVFP4: *values_per_block = 64u; *bytes_per_block = 36u; return true;
        case TERMITE_METAL_QUANT_FORMAT_TL1:
        case TERMITE_METAL_QUANT_FORMAT_TL2: *values_per_block = 1u; *bytes_per_block = 1u; return true;
        default: return false;
    }
}

typedef enum termite_metal_quant_matmul_epilogue {
    TERMITE_METAL_QUANT_MATMUL_EPILOGUE_NONE = 0,
    TERMITE_METAL_QUANT_MATMUL_EPILOGUE_PAIR_ACTIVATION_MUL = 1,
    TERMITE_METAL_QUANT_MATMUL_EPILOGUE_QKV = 2,
    TERMITE_METAL_QUANT_MATMUL_EPILOGUE_PAIR = 3,
    TERMITE_METAL_QUANT_MATMUL_EPILOGUE_ACTIVATION_RHS_MUL = 4,
    TERMITE_METAL_QUANT_MATMUL_EPILOGUE_PAIR_ACTIVATION_RMS_SCALE_1X = 5,
} termite_metal_quant_matmul_epilogue;

typedef struct termite_metal_quant_matmul_descriptor {
    size_t slot;
    uint32_t format;
    termite_metal_quant_matmul_activation_dtype activation_dtype;
    termite_metal_quant_matmul_activation_dtype input_dtype;
    termite_metal_quant_matmul_activation_dtype output_dtype;
    termite_metal_quant_matmul_epilogue epilogue;
    id<MTLComputePipelineState> fallback_pipeline;
    id<MTLBuffer> input_buffer;
    size_t input_offset;
    id<MTLBuffer> weight_buffer;
    id<MTLBuffer> second_weight_buffer;
    id<MTLBuffer> third_weight_buffer;
    id<MTLBuffer> output_buffer;
    size_t output_offset;
    id<MTLBuffer> second_output_buffer;
    size_t second_output_offset;
    id<MTLBuffer> third_output_buffer;
    size_t third_output_offset;
    id<MTLBuffer> rhs_buffer;
    size_t rhs_offset;
    id<MTLBuffer> inv_scale_buffer;
    size_t inv_scale_offset;
    size_t norm_slot;
    size_t rows;
    size_t in_dim;
    size_t out_dim;
    size_t second_out_dim;
    size_t values_per_block;
    size_t bytes_per_block;
    uint32_t activation_kind;
    termite_metal_q8_0_linear_family_kind q8_family;
    bool use_planned_dispatch;
    uint8_t planned_dispatch;
    int failure_code;
} termite_metal_quant_matmul_descriptor;

static uint8_t termite_metal_quant_matmul_descriptor_planned_dispatch(const termite_metal_quant_matmul_descriptor *descriptor);

static size_t termite_metal_quant_matmul_dtype_size(termite_metal_quant_matmul_activation_dtype dtype) {
    switch (dtype) {
        case TERMITE_METAL_QUANT_MATMUL_ACTIVATION_BF16:
        case TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F16:
            return sizeof(uint16_t);
        case TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32:
        default:
            return sizeof(float);
    }
}

static termite_metal_quant_matmul_activation_dtype termite_metal_quant_matmul_dtype_from_command(uint8_t dtype) {
    switch (dtype) {
        case TERMITE_METAL_QUANT_MATMUL_ACTIVATION_BF16:
            return TERMITE_METAL_QUANT_MATMUL_ACTIVATION_BF16;
        case TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F16:
            return TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F16;
        case TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32:
        default:
            return TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32;
    }
}

static bool termite_metal_quant_matmul_descriptor_dtype_supported(
    termite_metal_decode_runtime *runtime,
    const termite_metal_quant_matmul_descriptor *descriptor
) {
    if (runtime == NULL || descriptor == NULL) return false;
    if (descriptor->activation_dtype != TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32) return false;
    if (descriptor->input_dtype == TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32 &&
        descriptor->output_dtype == TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32) return true;
    if (descriptor->format != TERMITE_METAL_QUANT_FORMAT_Q8_0 ||
        descriptor->values_per_block != 32u ||
        descriptor->bytes_per_block != 34u) return false;
    const uint8_t planned_dispatch = termite_metal_quant_matmul_descriptor_planned_dispatch(descriptor);
    const termite_metal_q8_0_linear_dispatch_kind dispatch_kind = termite_metal_q8_0_dispatch_from_quant_dispatch(
        termite_metal_select_quant_matmul_dispatch_preferred(
            descriptor->format,
            descriptor->rows,
            descriptor->in_dim,
            descriptor->out_dim,
            descriptor->values_per_block,
            planned_dispatch));
    if (descriptor->epilogue == TERMITE_METAL_QUANT_MATMUL_EPILOGUE_PAIR_ACTIVATION_MUL &&
        descriptor->input_dtype == TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32 &&
        descriptor->output_dtype == TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F16) {
        return dispatch_kind == TERMITE_METAL_Q8_0_LINEAR_DISPATCH_MM &&
            runtime->q8_0_pair_activation_mm_f16_output_pipeline != nil;
    }
    if (descriptor->epilogue == TERMITE_METAL_QUANT_MATMUL_EPILOGUE_PAIR_ACTIVATION_RMS_SCALE_1X &&
        descriptor->rows == 1u &&
        descriptor->input_dtype == TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32 &&
        descriptor->output_dtype == TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F16) {
        return (dispatch_kind == TERMITE_METAL_Q8_0_LINEAR_DISPATCH_MMV ||
                dispatch_kind == TERMITE_METAL_Q8_0_LINEAR_DISPATCH_SCALAR) &&
            runtime->q8_0_pair_activation_rms_scale_mmv_f16_output_pipeline != nil;
    }
    if (descriptor->epilogue == TERMITE_METAL_QUANT_MATMUL_EPILOGUE_NONE &&
        descriptor->input_dtype == TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F16 &&
        descriptor->output_dtype == TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32) {
        return (dispatch_kind == TERMITE_METAL_Q8_0_LINEAR_DISPATCH_MM &&
                runtime->q8_0_mm_f16_input_pipeline != nil) ||
            ((dispatch_kind == TERMITE_METAL_Q8_0_LINEAR_DISPATCH_MMV ||
                dispatch_kind == TERMITE_METAL_Q8_0_LINEAR_DISPATCH_SCALAR) &&
                runtime->q8_0_mmv_f16_input_pipeline != nil);
    }
    return false;
}

static int termite_metal_encode_quant_matmul_none_on_encoder(
    termite_metal_decode_runtime *runtime,
    id<MTLComputeCommandEncoder> encoder,
    const termite_metal_quant_matmul_descriptor *descriptor
);
static int termite_metal_encode_quant_matmul_pair_activation_mul_on_encoder(
    termite_metal_decode_runtime *runtime,
    id<MTLComputeCommandEncoder> encoder,
    const termite_metal_quant_matmul_descriptor *descriptor
);
static int termite_metal_encode_quant_matmul_pair_activation_rms_scale_1x_on_encoder(
    termite_metal_decode_runtime *runtime,
    id<MTLComputeCommandEncoder> encoder,
    const termite_metal_quant_matmul_descriptor *descriptor
);
static int termite_metal_encode_quant_matmul_activation_rhs_mul_on_encoder(
    termite_metal_decode_runtime *runtime,
    id<MTLComputeCommandEncoder> encoder,
    const termite_metal_quant_matmul_descriptor *descriptor
);
static int termite_metal_encode_quant_matmul_pair_on_encoder(
    termite_metal_decode_runtime *runtime,
    id<MTLComputeCommandEncoder> encoder,
    const termite_metal_quant_matmul_descriptor *descriptor
);
static int termite_metal_encode_quant_matmul_generic_pair_on_encoder(
    termite_metal_decode_runtime *runtime,
    id<MTLComputeCommandEncoder> encoder,
    const termite_metal_quant_matmul_descriptor *descriptor
);
static int termite_metal_encode_quant_matmul_qkv_on_encoder(
    termite_metal_decode_runtime *runtime,
    id<MTLComputeCommandEncoder> encoder,
    const termite_metal_quant_matmul_descriptor *descriptor
);
static int termite_metal_encode_quant_matmul_generic_none_on_encoder(
    termite_metal_decode_runtime *runtime,
    id<MTLComputeCommandEncoder> encoder,
    const termite_metal_quant_matmul_descriptor *descriptor
);

static void termite_metal_decode_runtime_set_quant_linear_slot_view(
    termite_metal_quant_linear_slot_view *view,
    id<MTLComputePipelineState> pipeline,
    id<MTLBuffer> weight_buffer,
    uint8_t prepared,
    size_t in_dim,
    size_t out_dim,
    uint32_t format,
    size_t values_per_block,
    size_t bytes_per_block
) {
    view->pipeline = pipeline;
    view->weight_buffer = weight_buffer;
    view->prepared = prepared;
    view->in_dim = in_dim;
    view->out_dim = out_dim;
    view->format = format;
    view->values_per_block = values_per_block;
    view->bytes_per_block = bytes_per_block;
}

static int termite_metal_decode_runtime_quant_linear_slot_view(
    termite_metal_decode_runtime *runtime,
    uint32_t format,
    size_t slot,
    termite_metal_quant_linear_slot_view *view
) {
    if (runtime == NULL || view == NULL) return -1;
    if (slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -2;
    if (runtime->quant_linear_formats[slot] != format) return -4;
    id<MTLComputePipelineState> pipeline = nil;
    switch (format) {
        case TERMITE_METAL_QUANT_FORMAT_Q1_0:
            pipeline = runtime->q1_0_pipeline;
            break;
        case TERMITE_METAL_QUANT_FORMAT_I2_S:
            pipeline = runtime->i2_s_pipeline;
            break;
        case TERMITE_METAL_QUANT_FORMAT_I8_S:
            pipeline = runtime->i8_s_pipeline;
            break;
        case TERMITE_METAL_QUANT_FORMAT_Q2_K:
            pipeline = runtime->q2_k_pipeline;
            break;
        case TERMITE_METAL_QUANT_FORMAT_Q3_K:
            pipeline = runtime->q3_k_pipeline;
            break;
        case TERMITE_METAL_QUANT_FORMAT_Q4_0:
            pipeline = runtime->q4_0_pipeline;
            break;
        case TERMITE_METAL_QUANT_FORMAT_Q4_1:
            pipeline = runtime->q4_1_pipeline;
            break;
        case TERMITE_METAL_QUANT_FORMAT_Q5_0:
            pipeline = runtime->q5_0_pipeline;
            break;
        case TERMITE_METAL_QUANT_FORMAT_Q5_1:
            pipeline = runtime->q5_1_pipeline;
            break;
        case TERMITE_METAL_QUANT_FORMAT_Q4_K:
            pipeline = runtime->q4_k_pipeline;
            break;
        case TERMITE_METAL_QUANT_FORMAT_Q5_K:
            pipeline = runtime->q5_k_pipeline;
            break;
        case TERMITE_METAL_QUANT_FORMAT_Q6_K:
            pipeline = runtime->q6_k_pipeline;
            break;
        case TERMITE_METAL_QUANT_FORMAT_Q8_0:
            pipeline = runtime->q8_0_pipeline;
            break;
        case TERMITE_METAL_QUANT_FORMAT_Q8_1:
            pipeline = runtime->q8_1_pipeline;
            break;
        case TERMITE_METAL_QUANT_FORMAT_Q8_K:
            pipeline = runtime->q8_k_pipeline;
            break;
        case TERMITE_METAL_QUANT_FORMAT_IQ4_NL:
            pipeline = runtime->iq4_nl_pipeline;
            break;
        case TERMITE_METAL_QUANT_FORMAT_IQ4_XS:
            pipeline = runtime->iq4_xs_pipeline;
            break;
        case TERMITE_METAL_QUANT_FORMAT_MXFP4:
            pipeline = runtime->mxfp4_pipeline;
            break;
        case TERMITE_METAL_QUANT_FORMAT_NVFP4:
            pipeline = runtime->nvfp4_pipeline;
            break;
        case TERMITE_METAL_QUANT_FORMAT_IQ2_XS:
            pipeline = runtime->iq2_xs_pipeline;
            break;
        case TERMITE_METAL_QUANT_FORMAT_TL1:
            pipeline = runtime->tl1_pipeline;
            break;
        case TERMITE_METAL_QUANT_FORMAT_TL2:
            pipeline = runtime->tl2_pipeline;
            break;
        default:
            return -3;
    }
    termite_metal_decode_runtime_set_quant_linear_slot_view(view, pipeline, runtime->quant_linear_weight_buffers[slot], runtime->quant_linear_slot_prepared[slot], runtime->quant_linear_in_dims[slot], runtime->quant_linear_out_dims[slot], format, runtime->quant_linear_values_per_block[slot], runtime->quant_linear_bytes_per_block[slot]);
    return 0;
}

static int termite_metal_decode_runtime_require_quant_linear_slot(
    termite_metal_decode_runtime *runtime,
    uint32_t format,
    size_t slot,
    size_t in_dim,
    size_t out_dim,
    termite_metal_quant_linear_slot_view *view
) {
    int rc = termite_metal_decode_runtime_quant_linear_slot_view(runtime, format, slot, view);
    if (rc != 0) return rc;
    if (view->pipeline == nil || view->weight_buffer == nil) return -20;
    if (view->prepared == 0) return -21;
    if (view->in_dim != in_dim || view->out_dim != out_dim) return -22;
    if (termite_metal_decode_runtime_check_quant_linear_descriptor(runtime, slot, format, view->values_per_block, view->bytes_per_block, in_dim) != 0) return -23;
    return 0;
}

static uint8_t termite_metal_quant_matmul_descriptor_planned_dispatch(
    const termite_metal_quant_matmul_descriptor *descriptor
) {
    return (descriptor != NULL && descriptor->use_planned_dispatch)
        ? descriptor->planned_dispatch
        : TERMITE_METAL_QUANT_MATMUL_DISPATCH_UNKNOWN;
}

static int termite_metal_prepare_quant_matmul_accesses(
    termite_metal_decode_runtime *runtime,
    const termite_metal_quant_matmul_descriptor *descriptor,
    int failure_code
);

static int termite_metal_encode_quant_matmul_generic_pair_on_encoder(
    termite_metal_decode_runtime *runtime,
    id<MTLComputeCommandEncoder> encoder,
    const termite_metal_quant_matmul_descriptor *descriptor
) {
    if (runtime == NULL || encoder == nil || descriptor == NULL) return -1;
    const int failure_code = descriptor->failure_code;
    if (descriptor->activation_dtype != TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32 ||
        descriptor->epilogue != TERMITE_METAL_QUANT_MATMUL_EPILOGUE_PAIR ||
        descriptor->input_buffer == nil ||
        descriptor->weight_buffer == nil ||
        descriptor->second_weight_buffer == nil ||
        descriptor->output_buffer == nil ||
        descriptor->second_output_buffer == nil) return failure_code;
    if (descriptor->rows == 0 ||
        descriptor->in_dim == 0 ||
        descriptor->out_dim == 0 ||
        descriptor->values_per_block == 0 ||
        descriptor->in_dim % descriptor->values_per_block != 0) return failure_code;
    size_t expected_values_per_block = 0;
    size_t expected_bytes_per_block = 0;
    if (!termite_metal_quant_format_block_layout(descriptor->format, &expected_values_per_block, &expected_bytes_per_block) ||
        descriptor->values_per_block != expected_values_per_block ||
        descriptor->bytes_per_block != expected_bytes_per_block) return failure_code;
    if (termite_metal_prepare_quant_matmul_accesses(runtime, descriptor, failure_code) != 0) return failure_code;

    id<MTLComputePipelineState> pipeline = descriptor->fallback_pipeline;
    BOOL two_dimensional_grid = NO;
    switch (descriptor->format) {
        case TERMITE_METAL_QUANT_FORMAT_I2_S:
            pipeline = pipeline != nil ? pipeline : runtime->i2_s_pair_pipeline;
            break;
        case TERMITE_METAL_QUANT_FORMAT_Q4_0:
            pipeline = pipeline != nil ? pipeline : runtime->q4_0_pair_pipeline;
            two_dimensional_grid = YES;
            break;
        case TERMITE_METAL_QUANT_FORMAT_Q4_K:
            pipeline = pipeline != nil ? pipeline : runtime->q4_k_pair_pipeline;
            two_dimensional_grid = YES;
            break;
        case TERMITE_METAL_QUANT_FORMAT_Q6_K:
            pipeline = pipeline != nil ? pipeline : runtime->q6_k_pair_pipeline;
            two_dimensional_grid = YES;
            break;
        default:
            return failure_code;
    }
    if (pipeline == nil) return failure_code;

    termite_metal_linear_params params = {
        .rows = (uint32_t)descriptor->rows,
        .in_dim = (uint32_t)descriptor->in_dim,
        .out_dim = (uint32_t)descriptor->out_dim,
        .row_blocks = (uint32_t)(descriptor->in_dim / descriptor->values_per_block),
    };
    [encoder setComputePipelineState:pipeline];
    [encoder setBuffer:descriptor->input_buffer offset:descriptor->input_offset atIndex:0];
    [encoder setBuffer:descriptor->weight_buffer offset:0 atIndex:1];
    [encoder setBuffer:descriptor->second_weight_buffer offset:0 atIndex:2];
    [encoder setBuffer:descriptor->output_buffer offset:descriptor->output_offset atIndex:3];
    [encoder setBuffer:descriptor->second_output_buffer offset:descriptor->second_output_offset atIndex:4];
    [encoder setBytes:&params length:sizeof(params) atIndex:5];
    if (two_dimensional_grid) {
        NSUInteger thread_width = pipeline.maxTotalThreadsPerThreadgroup;
        if (thread_width == 0) thread_width = 32;
        if (thread_width > descriptor->out_dim && descriptor->out_dim > 0) thread_width = descriptor->out_dim;
        [encoder dispatchThreads:MTLSizeMake(descriptor->out_dim, descriptor->rows, 1) threadsPerThreadgroup:MTLSizeMake(thread_width, 1, 1)];
    } else {
        const size_t total = descriptor->rows * descriptor->out_dim;
        [encoder dispatchThreads:MTLSizeMake(total, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(pipeline, total), 1, 1)];
    }
    return 0;
}

static int termite_metal_prepare_quant_matmul_accesses(
    termite_metal_decode_runtime *runtime,
    const termite_metal_quant_matmul_descriptor *descriptor,
    int failure_code
) {
    if (runtime == NULL || descriptor == NULL || runtime->active_planned_compute_encoder == nil) return 0;

    size_t input_bytes = 0;
    size_t output_bytes = 0;
    if (!termite_metal_size_mul3(descriptor->rows, descriptor->in_dim, termite_metal_quant_matmul_dtype_size(descriptor->input_dtype), &input_bytes)) return failure_code;
    if (!termite_metal_size_mul3(descriptor->rows, descriptor->out_dim, termite_metal_quant_matmul_dtype_size(descriptor->output_dtype), &output_bytes)) return failure_code;

    termite_metal_planned_encoder_range accesses[12];
    size_t count = 0;
#define TERMITE_PLAN_ACCESS(access_buffer, offset, bytes, kind) do { \
        if (termite_metal_planned_range_make((access_buffer), (offset), (bytes), (kind), &accesses[count], failure_code) != 0) return failure_code; \
        if (accesses[count].buffer != nil) count += 1; \
    } while (0)

    TERMITE_PLAN_ACCESS(descriptor->input_buffer, descriptor->input_offset, input_bytes, TERMITE_METAL_PLANNED_RANGE_READ);
    TERMITE_PLAN_ACCESS(descriptor->weight_buffer, 0, descriptor->weight_buffer.length, TERMITE_METAL_PLANNED_RANGE_READ);
    TERMITE_PLAN_ACCESS(descriptor->output_buffer, descriptor->output_offset, output_bytes, TERMITE_METAL_PLANNED_RANGE_WRITE);

    if (descriptor->second_weight_buffer != nil) {
        TERMITE_PLAN_ACCESS(descriptor->second_weight_buffer, 0, descriptor->second_weight_buffer.length, TERMITE_METAL_PLANNED_RANGE_READ);
    }
    if (descriptor->third_weight_buffer != nil) {
        TERMITE_PLAN_ACCESS(descriptor->third_weight_buffer, 0, descriptor->third_weight_buffer.length, TERMITE_METAL_PLANNED_RANGE_READ);
    }
    if (descriptor->second_output_buffer != nil) {
        const size_t second_out_dim = descriptor->second_out_dim != 0 ? descriptor->second_out_dim : descriptor->out_dim;
        size_t second_output_bytes = 0;
        if (!termite_metal_size_mul3(descriptor->rows, second_out_dim, termite_metal_quant_matmul_dtype_size(descriptor->output_dtype), &second_output_bytes)) return failure_code;
        TERMITE_PLAN_ACCESS(descriptor->second_output_buffer, descriptor->second_output_offset, second_output_bytes, TERMITE_METAL_PLANNED_RANGE_WRITE);
    }
    if (descriptor->third_output_buffer != nil) {
        const size_t third_out_dim = descriptor->second_out_dim != 0 ? descriptor->second_out_dim : descriptor->out_dim;
        size_t third_output_bytes = 0;
        if (!termite_metal_size_mul3(descriptor->rows, third_out_dim, termite_metal_quant_matmul_dtype_size(descriptor->output_dtype), &third_output_bytes)) return failure_code;
        TERMITE_PLAN_ACCESS(descriptor->third_output_buffer, descriptor->third_output_offset, third_output_bytes, TERMITE_METAL_PLANNED_RANGE_WRITE);
    }
    if (descriptor->rhs_buffer != nil) {
        TERMITE_PLAN_ACCESS(descriptor->rhs_buffer, descriptor->rhs_offset, output_bytes, TERMITE_METAL_PLANNED_RANGE_READ);
    }
    if (descriptor->inv_scale_buffer != nil) {
        TERMITE_PLAN_ACCESS(descriptor->inv_scale_buffer, descriptor->inv_scale_offset, sizeof(float), TERMITE_METAL_PLANNED_RANGE_READ);
    }

#undef TERMITE_PLAN_ACCESS
    return termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, accesses, count, failure_code);
}

static int termite_metal_encode_quant_matmul_descriptor_on_encoder(
    termite_metal_decode_runtime *runtime,
    id<MTLComputeCommandEncoder> encoder,
    const termite_metal_quant_matmul_descriptor *descriptor
) {
    if (runtime == NULL || encoder == nil || descriptor == NULL) return -1;
    const int failure_code = descriptor->failure_code;
    if (!termite_metal_quant_matmul_descriptor_dtype_supported(runtime, descriptor)) return failure_code;
    if (descriptor->input_buffer == nil || descriptor->weight_buffer == nil || descriptor->output_buffer == nil) return failure_code;
    if (descriptor->rows == 0 || descriptor->in_dim == 0 || descriptor->out_dim == 0) return failure_code;
    if (descriptor->values_per_block == 0 || descriptor->in_dim % descriptor->values_per_block != 0) return failure_code;
    const uint8_t planned_dispatch = termite_metal_quant_matmul_descriptor_planned_dispatch(descriptor);
    if (descriptor->format == TERMITE_METAL_QUANT_FORMAT_Q8_0 &&
        descriptor->epilogue == TERMITE_METAL_QUANT_MATMUL_EPILOGUE_NONE)
    {
        return termite_metal_encode_quant_matmul_none_on_encoder(runtime, encoder, descriptor);
    }
    if (descriptor->format == TERMITE_METAL_QUANT_FORMAT_Q8_0 &&
        descriptor->epilogue == TERMITE_METAL_QUANT_MATMUL_EPILOGUE_PAIR_ACTIVATION_MUL)
    {
        if (descriptor->second_weight_buffer == nil) return failure_code;
        return termite_metal_encode_quant_matmul_pair_activation_mul_on_encoder(runtime, encoder, descriptor);
    }
    if (descriptor->format == TERMITE_METAL_QUANT_FORMAT_Q8_0 &&
        descriptor->epilogue == TERMITE_METAL_QUANT_MATMUL_EPILOGUE_PAIR_ACTIVATION_RMS_SCALE_1X)
    {
        if (descriptor->rows != 1 || descriptor->second_weight_buffer == nil || descriptor->inv_scale_buffer == nil) return failure_code;
        return termite_metal_encode_quant_matmul_pair_activation_rms_scale_1x_on_encoder(runtime, encoder, descriptor);
    }
    if (descriptor->format == TERMITE_METAL_QUANT_FORMAT_Q8_0 &&
        descriptor->epilogue == TERMITE_METAL_QUANT_MATMUL_EPILOGUE_ACTIVATION_RHS_MUL)
    {
        if (descriptor->rhs_buffer == nil) return failure_code;
        (void)planned_dispatch;
        return termite_metal_encode_quant_matmul_activation_rhs_mul_on_encoder(runtime, encoder, descriptor);
    }
    if (descriptor->format == TERMITE_METAL_QUANT_FORMAT_Q8_0 &&
        descriptor->epilogue == TERMITE_METAL_QUANT_MATMUL_EPILOGUE_PAIR)
    {
        if (descriptor->second_weight_buffer == nil || descriptor->second_output_buffer == nil) return failure_code;
        return termite_metal_encode_quant_matmul_pair_on_encoder(runtime, encoder, descriptor);
    }
    if (descriptor->epilogue == TERMITE_METAL_QUANT_MATMUL_EPILOGUE_PAIR) {
        if (descriptor->second_weight_buffer == nil || descriptor->second_output_buffer == nil) return failure_code;
        return termite_metal_encode_quant_matmul_generic_pair_on_encoder(runtime, encoder, descriptor);
    }
    if (descriptor->format == TERMITE_METAL_QUANT_FORMAT_Q8_0 &&
        descriptor->epilogue == TERMITE_METAL_QUANT_MATMUL_EPILOGUE_QKV)
    {
        if (descriptor->second_weight_buffer == nil ||
            descriptor->third_weight_buffer == nil ||
            descriptor->second_output_buffer == nil ||
            descriptor->third_output_buffer == nil ||
            descriptor->second_out_dim == 0) return failure_code;
        return termite_metal_encode_quant_matmul_qkv_on_encoder(runtime, encoder, descriptor);
    }
    if (descriptor->epilogue == TERMITE_METAL_QUANT_MATMUL_EPILOGUE_NONE) {
        return termite_metal_encode_quant_matmul_generic_none_on_encoder(runtime, encoder, descriptor);
    }
    return failure_code;
}

static int termite_metal_encode_quant_matmul_descriptor(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    const termite_metal_quant_matmul_descriptor *descriptor
) {
    if (runtime == NULL || command_buffer == nil || descriptor == NULL) return -1;
    const int failure_code = descriptor->failure_code;
    if (!termite_metal_quant_matmul_descriptor_dtype_supported(runtime, descriptor)) return failure_code;
    if (descriptor->input_buffer == nil || descriptor->weight_buffer == nil || descriptor->output_buffer == nil) return failure_code;
    if (descriptor->rows == 0 || descriptor->in_dim == 0 || descriptor->out_dim == 0) return failure_code;
    if (descriptor->values_per_block == 0 || descriptor->in_dim % descriptor->values_per_block != 0) return failure_code;
    if (descriptor->epilogue == TERMITE_METAL_QUANT_MATMUL_EPILOGUE_PAIR_ACTIVATION_MUL) {
        if (descriptor->format != TERMITE_METAL_QUANT_FORMAT_Q8_0 || descriptor->second_weight_buffer == nil) return failure_code;
        if (runtime->active_planned_compute_encoder != nil) {
            return termite_metal_encode_quant_matmul_descriptor_on_encoder(runtime, runtime->active_planned_compute_encoder, descriptor);
        }
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_QUANT_PAIR_ACT);
        if (encoder == nil) return failure_code;
        const int rc = termite_metal_encode_quant_matmul_descriptor_on_encoder(runtime, encoder, descriptor);
        [encoder endEncoding];
        return rc;
    }
    if (descriptor->epilogue == TERMITE_METAL_QUANT_MATMUL_EPILOGUE_ACTIVATION_RHS_MUL) {
        if (descriptor->format != TERMITE_METAL_QUANT_FORMAT_Q8_0 || descriptor->rhs_buffer == nil) return failure_code;
        if (runtime->active_planned_compute_encoder != nil) {
            return termite_metal_encode_quant_matmul_descriptor_on_encoder(runtime, runtime->active_planned_compute_encoder, descriptor);
        }
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_QUANT_PAIR_ACT);
        if (encoder == nil) return failure_code;
        const int rc = termite_metal_encode_quant_matmul_descriptor_on_encoder(runtime, encoder, descriptor);
        [encoder endEncoding];
        return rc;
    }
    if (descriptor->epilogue == TERMITE_METAL_QUANT_MATMUL_EPILOGUE_PAIR_ACTIVATION_RMS_SCALE_1X) {
        if (descriptor->format != TERMITE_METAL_QUANT_FORMAT_Q8_0 ||
            descriptor->rows != 1 ||
            descriptor->second_weight_buffer == nil ||
            descriptor->inv_scale_buffer == nil) return failure_code;
        if (runtime->active_planned_compute_encoder != nil) {
            return termite_metal_encode_quant_matmul_descriptor_on_encoder(runtime, runtime->active_planned_compute_encoder, descriptor);
        }
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_QUANT_PAIR_ACT);
        if (encoder == nil) return failure_code;
        const int rc = termite_metal_encode_quant_matmul_descriptor_on_encoder(runtime, encoder, descriptor);
        [encoder endEncoding];
        return rc;
    }
    if (descriptor->epilogue == TERMITE_METAL_QUANT_MATMUL_EPILOGUE_PAIR) {
        if (descriptor->second_weight_buffer == nil ||
            descriptor->second_output_buffer == nil) return failure_code;
        if (runtime->active_planned_compute_encoder != nil) {
            return termite_metal_encode_quant_matmul_descriptor_on_encoder(runtime, runtime->active_planned_compute_encoder, descriptor);
        }
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_QUANT_PAIR_ACT);
        if (encoder == nil) return failure_code;
        const int rc = termite_metal_encode_quant_matmul_descriptor_on_encoder(runtime, encoder, descriptor);
        [encoder endEncoding];
        return rc;
    }
    if (descriptor->epilogue == TERMITE_METAL_QUANT_MATMUL_EPILOGUE_QKV) {
        if (descriptor->format != TERMITE_METAL_QUANT_FORMAT_Q8_0 ||
            descriptor->second_weight_buffer == nil ||
            descriptor->third_weight_buffer == nil ||
            descriptor->second_output_buffer == nil ||
            descriptor->third_output_buffer == nil ||
            descriptor->second_out_dim == 0) return failure_code;
        if (runtime->active_planned_compute_encoder != nil) {
            return termite_metal_encode_quant_matmul_descriptor_on_encoder(runtime, runtime->active_planned_compute_encoder, descriptor);
        }
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_QUANT_QKV);
        if (encoder == nil) return failure_code;
        const int rc = termite_metal_encode_quant_matmul_descriptor_on_encoder(runtime, encoder, descriptor);
        [encoder endEncoding];
        return rc;
    }
    if (descriptor->epilogue != TERMITE_METAL_QUANT_MATMUL_EPILOGUE_NONE) return failure_code;
    id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
    const BOOL planned_encoder = (encoder != nil);
    if (!planned_encoder) {
        encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_QUANT_LINEAR);
        if (encoder == nil) return failure_code;
    }
    const int rc = termite_metal_encode_quant_matmul_descriptor_on_encoder(runtime, encoder, descriptor);
    if (!planned_encoder) [encoder endEncoding];
    return rc;
}

static int termite_metal_decode_runtime_apply_quantized_linear_slot_device_direct(
    termite_metal_decode_runtime *runtime,
    size_t slot,
    void *input_handle,
    size_t input_offset,
    size_t rows,
    size_t in_dim,
    size_t out_dim,
    void *output_handle,
    size_t output_offset,
    id<MTLComputePipelineState> pipeline,
    id<MTLBuffer> weight_buffer,
    uint8_t slot_prepared,
    size_t slot_in_dim,
    size_t slot_out_dim,
    uint32_t format,
    size_t values_per_block,
    size_t bytes_per_block
) {
    if (runtime == NULL || input_handle == NULL || output_handle == NULL) return -1;
    if (pipeline == nil || weight_buffer == nil) return -2;
    if (slot_prepared == 0) return -3;
    if (slot_in_dim != in_dim || slot_out_dim != out_dim) return -4;
    if (rows == 0 || in_dim == 0 || out_dim == 0) return -5;
    if (rows > UINT32_MAX || in_dim > UINT32_MAX || out_dim > UINT32_MAX) return -6;
    if (termite_metal_decode_runtime_check_quant_linear_descriptor(runtime, slot, format, values_per_block, bytes_per_block, in_dim) != 0) return -7;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t input_bytes = rows * in_dim * sizeof(float);
        const size_t output_bytes = rows * out_dim * sizeof(float);
        if (input_offset + input_bytes > input_buffer.length) return -8;
        if (output_offset + output_bytes > output_buffer.length) return -9;
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -10;
        termite_metal_quant_matmul_descriptor descriptor = {
            .slot = slot,
            .format = format,
            .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
            .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_NONE,
            .fallback_pipeline = pipeline,
            .input_buffer = input_buffer,
            .input_offset = input_offset,
            .weight_buffer = weight_buffer,
            .output_buffer = output_buffer,
            .output_offset = output_offset,
            .rows = rows,
            .in_dim = in_dim,
            .out_dim = out_dim,
            .values_per_block = values_per_block,
            .bytes_per_block = bytes_per_block,
            .failure_code = -11,
        };
        if (termite_metal_encode_quant_matmul_descriptor(runtime, command_buffer, &descriptor) != 0) return -11;
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -12);
    }
}

int termite_metal_decode_runtime_apply_quantized_linear_slot_device(
    termite_metal_decode_runtime *runtime,
    uint32_t format,
    size_t slot,
    void *input_handle,
    size_t input_offset,
    size_t rows,
    size_t in_dim,
    size_t out_dim,
    void *output_handle,
    size_t output_offset
);

int termite_metal_decode_runtime_apply_quantized_linear_slot_host(
    termite_metal_decode_runtime *runtime,
    uint32_t format,
    size_t slot,
    const float *input,
    size_t rows,
    size_t in_dim,
    size_t out_dim,
    float *output
) {
    if (runtime == NULL || input == NULL || output == NULL) return -1;
    if (slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || rows == 0 || in_dim == 0 || out_dim == 0) return -3;
    termite_metal_quant_linear_slot_view view;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, format, slot, in_dim, out_dim, &view) != 0) return -4;
    if (rows > UINT32_MAX || in_dim > UINT32_MAX || out_dim > UINT32_MAX) return -6;
    @autoreleasepool {
        const size_t input_bytes = rows * in_dim * sizeof(float);
        const size_t output_bytes = rows * out_dim * sizeof(float);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)input length:input_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:input length:input_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithBytesNoCopy:output length:output_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) output_buffer = [runtime->device newBufferWithBytes:output length:output_bytes options:MTLResourceStorageModeShared];
        if (input_buffer == nil || output_buffer == nil) return -7;
        if (format == TERMITE_METAL_QUANT_FORMAT_TL1 || format == TERMITE_METAL_QUANT_FORMAT_TL2) {
            return termite_metal_decode_runtime_apply_quantized_linear_slot_device(
                runtime,
                format,
                slot,
                (__bridge void *)input_buffer,
                0,
                rows,
                in_dim,
                out_dim,
                (__bridge void *)output_buffer,
                0
            );
        }
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -8;
        termite_metal_quant_matmul_descriptor descriptor = {
            .slot = slot,
            .format = format,
            .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
            .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_NONE,
            .fallback_pipeline = view.pipeline,
            .input_buffer = input_buffer,
            .input_offset = 0,
            .weight_buffer = view.weight_buffer,
            .output_buffer = output_buffer,
            .output_offset = 0,
            .rows = rows,
            .in_dim = in_dim,
            .out_dim = out_dim,
            .values_per_block = view.values_per_block,
            .bytes_per_block = view.bytes_per_block,
            .failure_code = -9,
        };
        if (termite_metal_encode_quant_matmul_descriptor(runtime, command_buffer, &descriptor) != 0) return -9;
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -10);
    }
}

static termite_metal_quant_matmul_dispatch_kind termite_metal_select_quant_matmul_dispatch(
    uint32_t format,
    size_t rows,
    size_t in_dim,
    size_t out_dim,
    size_t values_per_block
) {
    if (format == TERMITE_METAL_QUANT_FORMAT_UNSUPPORTED ||
        rows == 0 ||
        in_dim == 0 ||
        out_dim == 0 ||
        values_per_block == 0 ||
        in_dim % values_per_block != 0) {
        return TERMITE_METAL_QUANT_MATMUL_DISPATCH_SCALAR;
    }
    if (rows >= 2 && rows <= 8) return TERMITE_METAL_QUANT_MATMUL_DISPATCH_SMALL_BATCH;
    if (rows >= 9u) return TERMITE_METAL_QUANT_MATMUL_DISPATCH_MM;
    return TERMITE_METAL_QUANT_MATMUL_DISPATCH_MMV;
}

static bool termite_metal_quant_matmul_dispatch_valid_for_shape(
    termite_metal_quant_matmul_dispatch_kind dispatch_kind,
    uint32_t format,
    size_t rows,
    size_t in_dim,
    size_t out_dim,
    size_t values_per_block
) {
    if (format == TERMITE_METAL_QUANT_FORMAT_UNSUPPORTED ||
        rows == 0 ||
        in_dim == 0 ||
        out_dim == 0 ||
        values_per_block == 0 ||
        in_dim % values_per_block != 0) {
        return dispatch_kind == TERMITE_METAL_QUANT_MATMUL_DISPATCH_SCALAR;
    }
    switch (dispatch_kind) {
        case TERMITE_METAL_QUANT_MATMUL_DISPATCH_SCALAR:
            return true;
        case TERMITE_METAL_QUANT_MATMUL_DISPATCH_MMV:
            return rows == 1;
        case TERMITE_METAL_QUANT_MATMUL_DISPATCH_SMALL_BATCH:
            return rows >= 2 && rows <= 8;
        case TERMITE_METAL_QUANT_MATMUL_DISPATCH_MM:
            return rows >= 9;
    }
    return false;
}

static termite_metal_quant_matmul_dispatch_kind termite_metal_select_quant_matmul_dispatch_preferred(
    uint32_t format,
    size_t rows,
    size_t in_dim,
    size_t out_dim,
    size_t values_per_block,
    uint8_t preferred_dispatch
) {
    if (preferred_dispatch <= (uint8_t)TERMITE_METAL_QUANT_MATMUL_DISPATCH_MM) {
        const termite_metal_quant_matmul_dispatch_kind preferred = (termite_metal_quant_matmul_dispatch_kind)preferred_dispatch;
        if (termite_metal_quant_matmul_dispatch_valid_for_shape(preferred, format, rows, in_dim, out_dim, values_per_block)) {
            return preferred;
        }
    }
    return termite_metal_select_quant_matmul_dispatch(format, rows, in_dim, out_dim, values_per_block);
}

static termite_metal_quant_matmul_primitive_kind termite_metal_quant_matmul_primitive_for_dispatch(
    termite_metal_quant_matmul_dispatch_kind dispatch_kind
) {
    switch (dispatch_kind) {
        case TERMITE_METAL_QUANT_MATMUL_DISPATCH_MMV:
            return TERMITE_METAL_QUANT_MATMUL_PRIMITIVE_QGEMV;
        case TERMITE_METAL_QUANT_MATMUL_DISPATCH_SMALL_BATCH:
            return TERMITE_METAL_QUANT_MATMUL_PRIMITIVE_QGEMV_BATCH;
        case TERMITE_METAL_QUANT_MATMUL_DISPATCH_MM:
            return TERMITE_METAL_QUANT_MATMUL_PRIMITIVE_QMATMUL;
        case TERMITE_METAL_QUANT_MATMUL_DISPATCH_SCALAR:
            return TERMITE_METAL_QUANT_MATMUL_PRIMITIVE_SCALAR;
    }
    return TERMITE_METAL_QUANT_MATMUL_PRIMITIVE_SCALAR;
}

static termite_metal_q8_0_linear_dispatch_kind termite_metal_q8_0_dispatch_from_quant_dispatch(
    termite_metal_quant_matmul_dispatch_kind dispatch_kind
) {
    switch (dispatch_kind) {
        case TERMITE_METAL_QUANT_MATMUL_DISPATCH_MMV:
            return TERMITE_METAL_Q8_0_LINEAR_DISPATCH_MMV;
        case TERMITE_METAL_QUANT_MATMUL_DISPATCH_SMALL_BATCH:
            return TERMITE_METAL_Q8_0_LINEAR_DISPATCH_SMALL_BATCH;
        case TERMITE_METAL_QUANT_MATMUL_DISPATCH_MM:
            return TERMITE_METAL_Q8_0_LINEAR_DISPATCH_MM;
        case TERMITE_METAL_QUANT_MATMUL_DISPATCH_SCALAR:
            return TERMITE_METAL_Q8_0_LINEAR_DISPATCH_SCALAR;
    }
    return TERMITE_METAL_Q8_0_LINEAR_DISPATCH_SCALAR;
}

static void termite_metal_record_q8_0_linear_dispatch(
    termite_metal_decode_runtime *runtime,
    termite_metal_q8_0_linear_dispatch_kind dispatch_kind,
    size_t rows,
    termite_metal_q8_0_linear_family_kind family
) {
    if (runtime == NULL) return;
    switch (dispatch_kind) {
        case TERMITE_METAL_Q8_0_LINEAR_DISPATCH_SCALAR:
            runtime->q8_0_linear_dispatch_scalar += 1;
            break;
        case TERMITE_METAL_Q8_0_LINEAR_DISPATCH_MMV:
            runtime->q8_0_linear_dispatch_mmv += 1;
            break;
        case TERMITE_METAL_Q8_0_LINEAR_DISPATCH_SMALL_BATCH:
            runtime->q8_0_linear_dispatch_small_batch += 1;
            break;
        case TERMITE_METAL_Q8_0_LINEAR_DISPATCH_MM:
            runtime->q8_0_linear_dispatch_mm += 1;
            break;
    }
    if (rows == 1) {
        runtime->q8_0_linear_rows_1 += 1;
    } else if (rows <= 8) {
        runtime->q8_0_linear_rows_2_8 += 1;
    } else if (rows <= 64) {
        runtime->q8_0_linear_rows_9_64 += 1;
    } else {
        runtime->q8_0_linear_rows_65_plus += 1;
    }
    const size_t family_index = (size_t)family;
    const size_t dispatch_index = (size_t)dispatch_kind;
    if (family_index < TERMITE_METAL_Q8_0_LINEAR_FAMILY_COUNT &&
        dispatch_index < TERMITE_METAL_Q8_0_LINEAR_DISPATCH_COUNT) {
        runtime->q8_0_linear_family_dispatch_counts[family_index][dispatch_index] += 1;
    }
}

static int termite_metal_encode_quant_matmul_none_on_encoder_family(
    termite_metal_decode_runtime *runtime,
    id<MTLComputeCommandEncoder> encoder,
    const termite_metal_quant_matmul_descriptor *descriptor,
    termite_metal_q8_0_linear_family_kind family
) {
    if (runtime == NULL || encoder == nil || descriptor == NULL) return -1;
    const int failure_code = descriptor->failure_code;
    if (descriptor->activation_dtype != TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32 ||
        descriptor->epilogue != TERMITE_METAL_QUANT_MATMUL_EPILOGUE_NONE ||
        descriptor->input_buffer == nil ||
        descriptor->weight_buffer == nil ||
        descriptor->output_buffer == nil) return failure_code;
    if (descriptor->rows == 0 ||
        descriptor->in_dim == 0 ||
        descriptor->out_dim == 0 ||
        descriptor->values_per_block == 0 ||
        descriptor->in_dim % descriptor->values_per_block != 0) return failure_code;
    const BOOL q8_0_format =
        descriptor->format == TERMITE_METAL_QUANT_FORMAT_Q8_0 &&
        descriptor->values_per_block == 32u &&
        descriptor->bytes_per_block == 34u;
    if (!q8_0_format && descriptor->fallback_pipeline == nil) return failure_code;

    termite_metal_linear_params params = {
        .rows = (uint32_t)descriptor->rows,
        .in_dim = (uint32_t)descriptor->in_dim,
        .out_dim = (uint32_t)descriptor->out_dim,
        .row_blocks = (uint32_t)(descriptor->in_dim / descriptor->values_per_block),
    };
    const uint8_t planned_dispatch = termite_metal_quant_matmul_descriptor_planned_dispatch(descriptor);
    const termite_metal_quant_matmul_dispatch_kind quant_dispatch_kind = termite_metal_select_quant_matmul_dispatch_preferred(
        descriptor->format,
        descriptor->rows,
        descriptor->in_dim,
        descriptor->out_dim,
        descriptor->values_per_block,
        planned_dispatch);
    const termite_metal_q8_0_linear_dispatch_kind dispatch_kind =
        termite_metal_q8_0_dispatch_from_quant_dispatch(quant_dispatch_kind);
    (void)termite_metal_quant_matmul_primitive_for_dispatch(quant_dispatch_kind);
    const BOOL f32_activation_buffers =
        descriptor->input_dtype == TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32 &&
        descriptor->output_dtype == TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32;
    const BOOL use_mm_f16_input = (q8_0_format &&
        descriptor->input_dtype == TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F16 &&
        descriptor->output_dtype == TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32 &&
        runtime->q8_0_mm_f16_input_pipeline != nil &&
        dispatch_kind == TERMITE_METAL_Q8_0_LINEAR_DISPATCH_MM);
    const BOOL use_mmv_f16_input = (!use_mm_f16_input &&
        q8_0_format &&
        descriptor->input_dtype == TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F16 &&
        descriptor->output_dtype == TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32 &&
        runtime->q8_0_mmv_f16_input_pipeline != nil &&
        (dispatch_kind == TERMITE_METAL_Q8_0_LINEAR_DISPATCH_MMV ||
            dispatch_kind == TERMITE_METAL_Q8_0_LINEAR_DISPATCH_SCALAR));
    if (!f32_activation_buffers && !use_mm_f16_input && !use_mmv_f16_input) return failure_code;
    const BOOL use_mm_sg = (q8_0_format &&
        f32_activation_buffers &&
        descriptor->rows >= 65u &&
        runtime->q8_0_mm_sg_pipeline != nil &&
        dispatch_kind == TERMITE_METAL_Q8_0_LINEAR_DISPATCH_MM);
    const BOOL use_mm = (!use_mm_sg && !use_mm_f16_input && q8_0_format && f32_activation_buffers && runtime->q8_0_mm_pipeline != nil && dispatch_kind == TERMITE_METAL_Q8_0_LINEAR_DISPATCH_MM);
    id<MTLComputePipelineState> small_batch_pipeline = runtime->q8_0_small_batch_pipeline;
    size_t small_batch_rows_per_tg = 5u;
    if (descriptor->rows <= 2u && runtime->q8_0_small_batch_r2_pipeline != nil) {
        small_batch_pipeline = runtime->q8_0_small_batch_r2_pipeline;
        small_batch_rows_per_tg = 2u;
    } else if (descriptor->rows == 3u && runtime->q8_0_small_batch_r3_pipeline != nil) {
        small_batch_pipeline = runtime->q8_0_small_batch_r3_pipeline;
        small_batch_rows_per_tg = 3u;
    } else if (descriptor->rows == 4u && runtime->q8_0_small_batch_r4_pipeline != nil) {
        small_batch_pipeline = runtime->q8_0_small_batch_r4_pipeline;
        small_batch_rows_per_tg = 4u;
    }
    const BOOL use_small_batch = (q8_0_format &&
        f32_activation_buffers &&
        !use_mm_sg &&
        !use_mm &&
        !use_mm_f16_input &&
        small_batch_pipeline != nil &&
        descriptor->rows > 1 &&
        (dispatch_kind == TERMITE_METAL_Q8_0_LINEAR_DISPATCH_SMALL_BATCH ||
            dispatch_kind == TERMITE_METAL_Q8_0_LINEAR_DISPATCH_MM));
    const BOOL use_mmv = (!use_mm_sg &&
        !use_mm &&
        !use_mm_f16_input &&
        q8_0_format &&
        f32_activation_buffers &&
        !use_small_batch &&
        runtime->q8_0_mmv_pipeline != nil &&
        (dispatch_kind == TERMITE_METAL_Q8_0_LINEAR_DISPATCH_MMV ||
            dispatch_kind == TERMITE_METAL_Q8_0_LINEAR_DISPATCH_MM));
    const termite_metal_q8_0_linear_dispatch_kind actual_kind = (use_mm_sg || use_mm || use_mm_f16_input)
        ? TERMITE_METAL_Q8_0_LINEAR_DISPATCH_MM
        : (use_small_batch
            ? TERMITE_METAL_Q8_0_LINEAR_DISPATCH_SMALL_BATCH
            : ((use_mmv || use_mmv_f16_input)
                ? TERMITE_METAL_Q8_0_LINEAR_DISPATCH_MMV
                : TERMITE_METAL_Q8_0_LINEAR_DISPATCH_SCALAR));
    if (q8_0_format) {
        if (use_mm_f16_input) runtime->q8_0_linear_mm_f16_input += 1;
        if (use_mmv_f16_input) runtime->q8_0_linear_mmv_f16_input += 1;
        termite_metal_record_q8_0_linear_dispatch(runtime, actual_kind, descriptor->rows, family);
    }
    id<MTLComputePipelineState> scalar_pipeline = descriptor->fallback_pipeline != nil
        ? descriptor->fallback_pipeline
        : runtime->q8_0_pipeline;
    id<MTLComputePipelineState> pipeline = use_mm_sg
        ? runtime->q8_0_mm_sg_pipeline
        : (use_mm_f16_input
            ? runtime->q8_0_mm_f16_input_pipeline
        : (use_mm
            ? runtime->q8_0_mm_pipeline
        : (use_mmv_f16_input
            ? runtime->q8_0_mmv_f16_input_pipeline
        : (use_mmv
            ? runtime->q8_0_mmv_pipeline
            : (use_small_batch
                ? small_batch_pipeline
                : scalar_pipeline)))));
    if (pipeline == nil) return failure_code;
    [encoder setComputePipelineState:pipeline];
    [encoder setBuffer:descriptor->input_buffer offset:descriptor->input_offset atIndex:0];
    [encoder setBuffer:descriptor->weight_buffer offset:0 atIndex:1];
    [encoder setBuffer:descriptor->output_buffer offset:descriptor->output_offset atIndex:2];
    [encoder setBytes:&params length:sizeof(params) atIndex:3];
    if (use_mm_sg) {
        [encoder setThreadgroupMemoryLength:8192u atIndex:0];
        [encoder dispatchThreadgroups:MTLSizeMake((descriptor->out_dim + 63u) / 64u, (descriptor->rows + 31u) / 32u, 1) threadsPerThreadgroup:MTLSizeMake(128, 1, 1)];
    } else if (use_mm || use_mm_f16_input) {
        [encoder dispatchThreadgroups:MTLSizeMake((descriptor->out_dim + 31u) / 32u, (descriptor->rows + 7u) / 8u, 1) threadsPerThreadgroup:MTLSizeMake(128, 1, 1)];
    } else if (use_mmv || use_mmv_f16_input) {
        [encoder setThreadgroupMemoryLength:64u * sizeof(float) atIndex:0];
        [encoder dispatchThreadgroups:MTLSizeMake((descriptor->out_dim + 1u) / 2u, descriptor->rows, 1) threadsPerThreadgroup:MTLSizeMake(128, 1, 1)];
    } else if (use_small_batch) {
        [encoder dispatchThreadgroups:MTLSizeMake((descriptor->out_dim + 15u) / 16u, (descriptor->rows + small_batch_rows_per_tg - 1u) / small_batch_rows_per_tg, 1) threadsPerThreadgroup:MTLSizeMake(128, 1, 1)];
    } else {
        const size_t total = descriptor->rows * descriptor->out_dim;
        [encoder dispatchThreads:MTLSizeMake(total, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(pipeline, total), 1, 1)];
    }
    return 0;
}

static int termite_metal_encode_quant_matmul_none_on_encoder(
    termite_metal_decode_runtime *runtime,
    id<MTLComputeCommandEncoder> encoder,
    const termite_metal_quant_matmul_descriptor *descriptor
) {
    if (runtime == NULL || encoder == nil || descriptor == NULL) return -1;
    const int failure_code = descriptor->failure_code;
    if (termite_metal_prepare_quant_matmul_accesses(runtime, descriptor, failure_code) != 0) return failure_code;
    const termite_metal_q8_0_linear_family_kind family =
        descriptor->q8_family;
    return termite_metal_encode_quant_matmul_none_on_encoder_family(
        runtime,
        encoder,
        descriptor,
        family);
}

static int termite_metal_encode_quant_matmul_pair_activation_mul_on_encoder(
    termite_metal_decode_runtime *runtime,
    id<MTLComputeCommandEncoder> encoder,
    const termite_metal_quant_matmul_descriptor *descriptor
) {
    if (runtime == NULL || encoder == nil || descriptor == NULL) return -1;
    const int failure_code = descriptor->failure_code;
    if (descriptor->activation_dtype != TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32 ||
        descriptor->epilogue != TERMITE_METAL_QUANT_MATMUL_EPILOGUE_PAIR_ACTIVATION_MUL ||
        descriptor->input_buffer == nil ||
        descriptor->weight_buffer == nil ||
        descriptor->second_weight_buffer == nil ||
        descriptor->output_buffer == nil) return failure_code;
    if (descriptor->rows == 0 ||
        descriptor->in_dim == 0 ||
        descriptor->out_dim == 0 ||
        descriptor->values_per_block == 0 ||
        descriptor->in_dim % descriptor->values_per_block != 0) return failure_code;
    if (descriptor->format != TERMITE_METAL_QUANT_FORMAT_Q8_0 ||
        descriptor->values_per_block != 32u ||
        descriptor->bytes_per_block != 34u) return failure_code;
    if (termite_metal_prepare_quant_matmul_accesses(runtime, descriptor, failure_code) != 0) return failure_code;

    const uint8_t planned_dispatch = termite_metal_quant_matmul_descriptor_planned_dispatch(descriptor);
    const termite_metal_q8_0_linear_dispatch_kind dispatch_kind = termite_metal_q8_0_dispatch_from_quant_dispatch(
        termite_metal_select_quant_matmul_dispatch_preferred(
            descriptor->format,
            descriptor->rows,
            descriptor->in_dim,
            descriptor->out_dim,
            descriptor->values_per_block,
            planned_dispatch));
    const BOOL f32_activation_buffers =
        descriptor->input_dtype == TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32 &&
        descriptor->output_dtype == TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32;
    const BOOL use_mm_f16_output = (descriptor->input_dtype == TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32 &&
        descriptor->output_dtype == TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F16 &&
        runtime->q8_0_pair_activation_mm_f16_output_pipeline != nil &&
        dispatch_kind == TERMITE_METAL_Q8_0_LINEAR_DISPATCH_MM);
    if (!f32_activation_buffers && !use_mm_f16_output) return failure_code;
    const BOOL use_mm_sg = (descriptor->rows >= TERMITE_METAL_Q8_0_PAIR_ACTIVATION_SPLIT_MM_SG_MIN_ROWS &&
        f32_activation_buffers &&
        runtime->q8_0_pair_activation_mm_pipeline == nil &&
        runtime->q8_0_mm_sg_pipeline != nil &&
        runtime->activation_multiply_pipeline != nil &&
        dispatch_kind == TERMITE_METAL_Q8_0_LINEAR_DISPATCH_MM);
    if (use_mm_sg) {
        if (descriptor->rows > UINT32_MAX || descriptor->in_dim > UINT32_MAX || descriptor->out_dim > UINT32_MAX) return failure_code;
        if (descriptor->rows > SIZE_MAX / descriptor->out_dim) return failure_code;
        const size_t total = descriptor->rows * descriptor->out_dim;
        if (total > UINT32_MAX || total > SIZE_MAX / sizeof(float)) return failure_code;
        const size_t intermediate_bytes = total * sizeof(float);
        if (descriptor->output_offset > descriptor->output_buffer.length ||
            intermediate_bytes > descriptor->output_buffer.length - descriptor->output_offset) return failure_code;
        if (termite_metal_decode_runtime_ensure_hot_buffers(runtime, 0, intermediate_bytes) != 0) return failure_code;
        id<MTLBuffer> gate_output_buffer = runtime->hot_intermediate_buffers[0];
        id<MTLBuffer> up_output_buffer = runtime->hot_intermediate_buffers[1];
        if (gate_output_buffer == nil || up_output_buffer == nil) return failure_code;
        if (intermediate_bytes > gate_output_buffer.length || intermediate_bytes > up_output_buffer.length) return failure_code;

        termite_metal_linear_params params = {
            .rows = (uint32_t)descriptor->rows,
            .in_dim = (uint32_t)descriptor->in_dim,
            .out_dim = (uint32_t)descriptor->out_dim,
            .row_blocks = (uint32_t)(descriptor->in_dim / descriptor->values_per_block),
        };
        termite_metal_apply_activation_params activation_params = {
            .activation_kind = descriptor->activation_kind,
            .rows = (uint32_t)descriptor->rows,
            .dim = (uint32_t)descriptor->out_dim,
        };
        termite_metal_record_q8_0_linear_dispatch(
            runtime,
            TERMITE_METAL_Q8_0_LINEAR_DISPATCH_MM,
            descriptor->rows,
            TERMITE_METAL_Q8_0_LINEAR_FAMILY_PAIR_ACTIVATION);
        termite_metal_record_q8_0_linear_dispatch(
            runtime,
            TERMITE_METAL_Q8_0_LINEAR_DISPATCH_MM,
            descriptor->rows,
            TERMITE_METAL_Q8_0_LINEAR_FAMILY_PAIR_ACTIVATION);

        [encoder setComputePipelineState:runtime->q8_0_mm_sg_pipeline];
        [encoder setBuffer:descriptor->input_buffer offset:descriptor->input_offset atIndex:0];
        [encoder setBuffer:descriptor->weight_buffer offset:0 atIndex:1];
        [encoder setBuffer:gate_output_buffer offset:0 atIndex:2];
        [encoder setBytes:&params length:sizeof(params) atIndex:3];
        [encoder setThreadgroupMemoryLength:8192u atIndex:0];
        [encoder dispatchThreadgroups:MTLSizeMake((descriptor->out_dim + 63u) / 64u, (descriptor->rows + 31u) / 32u, 1) threadsPerThreadgroup:MTLSizeMake(128, 1, 1)];

        [encoder setComputePipelineState:runtime->q8_0_mm_sg_pipeline];
        [encoder setBuffer:descriptor->input_buffer offset:descriptor->input_offset atIndex:0];
        [encoder setBuffer:descriptor->second_weight_buffer offset:0 atIndex:1];
        [encoder setBuffer:up_output_buffer offset:0 atIndex:2];
        [encoder setBytes:&params length:sizeof(params) atIndex:3];
        [encoder setThreadgroupMemoryLength:8192u atIndex:0];
        [encoder dispatchThreadgroups:MTLSizeMake((descriptor->out_dim + 63u) / 64u, (descriptor->rows + 31u) / 32u, 1) threadsPerThreadgroup:MTLSizeMake(128, 1, 1)];

        [encoder memoryBarrierWithScope:MTLBarrierScopeBuffers];
        [encoder setComputePipelineState:runtime->activation_multiply_pipeline];
        [encoder setBuffer:gate_output_buffer offset:0 atIndex:0];
        [encoder setBuffer:up_output_buffer offset:0 atIndex:1];
        [encoder setBuffer:descriptor->output_buffer offset:descriptor->output_offset atIndex:2];
        [encoder setBytes:&activation_params length:sizeof(activation_params) atIndex:3];
        [encoder dispatchThreads:MTLSizeMake(total, 1, 1)
           threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->activation_multiply_pipeline, total), 1, 1)];
        return 0;
    }

    const BOOL use_mm = (!use_mm_f16_output && f32_activation_buffers && runtime->q8_0_pair_activation_mm_pipeline != nil && dispatch_kind == TERMITE_METAL_Q8_0_LINEAR_DISPATCH_MM);
    const BOOL use_mmv = (!use_mm &&
        !use_mm_f16_output &&
        f32_activation_buffers &&
        runtime->q8_0_pair_activation_mmv_pipeline != nil &&
        dispatch_kind == TERMITE_METAL_Q8_0_LINEAR_DISPATCH_MMV);
    id<MTLComputePipelineState> small_batch_pipeline = runtime->q8_0_pair_activation_small_batch_pipeline;
    size_t small_batch_rows_per_tg = 5u;
    if (descriptor->rows <= 2u && runtime->q8_0_pair_activation_small_batch_r2_pipeline != nil) {
        small_batch_pipeline = runtime->q8_0_pair_activation_small_batch_r2_pipeline;
        small_batch_rows_per_tg = 2u;
    } else if (descriptor->rows == 3u && runtime->q8_0_pair_activation_small_batch_r3_pipeline != nil) {
        small_batch_pipeline = runtime->q8_0_pair_activation_small_batch_r3_pipeline;
        small_batch_rows_per_tg = 3u;
    } else if (descriptor->rows == 4u && runtime->q8_0_pair_activation_small_batch_r4_pipeline != nil) {
        small_batch_pipeline = runtime->q8_0_pair_activation_small_batch_r4_pipeline;
        small_batch_rows_per_tg = 4u;
    }
    const BOOL use_small_batch = (!use_mm &&
        !use_mm_f16_output &&
        f32_activation_buffers &&
        !use_mmv &&
        small_batch_pipeline != nil &&
        descriptor->rows > 1 &&
        (dispatch_kind == TERMITE_METAL_Q8_0_LINEAR_DISPATCH_SMALL_BATCH ||
            dispatch_kind == TERMITE_METAL_Q8_0_LINEAR_DISPATCH_MM));
    id<MTLComputePipelineState> scalar_pipeline = descriptor->fallback_pipeline != nil
        ? descriptor->fallback_pipeline
        : runtime->q8_0_pair_activation_reduce_pipeline;
    id<MTLComputePipelineState> pipeline = use_mm
        ? runtime->q8_0_pair_activation_mm_pipeline
        : (use_mm_f16_output
            ? runtime->q8_0_pair_activation_mm_f16_output_pipeline
        : (use_mmv
            ? runtime->q8_0_pair_activation_mmv_pipeline
            : (use_small_batch
                ? small_batch_pipeline
                : scalar_pipeline)));
    if (pipeline == nil) return failure_code;
    const termite_metal_q8_0_linear_dispatch_kind actual_kind = (use_mm || use_mm_f16_output)
        ? TERMITE_METAL_Q8_0_LINEAR_DISPATCH_MM
        : (use_small_batch
            ? TERMITE_METAL_Q8_0_LINEAR_DISPATCH_SMALL_BATCH
            : (use_mmv
                ? TERMITE_METAL_Q8_0_LINEAR_DISPATCH_MMV
                : TERMITE_METAL_Q8_0_LINEAR_DISPATCH_SCALAR));
    termite_metal_record_q8_0_linear_dispatch(
        runtime,
        actual_kind,
        descriptor->rows,
        TERMITE_METAL_Q8_0_LINEAR_FAMILY_PAIR_ACTIVATION);
    if (use_mm_f16_output) runtime->q8_0_pair_activation_mm_f16_output += 1;
    termite_metal_linear_params params = {
        .rows = (uint32_t)descriptor->rows,
        .in_dim = (uint32_t)descriptor->in_dim,
        .out_dim = (uint32_t)descriptor->out_dim,
        .row_blocks = (uint32_t)(descriptor->in_dim / descriptor->values_per_block),
    };
    termite_metal_apply_activation_params activation_params = {
        .activation_kind = descriptor->activation_kind,
        .rows = (uint32_t)descriptor->rows,
        .dim = (uint32_t)descriptor->out_dim,
    };
    [encoder setComputePipelineState:pipeline];
    [encoder setBuffer:descriptor->input_buffer offset:descriptor->input_offset atIndex:0];
    [encoder setBuffer:descriptor->weight_buffer offset:0 atIndex:1];
    [encoder setBuffer:descriptor->second_weight_buffer offset:0 atIndex:2];
    [encoder setBuffer:descriptor->output_buffer offset:descriptor->output_offset atIndex:3];
    [encoder setBytes:&params length:sizeof(params) atIndex:4];
    [encoder setBytes:&activation_params length:sizeof(activation_params) atIndex:5];
    if (use_mm || use_mm_f16_output) {
        [encoder dispatchThreadgroups:MTLSizeMake((descriptor->out_dim + 31u) / 32u, (descriptor->rows + 7u) / 8u, 1) threadsPerThreadgroup:MTLSizeMake(128, 1, 1)];
    } else if (use_mmv) {
        [encoder dispatchThreadgroups:MTLSizeMake((descriptor->out_dim + 15u) / 16u, descriptor->rows, 1) threadsPerThreadgroup:MTLSizeMake(128, 1, 1)];
    } else if (use_small_batch) {
        [encoder dispatchThreadgroups:MTLSizeMake((descriptor->out_dim + 15u) / 16u, (descriptor->rows + small_batch_rows_per_tg - 1u) / small_batch_rows_per_tg, 1) threadsPerThreadgroup:MTLSizeMake(128, 1, 1)];
    } else {
        [encoder setThreadgroupMemoryLength:256u * sizeof(float) atIndex:0];
        [encoder dispatchThreadgroups:MTLSizeMake((descriptor->out_dim + 3u) / 4u, descriptor->rows, 1) threadsPerThreadgroup:MTLSizeMake(128, 1, 1)];
    }
    return 0;
}

static int termite_metal_encode_quant_matmul_pair_activation_rms_scale_1x_on_encoder(
    termite_metal_decode_runtime *runtime,
    id<MTLComputeCommandEncoder> encoder,
    const termite_metal_quant_matmul_descriptor *descriptor
) {
    if (runtime == NULL || encoder == nil || descriptor == NULL) return -1;
    const int failure_code = descriptor->failure_code;
    if (descriptor->activation_dtype != TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32 ||
        descriptor->epilogue != TERMITE_METAL_QUANT_MATMUL_EPILOGUE_PAIR_ACTIVATION_RMS_SCALE_1X ||
        descriptor->input_buffer == nil ||
        descriptor->weight_buffer == nil ||
        descriptor->second_weight_buffer == nil ||
        descriptor->inv_scale_buffer == nil ||
        descriptor->output_buffer == nil) return failure_code;
    if (descriptor->rows != 1 ||
        descriptor->in_dim == 0 ||
        descriptor->out_dim == 0 ||
        descriptor->values_per_block == 0 ||
        descriptor->in_dim % descriptor->values_per_block != 0) return failure_code;
    if (descriptor->format != TERMITE_METAL_QUANT_FORMAT_Q8_0 ||
        descriptor->values_per_block != 32u ||
        descriptor->bytes_per_block != 34u) return failure_code;
    const BOOL f32_activation_buffers =
        descriptor->input_dtype == TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32 &&
        descriptor->output_dtype == TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32;
    const BOOL use_f16_output =
        descriptor->input_dtype == TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32 &&
        descriptor->output_dtype == TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F16 &&
        runtime->q8_0_pair_activation_rms_scale_mmv_f16_output_pipeline != nil;
    if (!f32_activation_buffers && !use_f16_output) return failure_code;
    if (descriptor->norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return failure_code;
    if (runtime->rms_norm_slot_prepared[descriptor->norm_slot] == 0 ||
        runtime->rms_norm_hidden_sizes[descriptor->norm_slot] != descriptor->in_dim) return failure_code;
    if (descriptor->in_dim > UINT32_MAX || descriptor->out_dim > UINT32_MAX) return failure_code;
    if (termite_metal_prepare_quant_matmul_accesses(runtime, descriptor, failure_code) != 0) return failure_code;
    const uint8_t planned_dispatch = termite_metal_quant_matmul_descriptor_planned_dispatch(descriptor);
    const termite_metal_q8_0_linear_dispatch_kind dispatch_kind = termite_metal_q8_0_dispatch_from_quant_dispatch(
        termite_metal_select_quant_matmul_dispatch_preferred(
            descriptor->format,
            1,
            descriptor->in_dim,
            descriptor->out_dim,
            descriptor->values_per_block,
            planned_dispatch));
    if (dispatch_kind != TERMITE_METAL_Q8_0_LINEAR_DISPATCH_MMV &&
        dispatch_kind != TERMITE_METAL_Q8_0_LINEAR_DISPATCH_SCALAR) return failure_code;
    id<MTLComputePipelineState> pipeline = descriptor->fallback_pipeline != nil
        ? descriptor->fallback_pipeline
        : runtime->q8_0_pair_activation_rms_scale_mmv_pipeline;
    if (use_f16_output) pipeline = runtime->q8_0_pair_activation_rms_scale_mmv_f16_output_pipeline;
    if (pipeline == nil) return failure_code;
    const termite_metal_q8_0_linear_dispatch_kind actual_kind =
        (descriptor->fallback_pipeline != nil && dispatch_kind == TERMITE_METAL_Q8_0_LINEAR_DISPATCH_SCALAR)
            ? TERMITE_METAL_Q8_0_LINEAR_DISPATCH_SCALAR
            : TERMITE_METAL_Q8_0_LINEAR_DISPATCH_MMV;
    termite_metal_record_q8_0_linear_dispatch(
        runtime,
        actual_kind,
        1,
        TERMITE_METAL_Q8_0_LINEAR_FAMILY_PAIR_ACTIVATION_RMS);
    if (use_f16_output) runtime->q8_0_pair_activation_rms_scale_mmv_f16_output += 1;
    termite_metal_linear_params params = {
        .rows = 1,
        .in_dim = (uint32_t)descriptor->in_dim,
        .out_dim = (uint32_t)descriptor->out_dim,
        .row_blocks = (uint32_t)(descriptor->in_dim / descriptor->values_per_block),
    };
    termite_metal_apply_activation_params activation_params = {
        .activation_kind = descriptor->activation_kind,
        .rows = 1,
        .dim = (uint32_t)descriptor->out_dim,
    };
    [encoder setComputePipelineState:pipeline];
    [encoder setBuffer:descriptor->input_buffer offset:descriptor->input_offset atIndex:0];
    [encoder setBuffer:descriptor->weight_buffer offset:0 atIndex:1];
    [encoder setBuffer:descriptor->second_weight_buffer offset:0 atIndex:2];
    [encoder setBuffer:descriptor->output_buffer offset:descriptor->output_offset atIndex:3];
    [encoder setBytes:&params length:sizeof(params) atIndex:4];
    [encoder setBytes:&activation_params length:sizeof(activation_params) atIndex:5];
    [encoder setBuffer:runtime->rms_norm_weight_buffers[descriptor->norm_slot] offset:0 atIndex:6];
    [encoder setBuffer:descriptor->inv_scale_buffer offset:descriptor->inv_scale_offset atIndex:7];
    [encoder dispatchThreadgroups:MTLSizeMake((descriptor->out_dim + 15u) / 16u, 1, 1) threadsPerThreadgroup:MTLSizeMake(128, 1, 1)];
    return 0;
}

static int termite_metal_encode_quant_matmul_activation_rhs_mul_on_encoder(
    termite_metal_decode_runtime *runtime,
    id<MTLComputeCommandEncoder> encoder,
    const termite_metal_quant_matmul_descriptor *descriptor
) {
    if (runtime == NULL || encoder == nil || descriptor == NULL) return -1;
    const int failure_code = descriptor->failure_code;
    if (descriptor->activation_dtype != TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32 ||
        descriptor->epilogue != TERMITE_METAL_QUANT_MATMUL_EPILOGUE_ACTIVATION_RHS_MUL ||
        descriptor->input_buffer == nil ||
        descriptor->weight_buffer == nil ||
        descriptor->rhs_buffer == nil ||
        descriptor->output_buffer == nil) return failure_code;
    if (descriptor->rows == 0 ||
        descriptor->in_dim == 0 ||
        descriptor->out_dim == 0 ||
        descriptor->values_per_block == 0 ||
        descriptor->in_dim % descriptor->values_per_block != 0) return failure_code;
    if (descriptor->format != TERMITE_METAL_QUANT_FORMAT_Q8_0 ||
        descriptor->values_per_block != 32u ||
        descriptor->bytes_per_block != 34u) return failure_code;
    if (termite_metal_prepare_quant_matmul_accesses(runtime, descriptor, failure_code) != 0) return failure_code;
    const uint8_t planned_dispatch = termite_metal_quant_matmul_descriptor_planned_dispatch(descriptor);
    (void)termite_metal_select_quant_matmul_dispatch_preferred(
        descriptor->format,
        descriptor->rows,
        descriptor->in_dim,
        descriptor->out_dim,
        descriptor->values_per_block,
        planned_dispatch);
    const bool use_mmv = descriptor->rows == 1u && runtime->q8_0_activation_multiply_mmv_pipeline != nil;
    id<MTLComputePipelineState> pipeline = use_mmv
        ? runtime->q8_0_activation_multiply_mmv_pipeline
        : (descriptor->fallback_pipeline != nil
            ? descriptor->fallback_pipeline
            : runtime->q8_0_activation_multiply_reduce_pipeline);
    if (pipeline == nil) return failure_code;
    termite_metal_record_q8_0_linear_dispatch(
        runtime,
        use_mmv ? TERMITE_METAL_Q8_0_LINEAR_DISPATCH_MMV : TERMITE_METAL_Q8_0_LINEAR_DISPATCH_SCALAR,
        descriptor->rows,
        TERMITE_METAL_Q8_0_LINEAR_FAMILY_ACTIVATION_RHS);
    termite_metal_linear_params params = {
        .rows = (uint32_t)descriptor->rows,
        .in_dim = (uint32_t)descriptor->in_dim,
        .out_dim = (uint32_t)descriptor->out_dim,
        .row_blocks = (uint32_t)(descriptor->in_dim / descriptor->values_per_block),
    };
    termite_metal_apply_activation_params activation_params = {
        .activation_kind = descriptor->activation_kind,
        .rows = (uint32_t)descriptor->rows,
        .dim = (uint32_t)descriptor->out_dim,
    };
    [encoder setComputePipelineState:pipeline];
    [encoder setBuffer:descriptor->input_buffer offset:descriptor->input_offset atIndex:0];
    [encoder setBuffer:descriptor->weight_buffer offset:0 atIndex:1];
    [encoder setBuffer:descriptor->rhs_buffer offset:descriptor->rhs_offset atIndex:2];
    [encoder setBuffer:descriptor->output_buffer offset:descriptor->output_offset atIndex:3];
    [encoder setBytes:&params length:sizeof(params) atIndex:4];
    [encoder setBytes:&activation_params length:sizeof(activation_params) atIndex:5];
    if (use_mmv) {
        [encoder dispatchThreadgroups:MTLSizeMake((descriptor->out_dim + 15u) / 16u, descriptor->rows, 1) threadsPerThreadgroup:MTLSizeMake(128, 1, 1)];
        return 0;
    }
    [encoder setThreadgroupMemoryLength:128u * sizeof(float) atIndex:0];
    [encoder dispatchThreadgroups:MTLSizeMake((descriptor->out_dim + 3u) / 4u, descriptor->rows, 1) threadsPerThreadgroup:MTLSizeMake(128, 1, 1)];
    return 0;
}

static int termite_metal_encode_quant_matmul_pair_on_encoder(
    termite_metal_decode_runtime *runtime,
    id<MTLComputeCommandEncoder> encoder,
    const termite_metal_quant_matmul_descriptor *descriptor
) {
    if (runtime == NULL || encoder == nil || descriptor == NULL) return -1;
    const int failure_code = descriptor->failure_code;
    if (descriptor->activation_dtype != TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32 ||
        descriptor->epilogue != TERMITE_METAL_QUANT_MATMUL_EPILOGUE_PAIR ||
        descriptor->input_buffer == nil ||
        descriptor->weight_buffer == nil ||
        descriptor->second_weight_buffer == nil ||
        descriptor->output_buffer == nil ||
        descriptor->second_output_buffer == nil) return failure_code;
    if (descriptor->rows == 0 ||
        descriptor->in_dim == 0 ||
        descriptor->out_dim == 0 ||
        descriptor->values_per_block == 0 ||
        descriptor->in_dim % descriptor->values_per_block != 0) return failure_code;
    if (descriptor->format != TERMITE_METAL_QUANT_FORMAT_Q8_0 ||
        descriptor->values_per_block != 32u ||
        descriptor->bytes_per_block != 34u) return failure_code;
    if (termite_metal_prepare_quant_matmul_accesses(runtime, descriptor, failure_code) != 0) return failure_code;
    if (descriptor->rows >= 9u && runtime->q8_0_mm_pipeline != nil) {
        termite_metal_quant_matmul_descriptor first = *descriptor;
        first.epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_NONE;
        first.second_weight_buffer = nil;
        first.third_weight_buffer = nil;
        first.second_output_buffer = nil;
        first.third_output_buffer = nil;
        first.second_out_dim = 0;
        int rc = termite_metal_encode_quant_matmul_none_on_encoder_family(
            runtime,
            encoder,
            &first,
            TERMITE_METAL_Q8_0_LINEAR_FAMILY_PAIR);
        if (rc != 0) return rc;
        termite_metal_quant_matmul_descriptor second = first;
        second.weight_buffer = descriptor->second_weight_buffer;
        second.output_buffer = descriptor->second_output_buffer;
        second.output_offset = descriptor->second_output_offset;
        return termite_metal_encode_quant_matmul_none_on_encoder_family(
            runtime,
            encoder,
            &second,
            TERMITE_METAL_Q8_0_LINEAR_FAMILY_PAIR);
    }
    termite_metal_linear_params params = {
        .rows = (uint32_t)descriptor->rows,
        .in_dim = (uint32_t)descriptor->in_dim,
        .out_dim = (uint32_t)descriptor->out_dim,
        .row_blocks = (uint32_t)(descriptor->in_dim / descriptor->values_per_block),
    };
    const uint8_t planned_dispatch = termite_metal_quant_matmul_descriptor_planned_dispatch(descriptor);
    const termite_metal_q8_0_linear_dispatch_kind dispatch_kind = termite_metal_q8_0_dispatch_from_quant_dispatch(
        termite_metal_select_quant_matmul_dispatch_preferred(
            descriptor->format,
            descriptor->rows,
            descriptor->in_dim,
            descriptor->out_dim,
            descriptor->values_per_block,
            planned_dispatch));
    id<MTLComputePipelineState> small_batch_pipeline = runtime->q8_0_pair_small_batch_pipeline;
    size_t small_batch_rows_per_tg = 5u;
    if (descriptor->rows <= 2u && runtime->q8_0_pair_small_batch_r2_pipeline != nil) {
        small_batch_pipeline = runtime->q8_0_pair_small_batch_r2_pipeline;
        small_batch_rows_per_tg = 2u;
    } else if (descriptor->rows == 3u && runtime->q8_0_pair_small_batch_r3_pipeline != nil) {
        small_batch_pipeline = runtime->q8_0_pair_small_batch_r3_pipeline;
        small_batch_rows_per_tg = 3u;
    } else if (descriptor->rows == 4u && runtime->q8_0_pair_small_batch_r4_pipeline != nil) {
        small_batch_pipeline = runtime->q8_0_pair_small_batch_r4_pipeline;
        small_batch_rows_per_tg = 4u;
    }
    const BOOL use_small_batch = (small_batch_pipeline != nil &&
        descriptor->rows > 1 &&
        dispatch_kind != TERMITE_METAL_Q8_0_LINEAR_DISPATCH_SCALAR);
    const BOOL use_mmv = (!use_small_batch && runtime->q8_0_pair_mmv_pipeline != nil);
    id<MTLComputePipelineState> scalar_pipeline = descriptor->fallback_pipeline != nil
        ? descriptor->fallback_pipeline
        : runtime->q8_0_pair_pipeline;
    id<MTLComputePipelineState> pipeline = use_small_batch
        ? small_batch_pipeline
        : (use_mmv
            ? runtime->q8_0_pair_mmv_pipeline
            : scalar_pipeline);
    if (pipeline == nil) return failure_code;
    const termite_metal_q8_0_linear_dispatch_kind actual_kind = use_small_batch
        ? TERMITE_METAL_Q8_0_LINEAR_DISPATCH_SMALL_BATCH
        : (use_mmv
            ? TERMITE_METAL_Q8_0_LINEAR_DISPATCH_MMV
            : TERMITE_METAL_Q8_0_LINEAR_DISPATCH_SCALAR);
    termite_metal_record_q8_0_linear_dispatch(
        runtime,
        actual_kind,
        descriptor->rows,
        TERMITE_METAL_Q8_0_LINEAR_FAMILY_PAIR);
    [encoder setComputePipelineState:pipeline];
    [encoder setBuffer:descriptor->input_buffer offset:descriptor->input_offset atIndex:0];
    [encoder setBuffer:descriptor->weight_buffer offset:0 atIndex:1];
    [encoder setBuffer:descriptor->second_weight_buffer offset:0 atIndex:2];
    [encoder setBuffer:descriptor->output_buffer offset:descriptor->output_offset atIndex:3];
    [encoder setBuffer:descriptor->second_output_buffer offset:descriptor->second_output_offset atIndex:4];
    [encoder setBytes:&params length:sizeof(params) atIndex:5];
    if (use_small_batch) {
        [encoder dispatchThreadgroups:MTLSizeMake((descriptor->out_dim + 15u) / 16u, (descriptor->rows + small_batch_rows_per_tg - 1u) / small_batch_rows_per_tg, 1) threadsPerThreadgroup:MTLSizeMake(128, 1, 1)];
    } else if (use_mmv) {
        [encoder setThreadgroupMemoryLength:128u * sizeof(float) atIndex:0];
        [encoder dispatchThreadgroups:MTLSizeMake((descriptor->out_dim + 1u) / 2u, descriptor->rows, 1) threadsPerThreadgroup:MTLSizeMake(128, 1, 1)];
    } else {
        const size_t total = descriptor->rows * descriptor->out_dim;
        [encoder dispatchThreads:MTLSizeMake(total, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(pipeline, total), 1, 1)];
    }
    return 0;
}

static int termite_metal_encode_quant_matmul_qkv_on_encoder(
    termite_metal_decode_runtime *runtime,
    id<MTLComputeCommandEncoder> encoder,
    const termite_metal_quant_matmul_descriptor *descriptor
) {
    if (runtime == NULL || encoder == nil || descriptor == NULL) return -1;
    const int failure_code = descriptor->failure_code;
    if (descriptor->activation_dtype != TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32 ||
        descriptor->epilogue != TERMITE_METAL_QUANT_MATMUL_EPILOGUE_QKV ||
        descriptor->input_buffer == nil ||
        descriptor->weight_buffer == nil ||
        descriptor->second_weight_buffer == nil ||
        descriptor->third_weight_buffer == nil ||
        descriptor->output_buffer == nil ||
        descriptor->second_output_buffer == nil ||
        descriptor->third_output_buffer == nil) return failure_code;
    if (descriptor->rows == 0 ||
        descriptor->in_dim == 0 ||
        descriptor->out_dim == 0 ||
        descriptor->second_out_dim == 0 ||
        descriptor->values_per_block == 0 ||
        descriptor->in_dim % descriptor->values_per_block != 0) return failure_code;
    if (descriptor->format != TERMITE_METAL_QUANT_FORMAT_Q8_0 ||
        descriptor->values_per_block != 32u ||
        descriptor->bytes_per_block != 34u) return failure_code;
    if (termite_metal_prepare_quant_matmul_accesses(runtime, descriptor, failure_code) != 0) return failure_code;
    if (descriptor->rows != 1u) {
        termite_metal_quant_matmul_descriptor q = *descriptor;
        q.epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_NONE;
        q.second_weight_buffer = nil;
        q.third_weight_buffer = nil;
        q.second_output_buffer = nil;
        q.third_output_buffer = nil;
        q.second_out_dim = 0;
        int rc = termite_metal_encode_quant_matmul_none_on_encoder_family(
            runtime,
            encoder,
            &q,
            TERMITE_METAL_Q8_0_LINEAR_FAMILY_QKV);
        if (rc != 0) return rc;
        termite_metal_quant_matmul_descriptor k = q;
        k.weight_buffer = descriptor->second_weight_buffer;
        k.output_buffer = descriptor->second_output_buffer;
        k.output_offset = descriptor->second_output_offset;
        k.out_dim = descriptor->second_out_dim;
        rc = termite_metal_encode_quant_matmul_none_on_encoder_family(
            runtime,
            encoder,
            &k,
            TERMITE_METAL_Q8_0_LINEAR_FAMILY_QKV);
        if (rc != 0) return rc;
        termite_metal_quant_matmul_descriptor v = k;
        v.weight_buffer = descriptor->third_weight_buffer;
        v.output_buffer = descriptor->third_output_buffer;
        v.output_offset = descriptor->third_output_offset;
        return termite_metal_encode_quant_matmul_none_on_encoder_family(
            runtime,
            encoder,
            &v,
            TERMITE_METAL_Q8_0_LINEAR_FAMILY_QKV);
    }
    id<MTLComputePipelineState> pipeline = descriptor->fallback_pipeline != nil
        ? descriptor->fallback_pipeline
        : runtime->q8_0_qkv_mmv_pipeline;
    if (pipeline == nil) return failure_code;
    termite_metal_record_q8_0_linear_dispatch(
        runtime,
        TERMITE_METAL_Q8_0_LINEAR_DISPATCH_MMV,
        descriptor->rows,
        TERMITE_METAL_Q8_0_LINEAR_FAMILY_QKV);
    termite_metal_qkv_linear_params params = {
        .rows = (uint32_t)descriptor->rows,
        .in_dim = (uint32_t)descriptor->in_dim,
        .q_out_dim = (uint32_t)descriptor->out_dim,
        .kv_out_dim = (uint32_t)descriptor->second_out_dim,
        .row_blocks = (uint32_t)(descriptor->in_dim / descriptor->values_per_block),
    };
    [encoder setComputePipelineState:pipeline];
    [encoder setBuffer:descriptor->input_buffer offset:descriptor->input_offset atIndex:0];
    [encoder setBuffer:descriptor->weight_buffer offset:0 atIndex:1];
    [encoder setBuffer:descriptor->second_weight_buffer offset:0 atIndex:2];
    [encoder setBuffer:descriptor->third_weight_buffer offset:0 atIndex:3];
    [encoder setBuffer:descriptor->output_buffer offset:descriptor->output_offset atIndex:4];
    [encoder setBuffer:descriptor->second_output_buffer offset:descriptor->second_output_offset atIndex:5];
    [encoder setBuffer:descriptor->third_output_buffer offset:descriptor->third_output_offset atIndex:6];
    [encoder setBytes:&params length:sizeof(params) atIndex:7];
    [encoder setThreadgroupMemoryLength:128u * sizeof(float) atIndex:0];
    const size_t q_groups = (descriptor->out_dim + 1u) / 2u;
    const size_t kv_groups = (descriptor->second_out_dim + 1u) / 2u;
    [encoder dispatchThreadgroups:MTLSizeMake(q_groups + kv_groups, descriptor->rows, 1) threadsPerThreadgroup:MTLSizeMake(128, 1, 1)];
    return 0;
}

static int termite_metal_encode_quant_matmul_generic_none_on_encoder(
    termite_metal_decode_runtime *runtime,
    id<MTLComputeCommandEncoder> encoder,
    const termite_metal_quant_matmul_descriptor *descriptor
) {
    if (runtime == NULL || encoder == nil || descriptor == NULL) return -1;
    const int failure_code = descriptor->failure_code;
    if (descriptor->activation_dtype != TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32 ||
        descriptor->epilogue != TERMITE_METAL_QUANT_MATMUL_EPILOGUE_NONE ||
        descriptor->input_buffer == nil ||
        descriptor->weight_buffer == nil ||
        descriptor->output_buffer == nil) return failure_code;
    if (descriptor->rows == 0 ||
        descriptor->in_dim == 0 ||
        descriptor->out_dim == 0 ||
        descriptor->values_per_block == 0 ||
        descriptor->in_dim % descriptor->values_per_block != 0) return failure_code;
    size_t expected_values_per_block = 0;
    size_t expected_bytes_per_block = 0;
    if (!termite_metal_quant_format_block_layout(descriptor->format, &expected_values_per_block, &expected_bytes_per_block) ||
        descriptor->values_per_block != expected_values_per_block ||
        descriptor->bytes_per_block != expected_bytes_per_block) return failure_code;
    if (descriptor->format == TERMITE_METAL_QUANT_FORMAT_Q8_0) {
        return termite_metal_encode_quant_matmul_none_on_encoder(runtime, encoder, descriptor);
    }
    if (termite_metal_prepare_quant_matmul_accesses(runtime, descriptor, failure_code) != 0) return failure_code;

    id<MTLComputePipelineState> pipeline = descriptor->fallback_pipeline;
    BOOL use_reduce = NO;
    size_t reduce_shmem_floats = 0;
    MTLSize reduce_threadgroups = MTLSizeMake(0, 0, 0);
    MTLSize reduce_threads = MTLSizeMake(64, 1, 1);
    switch (descriptor->format) {
        case TERMITE_METAL_QUANT_FORMAT_Q1_0:
            pipeline = pipeline != nil ? pipeline : runtime->q1_0_pipeline;
            break;
        case TERMITE_METAL_QUANT_FORMAT_I2_S:
            pipeline = pipeline != nil ? pipeline : runtime->i2_s_pipeline;
            break;
        case TERMITE_METAL_QUANT_FORMAT_I8_S:
            pipeline = pipeline != nil ? pipeline : runtime->i8_s_pipeline;
            break;
        case TERMITE_METAL_QUANT_FORMAT_Q2_K:
            pipeline = pipeline != nil ? pipeline : runtime->q2_k_pipeline;
            break;
        case TERMITE_METAL_QUANT_FORMAT_Q3_K:
            pipeline = pipeline != nil ? pipeline : runtime->q3_k_pipeline;
            break;
        case TERMITE_METAL_QUANT_FORMAT_Q4_0:
            if (descriptor->rows == 1 && runtime->q4_0_reduce_pipeline != nil) {
                pipeline = runtime->q4_0_reduce_pipeline;
                use_reduce = YES;
                reduce_shmem_floats = 128u;
                reduce_threadgroups = MTLSizeMake((descriptor->out_dim + 3u) / 4u, descriptor->rows, 1);
            } else {
                pipeline = pipeline != nil ? pipeline : runtime->q4_0_pipeline;
            }
            break;
        case TERMITE_METAL_QUANT_FORMAT_Q4_1:
            if (descriptor->rows == 1 && runtime->q4_1_reduce_pipeline != nil) {
                pipeline = runtime->q4_1_reduce_pipeline;
                use_reduce = YES;
                reduce_shmem_floats = 128u;
                reduce_threadgroups = MTLSizeMake((descriptor->out_dim + 3u) / 4u, descriptor->rows, 1);
            } else {
                pipeline = pipeline != nil ? pipeline : runtime->q4_1_pipeline;
            }
            break;
        case TERMITE_METAL_QUANT_FORMAT_Q4_K:
            if (descriptor->rows == 1 && runtime->q4_k_reduce_pipeline != nil) {
                pipeline = runtime->q4_k_reduce_pipeline;
                use_reduce = YES;
                reduce_shmem_floats = 64u;
                reduce_threadgroups = MTLSizeMake((descriptor->out_dim + 1u) / 2u, descriptor->rows, 1);
            } else {
                pipeline = pipeline != nil ? pipeline : runtime->q4_k_pipeline;
            }
            break;
        case TERMITE_METAL_QUANT_FORMAT_Q5_0:
            if (descriptor->rows == 1 && runtime->q5_0_reduce_pipeline != nil) {
                pipeline = runtime->q5_0_reduce_pipeline;
                use_reduce = YES;
                reduce_shmem_floats = 64u;
                reduce_threadgroups = MTLSizeMake((descriptor->out_dim + 1u) / 2u, descriptor->rows, 1);
            } else {
                pipeline = pipeline != nil ? pipeline : runtime->q5_0_pipeline;
            }
            break;
        case TERMITE_METAL_QUANT_FORMAT_Q5_1:
            if (descriptor->rows == 1 && runtime->q5_1_reduce_pipeline != nil) {
                pipeline = runtime->q5_1_reduce_pipeline;
                use_reduce = YES;
                reduce_shmem_floats = 64u;
                reduce_threadgroups = MTLSizeMake((descriptor->out_dim + 1u) / 2u, descriptor->rows, 1);
            } else {
                pipeline = pipeline != nil ? pipeline : runtime->q5_1_pipeline;
            }
            break;
        case TERMITE_METAL_QUANT_FORMAT_Q5_K:
            if (descriptor->rows == 1 && runtime->q5_k_reduce_pipeline != nil) {
                pipeline = runtime->q5_k_reduce_pipeline;
                use_reduce = YES;
                reduce_shmem_floats = 32u;
                reduce_threadgroups = MTLSizeMake(descriptor->out_dim, descriptor->rows, 1);
            } else {
                pipeline = pipeline != nil ? pipeline : runtime->q5_k_pipeline;
            }
            break;
        case TERMITE_METAL_QUANT_FORMAT_Q6_K:
            if (descriptor->rows == 1 && runtime->q6_k_reduce_pipeline != nil) {
                pipeline = runtime->q6_k_reduce_pipeline;
                use_reduce = YES;
                reduce_shmem_floats = 64u;
                reduce_threadgroups = MTLSizeMake((descriptor->out_dim + 1u) / 2u, descriptor->rows, 1);
            } else {
                pipeline = pipeline != nil ? pipeline : runtime->q6_k_pipeline;
            }
            break;
        case TERMITE_METAL_QUANT_FORMAT_Q8_1:
            if (descriptor->rows == 1 && runtime->q8_1_reduce_pipeline != nil) {
                pipeline = runtime->q8_1_reduce_pipeline;
                use_reduce = YES;
                reduce_shmem_floats = 64u;
                reduce_threadgroups = MTLSizeMake((descriptor->out_dim + 1u) / 2u, descriptor->rows, 1);
            } else {
                pipeline = pipeline != nil ? pipeline : runtime->q8_1_pipeline;
            }
            break;
        case TERMITE_METAL_QUANT_FORMAT_Q8_K:
            if (descriptor->rows == 1 && runtime->q8_k_reduce_pipeline != nil) {
                pipeline = runtime->q8_k_reduce_pipeline;
                use_reduce = YES;
                reduce_shmem_floats = 64u;
                reduce_threadgroups = MTLSizeMake((descriptor->out_dim + 1u) / 2u, descriptor->rows, 1);
            } else {
                pipeline = pipeline != nil ? pipeline : runtime->q8_k_pipeline;
            }
            break;
        case TERMITE_METAL_QUANT_FORMAT_IQ4_NL:
            if (descriptor->rows == 1 && runtime->iq4_nl_reduce_pipeline != nil) {
                pipeline = runtime->iq4_nl_reduce_pipeline;
                use_reduce = YES;
                reduce_shmem_floats = 64u;
                reduce_threadgroups = MTLSizeMake((descriptor->out_dim + 1u) / 2u, descriptor->rows, 1);
            } else {
                pipeline = pipeline != nil ? pipeline : runtime->iq4_nl_pipeline;
            }
            break;
        case TERMITE_METAL_QUANT_FORMAT_IQ4_XS:
            if (descriptor->rows == 1 && runtime->iq4_xs_reduce_pipeline != nil) {
                pipeline = runtime->iq4_xs_reduce_pipeline;
                use_reduce = YES;
                reduce_shmem_floats = 64u;
                reduce_threadgroups = MTLSizeMake((descriptor->out_dim + 1u) / 2u, descriptor->rows, 1);
            } else {
                pipeline = pipeline != nil ? pipeline : runtime->iq4_xs_pipeline;
            }
            break;
        case TERMITE_METAL_QUANT_FORMAT_MXFP4:
            if (descriptor->rows == 1 && runtime->mxfp4_reduce_pipeline != nil) {
                pipeline = runtime->mxfp4_reduce_pipeline;
                use_reduce = YES;
                reduce_shmem_floats = 64u;
                reduce_threadgroups = MTLSizeMake((descriptor->out_dim + 1u) / 2u, descriptor->rows, 1);
            } else {
                pipeline = pipeline != nil ? pipeline : runtime->mxfp4_pipeline;
            }
            break;
        case TERMITE_METAL_QUANT_FORMAT_NVFP4:
            pipeline = pipeline != nil ? pipeline : runtime->nvfp4_pipeline;
            break;
        case TERMITE_METAL_QUANT_FORMAT_IQ2_XS:
            pipeline = pipeline != nil ? pipeline : runtime->iq2_xs_pipeline;
            break;
        default:
            break;
    }
    if (pipeline == nil) return failure_code;
    termite_metal_linear_params params = {
        .rows = (uint32_t)descriptor->rows,
        .in_dim = (uint32_t)descriptor->in_dim,
        .out_dim = (uint32_t)descriptor->out_dim,
        .row_blocks = (uint32_t)(descriptor->in_dim / descriptor->values_per_block),
    };
    [encoder setComputePipelineState:pipeline];
    [encoder setBuffer:descriptor->input_buffer offset:descriptor->input_offset atIndex:0];
    [encoder setBuffer:descriptor->weight_buffer offset:0 atIndex:1];
    [encoder setBuffer:descriptor->output_buffer offset:descriptor->output_offset atIndex:2];
    [encoder setBytes:&params length:sizeof(params) atIndex:3];
    if (use_reduce) {
        [encoder setThreadgroupMemoryLength:reduce_shmem_floats * sizeof(float) atIndex:0];
        [encoder dispatchThreadgroups:reduce_threadgroups threadsPerThreadgroup:reduce_threads];
    } else {
        const size_t total = descriptor->rows * descriptor->out_dim;
        [encoder dispatchThreads:MTLSizeMake(total, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(pipeline, total), 1, 1)];
    }
    return 0;
}

static int termite_metal_encode_q8_0_linear(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    id<MTLBuffer> input_buffer,
    size_t input_offset,
    id<MTLBuffer> weight_buffer,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    size_t rows,
    size_t in_dim,
    size_t out_dim,
    int failure_code
) {
    return termite_metal_encode_q8_0_linear_family(
        runtime,
        command_buffer,
        input_buffer,
        input_offset,
        weight_buffer,
        output_buffer,
        output_offset,
        rows,
        in_dim,
        out_dim,
        TERMITE_METAL_Q8_0_LINEAR_FAMILY_NONE,
        failure_code
    );
}

static int termite_metal_encode_q8_0_linear_family(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    id<MTLBuffer> input_buffer,
    size_t input_offset,
    id<MTLBuffer> weight_buffer,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    size_t rows,
    size_t in_dim,
    size_t out_dim,
    termite_metal_q8_0_linear_family_kind q8_family,
    int failure_code
) {
    termite_metal_quant_matmul_descriptor descriptor = {
        .slot = SIZE_MAX,
        .format = TERMITE_METAL_QUANT_FORMAT_Q8_0,
        .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
        .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_NONE,
        .fallback_pipeline = runtime != NULL ? runtime->q8_0_pipeline : nil,
        .input_buffer = input_buffer,
        .input_offset = input_offset,
        .weight_buffer = weight_buffer,
        .output_buffer = output_buffer,
        .output_offset = output_offset,
        .rows = rows,
        .in_dim = in_dim,
        .out_dim = out_dim,
        .values_per_block = 32u,
        .bytes_per_block = 34u,
        .q8_family = q8_family,
        .failure_code = failure_code,
    };
    return termite_metal_encode_quant_matmul_descriptor(runtime, command_buffer, &descriptor);
}

static int termite_metal_encode_q8_0_linear_raw_on_encoder_planned(
    termite_metal_decode_runtime *runtime,
    id<MTLComputeCommandEncoder> encoder,
    id<MTLBuffer> input_buffer,
    size_t input_offset,
    id<MTLBuffer> weight_buffer,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    size_t rows,
    size_t in_dim,
    size_t out_dim,
    uint8_t planned_dispatch,
    int failure_code
) {
    return termite_metal_encode_q8_0_linear_raw_on_encoder_planned_family(
        runtime,
        encoder,
        input_buffer,
        input_offset,
        weight_buffer,
        output_buffer,
        output_offset,
        rows,
        in_dim,
        out_dim,
        planned_dispatch,
        TERMITE_METAL_Q8_0_LINEAR_FAMILY_NONE,
        failure_code
    );
}

static int termite_metal_encode_q8_0_linear_raw_on_encoder_planned_family(
    termite_metal_decode_runtime *runtime,
    id<MTLComputeCommandEncoder> encoder,
    id<MTLBuffer> input_buffer,
    size_t input_offset,
    id<MTLBuffer> weight_buffer,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    size_t rows,
    size_t in_dim,
    size_t out_dim,
    uint8_t planned_dispatch,
    termite_metal_q8_0_linear_family_kind q8_family,
    int failure_code
) {
    termite_metal_quant_matmul_descriptor descriptor = {
        .slot = SIZE_MAX,
        .format = TERMITE_METAL_QUANT_FORMAT_Q8_0,
        .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
        .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_NONE,
        .fallback_pipeline = runtime != NULL ? runtime->q8_0_pipeline : nil,
        .input_buffer = input_buffer,
        .input_offset = input_offset,
        .weight_buffer = weight_buffer,
        .output_buffer = output_buffer,
        .output_offset = output_offset,
        .rows = rows,
        .in_dim = in_dim,
        .out_dim = out_dim,
        .values_per_block = 32u,
        .bytes_per_block = 34u,
        .q8_family = q8_family,
        .use_planned_dispatch = true,
        .planned_dispatch = planned_dispatch,
        .failure_code = failure_code,
    };
    return termite_metal_encode_quant_matmul_descriptor_on_encoder(runtime, encoder, &descriptor);
}

static int termite_metal_encode_q8_0_linear_raw_on_encoder(
    termite_metal_decode_runtime *runtime,
    id<MTLComputeCommandEncoder> encoder,
    id<MTLBuffer> input_buffer,
    size_t input_offset,
    id<MTLBuffer> weight_buffer,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    size_t rows,
    size_t in_dim,
    size_t out_dim,
    int failure_code
) {
    return termite_metal_encode_q8_0_linear_raw_on_encoder_planned(
        runtime,
        encoder,
        input_buffer,
        input_offset,
        weight_buffer,
        output_buffer,
        output_offset,
        rows,
        in_dim,
        out_dim,
        TERMITE_METAL_QUANT_MATMUL_DISPATCH_UNKNOWN,
        failure_code
    );
}

static int termite_metal_encode_q8_0_linear_raw(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    id<MTLBuffer> input_buffer,
    size_t input_offset,
    id<MTLBuffer> weight_buffer,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    size_t rows,
    size_t in_dim,
    size_t out_dim,
    int failure_code
) {
    id<MTLComputeCommandEncoder> encoder = runtime != NULL ? runtime->active_planned_compute_encoder : nil;
    const BOOL planned_encoder = (encoder != nil);
    if (!planned_encoder) {
        encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_QUANT_LINEAR);
        if (encoder == nil) return failure_code;
    }
    const int rc = termite_metal_encode_q8_0_linear_raw_on_encoder(runtime, encoder, input_buffer, input_offset, weight_buffer, output_buffer, output_offset, rows, in_dim, out_dim, failure_code);
    if (!planned_encoder) [encoder endEncoding];
    return rc;
}

static int termite_metal_encode_q8_0_linear_rms_scale_1x(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    id<MTLBuffer> input_buffer,
    size_t input_offset,
    size_t norm_slot,
    id<MTLBuffer> inv_scale_buffer,
    size_t inv_scale_offset,
    id<MTLBuffer> weight_buffer,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    size_t in_dim,
    size_t out_dim,
    int failure_code
) {
    if (runtime == NULL || command_buffer == nil || input_buffer == nil || inv_scale_buffer == nil || weight_buffer == nil || output_buffer == nil) return failure_code;
    if (runtime->q8_0_rms_scale_mmv_pipeline == nil) return failure_code;
    if (norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return failure_code;
    if (runtime->rms_norm_slot_prepared[norm_slot] == 0 || runtime->rms_norm_hidden_sizes[norm_slot] != in_dim) return failure_code;
    if (in_dim == 0 || out_dim == 0 || in_dim % 32u != 0 || in_dim > UINT32_MAX || out_dim > UINT32_MAX) return failure_code;
    termite_metal_linear_params params = {
        .rows = 1,
        .in_dim = (uint32_t)in_dim,
        .out_dim = (uint32_t)out_dim,
        .row_blocks = (uint32_t)(in_dim / 32u),
    };
    termite_metal_record_q8_0_linear_dispatch(
        runtime,
        TERMITE_METAL_Q8_0_LINEAR_DISPATCH_MMV,
        1,
        TERMITE_METAL_Q8_0_LINEAR_FAMILY_RMS_SCALE);
    id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_QUANT_LINEAR);
    if (encoder == nil) return failure_code;
    [encoder setComputePipelineState:runtime->q8_0_rms_scale_mmv_pipeline];
    [encoder setBuffer:input_buffer offset:input_offset atIndex:0];
    [encoder setBuffer:weight_buffer offset:0 atIndex:1];
    [encoder setBuffer:output_buffer offset:output_offset atIndex:2];
    [encoder setBytes:&params length:sizeof(params) atIndex:3];
    [encoder setBuffer:runtime->rms_norm_weight_buffers[norm_slot] offset:0 atIndex:4];
    [encoder setBuffer:inv_scale_buffer offset:inv_scale_offset atIndex:5];
    [encoder setThreadgroupMemoryLength:128u * sizeof(float) atIndex:0];
    [encoder dispatchThreadgroups:MTLSizeMake((out_dim + 3u) / 4u, 1, 1) threadsPerThreadgroup:MTLSizeMake(128, 1, 1)];
    [encoder endEncoding];
    return 0;
}

static int termite_metal_encode_q8_0_pair_linear(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    id<MTLBuffer> input_buffer,
    size_t input_offset,
    id<MTLBuffer> first_weight_buffer,
    id<MTLBuffer> second_weight_buffer,
    id<MTLBuffer> first_output_buffer,
    size_t first_output_offset,
    id<MTLBuffer> second_output_buffer,
    size_t second_output_offset,
    size_t rows,
    size_t in_dim,
    size_t out_dim,
    int failure_code
) {
    termite_metal_quant_matmul_descriptor descriptor = {
        .slot = SIZE_MAX,
        .format = TERMITE_METAL_QUANT_FORMAT_Q8_0,
        .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
        .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_PAIR,
        .fallback_pipeline = runtime != NULL ? runtime->q8_0_pair_pipeline : nil,
        .input_buffer = input_buffer,
        .input_offset = input_offset,
        .weight_buffer = first_weight_buffer,
        .second_weight_buffer = second_weight_buffer,
        .output_buffer = first_output_buffer,
        .output_offset = first_output_offset,
        .second_output_buffer = second_output_buffer,
        .second_output_offset = second_output_offset,
        .rows = rows,
        .in_dim = in_dim,
        .out_dim = out_dim,
        .values_per_block = 32u,
        .bytes_per_block = 34u,
        .failure_code = failure_code,
    };
    return termite_metal_encode_quant_matmul_descriptor(runtime, command_buffer, &descriptor);
}

static int termite_metal_encode_q8_0_qkv_linear(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    id<MTLBuffer> input_buffer,
    size_t input_offset,
    id<MTLBuffer> q_weight_buffer,
    id<MTLBuffer> k_weight_buffer,
    id<MTLBuffer> v_weight_buffer,
    id<MTLBuffer> q_output_buffer,
    size_t q_output_offset,
    id<MTLBuffer> k_output_buffer,
    size_t k_output_offset,
    id<MTLBuffer> v_output_buffer,
    size_t v_output_offset,
    size_t rows,
    size_t in_dim,
    size_t q_out_dim,
    size_t kv_out_dim,
    int failure_code
) {
    termite_metal_quant_matmul_descriptor descriptor = {
        .slot = SIZE_MAX,
        .format = TERMITE_METAL_QUANT_FORMAT_Q8_0,
        .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
        .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_QKV,
        .fallback_pipeline = runtime != NULL ? runtime->q8_0_qkv_mmv_pipeline : nil,
        .input_buffer = input_buffer,
        .input_offset = input_offset,
        .weight_buffer = q_weight_buffer,
        .second_weight_buffer = k_weight_buffer,
        .third_weight_buffer = v_weight_buffer,
        .output_buffer = q_output_buffer,
        .output_offset = q_output_offset,
        .second_output_buffer = k_output_buffer,
        .second_output_offset = k_output_offset,
        .third_output_buffer = v_output_buffer,
        .third_output_offset = v_output_offset,
        .rows = rows,
        .in_dim = in_dim,
        .out_dim = q_out_dim,
        .second_out_dim = kv_out_dim,
        .values_per_block = 32u,
        .bytes_per_block = 34u,
        .failure_code = failure_code,
    };
    return termite_metal_encode_quant_matmul_descriptor(runtime, command_buffer, &descriptor);
}

static int termite_metal_encode_q8_0_pair_activation_multiply(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    id<MTLBuffer> input_buffer,
    size_t input_offset,
    id<MTLBuffer> gate_weight_buffer,
    id<MTLBuffer> up_weight_buffer,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    size_t rows,
    size_t in_dim,
    size_t out_dim,
    uint32_t activation_kind,
    int failure_code
) {
    termite_metal_quant_matmul_descriptor descriptor = {
        .slot = SIZE_MAX,
        .format = TERMITE_METAL_QUANT_FORMAT_Q8_0,
        .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
        .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_PAIR_ACTIVATION_MUL,
        .fallback_pipeline = runtime != NULL ? runtime->q8_0_pair_activation_reduce_pipeline : nil,
        .input_buffer = input_buffer,
        .input_offset = input_offset,
        .weight_buffer = gate_weight_buffer,
        .second_weight_buffer = up_weight_buffer,
        .output_buffer = output_buffer,
        .output_offset = output_offset,
        .rows = rows,
        .in_dim = in_dim,
        .out_dim = out_dim,
        .values_per_block = 32u,
        .bytes_per_block = 34u,
        .activation_kind = activation_kind,
        .failure_code = failure_code,
    };
    return termite_metal_encode_quant_matmul_descriptor(runtime, command_buffer, &descriptor);
}

static int termite_metal_encode_q8_0_pair_activation_multiply_on_encoder(
    termite_metal_decode_runtime *runtime,
    id<MTLComputeCommandEncoder> encoder,
    id<MTLBuffer> input_buffer,
    size_t input_offset,
    id<MTLBuffer> gate_weight_buffer,
    id<MTLBuffer> up_weight_buffer,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    size_t rows,
    size_t in_dim,
    size_t out_dim,
    uint32_t activation_kind,
    int failure_code
) {
    return termite_metal_encode_q8_0_pair_activation_multiply_on_encoder_planned(
        runtime,
        encoder,
        input_buffer,
        input_offset,
        gate_weight_buffer,
        up_weight_buffer,
        output_buffer,
        output_offset,
        rows,
        in_dim,
        out_dim,
        activation_kind,
        TERMITE_METAL_QUANT_MATMUL_DISPATCH_UNKNOWN,
        failure_code
    );
}

static int termite_metal_encode_q8_0_pair_activation_multiply_on_encoder_planned(
    termite_metal_decode_runtime *runtime,
    id<MTLComputeCommandEncoder> encoder,
    id<MTLBuffer> input_buffer,
    size_t input_offset,
    id<MTLBuffer> gate_weight_buffer,
    id<MTLBuffer> up_weight_buffer,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    size_t rows,
    size_t in_dim,
    size_t out_dim,
    uint32_t activation_kind,
    uint8_t planned_dispatch,
    int failure_code
) {
    termite_metal_quant_matmul_descriptor descriptor = {
        .slot = SIZE_MAX,
        .format = TERMITE_METAL_QUANT_FORMAT_Q8_0,
        .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
        .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_PAIR_ACTIVATION_MUL,
        .fallback_pipeline = runtime != NULL ? runtime->q8_0_pair_activation_reduce_pipeline : nil,
        .input_buffer = input_buffer,
        .input_offset = input_offset,
        .weight_buffer = gate_weight_buffer,
        .second_weight_buffer = up_weight_buffer,
        .output_buffer = output_buffer,
        .output_offset = output_offset,
        .rows = rows,
        .in_dim = in_dim,
        .out_dim = out_dim,
        .values_per_block = 32u,
        .bytes_per_block = 34u,
        .activation_kind = activation_kind,
        .use_planned_dispatch = true,
        .planned_dispatch = planned_dispatch,
        .failure_code = failure_code,
    };
    return termite_metal_encode_quant_matmul_descriptor_on_encoder(runtime, encoder, &descriptor);
}

static int termite_metal_encode_q8_0_pair_activation_multiply_rms_scale_1x(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    id<MTLBuffer> input_buffer,
    size_t input_offset,
    size_t norm_slot,
    id<MTLBuffer> inv_scale_buffer,
    size_t inv_scale_offset,
    id<MTLBuffer> gate_weight_buffer,
    id<MTLBuffer> up_weight_buffer,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    size_t in_dim,
    size_t out_dim,
    uint32_t activation_kind,
    int failure_code
) {
    if (runtime == NULL || command_buffer == nil || input_buffer == nil || inv_scale_buffer == nil || gate_weight_buffer == nil || up_weight_buffer == nil || output_buffer == nil) return failure_code;
    id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_QUANT_PAIR_ACT);
    if (encoder == nil) return failure_code;
    const int rc = termite_metal_encode_q8_0_pair_activation_multiply_rms_scale_1x_on_encoder(runtime, encoder, input_buffer, input_offset, norm_slot, inv_scale_buffer, inv_scale_offset, gate_weight_buffer, up_weight_buffer, output_buffer, output_offset, in_dim, out_dim, activation_kind, failure_code);
    [encoder endEncoding];
    return rc;
}

static int termite_metal_encode_q8_0_pair_activation_multiply_rms_scale_1x_on_encoder(
    termite_metal_decode_runtime *runtime,
    id<MTLComputeCommandEncoder> encoder,
    id<MTLBuffer> input_buffer,
    size_t input_offset,
    size_t norm_slot,
    id<MTLBuffer> inv_scale_buffer,
    size_t inv_scale_offset,
    id<MTLBuffer> gate_weight_buffer,
    id<MTLBuffer> up_weight_buffer,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    size_t in_dim,
    size_t out_dim,
    uint32_t activation_kind,
    int failure_code
) {
    return termite_metal_encode_q8_0_pair_activation_multiply_rms_scale_1x_on_encoder_planned(
        runtime,
        encoder,
        input_buffer,
        input_offset,
        norm_slot,
        inv_scale_buffer,
        inv_scale_offset,
        gate_weight_buffer,
        up_weight_buffer,
        output_buffer,
        output_offset,
        in_dim,
        out_dim,
        activation_kind,
        TERMITE_METAL_QUANT_MATMUL_DISPATCH_UNKNOWN,
        failure_code
    );
}

static int termite_metal_encode_q8_0_pair_activation_multiply_rms_scale_1x_on_encoder_planned(
    termite_metal_decode_runtime *runtime,
    id<MTLComputeCommandEncoder> encoder,
    id<MTLBuffer> input_buffer,
    size_t input_offset,
    size_t norm_slot,
    id<MTLBuffer> inv_scale_buffer,
    size_t inv_scale_offset,
    id<MTLBuffer> gate_weight_buffer,
    id<MTLBuffer> up_weight_buffer,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    size_t in_dim,
    size_t out_dim,
    uint32_t activation_kind,
    uint8_t planned_dispatch,
    int failure_code
) {
    termite_metal_quant_matmul_descriptor descriptor = {
        .slot = SIZE_MAX,
        .format = TERMITE_METAL_QUANT_FORMAT_Q8_0,
        .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
        .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_PAIR_ACTIVATION_RMS_SCALE_1X,
        .fallback_pipeline = runtime != NULL ? runtime->q8_0_pair_activation_rms_scale_mmv_pipeline : nil,
        .input_buffer = input_buffer,
        .input_offset = input_offset,
        .weight_buffer = gate_weight_buffer,
        .second_weight_buffer = up_weight_buffer,
        .output_buffer = output_buffer,
        .output_offset = output_offset,
        .inv_scale_buffer = inv_scale_buffer,
        .inv_scale_offset = inv_scale_offset,
        .norm_slot = norm_slot,
        .rows = 1,
        .in_dim = in_dim,
        .out_dim = out_dim,
        .values_per_block = 32u,
        .bytes_per_block = 34u,
        .activation_kind = activation_kind,
        .use_planned_dispatch = true,
        .planned_dispatch = planned_dispatch,
        .failure_code = failure_code,
    };
    return termite_metal_encode_quant_matmul_descriptor_on_encoder(runtime, encoder, &descriptor);
}

static int termite_metal_encode_q8_0_activation_multiply(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    id<MTLBuffer> input_buffer,
    size_t input_offset,
    id<MTLBuffer> weight_buffer,
    id<MTLBuffer> rhs_buffer,
    size_t rhs_offset,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    size_t rows,
    size_t in_dim,
    size_t out_dim,
    uint32_t activation_kind,
    int failure_code
) {
    if (runtime == NULL || command_buffer == nil || input_buffer == nil || weight_buffer == nil || rhs_buffer == nil || output_buffer == nil) return failure_code;
    BOOL encoder_owned = YES;
    id<MTLComputeCommandEncoder> encoder = termite_metal_scoped_compute_encoder_for(runtime, command_buffer, TERMITE_METAL_COMPUTE_SOURCE_QUANT_PAIR_ACT, &encoder_owned);
    if (encoder == nil) return failure_code;
    const int rc = termite_metal_encode_q8_0_activation_multiply_on_encoder(runtime, encoder, input_buffer, input_offset, weight_buffer, rhs_buffer, rhs_offset, output_buffer, output_offset, rows, in_dim, out_dim, activation_kind, failure_code);
    termite_metal_end_scoped_compute_encoder(encoder, encoder_owned);
    return rc;
}

static int termite_metal_encode_q8_0_activation_multiply_on_encoder(
    termite_metal_decode_runtime *runtime,
    id<MTLComputeCommandEncoder> encoder,
    id<MTLBuffer> input_buffer,
    size_t input_offset,
    id<MTLBuffer> weight_buffer,
    id<MTLBuffer> rhs_buffer,
    size_t rhs_offset,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    size_t rows,
    size_t in_dim,
    size_t out_dim,
    uint32_t activation_kind,
    int failure_code
) {
    return termite_metal_encode_q8_0_activation_multiply_on_encoder_planned(
        runtime,
        encoder,
        input_buffer,
        input_offset,
        weight_buffer,
        rhs_buffer,
        rhs_offset,
        output_buffer,
        output_offset,
        rows,
        in_dim,
        out_dim,
        activation_kind,
        TERMITE_METAL_QUANT_MATMUL_DISPATCH_UNKNOWN,
        failure_code
    );
}

static int termite_metal_encode_q8_0_activation_multiply_on_encoder_planned(
    termite_metal_decode_runtime *runtime,
    id<MTLComputeCommandEncoder> encoder,
    id<MTLBuffer> input_buffer,
    size_t input_offset,
    id<MTLBuffer> weight_buffer,
    id<MTLBuffer> rhs_buffer,
    size_t rhs_offset,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    size_t rows,
    size_t in_dim,
    size_t out_dim,
    uint32_t activation_kind,
    uint8_t planned_dispatch,
    int failure_code
) {
    termite_metal_quant_matmul_descriptor descriptor = {
        .slot = SIZE_MAX,
        .format = TERMITE_METAL_QUANT_FORMAT_Q8_0,
        .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
        .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_ACTIVATION_RHS_MUL,
        .fallback_pipeline = runtime != NULL ? runtime->q8_0_activation_multiply_reduce_pipeline : nil,
        .input_buffer = input_buffer,
        .input_offset = input_offset,
        .weight_buffer = weight_buffer,
        .rhs_buffer = rhs_buffer,
        .rhs_offset = rhs_offset,
        .output_buffer = output_buffer,
        .output_offset = output_offset,
        .rows = rows,
        .in_dim = in_dim,
        .out_dim = out_dim,
        .values_per_block = 32u,
        .bytes_per_block = 34u,
        .activation_kind = activation_kind,
        .use_planned_dispatch = true,
        .planned_dispatch = planned_dispatch,
        .failure_code = failure_code,
    };
    return termite_metal_encode_quant_matmul_descriptor_on_encoder(runtime, encoder, &descriptor);
}

static int termite_metal_encode_rms_norm_add(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    id<MTLBuffer> input_buffer,
    size_t input_offset,
    size_t norm_slot,
    id<MTLBuffer> residual_buffer,
    size_t residual_offset,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    size_t hidden_size,
    float eps,
    int failure_code
) {
    if (runtime == NULL || command_buffer == nil) return failure_code;
    id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_RMS_NORM);
    if (encoder == nil) return failure_code;
    const int rc = termite_metal_encode_rms_norm_add_on_encoder(runtime, encoder, input_buffer, input_offset, norm_slot, residual_buffer, residual_offset, output_buffer, output_offset, hidden_size, eps, failure_code);
    [encoder endEncoding];
    return rc;
}

static int termite_metal_encode_rms_norm_add_on_encoder(
    termite_metal_decode_runtime *runtime,
    id<MTLComputeCommandEncoder> encoder,
    id<MTLBuffer> input_buffer,
    size_t input_offset,
    size_t norm_slot,
    id<MTLBuffer> residual_buffer,
    size_t residual_offset,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    size_t hidden_size,
    float eps,
    int failure_code
) {
    if (runtime == NULL || encoder == nil || input_buffer == nil || residual_buffer == nil || output_buffer == nil) return failure_code;
    if (runtime->rms_norm_add_pipeline == nil) return failure_code;
    if (norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return failure_code;
    if (runtime->rms_norm_slot_prepared[norm_slot] == 0 || runtime->rms_norm_hidden_sizes[norm_slot] != hidden_size) return failure_code;
    if (hidden_size == 0 || hidden_size > UINT32_MAX) return failure_code;
    size_t hidden_bytes = 0;
    if (!termite_metal_size_mul(hidden_size, sizeof(float), &hidden_bytes)) return failure_code;
    termite_metal_planned_encoder_range accesses[4];
    if (termite_metal_planned_range_make(input_buffer, input_offset, hidden_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[0], failure_code) != 0 ||
        termite_metal_planned_range_make(runtime->rms_norm_weight_buffers[norm_slot], 0, runtime->rms_norm_weight_buffers[norm_slot].length, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[1], failure_code) != 0 ||
        termite_metal_planned_range_make(residual_buffer, residual_offset, hidden_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[2], failure_code) != 0 ||
        termite_metal_planned_range_make(output_buffer, output_offset, hidden_bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &accesses[3], failure_code) != 0 ||
        termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, accesses, 4, failure_code) != 0)
    {
        return failure_code;
    }
    termite_metal_apply_layer_norm_params params = {
        .hidden_size = (uint32_t)hidden_size,
        .eps = eps,
    };
    [encoder setComputePipelineState:runtime->rms_norm_add_pipeline];
    [encoder setBuffer:input_buffer offset:input_offset atIndex:0];
    [encoder setBuffer:runtime->rms_norm_weight_buffers[norm_slot] offset:0 atIndex:1];
    [encoder setBuffer:residual_buffer offset:residual_offset atIndex:2];
    [encoder setBuffer:output_buffer offset:output_offset atIndex:3];
    [encoder setBytes:&params length:sizeof(params) atIndex:4];
    [encoder setThreadgroupMemoryLength:256u * sizeof(float) atIndex:0];
    [encoder dispatchThreadgroups:MTLSizeMake(1, 1, 1) threadsPerThreadgroup:MTLSizeMake(256, 1, 1)];
    return 0;
}

static int termite_metal_encode_attention_f32_on_encoder(
    termite_metal_decode_runtime *runtime,
    id<MTLComputeCommandEncoder> encoder,
    id<MTLBuffer> q_buffer,
    size_t q_offset,
    id<MTLBuffer> k_buffer,
    size_t k_offset,
    id<MTLBuffer> v_buffer,
    size_t v_offset,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    size_t q_len,
    size_t kv_tokens,
    size_t num_heads,
    size_t num_kv_heads,
    size_t head_dim,
    size_t query_position_offset,
    size_t kv_position_offset,
    size_t sliding_window,
    size_t total_sequence_len,
    int failure_code
) {
    if (runtime == NULL || encoder == nil || q_buffer == nil || k_buffer == nil || v_buffer == nil || output_buffer == nil) return failure_code;
    if (runtime->attention_f32_pipeline == nil) return failure_code;
    if (q_len == 0 || kv_tokens == 0 || num_heads == 0 || num_kv_heads == 0 || head_dim == 0) return failure_code;
    if (q_len > UINT32_MAX || kv_tokens > UINT32_MAX || num_heads > UINT32_MAX || num_kv_heads > UINT32_MAX || head_dim > UINT32_MAX || query_position_offset > UINT32_MAX || kv_position_offset > UINT32_MAX || sliding_window > UINT32_MAX || total_sequence_len > UINT32_MAX) return failure_code;
    size_t q_rows = 0;
    size_t kv_rows = 0;
    size_t q_bytes = 0;
    size_t kv_bytes = 0;
    size_t output_bytes = 0;
    if (!termite_metal_size_mul(q_len, num_heads, &q_rows) ||
        !termite_metal_size_mul(kv_tokens, num_kv_heads, &kv_rows) ||
        !termite_metal_size_mul3(q_rows, head_dim, sizeof(float), &q_bytes) ||
        !termite_metal_size_mul3(kv_rows, head_dim, sizeof(float), &kv_bytes) ||
        !termite_metal_size_mul3(q_rows, head_dim, sizeof(float), &output_bytes))
    {
        return failure_code;
    }
    termite_metal_planned_encoder_range accesses[4];
    if (termite_metal_planned_range_make(q_buffer, q_offset, q_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[0], failure_code) != 0 ||
        termite_metal_planned_range_make(k_buffer, k_offset, kv_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[1], failure_code) != 0 ||
        termite_metal_planned_range_make(v_buffer, v_offset, kv_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[2], failure_code) != 0 ||
        termite_metal_planned_range_make(output_buffer, output_offset, output_bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &accesses[3], failure_code) != 0 ||
        termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, accesses, 4, failure_code) != 0)
    {
        return failure_code;
    }
    const float zero_bias = 0.0f;
    const uint8_t zero_mask = 0;
    termite_metal_attention_f32_params params = {
        .q_len = (uint32_t)q_len,
        .kv_len = (uint32_t)kv_tokens,
        .num_heads = (uint32_t)num_heads,
        .num_kv_heads = (uint32_t)num_kv_heads,
        .head_dim = (uint32_t)head_dim,
        .query_position_offset = (uint32_t)query_position_offset,
        .kv_position_offset = (uint32_t)kv_position_offset,
        .sliding_window = (uint32_t)sliding_window,
        .total_sequence_len = (uint32_t)total_sequence_len,
        .has_bias = 0u,
        .has_mask = 0u,
    };
    const BOOL use_prefill_tiled = (runtime->attention_f32_prefill_pipeline != nil && q_len >= 1 && kv_tokens <= 2048u && head_dim <= 1024u);
    id<MTLComputePipelineState> pipeline = use_prefill_tiled ? runtime->attention_f32_prefill_pipeline : runtime->attention_f32_pipeline;
    [encoder setComputePipelineState:pipeline];
    [encoder setBuffer:q_buffer offset:q_offset atIndex:0];
    [encoder setBuffer:k_buffer offset:k_offset atIndex:1];
    [encoder setBuffer:v_buffer offset:v_offset atIndex:2];
    [encoder setBuffer:output_buffer offset:output_offset atIndex:3];
    [encoder setBytes:&zero_bias length:sizeof(zero_bias) atIndex:4];
    [encoder setBytes:&zero_mask length:sizeof(zero_mask) atIndex:5];
    [encoder setBytes:&params length:sizeof(params) atIndex:6];
    if (use_prefill_tiled) {
        [encoder setThreadgroupMemoryLength:termite_metal_threadgroup_memory_16((kv_tokens + 256u) * sizeof(float)) atIndex:0];
        [encoder dispatchThreadgroups:MTLSizeMake(q_len, num_heads, 1) threadsPerThreadgroup:MTLSizeMake(256, 1, 1)];
    } else {
        const size_t total_rows = q_len * num_heads;
        [encoder dispatchThreads:MTLSizeMake(total_rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->attention_f32_pipeline, total_rows), 1, 1)];
    }
    return 0;
}

static int termite_metal_encode_paged_attention_slot_on_encoder(
    termite_metal_decode_runtime *runtime,
    id<MTLComputeCommandEncoder> encoder,
    size_t slot,
    uint32_t format,
    id<MTLBuffer> q_buffer,
    size_t q_offset,
    const uint32_t *block_table,
    size_t block_count,
    size_t page_size,
    size_t q_len,
    size_t kv_tokens,
    size_t num_heads,
    size_t num_kv_heads,
    size_t head_dim,
    size_t key_row_bytes,
    size_t base_key_row_bytes,
    size_t query_position_offset,
    size_t kv_position_offset,
    size_t sliding_window,
    float softcap,
    const float *sinks,
    size_t sink_count,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    int failure_code
) {
    if (runtime == NULL || encoder == nil || q_buffer == nil || block_table == NULL || output_buffer == nil) return failure_code;
    if (slot >= TERMITE_METAL_ATTENTION_SPAN_SLOT_CAPACITY) return failure_code;
    if (q_len == 0 || kv_tokens == 0 || num_heads == 0 || num_kv_heads == 0 || head_dim == 0 || page_size == 0 || block_count == 0) return failure_code;
    if (num_heads % num_kv_heads != 0) return failure_code;
    if (q_len > UINT32_MAX || kv_tokens > UINT32_MAX || num_heads > UINT32_MAX || num_kv_heads > UINT32_MAX || head_dim > UINT32_MAX) return failure_code;
    if (key_row_bytes > UINT32_MAX || base_key_row_bytes > UINT32_MAX || query_position_offset > UINT32_MAX || kv_position_offset > UINT32_MAX || sliding_window > UINT32_MAX || page_size > UINT32_MAX || block_count > UINT32_MAX) return failure_code;
    if (runtime->attention_paged_pipeline == nil) return failure_code;
    if (runtime->attention_span_encoded_key_buffers[slot] == nil || runtime->attention_span_v_buffers[slot] == nil) return failure_code;
    if (runtime->attention_span_slot_tokens[slot] < kv_tokens || runtime->attention_span_slot_key_row_bytes[slot] != key_row_bytes || runtime->attention_span_slot_position_offset[slot] != kv_position_offset) return failure_code;
    if (runtime->attention_span_slot_v_row_stride[slot] > UINT32_MAX) return failure_code;
    if (sinks != NULL && sink_count < num_heads) return failure_code;
    if (block_count > SIZE_MAX / sizeof(uint32_t)) return failure_code;
    const uint32_t v_element_bytes = (format == 3u) ? (uint32_t)sizeof(uint16_t) : (uint32_t)sizeof(float);
    switch (format) {
        case 0u:
        case 1u:
        case 2u:
        case 3u:
        case 4u:
            break;
        default:
            return failure_code;
    }

    termite_metal_paged_attention_params params = {
        .q_len = (uint32_t)q_len,
        .kv_tokens = (uint32_t)kv_tokens,
        .num_heads = (uint32_t)num_heads,
        .num_kv_heads = (uint32_t)num_kv_heads,
        .head_dim = (uint32_t)head_dim,
        .key_row_bytes = (uint32_t)key_row_bytes,
        .base_key_row_bytes = (uint32_t)base_key_row_bytes,
        .query_position_offset = (uint32_t)query_position_offset,
        .kv_position_offset = (uint32_t)kv_position_offset,
        .sliding_window = (uint32_t)sliding_window,
        .v_row_stride = (uint32_t)runtime->attention_span_slot_v_row_stride[slot],
        .page_size = (uint32_t)page_size,
        .block_count = (uint32_t)block_count,
        .format = format,
        .v_element_bytes = v_element_bytes,
        .has_sinks = sinks != NULL ? 1u : 0u,
        .softcap = softcap,
    };
    const float zero_sink = 0.0f;
    const size_t block_table_bytes = block_count * sizeof(uint32_t);
    const size_t sinks_bytes = sinks != NULL ? num_heads * sizeof(float) : sizeof(zero_sink);
    id<MTLBuffer> block_table_buffer = nil;
    id<MTLBuffer> sinks_buffer = nil;
    size_t q_bytes = 0;
    size_t output_bytes = 0;
    if (!termite_metal_size_mul3(q_len, num_heads, head_dim, &q_bytes) ||
        !termite_metal_size_mul(q_bytes, sizeof(float), &q_bytes) ||
        !termite_metal_size_mul3(q_len, num_heads, head_dim, &output_bytes) ||
        !termite_metal_size_mul(output_bytes, sizeof(float), &output_bytes))
    {
        return failure_code;
    }
    termite_metal_planned_encoder_range accesses[4];
    if (termite_metal_planned_range_make(q_buffer, q_offset, q_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[0], failure_code) != 0 ||
        termite_metal_planned_range_make(runtime->attention_span_encoded_key_buffers[slot], 0, runtime->attention_span_encoded_key_buffers[slot].length, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[1], failure_code) != 0 ||
        termite_metal_planned_range_make(runtime->attention_span_v_buffers[slot], 0, runtime->attention_span_v_buffers[slot].length, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[2], failure_code) != 0 ||
        termite_metal_planned_range_make(output_buffer, output_offset, output_bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &accesses[3], failure_code) != 0 ||
        termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, accesses, 4, failure_code) != 0)
    {
        return failure_code;
    }

    [encoder setComputePipelineState:runtime->attention_paged_pipeline];
    [encoder setBuffer:q_buffer offset:q_offset atIndex:0];
    [encoder setBuffer:runtime->attention_span_encoded_key_buffers[slot] offset:0 atIndex:1];
    [encoder setBuffer:runtime->attention_span_v_buffers[slot] offset:0 atIndex:2];
    if (block_table_bytes <= 4096u) {
        [encoder setBytes:block_table length:block_table_bytes atIndex:3];
    } else {
        block_table_buffer = [runtime->device newBufferWithBytes:block_table length:block_table_bytes options:MTLResourceStorageModeShared];
        if (block_table_buffer == nil) return failure_code;
        [encoder setBuffer:block_table_buffer offset:0 atIndex:3];
    }
    if (sinks == NULL) {
        [encoder setBytes:&zero_sink length:sizeof(zero_sink) atIndex:4];
    } else if (sinks_bytes <= 4096u) {
        [encoder setBytes:sinks length:sinks_bytes atIndex:4];
    } else {
        sinks_buffer = [runtime->device newBufferWithBytes:sinks length:sinks_bytes options:MTLResourceStorageModeShared];
        if (sinks_buffer == nil) return failure_code;
        [encoder setBuffer:sinks_buffer offset:0 atIndex:4];
    }
    [encoder setBuffer:output_buffer offset:output_offset atIndex:5];
    [encoder setBytes:&params length:sizeof(params) atIndex:6];
    const size_t total = q_len * num_heads;
    [encoder dispatchThreads:MTLSizeMake(total, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->attention_paged_pipeline, total), 1, 1)];
    (void)block_table_buffer;
    (void)sinks_buffer;
    return 0;
}

static int termite_metal_encode_q8_0_linear_rms_add_planned_1x(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    id<MTLBuffer> linear_input_buffer,
    size_t linear_input_offset,
    id<MTLBuffer> linear_weight_buffer,
    id<MTLBuffer> projected_buffer,
    size_t projected_offset,
    size_t in_dim,
    size_t hidden_size,
    size_t post_rms_norm_slot,
    id<MTLBuffer> residual_buffer,
    size_t residual_offset,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    float eps,
    size_t source,
    size_t region,
    int failure_code
) {
    if (runtime == NULL || command_buffer == nil || runtime->active_frame_cb != command_buffer) return failure_code;
    if (linear_input_buffer == nil || linear_weight_buffer == nil || projected_buffer == nil || residual_buffer == nil || output_buffer == nil) return failure_code;
    if (termite_metal_decode_runtime_begin_planned_compute_scope(runtime, source, region) != 0) return failure_code;
    id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
    int rc = termite_metal_encode_q8_0_linear_raw_on_encoder(
        runtime,
        encoder,
        linear_input_buffer,
        linear_input_offset,
        linear_weight_buffer,
        projected_buffer,
        projected_offset,
        1,
        in_dim,
        hidden_size,
        failure_code
    );
    if (rc == 0) {
        rc = termite_metal_encode_rms_norm_add_on_encoder(
            runtime,
            encoder,
            projected_buffer,
            projected_offset,
            post_rms_norm_slot,
            residual_buffer,
            residual_offset,
            output_buffer,
            output_offset,
            hidden_size,
            eps,
            failure_code
        );
    }
    const int end_rc = termite_metal_decode_runtime_end_planned_compute_scope(runtime);
    if (rc != 0) return rc;
    return end_rc == 0 ? 0 : failure_code;
}

static int termite_metal_encode_q8_0_ffn_pair_down_rms_add_planned_1x(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    id<MTLBuffer> input_buffer,
    size_t input_offset,
    id<MTLBuffer> gate_weight_buffer,
    id<MTLBuffer> up_weight_buffer,
    id<MTLBuffer> gated_buffer,
    id<MTLBuffer> down_weight_buffer,
    id<MTLBuffer> projected_buffer,
    size_t hidden_size,
    size_t intermediate_size,
    uint32_t activation_kind,
    size_t post_down_rms_norm_slot,
    id<MTLBuffer> residual_buffer,
    size_t residual_offset,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    float eps,
    int failure_code
) {
    if (runtime == NULL || command_buffer == nil || runtime->active_frame_cb != command_buffer) return failure_code;
    if (input_buffer == nil || gate_weight_buffer == nil || up_weight_buffer == nil || gated_buffer == nil || down_weight_buffer == nil || projected_buffer == nil || residual_buffer == nil || output_buffer == nil) return failure_code;
    if (termite_metal_decode_runtime_begin_planned_compute_scope(runtime, TERMITE_METAL_COMPUTE_SOURCE_FFN, TERMITE_METAL_COMPUTE_REGION_FFN) != 0) return failure_code;
    id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
    int rc = termite_metal_encode_q8_0_pair_activation_multiply_on_encoder(
        runtime,
        encoder,
        input_buffer,
        input_offset,
        gate_weight_buffer,
        up_weight_buffer,
        gated_buffer,
        0,
        1,
        hidden_size,
        intermediate_size,
        activation_kind,
        failure_code
    );
    if (rc == 0) {
        rc = termite_metal_encode_q8_0_linear_raw_on_encoder(
            runtime,
            encoder,
            gated_buffer,
            0,
            down_weight_buffer,
            projected_buffer,
            0,
            1,
            intermediate_size,
            hidden_size,
            failure_code
        );
    }
    if (rc == 0) {
        rc = termite_metal_encode_rms_norm_add_on_encoder(
            runtime,
            encoder,
            projected_buffer,
            0,
            post_down_rms_norm_slot,
            residual_buffer,
            residual_offset,
            output_buffer,
            output_offset,
            hidden_size,
            eps,
            failure_code
        );
    }
    const int end_rc = termite_metal_decode_runtime_end_planned_compute_scope(runtime);
    if (rc != 0) return rc;
    return end_rc == 0 ? 0 : failure_code;
}

static int termite_metal_encode_q8_0_ffn_pregate_pair_down_rms_add_planned_1x(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    id<MTLBuffer> input_buffer,
    size_t input_offset,
    size_t pre_gate_rms_norm_slot,
    id<MTLBuffer> inv_scale_buffer,
    id<MTLBuffer> gate_weight_buffer,
    id<MTLBuffer> up_weight_buffer,
    id<MTLBuffer> gated_buffer,
    id<MTLBuffer> down_weight_buffer,
    id<MTLBuffer> projected_buffer,
    size_t hidden_size,
    size_t intermediate_size,
    uint32_t activation_kind,
    size_t post_down_rms_norm_slot,
    id<MTLBuffer> residual_buffer,
    size_t residual_offset,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    float eps,
    int failure_code
) {
    if (runtime == NULL || command_buffer == nil || runtime->active_frame_cb != command_buffer) return failure_code;
    if (input_buffer == nil || inv_scale_buffer == nil || gate_weight_buffer == nil || up_weight_buffer == nil || gated_buffer == nil || down_weight_buffer == nil || projected_buffer == nil || residual_buffer == nil || output_buffer == nil) return failure_code;
    if (termite_metal_decode_runtime_begin_planned_compute_scope(runtime, TERMITE_METAL_COMPUTE_SOURCE_FFN, TERMITE_METAL_COMPUTE_REGION_FFN) != 0) return failure_code;
    id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
    int rc = termite_metal_encode_rms_inv_scale_1x_on_encoder(
        runtime,
        encoder,
        input_buffer,
        input_offset,
        inv_scale_buffer,
        0,
        hidden_size,
        eps,
        failure_code
    );
    if (rc == 0) {
        rc = termite_metal_encode_q8_0_pair_activation_multiply_rms_scale_1x_on_encoder(
            runtime,
            encoder,
            input_buffer,
            input_offset,
            pre_gate_rms_norm_slot,
            inv_scale_buffer,
            0,
            gate_weight_buffer,
            up_weight_buffer,
            gated_buffer,
            0,
            hidden_size,
            intermediate_size,
            activation_kind,
            failure_code
        );
    }
    if (rc == 0) {
        rc = termite_metal_encode_q8_0_linear_raw_on_encoder(
            runtime,
            encoder,
            gated_buffer,
            0,
            down_weight_buffer,
            projected_buffer,
            0,
            1,
            intermediate_size,
            hidden_size,
            failure_code
        );
    }
    if (rc == 0) {
        rc = termite_metal_encode_rms_norm_add_on_encoder(
            runtime,
            encoder,
            projected_buffer,
            0,
            post_down_rms_norm_slot,
            residual_buffer,
            residual_offset,
            output_buffer,
            output_offset,
            hidden_size,
            eps,
            failure_code
        );
    }
    const int end_rc = termite_metal_decode_runtime_end_planned_compute_scope(runtime);
    if (rc != 0) return rc;
    return end_rc == 0 ? 0 : failure_code;
}

static int termite_metal_encode_attention_q8_0_linear_rms_add_planned_1x(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    id<MTLBuffer> q_buffer,
    size_t q_offset,
    id<MTLBuffer> k_buffer,
    size_t k_offset,
    id<MTLBuffer> v_buffer,
    size_t v_offset,
    id<MTLBuffer> attention_output_buffer,
    size_t attention_output_offset,
    size_t kv_tokens,
    size_t num_heads,
    size_t num_kv_heads,
    size_t head_dim,
    size_t query_position_offset,
    size_t kv_position_offset,
    size_t sliding_window,
    size_t total_sequence_len,
    id<MTLBuffer> linear_weight_buffer,
    id<MTLBuffer> projected_buffer,
    size_t projected_offset,
    size_t attention_input_size,
    size_t hidden_size,
    size_t post_rms_norm_slot,
    id<MTLBuffer> residual_buffer,
    size_t residual_offset,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    float eps,
    int failure_code
) {
    if (runtime == NULL || command_buffer == nil || runtime->active_frame_cb != command_buffer) return failure_code;
    if (q_buffer == nil || k_buffer == nil || v_buffer == nil || attention_output_buffer == nil || linear_weight_buffer == nil || projected_buffer == nil || residual_buffer == nil || output_buffer == nil) return failure_code;
    if (termite_metal_decode_runtime_begin_planned_compute_scope(runtime, TERMITE_METAL_COMPUTE_SOURCE_ATTENTION, TERMITE_METAL_COMPUTE_REGION_ATTENTION) != 0) return failure_code;
    id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
    int rc = termite_metal_encode_attention_f32_on_encoder(
        runtime,
        encoder,
        q_buffer,
        q_offset,
        k_buffer,
        k_offset,
        v_buffer,
        v_offset,
        attention_output_buffer,
        attention_output_offset,
        1,
        kv_tokens,
        num_heads,
        num_kv_heads,
        head_dim,
        query_position_offset,
        kv_position_offset,
        sliding_window,
        total_sequence_len,
        failure_code
    );
    if (rc == 0) {
        rc = termite_metal_encode_q8_0_linear_raw_on_encoder(
            runtime,
            encoder,
            attention_output_buffer,
            attention_output_offset,
            linear_weight_buffer,
            projected_buffer,
            projected_offset,
            1,
            attention_input_size,
            hidden_size,
            failure_code
        );
    }
    if (rc == 0) {
        rc = termite_metal_encode_rms_norm_add_on_encoder(
            runtime,
            encoder,
            projected_buffer,
            projected_offset,
            post_rms_norm_slot,
            residual_buffer,
            residual_offset,
            output_buffer,
            output_offset,
            hidden_size,
            eps,
            failure_code
        );
    }
    const int end_rc = termite_metal_decode_runtime_end_planned_compute_scope(runtime);
    if (rc != 0) return rc;
    return end_rc == 0 ? 0 : failure_code;
}

static int termite_metal_encode_rms_norm_add_scale_on_encoder(
    termite_metal_decode_runtime *runtime,
    id<MTLComputeCommandEncoder> encoder,
    id<MTLBuffer> input_buffer,
    size_t input_offset,
    size_t norm_slot,
    id<MTLBuffer> residual_buffer,
    size_t residual_offset,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    size_t hidden_size,
    float eps,
    float scale,
    int failure_code
) {
    if (runtime == NULL || encoder == nil || input_buffer == nil || residual_buffer == nil || output_buffer == nil) return failure_code;
    if (runtime->rms_norm_add_scale_pipeline == nil) return failure_code;
    if (norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return failure_code;
    if (runtime->rms_norm_slot_prepared[norm_slot] == 0 || runtime->rms_norm_hidden_sizes[norm_slot] != hidden_size) return failure_code;
    if (hidden_size == 0 || hidden_size > UINT32_MAX) return failure_code;
    size_t hidden_bytes = 0;
    if (!termite_metal_size_mul(hidden_size, sizeof(float), &hidden_bytes)) return failure_code;
    termite_metal_planned_encoder_range accesses[4];
    if (termite_metal_planned_range_make(input_buffer, input_offset, hidden_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[0], failure_code) != 0 ||
        termite_metal_planned_range_make(runtime->rms_norm_weight_buffers[norm_slot], 0, runtime->rms_norm_weight_buffers[norm_slot].length, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[1], failure_code) != 0 ||
        termite_metal_planned_range_make(residual_buffer, residual_offset, hidden_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[2], failure_code) != 0 ||
        termite_metal_planned_range_make(output_buffer, output_offset, hidden_bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &accesses[3], failure_code) != 0 ||
        termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, accesses, 4, failure_code) != 0)
    {
        return failure_code;
    }
    termite_metal_apply_layer_norm_scale_params params = {
        .hidden_size = (uint32_t)hidden_size,
        .eps = eps,
        .scale = scale,
        .reserved = 0,
    };
    id<MTLBuffer> params_buffer = [runtime->device newBufferWithBytes:&params length:sizeof(params) options:MTLResourceStorageModeShared];
    if (params_buffer == nil) return failure_code;
    [encoder setComputePipelineState:runtime->rms_norm_add_scale_pipeline];
    [encoder setBuffer:input_buffer offset:input_offset atIndex:0];
    [encoder setBuffer:runtime->rms_norm_weight_buffers[norm_slot] offset:0 atIndex:1];
    [encoder setBuffer:residual_buffer offset:residual_offset atIndex:2];
    [encoder setBuffer:output_buffer offset:output_offset atIndex:3];
    [encoder setBuffer:params_buffer offset:0 atIndex:4];
    [encoder setThreadgroupMemoryLength:256u * sizeof(float) atIndex:0];
    [encoder dispatchThreadgroups:MTLSizeMake(1, 1, 1) threadsPerThreadgroup:MTLSizeMake(256, 1, 1)];
    return 0;
}

static int termite_metal_encode_q8_0_linear_rms_add_scale_planned_1x(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    id<MTLBuffer> linear_input_buffer,
    size_t linear_input_offset,
    id<MTLBuffer> linear_weight_buffer,
    id<MTLBuffer> projected_buffer,
    size_t projected_offset,
    size_t in_dim,
    size_t hidden_size,
    size_t post_rms_norm_slot,
    id<MTLBuffer> residual_buffer,
    size_t residual_offset,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    float eps,
    float scale,
    size_t source,
    size_t region,
    int failure_code
) {
    if (runtime == NULL || command_buffer == nil || runtime->active_frame_cb != command_buffer) return failure_code;
    if (linear_input_buffer == nil || linear_weight_buffer == nil || projected_buffer == nil || residual_buffer == nil || output_buffer == nil) return failure_code;
    if (termite_metal_decode_runtime_begin_planned_compute_scope(runtime, source, region) != 0) return failure_code;
    id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
    int rc = termite_metal_encode_q8_0_linear_raw_on_encoder(
        runtime,
        encoder,
        linear_input_buffer,
        linear_input_offset,
        linear_weight_buffer,
        projected_buffer,
        projected_offset,
        1,
        in_dim,
        hidden_size,
        failure_code
    );
    if (rc == 0) {
        rc = termite_metal_encode_rms_norm_add_scale_on_encoder(
            runtime,
            encoder,
            projected_buffer,
            projected_offset,
            post_rms_norm_slot,
            residual_buffer,
            residual_offset,
            output_buffer,
            output_offset,
            hidden_size,
            eps,
            scale,
            failure_code
        );
    }
    const int end_rc = termite_metal_decode_runtime_end_planned_compute_scope(runtime);
    if (rc != 0) return rc;
    return end_rc == 0 ? 0 : failure_code;
}

static int termite_metal_encode_q8_0_ple_gate_proj_rms_add_scale_planned_1x(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    id<MTLBuffer> hidden_buffer,
    size_t hidden_offset,
    id<MTLBuffer> ple_buffer,
    size_t ple_offset,
    id<MTLBuffer> gate_weight_buffer,
    id<MTLBuffer> gated_buffer,
    id<MTLBuffer> proj_weight_buffer,
    id<MTLBuffer> projected_buffer,
    size_t hidden_size,
    size_t ple_hidden_size,
    uint32_t activation_kind,
    size_t post_norm_slot,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    float eps,
    int output_scale_present,
    float output_scale,
    int failure_code
) {
    if (runtime == NULL || command_buffer == nil || runtime->active_frame_cb != command_buffer) return failure_code;
    if (hidden_buffer == nil || ple_buffer == nil || gate_weight_buffer == nil || gated_buffer == nil || proj_weight_buffer == nil || projected_buffer == nil || output_buffer == nil) return failure_code;
    if (termite_metal_decode_runtime_begin_planned_compute_scope(runtime, TERMITE_METAL_COMPUTE_SOURCE_PLE, TERMITE_METAL_COMPUTE_REGION_PLE) != 0) return failure_code;
    id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
    int rc = termite_metal_encode_q8_0_activation_multiply_on_encoder(
        runtime,
        encoder,
        hidden_buffer,
        hidden_offset,
        gate_weight_buffer,
        ple_buffer,
        ple_offset,
        gated_buffer,
        0,
        1,
        hidden_size,
        ple_hidden_size,
        activation_kind,
        failure_code
    );
    if (rc == 0) {
        rc = termite_metal_encode_q8_0_linear_raw_on_encoder(
            runtime,
            encoder,
            gated_buffer,
            0,
            proj_weight_buffer,
            projected_buffer,
            0,
            1,
            ple_hidden_size,
            hidden_size,
            failure_code
        );
    }
    if (rc == 0) {
        rc = output_scale_present != 0
            ? termite_metal_encode_rms_norm_add_scale_on_encoder(
                runtime,
                encoder,
                projected_buffer,
                0,
                post_norm_slot,
                hidden_buffer,
                hidden_offset,
                output_buffer,
                output_offset,
                hidden_size,
                eps,
                output_scale,
                failure_code)
            : termite_metal_encode_rms_norm_add_on_encoder(
                runtime,
                encoder,
                projected_buffer,
                0,
                post_norm_slot,
                hidden_buffer,
                hidden_offset,
                output_buffer,
                output_offset,
                hidden_size,
                eps,
                failure_code);
    }
    const int end_rc = termite_metal_decode_runtime_end_planned_compute_scope(runtime);
    if (rc != 0) return rc;
    return end_rc == 0 ? 0 : failure_code;
}

static int termite_metal_encode_q8_0_ffn_pregate_ple_planned_1x(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    id<MTLBuffer> input_buffer,
    size_t input_offset,
    size_t pre_gate_rms_norm_slot,
    size_t gate_ffn_linear_slot,
    size_t up_ffn_linear_slot,
    size_t down_ffn_linear_slot,
    size_t post_down_rms_norm_slot,
    id<MTLBuffer> residual_buffer,
    size_t residual_offset,
    id<MTLBuffer> ffn_output_buffer,
    size_t ffn_output_offset,
    id<MTLBuffer> ple_buffer,
    size_t ple_offset,
    size_t ple_gate_linear_slot,
    size_t ple_proj_linear_slot,
    size_t ple_post_norm_slot,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    size_t hidden_size,
    size_t intermediate_size,
    size_t ple_hidden_size,
    uint32_t activation_kind,
    float eps,
    int output_scale_present,
    float output_scale,
    int failure_code
) {
    if (runtime == NULL || command_buffer == nil || runtime->active_frame_cb != command_buffer) return failure_code;
    if (input_buffer == nil || residual_buffer == nil || ffn_output_buffer == nil || ple_buffer == nil || output_buffer == nil) return failure_code;
    if (hidden_size == 0 || intermediate_size == 0 || ple_hidden_size == 0 || hidden_size % 32u != 0 || intermediate_size % 32u != 0 || ple_hidden_size % 32u != 0) return failure_code;
    if (pre_gate_rms_norm_slot == SIZE_MAX || post_down_rms_norm_slot == SIZE_MAX || ple_post_norm_slot == SIZE_MAX) return failure_code;
    if (runtime->rms_inv_scale_pipeline == nil || runtime->q8_0_pair_activation_rms_scale_mmv_pipeline == nil) return failure_code;

    termite_metal_quant_linear_slot_view gate_view;
    termite_metal_quant_linear_slot_view up_view;
    termite_metal_quant_linear_slot_view down_view;
    termite_metal_quant_linear_slot_view ple_gate_view;
    termite_metal_quant_linear_slot_view ple_proj_view;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q8_0, gate_ffn_linear_slot, hidden_size, intermediate_size, &gate_view) != 0) return failure_code;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q8_0, up_ffn_linear_slot, hidden_size, intermediate_size, &up_view) != 0) return failure_code;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q8_0, down_ffn_linear_slot, intermediate_size, hidden_size, &down_view) != 0) return failure_code;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q8_0, ple_gate_linear_slot, hidden_size, ple_hidden_size, &ple_gate_view) != 0) return failure_code;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q8_0, ple_proj_linear_slot, ple_hidden_size, hidden_size, &ple_proj_view) != 0) return failure_code;
    if (pre_gate_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY || post_down_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY || ple_post_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return failure_code;
    if (runtime->rms_norm_slot_prepared[pre_gate_rms_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[pre_gate_rms_norm_slot] != hidden_size) return failure_code;
    if (runtime->rms_norm_slot_prepared[post_down_rms_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[post_down_rms_norm_slot] != hidden_size) return failure_code;
    if (runtime->rms_norm_slot_prepared[ple_post_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[ple_post_norm_slot] != hidden_size) return failure_code;

    const size_t hidden_bytes = hidden_size * sizeof(float);
    const size_t intermediate_bytes = intermediate_size * sizeof(float);
    const size_t ple_bytes = ple_hidden_size * sizeof(float);
    if (input_offset + hidden_bytes > input_buffer.length || residual_offset + hidden_bytes > residual_buffer.length || ffn_output_offset + hidden_bytes > ffn_output_buffer.length || ple_offset + ple_bytes > ple_buffer.length || output_offset + hidden_bytes > output_buffer.length) return failure_code;
    if (termite_metal_decode_runtime_ensure_hot_buffers(runtime, hidden_bytes, intermediate_bytes > ple_bytes ? intermediate_bytes : ple_bytes) != 0) return failure_code;

    id<MTLBuffer> inv_scale_buffer = runtime->hot_hidden_buffers[1];
    id<MTLBuffer> ffn_gated_buffer = runtime->hot_intermediate_buffers[2];
    id<MTLBuffer> ffn_projected_buffer = runtime->hot_hidden_buffers[0];
    id<MTLBuffer> ple_gated_buffer = runtime->hot_intermediate_buffers[3];
    id<MTLBuffer> ple_projected_buffer = runtime->hot_hidden_buffers[2];
    if (inv_scale_buffer == nil || ffn_gated_buffer == nil || ffn_projected_buffer == nil || ple_gated_buffer == nil || ple_projected_buffer == nil) return failure_code;

    if (termite_metal_decode_runtime_begin_planned_compute_scope(runtime, TERMITE_METAL_COMPUTE_SOURCE_FFN, TERMITE_METAL_COMPUTE_REGION_FFN) != 0) return failure_code;
    id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
    int rc = termite_metal_encode_rms_inv_scale_1x_on_encoder(
        runtime,
        encoder,
        input_buffer,
        input_offset,
        inv_scale_buffer,
        0,
        hidden_size,
        eps,
        failure_code
    );
    if (rc == 0) {
        rc = termite_metal_encode_q8_0_pair_activation_multiply_rms_scale_1x_on_encoder(
            runtime,
            encoder,
            input_buffer,
            input_offset,
            pre_gate_rms_norm_slot,
            inv_scale_buffer,
            0,
            gate_view.weight_buffer,
            up_view.weight_buffer,
            ffn_gated_buffer,
            0,
            hidden_size,
            intermediate_size,
            activation_kind,
            failure_code
        );
    }
    if (rc == 0) {
        rc = termite_metal_encode_q8_0_linear_raw_on_encoder(
            runtime,
            encoder,
            ffn_gated_buffer,
            0,
            down_view.weight_buffer,
            ffn_projected_buffer,
            0,
            1,
            intermediate_size,
            hidden_size,
            failure_code
        );
    }
    if (rc == 0) {
        rc = termite_metal_encode_rms_norm_add_on_encoder(
            runtime,
            encoder,
            ffn_projected_buffer,
            0,
            post_down_rms_norm_slot,
            residual_buffer,
            residual_offset,
            ffn_output_buffer,
            ffn_output_offset,
            hidden_size,
            eps,
            failure_code
        );
    }
    if (rc == 0) {
        rc = termite_metal_encode_q8_0_activation_multiply_on_encoder(
            runtime,
            encoder,
            ffn_output_buffer,
            ffn_output_offset,
            ple_gate_view.weight_buffer,
            ple_buffer,
            ple_offset,
            ple_gated_buffer,
            0,
            1,
            hidden_size,
            ple_hidden_size,
            activation_kind,
            failure_code
        );
    }
    if (rc == 0) {
        rc = termite_metal_encode_q8_0_linear_raw_on_encoder(
            runtime,
            encoder,
            ple_gated_buffer,
            0,
            ple_proj_view.weight_buffer,
            ple_projected_buffer,
            0,
            1,
            ple_hidden_size,
            hidden_size,
            failure_code
        );
    }
    if (rc == 0) {
        rc = output_scale_present != 0
            ? termite_metal_encode_rms_norm_add_scale_on_encoder(
                runtime,
                encoder,
                ple_projected_buffer,
                0,
                ple_post_norm_slot,
                ffn_output_buffer,
                ffn_output_offset,
                output_buffer,
                output_offset,
                hidden_size,
                eps,
                output_scale,
                failure_code)
            : termite_metal_encode_rms_norm_add_on_encoder(
                runtime,
                encoder,
                ple_projected_buffer,
                0,
                ple_post_norm_slot,
                ffn_output_buffer,
                ffn_output_offset,
                output_buffer,
                output_offset,
                hidden_size,
                eps,
                failure_code);
    }
    const int end_rc = termite_metal_decode_runtime_end_planned_compute_scope(runtime);
    if (rc != 0) return rc;
    return end_rc == 0 ? 0 : failure_code;
}

static int termite_metal_encode_attention_ffn_ple_q8_0_planned_1x(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    id<MTLBuffer> q_buffer,
    size_t q_offset,
    id<MTLBuffer> k_buffer,
    size_t k_offset,
    id<MTLBuffer> v_buffer,
    size_t v_offset,
    id<MTLBuffer> attention_output_buffer,
    size_t kv_tokens,
    size_t num_heads,
    size_t num_kv_heads,
    size_t head_dim,
    size_t query_position_offset,
    size_t kv_position_offset,
    size_t sliding_window,
    size_t total_sequence_len,
    size_t attention_linear_slot,
    size_t attention_post_linear_rms_norm_slot,
    id<MTLBuffer> residual_buffer,
    size_t residual_offset,
    id<MTLBuffer> attn_added_buffer,
    id<MTLBuffer> ffn_output_buffer,
    size_t ffn_output_offset,
    size_t ffn_pre_gate_rms_norm_slot,
    size_t gate_ffn_linear_slot,
    size_t up_ffn_linear_slot,
    size_t down_ffn_linear_slot,
    size_t ffn_post_down_rms_norm_slot,
    id<MTLBuffer> ple_buffer,
    size_t ple_offset,
    size_t ple_gate_linear_slot,
    size_t ple_proj_linear_slot,
    size_t ple_post_norm_slot,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    size_t hidden_size,
    size_t attention_input_size,
    size_t intermediate_size,
    size_t ple_hidden_size,
    uint32_t activation_kind,
    float eps,
    int output_scale_present,
    float output_scale,
    termite_metal_planned_layer_contract planned_contract,
    bool use_paged_kv,
    size_t paged_slot,
    uint32_t paged_format,
    const uint32_t *paged_block_table,
    size_t paged_block_count,
    size_t paged_page_size,
    size_t paged_key_row_bytes,
    size_t paged_base_key_row_bytes,
    size_t paged_v_row_stride,
    size_t paged_suffix_tokens,
    int failure_code
) {
    const bool trace_q80_block = termite_metal_trace_q80_block();
#define TERMITE_Q80_PLANNED_FAIL(REASON) do { \
        if (trace_q80_block) { \
            fprintf(stderr, "metal-planned-q80-1x-fail reason=%s paged=%d suffix=%zu start=%zu ops=%zu\n", (REASON), use_paged_kv ? 1 : 0, paged_suffix_tokens, planned_contract.start_index, planned_contract.command_op_count); \
        } \
        return failure_code; \
    } while (0)
    if (runtime == NULL || command_buffer == nil || runtime->active_frame_cb != command_buffer) TERMITE_Q80_PLANNED_FAIL("runtime-or-frame");
    if (q_buffer == nil || attention_output_buffer == nil || residual_buffer == nil || attn_added_buffer == nil || ffn_output_buffer == nil || ple_buffer == nil || output_buffer == nil) TERMITE_Q80_PLANNED_FAIL("buffer");
    if (!use_paged_kv && (k_buffer == nil || v_buffer == nil)) TERMITE_Q80_PLANNED_FAIL("dense-kv-buffer");
    if (use_paged_kv && paged_suffix_tokens != 0 && (k_buffer == nil || v_buffer == nil)) TERMITE_Q80_PLANNED_FAIL("paged-suffix-buffer");
    if (use_paged_kv && (paged_block_table == NULL || paged_block_count == 0 || paged_page_size == 0 || paged_key_row_bytes == 0 || paged_base_key_row_bytes == 0 || paged_v_row_stride == 0)) TERMITE_Q80_PLANNED_FAIL("paged-descriptor");
    if (hidden_size == 0 || attention_input_size == 0 || intermediate_size == 0 || ple_hidden_size == 0) TERMITE_Q80_PLANNED_FAIL("zero-dim");
    if (hidden_size % 32u != 0 || attention_input_size % 32u != 0 || intermediate_size % 32u != 0 || ple_hidden_size % 32u != 0) TERMITE_Q80_PLANNED_FAIL("dim-alignment");
    if (attention_post_linear_rms_norm_slot == SIZE_MAX || ffn_pre_gate_rms_norm_slot == SIZE_MAX || ffn_post_down_rms_norm_slot == SIZE_MAX || ple_post_norm_slot == SIZE_MAX) TERMITE_Q80_PLANNED_FAIL("missing-rms-slot");
    if (runtime->attention_f32_pipeline == nil || runtime->rms_inv_scale_pipeline == nil || runtime->q8_0_pair_activation_rms_scale_mmv_pipeline == nil) TERMITE_Q80_PLANNED_FAIL("pipeline");

    termite_metal_quant_linear_slot_view attention_view;
    termite_metal_quant_linear_slot_view gate_view;
    termite_metal_quant_linear_slot_view up_view;
    termite_metal_quant_linear_slot_view down_view;
    termite_metal_quant_linear_slot_view ple_gate_view;
    termite_metal_quant_linear_slot_view ple_proj_view;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q8_0, attention_linear_slot, attention_input_size, hidden_size, &attention_view) != 0) TERMITE_Q80_PLANNED_FAIL("attention-slot");
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q8_0, gate_ffn_linear_slot, hidden_size, intermediate_size, &gate_view) != 0) TERMITE_Q80_PLANNED_FAIL("gate-slot");
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q8_0, up_ffn_linear_slot, hidden_size, intermediate_size, &up_view) != 0) TERMITE_Q80_PLANNED_FAIL("up-slot");
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q8_0, down_ffn_linear_slot, intermediate_size, hidden_size, &down_view) != 0) TERMITE_Q80_PLANNED_FAIL("down-slot");
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q8_0, ple_gate_linear_slot, hidden_size, ple_hidden_size, &ple_gate_view) != 0) TERMITE_Q80_PLANNED_FAIL("ple-gate-slot");
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q8_0, ple_proj_linear_slot, ple_hidden_size, hidden_size, &ple_proj_view) != 0) TERMITE_Q80_PLANNED_FAIL("ple-proj-slot");
    if (attention_post_linear_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY || ffn_pre_gate_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY || ffn_post_down_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY || ple_post_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) TERMITE_Q80_PLANNED_FAIL("rms-slot-range");
    if (runtime->rms_norm_slot_prepared[attention_post_linear_rms_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[attention_post_linear_rms_norm_slot] != hidden_size) TERMITE_Q80_PLANNED_FAIL("attention-rms-slot");
    if (runtime->rms_norm_slot_prepared[ffn_pre_gate_rms_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[ffn_pre_gate_rms_norm_slot] != hidden_size) TERMITE_Q80_PLANNED_FAIL("ffn-pre-rms-slot");
    if (runtime->rms_norm_slot_prepared[ffn_post_down_rms_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[ffn_post_down_rms_norm_slot] != hidden_size) TERMITE_Q80_PLANNED_FAIL("ffn-post-rms-slot");
    if (runtime->rms_norm_slot_prepared[ple_post_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[ple_post_norm_slot] != hidden_size) TERMITE_Q80_PLANNED_FAIL("ple-rms-slot");

    const size_t hidden_bytes = hidden_size * sizeof(float);
    const size_t attention_bytes = attention_input_size * sizeof(float);
    const size_t intermediate_bytes = intermediate_size * sizeof(float);
    const size_t ple_bytes = ple_hidden_size * sizeof(float);
    const size_t hot_intermediate_bytes = intermediate_bytes > ple_bytes ? intermediate_bytes : ple_bytes;
    if (q_offset + attention_bytes > q_buffer.length || residual_offset + hidden_bytes > residual_buffer.length || ffn_output_offset + hidden_bytes > ffn_output_buffer.length || ple_offset + ple_bytes > ple_buffer.length || output_offset + hidden_bytes > output_buffer.length) TERMITE_Q80_PLANNED_FAIL("bounds");
    if (termite_metal_decode_runtime_ensure_hot_buffers(runtime, hidden_bytes, hot_intermediate_bytes) != 0) TERMITE_Q80_PLANNED_FAIL("hot-buffers");

    id<MTLBuffer> attention_projected_buffer = runtime->hot_hidden_buffers[3];
    id<MTLBuffer> inv_scale_buffer = runtime->hot_hidden_buffers[1];
    id<MTLBuffer> ffn_gated_buffer = runtime->hot_intermediate_buffers[2];
    id<MTLBuffer> ffn_projected_buffer = runtime->hot_hidden_buffers[0];
    id<MTLBuffer> ple_gated_buffer = runtime->hot_intermediate_buffers[3];
    id<MTLBuffer> ple_projected_buffer = runtime->hot_hidden_buffers[2];
    if (attention_projected_buffer == nil || inv_scale_buffer == nil || ffn_gated_buffer == nil || ffn_projected_buffer == nil || ple_gated_buffer == nil || ple_projected_buffer == nil) TERMITE_Q80_PLANNED_FAIL("hot-buffer-nil");

    const BOOL reusing_planned_encoder = (runtime->active_planned_compute_encoder != nil);
    if (!reusing_planned_encoder && termite_metal_decode_runtime_begin_planned_compute_scope(runtime, TERMITE_METAL_COMPUTE_SOURCE_LAYER, TERMITE_METAL_COMPUTE_REGION_LAYER) != 0) TERMITE_Q80_PLANNED_FAIL("begin-scope");
    id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
    if (encoder == nil) TERMITE_Q80_PLANNED_FAIL("encoder");
    if (planned_contract.command_ops != NULL) {
        int rc = 0;
        bool paged_kv_seed_encoded = false;
        if (trace_q80_block) {
            fprintf(
                stderr,
                "metal-planned-q80-1x-start paged=%d suffix=%zu start=%zu ops=%zu q=%zu kv=%zu page=%zu\n",
                use_paged_kv ? 1 : 0,
                paged_suffix_tokens,
                planned_contract.start_index,
                planned_contract.command_op_count,
                attention_input_size,
                kv_tokens,
                paged_page_size
            );
            const size_t trace_limit = planned_contract.command_op_count < 16u ? planned_contract.command_op_count : 16u;
            fprintf(stderr, "metal-planned-q80-1x-ops");
            for (size_t trace_index = planned_contract.start_index; trace_index < trace_limit; trace_index += 1) {
                const termite_metal_runtime_command_op trace_op = planned_contract.command_ops[trace_index];
                fprintf(
                    stderr,
                    " %zu:%u/b%u/q%u/o%u/f%u",
                    trace_index,
                    trace_op.kind,
                    trace_op.barrier_before,
                    trace_op.quant_dispatch,
                    trace_op.op,
                    trace_op.format
                );
            }
            fprintf(stderr, "\n");
        }
        for (size_t op_index = planned_contract.start_index;
             rc == 0 && op_index < planned_contract.command_op_count;
             op_index += 1)
        {
            const termite_metal_runtime_command_op op = planned_contract.command_ops[op_index];
            termite_metal_decode_runtime_record_planned_command_op(runtime, op);
            switch (op.kind) {
                case TERMITE_METAL_PLAN_OP_DECODE_KV_SEED:
                    if (use_paged_kv && paged_suffix_tokens != 0) {
                        if (paged_format == 2u) {
                            rc = termite_metal_encode_paged_f32_kv_seed_on_encoder(
                                runtime,
                                encoder,
                                paged_slot,
                                k_buffer,
                                k_offset,
                                v_buffer,
                                v_offset,
                                kv_tokens,
                                paged_suffix_tokens,
                                num_kv_heads,
                                head_dim,
                                paged_key_row_bytes,
                                paged_v_row_stride,
                                kv_position_offset,
                                paged_block_table,
                                paged_block_count,
	                                paged_page_size,
	                                failure_code);
	                            if (rc != 0) break;
	                        } else if (paged_format == 0u || paged_format == 1u || paged_format == 3u || paged_format == 4u) {
	                            rc = termite_metal_encode_paged_quantized_kv_seed_on_encoder(
                                runtime,
                                encoder,
                                paged_slot,
                                paged_format,
                                k_buffer,
                                k_offset,
                                v_buffer,
                                v_offset,
                                kv_tokens,
                                paged_suffix_tokens,
                                num_kv_heads,
                                head_dim,
                                paged_key_row_bytes,
                                paged_base_key_row_bytes,
                                paged_v_row_stride,
                                kv_position_offset,
                                paged_block_table,
                                paged_block_count,
	                                paged_page_size,
	                                failure_code);
	                            if (rc != 0) break;
	                        } else {
	                            if (termite_metal_decode_runtime_end_planned_compute_scope(runtime) != 0) {
	                                rc = failure_code;
                                break;
                            }
                            rc = termite_metal_decode_runtime_encode_attention_paged_update_from_f32_key_device_slot(
                                runtime,
                                command_buffer,
                                paged_slot,
                                paged_format,
                                (__bridge void *)k_buffer,
                                k_offset,
                                (__bridge void *)v_buffer,
                                v_offset,
                                kv_tokens,
                                paged_suffix_tokens,
                                num_kv_heads,
                                head_dim,
                                paged_key_row_bytes,
                                paged_base_key_row_bytes,
                                paged_v_row_stride,
                                kv_position_offset,
                                paged_block_table,
                                paged_block_count,
                                paged_page_size);
                            if (rc != 0) break;
                            if (termite_metal_decode_runtime_begin_planned_compute_scope(runtime, TERMITE_METAL_COMPUTE_SOURCE_LAYER, TERMITE_METAL_COMPUTE_REGION_LAYER) != 0) {
                                rc = failure_code;
                                break;
                            }
                            encoder = runtime->active_planned_compute_encoder;
                            if (encoder == nil) {
	                                rc = failure_code;
	                                break;
	                            }
	                        }
                        paged_kv_seed_encoded = true;
                    }
                    break;
                case TERMITE_METAL_PLAN_OP_DECODE_ATTENTION:
                    if (use_paged_kv) {
                        if (op.op != TERMITE_METAL_OPERATOR_UNKNOWN &&
                            op.op != TERMITE_METAL_OPERATOR_ATTENTION_PAGED &&
                            op.op != TERMITE_METAL_OPERATOR_ATTENTION_QUANTIZED_KV)
                        {
                            rc = failure_code;
                            break;
                        }
                        if (op.format != UINT8_MAX &&
                            op.format != TERMITE_METAL_GRAPH_ATTENTION_KV_FORMAT_F32 &&
                            op.format != TERMITE_METAL_GRAPH_ATTENTION_KV_FORMAT_POLAR4 &&
                            op.format != TERMITE_METAL_GRAPH_ATTENTION_KV_FORMAT_TURBO3 &&
                            op.format != TERMITE_METAL_GRAPH_ATTENTION_KV_FORMAT_QUANTIZED)
                        {
                            rc = failure_code;
                            break;
                        }
                        if (paged_suffix_tokens != 0 && !paged_kv_seed_encoded) {
                            if (paged_format == 2u) {
                                rc = termite_metal_encode_paged_f32_kv_seed_on_encoder(
                                    runtime,
                                    encoder,
                                    paged_slot,
                                    k_buffer,
                                    k_offset,
                                    v_buffer,
                                    v_offset,
                                    kv_tokens,
                                    paged_suffix_tokens,
                                    num_kv_heads,
                                    head_dim,
                                    paged_key_row_bytes,
                                    paged_v_row_stride,
                                    kv_position_offset,
                                    paged_block_table,
                                    paged_block_count,
	                                    paged_page_size,
	                                    failure_code);
	                                if (rc != 0) break;
	                            } else if (paged_format == 0u || paged_format == 1u || paged_format == 3u || paged_format == 4u) {
	                                rc = termite_metal_encode_paged_quantized_kv_seed_on_encoder(
                                    runtime,
                                    encoder,
                                    paged_slot,
                                    paged_format,
                                    k_buffer,
                                    k_offset,
                                    v_buffer,
                                    v_offset,
                                    kv_tokens,
                                    paged_suffix_tokens,
                                    num_kv_heads,
                                    head_dim,
                                    paged_key_row_bytes,
                                    paged_base_key_row_bytes,
                                    paged_v_row_stride,
                                    kv_position_offset,
                                    paged_block_table,
                                    paged_block_count,
	                                    paged_page_size,
	                                    failure_code);
	                                if (rc != 0) break;
	                            } else {
	                                if (termite_metal_decode_runtime_end_planned_compute_scope(runtime) != 0) {
	                                    rc = failure_code;
                                    break;
                                }
                                rc = termite_metal_decode_runtime_encode_attention_paged_update_from_f32_key_device_slot(
                                    runtime,
                                    command_buffer,
                                    paged_slot,
                                    paged_format,
                                    (__bridge void *)k_buffer,
                                    k_offset,
                                    (__bridge void *)v_buffer,
                                    v_offset,
                                    kv_tokens,
                                    paged_suffix_tokens,
                                    num_kv_heads,
                                    head_dim,
                                    paged_key_row_bytes,
                                    paged_base_key_row_bytes,
                                    paged_v_row_stride,
                                    kv_position_offset,
                                    paged_block_table,
                                    paged_block_count,
                                    paged_page_size);
                                if (rc != 0) break;
                                if (termite_metal_decode_runtime_begin_planned_compute_scope(runtime, TERMITE_METAL_COMPUTE_SOURCE_LAYER, TERMITE_METAL_COMPUTE_REGION_LAYER) != 0) {
                                    rc = failure_code;
                                    break;
                                }
                                encoder = runtime->active_planned_compute_encoder;
                                if (encoder == nil) {
	                                    rc = failure_code;
	                                    break;
	                                }
	                            }
                            paged_kv_seed_encoded = true;
                        }
                        rc = termite_metal_encode_paged_attention_slot_on_encoder(
                            runtime,
                            encoder,
                            paged_slot,
                            paged_format,
                            q_buffer,
                            q_offset,
                            paged_block_table,
                            paged_block_count,
                            paged_page_size,
                            1,
                            kv_tokens,
                            num_heads,
                            num_kv_heads,
                            head_dim,
                            paged_key_row_bytes,
                            paged_base_key_row_bytes,
                            query_position_offset,
                            kv_position_offset,
                            sliding_window,
                            0.0f,
                            NULL,
                            0,
                            attention_output_buffer,
                            0,
                            failure_code);
                    } else {
                        if (op.op != TERMITE_METAL_OPERATOR_UNKNOWN &&
                            op.op != TERMITE_METAL_OPERATOR_ATTENTION_FLASH)
                        {
                            rc = failure_code;
                            break;
                        }
                        if (!termite_metal_runtime_command_format_is_f32_attention(op.format)) {
                            rc = failure_code;
                            break;
                        }
                        rc = termite_metal_encode_attention_f32_on_encoder(
                            runtime,
                            encoder,
                            q_buffer,
                            q_offset,
                            k_buffer,
                            k_offset,
                            v_buffer,
                            v_offset,
                            attention_output_buffer,
                            0,
                            1,
                            kv_tokens,
                            num_heads,
                            num_kv_heads,
                            head_dim,
                            query_position_offset,
                            kv_position_offset,
                            sliding_window,
                            total_sequence_len,
                            failure_code);
                    }
                    break;
                case TERMITE_METAL_PLAN_OP_DECODE_ATTENTION_OUTPUT_LINEAR: {
                    if (!termite_metal_runtime_operator_is_quant_matmul(op.op)) {
                        rc = failure_code;
                        break;
                    }
                    if (!termite_metal_runtime_command_format_matches_quant(op.format, TERMITE_METAL_QUANT_FORMAT_Q8_0)) {
                        rc = failure_code;
                        break;
                    }
                    termite_metal_quant_matmul_descriptor descriptor = {
                        .slot = attention_linear_slot,
                        .format = TERMITE_METAL_QUANT_FORMAT_Q8_0,
                        .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
                        .input_dtype = termite_metal_quant_matmul_dtype_from_command(op.input_dtype),
                        .output_dtype = termite_metal_quant_matmul_dtype_from_command(op.output_dtype),
                        .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_NONE,
                        .fallback_pipeline = runtime->q8_0_pipeline,
                        .input_buffer = attention_output_buffer,
                        .weight_buffer = attention_view.weight_buffer,
                        .output_buffer = attention_projected_buffer,
                        .rows = 1,
                        .in_dim = attention_input_size,
                        .out_dim = hidden_size,
                        .values_per_block = 32u,
                        .bytes_per_block = 34u,
                        .q8_family = TERMITE_METAL_Q8_0_LINEAR_FAMILY_ATTENTION_OUTPUT,
                        .use_planned_dispatch = true,
                        .planned_dispatch = op.quant_dispatch,
                        .failure_code = failure_code,
                    };
                    rc = termite_metal_encode_quant_matmul_descriptor_on_encoder(runtime, encoder, &descriptor);
                    break;
                }
                case TERMITE_METAL_PLAN_OP_DECODE_ATTENTION_POST_NORM_RESIDUAL:
                    rc = termite_metal_encode_rms_norm_add_on_encoder(
                        runtime,
                        encoder,
                        attention_projected_buffer,
                        0,
                        attention_post_linear_rms_norm_slot,
                        residual_buffer,
                        residual_offset,
                        attn_added_buffer,
                        0,
                        hidden_size,
                        eps,
                        failure_code);
                    break;
                case TERMITE_METAL_PLAN_OP_DECODE_FFN_PRE_NORM_SCALE:
                    rc = termite_metal_encode_rms_inv_scale_1x_on_encoder(
                        runtime,
                        encoder,
                        attn_added_buffer,
                        0,
                        inv_scale_buffer,
                        0,
                        hidden_size,
                        eps,
                        failure_code);
                    break;
                case TERMITE_METAL_PLAN_OP_DECODE_FFN_GATE_UP_ACTIVATION: {
                    if (!termite_metal_runtime_operator_is_quant_matmul(op.op)) {
                        rc = failure_code;
                        break;
                    }
                    if (!termite_metal_runtime_command_format_matches_quant(op.format, TERMITE_METAL_QUANT_FORMAT_Q8_0)) {
                        rc = failure_code;
                        break;
                    }
                    termite_metal_quant_matmul_descriptor descriptor = {
                        .slot = gate_ffn_linear_slot,
                        .format = TERMITE_METAL_QUANT_FORMAT_Q8_0,
                        .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
                        .input_dtype = termite_metal_quant_matmul_dtype_from_command(op.input_dtype),
                        .output_dtype = termite_metal_quant_matmul_dtype_from_command(op.output_dtype),
                        .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_PAIR_ACTIVATION_RMS_SCALE_1X,
                        .fallback_pipeline = runtime->q8_0_pair_activation_rms_scale_mmv_pipeline,
                        .input_buffer = attn_added_buffer,
                        .weight_buffer = gate_view.weight_buffer,
                        .second_weight_buffer = up_view.weight_buffer,
                        .output_buffer = ffn_gated_buffer,
                        .inv_scale_buffer = inv_scale_buffer,
                        .norm_slot = ffn_pre_gate_rms_norm_slot,
                        .rows = 1,
                        .in_dim = hidden_size,
                        .out_dim = intermediate_size,
                        .values_per_block = 32u,
                        .bytes_per_block = 34u,
                        .activation_kind = activation_kind,
                        .use_planned_dispatch = true,
                        .planned_dispatch = op.quant_dispatch,
                        .failure_code = failure_code,
                    };
                    rc = termite_metal_encode_quant_matmul_descriptor_on_encoder(runtime, encoder, &descriptor);
                    break;
                }
                case TERMITE_METAL_PLAN_OP_DECODE_FFN_DOWN_LINEAR: {
                    if (!termite_metal_runtime_operator_is_quant_matmul(op.op)) {
                        rc = failure_code;
                        break;
                    }
                    if (!termite_metal_runtime_command_format_matches_quant(op.format, TERMITE_METAL_QUANT_FORMAT_Q8_0)) {
                        rc = failure_code;
                        break;
                    }
                    termite_metal_quant_matmul_descriptor descriptor = {
                        .slot = down_ffn_linear_slot,
                        .format = TERMITE_METAL_QUANT_FORMAT_Q8_0,
                        .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
                        .input_dtype = termite_metal_quant_matmul_dtype_from_command(op.input_dtype),
                        .output_dtype = termite_metal_quant_matmul_dtype_from_command(op.output_dtype),
                        .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_NONE,
                        .fallback_pipeline = runtime->q8_0_pipeline,
                        .input_buffer = ffn_gated_buffer,
                        .weight_buffer = down_view.weight_buffer,
                        .output_buffer = ffn_projected_buffer,
                        .rows = 1,
                        .in_dim = intermediate_size,
                        .out_dim = hidden_size,
                        .values_per_block = 32u,
                        .bytes_per_block = 34u,
                        .q8_family = TERMITE_METAL_Q8_0_LINEAR_FAMILY_FFN_DOWN,
                        .use_planned_dispatch = true,
                        .planned_dispatch = op.quant_dispatch,
                        .failure_code = failure_code,
                    };
                    rc = termite_metal_encode_quant_matmul_descriptor_on_encoder(runtime, encoder, &descriptor);
                    break;
                }
                case TERMITE_METAL_PLAN_OP_DECODE_FFN_POST_NORM_RESIDUAL:
                    rc = termite_metal_encode_rms_norm_add_on_encoder(
                        runtime,
                        encoder,
                        ffn_projected_buffer,
                        0,
                        ffn_post_down_rms_norm_slot,
                        attn_added_buffer,
                        0,
                        ffn_output_buffer,
                        ffn_output_offset,
                        hidden_size,
                        eps,
                        failure_code);
                    break;
                case TERMITE_METAL_PLAN_OP_DECODE_PLE_GATE_ACTIVATION: {
                    if (!termite_metal_runtime_operator_is_quant_matmul(op.op)) {
                        rc = failure_code;
                        break;
                    }
                    if (!termite_metal_runtime_command_format_matches_quant(op.format, TERMITE_METAL_QUANT_FORMAT_Q8_0)) {
                        rc = failure_code;
                        break;
                    }
                    termite_metal_quant_matmul_descriptor descriptor = {
                        .slot = ple_gate_linear_slot,
                        .format = TERMITE_METAL_QUANT_FORMAT_Q8_0,
                        .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
                        .input_dtype = termite_metal_quant_matmul_dtype_from_command(op.input_dtype),
                        .output_dtype = termite_metal_quant_matmul_dtype_from_command(op.output_dtype),
                        .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_ACTIVATION_RHS_MUL,
                        .fallback_pipeline = runtime->q8_0_activation_multiply_reduce_pipeline,
                        .input_buffer = ffn_output_buffer,
                        .input_offset = ffn_output_offset,
                        .weight_buffer = ple_gate_view.weight_buffer,
                        .rhs_buffer = ple_buffer,
                        .rhs_offset = ple_offset,
                        .output_buffer = ple_gated_buffer,
                        .rows = 1,
                        .in_dim = hidden_size,
                        .out_dim = ple_hidden_size,
                        .values_per_block = 32u,
                        .bytes_per_block = 34u,
                        .activation_kind = activation_kind,
                        .use_planned_dispatch = true,
                        .planned_dispatch = op.quant_dispatch,
                        .failure_code = failure_code,
                    };
                    rc = termite_metal_encode_quant_matmul_descriptor_on_encoder(runtime, encoder, &descriptor);
                    break;
                }
                case TERMITE_METAL_PLAN_OP_DECODE_PLE_PROJECTION: {
                    if (!termite_metal_runtime_operator_is_quant_matmul(op.op)) {
                        rc = failure_code;
                        break;
                    }
                    if (!termite_metal_runtime_command_format_matches_quant(op.format, TERMITE_METAL_QUANT_FORMAT_Q8_0)) {
                        rc = failure_code;
                        break;
                    }
                    termite_metal_quant_matmul_descriptor descriptor = {
                        .slot = ple_proj_linear_slot,
                        .format = TERMITE_METAL_QUANT_FORMAT_Q8_0,
                        .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
                        .input_dtype = termite_metal_quant_matmul_dtype_from_command(op.input_dtype),
                        .output_dtype = termite_metal_quant_matmul_dtype_from_command(op.output_dtype),
                        .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_NONE,
                        .fallback_pipeline = runtime->q8_0_pipeline,
                        .input_buffer = ple_gated_buffer,
                        .weight_buffer = ple_proj_view.weight_buffer,
                        .output_buffer = ple_projected_buffer,
                        .rows = 1,
                        .in_dim = ple_hidden_size,
                        .out_dim = hidden_size,
                        .values_per_block = 32u,
                        .bytes_per_block = 34u,
                        .q8_family = TERMITE_METAL_Q8_0_LINEAR_FAMILY_PLE_PROJECTION,
                        .use_planned_dispatch = true,
                        .planned_dispatch = op.quant_dispatch,
                        .failure_code = failure_code,
                    };
                    rc = termite_metal_encode_quant_matmul_descriptor_on_encoder(runtime, encoder, &descriptor);
                    break;
                }
                case TERMITE_METAL_PLAN_OP_DECODE_PLE_POST_NORM_RESIDUAL:
                    rc = output_scale_present != 0
                        ? termite_metal_encode_rms_norm_add_scale_on_encoder(
                            runtime,
                            encoder,
                            ple_projected_buffer,
                            0,
                            ple_post_norm_slot,
                            ffn_output_buffer,
                            ffn_output_offset,
                            output_buffer,
                            output_offset,
                            hidden_size,
                            eps,
                            output_scale,
                            failure_code)
                        : termite_metal_encode_rms_norm_add_on_encoder(
                            runtime,
                            encoder,
                            ple_projected_buffer,
                            0,
                            ple_post_norm_slot,
                            ffn_output_buffer,
                            ffn_output_offset,
                            output_buffer,
                            output_offset,
                            hidden_size,
                            eps,
                            failure_code);
                    break;
                default:
                    rc = failure_code;
                    break;
            }
            if (trace_q80_block && rc != 0) {
                fprintf(
                    stderr,
                    "metal-planned-q80-1x-op-fail index=%zu kind=%u op=%u format=%u dispatch=%u rc=%d paged=%d suffix=%zu\n",
                    op_index,
                    (unsigned)op.kind,
                    (unsigned)op.op,
                    (unsigned)op.format,
                    (unsigned)op.quant_dispatch,
                    rc,
                    use_paged_kv ? 1 : 0,
                    paged_suffix_tokens
                );
            }
        }
	        const int end_rc = termite_metal_decode_runtime_end_planned_compute_scope(runtime);
        if (trace_q80_block && rc == 0 && end_rc != 0) {
            fprintf(stderr, "metal-planned-q80-1x-fail reason=end-scope rc=%d\n", end_rc);
        }
        if (rc != 0) return rc;
#undef TERMITE_Q80_PLANNED_FAIL
        return end_rc == 0 ? 0 : failure_code;
    }
    termite_metal_planned_layer_cursor planned_cursor = termite_metal_planned_layer_cursor_make(runtime, planned_contract, failure_code);
    int rc = 0;
    rc = termite_metal_planned_layer_cursor_before_step(&planned_cursor, TERMITE_METAL_Q80_F32_KV_LAYER_STEP_KINDS, TERMITE_METAL_Q80_F32_KV_LAYER_STEP_COUNT, TERMITE_METAL_Q80_F32_KV_LAYER_STEP_ATTENTION);
	    if (rc == 0 && use_paged_kv) {
	        if (paged_suffix_tokens != 0) {
	            if (rc == 0 && (paged_format == 0u || paged_format == 1u || paged_format == 3u || paged_format == 4u)) {
                rc = termite_metal_encode_paged_quantized_kv_seed_on_encoder(
                    runtime,
                    encoder,
                    paged_slot,
                    paged_format,
                    k_buffer,
                    k_offset,
                    v_buffer,
                    v_offset,
                    kv_tokens,
                    paged_suffix_tokens,
                    num_kv_heads,
                    head_dim,
                    paged_key_row_bytes,
                    paged_base_key_row_bytes,
                    paged_v_row_stride,
                    kv_position_offset,
                    paged_block_table,
                    paged_block_count,
                    paged_page_size,
	                    failure_code);
	            } else {
                if (termite_metal_decode_runtime_end_planned_compute_scope(runtime) != 0) {
                    rc = failure_code;
                }
                if (rc == 0) {
                    rc = termite_metal_decode_runtime_encode_attention_paged_update_from_f32_key_device_slot(
                        runtime,
                        command_buffer,
                        paged_slot,
                        paged_format,
                        (__bridge void *)k_buffer,
                        k_offset,
                        (__bridge void *)v_buffer,
                        v_offset,
                        kv_tokens,
                        paged_suffix_tokens,
                        num_kv_heads,
                        head_dim,
                        paged_key_row_bytes,
                        paged_base_key_row_bytes,
                        paged_v_row_stride,
                        kv_position_offset,
                        paged_block_table,
                        paged_block_count,
                        paged_page_size);
                }
                if (rc == 0 && termite_metal_decode_runtime_begin_planned_compute_scope(runtime, TERMITE_METAL_COMPUTE_SOURCE_LAYER, TERMITE_METAL_COMPUTE_REGION_LAYER) != 0) rc = failure_code;
	                encoder = runtime->active_planned_compute_encoder;
	                if (rc == 0 && encoder == nil) rc = failure_code;
	            }
	        }
        if (rc == 0) rc = termite_metal_encode_paged_attention_slot_on_encoder(
            runtime,
            encoder,
            paged_slot,
            paged_format,
            q_buffer,
            q_offset,
            paged_block_table,
            paged_block_count,
            paged_page_size,
            1,
            kv_tokens,
            num_heads,
            num_kv_heads,
            head_dim,
            paged_key_row_bytes,
            paged_base_key_row_bytes,
            query_position_offset,
            kv_position_offset,
            sliding_window,
            0.0f,
            NULL,
            0,
            attention_output_buffer,
            0,
            failure_code
        );
    } else if (rc == 0) rc = termite_metal_encode_attention_f32_on_encoder(
        runtime,
        encoder,
        q_buffer,
        q_offset,
        k_buffer,
        k_offset,
        v_buffer,
        v_offset,
        attention_output_buffer,
        0,
        1,
        kv_tokens,
        num_heads,
        num_kv_heads,
        head_dim,
        query_position_offset,
        kv_position_offset,
        sliding_window,
        total_sequence_len,
        failure_code
    );
    if (rc == 0) rc = termite_metal_planned_layer_cursor_fallback_barrier_after_op(&planned_cursor);
    if (rc == 0) {
        rc = termite_metal_planned_layer_cursor_before_step(&planned_cursor, TERMITE_METAL_Q80_F32_KV_LAYER_STEP_KINDS, TERMITE_METAL_Q80_F32_KV_LAYER_STEP_COUNT, TERMITE_METAL_Q80_F32_KV_LAYER_STEP_ATTENTION_OUTPUT_LINEAR);
    }
    if (rc == 0) {
        termite_metal_quant_matmul_descriptor descriptor = {
            .slot = attention_linear_slot,
            .format = TERMITE_METAL_QUANT_FORMAT_Q8_0,
            .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
            .input_dtype = termite_metal_quant_matmul_dtype_from_command(termite_metal_planned_layer_cursor_input_dtype(&planned_cursor)),
            .output_dtype = termite_metal_quant_matmul_dtype_from_command(termite_metal_planned_layer_cursor_output_dtype(&planned_cursor)),
            .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_NONE,
            .fallback_pipeline = runtime->q8_0_pipeline,
            .input_buffer = attention_output_buffer,
            .weight_buffer = attention_view.weight_buffer,
            .output_buffer = attention_projected_buffer,
            .rows = 1,
            .in_dim = attention_input_size,
            .out_dim = hidden_size,
            .values_per_block = 32u,
            .bytes_per_block = 34u,
            .q8_family = TERMITE_METAL_Q8_0_LINEAR_FAMILY_ATTENTION_OUTPUT,
            .use_planned_dispatch = true,
            .planned_dispatch = termite_metal_planned_layer_cursor_quant_dispatch(&planned_cursor),
            .failure_code = failure_code,
        };
        rc = termite_metal_encode_quant_matmul_descriptor_on_encoder(runtime, encoder, &descriptor);
    }
    if (rc == 0) rc = termite_metal_planned_layer_cursor_fallback_barrier_after_op(&planned_cursor);
    if (rc == 0) {
        rc = termite_metal_planned_layer_cursor_before_step(&planned_cursor, TERMITE_METAL_Q80_F32_KV_LAYER_STEP_KINDS, TERMITE_METAL_Q80_F32_KV_LAYER_STEP_COUNT, TERMITE_METAL_Q80_F32_KV_LAYER_STEP_ATTENTION_POST_NORM_RESIDUAL);
    }
    if (rc == 0) {
        rc = termite_metal_encode_rms_norm_add_on_encoder(
            runtime,
            encoder,
            attention_projected_buffer,
            0,
            attention_post_linear_rms_norm_slot,
            residual_buffer,
            residual_offset,
            attn_added_buffer,
            0,
            hidden_size,
            eps,
            failure_code
        );
    }
    if (rc == 0) rc = termite_metal_planned_layer_cursor_fallback_barrier_after_op(&planned_cursor);
    if (rc == 0) {
        rc = termite_metal_planned_layer_cursor_before_step(&planned_cursor, TERMITE_METAL_Q80_F32_KV_LAYER_STEP_KINDS, TERMITE_METAL_Q80_F32_KV_LAYER_STEP_COUNT, TERMITE_METAL_Q80_F32_KV_LAYER_STEP_FFN_PRE_NORM_SCALE);
    }
    if (rc == 0) {
        rc = termite_metal_encode_rms_inv_scale_1x_on_encoder(
            runtime,
            encoder,
            attn_added_buffer,
            0,
            inv_scale_buffer,
            0,
            hidden_size,
            eps,
            failure_code
        );
    }
    if (rc == 0) rc = termite_metal_planned_layer_cursor_fallback_barrier_after_op(&planned_cursor);
    if (rc == 0) {
        rc = termite_metal_planned_layer_cursor_before_step(&planned_cursor, TERMITE_METAL_Q80_F32_KV_LAYER_STEP_KINDS, TERMITE_METAL_Q80_F32_KV_LAYER_STEP_COUNT, TERMITE_METAL_Q80_F32_KV_LAYER_STEP_FFN_GATE_UP_ACTIVATION);
    }
    if (rc == 0) {
        termite_metal_quant_matmul_descriptor descriptor = {
            .slot = gate_ffn_linear_slot,
            .format = TERMITE_METAL_QUANT_FORMAT_Q8_0,
            .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
            .input_dtype = termite_metal_quant_matmul_dtype_from_command(termite_metal_planned_layer_cursor_input_dtype(&planned_cursor)),
            .output_dtype = termite_metal_quant_matmul_dtype_from_command(termite_metal_planned_layer_cursor_output_dtype(&planned_cursor)),
            .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_PAIR_ACTIVATION_RMS_SCALE_1X,
            .fallback_pipeline = runtime->q8_0_pair_activation_rms_scale_mmv_pipeline,
            .input_buffer = attn_added_buffer,
            .weight_buffer = gate_view.weight_buffer,
            .second_weight_buffer = up_view.weight_buffer,
            .output_buffer = ffn_gated_buffer,
            .inv_scale_buffer = inv_scale_buffer,
            .norm_slot = ffn_pre_gate_rms_norm_slot,
            .rows = 1,
            .in_dim = hidden_size,
            .out_dim = intermediate_size,
            .values_per_block = 32u,
            .bytes_per_block = 34u,
            .activation_kind = activation_kind,
            .use_planned_dispatch = true,
            .planned_dispatch = termite_metal_planned_layer_cursor_quant_dispatch(&planned_cursor),
            .failure_code = failure_code,
        };
        rc = termite_metal_encode_quant_matmul_descriptor_on_encoder(runtime, encoder, &descriptor);
    }
    if (rc == 0) rc = termite_metal_planned_layer_cursor_fallback_barrier_after_op(&planned_cursor);
    if (rc == 0) {
        rc = termite_metal_planned_layer_cursor_before_step(&planned_cursor, TERMITE_METAL_Q80_F32_KV_LAYER_STEP_KINDS, TERMITE_METAL_Q80_F32_KV_LAYER_STEP_COUNT, TERMITE_METAL_Q80_F32_KV_LAYER_STEP_FFN_DOWN_LINEAR);
    }
    if (rc == 0) {
        termite_metal_quant_matmul_descriptor descriptor = {
            .slot = down_ffn_linear_slot,
            .format = TERMITE_METAL_QUANT_FORMAT_Q8_0,
            .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
            .input_dtype = termite_metal_quant_matmul_dtype_from_command(termite_metal_planned_layer_cursor_input_dtype(&planned_cursor)),
            .output_dtype = termite_metal_quant_matmul_dtype_from_command(termite_metal_planned_layer_cursor_output_dtype(&planned_cursor)),
            .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_NONE,
            .fallback_pipeline = runtime->q8_0_pipeline,
            .input_buffer = ffn_gated_buffer,
            .weight_buffer = down_view.weight_buffer,
            .output_buffer = ffn_projected_buffer,
            .rows = 1,
            .in_dim = intermediate_size,
            .out_dim = hidden_size,
            .values_per_block = 32u,
            .bytes_per_block = 34u,
            .q8_family = TERMITE_METAL_Q8_0_LINEAR_FAMILY_FFN_DOWN,
            .use_planned_dispatch = true,
            .planned_dispatch = termite_metal_planned_layer_cursor_quant_dispatch(&planned_cursor),
            .failure_code = failure_code,
        };
        rc = termite_metal_encode_quant_matmul_descriptor_on_encoder(runtime, encoder, &descriptor);
    }
    if (rc == 0) rc = termite_metal_planned_layer_cursor_fallback_barrier_after_op(&planned_cursor);
    if (rc == 0) {
        rc = termite_metal_planned_layer_cursor_before_step(&planned_cursor, TERMITE_METAL_Q80_F32_KV_LAYER_STEP_KINDS, TERMITE_METAL_Q80_F32_KV_LAYER_STEP_COUNT, TERMITE_METAL_Q80_F32_KV_LAYER_STEP_FFN_POST_NORM_RESIDUAL);
    }
    if (rc == 0) {
        rc = termite_metal_encode_rms_norm_add_on_encoder(
            runtime,
            encoder,
            ffn_projected_buffer,
            0,
            ffn_post_down_rms_norm_slot,
            attn_added_buffer,
            0,
            ffn_output_buffer,
            ffn_output_offset,
            hidden_size,
            eps,
            failure_code
        );
    }
    if (rc == 0) rc = termite_metal_planned_layer_cursor_fallback_barrier_after_op(&planned_cursor);
    if (rc == 0) {
        rc = termite_metal_planned_layer_cursor_before_step(&planned_cursor, TERMITE_METAL_Q80_F32_KV_LAYER_STEP_KINDS, TERMITE_METAL_Q80_F32_KV_LAYER_STEP_COUNT, TERMITE_METAL_Q80_F32_KV_LAYER_STEP_PLE_GATE_ACTIVATION);
    }
    if (rc == 0) {
        termite_metal_quant_matmul_descriptor descriptor = {
            .slot = ple_gate_linear_slot,
            .format = TERMITE_METAL_QUANT_FORMAT_Q8_0,
            .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
            .input_dtype = termite_metal_quant_matmul_dtype_from_command(termite_metal_planned_layer_cursor_input_dtype(&planned_cursor)),
            .output_dtype = termite_metal_quant_matmul_dtype_from_command(termite_metal_planned_layer_cursor_output_dtype(&planned_cursor)),
            .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_ACTIVATION_RHS_MUL,
            .fallback_pipeline = runtime->q8_0_activation_multiply_reduce_pipeline,
            .input_buffer = ffn_output_buffer,
            .input_offset = ffn_output_offset,
            .weight_buffer = ple_gate_view.weight_buffer,
            .rhs_buffer = ple_buffer,
            .rhs_offset = ple_offset,
            .output_buffer = ple_gated_buffer,
            .rows = 1,
            .in_dim = hidden_size,
            .out_dim = ple_hidden_size,
            .values_per_block = 32u,
            .bytes_per_block = 34u,
            .activation_kind = activation_kind,
            .use_planned_dispatch = true,
            .planned_dispatch = termite_metal_planned_layer_cursor_quant_dispatch(&planned_cursor),
            .failure_code = failure_code,
        };
        rc = termite_metal_encode_quant_matmul_descriptor_on_encoder(runtime, encoder, &descriptor);
    }
    if (rc == 0) rc = termite_metal_planned_layer_cursor_fallback_barrier_after_op(&planned_cursor);
    if (rc == 0) {
        rc = termite_metal_planned_layer_cursor_before_step(&planned_cursor, TERMITE_METAL_Q80_F32_KV_LAYER_STEP_KINDS, TERMITE_METAL_Q80_F32_KV_LAYER_STEP_COUNT, TERMITE_METAL_Q80_F32_KV_LAYER_STEP_PLE_PROJECTION);
    }
    if (rc == 0) {
        termite_metal_quant_matmul_descriptor descriptor = {
            .slot = ple_proj_linear_slot,
            .format = TERMITE_METAL_QUANT_FORMAT_Q8_0,
            .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
            .input_dtype = termite_metal_quant_matmul_dtype_from_command(termite_metal_planned_layer_cursor_input_dtype(&planned_cursor)),
            .output_dtype = termite_metal_quant_matmul_dtype_from_command(termite_metal_planned_layer_cursor_output_dtype(&planned_cursor)),
            .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_NONE,
            .fallback_pipeline = runtime->q8_0_pipeline,
            .input_buffer = ple_gated_buffer,
            .weight_buffer = ple_proj_view.weight_buffer,
            .output_buffer = ple_projected_buffer,
            .rows = 1,
            .in_dim = ple_hidden_size,
            .out_dim = hidden_size,
            .values_per_block = 32u,
            .bytes_per_block = 34u,
            .q8_family = TERMITE_METAL_Q8_0_LINEAR_FAMILY_PLE_PROJECTION,
            .use_planned_dispatch = true,
            .planned_dispatch = termite_metal_planned_layer_cursor_quant_dispatch(&planned_cursor),
            .failure_code = failure_code,
        };
        rc = termite_metal_encode_quant_matmul_descriptor_on_encoder(runtime, encoder, &descriptor);
    }
    if (rc == 0) rc = termite_metal_planned_layer_cursor_fallback_barrier_after_op(&planned_cursor);
    if (rc == 0) {
        rc = termite_metal_planned_layer_cursor_before_step(&planned_cursor, TERMITE_METAL_Q80_F32_KV_LAYER_STEP_KINDS, TERMITE_METAL_Q80_F32_KV_LAYER_STEP_COUNT, TERMITE_METAL_Q80_F32_KV_LAYER_STEP_PLE_POST_NORM_RESIDUAL);
    }
    if (rc == 0) {
        rc = output_scale_present != 0
            ? termite_metal_encode_rms_norm_add_scale_on_encoder(
                runtime,
                encoder,
                ple_projected_buffer,
                0,
                ple_post_norm_slot,
                ffn_output_buffer,
                ffn_output_offset,
                output_buffer,
                output_offset,
                hidden_size,
                eps,
                output_scale,
                failure_code)
            : termite_metal_encode_rms_norm_add_on_encoder(
                runtime,
                encoder,
                ple_projected_buffer,
                0,
                ple_post_norm_slot,
                ffn_output_buffer,
                ffn_output_offset,
                output_buffer,
                output_offset,
                hidden_size,
                eps,
                failure_code);
    }
    const int end_rc = termite_metal_decode_runtime_end_planned_compute_scope(runtime);
    if (rc != 0) return rc;
    return end_rc == 0 ? 0 : failure_code;
}

static int termite_metal_encode_rms_norm_add_scale_for(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    id<MTLBuffer> input_buffer,
    size_t input_offset,
    size_t norm_slot,
    id<MTLBuffer> residual_buffer,
    size_t residual_offset,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    size_t hidden_size,
    float eps,
    float scale,
    size_t source,
    int failure_code
) {
    if (runtime == NULL || command_buffer == nil || input_buffer == nil || residual_buffer == nil || output_buffer == nil) return failure_code;
    if (runtime->rms_norm_add_scale_pipeline == nil) return failure_code;
    if (norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return failure_code;
    if (runtime->rms_norm_slot_prepared[norm_slot] == 0 || runtime->rms_norm_hidden_sizes[norm_slot] != hidden_size) return failure_code;
    if (hidden_size == 0 || hidden_size > UINT32_MAX) return failure_code;
    size_t hidden_bytes = 0;
    if (!termite_metal_size_mul(hidden_size, sizeof(float), &hidden_bytes)) return failure_code;
    termite_metal_planned_encoder_range accesses[4];
    if (termite_metal_planned_range_make(input_buffer, input_offset, hidden_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[0], failure_code) != 0 ||
        termite_metal_planned_range_make(runtime->rms_norm_weight_buffers[norm_slot], 0, runtime->rms_norm_weight_buffers[norm_slot].length, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[1], failure_code) != 0 ||
        termite_metal_planned_range_make(residual_buffer, residual_offset, hidden_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[2], failure_code) != 0 ||
        termite_metal_planned_range_make(output_buffer, output_offset, hidden_bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &accesses[3], failure_code) != 0 ||
        termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, accesses, 4, failure_code) != 0)
    {
        return failure_code;
    }
    termite_metal_apply_layer_norm_scale_params params = {
        .hidden_size = (uint32_t)hidden_size,
        .eps = eps,
        .scale = scale,
        .reserved = 0,
    };
    id<MTLBuffer> params_buffer = [runtime->device newBufferWithBytes:&params length:sizeof(params) options:MTLResourceStorageModeShared];
    if (params_buffer == nil) return failure_code;
    if (runtime->active_frame_cb == command_buffer && termite_metal_decode_runtime_retain_frame_resource(runtime, params_buffer) != 0) return failure_code;
    id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
    const BOOL planned_encoder = (encoder != nil);
    if (!planned_encoder) {
        encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, source);
        if (encoder == nil) return failure_code;
    }
    [encoder setComputePipelineState:runtime->rms_norm_add_scale_pipeline];
    [encoder setBuffer:input_buffer offset:input_offset atIndex:0];
    [encoder setBuffer:runtime->rms_norm_weight_buffers[norm_slot] offset:0 atIndex:1];
    [encoder setBuffer:residual_buffer offset:residual_offset atIndex:2];
    [encoder setBuffer:output_buffer offset:output_offset atIndex:3];
    [encoder setBuffer:params_buffer offset:0 atIndex:4];
    [encoder setThreadgroupMemoryLength:256u * sizeof(float) atIndex:0];
    [encoder dispatchThreadgroups:MTLSizeMake(1, 1, 1) threadsPerThreadgroup:MTLSizeMake(256, 1, 1)];
    if (!planned_encoder) [encoder endEncoding];
    return 0;
}

static int termite_metal_encode_rms_norm_add_scale_rows_for(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    id<MTLBuffer> input_buffer,
    size_t input_offset,
    size_t norm_slot,
    id<MTLBuffer> residual_buffer,
    size_t residual_offset,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    size_t rows,
    size_t hidden_size,
    float eps,
    float scale,
    size_t source,
    int failure_code
) {
    if (runtime == NULL || command_buffer == nil || input_buffer == nil || residual_buffer == nil || output_buffer == nil) return failure_code;
    if (runtime->rms_norm_add_scale_rows_pipeline == nil) return failure_code;
    if (norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return failure_code;
    if (runtime->rms_norm_slot_prepared[norm_slot] == 0 || runtime->rms_norm_hidden_sizes[norm_slot] != hidden_size) return failure_code;
    if (rows == 0 || hidden_size == 0 || rows > UINT32_MAX || hidden_size > UINT32_MAX) return failure_code;
    size_t elems = 0;
    size_t bytes = 0;
    if (!termite_metal_size_mul(rows, hidden_size, &elems) ||
        !termite_metal_size_mul(elems, sizeof(float), &bytes))
    {
        return failure_code;
    }
    termite_metal_planned_encoder_range accesses[4];
    if (termite_metal_planned_range_make(input_buffer, input_offset, bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[0], failure_code) != 0 ||
        termite_metal_planned_range_make(runtime->rms_norm_weight_buffers[norm_slot], 0, runtime->rms_norm_weight_buffers[norm_slot].length, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[1], failure_code) != 0 ||
        termite_metal_planned_range_make(residual_buffer, residual_offset, bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[2], failure_code) != 0 ||
        termite_metal_planned_range_make(output_buffer, output_offset, bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &accesses[3], failure_code) != 0 ||
        termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, accesses, 4, failure_code) != 0)
    {
        return failure_code;
    }
    termite_metal_apply_row_norm_scale_params params = {
        .rows = (uint32_t)rows,
        .hidden_size = (uint32_t)hidden_size,
        .eps = eps,
        .scale = scale,
    };
    id<MTLBuffer> params_buffer = [runtime->device newBufferWithBytes:&params length:sizeof(params) options:MTLResourceStorageModeShared];
    if (params_buffer == nil) return failure_code;
    if (runtime->active_frame_cb == command_buffer && termite_metal_decode_runtime_retain_frame_resource(runtime, params_buffer) != 0) return failure_code;
    id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
    const BOOL planned_encoder = (encoder != nil);
    if (!planned_encoder) {
        encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, source);
        if (encoder == nil) return failure_code;
    }
    [encoder setComputePipelineState:runtime->rms_norm_add_scale_rows_pipeline];
    [encoder setBuffer:input_buffer offset:input_offset atIndex:0];
    [encoder setBuffer:runtime->rms_norm_weight_buffers[norm_slot] offset:0 atIndex:1];
    [encoder setBuffer:residual_buffer offset:residual_offset atIndex:2];
    [encoder setBuffer:output_buffer offset:output_offset atIndex:3];
    [encoder setBuffer:params_buffer offset:0 atIndex:4];
    [encoder setThreadgroupMemoryLength:256u * sizeof(float) atIndex:0];
    [encoder dispatchThreadgroups:MTLSizeMake(rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(256, 1, 1)];
    if (!planned_encoder) [encoder endEncoding];
    return 0;
}

static int termite_metal_encode_rms_norm_add_rows_for(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    id<MTLBuffer> input_buffer,
    size_t input_offset,
    size_t norm_slot,
    id<MTLBuffer> residual_buffer,
    size_t residual_offset,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    size_t rows,
    size_t hidden_size,
    float eps,
    size_t source,
    int failure_code
) {
    return termite_metal_encode_rms_norm_add_scale_rows_for(
        runtime,
        command_buffer,
        input_buffer,
        input_offset,
        norm_slot,
        residual_buffer,
        residual_offset,
        output_buffer,
        output_offset,
        rows,
        hidden_size,
        eps,
        1.0f,
        source,
        failure_code
    );
}

static int termite_metal_encode_rms_norm_rows(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    id<MTLBuffer> input_buffer,
    size_t input_offset,
    size_t norm_slot,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    size_t rows,
    size_t hidden_size,
    float eps,
    int failure_code
) {
    if (runtime == NULL || command_buffer == nil || input_buffer == nil || output_buffer == nil) return failure_code;
    if (runtime->rms_norm_rows_pipeline == nil) return failure_code;
    if (norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return failure_code;
    if (runtime->rms_norm_slot_prepared[norm_slot] == 0 || runtime->rms_norm_hidden_sizes[norm_slot] != hidden_size) return failure_code;
    if (rows == 0 || hidden_size == 0 || rows > UINT32_MAX || hidden_size > UINT32_MAX) return failure_code;
    size_t elems = 0;
    size_t bytes = 0;
    if (!termite_metal_size_mul(rows, hidden_size, &elems) ||
        !termite_metal_size_mul(elems, sizeof(float), &bytes))
    {
        return failure_code;
    }
    if (termite_metal_decode_runtime_prepare_planned_compute_norm_accesses(
            runtime,
            input_buffer,
            input_offset,
            bytes,
            runtime->rms_norm_weight_buffers[norm_slot],
            output_buffer,
            output_offset,
            bytes,
            failure_code) != 0)
    {
        return failure_code;
    }
    termite_metal_apply_row_norm_params params = {
        .rows = (uint32_t)rows,
        .hidden_size = (uint32_t)hidden_size,
        .eps = eps,
    };
    id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
    const BOOL planned_encoder = (encoder != nil);
    if (!planned_encoder) {
        encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_RMS_NORM);
        if (encoder == nil) return failure_code;
    }
    [encoder setComputePipelineState:runtime->rms_norm_rows_pipeline];
    [encoder setBuffer:input_buffer offset:input_offset atIndex:0];
    [encoder setBuffer:runtime->rms_norm_weight_buffers[norm_slot] offset:0 atIndex:1];
    [encoder setBuffer:output_buffer offset:output_offset atIndex:2];
    [encoder setBytes:&params length:sizeof(params) atIndex:3];
    [encoder dispatchThreads:MTLSizeMake(rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->rms_norm_rows_pipeline, rows), 1, 1)];
    if (!planned_encoder) [encoder endEncoding];
    return 0;
}

static int termite_metal_encode_add(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    id<MTLBuffer> lhs_buffer,
    size_t lhs_offset,
    id<MTLBuffer> rhs_buffer,
    size_t rhs_offset,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    size_t elems,
    int failure_code
) {
    return termite_metal_encode_add_for(runtime, command_buffer, lhs_buffer, lhs_offset, rhs_buffer, rhs_offset, output_buffer, output_offset, elems, TERMITE_METAL_COMPUTE_SOURCE_OTHER, failure_code);
}

static int termite_metal_encode_add_for(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    id<MTLBuffer> lhs_buffer,
    size_t lhs_offset,
    id<MTLBuffer> rhs_buffer,
    size_t rhs_offset,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    size_t elems,
    size_t source,
    int failure_code
) {
    if (runtime == NULL || command_buffer == nil || lhs_buffer == nil || rhs_buffer == nil || output_buffer == nil) return failure_code;
    if (runtime->add_pipeline == nil) return failure_code;
    if (elems == 0 || elems > UINT32_MAX) return failure_code;
    size_t bytes = 0;
    if (!termite_metal_size_mul(elems, sizeof(float), &bytes)) return failure_code;
    if (termite_metal_decode_runtime_prepare_planned_compute_binary_accesses(
            runtime,
            lhs_buffer,
            lhs_offset,
            bytes,
            rhs_buffer,
            rhs_offset,
            bytes,
            output_buffer,
            output_offset,
            bytes,
            failure_code) != 0)
    {
        return failure_code;
    }
    termite_metal_apply_add_params params = {
        .rows = 1,
        .dim = (uint32_t)elems,
    };
    id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
    const BOOL planned_encoder = (encoder != nil);
    if (!planned_encoder) {
        encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, source);
        if (encoder == nil) return failure_code;
    }
    [encoder setComputePipelineState:runtime->add_pipeline];
    [encoder setBuffer:lhs_buffer offset:lhs_offset atIndex:0];
    [encoder setBuffer:rhs_buffer offset:rhs_offset atIndex:1];
    [encoder setBuffer:output_buffer offset:output_offset atIndex:2];
    [encoder setBytes:&params length:sizeof(params) atIndex:3];
    [encoder dispatchThreads:MTLSizeMake(elems, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->add_pipeline, elems), 1, 1)];
    if (!planned_encoder) [encoder endEncoding];
    return 0;
}

static int termite_metal_encode_rms_norm_reduce(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    id<MTLBuffer> input_buffer,
    size_t input_offset,
    size_t norm_slot,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    size_t hidden_size,
    float eps,
    int failure_code
) {
    if (runtime == NULL || command_buffer == nil || input_buffer == nil || output_buffer == nil) return failure_code;
    if (runtime->rms_norm_reduce_pipeline == nil) return failure_code;
    if (norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return failure_code;
    if (runtime->rms_norm_slot_prepared[norm_slot] == 0 || runtime->rms_norm_hidden_sizes[norm_slot] != hidden_size) return failure_code;
    if (hidden_size == 0 || hidden_size > UINT32_MAX) return failure_code;
    size_t hidden_bytes = 0;
    if (!termite_metal_size_mul(hidden_size, sizeof(float), &hidden_bytes)) return failure_code;
    termite_metal_planned_encoder_range accesses[3];
    if (termite_metal_planned_range_make(input_buffer, input_offset, hidden_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[0], failure_code) != 0 ||
        termite_metal_planned_range_make(runtime->rms_norm_weight_buffers[norm_slot], 0, runtime->rms_norm_weight_buffers[norm_slot].length, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[1], failure_code) != 0 ||
        termite_metal_planned_range_make(output_buffer, output_offset, hidden_bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &accesses[2], failure_code) != 0 ||
        termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, accesses, 3, failure_code) != 0)
    {
        return failure_code;
    }
    termite_metal_apply_layer_norm_params params = {
        .hidden_size = (uint32_t)hidden_size,
        .eps = eps,
    };
    id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
    const BOOL planned_encoder = (encoder != nil);
    if (!planned_encoder) {
        encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_RMS_NORM);
        if (encoder == nil) return failure_code;
    }
    [encoder setComputePipelineState:runtime->rms_norm_reduce_pipeline];
    [encoder setBuffer:input_buffer offset:input_offset atIndex:0];
    [encoder setBuffer:runtime->rms_norm_weight_buffers[norm_slot] offset:0 atIndex:1];
    [encoder setBuffer:output_buffer offset:output_offset atIndex:2];
    [encoder setBytes:&params length:sizeof(params) atIndex:3];
    [encoder setThreadgroupMemoryLength:256u * sizeof(float) atIndex:0];
    [encoder dispatchThreadgroups:MTLSizeMake(1, 1, 1) threadsPerThreadgroup:MTLSizeMake(256, 1, 1)];
    if (!planned_encoder) [encoder endEncoding];
    return 0;
}

static int termite_metal_encode_rms_inv_scale_1x(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    id<MTLBuffer> input_buffer,
    size_t input_offset,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    size_t hidden_size,
    float eps,
    int failure_code
) {
    if (runtime == NULL || command_buffer == nil || input_buffer == nil || output_buffer == nil) return failure_code;
    id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_RMS_NORM);
    if (encoder == nil) return failure_code;
    const int rc = termite_metal_encode_rms_inv_scale_1x_on_encoder(runtime, encoder, input_buffer, input_offset, output_buffer, output_offset, hidden_size, eps, failure_code);
    [encoder endEncoding];
    return rc;
}

static int termite_metal_encode_rms_inv_scale_1x_on_encoder(
    termite_metal_decode_runtime *runtime,
    id<MTLComputeCommandEncoder> encoder,
    id<MTLBuffer> input_buffer,
    size_t input_offset,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    size_t hidden_size,
    float eps,
    int failure_code
) {
    if (runtime == NULL || encoder == nil || input_buffer == nil || output_buffer == nil) return failure_code;
    if (runtime->rms_inv_scale_pipeline == nil) return failure_code;
    if (hidden_size == 0 || hidden_size > UINT32_MAX) return failure_code;
    size_t hidden_bytes = 0;
    if (!termite_metal_size_mul(hidden_size, sizeof(float), &hidden_bytes)) return failure_code;
    termite_metal_planned_encoder_range accesses[2];
    if (termite_metal_planned_range_make(input_buffer, input_offset, hidden_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[0], failure_code) != 0 ||
        termite_metal_planned_range_make(output_buffer, output_offset, sizeof(float), TERMITE_METAL_PLANNED_RANGE_WRITE, &accesses[1], failure_code) != 0 ||
        termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, accesses, 2, failure_code) != 0)
    {
        return failure_code;
    }
    termite_metal_apply_layer_norm_params params = {
        .hidden_size = (uint32_t)hidden_size,
        .eps = eps,
    };
    [encoder setComputePipelineState:runtime->rms_inv_scale_pipeline];
    [encoder setBuffer:input_buffer offset:input_offset atIndex:0];
    [encoder setBuffer:output_buffer offset:output_offset atIndex:1];
    [encoder setBytes:&params length:sizeof(params) atIndex:2];
    [encoder setThreadgroupMemoryLength:256u * sizeof(float) atIndex:0];
    [encoder dispatchThreadgroups:MTLSizeMake(1, 1, 1) threadsPerThreadgroup:MTLSizeMake(256, 1, 1)];
    return 0;
}

static id<MTLLibrary> termite_metal_shared_library(id<MTLDevice> device, BOOL precise_math) {
    static id<MTLLibrary> shared_library = nil;
    static id<MTLLibrary> shared_precise_library = nil;
    if (precise_math) {
        if (shared_precise_library == nil) {
            shared_precise_library = termite_metal_make_library(device, YES);
            if (shared_precise_library == nil) {
                shared_precise_library = termite_metal_make_library(device, NO);
            }
        }
        return shared_precise_library;
    }
    if (shared_library == nil) {
        shared_library = termite_metal_make_library(device, NO);
    }
    return shared_library;
}

termite_metal_provider *termite_metal_provider_create(void) {
    @autoreleasepool {
        id<MTLDevice> device = termite_metal_shared_device();
        if (device == nil) return NULL;
        id<MTLCommandQueue> queue = [device newCommandQueue];
        if (queue == nil) return NULL;
        id<MTLLibrary> library = termite_metal_shared_library(device, NO);
        if (library == nil) return NULL;

        termite_metal_provider *provider = calloc(1, sizeof(termite_metal_provider));
        if (provider == NULL) return NULL;
        provider->device = device;
        provider->queue = queue;
        provider->q4_0_pipeline = termite_metal_make_pipeline(device, library, @"termite_q4_0_linear");
        provider->q4_1_pipeline = termite_metal_make_pipeline(device, library, @"termite_q4_1_linear");
        provider->q5_0_pipeline = termite_metal_make_pipeline(device, library, @"termite_q5_0_linear");
        provider->q5_1_pipeline = termite_metal_make_pipeline(device, library, @"termite_q5_1_linear");
        provider->q8_0_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_0_linear");
        provider->q8_1_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_1_linear");
        provider->q8_0_mmv_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_0_linear_mmv");
        provider->q8_0_small_batch_r2_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_0_linear_r2_ext");
        provider->q8_0_small_batch_r3_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_0_linear_r3_ext");
        provider->q8_0_small_batch_r4_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_0_linear_r4_ext");
        provider->q8_0_small_batch_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_0_linear_r5_ext");
        provider->q8_0_mm_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_0_linear_mm");
        provider->q2_k_pipeline = termite_metal_make_pipeline(device, library, @"termite_q2_k_linear");
        provider->q3_k_pipeline = termite_metal_make_pipeline(device, library, @"termite_q3_k_linear");
        provider->q4_k_pipeline = termite_metal_make_pipeline(device, library, @"termite_q4_k_linear");
        provider->q5_k_pipeline = termite_metal_make_pipeline(device, library, @"termite_q5_k_linear");
        provider->q6_k_pipeline = termite_metal_make_pipeline(device, library, @"termite_q6_k_linear");
        provider->q8_k_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_k_linear");
        provider->iq4_nl_pipeline = termite_metal_make_pipeline(device, library, @"termite_iq4_nl_linear");
        provider->iq4_xs_pipeline = termite_metal_make_pipeline(device, library, @"termite_iq4_xs_linear");
        provider->mxfp4_pipeline = termite_metal_make_pipeline(device, library, @"termite_mxfp4_linear");
        provider->i2_s_pipeline = termite_metal_make_pipeline(device, library, @"termite_i2_s_linear");
        provider->i2_s_pair_pipeline = termite_metal_make_pipeline(device, library, @"termite_i2_s_pair_linear");
        provider->tl1_pipeline = termite_metal_make_pipeline(device, library, @"termite_tl1_linear");
        provider->tl2_pipeline = termite_metal_make_pipeline(device, library, @"termite_tl2_linear");
        provider->polar4_key_scores_pipeline = termite_metal_make_pipeline(device, library, @"termite_polar4_key_scores");
        provider->turbo3_key_scores_pipeline = termite_metal_make_pipeline(device, library, @"termite_turbo3_key_scores");
        if (provider->q4_0_pipeline == nil || provider->q4_1_pipeline == nil || provider->q5_0_pipeline == nil || provider->q5_1_pipeline == nil || provider->q8_0_pipeline == nil || provider->q8_1_pipeline == nil || provider->q8_0_mmv_pipeline == nil || provider->q8_0_small_batch_r2_pipeline == nil || provider->q8_0_small_batch_r3_pipeline == nil || provider->q8_0_small_batch_r4_pipeline == nil || provider->q8_0_small_batch_pipeline == nil || provider->q8_0_mm_pipeline == nil || provider->q2_k_pipeline == nil || provider->q3_k_pipeline == nil || provider->q4_k_pipeline == nil || provider->q5_k_pipeline == nil || provider->q6_k_pipeline == nil || provider->q8_k_pipeline == nil || provider->iq4_nl_pipeline == nil || provider->iq4_xs_pipeline == nil || provider->mxfp4_pipeline == nil || provider->i2_s_pipeline == nil || provider->i2_s_pair_pipeline == nil || provider->tl1_pipeline == nil || provider->tl2_pipeline == nil || provider->polar4_key_scores_pipeline == nil || provider->turbo3_key_scores_pipeline == nil) {
            free(provider);
            return NULL;
        }
        return provider;
    }
}

void termite_metal_provider_destroy(termite_metal_provider *provider) {
    if (provider == NULL) return;
    @autoreleasepool {
    provider->q4_0_pipeline = nil;
    provider->q4_1_pipeline = nil;
    provider->q5_0_pipeline = nil;
    provider->q5_1_pipeline = nil;
    provider->q8_0_pipeline = nil;
    provider->q8_1_pipeline = nil;
    provider->q2_k_pipeline = nil;
    provider->q3_k_pipeline = nil;
    provider->q4_k_pipeline = nil;
    provider->q5_k_pipeline = nil;
    provider->q6_k_pipeline = nil;
    provider->q8_k_pipeline = nil;
    provider->iq4_nl_pipeline = nil;
    provider->iq4_xs_pipeline = nil;
    provider->mxfp4_pipeline = nil;
    provider->i2_s_pipeline = nil;
    provider->i2_s_pair_pipeline = nil;
    provider->tl1_pipeline = nil;
    provider->tl2_pipeline = nil;
    provider->polar4_key_scores_pipeline = nil;
    provider->turbo3_key_scores_pipeline = nil;
    provider->queue = nil;
    provider->device = nil;
    free(provider);
    }
}

termite_metal_decode_runtime *termite_metal_decode_runtime_create(void) {
    @autoreleasepool {
        id<MTLDevice> device = termite_metal_shared_device();
        if (device == nil) {
            fprintf(stderr, "metal-runtime-create: device=nil\n");
            return NULL;
        }
        id<MTLCommandQueue> queue = [device newCommandQueue];
        if (queue == nil) {
            fprintf(stderr, "metal-runtime-create: queue=nil\n");
            return NULL;
        }
        id<MTLLibrary> library = termite_metal_shared_library(device, NO);
        if (library == nil) {
            fprintf(stderr, "metal-runtime-create: library=nil\n");
            return NULL;
        }
        id<MTLLibrary> precise_library = termite_metal_shared_library(device, YES);
        if (precise_library == nil) precise_library = library;

        termite_metal_decode_runtime *runtime = calloc(1, sizeof(termite_metal_decode_runtime));
        if (runtime == NULL) return NULL;
        runtime->device = device;
        runtime->queue = queue;
        runtime->library = library;
        runtime->precise_library = precise_library;
        runtime->embed_absolute_position_pipeline = termite_metal_make_pipeline(device, library, @"termite_embed_absolute_position");
        runtime->embedding_lookup_pipeline = termite_metal_make_pipeline(device, library, @"termite_embedding_lookup");
        runtime->embedding_lookup_bf16_pipeline = termite_metal_make_pipeline(device, library, @"termite_embedding_lookup_bf16");
        runtime->q4_0_get_rows_pipeline = termite_metal_make_pipeline(device, library, @"termite_q4_0_get_rows");
        runtime->q4_0_set_rows_pipeline = termite_metal_make_pipeline(device, library, @"termite_q4_0_set_rows");
        runtime->q4_0_cpy_q_to_f32_pipeline = termite_metal_make_pipeline(device, library, @"termite_q4_0_cpy_q_to_f32");
        runtime->q4_0_cpy_f32_to_q_pipeline = termite_metal_make_pipeline(device, library, @"termite_q4_0_cpy_f32_to_q");
        runtime->q4_1_get_rows_pipeline = termite_metal_make_pipeline(device, library, @"termite_q4_1_get_rows");
        runtime->q4_1_set_rows_pipeline = termite_metal_make_pipeline(device, library, @"termite_q4_1_set_rows");
        runtime->q4_1_cpy_q_to_f32_pipeline = termite_metal_make_pipeline(device, library, @"termite_q4_1_cpy_q_to_f32");
        runtime->q4_1_cpy_f32_to_q_pipeline = termite_metal_make_pipeline(device, library, @"termite_q4_1_cpy_f32_to_q");
        runtime->q5_0_get_rows_pipeline = termite_metal_make_pipeline(device, library, @"termite_q5_0_get_rows");
        runtime->q5_0_set_rows_pipeline = termite_metal_make_pipeline(device, library, @"termite_q5_0_set_rows");
        runtime->q5_0_cpy_q_to_f32_pipeline = termite_metal_make_pipeline(device, library, @"termite_q5_0_cpy_q_to_f32");
        runtime->q5_0_cpy_f32_to_q_pipeline = termite_metal_make_pipeline(device, library, @"termite_q5_0_cpy_f32_to_q");
        runtime->q5_1_get_rows_pipeline = termite_metal_make_pipeline(device, library, @"termite_q5_1_get_rows");
        runtime->q5_1_set_rows_pipeline = termite_metal_make_pipeline(device, library, @"termite_q5_1_set_rows");
        runtime->q5_1_cpy_q_to_f32_pipeline = termite_metal_make_pipeline(device, library, @"termite_q5_1_cpy_q_to_f32");
        runtime->q5_1_cpy_f32_to_q_pipeline = termite_metal_make_pipeline(device, library, @"termite_q5_1_cpy_f32_to_q");
        runtime->q4_k_get_rows_pipeline = termite_metal_make_pipeline(device, library, @"termite_q4_k_get_rows");
        runtime->q4_k_set_rows_pipeline = termite_metal_make_pipeline(device, library, @"termite_q4_k_set_rows");
        runtime->q4_k_cpy_q_to_f32_pipeline = termite_metal_make_pipeline(device, library, @"termite_q4_k_cpy_q_to_f32");
        runtime->q4_k_cpy_f32_to_q_pipeline = termite_metal_make_pipeline(device, library, @"termite_q4_k_cpy_f32_to_q");
        runtime->q5_k_get_rows_pipeline = termite_metal_make_pipeline(device, library, @"termite_q5_k_get_rows");
        runtime->q5_k_set_rows_pipeline = termite_metal_make_pipeline(device, library, @"termite_q5_k_set_rows");
        runtime->q5_k_cpy_q_to_f32_pipeline = termite_metal_make_pipeline(device, library, @"termite_q5_k_cpy_q_to_f32");
        runtime->q5_k_cpy_f32_to_q_pipeline = termite_metal_make_pipeline(device, library, @"termite_q5_k_cpy_f32_to_q");
        runtime->q6_k_get_rows_pipeline = termite_metal_make_pipeline(device, library, @"termite_q6_k_get_rows");
        runtime->q6_k_set_rows_pipeline = termite_metal_make_pipeline(device, library, @"termite_q6_k_set_rows");
        runtime->q6_k_cpy_q_to_f32_pipeline = termite_metal_make_pipeline(device, library, @"termite_q6_k_cpy_q_to_f32");
        runtime->q6_k_cpy_f32_to_q_pipeline = termite_metal_make_pipeline(device, library, @"termite_q6_k_cpy_f32_to_q");
        runtime->q8_0_get_rows_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_0_get_rows");
        runtime->q8_0_set_rows_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_0_set_rows");
        runtime->q8_0_cpy_q_to_f32_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_0_cpy_q_to_f32");
        runtime->q8_0_cpy_f32_to_q_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_0_cpy_f32_to_q");
        runtime->q8_1_get_rows_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_1_get_rows");
        runtime->q8_1_set_rows_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_1_set_rows");
        runtime->q8_1_cpy_q_to_f32_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_1_cpy_q_to_f32");
        runtime->q8_1_cpy_f32_to_q_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_1_cpy_f32_to_q");
        runtime->rope_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_apply_rope");
        runtime->head_rms_rope_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_apply_head_rms_rope");
        runtime->attention_f32_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_attention_f32");
        runtime->attention_f32_prefill_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_attention_f32_prefill_tiled");
        runtime->attention_paged_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_paged_attention_kv");
        runtime->compressed_attention_store_local_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_compressed_attention_store_local");
        runtime->compressed_attention_store_component_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_compressed_attention_store_component");
        runtime->compressed_attention_update_component_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_compressed_attention_update_component");
        runtime->compressed_attention_select_compressed_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_compressed_attention_select_compressed");
        runtime->compressed_attention_hybrid_attention_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_compressed_attention_hybrid_attention");
        runtime->paged_f32_kv_seed_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_paged_f32_kv_seed");
        runtime->paged_f16_kv_seed_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_paged_f16_kv_seed");
        runtime->paged_f32_v_seed_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_paged_f32_v_seed");
        runtime->slice_last_dim_f32_2d_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_slice_last_dim_f32_2d");
        runtime->gather_axis0_f32_2d_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_gather_axis0_f32_2d");
        runtime->concat_lastdim_f32_2d_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_concat_lastdim_f32_2d");
        runtime->deberta_embeddings_f32_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_deberta_embeddings_f32");
        runtime->gliner_word_embeddings_f32_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_gliner_word_embeddings_f32");
        runtime->gliner_gru_combine_f32_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_gliner_gru_combine_f32");
        runtime->argmax_axis_f32_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_argmax_axis_f32");
        runtime->convert_dtype_f32_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_convert_dtype_f32");
        runtime->sdpa_f32_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_sdpa_f32");
        runtime->disentangled_relative_attention_f32_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_disentangled_relative_attention_f32");
        runtime->disentangled_relative_attention_f32_tg_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_disentangled_relative_attention_f32_tg");
        runtime->disentangled_relative_attention_f32_flash4_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_disentangled_relative_attention_f32_flash4");
        runtime->deberta_relative_score_gemm_f32_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_deberta_relative_score_gemm_f32");
        runtime->deberta_relative_score_softmax_pv_f32_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_deberta_relative_score_softmax_pv_f32");
        runtime->transpose_f32_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_transpose_f32");
        runtime->dot_general_2d_f32_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_dot_general_2d_f32");
        runtime->dot_general_batched_f32_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_dot_general_batched_f32");
        runtime->conv1d_f32_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_conv1d_f32");
        runtime->conv2d_f32_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_conv2d_f32");
        runtime->layer_norm_pipeline = termite_metal_make_pipeline(device, library, @"termite_apply_layer_norm_1x");
        runtime->layer_norm_rows_pipeline = termite_metal_make_pipeline(device, library, @"termite_apply_layer_norm_rows");
        runtime->layer_norm_add_pipeline = termite_metal_make_pipeline(device, library, @"termite_apply_add_layer_norm_1x");
        runtime->layer_norm_add_rows_pipeline = termite_metal_make_pipeline(device, library, @"termite_apply_add_layer_norm_rows");
        runtime->bias_activation_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_apply_bias_activation_2d");
        runtime->bias_add_layer_norm_pipeline = termite_metal_make_pipeline(device, library, @"termite_apply_bias_add_layer_norm_1x");
        runtime->bias_add_layer_norm_rows_pipeline = termite_metal_make_pipeline(device, library, @"termite_apply_bias_add_layer_norm_rows");
        runtime->dense_qkv_split_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_dense_qkv_split_f32_2d");
        runtime->dense_pair_split_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_dense_pair_split_f32_2d");
        runtime->dense_qkv_split_bias_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_dense_qkv_split_bias_f32_2d");
        runtime->dense_pair_split_bias_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_dense_pair_split_bias_f32_2d");
        runtime->dense_qkv_direct_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_dense_qkv_direct_f32_2d");
        runtime->dense_pair_direct_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_dense_pair_direct_f32_2d");
        runtime->rms_norm_pipeline = termite_metal_make_pipeline(device, library, @"termite_apply_rms_norm_1x");
        runtime->rms_norm_reduce_pipeline = termite_metal_make_pipeline(device, library, @"termite_apply_rms_norm_1x_reduce");
        runtime->rms_inv_scale_pipeline = termite_metal_make_pipeline(device, library, @"termite_compute_rms_inv_scale_1x");
        runtime->rms_norm_rows_pipeline = termite_metal_make_pipeline(device, library, @"termite_apply_rms_norm_rows");
        runtime->rms_norm_add_pipeline = termite_metal_make_pipeline(device, library, @"termite_apply_rms_norm_add_1x");
        runtime->rms_norm_add_scale_pipeline = termite_metal_make_pipeline(device, library, @"termite_apply_rms_norm_add_scale_1x");
        runtime->rms_norm_add_scale_rows_pipeline = termite_metal_make_pipeline(device, library, @"termite_apply_rms_norm_add_scale_rows");
        runtime->linear_pipeline = termite_metal_make_pipeline(device, library, @"termite_apply_linear_1x");
        runtime->linear_reduce_pipeline = termite_metal_make_pipeline(device, library, @"termite_apply_linear_1x_reduce");
        runtime->linear_bf16_pipeline = termite_metal_make_pipeline(device, library, @"termite_apply_linear_bf16_1x");
        runtime->linear_bf16_reduce_pipeline = termite_metal_make_pipeline(device, library, @"termite_apply_linear_bf16_1x_reduce");
        runtime->linear_bf16_multi_row_pipeline = termite_metal_make_pipeline(device, library, @"termite_apply_linear_bf16_multi_row");
        runtime->linear_bf16_multi_row_reduce_pipeline = termite_metal_make_pipeline(device, library, @"termite_apply_linear_bf16_multi_row_reduce");
        runtime->linear_bf16_multi_row_shared_reduce_pipeline = termite_metal_make_pipeline(device, library, @"termite_apply_linear_bf16_multi_row_shared_reduce");
        runtime->linear_pair_reduce_pipeline = termite_metal_make_pipeline(device, library, @"termite_apply_linear_pair_1x_reduce");
        runtime->linear_multi_row_pipeline = termite_metal_make_pipeline(device, library, @"termite_apply_linear_multi_row");
        runtime->linear_multi_row_reduce_pipeline = termite_metal_make_pipeline(device, library, @"termite_apply_linear_multi_row_reduce");
        runtime->linear_multi_row_tiled_f32_pipeline = termite_metal_make_pipeline(device, library, @"termite_apply_linear_multi_row_tiled_f32");
        runtime->linear_multi_row_tiled_activation_f32_pipeline = termite_metal_make_pipeline(device, library, @"termite_apply_linear_multi_row_tiled_activation_f32");
        runtime->linear_multi_row_tiled32_f32_pipeline = termite_metal_make_pipeline(device, library, @"termite_apply_linear_multi_row_tiled32_f32");
        runtime->linear_multi_row_tiled32_activation_f32_pipeline = termite_metal_make_pipeline(device, library, @"termite_apply_linear_multi_row_tiled32_activation_f32");
        runtime->linear_multi_row_tiled32_m16_f32_pipeline = termite_metal_make_pipeline(device, library, @"termite_apply_linear_multi_row_tiled32_m16_f32");
        runtime->linear_multi_row_tiled32_m16_activation_f32_pipeline = termite_metal_make_pipeline(device, library, @"termite_apply_linear_multi_row_tiled32_m16_activation_f32");
        runtime->linear_bias_pipeline = termite_metal_make_pipeline(device, library, @"termite_apply_linear_bias_2d");
        runtime->argmax_logits_pipeline = termite_metal_make_pipeline(device, library, @"termite_argmax_logits_1x");
        runtime->argmax_logits_partials_pipeline = termite_metal_make_pipeline(device, library, @"termite_argmax_logits_partials");
        runtime->argmax_logits_reduce_pipeline = termite_metal_make_pipeline(device, library, @"termite_argmax_logits_reduce");
        runtime->sample_logits_pipeline = termite_metal_make_pipeline(device, library, @"termite_sample_logits_1x");
        runtime->sample_topk_partials_pipeline = termite_metal_make_pipeline(device, library, @"termite_sample_topk_partials");
        runtime->sample_topk_reduce_pipeline = termite_metal_make_pipeline(device, library, @"termite_sample_topk_reduce");
        runtime->activation_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_apply_activation_1x");
        runtime->activation_multiply_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_apply_activation_multiply_1x");
        runtime->softmax_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_apply_softmax_rows");
        runtime->reduce_last_dim_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_reduce_last_dim_rows");
        runtime->reduce_axis_f32_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_reduce_axis_f32");
        runtime->multiply_reduce_last_dim_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_multiply_reduce_last_dim_rows");
        runtime->broadcast_last_dim_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_broadcast_last_dim_f32_2d");
        runtime->broadcast_f32_pipeline = termite_metal_make_pipeline(device, precise_library, @"termite_broadcast_f32");
        runtime->multiply_pipeline = termite_metal_make_pipeline(device, library, @"termite_apply_multiply_1x");
        runtime->scale_pipeline = termite_metal_make_pipeline(device, library, @"termite_apply_scale_1x");
        runtime->add_pipeline = termite_metal_make_pipeline(device, library, @"termite_apply_add_1x");
        runtime->add_scale_pipeline = termite_metal_make_pipeline(device, library, @"termite_apply_add_scale_1x");
        runtime->scaled_add_scale_pipeline = termite_metal_make_pipeline(device, library, @"termite_apply_scaled_add_scale_1x");
        runtime->training_accumulate_pipeline = termite_metal_make_pipeline(device, library, @"termite_training_accumulate_f32");
        runtime->training_adamw_pipeline = termite_metal_make_pipeline(device, library, @"termite_training_adamw_f32");
        runtime->training_sumsq_pipeline = termite_metal_make_pipeline(device, library, @"termite_training_sumsq_f32");
        runtime->subtract_pipeline = termite_metal_make_pipeline(device, library, @"termite_apply_subtract_1x");
        runtime->divide_pipeline = termite_metal_make_pipeline(device, library, @"termite_apply_divide_1x");
        runtime->less_than_pipeline = termite_metal_make_pipeline(device, library, @"termite_apply_less_than_1x");
        runtime->where_select_pipeline = termite_metal_make_pipeline(device, library, @"termite_apply_where_select_1x");
        runtime->i2_s_quantize_pipeline = termite_metal_make_pipeline(device, library, @"termite_quantize_rows_i8");
        runtime->q1_0_pipeline = termite_metal_make_pipeline(device, library, @"termite_q1_0_linear");
        runtime->i8_s_pipeline = termite_metal_make_pipeline(device, library, @"termite_i8_s_linear");
        runtime->q2_k_pipeline = termite_metal_make_pipeline(device, library, @"termite_q2_k_linear");
        runtime->q3_k_pipeline = termite_metal_make_pipeline(device, library, @"termite_q3_k_linear");
        runtime->q4_k_pipeline = termite_metal_make_pipeline(device, library, @"termite_q4_k_linear");
        runtime->q4_k_reduce_pipeline = termite_metal_make_pipeline(device, library, @"termite_q4_k_linear_1x_reduce");
        runtime->q4_k_pair_pipeline = termite_metal_make_pipeline(device, library, @"termite_q4_k_pair_linear");
        runtime->q4_0_pipeline = termite_metal_make_pipeline(device, library, @"termite_q4_0_linear");
        runtime->q4_0_pair_pipeline = termite_metal_make_pipeline(device, library, @"termite_q4_0_pair_linear");
        runtime->q4_0_reduce_pipeline = termite_metal_make_pipeline(device, library, @"termite_q4_0_linear_1x_reduce");
        runtime->q4_1_pipeline = termite_metal_make_pipeline(device, library, @"termite_q4_1_linear");
        runtime->q4_1_reduce_pipeline = termite_metal_make_pipeline(device, library, @"termite_q4_1_linear_1x_reduce");
        runtime->q5_0_pipeline = termite_metal_make_pipeline(device, library, @"termite_q5_0_linear");
        runtime->q5_0_reduce_pipeline = termite_metal_make_pipeline(device, library, @"termite_q5_0_linear_1x_reduce");
        runtime->q5_1_pipeline = termite_metal_make_pipeline(device, library, @"termite_q5_1_linear");
        runtime->q5_1_reduce_pipeline = termite_metal_make_pipeline(device, library, @"termite_q5_1_linear_1x_reduce");
        runtime->q8_0_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_0_linear");
        runtime->q8_0_pair_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_0_pair_linear");
        runtime->q8_0_mmv_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_0_linear_mmv");
        runtime->q8_0_mmv_f16_input_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_0_linear_mmv_in_f16");
        runtime->q8_0_rms_scale_mmv_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_0_linear_rms_scale_mmv");
        runtime->q8_0_small_batch_r2_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_0_linear_r2_ext");
        runtime->q8_0_small_batch_r3_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_0_linear_r3_ext");
        runtime->q8_0_small_batch_r4_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_0_linear_r4_ext");
        runtime->q8_0_small_batch_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_0_linear_r5_ext");
        const bool disable_runtime_q8_mm = getenv("TERMITE_METAL_DISABLE_Q8_MM") != NULL;
        const bool disable_runtime_q8_mm_sg = disable_runtime_q8_mm || getenv("TERMITE_METAL_DISABLE_Q8_MM_SG") != NULL;
        const bool disable_runtime_q8_pair_activation_mm = getenv("TERMITE_METAL_DISABLE_Q8_PAIR_ACTIVATION_MM") != NULL;
        runtime->q8_0_mm_pipeline = disable_runtime_q8_mm ? nil : termite_metal_make_pipeline(device, library, @"termite_q8_0_linear_mm");
        runtime->q8_0_mm_f16_input_pipeline = disable_runtime_q8_mm ? nil : termite_metal_make_pipeline(device, library, @"termite_q8_0_linear_mm_in_f16");
        runtime->q8_0_mm_sg_pipeline = disable_runtime_q8_mm_sg ? nil : termite_metal_make_pipeline(device, library, @"termite_q8_0_linear_mm_sg");
        runtime->q8_0_pair_mmv_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_0_pair_linear_mmv");
        runtime->q8_0_pair_small_batch_r2_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_0_pair_linear_r2_ext");
        runtime->q8_0_pair_small_batch_r3_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_0_pair_linear_r3_ext");
        runtime->q8_0_pair_small_batch_r4_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_0_pair_linear_r4_ext");
        runtime->q8_0_pair_small_batch_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_0_pair_linear_r5_ext");
        runtime->q8_0_qkv_mmv_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_0_qkv_linear_mmv");
        runtime->q8_0_pair_activation_reduce_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_0_pair_activation_multiply_1x_reduce4");
        runtime->q8_0_pair_activation_mmv_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_0_pair_activation_multiply_1r_ext");
        runtime->q8_0_pair_activation_rms_scale_mmv_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_0_pair_activation_multiply_rms_scale_1r_ext");
        runtime->q8_0_pair_activation_rms_scale_mmv_f16_output_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_0_pair_activation_multiply_rms_scale_1r_ext_out_f16");
        runtime->q8_0_pair_activation_small_batch_r2_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_0_pair_activation_multiply_r2_ext");
        runtime->q8_0_pair_activation_small_batch_r3_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_0_pair_activation_multiply_r3_ext");
        runtime->q8_0_pair_activation_small_batch_r4_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_0_pair_activation_multiply_r4_ext");
        runtime->q8_0_pair_activation_small_batch_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_0_pair_activation_multiply_r5_ext");
        runtime->q8_0_pair_activation_mm_pipeline = (disable_runtime_q8_mm || disable_runtime_q8_pair_activation_mm) ? nil : termite_metal_make_pipeline(device, library, @"termite_q8_0_pair_activation_multiply_mm");
        runtime->q8_0_pair_activation_mm_f16_output_pipeline = (disable_runtime_q8_mm || disable_runtime_q8_pair_activation_mm) ? nil : termite_metal_make_pipeline(device, library, @"termite_q8_0_pair_activation_multiply_mm_out_f16");
        runtime->q8_0_activation_multiply_reduce_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_0_activation_multiply_1x_reduce4");
        runtime->q8_0_activation_multiply_mmv_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_0_activation_multiply_1r_ext");
        runtime->q8_1_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_1_linear");
        runtime->q8_1_reduce_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_1_linear_1x_reduce");
        runtime->q5_k_pipeline = termite_metal_make_pipeline(device, library, @"termite_q5_k_linear");
        runtime->q5_k_reduce_pipeline = termite_metal_make_pipeline(device, library, @"termite_q5_k_linear_1x_reduce");
        runtime->q6_k_pipeline = termite_metal_make_pipeline(device, library, @"termite_q6_k_linear");
        runtime->q6_k_reduce_pipeline = termite_metal_make_pipeline(device, library, @"termite_q6_k_linear_1x_reduce");
        runtime->q6_k_pair_pipeline = termite_metal_make_pipeline(device, library, @"termite_q6_k_pair_linear");
        runtime->q8_k_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_k_linear");
        runtime->q8_k_reduce_pipeline = termite_metal_make_pipeline(device, library, @"termite_q8_k_linear_1x_reduce");
        runtime->iq4_nl_pipeline = termite_metal_make_pipeline(device, library, @"termite_iq4_nl_linear");
        runtime->iq4_nl_reduce_pipeline = termite_metal_make_pipeline(device, library, @"termite_iq4_nl_linear_1x_reduce");
        runtime->iq4_xs_pipeline = termite_metal_make_pipeline(device, library, @"termite_iq4_xs_linear");
        runtime->iq4_xs_reduce_pipeline = termite_metal_make_pipeline(device, library, @"termite_iq4_xs_linear_1x_reduce");
        runtime->mxfp4_pipeline = termite_metal_make_pipeline(device, library, @"termite_mxfp4_linear");
        runtime->mxfp4_reduce_pipeline = termite_metal_make_pipeline(device, library, @"termite_mxfp4_linear_1x_reduce");
        runtime->nvfp4_pipeline = termite_metal_make_pipeline(device, library, @"termite_nvfp4_linear");
        runtime->iq2_xs_pipeline = termite_metal_make_pipeline(device, library, @"termite_iq2_xs_linear");
        runtime->i2_s_pipeline = termite_metal_make_pipeline(device, library, @"termite_i2_s_linear");
        runtime->i2_s_pair_pipeline = termite_metal_make_pipeline(device, library, @"termite_i2_s_pair_linear");
        runtime->i2_s_linear_i8_pipeline = termite_metal_make_pipeline(device, library, @"termite_i2_s_linear_i8");
        runtime->i2_s_pair_i8_pipeline = termite_metal_make_pipeline(device, library, @"termite_i2_s_pair_linear_i8");
        runtime->tl1_pipeline = termite_metal_make_pipeline(device, library, @"termite_tl1_linear");
        runtime->tl2_pipeline = termite_metal_make_pipeline(device, library, @"termite_tl2_linear");
        runtime->encode_polar4_key_pipeline = termite_metal_make_pipeline(device, library, @"termite_encode_polar4_key");
        runtime->encode_turbo3_key_pipeline = termite_metal_make_pipeline(device, library, @"termite_encode_turbo3_key");
        runtime->encode_f16_convert_pipeline = termite_metal_make_pipeline(device, library, @"termite_encode_f16_convert");
        runtime->encode_int8_per_head_pipeline = termite_metal_make_pipeline(device, library, @"termite_encode_int8_per_head");
        runtime->polar4_attention_span_pipeline = termite_metal_make_pipeline(device, library, @"termite_polar4_attention_span");
        runtime->turbo3_attention_span_pipeline = termite_metal_make_pipeline(device, library, @"termite_turbo3_attention_span");
        BOOL missing_quant_reduce_pipeline = runtime->q4_1_reduce_pipeline == nil || runtime->q5_1_reduce_pipeline == nil || runtime->q8_1_reduce_pipeline == nil || runtime->q8_k_reduce_pipeline == nil || runtime->iq4_nl_reduce_pipeline == nil || runtime->iq4_xs_reduce_pipeline == nil || runtime->mxfp4_reduce_pipeline == nil;
        BOOL missing_dense_multi_row_reduce_pipeline = runtime->linear_bf16_multi_row_reduce_pipeline == nil || runtime->linear_multi_row_reduce_pipeline == nil;
        if (missing_quant_reduce_pipeline || missing_dense_multi_row_reduce_pipeline || runtime->embed_absolute_position_pipeline == nil || runtime->embedding_lookup_pipeline == nil || runtime->q4_0_get_rows_pipeline == nil || runtime->q4_0_set_rows_pipeline == nil || runtime->q4_0_cpy_q_to_f32_pipeline == nil || runtime->q4_0_cpy_f32_to_q_pipeline == nil || runtime->q4_1_get_rows_pipeline == nil || runtime->q4_1_set_rows_pipeline == nil || runtime->q4_1_cpy_q_to_f32_pipeline == nil || runtime->q4_1_cpy_f32_to_q_pipeline == nil || runtime->q5_0_get_rows_pipeline == nil || runtime->q5_0_set_rows_pipeline == nil || runtime->q5_0_cpy_q_to_f32_pipeline == nil || runtime->q5_0_cpy_f32_to_q_pipeline == nil || runtime->q5_1_get_rows_pipeline == nil || runtime->q5_1_set_rows_pipeline == nil || runtime->q5_1_cpy_q_to_f32_pipeline == nil || runtime->q5_1_cpy_f32_to_q_pipeline == nil || runtime->q4_k_get_rows_pipeline == nil || runtime->q4_k_set_rows_pipeline == nil || runtime->q4_k_cpy_q_to_f32_pipeline == nil || runtime->q4_k_cpy_f32_to_q_pipeline == nil || runtime->q5_k_get_rows_pipeline == nil || runtime->q5_k_set_rows_pipeline == nil || runtime->q5_k_cpy_q_to_f32_pipeline == nil || runtime->q5_k_cpy_f32_to_q_pipeline == nil || runtime->q6_k_get_rows_pipeline == nil || runtime->q6_k_set_rows_pipeline == nil || runtime->q6_k_cpy_q_to_f32_pipeline == nil || runtime->q6_k_cpy_f32_to_q_pipeline == nil || runtime->q8_0_get_rows_pipeline == nil || runtime->q8_0_set_rows_pipeline == nil || runtime->q8_0_cpy_q_to_f32_pipeline == nil || runtime->q8_0_cpy_f32_to_q_pipeline == nil || runtime->q8_1_get_rows_pipeline == nil || runtime->q8_1_set_rows_pipeline == nil || runtime->q8_1_cpy_q_to_f32_pipeline == nil || runtime->q8_1_cpy_f32_to_q_pipeline == nil || runtime->rope_pipeline == nil || runtime->head_rms_rope_pipeline == nil || runtime->attention_f32_pipeline == nil || runtime->attention_f32_prefill_pipeline == nil || runtime->attention_paged_pipeline == nil || runtime->paged_f32_kv_seed_pipeline == nil || runtime->paged_f16_kv_seed_pipeline == nil || runtime->paged_f32_v_seed_pipeline == nil || runtime->slice_last_dim_f32_2d_pipeline == nil || runtime->transpose_f32_pipeline == nil || runtime->dot_general_2d_f32_pipeline == nil || runtime->dot_general_batched_f32_pipeline == nil || runtime->conv1d_f32_pipeline == nil || runtime->conv2d_f32_pipeline == nil || runtime->layer_norm_pipeline == nil || runtime->rms_norm_pipeline == nil || runtime->rms_norm_reduce_pipeline == nil || runtime->rms_norm_rows_pipeline == nil || runtime->rms_norm_add_pipeline == nil || runtime->rms_norm_add_scale_pipeline == nil || runtime->rms_norm_add_scale_rows_pipeline == nil || runtime->linear_pipeline == nil || runtime->linear_reduce_pipeline == nil || runtime->linear_bf16_pipeline == nil || runtime->linear_bf16_reduce_pipeline == nil || runtime->linear_bf16_multi_row_pipeline == nil || runtime->linear_pair_reduce_pipeline == nil || runtime->linear_multi_row_pipeline == nil || runtime->linear_bias_pipeline == nil || runtime->argmax_logits_pipeline == nil || runtime->argmax_logits_partials_pipeline == nil || runtime->argmax_logits_reduce_pipeline == nil || runtime->sample_logits_pipeline == nil || runtime->sample_topk_partials_pipeline == nil || runtime->sample_topk_reduce_pipeline == nil || runtime->activation_pipeline == nil || runtime->activation_multiply_pipeline == nil || runtime->softmax_pipeline == nil || runtime->reduce_last_dim_pipeline == nil || runtime->reduce_axis_f32_pipeline == nil || runtime->multiply_reduce_last_dim_pipeline == nil || runtime->broadcast_last_dim_pipeline == nil || runtime->broadcast_f32_pipeline == nil || runtime->multiply_pipeline == nil || runtime->scale_pipeline == nil || runtime->add_pipeline == nil || runtime->add_scale_pipeline == nil || runtime->subtract_pipeline == nil || runtime->divide_pipeline == nil || runtime->less_than_pipeline == nil || runtime->where_select_pipeline == nil || runtime->i2_s_quantize_pipeline == nil || runtime->q1_0_pipeline == nil || runtime->i8_s_pipeline == nil || runtime->q2_k_pipeline == nil || runtime->q3_k_pipeline == nil || runtime->q4_k_pipeline == nil || runtime->q4_k_reduce_pipeline == nil || runtime->q4_k_pair_pipeline == nil || runtime->q4_0_pipeline == nil || runtime->q4_0_pair_pipeline == nil || runtime->q4_0_reduce_pipeline == nil || runtime->q4_1_pipeline == nil || runtime->q5_0_pipeline == nil || runtime->q5_0_reduce_pipeline == nil || runtime->q5_1_pipeline == nil || runtime->q8_0_pipeline == nil || runtime->q8_0_pair_pipeline == nil || runtime->q8_0_mmv_pipeline == nil || runtime->q8_0_rms_scale_mmv_pipeline == nil || runtime->q8_0_small_batch_pipeline == nil || (runtime->q8_0_mm_pipeline == nil && runtime->q8_0_mm_sg_pipeline == nil) || runtime->q8_0_pair_mmv_pipeline == nil || runtime->q8_0_pair_small_batch_pipeline == nil || runtime->q8_0_qkv_mmv_pipeline == nil || runtime->q8_0_pair_activation_reduce_pipeline == nil || runtime->q8_0_pair_activation_mmv_pipeline == nil || runtime->q8_0_pair_activation_small_batch_pipeline == nil || runtime->q8_0_activation_multiply_reduce_pipeline == nil || runtime->q8_0_activation_multiply_mmv_pipeline == nil || runtime->q8_1_pipeline == nil || runtime->q5_k_pipeline == nil || runtime->q5_k_reduce_pipeline == nil || runtime->q6_k_pipeline == nil || runtime->q6_k_reduce_pipeline == nil || runtime->q6_k_pair_pipeline == nil || runtime->q8_k_pipeline == nil || runtime->iq4_nl_pipeline == nil || runtime->iq4_xs_pipeline == nil || runtime->mxfp4_pipeline == nil || runtime->nvfp4_pipeline == nil || runtime->iq2_xs_pipeline == nil || runtime->i2_s_pipeline == nil || runtime->i2_s_pair_pipeline == nil || runtime->i2_s_linear_i8_pipeline == nil || runtime->i2_s_pair_i8_pipeline == nil || runtime->tl1_pipeline == nil || runtime->tl2_pipeline == nil || runtime->encode_polar4_key_pipeline == nil || runtime->encode_turbo3_key_pipeline == nil || runtime->polar4_attention_span_pipeline == nil || runtime->turbo3_attention_span_pipeline == nil) {
            fprintf(stderr, "metal-runtime-create: pipeline=nil");
            if (runtime->embed_absolute_position_pipeline == nil) fprintf(stderr, " embed_absolute_position");
            if (runtime->embedding_lookup_pipeline == nil) fprintf(stderr, " embedding_lookup");
            if (runtime->q4_0_get_rows_pipeline == nil) fprintf(stderr, " q4_0_get_rows");
            if (runtime->q4_0_set_rows_pipeline == nil) fprintf(stderr, " q4_0_set_rows");
            if (runtime->q4_0_cpy_q_to_f32_pipeline == nil) fprintf(stderr, " q4_0_cpy_q_to_f32");
            if (runtime->q4_0_cpy_f32_to_q_pipeline == nil) fprintf(stderr, " q4_0_cpy_f32_to_q");
            if (runtime->q4_1_get_rows_pipeline == nil) fprintf(stderr, " q4_1_get_rows");
            if (runtime->q4_1_set_rows_pipeline == nil) fprintf(stderr, " q4_1_set_rows");
            if (runtime->q4_1_cpy_q_to_f32_pipeline == nil) fprintf(stderr, " q4_1_cpy_q_to_f32");
            if (runtime->q4_1_cpy_f32_to_q_pipeline == nil) fprintf(stderr, " q4_1_cpy_f32_to_q");
            if (runtime->q5_0_get_rows_pipeline == nil) fprintf(stderr, " q5_0_get_rows");
            if (runtime->q5_0_set_rows_pipeline == nil) fprintf(stderr, " q5_0_set_rows");
            if (runtime->q5_0_cpy_q_to_f32_pipeline == nil) fprintf(stderr, " q5_0_cpy_q_to_f32");
            if (runtime->q5_0_cpy_f32_to_q_pipeline == nil) fprintf(stderr, " q5_0_cpy_f32_to_q");
            if (runtime->q5_1_get_rows_pipeline == nil) fprintf(stderr, " q5_1_get_rows");
            if (runtime->q5_1_set_rows_pipeline == nil) fprintf(stderr, " q5_1_set_rows");
            if (runtime->q5_1_cpy_q_to_f32_pipeline == nil) fprintf(stderr, " q5_1_cpy_q_to_f32");
            if (runtime->q5_1_cpy_f32_to_q_pipeline == nil) fprintf(stderr, " q5_1_cpy_f32_to_q");
            if (runtime->q4_k_get_rows_pipeline == nil) fprintf(stderr, " q4_k_get_rows");
            if (runtime->q4_k_set_rows_pipeline == nil) fprintf(stderr, " q4_k_set_rows");
            if (runtime->q4_k_cpy_q_to_f32_pipeline == nil) fprintf(stderr, " q4_k_cpy_q_to_f32");
            if (runtime->q4_k_cpy_f32_to_q_pipeline == nil) fprintf(stderr, " q4_k_cpy_f32_to_q");
            if (runtime->q5_k_get_rows_pipeline == nil) fprintf(stderr, " q5_k_get_rows");
            if (runtime->q5_k_set_rows_pipeline == nil) fprintf(stderr, " q5_k_set_rows");
            if (runtime->q5_k_cpy_q_to_f32_pipeline == nil) fprintf(stderr, " q5_k_cpy_q_to_f32");
            if (runtime->q5_k_cpy_f32_to_q_pipeline == nil) fprintf(stderr, " q5_k_cpy_f32_to_q");
            if (runtime->q6_k_get_rows_pipeline == nil) fprintf(stderr, " q6_k_get_rows");
            if (runtime->q6_k_set_rows_pipeline == nil) fprintf(stderr, " q6_k_set_rows");
            if (runtime->q6_k_cpy_q_to_f32_pipeline == nil) fprintf(stderr, " q6_k_cpy_q_to_f32");
            if (runtime->q6_k_cpy_f32_to_q_pipeline == nil) fprintf(stderr, " q6_k_cpy_f32_to_q");
            if (runtime->q8_0_get_rows_pipeline == nil) fprintf(stderr, " q8_0_get_rows");
            if (runtime->q8_0_set_rows_pipeline == nil) fprintf(stderr, " q8_0_set_rows");
            if (runtime->q8_0_cpy_q_to_f32_pipeline == nil) fprintf(stderr, " q8_0_cpy_q_to_f32");
            if (runtime->q8_0_cpy_f32_to_q_pipeline == nil) fprintf(stderr, " q8_0_cpy_f32_to_q");
            if (runtime->q8_1_get_rows_pipeline == nil) fprintf(stderr, " q8_1_get_rows");
            if (runtime->q8_1_set_rows_pipeline == nil) fprintf(stderr, " q8_1_set_rows");
            if (runtime->q8_1_cpy_q_to_f32_pipeline == nil) fprintf(stderr, " q8_1_cpy_q_to_f32");
            if (runtime->q8_1_cpy_f32_to_q_pipeline == nil) fprintf(stderr, " q8_1_cpy_f32_to_q");
            if (runtime->rope_pipeline == nil) fprintf(stderr, " rope");
            if (runtime->head_rms_rope_pipeline == nil) fprintf(stderr, " head_rms_rope");
            if (runtime->attention_f32_pipeline == nil) fprintf(stderr, " attention_f32");
            if (runtime->attention_f32_prefill_pipeline == nil) fprintf(stderr, " attention_f32_prefill");
            if (runtime->attention_paged_pipeline == nil) fprintf(stderr, " attention_paged");
            if (runtime->paged_f32_kv_seed_pipeline == nil) fprintf(stderr, " paged_f32_kv_seed");
            if (runtime->paged_f16_kv_seed_pipeline == nil) fprintf(stderr, " paged_f16_kv_seed");
            if (runtime->paged_f32_v_seed_pipeline == nil) fprintf(stderr, " paged_f32_v_seed");
            if (runtime->slice_last_dim_f32_2d_pipeline == nil) fprintf(stderr, " slice_last_dim_f32_2d");
            if (runtime->transpose_f32_pipeline == nil) fprintf(stderr, " transpose_f32");
            if (runtime->dot_general_2d_f32_pipeline == nil) fprintf(stderr, " dot_general_2d_f32");
            if (runtime->dot_general_batched_f32_pipeline == nil) fprintf(stderr, " dot_general_batched_f32");
            if (runtime->conv1d_f32_pipeline == nil) fprintf(stderr, " conv1d_f32");
            if (runtime->conv2d_f32_pipeline == nil) fprintf(stderr, " conv2d_f32");
            if (runtime->layer_norm_pipeline == nil) fprintf(stderr, " layer_norm");
            if (runtime->rms_norm_pipeline == nil) fprintf(stderr, " rms_norm");
            if (runtime->rms_norm_reduce_pipeline == nil) fprintf(stderr, " rms_norm_reduce");
            if (runtime->rms_norm_rows_pipeline == nil) fprintf(stderr, " rms_norm_rows");
            if (runtime->rms_norm_add_pipeline == nil) fprintf(stderr, " rms_norm_add");
            if (runtime->rms_norm_add_scale_pipeline == nil) fprintf(stderr, " rms_norm_add_scale");
            if (runtime->rms_norm_add_scale_rows_pipeline == nil) fprintf(stderr, " rms_norm_add_scale_rows");
            if (runtime->linear_pipeline == nil) fprintf(stderr, " linear");
            if (runtime->linear_reduce_pipeline == nil) fprintf(stderr, " linear_reduce");
            if (runtime->linear_bf16_pipeline == nil) fprintf(stderr, " linear_bf16");
            if (runtime->linear_bf16_reduce_pipeline == nil) fprintf(stderr, " linear_bf16_reduce");
            if (runtime->linear_bf16_multi_row_pipeline == nil) fprintf(stderr, " linear_bf16_multi_row");
            if (runtime->linear_bf16_multi_row_reduce_pipeline == nil) fprintf(stderr, " linear_bf16_multi_row_reduce");
            if (runtime->linear_pair_reduce_pipeline == nil) fprintf(stderr, " linear_pair_reduce");
            if (runtime->linear_multi_row_pipeline == nil) fprintf(stderr, " linear_multi_row");
            if (runtime->linear_multi_row_reduce_pipeline == nil) fprintf(stderr, " linear_multi_row_reduce");
            if (runtime->argmax_logits_pipeline == nil) fprintf(stderr, " argmax_logits");
            if (runtime->argmax_logits_partials_pipeline == nil) fprintf(stderr, " argmax_logits_partials");
            if (runtime->argmax_logits_reduce_pipeline == nil) fprintf(stderr, " argmax_logits_reduce");
            if (runtime->sample_logits_pipeline == nil) fprintf(stderr, " sample_logits");
            if (runtime->sample_topk_partials_pipeline == nil) fprintf(stderr, " sample_topk_partials");
            if (runtime->sample_topk_reduce_pipeline == nil) fprintf(stderr, " sample_topk_reduce");
            if (runtime->activation_pipeline == nil) fprintf(stderr, " activation");
            if (runtime->activation_multiply_pipeline == nil) fprintf(stderr, " activation_multiply");
            if (runtime->softmax_pipeline == nil) fprintf(stderr, " softmax");
            if (runtime->reduce_last_dim_pipeline == nil) fprintf(stderr, " reduce_last_dim");
            if (runtime->reduce_axis_f32_pipeline == nil) fprintf(stderr, " reduce_axis_f32");
            if (runtime->multiply_reduce_last_dim_pipeline == nil) fprintf(stderr, " multiply_reduce_last_dim");
            if (runtime->broadcast_last_dim_pipeline == nil) fprintf(stderr, " broadcast_last_dim");
            if (runtime->broadcast_f32_pipeline == nil) fprintf(stderr, " broadcast_f32");
            if (runtime->multiply_pipeline == nil) fprintf(stderr, " multiply");
            if (runtime->scale_pipeline == nil) fprintf(stderr, " scale");
            if (runtime->add_pipeline == nil) fprintf(stderr, " add");
            if (runtime->add_scale_pipeline == nil) fprintf(stderr, " add_scale");
            if (runtime->subtract_pipeline == nil) fprintf(stderr, " subtract");
            if (runtime->divide_pipeline == nil) fprintf(stderr, " divide");
            if (runtime->less_than_pipeline == nil) fprintf(stderr, " less_than");
            if (runtime->where_select_pipeline == nil) fprintf(stderr, " where_select");
            if (runtime->i2_s_quantize_pipeline == nil) fprintf(stderr, " i2_s_quantize");
            if (runtime->q1_0_pipeline == nil) fprintf(stderr, " q1_0");
            if (runtime->i8_s_pipeline == nil) fprintf(stderr, " i8_s");
            if (runtime->q2_k_pipeline == nil) fprintf(stderr, " q2_k");
            if (runtime->q3_k_pipeline == nil) fprintf(stderr, " q3_k");
            if (runtime->q4_k_pipeline == nil) fprintf(stderr, " q4_k");
            if (runtime->q4_k_reduce_pipeline == nil) fprintf(stderr, " q4_k_reduce");
            if (runtime->q4_k_pair_pipeline == nil) fprintf(stderr, " q4_k_pair");
            if (runtime->q4_0_pipeline == nil) fprintf(stderr, " q4_0");
            if (runtime->q4_0_pair_pipeline == nil) fprintf(stderr, " q4_0_pair");
            if (runtime->q4_0_reduce_pipeline == nil) fprintf(stderr, " q4_0_reduce");
            if (runtime->q4_1_pipeline == nil) fprintf(stderr, " q4_1");
            if (runtime->q4_1_reduce_pipeline == nil) fprintf(stderr, " q4_1_reduce");
            if (runtime->q5_0_pipeline == nil) fprintf(stderr, " q5_0");
            if (runtime->q5_0_reduce_pipeline == nil) fprintf(stderr, " q5_0_reduce");
            if (runtime->q5_1_pipeline == nil) fprintf(stderr, " q5_1");
            if (runtime->q5_1_reduce_pipeline == nil) fprintf(stderr, " q5_1_reduce");
            if (runtime->q8_0_pipeline == nil) fprintf(stderr, " q8_0");
            if (runtime->q8_0_pair_pipeline == nil) fprintf(stderr, " q8_0_pair");
            if (runtime->q8_0_mmv_pipeline == nil) fprintf(stderr, " q8_0_mmv");
            if (runtime->q8_0_rms_scale_mmv_pipeline == nil) fprintf(stderr, " q8_0_rms_scale_mmv");
            if (runtime->q8_0_mm_pipeline == nil && runtime->q8_0_mm_sg_pipeline == nil) fprintf(stderr, " q8_0_mm");
            if (runtime->q8_0_small_batch_pipeline == nil) fprintf(stderr, " q8_0_small_batch");
            if (runtime->q8_0_pair_mmv_pipeline == nil) fprintf(stderr, " q8_0_pair_mmv");
            if (runtime->q8_0_pair_small_batch_pipeline == nil) fprintf(stderr, " q8_0_pair_small_batch");
            if (runtime->q8_0_qkv_mmv_pipeline == nil) fprintf(stderr, " q8_0_qkv_mmv");
            if (runtime->q8_0_pair_activation_reduce_pipeline == nil) fprintf(stderr, " q8_0_pair_activation_reduce");
            if (runtime->q8_0_pair_activation_mmv_pipeline == nil) fprintf(stderr, " q8_0_pair_activation_mmv");
            if (runtime->q8_0_pair_activation_small_batch_pipeline == nil) fprintf(stderr, " q8_0_pair_activation_small_batch");
            if (runtime->q8_0_activation_multiply_reduce_pipeline == nil) fprintf(stderr, " q8_0_activation_multiply_reduce");
            if (runtime->q8_0_activation_multiply_mmv_pipeline == nil) fprintf(stderr, " q8_0_activation_multiply_mmv");
            if (runtime->q8_1_pipeline == nil) fprintf(stderr, " q8_1");
            if (runtime->q8_1_reduce_pipeline == nil) fprintf(stderr, " q8_1_reduce");
            if (runtime->q5_k_pipeline == nil) fprintf(stderr, " q5_k");
            if (runtime->q5_k_reduce_pipeline == nil) fprintf(stderr, " q5_k_reduce");
            if (runtime->q6_k_pipeline == nil) fprintf(stderr, " q6_k");
            if (runtime->q6_k_reduce_pipeline == nil) fprintf(stderr, " q6_k_reduce");
            if (runtime->q6_k_pair_pipeline == nil) fprintf(stderr, " q6_k_pair");
            if (runtime->q8_k_pipeline == nil) fprintf(stderr, " q8_k");
            if (runtime->q8_k_reduce_pipeline == nil) fprintf(stderr, " q8_k_reduce");
            if (runtime->iq4_nl_pipeline == nil) fprintf(stderr, " iq4_nl");
            if (runtime->iq4_nl_reduce_pipeline == nil) fprintf(stderr, " iq4_nl_reduce");
            if (runtime->iq4_xs_pipeline == nil) fprintf(stderr, " iq4_xs");
            if (runtime->iq4_xs_reduce_pipeline == nil) fprintf(stderr, " iq4_xs_reduce");
            if (runtime->mxfp4_pipeline == nil) fprintf(stderr, " mxfp4");
            if (runtime->mxfp4_reduce_pipeline == nil) fprintf(stderr, " mxfp4_reduce");
            if (runtime->nvfp4_pipeline == nil) fprintf(stderr, " nvfp4");
            if (runtime->iq2_xs_pipeline == nil) fprintf(stderr, " iq2_xs");
            if (runtime->i2_s_pipeline == nil) fprintf(stderr, " i2_s");
            if (runtime->i2_s_pair_pipeline == nil) fprintf(stderr, " i2_s_pair");
            if (runtime->i2_s_linear_i8_pipeline == nil) fprintf(stderr, " i2_s_linear_i8");
            if (runtime->i2_s_pair_i8_pipeline == nil) fprintf(stderr, " i2_s_pair_linear_i8");
            if (runtime->tl1_pipeline == nil) fprintf(stderr, " tl1");
            if (runtime->tl2_pipeline == nil) fprintf(stderr, " tl2");
            if (runtime->encode_polar4_key_pipeline == nil) fprintf(stderr, " encode_polar4_key");
            if (runtime->encode_turbo3_key_pipeline == nil) fprintf(stderr, " encode_turbo3_key");
            if (runtime->encode_f16_convert_pipeline == nil) fprintf(stderr, " encode_f16_convert");
            if (runtime->encode_int8_per_head_pipeline == nil) fprintf(stderr, " encode_int8_per_head");
            if (runtime->polar4_attention_span_pipeline == nil) fprintf(stderr, " polar4_attention_span");
            if (runtime->turbo3_attention_span_pipeline == nil) fprintf(stderr, " turbo3_attention_span");
            fprintf(stderr, "\n");
            free(runtime);
            return NULL;
        }
        if (runtime->layer_norm_add_pipeline == nil) {
            fprintf(stderr, "metal-runtime-create: pipeline=nil layer_norm_add\n");
            free(runtime);
            return NULL;
        }
        if (runtime->bias_activation_pipeline == nil || runtime->bias_add_layer_norm_pipeline == nil) {
            fprintf(stderr, "metal-runtime-create: pipeline=nil deberta_ffn\n");
            free(runtime);
            return NULL;
        }
        return runtime;
    }
}

void termite_metal_decode_runtime_destroy(termite_metal_decode_runtime *runtime) {
    if (runtime == NULL) return;
    @autoreleasepool {
    if (runtime->submitted_frame_cb != nil) {
        (void)termite_metal_decode_runtime_wait_frame(runtime);
    }
    if (runtime->active_frame_cb != nil) {
        if (termite_metal_decode_runtime_active_frame_empty(runtime)) {
            (void)termite_metal_decode_runtime_cancel_frame(runtime);
        } else if (termite_metal_decode_runtime_submit_frame(runtime) == 0) {
            (void)termite_metal_decode_runtime_wait_frame(runtime);
        }
    }
    termite_metal_decode_runtime_clear_frame_resources(&runtime->active_frame_retained_resources);
    termite_metal_decode_runtime_clear_frame_resources(&runtime->submitted_frame_retained_resources);
    runtime->embed_absolute_position_pipeline = nil;
    runtime->embedding_lookup_pipeline = nil;
    runtime->embedding_lookup_bf16_pipeline = nil;
    runtime->q4_0_get_rows_pipeline = nil;
    runtime->q4_0_set_rows_pipeline = nil;
    runtime->q4_0_cpy_q_to_f32_pipeline = nil;
    runtime->q4_0_cpy_f32_to_q_pipeline = nil;
    runtime->q4_1_get_rows_pipeline = nil;
    runtime->q4_1_set_rows_pipeline = nil;
    runtime->q4_1_cpy_q_to_f32_pipeline = nil;
    runtime->q4_1_cpy_f32_to_q_pipeline = nil;
    runtime->q5_0_get_rows_pipeline = nil;
    runtime->q5_0_set_rows_pipeline = nil;
    runtime->q5_0_cpy_q_to_f32_pipeline = nil;
    runtime->q5_0_cpy_f32_to_q_pipeline = nil;
    runtime->q5_1_get_rows_pipeline = nil;
    runtime->q5_1_set_rows_pipeline = nil;
    runtime->q5_1_cpy_q_to_f32_pipeline = nil;
    runtime->q5_1_cpy_f32_to_q_pipeline = nil;
    runtime->q4_k_get_rows_pipeline = nil;
    runtime->q4_k_set_rows_pipeline = nil;
    runtime->q4_k_cpy_q_to_f32_pipeline = nil;
    runtime->q4_k_cpy_f32_to_q_pipeline = nil;
    runtime->q5_k_get_rows_pipeline = nil;
    runtime->q5_k_set_rows_pipeline = nil;
    runtime->q5_k_cpy_q_to_f32_pipeline = nil;
    runtime->q5_k_cpy_f32_to_q_pipeline = nil;
    runtime->q6_k_get_rows_pipeline = nil;
    runtime->q6_k_set_rows_pipeline = nil;
    runtime->q6_k_cpy_q_to_f32_pipeline = nil;
    runtime->q6_k_cpy_f32_to_q_pipeline = nil;
    runtime->q8_0_get_rows_pipeline = nil;
    runtime->q8_0_set_rows_pipeline = nil;
    runtime->q8_0_cpy_q_to_f32_pipeline = nil;
    runtime->q8_0_cpy_f32_to_q_pipeline = nil;
    runtime->q8_1_get_rows_pipeline = nil;
    runtime->q8_1_set_rows_pipeline = nil;
    runtime->q8_1_cpy_q_to_f32_pipeline = nil;
    runtime->q8_1_cpy_f32_to_q_pipeline = nil;
    runtime->rope_pipeline = nil;
    runtime->head_rms_rope_pipeline = nil;
    runtime->attention_f32_pipeline = nil;
    runtime->attention_f32_prefill_pipeline = nil;
    runtime->attention_paged_pipeline = nil;
    runtime->paged_f32_kv_seed_pipeline = nil;
    runtime->paged_f16_kv_seed_pipeline = nil;
    runtime->paged_f32_v_seed_pipeline = nil;
    runtime->slice_last_dim_f32_2d_pipeline = nil;
    runtime->gather_axis0_f32_2d_pipeline = nil;
    runtime->concat_lastdim_f32_2d_pipeline = nil;
    runtime->deberta_embeddings_f32_pipeline = nil;
    runtime->gliner_word_embeddings_f32_pipeline = nil;
    runtime->gliner_gru_combine_f32_pipeline = nil;
    runtime->argmax_axis_f32_pipeline = nil;
    runtime->convert_dtype_f32_pipeline = nil;
    runtime->sdpa_f32_pipeline = nil;
    runtime->disentangled_relative_attention_f32_pipeline = nil;
    runtime->disentangled_relative_attention_f32_tg_pipeline = nil;
    runtime->disentangled_relative_attention_f32_flash4_pipeline = nil;
    runtime->deberta_relative_score_gemm_f32_pipeline = nil;
    runtime->deberta_relative_score_softmax_pv_f32_pipeline = nil;
    runtime->transpose_f32_pipeline = nil;
    runtime->dot_general_2d_f32_pipeline = nil;
    runtime->dot_general_batched_f32_pipeline = nil;
    runtime->conv1d_f32_pipeline = nil;
    runtime->conv2d_f32_pipeline = nil;
    runtime->layer_norm_pipeline = nil;
    runtime->layer_norm_rows_pipeline = nil;
    runtime->layer_norm_add_pipeline = nil;
    runtime->layer_norm_add_rows_pipeline = nil;
    runtime->bias_activation_pipeline = nil;
    runtime->bias_add_layer_norm_pipeline = nil;
    runtime->bias_add_layer_norm_rows_pipeline = nil;
    runtime->dense_qkv_split_pipeline = nil;
    runtime->dense_pair_split_pipeline = nil;
    runtime->dense_qkv_split_bias_pipeline = nil;
    runtime->dense_pair_split_bias_pipeline = nil;
    runtime->dense_qkv_direct_pipeline = nil;
    runtime->dense_pair_direct_pipeline = nil;
    runtime->rms_norm_pipeline = nil;
    runtime->rms_norm_reduce_pipeline = nil;
    runtime->rms_inv_scale_pipeline = nil;
    runtime->rms_norm_rows_pipeline = nil;
    runtime->rms_norm_add_pipeline = nil;
    runtime->rms_norm_add_scale_pipeline = nil;
    runtime->rms_norm_add_scale_rows_pipeline = nil;
    runtime->linear_pipeline = nil;
    runtime->linear_reduce_pipeline = nil;
    runtime->linear_bf16_pipeline = nil;
    runtime->linear_bf16_reduce_pipeline = nil;
    runtime->linear_bf16_multi_row_pipeline = nil;
    runtime->linear_bf16_multi_row_reduce_pipeline = nil;
    runtime->linear_bf16_multi_row_shared_reduce_pipeline = nil;
    runtime->linear_pair_reduce_pipeline = nil;
    runtime->linear_multi_row_pipeline = nil;
    runtime->linear_multi_row_reduce_pipeline = nil;
    runtime->linear_multi_row_tiled_f32_pipeline = nil;
    runtime->linear_multi_row_tiled_activation_f32_pipeline = nil;
    runtime->linear_multi_row_tiled32_f32_pipeline = nil;
    runtime->linear_multi_row_tiled32_activation_f32_pipeline = nil;
    runtime->linear_multi_row_tiled32_m16_f32_pipeline = nil;
    runtime->linear_multi_row_tiled32_m16_activation_f32_pipeline = nil;
    runtime->argmax_logits_pipeline = nil;
    runtime->argmax_logits_partials_pipeline = nil;
    runtime->argmax_logits_reduce_pipeline = nil;
    runtime->sample_logits_pipeline = nil;
    runtime->sample_topk_partials_pipeline = nil;
    runtime->sample_topk_reduce_pipeline = nil;
    runtime->activation_pipeline = nil;
    runtime->activation_multiply_pipeline = nil;
    runtime->softmax_pipeline = nil;
    runtime->reduce_last_dim_pipeline = nil;
    runtime->reduce_axis_f32_pipeline = nil;
    runtime->multiply_reduce_last_dim_pipeline = nil;
    runtime->broadcast_last_dim_pipeline = nil;
    runtime->broadcast_f32_pipeline = nil;
    runtime->multiply_pipeline = nil;
    runtime->scale_pipeline = nil;
    runtime->add_pipeline = nil;
    runtime->add_scale_pipeline = nil;
    runtime->scaled_add_scale_pipeline = nil;
    runtime->training_accumulate_pipeline = nil;
    runtime->training_adamw_pipeline = nil;
    runtime->training_sumsq_pipeline = nil;
    runtime->subtract_pipeline = nil;
    runtime->divide_pipeline = nil;
    runtime->less_than_pipeline = nil;
    runtime->where_select_pipeline = nil;
    runtime->i2_s_quantize_pipeline = nil;
    runtime->q1_0_pipeline = nil;
    runtime->i8_s_pipeline = nil;
    runtime->q2_k_pipeline = nil;
    runtime->q3_k_pipeline = nil;
    runtime->q4_k_pipeline = nil;
    runtime->q4_k_reduce_pipeline = nil;
    runtime->q4_k_pair_pipeline = nil;
    runtime->q4_0_pipeline = nil;
    runtime->q4_0_pair_pipeline = nil;
    runtime->q4_0_reduce_pipeline = nil;
    runtime->q4_1_pipeline = nil;
    runtime->q4_1_reduce_pipeline = nil;
    runtime->q5_0_pipeline = nil;
    runtime->q5_0_reduce_pipeline = nil;
    runtime->q5_1_pipeline = nil;
    runtime->q5_1_reduce_pipeline = nil;
    runtime->q8_0_pipeline = nil;
    runtime->q8_0_pair_pipeline = nil;
    runtime->q8_0_mmv_pipeline = nil;
    runtime->q8_0_mmv_f16_input_pipeline = nil;
    runtime->q8_0_rms_scale_mmv_pipeline = nil;
    runtime->q8_0_small_batch_r2_pipeline = nil;
    runtime->q8_0_small_batch_r3_pipeline = nil;
    runtime->q8_0_small_batch_r4_pipeline = nil;
    runtime->q8_0_small_batch_pipeline = nil;
    runtime->q8_0_mm_pipeline = nil;
    runtime->q8_0_mm_f16_input_pipeline = nil;
    runtime->q8_0_mm_sg_pipeline = nil;
    runtime->q8_0_pair_mmv_pipeline = nil;
    runtime->q8_0_pair_small_batch_r2_pipeline = nil;
    runtime->q8_0_pair_small_batch_r3_pipeline = nil;
    runtime->q8_0_pair_small_batch_r4_pipeline = nil;
    runtime->q8_0_pair_small_batch_pipeline = nil;
    runtime->q8_0_qkv_mmv_pipeline = nil;
    runtime->q8_0_pair_activation_reduce_pipeline = nil;
    runtime->q8_0_pair_activation_mmv_pipeline = nil;
    runtime->q8_0_pair_activation_rms_scale_mmv_pipeline = nil;
    runtime->q8_0_pair_activation_rms_scale_mmv_f16_output_pipeline = nil;
    runtime->q8_0_pair_activation_small_batch_r2_pipeline = nil;
    runtime->q8_0_pair_activation_small_batch_r3_pipeline = nil;
    runtime->q8_0_pair_activation_small_batch_r4_pipeline = nil;
    runtime->q8_0_pair_activation_small_batch_pipeline = nil;
    runtime->q8_0_pair_activation_mm_pipeline = nil;
    runtime->q8_0_pair_activation_mm_f16_output_pipeline = nil;
    runtime->q8_0_activation_multiply_reduce_pipeline = nil;
    runtime->q8_0_activation_multiply_mmv_pipeline = nil;
    runtime->q8_1_pipeline = nil;
    runtime->q8_1_reduce_pipeline = nil;
    runtime->q5_k_pipeline = nil;
    runtime->q5_k_reduce_pipeline = nil;
    runtime->q6_k_pipeline = nil;
    runtime->q6_k_reduce_pipeline = nil;
    runtime->q6_k_pair_pipeline = nil;
    runtime->q8_k_pipeline = nil;
    runtime->q8_k_reduce_pipeline = nil;
    runtime->iq4_nl_pipeline = nil;
    runtime->iq4_nl_reduce_pipeline = nil;
    runtime->iq4_xs_pipeline = nil;
    runtime->iq4_xs_reduce_pipeline = nil;
    runtime->mxfp4_pipeline = nil;
    runtime->mxfp4_reduce_pipeline = nil;
    runtime->nvfp4_pipeline = nil;
    runtime->nvfp4_reduce_pipeline = nil;
    runtime->iq2_xs_pipeline = nil;
    runtime->iq2_xs_reduce_pipeline = nil;
    runtime->i2_s_pipeline = nil;
    runtime->i2_s_pair_pipeline = nil;
    runtime->i2_s_linear_i8_pipeline = nil;
    runtime->i2_s_pair_i8_pipeline = nil;
    runtime->tl1_pipeline = nil;
    runtime->tl2_pipeline = nil;
    runtime->encode_polar4_key_pipeline = nil;
    runtime->encode_turbo3_key_pipeline = nil;
    runtime->encode_f16_convert_pipeline = nil;
    runtime->encode_int8_per_head_pipeline = nil;
    runtime->polar4_attention_span_pipeline = nil;
    runtime->turbo3_attention_span_pipeline = nil;
    runtime->token_embedding_table_buffer = nil;
    runtime->position_embedding_table_buffer = nil;
    runtime->generic_embedding_table_buffer = nil;
    for (size_t slot = 0; slot < TERMITE_METAL_GENERIC_EMBEDDING_CACHE_CAPACITY; ++slot) {
        runtime->generic_embedding_cache[slot].buffer = nil;
        runtime->generic_embedding_cache[slot].bytes = 0;
        runtime->generic_embedding_cache[slot].rows = 0;
        runtime->generic_embedding_cache[slot].dim = 0;
        runtime->generic_embedding_cache[slot].source_id = 0;
        runtime->generic_embedding_cache[slot].source_offset = 0;
    }
    for (size_t slot = 0; slot < TERMITE_METAL_LAYER_NORM_SLOT_CAPACITY; ++slot) {
        runtime->layer_norm_weight_buffers[slot] = nil;
        runtime->layer_norm_bias_buffers[slot] = nil;
        runtime->layer_norm_hidden_sizes[slot] = 0;
        runtime->layer_norm_slot_prepared[slot] = 0;
    }
    for (size_t slot = 0; slot < TERMITE_METAL_RMS_NORM_SLOT_CAPACITY; ++slot) {
        runtime->rms_norm_weight_buffers[slot] = nil;
        runtime->rms_norm_hidden_sizes[slot] = 0;
        runtime->rms_norm_slot_prepared[slot] = 0;
    }
    for (size_t slot = 0; slot < TERMITE_METAL_LINEAR_SLOT_CAPACITY; ++slot) {
        runtime->linear_weight_buffers[slot] = nil;
        runtime->linear_bias_buffers[slot] = nil;
        runtime->linear_in_dims[slot] = 0;
        runtime->linear_out_dims[slot] = 0;
        runtime->linear_slot_prepared[slot] = 0;
        runtime->linear_weight_dtypes[slot] = TERMITE_METAL_DENSE_LINEAR_DTYPE_F32;
        runtime->linear_mps_mm[slot] = nil;
        runtime->linear_mps_mm_rows[slot] = 0;
        runtime->dense_qkv_packed_weight_buffers[slot] = nil;
        runtime->dense_qkv_packed_bias_buffers[slot] = nil;
        runtime->dense_qkv_packed_mps_mm[slot] = nil;
        runtime->dense_qkv_packed_k_slots[slot] = SIZE_MAX;
        runtime->dense_qkv_packed_v_slots[slot] = SIZE_MAX;
        runtime->dense_qkv_packed_in_dims[slot] = 0;
        runtime->dense_qkv_packed_q_out_dims[slot] = 0;
        runtime->dense_qkv_packed_kv_out_dims[slot] = 0;
        runtime->dense_qkv_packed_mps_rows[slot] = 0;
        runtime->q8_qkv_packed_weight_buffers[slot] = nil;
        runtime->q8_qkv_packed_k_slots[slot] = SIZE_MAX;
        runtime->q8_qkv_packed_v_slots[slot] = SIZE_MAX;
        runtime->q8_qkv_packed_in_dims[slot] = 0;
        runtime->q8_qkv_packed_q_out_dims[slot] = 0;
        runtime->q8_qkv_packed_kv_out_dims[slot] = 0;
        runtime->q8_qkv_packed_row_stride_bytes[slot] = 0;
        runtime->dense_pair_packed_weight_buffers[slot] = nil;
        runtime->dense_pair_packed_bias_buffers[slot] = nil;
        runtime->dense_pair_packed_mps_mm[slot] = nil;
        runtime->dense_pair_packed_b_slots[slot] = SIZE_MAX;
        runtime->dense_pair_packed_in_dims[slot] = 0;
        runtime->dense_pair_packed_out_dims[slot] = 0;
        runtime->dense_pair_packed_mps_rows[slot] = 0;
        runtime->quant_linear_weight_buffers[slot] = nil;
        runtime->quant_linear_in_dims[slot] = 0;
        runtime->quant_linear_out_dims[slot] = 0;
        runtime->quant_linear_slot_prepared[slot] = 0;
        runtime->quant_linear_formats[slot] = TERMITE_METAL_QUANT_FORMAT_UNSUPPORTED;
        runtime->quant_linear_values_per_block[slot] = 0;
        runtime->quant_linear_bytes_per_block[slot] = 0;
        runtime->quant_linear_row_blocks[slot] = 0;
        runtime->quant_linear_row_stride_bytes[slot] = 0;
    }
    runtime->scratch_buffer = nil;
    runtime->host_staging_buffer = nil;
    runtime->active_frame_host_staging_buffer = nil;
    runtime->active_frame_host_staging_retained_buffer = nil;
    runtime->token_buffer = nil;
    runtime->sample_logits_buffer = nil;
    runtime->sample_topk_values_buffer = nil;
    runtime->sample_topk_ids_buffer = nil;
    for (size_t i = 0; i < 4; ++i) {
        runtime->hot_hidden_buffers[i] = nil;
    }
    for (size_t i = 0; i < 5; ++i) {
        runtime->hot_intermediate_buffers[i] = nil;
    }
    for (size_t i = 0; i < TERMITE_METAL_DIRECT_BLOCK_HIDDEN_BUFFER_COUNT; ++i) {
        runtime->direct_block_hidden_buffers[i] = nil;
    }
    for (size_t i = 0; i < TERMITE_METAL_DIRECT_BLOCK_SHARED_BUFFER_COUNT; ++i) {
        runtime->direct_block_shared_buffers[i] = nil;
        runtime->direct_block_shared_capacities[i] = 0;
    }
    runtime->direct_block_q_buffer = nil;
    for (size_t i = 0; i < 6; ++i) {
        runtime->active_layer_projection_buffers[i] = nil;
        runtime->active_layer_projection_capacities[i] = 0;
    }
    runtime->i2s_qinput_buffer = nil;
    runtime->i2s_scale_buffer = nil;
    runtime->attention_span_encoded_key_buffer = nil;
    runtime->attention_span_v_buffer = nil;
    for (size_t slot = 0; slot < TERMITE_METAL_ATTENTION_SPAN_SLOT_CAPACITY; ++slot) {
        runtime->attention_span_encoded_key_buffers[slot] = nil;
        runtime->attention_span_v_buffers[slot] = nil;
        runtime->attention_span_encoded_key_capacities[slot] = 0;
        runtime->attention_span_v_capacities[slot] = 0;
        runtime->attention_span_slot_tokens[slot] = 0;
        runtime->attention_span_slot_key_row_bytes[slot] = 0;
        runtime->attention_span_slot_v_row_stride[slot] = 0;
        runtime->attention_span_slot_position_offset[slot] = 0;
    }
    for (size_t slot = 0; slot < TERMITE_METAL_SCRATCH_POOL_CAPACITY; ++slot) {
        runtime->scratch_pool_buffers[slot] = nil;
        runtime->scratch_pool_capacities[slot] = 0;
        runtime->scratch_pool_in_use[slot] = 0;
        runtime->scratch_pool_pending_frame_release[slot] = 0;
    }
    for (size_t slot = 0; slot < TERMITE_METAL_GRAPH_PLAN_SLOT_CAPACITY; ++slot) {
        runtime->graph_plan_buffers[slot] = nil;
        runtime->graph_plan_capacities[slot] = 0;
        runtime->graph_plan_requested_bytes[slot] = 0;
    }
    runtime->graph_plan_active = 0;
    runtime->graph_plan_count = 0;
    runtime->graph_plan_allocations = 0;
    runtime->graph_plan_reuses = 0;
    termite_metal_mpsgraph_ffn_cache_clear(&runtime->mpsgraph_ffn_cache);
    runtime->mpsgraph_ffn_calls = 0;
    runtime->mpsgraph_ffn_fallbacks = 0;
    runtime->mpsgraph_ffn_compiles = 0;
    runtime->mpsgraph_ffn_cache_hits = 0;
    runtime->mps_dense_linear_standalone_calls = 0;
    runtime->mps_dense_linear_active_frame_calls = 0;
    runtime->mps_dense_linear_standalone_wait_nanos = 0;
    runtime->mps_dense_linear_standalone_gpu_nanos = 0;
    runtime->hidden_state_front = nil;
    runtime->hidden_state_back = nil;
    runtime->hidden_state_capacity = 0;
    runtime->hidden_state_max_rows = 0;
    runtime->hidden_state_hidden_size = 0;
    runtime->active_frame_cb = nil;
    runtime->submitted_frame_cb = nil;
    runtime->frame_cb_count = 0;
    runtime->last_frame_gpu_nanos = 0;
    runtime->compute_encoder_count = 0;
    runtime->blit_encoder_count = 0;
    runtime->active_frame_compute_encoder_count = 0;
    runtime->active_frame_blit_encoder_count = 0;
    runtime->active_frame_planned_compute_scope_count = 0;
    runtime->active_frame_planned_barrier_count = 0;
    runtime->active_frame_mps_dense_linear_count = 0;
    runtime->active_frame_planned_command_op_count = 0;
    runtime->submitted_frame_compute_encoder_count = 0;
    runtime->submitted_frame_blit_encoder_count = 0;
    runtime->submitted_frame_planned_compute_scope_count = 0;
    runtime->submitted_frame_planned_barrier_count = 0;
    runtime->submitted_frame_mps_dense_linear_count = 0;
    runtime->submitted_frame_planned_command_op_count = 0;
    runtime->last_frame_compute_encoder_count = 0;
    runtime->last_frame_blit_encoder_count = 0;
    runtime->last_frame_planned_compute_scope_count = 0;
    runtime->last_frame_planned_barrier_count = 0;
    runtime->last_frame_mps_dense_linear_count = 0;
    runtime->active_planned_compute_encoder = nil;
    termite_metal_decode_runtime_reset_planned_compute_ranges(runtime);
    runtime->active_planned_compute_source = TERMITE_METAL_COMPUTE_SOURCE_OTHER;
    runtime->active_planned_compute_region = TERMITE_METAL_COMPUTE_REGION_OTHER;
    runtime->active_compute_region = TERMITE_METAL_COMPUTE_REGION_OTHER;
    memset(runtime->active_frame_compute_source_counts, 0, sizeof(runtime->active_frame_compute_source_counts));
    memset(runtime->submitted_frame_compute_source_counts, 0, sizeof(runtime->submitted_frame_compute_source_counts));
    memset(runtime->last_frame_compute_source_counts, 0, sizeof(runtime->last_frame_compute_source_counts));
    memset(runtime->active_frame_compute_region_counts, 0, sizeof(runtime->active_frame_compute_region_counts));
    memset(runtime->submitted_frame_compute_region_counts, 0, sizeof(runtime->submitted_frame_compute_region_counts));
    memset(runtime->last_frame_compute_region_counts, 0, sizeof(runtime->last_frame_compute_region_counts));
    memset(runtime->active_frame_compute_region_source_counts, 0, sizeof(runtime->active_frame_compute_region_source_counts));
    memset(runtime->submitted_frame_compute_region_source_counts, 0, sizeof(runtime->submitted_frame_compute_region_source_counts));
    memset(runtime->last_frame_compute_region_source_counts, 0, sizeof(runtime->last_frame_compute_region_source_counts));
    memset(runtime->active_frame_blit_source_counts, 0, sizeof(runtime->active_frame_blit_source_counts));
    memset(runtime->submitted_frame_blit_source_counts, 0, sizeof(runtime->submitted_frame_blit_source_counts));
    memset(runtime->last_frame_blit_source_counts, 0, sizeof(runtime->last_frame_blit_source_counts));
    runtime->precise_library = nil;
    runtime->library = nil;
    runtime->queue = nil;
    runtime->device = nil;
    runtime->token_embedding_table_bytes = 0;
    runtime->position_embedding_table_bytes = 0;
    runtime->generic_embedding_table_bytes = 0;
    runtime->resident_vocab_size = 0;
    runtime->resident_max_position_embeddings = 0;
    runtime->resident_hidden_size = 0;
    runtime->generic_embedding_rows = 0;
    runtime->generic_embedding_dim = 0;
    runtime->generic_embedding_source_id = 0;
    runtime->generic_embedding_source_offset = 0;
    runtime->generic_embedding_cache_next = 0;
    runtime->scratch_capacity = 0;
    runtime->host_staging_capacity = 0;
    runtime->active_frame_host_staging_capacity = 0;
    runtime->active_frame_host_staging_offset = 0;
    runtime->token_capacity = 0;
    runtime->sample_logits_capacity = 0;
    runtime->sample_topk_capacity = 0;
    runtime->hot_hidden_capacity = 0;
    runtime->hot_intermediate_capacity = 0;
    runtime->direct_block_hidden_capacity = 0;
    runtime->direct_block_q_capacity = 0;
    for (size_t i = 0; i < 6; ++i) runtime->active_layer_projection_capacities[i] = 0;
    runtime->i2s_qinput_capacity = 0;
    runtime->i2s_scale_capacity = 0;
    runtime->attention_span_encoded_key_capacity = 0;
    runtime->attention_span_v_capacity = 0;
    runtime->attention_span_tokens = 0;
    runtime->attention_span_key_row_bytes = 0;
    runtime->attention_span_v_row_stride = 0;
    runtime->attention_span_position_offset = 0;
    free(runtime);
    }
}

int termite_metal_decode_runtime_prepare_absolute_embeddings(
    termite_metal_decode_runtime *runtime,
    const float *token_embedding,
    size_t vocab_size,
    const float *position_embedding,
    size_t max_position_embeddings,
    size_t hidden_size
) {
    if (runtime == NULL || token_embedding == NULL || position_embedding == NULL) return -1;
    if (runtime->device == nil || runtime->queue == nil || runtime->library == nil) return -2;
    if (vocab_size == 0 || max_position_embeddings == 0 || hidden_size == 0) return -3;
    @autoreleasepool {
        const size_t token_bytes = vocab_size * hidden_size * sizeof(float);
        const size_t position_bytes = max_position_embeddings * hidden_size * sizeof(float);
        id<MTLBuffer> token_embedding_buffer = termite_metal_make_private_buffer_with_bytes(runtime->device, runtime->queue, token_embedding, token_bytes);
        id<MTLBuffer> position_embedding_buffer = termite_metal_make_private_buffer_with_bytes(runtime->device, runtime->queue, position_embedding, position_bytes);
        if (token_embedding_buffer == nil || position_embedding_buffer == nil) return -4;
        runtime->token_embedding_table_buffer = token_embedding_buffer;
        runtime->position_embedding_table_buffer = position_embedding_buffer;
        runtime->token_embedding_table_bytes = token_bytes;
        runtime->position_embedding_table_bytes = position_bytes;
        runtime->resident_vocab_size = vocab_size;
        runtime->resident_max_position_embeddings = max_position_embeddings;
        runtime->resident_hidden_size = hidden_size;
        return 0;
    }
}

static int termite_metal_decode_runtime_ensure_active_layer_projection_buffer(
    termite_metal_decode_runtime *runtime,
    size_t index,
    size_t bytes
) {
    if (runtime == NULL || index >= 6) return -1;
    if (bytes == 0) return 0;
    if (runtime->graph_plan_active != 0 && index < TERMITE_METAL_GRAPH_PLAN_SLOT_CAPACITY) {
        if (runtime->graph_plan_buffers[index] != nil &&
            runtime->graph_plan_capacities[index] >= bytes)
        {
            runtime->active_layer_projection_buffers[index] = runtime->graph_plan_buffers[index];
            runtime->active_layer_projection_capacities[index] = runtime->graph_plan_capacities[index];
            return 0;
        }
    }
    @autoreleasepool {
        if (bytes > runtime->active_layer_projection_capacities[index] ||
            runtime->active_layer_projection_buffers[index] == nil)
        {
            id<MTLBuffer> buffer = [runtime->device newBufferWithLength:bytes options:MTLResourceStorageModePrivate];
            if (buffer == nil) return -2;
            runtime->active_layer_projection_buffers[index] = buffer;
            runtime->active_layer_projection_capacities[index] = bytes;
        }
        return 0;
    }
}

int termite_metal_decode_runtime_begin_graph_plan(termite_metal_decode_runtime *runtime) {
    if (runtime == NULL) return -1;
    for (size_t slot = 0; slot < TERMITE_METAL_GRAPH_PLAN_SLOT_CAPACITY; ++slot) {
        runtime->graph_plan_requested_bytes[slot] = 0;
    }
    runtime->graph_plan_active = 0;
    return 0;
}

int termite_metal_decode_runtime_reserve_graph_plan_slot(
    termite_metal_decode_runtime *runtime,
    size_t slot,
    size_t bytes
) {
    if (runtime == NULL || slot >= TERMITE_METAL_GRAPH_PLAN_SLOT_CAPACITY) return -1;
    if (bytes > runtime->graph_plan_requested_bytes[slot]) {
        runtime->graph_plan_requested_bytes[slot] = bytes;
    }
    return 0;
}

static size_t termite_metal_decode_runtime_graph_plan_allocation_bytes(size_t current_capacity, size_t requested_bytes) {
    if (requested_bytes == 0) return 0;
    size_t allocation_bytes = requested_bytes;
    if (current_capacity > 0) {
        if (current_capacity <= SIZE_MAX / 2u && current_capacity * 2u > allocation_bytes) {
            allocation_bytes = current_capacity * 2u;
        }
    } else if (requested_bytes <= SIZE_MAX / 2u) {
        allocation_bytes = requested_bytes * 2u;
    }
    return allocation_bytes;
}

int termite_metal_decode_runtime_commit_graph_plan(termite_metal_decode_runtime *runtime) {
    if (runtime == NULL || runtime->device == nil) return -1;
    @autoreleasepool {
        const char *trace_graph_plan = getenv("TERMITE_METAL_TRACE_GRAPH_PLAN");
        const bool trace_enabled = trace_graph_plan != NULL && trace_graph_plan[0] != '\0' && strcmp(trace_graph_plan, "0") != 0;
        size_t trace_slots = 0;
        size_t trace_bytes = 0;
        size_t trace_allocations = 0;
        size_t trace_reuses = 0;
        for (size_t slot = 0; slot < TERMITE_METAL_GRAPH_PLAN_SLOT_CAPACITY; ++slot) {
            const size_t bytes = runtime->graph_plan_requested_bytes[slot];
            if (bytes == 0) continue;
            trace_slots += 1;
            trace_bytes += bytes;
            if (runtime->graph_plan_buffers[slot] != nil &&
                runtime->graph_plan_capacities[slot] >= bytes)
            {
                runtime->graph_plan_reuses += 1;
                trace_reuses += 1;
                continue;
            }
            const size_t allocation_bytes = termite_metal_decode_runtime_graph_plan_allocation_bytes(runtime->graph_plan_capacities[slot], bytes);
            id<MTLBuffer> buffer = [runtime->device newBufferWithLength:allocation_bytes options:MTLResourceStorageModePrivate];
            if (buffer == nil) return -2;
            runtime->graph_plan_buffers[slot] = buffer;
            runtime->graph_plan_capacities[slot] = allocation_bytes;
            runtime->graph_plan_allocations += 1;
            trace_allocations += 1;
        }
        runtime->graph_plan_active = 1;
        runtime->graph_plan_count += 1;
        if (trace_enabled) {
            fprintf(
                stderr,
                "metal_graph_plan_commit: count=%llu slots=%zu bytes=%zu allocations=%zu reuses=%zu\n",
                (unsigned long long)runtime->graph_plan_count,
                trace_slots,
                trace_bytes,
                trace_allocations,
                trace_reuses
            );
            if (strcmp(trace_graph_plan, "all") == 0) {
                fprintf(stderr, "metal_graph_plan_slots:");
                for (size_t slot = 0; slot < TERMITE_METAL_GRAPH_PLAN_SLOT_CAPACITY; ++slot) {
                    const size_t bytes = runtime->graph_plan_requested_bytes[slot];
                    if (bytes == 0) continue;
                    fprintf(stderr, " %zu=%zu", slot, bytes);
                }
        fprintf(stderr, "\n");
    }
}

        return 0;
    }
}

static bool termite_metal_trace_dense_linear_shapes_enabled(void) {
    const char *enabled = getenv("TERMITE_METAL_TRACE_DENSE_LINEAR_SHAPES");
    return enabled != NULL && enabled[0] != '\0' && strcmp(enabled, "0") != 0;
}

static const char *termite_metal_dense_linear_variant(
    bool use_bf16,
    bool use_shared_row_reduce,
    bool use_tiled32_m16,
    bool use_tiled32,
    bool use_tiled,
    bool use_reduce,
    bool activation_fused
) {
    if (use_bf16) {
        if (use_shared_row_reduce) return "bf16_shared_row_reduce";
        return use_reduce ? "bf16_multi_row_reduce" : "bf16_multi_row";
    }
    if (use_tiled32_m16) return activation_fused ? "f32_tiled32_m16_activation" : "f32_tiled32_m16";
    if (use_tiled32) return activation_fused ? "f32_tiled32_activation" : "f32_tiled32";
    if (use_tiled) return activation_fused ? "f32_tiled16_activation" : "f32_tiled16";
    return use_reduce ? "f32_row_reduce" : "f32_elementwise";
}

static void termite_metal_maybe_trace_dense_linear_dispatch(
    const char *site,
    size_t slot,
    size_t rows,
    size_t in_dim,
    size_t out_dim,
    const char *variant,
    bool active_frame,
    bool planned_encoder,
    bool packed_weight,
    bool activation_fused,
    bool mps
) {
    if (!termite_metal_trace_dense_linear_shapes_enabled()) return;
    fprintf(
        stderr,
        "metal_dense_linear_trace: site=%s slot=%zu rows=%zu in=%zu out=%zu variant=%s active_frame=%u planned_encoder=%u packed_weight=%u activation_fused=%u mps=%u\n",
        site,
        slot,
        rows,
        in_dim,
        out_dim,
        variant,
        active_frame ? 1u : 0u,
        planned_encoder ? 1u : 0u,
        packed_weight ? 1u : 0u,
        activation_fused ? 1u : 0u,
        mps ? 1u : 0u
    );
}

static bool termite_metal_decode_runtime_graph_slot_ready(
    termite_metal_decode_runtime *runtime,
    size_t slot,
    size_t bytes
) {
    if (runtime == NULL || slot >= TERMITE_METAL_GRAPH_PLAN_SLOT_CAPACITY) return false;
    if (bytes == 0) return true;
    return runtime->graph_plan_active != 0 &&
        runtime->graph_plan_buffers[slot] != nil &&
        runtime->graph_plan_capacities[slot] >= bytes;
}

static bool termite_metal_decode_runtime_graph_alias(
    termite_metal_decode_runtime *runtime,
    id<MTLBuffer> buffer
) {
    if (runtime == NULL || buffer == nil) return false;
    for (size_t slot = 0; slot < TERMITE_METAL_GRAPH_PLAN_SLOT_CAPACITY; ++slot) {
        if (runtime->graph_plan_buffers[slot] == buffer) return true;
    }
    return false;
}

static int termite_metal_decode_runtime_ensure_graph_plan_slot(
    termite_metal_decode_runtime *runtime,
    size_t slot,
    size_t bytes
) {
    if (runtime == NULL || slot >= TERMITE_METAL_GRAPH_PLAN_SLOT_CAPACITY) return -1;
    if (bytes == 0) return 0;
    if (termite_metal_decode_runtime_graph_slot_ready(runtime, slot, bytes)) return 0;
    if (runtime->graph_plan_active == 0) {
        if (termite_metal_decode_runtime_begin_graph_plan(runtime) != 0) return -2;
    }
    if (termite_metal_decode_runtime_reserve_graph_plan_slot(runtime, slot, bytes) != 0) return -3;
    if (termite_metal_decode_runtime_commit_graph_plan(runtime) != 0) return -4;
    return termite_metal_decode_runtime_graph_slot_ready(runtime, slot, bytes) ? 0 : -5;
}

static int termite_metal_decode_runtime_reserve_graph_plan_slots(
    termite_metal_decode_runtime *runtime,
    const size_t *slots,
    const size_t *bytes,
    size_t count
) {
    if (runtime == NULL || slots == NULL || bytes == NULL) return -1;
    bool all_ready = true;
    for (size_t i = 0; i < count; ++i) {
        if (slots[i] >= TERMITE_METAL_GRAPH_PLAN_SLOT_CAPACITY) return -2;
        if (bytes[i] == 0) continue;
        if (!termite_metal_decode_runtime_graph_slot_ready(runtime, slots[i], bytes[i])) {
            all_ready = false;
            break;
        }
    }
    if (all_ready) return 0;
    if (runtime->graph_plan_active == 0) {
        if (termite_metal_decode_runtime_begin_graph_plan(runtime) != 0) return -3;
    }
    for (size_t i = 0; i < count; ++i) {
        if (bytes[i] == 0) continue;
        if (termite_metal_decode_runtime_reserve_graph_plan_slot(runtime, slots[i], bytes[i]) != 0) return -4;
    }
    if (termite_metal_decode_runtime_commit_graph_plan(runtime) != 0) return -5;
    for (size_t i = 0; i < count; ++i) {
        if (!termite_metal_decode_runtime_graph_slot_ready(runtime, slots[i], bytes[i])) return -6;
    }
    return 0;
}

int termite_metal_decode_runtime_reserve_prefill_layer_scratch(
    termite_metal_decode_runtime *runtime,
    size_t rows,
    size_t num_heads,
    size_t num_kv_heads,
    size_t head_dim,
    size_t hidden_size,
    size_t intermediate_size,
    size_t tail_vocab_size
) {
    if (runtime == NULL) return -1;
    if (rows == 0 || num_heads == 0 || num_kv_heads == 0 || head_dim == 0) return -2;
    const size_t q_bytes = rows * num_heads * head_dim * sizeof(float);
    const size_t kv_bytes = rows * num_kv_heads * head_dim * sizeof(float);
    const size_t hidden_bytes = hidden_size > 0 ? rows * hidden_size * sizeof(float) : 0;
    const size_t direct_hidden_bytes = q_bytes > hidden_bytes ? q_bytes : hidden_bytes;
    const size_t hot_hidden_bytes = direct_hidden_bytes;
    const size_t intermediate_bytes = intermediate_size > 0 ? rows * intermediate_size * sizeof(float) : 0;
    const size_t tail_logits_bytes = tail_vocab_size * sizeof(float);
    size_t slots[21] = {
        0,
        1,
        2,
        3,
        4,
        5,
        TERMITE_METAL_GRAPH_PLAN_DIRECT_HIDDEN_SLOT_BASE + 0,
        TERMITE_METAL_GRAPH_PLAN_DIRECT_HIDDEN_SLOT_BASE + 1,
        TERMITE_METAL_GRAPH_PLAN_DIRECT_HIDDEN_SLOT_BASE + 2,
        TERMITE_METAL_GRAPH_PLAN_DIRECT_HIDDEN_SLOT_BASE + 3,
        TERMITE_METAL_GRAPH_PLAN_DIRECT_Q_SLOT,
        TERMITE_METAL_GRAPH_PLAN_SAMPLE_LOGITS_SLOT,
        TERMITE_METAL_GRAPH_PLAN_HOT_HIDDEN_SLOT_BASE + 0,
        TERMITE_METAL_GRAPH_PLAN_HOT_HIDDEN_SLOT_BASE + 1,
        TERMITE_METAL_GRAPH_PLAN_HOT_HIDDEN_SLOT_BASE + 2,
        TERMITE_METAL_GRAPH_PLAN_HOT_HIDDEN_SLOT_BASE + 3,
        TERMITE_METAL_GRAPH_PLAN_HOT_INTERMEDIATE_SLOT_BASE + 0,
        TERMITE_METAL_GRAPH_PLAN_HOT_INTERMEDIATE_SLOT_BASE + 1,
        TERMITE_METAL_GRAPH_PLAN_HOT_INTERMEDIATE_SLOT_BASE + 2,
        TERMITE_METAL_GRAPH_PLAN_HOT_INTERMEDIATE_SLOT_BASE + 3,
        TERMITE_METAL_GRAPH_PLAN_HOT_INTERMEDIATE_SLOT_BASE + 4,
    };
    size_t bytes[21] = {
        q_bytes,
        kv_bytes,
        kv_bytes,
        q_bytes,
        kv_bytes,
        kv_bytes,
        direct_hidden_bytes,
        direct_hidden_bytes,
        direct_hidden_bytes,
        direct_hidden_bytes,
        q_bytes,
        tail_logits_bytes,
        hot_hidden_bytes,
        hot_hidden_bytes,
        hot_hidden_bytes,
        hot_hidden_bytes,
        intermediate_bytes,
        intermediate_bytes,
        intermediate_bytes,
        intermediate_bytes,
        intermediate_bytes,
    };
    if (termite_metal_decode_runtime_reserve_graph_plan_slots(runtime, slots, bytes, 21) != 0) return -3;
    if (termite_metal_decode_runtime_ensure_active_layer_projection_buffer(runtime, 0, q_bytes) != 0) return -3;
    if (termite_metal_decode_runtime_ensure_active_layer_projection_buffer(runtime, 1, kv_bytes) != 0) return -4;
    if (termite_metal_decode_runtime_ensure_active_layer_projection_buffer(runtime, 2, kv_bytes) != 0) return -5;
    if (termite_metal_decode_runtime_ensure_active_layer_projection_buffer(runtime, 3, q_bytes) != 0) return -6;
    if (termite_metal_decode_runtime_ensure_active_layer_projection_buffer(runtime, 4, kv_bytes) != 0) return -7;
    if (termite_metal_decode_runtime_ensure_active_layer_projection_buffer(runtime, 5, kv_bytes) != 0) return -8;
    if (termite_metal_decode_runtime_ensure_direct_block_hidden_buffers(runtime, direct_hidden_bytes) != 0) return -10;
    if (termite_metal_decode_runtime_ensure_direct_block_q_buffer(runtime, q_bytes) != 0) return -11;
    if (hot_hidden_bytes > 0 || intermediate_bytes > 0) {
        if (termite_metal_decode_runtime_ensure_hot_buffers(runtime, hot_hidden_bytes, intermediate_bytes) != 0) return -12;
    }
    if (tail_logits_bytes > 0) {
        runtime->sample_logits_buffer = runtime->graph_plan_buffers[TERMITE_METAL_GRAPH_PLAN_SAMPLE_LOGITS_SLOT];
        runtime->sample_logits_capacity = runtime->graph_plan_capacities[TERMITE_METAL_GRAPH_PLAN_SAMPLE_LOGITS_SLOT];
    }
    return 0;
}

int termite_metal_decode_runtime_reserve_gated_ffn_scratch(
    termite_metal_decode_runtime *runtime,
    size_t rows,
    size_t hidden_size,
    size_t intermediate_size
) {
    if (runtime == NULL) return -1;
    if (rows == 0 || hidden_size == 0 || intermediate_size == 0) return -2;
    const size_t hidden_bytes = rows * hidden_size * sizeof(float);
    const size_t intermediate_bytes = rows * intermediate_size * sizeof(float);
    size_t slots[9] = {
        TERMITE_METAL_GRAPH_PLAN_HOT_HIDDEN_SLOT_BASE + 0,
        TERMITE_METAL_GRAPH_PLAN_HOT_HIDDEN_SLOT_BASE + 1,
        TERMITE_METAL_GRAPH_PLAN_HOT_HIDDEN_SLOT_BASE + 2,
        TERMITE_METAL_GRAPH_PLAN_HOT_HIDDEN_SLOT_BASE + 3,
        TERMITE_METAL_GRAPH_PLAN_HOT_INTERMEDIATE_SLOT_BASE + 0,
        TERMITE_METAL_GRAPH_PLAN_HOT_INTERMEDIATE_SLOT_BASE + 1,
        TERMITE_METAL_GRAPH_PLAN_HOT_INTERMEDIATE_SLOT_BASE + 2,
        TERMITE_METAL_GRAPH_PLAN_HOT_INTERMEDIATE_SLOT_BASE + 3,
        TERMITE_METAL_GRAPH_PLAN_HOT_INTERMEDIATE_SLOT_BASE + 4,
    };
    size_t bytes[9] = {
        hidden_bytes,
        hidden_bytes,
        hidden_bytes,
        hidden_bytes,
        intermediate_bytes,
        intermediate_bytes,
        intermediate_bytes,
        intermediate_bytes,
        intermediate_bytes,
    };
    if (termite_metal_decode_runtime_reserve_graph_plan_slots(runtime, slots, bytes, 9) != 0) return -3;
    if (termite_metal_decode_runtime_ensure_hot_buffers(runtime, hidden_bytes, intermediate_bytes) != 0) return -4;
    return 0;
}

int termite_metal_decode_runtime_embed_absolute_position(
    termite_metal_decode_runtime *runtime,
    size_t token_id,
    size_t position_id,
    size_t hidden_size,
    float *output
) {
    if (runtime == NULL || output == NULL) return -1;
    if (runtime->embed_absolute_position_pipeline == nil) return -2;
    if (runtime->token_embedding_table_buffer == nil || runtime->position_embedding_table_buffer == nil) return -3;
    if (hidden_size == 0) return -4;
    if (hidden_size > UINT32_MAX || token_id > UINT32_MAX || position_id > UINT32_MAX) return -5;
    if (hidden_size != runtime->resident_hidden_size) return -6;
    if (token_id >= runtime->resident_vocab_size || position_id >= runtime->resident_max_position_embeddings) return -7;
    @autoreleasepool {
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithBytesNoCopy:output length:hidden_size * sizeof(float) options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) output_buffer = [runtime->device newBufferWithBytes:output length:hidden_size * sizeof(float) options:MTLResourceStorageModeShared];
        if (output_buffer == nil) return -8;
        termite_metal_embed_absolute_position_params params = {
            .token_id = (uint32_t)token_id,
            .position_id = (uint32_t)position_id,
            .hidden_size = (uint32_t)hidden_size,
        };
        const bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLCommandBuffer> command_buffer = frame_owned
            ? termite_metal_new_command_buffer(runtime->queue, __func__)
            : runtime->active_frame_cb;
        if (command_buffer == nil) return -10;
        if (!frame_owned && termite_metal_decode_runtime_retain_frame_resource(runtime, output_buffer) != 0) return -9;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_EMBEDDING);
        if (encoder == nil) return -11;
        [encoder setComputePipelineState:runtime->embed_absolute_position_pipeline];
        [encoder setBuffer:runtime->token_embedding_table_buffer offset:0 atIndex:0];
        [encoder setBuffer:runtime->position_embedding_table_buffer offset:0 atIndex:1];
        [encoder setBuffer:output_buffer offset:0 atIndex:2];
        [encoder setBytes:&params length:sizeof(params) atIndex:3];
        MTLSize grid_size = MTLSizeMake(hidden_size, 1, 1);
        NSUInteger thread_width = runtime->embed_absolute_position_pipeline.maxTotalThreadsPerThreadgroup;
        if (thread_width == 0) thread_width = 64;
        if (thread_width > hidden_size && hidden_size > 0) thread_width = hidden_size;
        MTLSize group_size = MTLSizeMake(thread_width, 1, 1);
        [encoder dispatchThreads:grid_size threadsPerThreadgroup:group_size];
        [encoder endEncoding];
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -12);
    }
}

static bool termite_metal_decode_runtime_select_embedding_cache(
    termite_metal_decode_runtime *runtime,
    uintptr_t source_id,
    size_t source_offset,
    size_t rows,
    size_t dim,
    size_t bytes
) {
    for (size_t slot = 0; slot < TERMITE_METAL_GENERIC_EMBEDDING_CACHE_CAPACITY; ++slot) {
        termite_metal_embedding_table_cache_entry *entry = &runtime->generic_embedding_cache[slot];
        if (entry->buffer != nil &&
            entry->source_id == source_id &&
            entry->source_offset == source_offset &&
            entry->rows == rows &&
            entry->dim == dim &&
            entry->bytes == bytes) {
            runtime->generic_embedding_table_buffer = entry->buffer;
            runtime->generic_embedding_table_bytes = entry->bytes;
            runtime->generic_embedding_rows = entry->rows;
            runtime->generic_embedding_dim = entry->dim;
            runtime->generic_embedding_source_id = entry->source_id;
            runtime->generic_embedding_source_offset = entry->source_offset;
            return true;
        }
    }
    return false;
}

static void termite_metal_decode_runtime_store_embedding_cache(
    termite_metal_decode_runtime *runtime,
    id<MTLBuffer> buffer,
    uintptr_t source_id,
    size_t source_offset,
    size_t rows,
    size_t dim,
    size_t bytes
) {
    size_t slot = TERMITE_METAL_GENERIC_EMBEDDING_CACHE_CAPACITY;
    for (size_t i = 0; i < TERMITE_METAL_GENERIC_EMBEDDING_CACHE_CAPACITY; ++i) {
        if (runtime->generic_embedding_cache[i].buffer == nil) {
            slot = i;
            break;
        }
    }
    if (slot == TERMITE_METAL_GENERIC_EMBEDDING_CACHE_CAPACITY) {
        slot = runtime->generic_embedding_cache_next % TERMITE_METAL_GENERIC_EMBEDDING_CACHE_CAPACITY;
        runtime->generic_embedding_cache_next = (slot + 1) % TERMITE_METAL_GENERIC_EMBEDDING_CACHE_CAPACITY;
    }

    termite_metal_embedding_table_cache_entry *entry = &runtime->generic_embedding_cache[slot];
    entry->buffer = buffer;
    entry->bytes = bytes;
    entry->rows = rows;
    entry->dim = dim;
    entry->source_id = source_id;
    entry->source_offset = source_offset;
    runtime->generic_embedding_table_buffer = buffer;
    runtime->generic_embedding_table_bytes = bytes;
    runtime->generic_embedding_rows = rows;
    runtime->generic_embedding_dim = dim;
    runtime->generic_embedding_source_id = source_id;
    runtime->generic_embedding_source_offset = source_offset;
}

int termite_metal_decode_runtime_prepare_embedding_table(
    termite_metal_decode_runtime *runtime,
    const float *weight,
    size_t rows,
    size_t dim
) {
    if (runtime == NULL || weight == NULL) return -1;
    if (runtime->device == nil || runtime->queue == nil) return -2;
    if (rows == 0 || dim == 0) return -3;
    const uintptr_t source_id = (uintptr_t)weight;
    const size_t bytes = rows * dim * sizeof(float);
    if (termite_metal_decode_runtime_select_embedding_cache(runtime, source_id, 0, rows, dim, bytes)) return 0;
    @autoreleasepool {
        id<MTLBuffer> buffer = termite_metal_make_private_buffer_with_bytes(runtime->device, runtime->queue, weight, bytes);
        if (buffer == nil) return -4;
        termite_metal_decode_runtime_store_embedding_cache(runtime, buffer, source_id, 0, rows, dim, bytes);
        return 0;
    }
}

int termite_metal_decode_runtime_prepare_embedding_table_bf16(
    termite_metal_decode_runtime *runtime,
    const uint8_t *weight,
    size_t weight_bytes,
    size_t rows,
    size_t dim
) {
    if (runtime == NULL || weight == NULL) return -1;
    if (runtime->device == nil || runtime->queue == nil) return -2;
    if (rows == 0 || dim == 0) return -3;
    const size_t expected_bytes = rows * dim * sizeof(uint16_t);
    if (weight_bytes < expected_bytes) return -4;
    const uintptr_t source_id = (uintptr_t)weight;
    if (termite_metal_decode_runtime_select_embedding_cache(runtime, source_id, 0, rows, dim, expected_bytes)) return 0;
    @autoreleasepool {
        id<MTLBuffer> buffer = termite_metal_make_private_buffer_with_bytes(runtime->device, runtime->queue, weight, expected_bytes);
        if (buffer == nil) return -5;
        termite_metal_decode_runtime_store_embedding_cache(runtime, buffer, source_id, 0, rows, dim, expected_bytes);
        return 0;
    }
}

int termite_metal_decode_runtime_prepare_embedding_table_device(
    termite_metal_decode_runtime *runtime,
    void *src_handle,
    size_t src_offset,
    size_t rows,
    size_t dim
) {
    if (runtime == NULL || src_handle == NULL) return -1;
    if (runtime->device == nil || runtime->queue == nil) return -2;
    if (rows == 0 || dim == 0) return -3;
    const uintptr_t source_id = (uintptr_t)src_handle;
    const size_t bytes = rows * dim * sizeof(float);
    if (termite_metal_decode_runtime_select_embedding_cache(runtime, source_id, src_offset, rows, dim, bytes)) return 0;
    @autoreleasepool {
        id<MTLBuffer> src = (__bridge id<MTLBuffer>)src_handle;
        if (src_offset + bytes > src.length) return -4;
        id<MTLBuffer> dst = [runtime->device newBufferWithLength:bytes options:MTLResourceStorageModePrivate];
        if (dst == nil) return -5;
        const bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLCommandBuffer> command_buffer = frame_owned
            ? termite_metal_new_command_buffer(runtime->queue, __func__)
            : runtime->active_frame_cb;
        if (command_buffer == nil) return -6;
        termite_metal_record_active_frame_blit_source(command_buffer, TERMITE_METAL_BLIT_SOURCE_EMBEDDING);
        id<MTLBlitCommandEncoder> blit = termite_metal_tracked_blit_command_encoder(command_buffer);
        if (blit == nil) return -7;
        [blit copyFromBuffer:src sourceOffset:src_offset toBuffer:dst destinationOffset:0 size:bytes];
        [blit endEncoding];
        if (termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -8) != 0) return -8;
        termite_metal_decode_runtime_store_embedding_cache(runtime, dst, source_id, src_offset, rows, dim, bytes);
        return 0;
    }
}

int termite_metal_decode_runtime_prepare_quant_embedding_table(
    termite_metal_decode_runtime *runtime,
    uint32_t format,
    const uint8_t *weight_raw,
    size_t weight_bytes,
    size_t rows,
    size_t dim
) {
    if (runtime == NULL || weight_raw == NULL) return -1;
    if (runtime->device == nil || runtime->queue == nil) return -2;
    size_t values_per_block = 0;
    size_t bytes_per_block = 0;
    if (!termite_metal_quant_format_block_layout(format, &values_per_block, &bytes_per_block)) return -3;
    if (termite_metal_decode_runtime_quant_get_rows_pipeline(runtime, format) == nil) return -4;
    if (rows == 0 || dim == 0 || values_per_block == 0 || dim % values_per_block != 0) return -5;
    const size_t expected_bytes = rows * (dim / values_per_block) * bytes_per_block;
    if (expected_bytes > weight_bytes) return -6;
    const uintptr_t source_id = (uintptr_t)weight_raw;
    if (termite_metal_decode_runtime_select_embedding_cache(runtime, source_id, 0, rows, dim, expected_bytes)) return 0;
    @autoreleasepool {
        id<MTLBuffer> buffer = termite_metal_make_private_buffer_with_bytes(runtime->device, runtime->queue, weight_raw, expected_bytes);
        if (buffer == nil) return -7;
        termite_metal_decode_runtime_store_embedding_cache(runtime, buffer, source_id, 0, rows, dim, expected_bytes);
        return 0;
    }
}

int termite_metal_decode_runtime_embedding_lookup_prepared(
    termite_metal_decode_runtime *runtime,
    const uint32_t *ids,
    size_t total,
    size_t dim,
    float *output
) {
    if (runtime == NULL || ids == NULL || output == NULL) return -1;
    if (runtime->embedding_lookup_pipeline == nil) return -2;
    if (runtime->generic_embedding_table_buffer == nil) return -3;
    if (total == 0 || dim == 0 || dim != runtime->generic_embedding_dim) return -4;
    @autoreleasepool {
        const size_t ids_bytes = total * sizeof(uint32_t);
        const size_t output_bytes = total * dim * sizeof(float);
        const bool frame_owned = (runtime->active_frame_cb == nil);
        termite_metal_host_staging_slice ids_slice = { nil, 0 };
        if (termite_metal_decode_runtime_stage_host_bytes(runtime, ids, ids_bytes, frame_owned, &ids_slice) != 0) return -5;
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithBytesNoCopy:output length:output_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) output_buffer = [runtime->device newBufferWithBytes:output length:output_bytes options:MTLResourceStorageModeShared];
        if (ids_slice.buffer == nil || output_buffer == nil) return -5;
        termite_metal_embedding_lookup_params params = {
            .total = (uint32_t)total,
            .dim = (uint32_t)dim,
        };
        id<MTLCommandBuffer> command_buffer = frame_owned
            ? termite_metal_new_command_buffer(runtime->queue, __func__)
            : runtime->active_frame_cb;
        if (command_buffer == nil) return -7;
        if (!frame_owned) {
            if (termite_metal_decode_runtime_retain_frame_resource(runtime, output_buffer) != 0) return -6;
        }
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_EMBEDDING);
        if (encoder == nil) return -8;
        [encoder setComputePipelineState:runtime->embedding_lookup_pipeline];
        [encoder setBuffer:runtime->generic_embedding_table_buffer offset:0 atIndex:0];
        [encoder setBuffer:ids_slice.buffer offset:ids_slice.offset atIndex:1];
        [encoder setBuffer:output_buffer offset:0 atIndex:2];
        [encoder setBytes:&params length:sizeof(params) atIndex:3];
        const size_t total_values = total * dim;
        MTLSize grid_size = MTLSizeMake(total_values, 1, 1);
        NSUInteger thread_width = runtime->embedding_lookup_pipeline.maxTotalThreadsPerThreadgroup;
        if (thread_width == 0) thread_width = 64;
        if (thread_width > total_values && total_values > 0) thread_width = total_values;
        MTLSize group_size = MTLSizeMake(thread_width, 1, 1);
        [encoder dispatchThreads:grid_size threadsPerThreadgroup:group_size];
        [encoder endEncoding];
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -9);
    }
}

int termite_metal_decode_runtime_embedding_lookup_prepared_device(
    termite_metal_decode_runtime *runtime,
    const uint32_t *ids,
    size_t total,
    size_t dim,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || ids == NULL || output_handle == NULL) return -1;
    if (runtime->embedding_lookup_pipeline == nil) return -2;
    if (runtime->generic_embedding_table_buffer == nil) return -3;
    if (total == 0 || dim == 0 || dim != runtime->generic_embedding_dim) return -4;
    @autoreleasepool {
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t output_bytes = total * dim * sizeof(float);
        if (output_offset + output_bytes > output_buffer.length) return -5;
        const bool frame_owned = (runtime->active_frame_cb == nil);
        const size_t ids_bytes = total * sizeof(uint32_t);
        termite_metal_host_staging_slice ids_slice = { nil, 0 };
        if (termite_metal_decode_runtime_stage_host_bytes(runtime, ids, ids_bytes, frame_owned, &ids_slice) != 0) return -6;
        termite_metal_embedding_lookup_params params = {
            .total = (uint32_t)total,
            .dim = (uint32_t)dim,
        };
        id<MTLCommandBuffer> command_buffer = frame_owned
            ? termite_metal_new_command_buffer(runtime->queue, __func__)
            : runtime->active_frame_cb;
        if (command_buffer == nil) return -8;
        id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
        const BOOL planned_encoder = (encoder != nil);
        if (!planned_encoder) {
            encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_EMBEDDING);
            if (encoder == nil) return -9;
        }
        termite_metal_planned_encoder_range accesses[3];
        if (termite_metal_planned_range_make(runtime->generic_embedding_table_buffer, 0, runtime->generic_embedding_table_buffer.length, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[0], -9) != 0 ||
            termite_metal_planned_range_make(ids_slice.buffer, ids_slice.offset, ids_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[1], -9) != 0 ||
            termite_metal_planned_range_make(output_buffer, output_offset, output_bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &accesses[2], -9) != 0 ||
            termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, accesses, 3, -9) != 0)
        {
            return -9;
        }
        [encoder setComputePipelineState:runtime->embedding_lookup_pipeline];
        [encoder setBuffer:runtime->generic_embedding_table_buffer offset:0 atIndex:0];
        [encoder setBuffer:ids_slice.buffer offset:ids_slice.offset atIndex:1];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:2];
        [encoder setBytes:&params length:sizeof(params) atIndex:3];
        const size_t total_values = total * dim;
        MTLSize grid_size = MTLSizeMake(total_values, 1, 1);
        NSUInteger thread_width = runtime->embedding_lookup_pipeline.maxTotalThreadsPerThreadgroup;
        if (thread_width == 0) thread_width = 64;
        if (thread_width > total_values && total_values > 0) thread_width = total_values;
        MTLSize group_size = MTLSizeMake(thread_width, 1, 1);
        [encoder dispatchThreads:grid_size threadsPerThreadgroup:group_size];
        if (!planned_encoder) [encoder endEncoding];
        if (frame_owned) {
            [command_buffer commit];
            [command_buffer waitUntilCompleted];
            return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -10;
        }
        return 0;
    }
}

int termite_metal_decode_runtime_embedding_lookup_direct_device(
    termite_metal_decode_runtime *runtime,
    void *weight_handle,
    size_t weight_offset,
    size_t rows,
    const uint32_t *ids,
    size_t total,
    size_t dim,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || weight_handle == NULL || ids == NULL || output_handle == NULL) return -1;
    if (runtime->embedding_lookup_pipeline == nil) return -2;
    if (total == 0 || dim == 0 || rows == 0) return -3;
    @autoreleasepool {
        id<MTLBuffer> weight_buffer = (__bridge id<MTLBuffer>)weight_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t weight_bytes = rows * dim * sizeof(float);
        const size_t output_bytes = total * dim * sizeof(float);
        if (weight_offset + weight_bytes > weight_buffer.length) return -4;
        if (output_offset + output_bytes > output_buffer.length) return -5;
        const bool frame_owned = (runtime->active_frame_cb == nil);
        const size_t ids_bytes = total * sizeof(uint32_t);
        termite_metal_host_staging_slice ids_slice = { nil, 0 };
        if (termite_metal_decode_runtime_stage_host_bytes(runtime, ids, ids_bytes, frame_owned, &ids_slice) != 0) return -6;
        termite_metal_embedding_lookup_params params = {
            .total = (uint32_t)total,
            .dim = (uint32_t)dim,
        };
        id<MTLCommandBuffer> command_buffer = frame_owned
            ? termite_metal_new_command_buffer(runtime->queue, __func__)
            : runtime->active_frame_cb;
        if (command_buffer == nil) return -8;
        id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
        const BOOL planned_encoder = (encoder != nil);
        if (!planned_encoder) {
            encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_EMBEDDING);
            if (encoder == nil) return -9;
        }
        termite_metal_planned_encoder_range accesses[3];
        if (termite_metal_planned_range_make(weight_buffer, weight_offset, weight_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[0], -9) != 0 ||
            termite_metal_planned_range_make(ids_slice.buffer, ids_slice.offset, ids_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[1], -9) != 0 ||
            termite_metal_planned_range_make(output_buffer, output_offset, output_bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &accesses[2], -9) != 0 ||
            termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, accesses, 3, -9) != 0)
        {
            return -9;
        }
        [encoder setComputePipelineState:runtime->embedding_lookup_pipeline];
        [encoder setBuffer:weight_buffer offset:weight_offset atIndex:0];
        [encoder setBuffer:ids_slice.buffer offset:ids_slice.offset atIndex:1];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:2];
        [encoder setBytes:&params length:sizeof(params) atIndex:3];
        const size_t total_values = total * dim;
        MTLSize grid_size = MTLSizeMake(total_values, 1, 1);
        NSUInteger thread_width = runtime->embedding_lookup_pipeline.maxTotalThreadsPerThreadgroup;
        if (thread_width == 0) thread_width = 64;
        if (thread_width > total_values && total_values > 0) thread_width = total_values;
        MTLSize group_size = MTLSizeMake(thread_width, 1, 1);
        [encoder dispatchThreads:grid_size threadsPerThreadgroup:group_size];
        if (!planned_encoder) [encoder endEncoding];
        if (frame_owned) {
            [command_buffer commit];
            [command_buffer waitUntilCompleted];
            return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -10;
        }
        return 0;
    }
}

int termite_metal_decode_runtime_deberta_embeddings_f32_device(
    termite_metal_decode_runtime *runtime,
    void *weight_handle,
    size_t weight_offset,
    size_t rows,
    void *gamma_handle,
    size_t gamma_offset,
    void *beta_handle,
    size_t beta_offset,
    const uint32_t *ids,
    const uint32_t *mask,
    size_t total,
    size_t dim,
    float eps,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || weight_handle == NULL || gamma_handle == NULL || beta_handle == NULL || ids == NULL || mask == NULL || output_handle == NULL) return -1;
    if (runtime->deberta_embeddings_f32_pipeline == nil) return -2;
    if (total == 0 || dim == 0 || rows == 0) return -3;
    @autoreleasepool {
        id<MTLBuffer> weight_buffer = (__bridge id<MTLBuffer>)weight_handle;
        id<MTLBuffer> gamma_buffer = (__bridge id<MTLBuffer>)gamma_handle;
        id<MTLBuffer> beta_buffer = (__bridge id<MTLBuffer>)beta_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t weight_bytes = rows * dim * sizeof(float);
        const size_t scale_bytes = dim * sizeof(float);
        const size_t ids_bytes = total * sizeof(uint32_t);
        const size_t output_bytes = total * dim * sizeof(float);
        if (weight_offset + weight_bytes > weight_buffer.length) return -4;
        if (gamma_offset + scale_bytes > gamma_buffer.length) return -5;
        if (beta_offset + scale_bytes > beta_buffer.length) return -6;
        if (output_offset + output_bytes > output_buffer.length) return -7;
        const bool frame_owned = (runtime->active_frame_cb == nil);
        termite_metal_host_staging_slice ids_slice = { nil, 0 };
        termite_metal_host_staging_slice mask_slice = { nil, 0 };
        if (termite_metal_decode_runtime_stage_host_bytes(runtime, ids, ids_bytes, frame_owned, &ids_slice) != 0) return -8;
        if (termite_metal_decode_runtime_stage_host_bytes(runtime, mask, ids_bytes, frame_owned, &mask_slice) != 0) return -9;
        termite_metal_deberta_embeddings_f32_params params = {
            .total = (uint32_t)total,
            .rows = (uint32_t)rows,
            .dim = (uint32_t)dim,
            .eps = eps,
        };
        id<MTLCommandBuffer> command_buffer = frame_owned
            ? termite_metal_new_command_buffer(runtime->queue, __func__)
            : runtime->active_frame_cb;
        if (command_buffer == nil) return -10;
        id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
        const BOOL planned_encoder = (encoder != nil);
        if (!planned_encoder) {
            encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_EMBEDDING);
            if (encoder == nil) return -11;
        }
        termite_metal_planned_encoder_range accesses[6];
        if (termite_metal_planned_range_make(weight_buffer, weight_offset, weight_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[0], -11) != 0 ||
            termite_metal_planned_range_make(gamma_buffer, gamma_offset, scale_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[1], -11) != 0 ||
            termite_metal_planned_range_make(beta_buffer, beta_offset, scale_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[2], -11) != 0 ||
            termite_metal_planned_range_make(ids_slice.buffer, ids_slice.offset, ids_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[3], -11) != 0 ||
            termite_metal_planned_range_make(mask_slice.buffer, mask_slice.offset, ids_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[4], -11) != 0 ||
            termite_metal_planned_range_make(output_buffer, output_offset, output_bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &accesses[5], -11) != 0 ||
            termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, accesses, 6, -11) != 0)
        {
            return -11;
        }
        [encoder setComputePipelineState:runtime->deberta_embeddings_f32_pipeline];
        [encoder setBuffer:weight_buffer offset:weight_offset atIndex:0];
        [encoder setBuffer:gamma_buffer offset:gamma_offset atIndex:1];
        [encoder setBuffer:beta_buffer offset:beta_offset atIndex:2];
        [encoder setBuffer:ids_slice.buffer offset:ids_slice.offset atIndex:3];
        [encoder setBuffer:mask_slice.buffer offset:mask_slice.offset atIndex:4];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:5];
        [encoder setBytes:&params length:sizeof(params) atIndex:6];
        [encoder dispatchThreadgroups:MTLSizeMake(total, 1, 1) threadsPerThreadgroup:MTLSizeMake(256, 1, 1)];
        if (!planned_encoder) [encoder endEncoding];
        if (frame_owned) {
            [command_buffer commit];
            [command_buffer waitUntilCompleted];
            return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -12;
        }
        return 0;
    }
}

int termite_metal_decode_runtime_deberta_embeddings_f32(
    termite_metal_decode_runtime *runtime,
    const float *weight,
    size_t rows,
    const float *gamma,
    const float *beta,
    const uint32_t *ids,
    const uint32_t *mask,
    size_t total,
    size_t dim,
    float eps,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || weight == NULL || gamma == NULL || beta == NULL || ids == NULL || mask == NULL || output_handle == NULL) return -1;
    if (runtime->deberta_embeddings_f32_pipeline == nil) return -2;
    if (total == 0 || dim == 0 || rows == 0) return -3;
    @autoreleasepool {
        const int prep_rc = termite_metal_decode_runtime_prepare_embedding_table(runtime, weight, rows, dim);
        if (prep_rc != 0 || runtime->generic_embedding_table_buffer == nil) return -4;
        id<MTLBuffer> weight_buffer = runtime->generic_embedding_table_buffer;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t weight_bytes = rows * dim * sizeof(float);
        const size_t scale_bytes = dim * sizeof(float);
        const size_t ids_bytes = total * sizeof(uint32_t);
        const size_t output_bytes = total * dim * sizeof(float);
        if (weight_bytes > weight_buffer.length) return -5;
        if (output_offset + output_bytes > output_buffer.length) return -6;
        id<MTLBuffer> gamma_buffer = [runtime->device newBufferWithBytes:gamma length:scale_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> beta_buffer = [runtime->device newBufferWithBytes:beta length:scale_bytes options:MTLResourceStorageModeShared];
        if (gamma_buffer == nil || beta_buffer == nil) return -7;
        const bool frame_owned = (runtime->active_frame_cb == nil);
        if (!frame_owned) {
            if (termite_metal_decode_runtime_retain_frame_resource(runtime, gamma_buffer) != 0 ||
                termite_metal_decode_runtime_retain_frame_resource(runtime, beta_buffer) != 0)
            {
                return -8;
            }
        }
        termite_metal_host_staging_slice ids_slice = { nil, 0 };
        termite_metal_host_staging_slice mask_slice = { nil, 0 };
        if (termite_metal_decode_runtime_stage_host_bytes(runtime, ids, ids_bytes, frame_owned, &ids_slice) != 0) return -9;
        if (termite_metal_decode_runtime_stage_host_bytes(runtime, mask, ids_bytes, frame_owned, &mask_slice) != 0) return -10;
        termite_metal_deberta_embeddings_f32_params params = {
            .total = (uint32_t)total,
            .rows = (uint32_t)rows,
            .dim = (uint32_t)dim,
            .eps = eps,
        };
        id<MTLCommandBuffer> command_buffer = frame_owned
            ? termite_metal_new_command_buffer(runtime->queue, __func__)
            : runtime->active_frame_cb;
        if (command_buffer == nil) return -11;
        id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
        const BOOL planned_encoder = (encoder != nil);
        if (!planned_encoder) {
            encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_EMBEDDING);
            if (encoder == nil) return -12;
        }
        termite_metal_planned_encoder_range accesses[6];
        if (termite_metal_planned_range_make(weight_buffer, 0, weight_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[0], -13) != 0 ||
            termite_metal_planned_range_make(gamma_buffer, 0, scale_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[1], -13) != 0 ||
            termite_metal_planned_range_make(beta_buffer, 0, scale_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[2], -13) != 0 ||
            termite_metal_planned_range_make(ids_slice.buffer, ids_slice.offset, ids_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[3], -13) != 0 ||
            termite_metal_planned_range_make(mask_slice.buffer, mask_slice.offset, ids_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[4], -13) != 0 ||
            termite_metal_planned_range_make(output_buffer, output_offset, output_bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &accesses[5], -13) != 0 ||
            termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, accesses, 6, -13) != 0)
        {
            return -13;
        }
        [encoder setComputePipelineState:runtime->deberta_embeddings_f32_pipeline];
        [encoder setBuffer:weight_buffer offset:0 atIndex:0];
        [encoder setBuffer:gamma_buffer offset:0 atIndex:1];
        [encoder setBuffer:beta_buffer offset:0 atIndex:2];
        [encoder setBuffer:ids_slice.buffer offset:ids_slice.offset atIndex:3];
        [encoder setBuffer:mask_slice.buffer offset:mask_slice.offset atIndex:4];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:5];
        [encoder setBytes:&params length:sizeof(params) atIndex:6];
        [encoder dispatchThreadgroups:MTLSizeMake(total, 1, 1) threadsPerThreadgroup:MTLSizeMake(256, 1, 1)];
        if (!planned_encoder) [encoder endEncoding];
        if (frame_owned) {
            [command_buffer commit];
            [command_buffer waitUntilCompleted];
            return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -14;
        }
        return 0;
    }
}

int termite_metal_decode_runtime_embedding_lookup_bf16_prepared_device(
    termite_metal_decode_runtime *runtime,
    const uint32_t *ids,
    size_t total,
    size_t dim,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || ids == NULL || output_handle == NULL) return -1;
    if (runtime->embedding_lookup_bf16_pipeline == nil) return -2;
    if (runtime->generic_embedding_table_buffer == nil) return -3;
    if (total == 0 || dim == 0 || dim != runtime->generic_embedding_dim) return -4;
    @autoreleasepool {
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t output_bytes = total * dim * sizeof(float);
        if (output_offset + output_bytes > output_buffer.length) return -5;
        const bool frame_owned = (runtime->active_frame_cb == nil);
        const size_t ids_bytes = total * sizeof(uint32_t);
        termite_metal_host_staging_slice ids_slice = { nil, 0 };
        if (termite_metal_decode_runtime_stage_host_bytes(runtime, ids, ids_bytes, frame_owned, &ids_slice) != 0) return -6;
        termite_metal_embedding_lookup_params params = {
            .total = (uint32_t)total,
            .dim = (uint32_t)dim,
        };
        id<MTLCommandBuffer> command_buffer = frame_owned
            ? termite_metal_new_command_buffer(runtime->queue, __func__)
            : runtime->active_frame_cb;
        if (command_buffer == nil) return -8;
        id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
        const BOOL planned_encoder = (encoder != nil);
        if (!planned_encoder) {
            encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_EMBEDDING);
            if (encoder == nil) return -9;
        }
        termite_metal_planned_encoder_range accesses[3];
        if (termite_metal_planned_range_make(runtime->generic_embedding_table_buffer, 0, runtime->generic_embedding_table_buffer.length, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[0], -9) != 0 ||
            termite_metal_planned_range_make(ids_slice.buffer, ids_slice.offset, ids_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[1], -9) != 0 ||
            termite_metal_planned_range_make(output_buffer, output_offset, output_bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &accesses[2], -9) != 0 ||
            termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, accesses, 3, -9) != 0)
        {
            return -9;
        }
        [encoder setComputePipelineState:runtime->embedding_lookup_bf16_pipeline];
        [encoder setBuffer:runtime->generic_embedding_table_buffer offset:0 atIndex:0];
        [encoder setBuffer:ids_slice.buffer offset:ids_slice.offset atIndex:1];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:2];
        [encoder setBytes:&params length:sizeof(params) atIndex:3];
        const size_t total_values = total * dim;
        MTLSize grid_size = MTLSizeMake(total_values, 1, 1);
        NSUInteger thread_width = runtime->embedding_lookup_bf16_pipeline.maxTotalThreadsPerThreadgroup;
        if (thread_width == 0) thread_width = 64;
        if (thread_width > total_values && total_values > 0) thread_width = total_values;
        MTLSize group_size = MTLSizeMake(thread_width, 1, 1);
        [encoder dispatchThreads:grid_size threadsPerThreadgroup:group_size];
        if (!planned_encoder) [encoder endEncoding];
        if (frame_owned) {
            [command_buffer commit];
            [command_buffer waitUntilCompleted];
            return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -10;
        }
        return 0;
    }
}

int termite_metal_decode_runtime_quant_embedding_lookup_prepared_device(
    termite_metal_decode_runtime *runtime,
    uint32_t format,
    const uint32_t *ids,
    size_t total,
    size_t dim,
    float scale,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || ids == NULL || output_handle == NULL) return -1;
    id<MTLComputePipelineState> pipeline = termite_metal_decode_runtime_quant_get_rows_pipeline(runtime, format);
    if (pipeline == nil) return -2;
    if (runtime->generic_embedding_table_buffer == nil) return -3;
    size_t values_per_block = 0;
    size_t bytes_per_block = 0;
    if (!termite_metal_quant_format_block_layout(format, &values_per_block, &bytes_per_block)) return -4;
    if (total == 0 || dim == 0 || values_per_block == 0 || dim % values_per_block != 0 || dim != runtime->generic_embedding_dim) return -5;
    if (runtime->generic_embedding_rows == 0) return -6;
    @autoreleasepool {
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t output_bytes = total * dim * sizeof(float);
        if (output_offset + output_bytes > output_buffer.length) return -7;
        const bool frame_owned = (runtime->active_frame_cb == nil);
        const size_t ids_bytes = total * sizeof(uint32_t);
        termite_metal_host_staging_slice ids_slice = { nil, 0 };
        if (termite_metal_decode_runtime_stage_host_bytes(runtime, ids, ids_bytes, frame_owned, &ids_slice) != 0) return -8;
        termite_metal_quant_rows_params params = {
            .total = (uint32_t)total,
            .dim = (uint32_t)dim,
            .source_rows = (uint32_t)runtime->generic_embedding_rows,
            .row_offset = 0,
            .scale = scale,
        };
        id<MTLCommandBuffer> command_buffer = frame_owned
            ? termite_metal_new_command_buffer(runtime->queue, __func__)
            : runtime->active_frame_cb;
        if (command_buffer == nil) return -9;
        id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
        const BOOL planned_encoder = (encoder != nil);
        if (!planned_encoder) {
            encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_EMBEDDING);
            if (encoder == nil) return -10;
        }
        termite_metal_planned_encoder_range accesses[3];
        if (termite_metal_planned_range_make(runtime->generic_embedding_table_buffer, 0, runtime->generic_embedding_table_buffer.length, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[0], -10) != 0 ||
            termite_metal_planned_range_make(ids_slice.buffer, ids_slice.offset, ids_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[1], -10) != 0 ||
            termite_metal_planned_range_make(output_buffer, output_offset, output_bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &accesses[2], -10) != 0 ||
            termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, accesses, 3, -10) != 0)
        {
            return -10;
        }
        [encoder setComputePipelineState:pipeline];
        [encoder setBuffer:runtime->generic_embedding_table_buffer offset:0 atIndex:0];
        [encoder setBuffer:ids_slice.buffer offset:ids_slice.offset atIndex:1];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:2];
        [encoder setBytes:&params length:sizeof(params) atIndex:3];
        const size_t total_values = total * dim;
        [encoder dispatchThreads:MTLSizeMake(total_values, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(pipeline, total_values), 1, 1)];
        if (!planned_encoder) [encoder endEncoding];
        if (frame_owned) {
            [command_buffer commit];
            [command_buffer waitUntilCompleted];
            return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -11;
        }
        return 0;
    }
}

static id<MTLComputePipelineState> termite_metal_decode_runtime_quant_get_rows_pipeline(
    termite_metal_decode_runtime *runtime,
    uint32_t format
) {
    if (runtime == NULL) return nil;
    switch (format) {
        case TERMITE_METAL_QUANT_FORMAT_Q4_0: return runtime->q4_0_get_rows_pipeline;
        case TERMITE_METAL_QUANT_FORMAT_Q4_1: return runtime->q4_1_get_rows_pipeline;
        case TERMITE_METAL_QUANT_FORMAT_Q5_0: return runtime->q5_0_get_rows_pipeline;
        case TERMITE_METAL_QUANT_FORMAT_Q5_1: return runtime->q5_1_get_rows_pipeline;
        case TERMITE_METAL_QUANT_FORMAT_Q4_K: return runtime->q4_k_get_rows_pipeline;
        case TERMITE_METAL_QUANT_FORMAT_Q5_K: return runtime->q5_k_get_rows_pipeline;
        case TERMITE_METAL_QUANT_FORMAT_Q6_K: return runtime->q6_k_get_rows_pipeline;
        case TERMITE_METAL_QUANT_FORMAT_Q8_0: return runtime->q8_0_get_rows_pipeline;
        case TERMITE_METAL_QUANT_FORMAT_Q8_1: return runtime->q8_1_get_rows_pipeline;
        default: return nil;
    }
}

static id<MTLComputePipelineState> termite_metal_decode_runtime_quant_set_rows_pipeline(
    termite_metal_decode_runtime *runtime,
    uint32_t format
) {
    if (runtime == NULL) return nil;
    switch (format) {
        case TERMITE_METAL_QUANT_FORMAT_Q4_0: return runtime->q4_0_set_rows_pipeline;
        case TERMITE_METAL_QUANT_FORMAT_Q4_1: return runtime->q4_1_set_rows_pipeline;
        case TERMITE_METAL_QUANT_FORMAT_Q5_0: return runtime->q5_0_set_rows_pipeline;
        case TERMITE_METAL_QUANT_FORMAT_Q5_1: return runtime->q5_1_set_rows_pipeline;
        case TERMITE_METAL_QUANT_FORMAT_Q4_K: return runtime->q4_k_set_rows_pipeline;
        case TERMITE_METAL_QUANT_FORMAT_Q5_K: return runtime->q5_k_set_rows_pipeline;
        case TERMITE_METAL_QUANT_FORMAT_Q6_K: return runtime->q6_k_set_rows_pipeline;
        case TERMITE_METAL_QUANT_FORMAT_Q8_0: return runtime->q8_0_set_rows_pipeline;
        case TERMITE_METAL_QUANT_FORMAT_Q8_1: return runtime->q8_1_set_rows_pipeline;
        default: return nil;
    }
}

static id<MTLComputePipelineState> termite_metal_decode_runtime_quant_cpy_q_to_f32_pipeline(
    termite_metal_decode_runtime *runtime,
    uint32_t format
) {
    if (runtime == NULL) return nil;
    switch (format) {
        case TERMITE_METAL_QUANT_FORMAT_Q4_0: return runtime->q4_0_cpy_q_to_f32_pipeline;
        case TERMITE_METAL_QUANT_FORMAT_Q4_1: return runtime->q4_1_cpy_q_to_f32_pipeline;
        case TERMITE_METAL_QUANT_FORMAT_Q5_0: return runtime->q5_0_cpy_q_to_f32_pipeline;
        case TERMITE_METAL_QUANT_FORMAT_Q5_1: return runtime->q5_1_cpy_q_to_f32_pipeline;
        case TERMITE_METAL_QUANT_FORMAT_Q4_K: return runtime->q4_k_cpy_q_to_f32_pipeline;
        case TERMITE_METAL_QUANT_FORMAT_Q5_K: return runtime->q5_k_cpy_q_to_f32_pipeline;
        case TERMITE_METAL_QUANT_FORMAT_Q6_K: return runtime->q6_k_cpy_q_to_f32_pipeline;
        case TERMITE_METAL_QUANT_FORMAT_Q8_0: return runtime->q8_0_cpy_q_to_f32_pipeline;
        case TERMITE_METAL_QUANT_FORMAT_Q8_1: return runtime->q8_1_cpy_q_to_f32_pipeline;
        default: return nil;
    }
}

static id<MTLComputePipelineState> termite_metal_decode_runtime_quant_cpy_f32_to_q_pipeline(
    termite_metal_decode_runtime *runtime,
    uint32_t format
) {
    if (runtime == NULL) return nil;
    switch (format) {
        case TERMITE_METAL_QUANT_FORMAT_Q4_0: return runtime->q4_0_cpy_f32_to_q_pipeline;
        case TERMITE_METAL_QUANT_FORMAT_Q4_1: return runtime->q4_1_cpy_f32_to_q_pipeline;
        case TERMITE_METAL_QUANT_FORMAT_Q5_0: return runtime->q5_0_cpy_f32_to_q_pipeline;
        case TERMITE_METAL_QUANT_FORMAT_Q5_1: return runtime->q5_1_cpy_f32_to_q_pipeline;
        case TERMITE_METAL_QUANT_FORMAT_Q4_K: return runtime->q4_k_cpy_f32_to_q_pipeline;
        case TERMITE_METAL_QUANT_FORMAT_Q5_K: return runtime->q5_k_cpy_f32_to_q_pipeline;
        case TERMITE_METAL_QUANT_FORMAT_Q6_K: return runtime->q6_k_cpy_f32_to_q_pipeline;
        case TERMITE_METAL_QUANT_FORMAT_Q8_0: return runtime->q8_0_cpy_f32_to_q_pipeline;
        case TERMITE_METAL_QUANT_FORMAT_Q8_1: return runtime->q8_1_cpy_f32_to_q_pipeline;
        default: return nil;
    }
}

int termite_metal_decode_runtime_quant_get_rows_linear_slot_device(
    termite_metal_decode_runtime *runtime,
    uint32_t format,
    size_t slot,
    const uint32_t *ids,
    size_t total,
    size_t dim,
    size_t source_rows,
    float scale,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || ids == NULL || output_handle == NULL) return -1;
    id<MTLComputePipelineState> pipeline = termite_metal_decode_runtime_quant_get_rows_pipeline(runtime, format);
    if (pipeline == nil) return -2;
    if (slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -3;
    if (total == 0 || dim == 0 || source_rows == 0 || dim % 32u != 0) return -4;
    if (total > UINT32_MAX || dim > UINT32_MAX || source_rows > UINT32_MAX) return -5;
    termite_metal_quant_linear_slot_view view;
    if (termite_metal_decode_runtime_require_quant_linear_slot(
            runtime,
            format,
            slot,
            dim,
            source_rows,
            &view) != 0) return -6;
    @autoreleasepool {
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t output_bytes = total * dim * sizeof(float);
        if (output_offset + output_bytes > output_buffer.length) return -7;
        const bool frame_owned = (runtime->active_frame_cb == nil);
        const size_t ids_bytes = total * sizeof(uint32_t);
        termite_metal_host_staging_slice ids_slice = { nil, 0 };
        if (termite_metal_decode_runtime_stage_host_bytes(runtime, ids, ids_bytes, frame_owned, &ids_slice) != 0) return -8;
        termite_metal_quant_rows_params params = {
            .total = (uint32_t)total,
            .dim = (uint32_t)dim,
            .source_rows = (uint32_t)source_rows,
            .row_offset = 0,
            .scale = scale,
        };
        id<MTLCommandBuffer> command_buffer = frame_owned
            ? termite_metal_new_command_buffer(runtime->queue, __func__)
            : runtime->active_frame_cb;
        if (command_buffer == nil) return -9;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_EMBEDDING);
        if (encoder == nil) return -10;
        [encoder setComputePipelineState:pipeline];
        [encoder setBuffer:view.weight_buffer offset:0 atIndex:0];
        [encoder setBuffer:ids_slice.buffer offset:ids_slice.offset atIndex:1];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:2];
        [encoder setBytes:&params length:sizeof(params) atIndex:3];
        const size_t total_values = total * dim;
        [encoder dispatchThreads:MTLSizeMake(total_values, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(pipeline, total_values), 1, 1)];
        [encoder endEncoding];
        if (frame_owned) {
            [command_buffer commit];
            [command_buffer waitUntilCompleted];
            return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -11;
        }
        return 0;
    }
}

int termite_metal_decode_runtime_q8_0_get_rows_linear_slot_device(
    termite_metal_decode_runtime *runtime,
    size_t slot,
    const uint32_t *ids,
    size_t total,
    size_t dim,
    size_t source_rows,
    float scale,
    void *output_handle,
    size_t output_offset
) {
    return termite_metal_decode_runtime_quant_get_rows_linear_slot_device(
        runtime,
        TERMITE_METAL_QUANT_FORMAT_Q8_0,
        slot,
        ids,
        total,
        dim,
        source_rows,
        scale,
        output_handle,
        output_offset);
}

int termite_metal_decode_runtime_quant_copy_linear_slot_to_f32_device(
    termite_metal_decode_runtime *runtime,
    uint32_t format,
    size_t slot,
    size_t row_offset,
    size_t rows,
    size_t dim,
    size_t source_rows,
    float scale,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || output_handle == NULL) return -1;
    id<MTLComputePipelineState> pipeline = termite_metal_decode_runtime_quant_cpy_q_to_f32_pipeline(runtime, format);
    if (pipeline == nil) return -2;
    if (slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -3;
    if (rows == 0 || dim == 0 || source_rows == 0 || dim % 32u != 0) return -4;
    if (row_offset > source_rows || rows > source_rows - row_offset) return -5;
    if (rows > UINT32_MAX || dim > UINT32_MAX || source_rows > UINT32_MAX || row_offset > UINT32_MAX) return -6;
    termite_metal_quant_linear_slot_view view;
    if (termite_metal_decode_runtime_require_quant_linear_slot(
            runtime,
            format,
            slot,
            dim,
            source_rows,
            &view) != 0) return -7;
    @autoreleasepool {
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t output_bytes = rows * dim * sizeof(float);
        if (output_offset + output_bytes > output_buffer.length) return -8;
        termite_metal_quant_rows_params params = {
            .total = (uint32_t)rows,
            .dim = (uint32_t)dim,
            .source_rows = (uint32_t)source_rows,
            .row_offset = (uint32_t)row_offset,
            .scale = scale,
        };
        const bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLCommandBuffer> command_buffer = frame_owned
            ? termite_metal_new_command_buffer(runtime->queue, __func__)
            : runtime->active_frame_cb;
        if (command_buffer == nil) return -9;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_EMBEDDING);
        if (encoder == nil) return -10;
        [encoder setComputePipelineState:pipeline];
        [encoder setBuffer:view.weight_buffer offset:0 atIndex:0];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:1];
        [encoder setBytes:&params length:sizeof(params) atIndex:2];
        const size_t total_values = rows * dim;
        [encoder dispatchThreads:MTLSizeMake(total_values, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(pipeline, total_values), 1, 1)];
        [encoder endEncoding];
        if (frame_owned) {
            [command_buffer commit];
            [command_buffer waitUntilCompleted];
            return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -11;
        }
        return 0;
    }
}

int termite_metal_decode_runtime_q8_0_copy_linear_slot_to_f32_device(
    termite_metal_decode_runtime *runtime,
    size_t slot,
    size_t row_offset,
    size_t rows,
    size_t dim,
    size_t source_rows,
    float scale,
    void *output_handle,
    size_t output_offset
) {
    return termite_metal_decode_runtime_quant_copy_linear_slot_to_f32_device(
        runtime,
        TERMITE_METAL_QUANT_FORMAT_Q8_0,
        slot,
        row_offset,
        rows,
        dim,
        source_rows,
        scale,
        output_handle,
        output_offset);
}

int termite_metal_decode_runtime_quant_copy_f32_to_linear_slot_device(
    termite_metal_decode_runtime *runtime,
    uint32_t format,
    size_t slot,
    size_t row_offset,
    size_t rows,
    size_t dim,
    size_t source_rows,
    float scale,
    void *input_handle,
    size_t input_offset
) {
    if (runtime == NULL || input_handle == NULL) return -1;
    id<MTLComputePipelineState> pipeline = termite_metal_decode_runtime_quant_cpy_f32_to_q_pipeline(runtime, format);
    if (pipeline == nil) return -2;
    if (slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -3;
    if (rows == 0 || dim == 0 || source_rows == 0 || dim % 32u != 0) return -4;
    if (row_offset > source_rows || rows > source_rows - row_offset) return -5;
    if (rows > UINT32_MAX || dim > UINT32_MAX || source_rows > UINT32_MAX || row_offset > UINT32_MAX) return -6;
    termite_metal_quant_linear_slot_view view;
    if (termite_metal_decode_runtime_require_quant_linear_slot(
            runtime,
            format,
            slot,
            dim,
            source_rows,
            &view) != 0) return -7;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        const size_t input_bytes = rows * dim * sizeof(float);
        if (input_offset + input_bytes > input_buffer.length) return -8;
        termite_metal_quant_rows_params params = {
            .total = (uint32_t)rows,
            .dim = (uint32_t)dim,
            .source_rows = (uint32_t)source_rows,
            .row_offset = (uint32_t)row_offset,
            .scale = scale,
        };
        const bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLCommandBuffer> command_buffer = frame_owned
            ? termite_metal_new_command_buffer(runtime->queue, __func__)
            : runtime->active_frame_cb;
        if (command_buffer == nil) return -9;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_EMBEDDING);
        if (encoder == nil) return -10;
        [encoder setComputePipelineState:pipeline];
        [encoder setBuffer:input_buffer offset:input_offset atIndex:0];
        [encoder setBuffer:view.weight_buffer offset:0 atIndex:1];
        [encoder setBytes:&params length:sizeof(params) atIndex:2];
        const size_t row_blocks = dim / 32u;
        const size_t total_blocks = rows * row_blocks;
        [encoder dispatchThreads:MTLSizeMake(total_blocks, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(pipeline, total_blocks), 1, 1)];
        [encoder endEncoding];
        if (frame_owned) {
            [command_buffer commit];
            [command_buffer waitUntilCompleted];
            return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -11;
        }
        return 0;
    }
}

int termite_metal_decode_runtime_quant_set_rows_linear_slot_device(
    termite_metal_decode_runtime *runtime,
    uint32_t format,
    size_t slot,
    const uint32_t *ids,
    size_t total,
    size_t dim,
    size_t source_rows,
    float scale,
    void *input_handle,
    size_t input_offset
) {
    if (runtime == NULL || ids == NULL || input_handle == NULL) return -1;
    id<MTLComputePipelineState> pipeline = termite_metal_decode_runtime_quant_set_rows_pipeline(runtime, format);
    if (pipeline == nil) return -2;
    if (slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -3;
    if (total == 0 || dim == 0 || source_rows == 0 || dim % 32u != 0) return -4;
    if (total > UINT32_MAX || dim > UINT32_MAX || source_rows > UINT32_MAX) return -5;
    termite_metal_quant_linear_slot_view view;
    if (termite_metal_decode_runtime_require_quant_linear_slot(
            runtime,
            format,
            slot,
            dim,
            source_rows,
            &view) != 0) return -6;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        const size_t input_bytes = total * dim * sizeof(float);
        if (input_offset + input_bytes > input_buffer.length) return -7;
        const bool frame_owned = (runtime->active_frame_cb == nil);
        const size_t ids_bytes = total * sizeof(uint32_t);
        termite_metal_host_staging_slice ids_slice = { nil, 0 };
        if (termite_metal_decode_runtime_stage_host_bytes(runtime, ids, ids_bytes, frame_owned, &ids_slice) != 0) return -8;
        termite_metal_quant_rows_params params = {
            .total = (uint32_t)total,
            .dim = (uint32_t)dim,
            .source_rows = (uint32_t)source_rows,
            .row_offset = 0,
            .scale = scale,
        };
        id<MTLCommandBuffer> command_buffer = frame_owned
            ? termite_metal_new_command_buffer(runtime->queue, __func__)
            : runtime->active_frame_cb;
        if (command_buffer == nil) return -9;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_EMBEDDING);
        if (encoder == nil) return -10;
        [encoder setComputePipelineState:pipeline];
        [encoder setBuffer:input_buffer offset:input_offset atIndex:0];
        [encoder setBuffer:ids_slice.buffer offset:ids_slice.offset atIndex:1];
        [encoder setBuffer:view.weight_buffer offset:0 atIndex:2];
        [encoder setBytes:&params length:sizeof(params) atIndex:3];
        const size_t row_blocks = dim / 32u;
        const size_t total_blocks = total * row_blocks;
        [encoder dispatchThreads:MTLSizeMake(total_blocks, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(pipeline, total_blocks), 1, 1)];
        [encoder endEncoding];
        if (frame_owned) {
            [command_buffer commit];
            [command_buffer waitUntilCompleted];
            return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -11;
        }
        return 0;
    }
}

int termite_metal_decode_runtime_embedding_lookup(
    termite_metal_decode_runtime *runtime,
    const float *weight,
    size_t rows,
    const uint32_t *ids,
    size_t total,
    size_t dim,
    float *output
) {
    const int prep_rc = termite_metal_decode_runtime_prepare_embedding_table(runtime, weight, rows, dim);
    if (prep_rc != 0) return prep_rc;
    return termite_metal_decode_runtime_embedding_lookup_prepared(runtime, ids, total, dim, output);
}

int termite_metal_decode_runtime_apply_rope(
    termite_metal_decode_runtime *runtime,
    const float *input,
    const uint32_t *positions,
    size_t total_chunks,
    size_t head_dim,
    size_t rope_dim,
    float theta,
    float freq_scale,
    uint32_t consecutive_pairs,
    float *output
) {
    if (runtime == NULL || input == NULL || positions == NULL || output == NULL) return -1;
    if (runtime->rope_pipeline == nil) return -2;
    if (total_chunks == 0 || head_dim == 0 || rope_dim == 0) return -3;
    if (total_chunks > UINT32_MAX || head_dim > UINT32_MAX || rope_dim > UINT32_MAX) return -4;
    @autoreleasepool {
        bool frame_owned = (runtime->active_frame_cb == nil);
        const size_t input_bytes = total_chunks * head_dim * sizeof(float);
        const size_t positions_bytes = total_chunks * sizeof(uint32_t);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)input length:input_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:input length:input_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> positions_buffer = frame_owned
            ? [runtime->device newBufferWithBytesNoCopy:(void *)positions length:positions_bytes options:MTLResourceStorageModeShared deallocator:nil]
            : [runtime->device newBufferWithBytes:positions length:positions_bytes options:MTLResourceStorageModeShared];
        if (positions_buffer == nil) positions_buffer = [runtime->device newBufferWithBytes:positions length:positions_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithBytesNoCopy:output length:input_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) output_buffer = [runtime->device newBufferWithBytes:output length:input_bytes options:MTLResourceStorageModeShared];
        if (input_buffer == nil || positions_buffer == nil || output_buffer == nil) return -5;
        termite_metal_rope_params params = {
            .total_chunks = (uint32_t)total_chunks,
            .head_dim = (uint32_t)head_dim,
            .rope_dim = (uint32_t)rope_dim,
            .theta = theta,
            .freq_scale = freq_scale,
            .consecutive_pairs = consecutive_pairs,
        };
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -7;
        if (!frame_owned && termite_metal_decode_runtime_retain_frame_resource(runtime, positions_buffer) != 0) return -6;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_HEAD_ROPE);
        if (encoder == nil) return -8;
        [encoder setComputePipelineState:runtime->rope_pipeline];
        [encoder setBuffer:input_buffer offset:0 atIndex:0];
        [encoder setBuffer:positions_buffer offset:0 atIndex:1];
        [encoder setBuffer:output_buffer offset:0 atIndex:2];
        [encoder setBytes:&params length:sizeof(params) atIndex:3];
        const size_t total_values = total_chunks * head_dim;
        MTLSize grid_size = MTLSizeMake(total_values, 1, 1);
        NSUInteger thread_width = runtime->rope_pipeline.maxTotalThreadsPerThreadgroup;
        if (thread_width == 0) thread_width = 64;
        if (thread_width > total_values && total_values > 0) thread_width = total_values;
        MTLSize group_size = MTLSizeMake(thread_width, 1, 1);
        [encoder dispatchThreads:grid_size threadsPerThreadgroup:group_size];
        [encoder endEncoding];
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -9);
    }
}

int termite_metal_decode_runtime_apply_rope_device(
    termite_metal_decode_runtime *runtime,
    void *input_handle,
    size_t input_offset,
    const uint32_t *positions,
    size_t total_chunks,
    size_t head_dim,
    size_t rope_dim,
    float theta,
    float freq_scale,
    uint32_t consecutive_pairs,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || input_handle == NULL || positions == NULL || output_handle == NULL) return -1;
    if (runtime->rope_pipeline == nil) return -2;
    if (total_chunks == 0 || head_dim == 0 || rope_dim == 0) return -3;
    if (total_chunks > UINT32_MAX || head_dim > UINT32_MAX || rope_dim > UINT32_MAX) return -4;
    @autoreleasepool {
        bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t input_bytes = total_chunks * head_dim * sizeof(float);
        if (input_offset + input_bytes > input_buffer.length || output_offset + input_bytes > output_buffer.length) return -5;
        const size_t positions_bytes = total_chunks * sizeof(uint32_t);
        id<MTLBuffer> positions_buffer = frame_owned
            ? [runtime->device newBufferWithBytesNoCopy:(void *)positions length:positions_bytes options:MTLResourceStorageModeShared deallocator:nil]
            : [runtime->device newBufferWithBytes:positions length:positions_bytes options:MTLResourceStorageModeShared];
        if (positions_buffer == nil) positions_buffer = [runtime->device newBufferWithBytes:positions length:positions_bytes options:MTLResourceStorageModeShared];
        if (positions_buffer == nil) return -6;
        termite_metal_rope_params params = {
            .total_chunks = (uint32_t)total_chunks,
            .head_dim = (uint32_t)head_dim,
            .rope_dim = (uint32_t)rope_dim,
            .theta = theta,
            .freq_scale = freq_scale,
            .consecutive_pairs = consecutive_pairs,
        };
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -8;
        if (!frame_owned && termite_metal_decode_runtime_retain_frame_resource(runtime, positions_buffer) != 0) return -7;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_HEAD_ROPE);
        if (encoder == nil) return -9;
        [encoder setComputePipelineState:runtime->rope_pipeline];
        [encoder setBuffer:input_buffer offset:input_offset atIndex:0];
        [encoder setBuffer:positions_buffer offset:0 atIndex:1];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:2];
        [encoder setBytes:&params length:sizeof(params) atIndex:3];
        const size_t total_values = total_chunks * head_dim;
        MTLSize grid_size = MTLSizeMake(total_values, 1, 1);
        NSUInteger thread_width = runtime->rope_pipeline.maxTotalThreadsPerThreadgroup;
        if (thread_width == 0) thread_width = 64;
        if (thread_width > total_values && total_values > 0) thread_width = total_values;
        MTLSize group_size = MTLSizeMake(thread_width, 1, 1);
        [encoder dispatchThreads:grid_size threadsPerThreadgroup:group_size];
        [encoder endEncoding];
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -10);
    }
}

int termite_metal_decode_runtime_apply_head_rms_rope_device(
    termite_metal_decode_runtime *runtime,
    void *input_handle,
    size_t input_offset,
    size_t norm_slot,
    size_t total_heads,
    size_t head_dim,
    size_t rope_dim,
    size_t position,
    float theta,
    float freq_scale,
    float eps,
    float value_scale,
    uint32_t consecutive_pairs,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || input_handle == NULL || output_handle == NULL) return -1;
    if (norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY || total_heads == 0 || head_dim == 0 || rope_dim == 0) return -3;
    if (total_heads > UINT32_MAX || head_dim > UINT32_MAX || rope_dim > UINT32_MAX || position > UINT32_MAX) return -5;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t bytes = total_heads * head_dim * sizeof(float);
        if (input_offset + bytes > input_buffer.length) return -6;
        if (output_offset + bytes > output_buffer.length) return -7;
        bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -8;
        id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
        const BOOL planned_encoder = (encoder != nil);
        if (!planned_encoder) {
            encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_HEAD_ROPE);
            if (encoder == nil) return -9;
        }
        const int encode_rc = termite_metal_encode_head_rms_rope_on_encoder(runtime, encoder, input_buffer, input_offset, norm_slot, total_heads, head_dim, rope_dim, position, theta, freq_scale, eps, value_scale, consecutive_pairs, total_heads, 0, output_buffer, output_offset, -9);
        if (!planned_encoder) [encoder endEncoding];
        if (encode_rc != 0) return encode_rc;
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -10);
    }
}

int termite_metal_decode_runtime_apply_head_rms_rope_batched_device(
    termite_metal_decode_runtime *runtime,
    void *input_handle,
    size_t input_offset,
    size_t norm_slot,
    size_t total_heads,
    size_t head_dim,
    size_t rope_dim,
    size_t position,
    float theta,
    float freq_scale,
    float eps,
    float value_scale,
    uint32_t consecutive_pairs,
    size_t heads_per_row,
    size_t position_period,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || input_handle == NULL || output_handle == NULL) return -1;
    if (norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY || total_heads == 0 || head_dim == 0 || rope_dim == 0 || heads_per_row == 0) return -3;
    if (total_heads > UINT32_MAX || head_dim > UINT32_MAX || rope_dim > UINT32_MAX || position > UINT32_MAX || heads_per_row > UINT32_MAX || position_period > UINT32_MAX) return -5;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t bytes = total_heads * head_dim * sizeof(float);
        if (input_offset + bytes > input_buffer.length) return -6;
        if (output_offset + bytes > output_buffer.length) return -7;
        bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -8;
        id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
        const BOOL planned_encoder = (encoder != nil);
        if (!planned_encoder) {
            encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_HEAD_ROPE);
            if (encoder == nil) return -9;
        }
        const int encode_rc = termite_metal_encode_head_rms_rope_on_encoder(runtime, encoder, input_buffer, input_offset, norm_slot, total_heads, head_dim, rope_dim, position, theta, freq_scale, eps, value_scale, consecutive_pairs, heads_per_row, position_period, output_buffer, output_offset, -9);
        if (!planned_encoder) [encoder endEncoding];
        if (encode_rc != 0) return encode_rc;
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -10);
    }
}

int termite_metal_decode_runtime_apply_head_rms_rope_scratch_device(
    termite_metal_decode_runtime *runtime,
    void *input_handle,
    size_t input_offset,
    size_t norm_slot,
    size_t total_heads,
    size_t head_dim,
    size_t rope_dim,
    size_t position,
    float theta,
    float freq_scale,
    float eps,
    float value_scale,
    uint32_t consecutive_pairs,
    size_t heads_per_row,
    size_t output_index,
    void **output_handle
) {
    if (runtime == NULL || input_handle == NULL || output_handle == NULL) return -1;
    *output_handle = NULL;
    if (norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY || total_heads == 0 || head_dim == 0 || rope_dim == 0 || heads_per_row == 0) return -3;
    if (total_heads > UINT32_MAX || head_dim > UINT32_MAX || rope_dim > UINT32_MAX || position > UINT32_MAX || heads_per_row > UINT32_MAX) return -5;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        const size_t bytes = total_heads * head_dim * sizeof(float);
        if (input_offset + bytes > input_buffer.length) return -6;
        if (termite_metal_decode_runtime_ensure_active_layer_projection_buffer(runtime, output_index, bytes) != 0) return -7;
        id<MTLBuffer> output_buffer = runtime->active_layer_projection_buffers[output_index];
        bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -8;
        id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
        const BOOL planned_encoder = (encoder != nil);
        if (!planned_encoder) {
            encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_HEAD_ROPE);
            if (encoder == nil) return -9;
        }
        const int encode_rc = termite_metal_encode_head_rms_rope_on_encoder(runtime, encoder, input_buffer, input_offset, norm_slot, total_heads, head_dim, rope_dim, position, theta, freq_scale, eps, value_scale, consecutive_pairs, heads_per_row, 0, output_buffer, 0, -9);
        if (!planned_encoder) [encoder endEncoding];
        if (encode_rc != 0) return encode_rc;
        const int rc = termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -10);
        if (rc != 0) return rc;
        *output_handle = (__bridge void *)output_buffer;
        return 0;
    }
}

int termite_metal_decode_runtime_apply_attention_f32(
    termite_metal_decode_runtime *runtime,
    const float *q,
    const float *k,
    const float *v,
    const float *bias,
    const uint8_t *mask,
    size_t q_len,
    size_t kv_len,
    size_t num_heads,
    size_t num_kv_heads,
    size_t head_dim,
    size_t query_position_offset,
    size_t kv_position_offset,
    size_t sliding_window,
    size_t total_sequence_len,
    float *output
) {
    if (runtime == NULL || q == NULL || k == NULL || v == NULL || output == NULL) return -1;
    if (runtime->attention_f32_pipeline == nil) return -2;
    if (q_len == 0 || kv_len == 0 || num_heads == 0 || num_kv_heads == 0 || head_dim == 0) return -3;
    if (num_heads % num_kv_heads != 0) return -4;
    if (q_len > UINT32_MAX || kv_len > UINT32_MAX || num_heads > UINT32_MAX || num_kv_heads > UINT32_MAX || head_dim > UINT32_MAX || query_position_offset > UINT32_MAX || kv_position_offset > UINT32_MAX || sliding_window > UINT32_MAX || total_sequence_len > UINT32_MAX) return -5;
    @autoreleasepool {
        const size_t q_bytes = q_len * num_heads * head_dim * sizeof(float);
        const size_t kv_bytes = kv_len * num_kv_heads * head_dim * sizeof(float);
        const bool frame_owned_expected = (runtime->active_frame_cb == nil);
        id<MTLBuffer> q_buffer = [runtime->device newBufferWithBytes:q length:q_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> k_buffer = [runtime->device newBufferWithBytes:k length:kv_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> v_buffer = [runtime->device newBufferWithBytes:v length:kv_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> output_buffer = frame_owned_expected
            ? [runtime->device newBufferWithLength:q_bytes options:MTLResourceStorageModeShared]
            : [runtime->device newBufferWithBytesNoCopy:output length:q_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (q_buffer == nil || k_buffer == nil || v_buffer == nil || output_buffer == nil) return -6;
        id<MTLBuffer> bias_buffer = nil;
        if (bias != NULL) {
            const size_t bias_bytes = num_heads * q_len * kv_len * sizeof(float);
            bias_buffer = [runtime->device newBufferWithBytes:bias length:bias_bytes options:MTLResourceStorageModeShared];
            if (bias_buffer == nil) return -7;
        }
        id<MTLBuffer> mask_buffer = nil;
        if (mask != NULL) {
            const size_t mask_bytes = total_sequence_len * total_sequence_len * sizeof(uint8_t);
            mask_buffer = [runtime->device newBufferWithBytes:mask length:mask_bytes options:MTLResourceStorageModeShared];
            if (mask_buffer == nil) return -8;
        }
        const float zero_bias = 0.0f;
        const uint8_t zero_mask = 0;
        termite_metal_attention_f32_params params = {
            .q_len = (uint32_t)q_len,
            .kv_len = (uint32_t)kv_len,
            .num_heads = (uint32_t)num_heads,
            .num_kv_heads = (uint32_t)num_kv_heads,
            .head_dim = (uint32_t)head_dim,
            .query_position_offset = (uint32_t)query_position_offset,
            .kv_position_offset = (uint32_t)kv_position_offset,
            .sliding_window = (uint32_t)sliding_window,
            .total_sequence_len = (uint32_t)total_sequence_len,
            .has_bias = bias != NULL ? 1u : 0u,
            .has_mask = mask != NULL ? 1u : 0u,
        };
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -10;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_ATTENTION);
        if (encoder == nil) return -11;
        const BOOL use_prefill_tiled = (runtime->attention_f32_prefill_pipeline != nil && q_len >= 1 && kv_len <= 2048u && head_dim <= 1024u);
        [encoder setComputePipelineState:(use_prefill_tiled ? runtime->attention_f32_prefill_pipeline : runtime->attention_f32_pipeline)];
        [encoder setBuffer:q_buffer offset:0 atIndex:0];
        [encoder setBuffer:k_buffer offset:0 atIndex:1];
        [encoder setBuffer:v_buffer offset:0 atIndex:2];
        [encoder setBuffer:output_buffer offset:0 atIndex:3];
        if (bias_buffer != nil) {
            [encoder setBuffer:bias_buffer offset:0 atIndex:4];
        } else {
            [encoder setBytes:&zero_bias length:sizeof(zero_bias) atIndex:4];
        }
        if (mask_buffer != nil) {
            [encoder setBuffer:mask_buffer offset:0 atIndex:5];
        } else {
            [encoder setBytes:&zero_mask length:sizeof(zero_mask) atIndex:5];
        }
        [encoder setBytes:&params length:sizeof(params) atIndex:6];
        const size_t total_rows = q_len * num_heads;
        if (use_prefill_tiled) {
            [encoder setThreadgroupMemoryLength:termite_metal_threadgroup_memory_16((kv_len + 256u) * sizeof(float)) atIndex:0];
            [encoder dispatchThreadgroups:MTLSizeMake(q_len, num_heads, 1) threadsPerThreadgroup:MTLSizeMake(256, 1, 1)];
        } else {
            MTLSize grid_size = MTLSizeMake(total_rows, 1, 1);
            NSUInteger thread_width = runtime->attention_f32_pipeline.maxTotalThreadsPerThreadgroup;
            if (thread_width == 0) thread_width = 64;
            if (thread_width > total_rows && total_rows > 0) thread_width = total_rows;
            MTLSize group_size = MTLSizeMake(thread_width, 1, 1);
            [encoder dispatchThreads:grid_size threadsPerThreadgroup:group_size];
        }
        [encoder endEncoding];
        int rc = termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -12);
        if (rc != 0) return rc;
        if (frame_owned) memcpy(output, output_buffer.contents, q_bytes);
        return 0;
    }
}

int termite_metal_decode_runtime_apply_attention_f32_device(
    termite_metal_decode_runtime *runtime,
    void *q_handle,
    size_t q_offset,
    void *k_handle,
    size_t k_offset,
    void *v_handle,
    size_t v_offset,
    size_t q_len,
    size_t kv_len,
    size_t num_heads,
    size_t num_kv_heads,
    size_t head_dim,
    size_t query_position_offset,
    size_t kv_position_offset,
    size_t sliding_window,
    void *bias_handle,
    size_t bias_offset,
    const float *bias_host,
    const uint8_t *mask,
    size_t total_sequence_len,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || q_handle == NULL || k_handle == NULL || v_handle == NULL || output_handle == NULL) return -1;
    if (runtime->attention_f32_pipeline == nil) return -2;
    if (q_len == 0 || kv_len == 0 || num_heads == 0 || num_kv_heads == 0 || head_dim == 0) return -3;
    if (num_heads % num_kv_heads != 0) return -4;
    if (q_len > UINT32_MAX || kv_len > UINT32_MAX || num_heads > UINT32_MAX || num_kv_heads > UINT32_MAX || head_dim > UINT32_MAX || query_position_offset > UINT32_MAX || kv_position_offset > UINT32_MAX || sliding_window > UINT32_MAX || total_sequence_len > UINT32_MAX) return -5;
    @autoreleasepool {
        id<MTLBuffer> q_buffer = (__bridge id<MTLBuffer>)q_handle;
        id<MTLBuffer> k_buffer = (__bridge id<MTLBuffer>)k_handle;
        id<MTLBuffer> v_buffer = (__bridge id<MTLBuffer>)v_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t q_bytes = q_len * num_heads * head_dim * sizeof(float);
        const size_t kv_bytes = kv_len * num_kv_heads * head_dim * sizeof(float);
        if (q_offset + q_bytes > q_buffer.length ||
            k_offset + kv_bytes > k_buffer.length ||
            v_offset + kv_bytes > v_buffer.length ||
            output_offset + q_bytes > output_buffer.length) return -6;
        id<MTLBuffer> bias_buffer = nil;
        if (bias_handle != NULL) {
            bias_buffer = (__bridge id<MTLBuffer>)bias_handle;
            const size_t bias_bytes = num_heads * q_len * kv_len * sizeof(float);
            if (bias_offset + bias_bytes > bias_buffer.length) return -7;
        } else if (bias_host != NULL) {
            const size_t bias_bytes = num_heads * q_len * kv_len * sizeof(float);
            bias_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)bias_host length:bias_bytes options:MTLResourceStorageModeShared deallocator:nil];
            if (bias_buffer == nil) bias_buffer = [runtime->device newBufferWithBytes:bias_host length:bias_bytes options:MTLResourceStorageModeShared];
            if (bias_buffer == nil) return -8;
        }
        id<MTLBuffer> mask_buffer = nil;
        if (mask != NULL) {
            const size_t mask_bytes = total_sequence_len * total_sequence_len * sizeof(uint8_t);
            mask_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)mask length:mask_bytes options:MTLResourceStorageModeShared deallocator:nil];
            if (mask_buffer == nil) mask_buffer = [runtime->device newBufferWithBytes:mask length:mask_bytes options:MTLResourceStorageModeShared];
            if (mask_buffer == nil) return -9;
        }
        const float zero_bias = 0.0f;
        const uint8_t zero_mask = 0;
        const bool has_bias_input = (bias_handle != NULL || bias_host != NULL);
        const bool has_mask_input = (mask != NULL);
        termite_metal_attention_f32_params params = {
            .q_len = (uint32_t)q_len,
            .kv_len = (uint32_t)kv_len,
            .num_heads = (uint32_t)num_heads,
            .num_kv_heads = (uint32_t)num_kv_heads,
            .head_dim = (uint32_t)head_dim,
            .query_position_offset = (uint32_t)query_position_offset,
            .kv_position_offset = (uint32_t)kv_position_offset,
            .sliding_window = (uint32_t)sliding_window,
            .total_sequence_len = (uint32_t)total_sequence_len,
            .has_bias = has_bias_input ? 1u : 0u,
            .has_mask = has_mask_input ? 1u : 0u,
        };
        const bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLCommandBuffer> command_buffer = frame_owned
            ? termite_metal_new_command_buffer(runtime->queue, __func__)
            : runtime->active_frame_cb;
        if (command_buffer == nil) return -11;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_ATTENTION);
        if (encoder == nil) return -12;
        const BOOL use_prefill_tiled = (runtime->attention_f32_prefill_pipeline != nil && q_len >= 1 && kv_len <= 2048u && head_dim <= 1024u);
        [encoder setComputePipelineState:(use_prefill_tiled ? runtime->attention_f32_prefill_pipeline : runtime->attention_f32_pipeline)];
        [encoder setBuffer:q_buffer offset:q_offset atIndex:0];
        [encoder setBuffer:k_buffer offset:k_offset atIndex:1];
        [encoder setBuffer:v_buffer offset:v_offset atIndex:2];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:3];
        const size_t effective_bias_offset = bias_buffer != nil && bias_handle != NULL ? bias_offset : 0;
        if (bias_buffer != nil) {
            [encoder setBuffer:bias_buffer offset:effective_bias_offset atIndex:4];
        } else {
            [encoder setBytes:&zero_bias length:sizeof(zero_bias) atIndex:4];
        }
        if (mask_buffer != nil) {
            [encoder setBuffer:mask_buffer offset:0 atIndex:5];
        } else {
            [encoder setBytes:&zero_mask length:sizeof(zero_mask) atIndex:5];
        }
        [encoder setBytes:&params length:sizeof(params) atIndex:6];
        const size_t total_rows = q_len * num_heads;
        if (use_prefill_tiled) {
            [encoder setThreadgroupMemoryLength:termite_metal_threadgroup_memory_16((kv_len + 256u) * sizeof(float)) atIndex:0];
            [encoder dispatchThreadgroups:MTLSizeMake(q_len, num_heads, 1) threadsPerThreadgroup:MTLSizeMake(256, 1, 1)];
        } else {
            MTLSize grid_size = MTLSizeMake(total_rows, 1, 1);
            NSUInteger thread_width = runtime->attention_f32_pipeline.maxTotalThreadsPerThreadgroup;
            if (thread_width == 0) thread_width = 64;
            if (thread_width > total_rows && total_rows > 0) thread_width = total_rows;
            MTLSize group_size = MTLSizeMake(thread_width, 1, 1);
            [encoder dispatchThreads:grid_size threadsPerThreadgroup:group_size];
        }
        [encoder endEncoding];
        if (frame_owned) {
            [command_buffer commit];
            [command_buffer waitUntilCompleted];
            return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -13;
        }
        return 0;
    }
}

int termite_metal_decode_runtime_compressed_attention_store_local_device(
    termite_metal_decode_runtime *runtime,
    void *input_handle,
    size_t input_offset,
    void *local_handle,
    size_t local_offset,
    size_t query_rows,
    size_t query_abs_start,
    size_t head_dim
) {
    if (runtime == NULL || input_handle == NULL || local_handle == NULL) return -1;
    if (runtime->compressed_attention_store_local_pipeline == nil) return -2;
    if (query_rows == 0 || head_dim == 0 || query_rows > UINT32_MAX || query_abs_start > UINT32_MAX || head_dim > UINT32_MAX) return -3;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLBuffer> local_buffer = (__bridge id<MTLBuffer>)local_handle;
        size_t input_bytes = 0;
        size_t local_tokens = 0;
        size_t local_min_bytes = 0;
        if (!termite_metal_size_mul3(query_rows, head_dim, sizeof(float), &input_bytes)) return -4;
        if (query_abs_start > SIZE_MAX - query_rows) return -4;
        local_tokens = query_abs_start + query_rows;
        if (!termite_metal_size_mul3(local_tokens, head_dim, sizeof(float), &local_min_bytes)) return -4;
        if (!termite_metal_buffer_range_valid(input_buffer, input_offset, input_bytes) ||
            !termite_metal_buffer_range_valid(local_buffer, local_offset, local_min_bytes)) return -4;
        const bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLCommandBuffer> command_buffer = frame_owned ? termite_metal_new_command_buffer(runtime->queue, __func__) : runtime->active_frame_cb;
        if (command_buffer == nil) return -5;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_ATTENTION);
        if (encoder == nil) return -6;
        termite_metal_compressed_attention_store_local_params params = {
            .query_rows = (uint32_t)query_rows,
            .query_abs_start = (uint32_t)query_abs_start,
            .head_dim = (uint32_t)head_dim,
            .reserved = 0u,
        };
        [encoder setComputePipelineState:runtime->compressed_attention_store_local_pipeline];
        [encoder setBuffer:input_buffer offset:input_offset atIndex:0];
        [encoder setBuffer:local_buffer offset:local_offset atIndex:1];
        [encoder setBytes:&params length:sizeof(params) atIndex:2];
        const size_t total = query_rows * head_dim;
        [encoder dispatchThreads:MTLSizeMake(total, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->compressed_attention_store_local_pipeline, total), 1, 1)];
        [encoder endEncoding];
        if (frame_owned) {
            [command_buffer commit];
            [command_buffer waitUntilCompleted];
            return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -7;
        }
        return 0;
    }
}

int termite_metal_decode_runtime_compressed_attention_update_component_device(
    termite_metal_decode_runtime *runtime,
    void *projected_handle,
    size_t projected_offset,
    void *gate_handle,
    size_t gate_offset,
    void *bias_handle,
    size_t bias_offset,
    size_t bias_rows,
    void *norm_handle,
    size_t norm_offset,
    void *projected_cache_handle,
    size_t projected_cache_offset,
    void *gate_cache_handle,
    size_t gate_cache_offset,
    void *compressed_handle,
    size_t compressed_offset,
    void *positions_handle,
    size_t positions_offset,
    size_t query_rows,
    size_t query_abs_start,
    size_t total_tokens,
    size_t compress_rate,
    size_t row_dim,
    size_t gate_width,
    size_t row_count,
    size_t rope_dim,
    float theta,
    float freq_scale,
    float eps,
    uint32_t consecutive_pairs
) {
    if (runtime == NULL || projected_handle == NULL || gate_handle == NULL || bias_handle == NULL || norm_handle == NULL || projected_cache_handle == NULL || gate_cache_handle == NULL || compressed_handle == NULL || positions_handle == NULL) return -1;
    if (runtime->compressed_attention_store_component_pipeline == nil || runtime->compressed_attention_update_component_pipeline == nil) return -2;
    if (query_rows == 0 || total_tokens == 0 || compress_rate == 0 || row_dim == 0 || gate_width == 0 || row_count == 0) return -3;
    if (query_rows > UINT32_MAX || query_abs_start > UINT32_MAX || total_tokens > UINT32_MAX || compress_rate > UINT32_MAX || row_dim > UINT32_MAX || gate_width > UINT32_MAX || row_count > UINT32_MAX || rope_dim > UINT32_MAX || bias_rows > UINT32_MAX) return -4;
    @autoreleasepool {
        id<MTLBuffer> projected_buffer = (__bridge id<MTLBuffer>)projected_handle;
        id<MTLBuffer> gate_buffer = (__bridge id<MTLBuffer>)gate_handle;
        id<MTLBuffer> bias_buffer = (__bridge id<MTLBuffer>)bias_handle;
        id<MTLBuffer> norm_buffer = (__bridge id<MTLBuffer>)norm_handle;
        id<MTLBuffer> projected_cache_buffer = (__bridge id<MTLBuffer>)projected_cache_handle;
        id<MTLBuffer> gate_cache_buffer = (__bridge id<MTLBuffer>)gate_cache_handle;
        id<MTLBuffer> compressed_buffer = (__bridge id<MTLBuffer>)compressed_handle;
        id<MTLBuffer> positions_buffer = (__bridge id<MTLBuffer>)positions_handle;
        size_t projected_bytes = 0;
        size_t gate_bytes = 0;
        size_t bias_bytes = 0;
        size_t norm_bytes = 0;
        size_t projected_cache_bytes = 0;
        size_t gate_cache_bytes = 0;
        size_t compressed_bytes = 0;
        size_t positions_bytes = 0;
        if (!termite_metal_size_mul3(query_rows, row_dim, sizeof(float), &projected_bytes) ||
            !termite_metal_size_mul3(query_rows, gate_width, sizeof(float), &gate_bytes) ||
            !termite_metal_size_mul3(bias_rows, row_dim, sizeof(float), &bias_bytes) ||
            !termite_metal_size_mul(row_dim, sizeof(float), &norm_bytes) ||
            !termite_metal_size_mul3(total_tokens, row_dim, sizeof(float), &projected_cache_bytes) ||
            !termite_metal_size_mul3(total_tokens, gate_width, sizeof(float), &gate_cache_bytes) ||
            !termite_metal_size_mul3(row_count, row_dim, sizeof(float), &compressed_bytes) ||
            !termite_metal_size_mul(row_count, sizeof(uint32_t), &positions_bytes)) return -5;
        if (!termite_metal_buffer_range_valid(projected_buffer, projected_offset, projected_bytes) ||
            !termite_metal_buffer_range_valid(gate_buffer, gate_offset, gate_bytes) ||
            !termite_metal_buffer_range_valid(bias_buffer, bias_offset, bias_bytes) ||
            !termite_metal_buffer_range_valid(norm_buffer, norm_offset, norm_bytes) ||
            !termite_metal_buffer_range_valid(projected_cache_buffer, projected_cache_offset, projected_cache_bytes) ||
            !termite_metal_buffer_range_valid(gate_cache_buffer, gate_cache_offset, gate_cache_bytes) ||
            !termite_metal_buffer_range_valid(compressed_buffer, compressed_offset, compressed_bytes) ||
            !termite_metal_buffer_range_valid(positions_buffer, positions_offset, positions_bytes)) return -5;
        const bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLCommandBuffer> command_buffer = frame_owned ? termite_metal_new_command_buffer(runtime->queue, __func__) : runtime->active_frame_cb;
        if (command_buffer == nil) return -6;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_ATTENTION);
        if (encoder == nil) return -7;
        termite_metal_compressed_attention_component_params params = {
            .query_rows = (uint32_t)query_rows,
            .query_abs_start = (uint32_t)query_abs_start,
            .total_tokens = (uint32_t)total_tokens,
            .compress_rate = (uint32_t)compress_rate,
            .row_dim = (uint32_t)row_dim,
            .gate_width = (uint32_t)gate_width,
            .row_count = (uint32_t)row_count,
            .rope_dim = (uint32_t)rope_dim,
            .theta = theta,
            .freq_scale = freq_scale,
            .eps = eps,
            .consecutive_pairs = consecutive_pairs,
            .bias_rows = (uint32_t)bias_rows,
            .reserved0 = 0u,
            .reserved1 = 0u,
            .reserved2 = 0u,
        };
        const size_t store_total = MAX(projected_bytes / sizeof(float), gate_bytes / sizeof(float));
        [encoder setComputePipelineState:runtime->compressed_attention_store_component_pipeline];
        [encoder setBuffer:projected_buffer offset:projected_offset atIndex:0];
        [encoder setBuffer:gate_buffer offset:gate_offset atIndex:1];
        [encoder setBuffer:projected_cache_buffer offset:projected_cache_offset atIndex:2];
        [encoder setBuffer:gate_cache_buffer offset:gate_cache_offset atIndex:3];
        [encoder setBytes:&params length:sizeof(params) atIndex:4];
        [encoder dispatchThreads:MTLSizeMake(store_total, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->compressed_attention_store_component_pipeline, store_total), 1, 1)];
        [encoder setComputePipelineState:runtime->compressed_attention_update_component_pipeline];
        [encoder setBuffer:bias_buffer offset:bias_offset atIndex:0];
        [encoder setBuffer:norm_buffer offset:norm_offset atIndex:1];
        [encoder setBuffer:projected_cache_buffer offset:projected_cache_offset atIndex:2];
        [encoder setBuffer:gate_cache_buffer offset:gate_cache_offset atIndex:3];
        [encoder setBuffer:compressed_buffer offset:compressed_offset atIndex:4];
        [encoder setBuffer:positions_buffer offset:positions_offset atIndex:5];
        [encoder setBytes:&params length:sizeof(params) atIndex:6];
        [encoder dispatchThreads:MTLSizeMake(row_count, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->compressed_attention_update_component_pipeline, row_count), 1, 1)];
        [encoder endEncoding];
        if (frame_owned) {
            [command_buffer commit];
            [command_buffer waitUntilCompleted];
            return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -8;
        }
        return 0;
    }
}

int termite_metal_decode_runtime_compressed_attention_hybrid_attention_device(
    termite_metal_decode_runtime *runtime,
    void *q_handle,
    size_t q_offset,
    void *local_handle,
    size_t local_offset,
    void *compressed_handle,
    size_t compressed_offset,
    void *positions_handle,
    size_t positions_offset,
    void *index_handle,
    size_t index_offset,
    void *index_positions_handle,
    size_t index_positions_offset,
    void *index_query_handle,
    size_t index_query_offset,
    void *index_head_weights_handle,
    size_t index_head_weights_offset,
    void *selected_indices_handle,
    size_t selected_indices_offset,
    void *selected_scores_handle,
    size_t selected_scores_offset,
    void *sinks_handle,
    size_t sinks_offset,
    void *output_handle,
    size_t output_offset,
    size_t query_abs_start,
    size_t query_rows,
    size_t token_count,
    size_t compressed_rows,
    size_t num_heads,
    size_t head_dim,
    size_t sliding_window,
    size_t top_k,
    size_t index_rows,
    size_t index_heads,
    size_t index_head_dim
) {
    if (runtime == NULL || q_handle == NULL || local_handle == NULL || compressed_handle == NULL || positions_handle == NULL || sinks_handle == NULL || output_handle == NULL) return -1;
    if (runtime->compressed_attention_hybrid_attention_pipeline == nil) return -2;
    if (query_rows == 0 || token_count == 0 || num_heads == 0 || head_dim == 0) return -3;
    const bool has_indexer = (index_handle != NULL && index_positions_handle != NULL && index_query_handle != NULL && index_head_weights_handle != NULL && selected_indices_handle != NULL && selected_scores_handle != NULL && index_rows > 0 && index_heads > 0 && index_head_dim > 0 && top_k > 0);
    if (has_indexer && runtime->compressed_attention_select_compressed_pipeline == nil) return -2;
    if (query_abs_start > UINT32_MAX || query_rows > UINT32_MAX || token_count > UINT32_MAX || compressed_rows > UINT32_MAX || num_heads > UINT32_MAX || head_dim > UINT32_MAX || sliding_window > UINT32_MAX || top_k > UINT32_MAX || index_rows > UINT32_MAX || index_heads > UINT32_MAX || index_head_dim > UINT32_MAX) return -4;
    @autoreleasepool {
        id<MTLBuffer> q_buffer = (__bridge id<MTLBuffer>)q_handle;
        id<MTLBuffer> local_buffer = (__bridge id<MTLBuffer>)local_handle;
        id<MTLBuffer> compressed_buffer = (__bridge id<MTLBuffer>)compressed_handle;
        id<MTLBuffer> positions_buffer = (__bridge id<MTLBuffer>)positions_handle;
        id<MTLBuffer> sinks_buffer = (__bridge id<MTLBuffer>)sinks_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        id<MTLBuffer> index_buffer = has_indexer ? (__bridge id<MTLBuffer>)index_handle : compressed_buffer;
        id<MTLBuffer> index_positions_buffer = has_indexer ? (__bridge id<MTLBuffer>)index_positions_handle : positions_buffer;
        id<MTLBuffer> index_query_buffer = has_indexer ? (__bridge id<MTLBuffer>)index_query_handle : q_buffer;
        id<MTLBuffer> index_head_weights_buffer = has_indexer ? (__bridge id<MTLBuffer>)index_head_weights_handle : sinks_buffer;
        id<MTLBuffer> selected_indices_buffer = has_indexer ? (__bridge id<MTLBuffer>)selected_indices_handle : positions_buffer;
        id<MTLBuffer> selected_scores_buffer = has_indexer ? (__bridge id<MTLBuffer>)selected_scores_handle : sinks_buffer;
        size_t q_bytes = 0;
        size_t local_bytes = 0;
        size_t compressed_bytes = 0;
        size_t positions_bytes = 0;
        size_t sinks_bytes = 0;
        if (!termite_metal_size_mul3(query_rows, num_heads, head_dim, &q_bytes) ||
            !termite_metal_size_mul(q_bytes, sizeof(float), &q_bytes) ||
            !termite_metal_size_mul3(token_count, head_dim, sizeof(float), &local_bytes) ||
            !termite_metal_size_mul3(compressed_rows, head_dim, sizeof(float), &compressed_bytes) ||
            !termite_metal_size_mul(compressed_rows, sizeof(uint32_t), &positions_bytes) ||
            !termite_metal_size_mul(num_heads, sizeof(float), &sinks_bytes)) return -5;
        if (!termite_metal_buffer_range_valid(q_buffer, q_offset, q_bytes) ||
            !termite_metal_buffer_range_valid(local_buffer, local_offset, local_bytes) ||
            !termite_metal_buffer_range_valid(compressed_buffer, compressed_offset, compressed_bytes) ||
            !termite_metal_buffer_range_valid(positions_buffer, positions_offset, positions_bytes) ||
            !termite_metal_buffer_range_valid(sinks_buffer, sinks_offset, sinks_bytes) ||
            !termite_metal_buffer_range_valid(output_buffer, output_offset, q_bytes)) return -5;
        if (has_indexer) {
            size_t index_bytes = 0;
            size_t index_positions_bytes = 0;
            size_t index_query_bytes = 0;
            size_t index_head_weights_bytes = 0;
            size_t selected_indices_bytes = 0;
            size_t selected_scores_bytes = 0;
            const bool index_sizes_ok = termite_metal_size_mul3(index_rows, index_head_dim, sizeof(float), &index_bytes) &&
                termite_metal_size_mul(index_rows, sizeof(uint32_t), &index_positions_bytes) &&
                termite_metal_size_mul3(query_rows, index_heads, index_head_dim, &index_query_bytes) &&
                termite_metal_size_mul(index_query_bytes, sizeof(float), &index_query_bytes) &&
                termite_metal_size_mul3(query_rows, index_heads, sizeof(float), &index_head_weights_bytes) &&
                termite_metal_size_mul3(query_rows, top_k, sizeof(uint32_t), &selected_indices_bytes) &&
                termite_metal_size_mul3(query_rows, top_k, sizeof(float), &selected_scores_bytes);
            if (!index_sizes_ok ||
                !termite_metal_buffer_range_valid(index_buffer, index_offset, index_bytes) ||
                !termite_metal_buffer_range_valid(index_positions_buffer, index_positions_offset, index_positions_bytes) ||
                !termite_metal_buffer_range_valid(index_query_buffer, index_query_offset, index_query_bytes) ||
                !termite_metal_buffer_range_valid(index_head_weights_buffer, index_head_weights_offset, index_head_weights_bytes) ||
                !termite_metal_buffer_range_valid(selected_indices_buffer, selected_indices_offset, selected_indices_bytes) ||
                !termite_metal_buffer_range_valid(selected_scores_buffer, selected_scores_offset, selected_scores_bytes)) return -6;
        }
        const bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLCommandBuffer> command_buffer = frame_owned ? termite_metal_new_command_buffer(runtime->queue, __func__) : runtime->active_frame_cb;
        if (command_buffer == nil) return -7;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_ATTENTION);
        if (encoder == nil) return -8;
        termite_metal_compressed_attention_params params = {
            .query_abs_start = (uint32_t)query_abs_start,
            .query_rows = (uint32_t)query_rows,
            .token_count = (uint32_t)token_count,
            .compressed_rows = (uint32_t)compressed_rows,
            .num_heads = (uint32_t)num_heads,
            .head_dim = (uint32_t)head_dim,
            .sliding_window = (uint32_t)sliding_window,
            .top_k = (uint32_t)top_k,
            .has_indexer = has_indexer ? 1u : 0u,
            .index_rows = (uint32_t)index_rows,
            .index_heads = (uint32_t)index_heads,
            .index_head_dim = (uint32_t)index_head_dim,
            .has_sinks = 1u,
            .reserved0 = 0u,
            .scale = 1.0f / sqrtf((float)head_dim),
            .reserved1 = 0.0f,
        };
        if (has_indexer) {
            [encoder setComputePipelineState:runtime->compressed_attention_select_compressed_pipeline];
            [encoder setBuffer:index_buffer offset:index_offset atIndex:0];
            [encoder setBuffer:index_positions_buffer offset:index_positions_offset atIndex:1];
            [encoder setBuffer:index_query_buffer offset:index_query_offset atIndex:2];
            [encoder setBuffer:index_head_weights_buffer offset:index_head_weights_offset atIndex:3];
            [encoder setBuffer:selected_indices_buffer offset:selected_indices_offset atIndex:4];
            [encoder setBuffer:selected_scores_buffer offset:selected_scores_offset atIndex:5];
            [encoder setBytes:&params length:sizeof(params) atIndex:6];
            [encoder dispatchThreads:MTLSizeMake(query_rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->compressed_attention_select_compressed_pipeline, query_rows), 1, 1)];
        }
        [encoder setComputePipelineState:runtime->compressed_attention_hybrid_attention_pipeline];
        [encoder setBuffer:q_buffer offset:q_offset atIndex:0];
        [encoder setBuffer:local_buffer offset:local_offset atIndex:1];
        [encoder setBuffer:compressed_buffer offset:compressed_offset atIndex:2];
        [encoder setBuffer:positions_buffer offset:positions_offset atIndex:3];
        [encoder setBuffer:selected_indices_buffer offset:selected_indices_offset atIndex:4];
        [encoder setBuffer:sinks_buffer offset:sinks_offset atIndex:5];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:6];
        [encoder setBytes:&params length:sizeof(params) atIndex:7];
        const size_t total = query_rows * num_heads;
        [encoder dispatchThreads:MTLSizeMake(total, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->compressed_attention_hybrid_attention_pipeline, total), 1, 1)];
        [encoder endEncoding];
        if (frame_owned) {
            [command_buffer commit];
            [command_buffer waitUntilCompleted];
            return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -9;
        }
        return 0;
    }
}

int termite_metal_decode_runtime_prepare_layer_norm(
    termite_metal_decode_runtime *runtime,
    size_t slot,
    const float *weight,
    const float *bias,
    size_t hidden_size
) {
    if (runtime == NULL || weight == NULL || bias == NULL) return -1;
    if (runtime->device == nil || runtime->queue == nil || runtime->library == nil) return -2;
    if (slot >= TERMITE_METAL_LAYER_NORM_SLOT_CAPACITY || hidden_size == 0) return -3;
    @autoreleasepool {
        const size_t bytes = hidden_size * sizeof(float);
        id<MTLBuffer> weight_buffer = termite_metal_make_private_buffer_with_bytes(runtime->device, runtime->queue, weight, bytes);
        id<MTLBuffer> bias_buffer = termite_metal_make_private_buffer_with_bytes(runtime->device, runtime->queue, bias, bytes);
        if (weight_buffer == nil || bias_buffer == nil) return -4;
        runtime->layer_norm_weight_buffers[slot] = weight_buffer;
        runtime->layer_norm_bias_buffers[slot] = bias_buffer;
        runtime->layer_norm_hidden_sizes[slot] = hidden_size;
        runtime->layer_norm_slot_prepared[slot] = 1;
        return 0;
    }
}

int termite_metal_decode_runtime_apply_layer_norm(
    termite_metal_decode_runtime *runtime,
    size_t slot,
    const float *input,
    size_t rows,
    size_t hidden_size,
    float eps,
    float *output
) {
    if (runtime == NULL || input == NULL || output == NULL) return -1;
    if (runtime->layer_norm_pipeline == nil) return -2;
    if (slot >= TERMITE_METAL_LAYER_NORM_SLOT_CAPACITY || rows == 0 || hidden_size == 0) return -3;
    if (runtime->layer_norm_slot_prepared[slot] == 0) return -4;
    if (runtime->layer_norm_hidden_sizes[slot] != hidden_size) return -5;
    if (rows > UINT32_MAX || hidden_size > UINT32_MAX) return -6;
    @autoreleasepool {
        const size_t bytes = rows * hidden_size * sizeof(float);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)input length:bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:input length:bytes options:MTLResourceStorageModeShared];
        BOOL output_borrowed = YES;
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithBytesNoCopy:output length:bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) {
            output_borrowed = NO;
            output_buffer = [runtime->device newBufferWithBytes:output length:bytes options:MTLResourceStorageModeShared];
        }
        if (input_buffer == nil || output_buffer == nil) return -7;
        termite_metal_apply_layer_norm_params params = {
            .hidden_size = (uint32_t)hidden_size,
            .eps = eps,
        };
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -9;
        if (!frame_owned) {
            if (termite_metal_decode_runtime_retain_frame_resource(runtime, input_buffer) != 0) return -8;
            if (termite_metal_decode_runtime_retain_frame_resource(runtime, output_buffer) != 0) return -8;
        }
        id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
        const BOOL planned_encoder = (encoder != nil);
        if (!planned_encoder) {
            encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_RMS_NORM);
            if (encoder == nil) return -10;
        }
        termite_metal_planned_encoder_range accesses[4];
        if (termite_metal_planned_range_make(input_buffer, 0, bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[0], -10) != 0 ||
            termite_metal_planned_range_make(runtime->layer_norm_weight_buffers[slot], 0, runtime->layer_norm_weight_buffers[slot].length, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[1], -10) != 0 ||
            termite_metal_planned_range_make(runtime->layer_norm_bias_buffers[slot], 0, runtime->layer_norm_bias_buffers[slot].length, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[2], -10) != 0 ||
            termite_metal_planned_range_make(output_buffer, 0, bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &accesses[3], -10) != 0 ||
            termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, accesses, 4, -10) != 0)
        {
            return -10;
        }
        [encoder setComputePipelineState:runtime->layer_norm_pipeline];
        [encoder setBuffer:input_buffer offset:0 atIndex:0];
        [encoder setBuffer:runtime->layer_norm_weight_buffers[slot] offset:0 atIndex:1];
        [encoder setBuffer:runtime->layer_norm_bias_buffers[slot] offset:0 atIndex:2];
        [encoder setBuffer:output_buffer offset:0 atIndex:3];
        [encoder setBytes:&params length:sizeof(params) atIndex:4];
        [encoder dispatchThreads:MTLSizeMake(rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(1, 1, 1)];
        if (!planned_encoder) [encoder endEncoding];
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -11);
    }
}

int termite_metal_decode_runtime_prepare_rms_norm(
    termite_metal_decode_runtime *runtime,
    size_t slot,
    const float *weight,
    size_t hidden_size
) {
    if (runtime == NULL || weight == NULL) return -1;
    if (runtime->device == nil || runtime->queue == nil || runtime->library == nil) return -2;
    if (slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY || hidden_size == 0) return -3;
    @autoreleasepool {
        const size_t bytes = hidden_size * sizeof(float);
        id<MTLBuffer> weight_buffer = termite_metal_make_private_buffer_with_bytes(runtime->device, runtime->queue, weight, bytes);
        if (weight_buffer == nil) return -4;
        runtime->rms_norm_weight_buffers[slot] = weight_buffer;
        runtime->rms_norm_hidden_sizes[slot] = hidden_size;
        runtime->rms_norm_slot_prepared[slot] = 1;
        return 0;
    }
}

int termite_metal_decode_runtime_prepare_rms_norm_from_buffer(
    termite_metal_decode_runtime *runtime,
    size_t slot,
    void *weight_handle,
    size_t weight_offset,
    size_t hidden_size
) {
    if (runtime == NULL || weight_handle == NULL) return -1;
    if (runtime->device == nil || runtime->queue == nil || runtime->library == nil) return -2;
    if (slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY || hidden_size == 0) return -3;
    @autoreleasepool {
        const size_t bytes = hidden_size * sizeof(float);
        id<MTLBuffer> source_buffer = (__bridge id<MTLBuffer>)weight_handle;
        if (source_buffer == nil || weight_offset > source_buffer.length || bytes > source_buffer.length - weight_offset) return -4;
        id<MTLBuffer> weight_buffer = [runtime->device newBufferWithLength:bytes options:MTLResourceStorageModePrivate];
        if (weight_buffer == nil) return -5;

        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -6;
        termite_metal_record_active_frame_blit_source(command_buffer, TERMITE_METAL_BLIT_SOURCE_BUFFER_COPY);
        if (!frame_owned && termite_metal_decode_runtime_retain_frame_resource(runtime, source_buffer) != 0) return -7;
        id<MTLBlitCommandEncoder> blit = termite_metal_tracked_blit_command_encoder(command_buffer);
        if (blit == nil) return -8;
        if ([blit respondsToSelector:@selector(setLabel:)]) {
            blit.label = @"prepare-rms-norm-weight";
        }
        [blit copyFromBuffer:source_buffer sourceOffset:weight_offset toBuffer:weight_buffer destinationOffset:0 size:bytes];
        [blit endEncoding];
        const int rc = termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -9);
        if (rc != 0) return rc;

        runtime->rms_norm_weight_buffers[slot] = weight_buffer;
        runtime->rms_norm_hidden_sizes[slot] = hidden_size;
        runtime->rms_norm_slot_prepared[slot] = 1;
        return 0;
    }
}

static int termite_metal_decode_runtime_ensure_hot_buffers(
    termite_metal_decode_runtime *runtime,
    size_t hidden_bytes,
    size_t intermediate_bytes
) {
    if (runtime == NULL) return -1;
    bool hidden_planned_ready = true;
    if (hidden_bytes > 0) {
        for (size_t i = 0; i < 4; ++i) {
            const size_t slot = TERMITE_METAL_GRAPH_PLAN_HOT_HIDDEN_SLOT_BASE + i;
            const size_t planned_bytes = runtime->graph_plan_requested_bytes[slot] > hidden_bytes
                ? runtime->graph_plan_requested_bytes[slot]
                : hidden_bytes;
            if (!termite_metal_decode_runtime_graph_slot_ready(runtime, slot, planned_bytes)) {
                hidden_planned_ready = false;
                break;
            }
        }
    }
    bool intermediate_planned_ready = true;
    if (intermediate_bytes > 0) {
        for (size_t i = 0; i < 5; ++i) {
            const size_t slot = TERMITE_METAL_GRAPH_PLAN_HOT_INTERMEDIATE_SLOT_BASE + i;
            const size_t planned_bytes = runtime->graph_plan_requested_bytes[slot] > intermediate_bytes
                ? runtime->graph_plan_requested_bytes[slot]
                : intermediate_bytes;
            if (!termite_metal_decode_runtime_graph_slot_ready(runtime, slot, planned_bytes)) {
                intermediate_planned_ready = false;
                break;
            }
        }
    }
    if (hidden_planned_ready && intermediate_planned_ready) {
        if (hidden_bytes > 0) {
            for (size_t i = 0; i < 4; ++i) {
                runtime->hot_hidden_buffers[i] = runtime->graph_plan_buffers[TERMITE_METAL_GRAPH_PLAN_HOT_HIDDEN_SLOT_BASE + i];
            }
            runtime->hot_hidden_capacity = hidden_bytes;
        }
        if (intermediate_bytes > 0) {
            for (size_t i = 0; i < 5; ++i) {
                runtime->hot_intermediate_buffers[i] = runtime->graph_plan_buffers[TERMITE_METAL_GRAPH_PLAN_HOT_INTERMEDIATE_SLOT_BASE + i];
            }
            runtime->hot_intermediate_capacity = intermediate_bytes;
        }
        return 0;
    }
    if (runtime->active_frame_cb != nil) return -6;
    @autoreleasepool {
        if (hidden_bytes > 0) {
            if (hidden_bytes > runtime->hot_hidden_capacity) {
                for (size_t i = 0; i < 4; ++i) {
                    id<MTLBuffer> buffer = [runtime->device newBufferWithLength:hidden_bytes options:MTLResourceStorageModePrivate];
                    if (buffer == nil) return -2;
                    runtime->hot_hidden_buffers[i] = buffer;
                }
                runtime->hot_hidden_capacity = hidden_bytes;
            } else {
                for (size_t i = 0; i < 4; ++i) {
                    if (runtime->hot_hidden_buffers[i] == nil) {
                        id<MTLBuffer> buffer = [runtime->device newBufferWithLength:hidden_bytes options:MTLResourceStorageModePrivate];
                        if (buffer == nil) return -3;
                        runtime->hot_hidden_buffers[i] = buffer;
                    }
                }
            }
        }
        if (intermediate_bytes > 0) {
            if (intermediate_bytes > runtime->hot_intermediate_capacity) {
                for (size_t i = 0; i < 5; ++i) {
                    id<MTLBuffer> buffer = [runtime->device newBufferWithLength:intermediate_bytes options:MTLResourceStorageModePrivate];
                    if (buffer == nil) return -4;
                    runtime->hot_intermediate_buffers[i] = buffer;
                }
                runtime->hot_intermediate_capacity = intermediate_bytes;
            } else {
                for (size_t i = 0; i < 5; ++i) {
                    if (runtime->hot_intermediate_buffers[i] == nil) {
                        id<MTLBuffer> buffer = [runtime->device newBufferWithLength:intermediate_bytes options:MTLResourceStorageModePrivate];
                        if (buffer == nil) return -5;
                        runtime->hot_intermediate_buffers[i] = buffer;
                    }
                }
            }
        }
        return 0;
    }
}

static int termite_metal_decode_runtime_ensure_i2s_quantize_buffers(
    termite_metal_decode_runtime *runtime,
    size_t qinput_bytes,
    size_t scale_bytes
) {
    if (runtime == NULL) return -1;
    @autoreleasepool {
        if (qinput_bytes > 0) {
            if (qinput_bytes > runtime->i2s_qinput_capacity || runtime->i2s_qinput_buffer == nil) {
                id<MTLBuffer> buffer = [runtime->device newBufferWithLength:qinput_bytes options:MTLResourceStorageModePrivate];
                if (buffer == nil) return -2;
                runtime->i2s_qinput_buffer = buffer;
                runtime->i2s_qinput_capacity = qinput_bytes;
            }
        }
        if (scale_bytes > 0) {
            if (scale_bytes > runtime->i2s_scale_capacity || runtime->i2s_scale_buffer == nil) {
                id<MTLBuffer> buffer = [runtime->device newBufferWithLength:scale_bytes options:MTLResourceStorageModePrivate];
                if (buffer == nil) return -3;
                runtime->i2s_scale_buffer = buffer;
                runtime->i2s_scale_capacity = scale_bytes;
            }
        }
        return 0;
    }
}

static int termite_metal_decode_runtime_ensure_direct_block_hidden_buffers(
    termite_metal_decode_runtime *runtime,
    size_t hidden_bytes
) {
    if (runtime == NULL) return -1;
    if (hidden_bytes == 0) return 0;
    bool planned_ready = true;
    for (size_t i = 0; i < TERMITE_METAL_DIRECT_BLOCK_HIDDEN_BUFFER_COUNT; ++i) {
        const size_t slot = TERMITE_METAL_GRAPH_PLAN_DIRECT_HIDDEN_SLOT_BASE + i;
        if (!termite_metal_decode_runtime_graph_slot_ready(runtime, slot, hidden_bytes)) {
            planned_ready = false;
            break;
        }
    }
    if (!planned_ready) {
        if (runtime->active_frame_cb != nil) return -5;
        if (runtime->graph_plan_active == 0) {
            if (termite_metal_decode_runtime_begin_graph_plan(runtime) != 0) return -2;
        }
        for (size_t i = 0; i < TERMITE_METAL_DIRECT_BLOCK_HIDDEN_BUFFER_COUNT; ++i) {
            const size_t slot = TERMITE_METAL_GRAPH_PLAN_DIRECT_HIDDEN_SLOT_BASE + i;
            if (termite_metal_decode_runtime_reserve_graph_plan_slot(runtime, slot, hidden_bytes) != 0) return -3;
        }
        if (termite_metal_decode_runtime_commit_graph_plan(runtime) != 0) return -4;
    }
    planned_ready = true;
    for (size_t i = 0; i < TERMITE_METAL_DIRECT_BLOCK_HIDDEN_BUFFER_COUNT; ++i) {
        const size_t slot = TERMITE_METAL_GRAPH_PLAN_DIRECT_HIDDEN_SLOT_BASE + i;
        if (!termite_metal_decode_runtime_graph_slot_ready(runtime, slot, hidden_bytes)) {
            planned_ready = false;
            break;
        }
    }
    if (planned_ready) {
        for (size_t i = 0; i < TERMITE_METAL_DIRECT_BLOCK_HIDDEN_BUFFER_COUNT; ++i) {
            const size_t slot = TERMITE_METAL_GRAPH_PLAN_DIRECT_HIDDEN_SLOT_BASE + i;
            runtime->direct_block_hidden_buffers[i] = runtime->graph_plan_buffers[slot];
        }
        runtime->direct_block_hidden_capacity = hidden_bytes;
        return 0;
    }
    @autoreleasepool {
        if (hidden_bytes > runtime->direct_block_hidden_capacity) {
            for (size_t i = 0; i < TERMITE_METAL_DIRECT_BLOCK_HIDDEN_BUFFER_COUNT; ++i) {
                id<MTLBuffer> buffer = [runtime->device newBufferWithLength:hidden_bytes options:MTLResourceStorageModePrivate];
                if (buffer == nil) return -2;
                runtime->direct_block_hidden_buffers[i] = buffer;
            }
            runtime->direct_block_hidden_capacity = hidden_bytes;
            return 0;
        }
        for (size_t i = 0; i < TERMITE_METAL_DIRECT_BLOCK_HIDDEN_BUFFER_COUNT; ++i) {
            if (runtime->direct_block_hidden_buffers[i] == nil) {
                id<MTLBuffer> buffer = [runtime->device newBufferWithLength:hidden_bytes options:MTLResourceStorageModePrivate];
                if (buffer == nil) return -3;
                runtime->direct_block_hidden_buffers[i] = buffer;
            }
        }
        return 0;
    }
}

static int termite_metal_decode_runtime_ensure_direct_block_shared_buffer(
    termite_metal_decode_runtime *runtime,
    size_t index,
    size_t bytes
) {
    if (runtime == NULL || index >= TERMITE_METAL_DIRECT_BLOCK_SHARED_BUFFER_COUNT) return -1;
    if (bytes == 0) return 0;
    @autoreleasepool {
        if (bytes > runtime->direct_block_shared_capacities[index] ||
            runtime->direct_block_shared_buffers[index] == nil ||
            runtime->direct_block_shared_buffers[index].storageMode != MTLStorageModeShared)
        {
            id<MTLBuffer> buffer = [runtime->device newBufferWithLength:bytes options:MTLResourceStorageModeShared];
            if (buffer == nil) return -2;
            runtime->direct_block_shared_buffers[index] = buffer;
            runtime->direct_block_shared_capacities[index] = bytes;
        }
        return 0;
    }
}

static int termite_metal_decode_runtime_ensure_direct_block_q_buffer(
    termite_metal_decode_runtime *runtime,
    size_t q_bytes
) {
    if (runtime == NULL) return -1;
    if (q_bytes == 0) return 0;
    if (termite_metal_decode_runtime_ensure_graph_plan_slot(runtime, TERMITE_METAL_GRAPH_PLAN_DIRECT_Q_SLOT, q_bytes) == 0) {
        runtime->direct_block_q_buffer = runtime->graph_plan_buffers[TERMITE_METAL_GRAPH_PLAN_DIRECT_Q_SLOT];
        runtime->direct_block_q_capacity = runtime->graph_plan_capacities[TERMITE_METAL_GRAPH_PLAN_DIRECT_Q_SLOT];
        return 0;
    }
    if (runtime->active_frame_cb != nil) return -3;
    @autoreleasepool {
        if (q_bytes > runtime->direct_block_q_capacity || runtime->direct_block_q_buffer == nil) {
            id<MTLBuffer> buffer = [runtime->device newBufferWithLength:q_bytes options:MTLResourceStorageModePrivate];
            if (buffer == nil) return -2;
            runtime->direct_block_q_buffer = buffer;
            runtime->direct_block_q_capacity = q_bytes;
        }
        return 0;
    }
}

static int termite_metal_decode_runtime_sample_tail_scratch_sizes(
    size_t out_dim,
    size_t top_k,
    size_t *logits_bytes,
    size_t *topk_bytes,
    size_t *probs_bytes
) {
    if (logits_bytes == NULL || topk_bytes == NULL || probs_bytes == NULL) return -1;
    if (out_dim == 0 || out_dim > SIZE_MAX / sizeof(float)) return -2;
    *logits_bytes = out_dim * sizeof(float);
    *topk_bytes = 0;
    *probs_bytes = out_dim * sizeof(float);
    if (top_k > 0 && top_k <= 64) {
        const size_t actual_top_k = top_k < out_dim ? top_k : out_dim;
        const size_t block_size = 512;
        const size_t block_count = (out_dim + block_size - 1) / block_size;
        if (actual_top_k == 0 || block_count == 0 || block_count > SIZE_MAX / actual_top_k) return -3;
        const size_t candidate_count = block_count * actual_top_k;
        if (candidate_count > SIZE_MAX / sizeof(float)) return -4;
        *topk_bytes = candidate_count * sizeof(float);
        *probs_bytes = actual_top_k * sizeof(float);
    }
    return 0;
}

int termite_metal_decode_runtime_reserve_sample_tail_scratch(
    termite_metal_decode_runtime *runtime,
    size_t out_dim,
    size_t top_k
) {
    if (runtime == NULL) return -1;
    size_t logits_bytes = 0;
    size_t topk_bytes = 0;
    size_t probs_bytes = 0;
    if (termite_metal_decode_runtime_sample_tail_scratch_sizes(out_dim, top_k, &logits_bytes, &topk_bytes, &probs_bytes) != 0) return -2;
    size_t slots[4] = {
        TERMITE_METAL_GRAPH_PLAN_SAMPLE_LOGITS_SLOT,
        TERMITE_METAL_GRAPH_PLAN_SAMPLE_PROBS_SLOT,
        TERMITE_METAL_GRAPH_PLAN_SAMPLE_TOPK_VALUES_SLOT,
        TERMITE_METAL_GRAPH_PLAN_SAMPLE_TOPK_IDS_SLOT,
    };
    size_t bytes[4] = {
        logits_bytes,
        probs_bytes,
        topk_bytes,
        topk_bytes,
    };
    if (termite_metal_decode_runtime_reserve_graph_plan_slots(runtime, slots, bytes, 4) != 0) return -3;
    runtime->sample_logits_buffer = runtime->graph_plan_buffers[TERMITE_METAL_GRAPH_PLAN_SAMPLE_LOGITS_SLOT];
    runtime->sample_logits_capacity = runtime->graph_plan_capacities[TERMITE_METAL_GRAPH_PLAN_SAMPLE_LOGITS_SLOT];
    runtime->scratch_buffer = runtime->graph_plan_buffers[TERMITE_METAL_GRAPH_PLAN_SAMPLE_PROBS_SLOT];
    runtime->scratch_capacity = runtime->graph_plan_capacities[TERMITE_METAL_GRAPH_PLAN_SAMPLE_PROBS_SLOT];
    if (topk_bytes > 0) {
        runtime->sample_topk_values_buffer = runtime->graph_plan_buffers[TERMITE_METAL_GRAPH_PLAN_SAMPLE_TOPK_VALUES_SLOT];
        runtime->sample_topk_ids_buffer = runtime->graph_plan_buffers[TERMITE_METAL_GRAPH_PLAN_SAMPLE_TOPK_IDS_SLOT];
        runtime->sample_topk_capacity = runtime->graph_plan_capacities[TERMITE_METAL_GRAPH_PLAN_SAMPLE_TOPK_VALUES_SLOT];
    }
    return 0;
}

int termite_metal_decode_runtime_reserve_attention_span_scratch(
    termite_metal_decode_runtime *runtime,
    size_t kv_tokens,
    size_t key_row_bytes,
    size_t v_row_stride
) {
    if (runtime == NULL) return -1;
    if (kv_tokens == 0 || key_row_bytes == 0 || v_row_stride == 0) return -2;
    if (kv_tokens > SIZE_MAX / key_row_bytes) return -3;
    if (kv_tokens > SIZE_MAX / v_row_stride) return -4;
    const size_t encoded_bytes = kv_tokens * key_row_bytes;
    const size_t v_elements = kv_tokens * v_row_stride;
    if (v_elements > SIZE_MAX / sizeof(float)) return -5;
    const size_t v_bytes = v_elements * sizeof(float);
    size_t slots[2] = {
        TERMITE_METAL_GRAPH_PLAN_ATTENTION_SPAN_KEY_SLOT,
        TERMITE_METAL_GRAPH_PLAN_ATTENTION_SPAN_VALUE_SLOT,
    };
    size_t bytes[2] = {
        encoded_bytes,
        v_bytes,
    };
    if (termite_metal_decode_runtime_reserve_graph_plan_slots(runtime, slots, bytes, 2) != 0) return -6;
    runtime->attention_span_encoded_key_buffers[0] = runtime->graph_plan_buffers[TERMITE_METAL_GRAPH_PLAN_ATTENTION_SPAN_KEY_SLOT];
    runtime->attention_span_v_buffers[0] = runtime->graph_plan_buffers[TERMITE_METAL_GRAPH_PLAN_ATTENTION_SPAN_VALUE_SLOT];
    runtime->attention_span_encoded_key_capacities[0] = runtime->graph_plan_capacities[TERMITE_METAL_GRAPH_PLAN_ATTENTION_SPAN_KEY_SLOT];
    runtime->attention_span_v_capacities[0] = runtime->graph_plan_capacities[TERMITE_METAL_GRAPH_PLAN_ATTENTION_SPAN_VALUE_SLOT];
    runtime->attention_span_encoded_key_buffer = runtime->attention_span_encoded_key_buffers[0];
    runtime->attention_span_v_buffer = runtime->attention_span_v_buffers[0];
    runtime->attention_span_encoded_key_capacity = runtime->attention_span_encoded_key_capacities[0];
    runtime->attention_span_v_capacity = runtime->attention_span_v_capacities[0];
    return 0;
}

static int termite_metal_decode_runtime_ensure_sample_logits_buffer(
    termite_metal_decode_runtime *runtime,
    size_t logits_bytes
) {
    if (runtime == NULL) return -1;
    if (logits_bytes == 0) return 0;
    if (termite_metal_decode_runtime_ensure_graph_plan_slot(runtime, TERMITE_METAL_GRAPH_PLAN_SAMPLE_LOGITS_SLOT, logits_bytes) == 0) {
        runtime->sample_logits_buffer = runtime->graph_plan_buffers[TERMITE_METAL_GRAPH_PLAN_SAMPLE_LOGITS_SLOT];
        runtime->sample_logits_capacity = runtime->graph_plan_capacities[TERMITE_METAL_GRAPH_PLAN_SAMPLE_LOGITS_SLOT];
        return 0;
    }
    if (runtime->active_frame_cb != nil) return -2;
    @autoreleasepool {
        if (logits_bytes > runtime->sample_logits_capacity || runtime->sample_logits_buffer == nil) {
            id<MTLBuffer> logits_buffer = [runtime->device newBufferWithLength:logits_bytes options:MTLResourceStorageModePrivate];
            if (logits_buffer == nil) return -2;
            runtime->sample_logits_buffer = logits_buffer;
            runtime->sample_logits_capacity = logits_bytes;
        }
        return 0;
    }
}

static int termite_metal_decode_runtime_ensure_sample_probs_buffer(
    termite_metal_decode_runtime *runtime,
    size_t probs_bytes
) {
    if (runtime == NULL) return -1;
    if (probs_bytes == 0) return 0;
    if (termite_metal_decode_runtime_ensure_graph_plan_slot(runtime, TERMITE_METAL_GRAPH_PLAN_SAMPLE_PROBS_SLOT, probs_bytes) == 0) {
        runtime->scratch_buffer = runtime->graph_plan_buffers[TERMITE_METAL_GRAPH_PLAN_SAMPLE_PROBS_SLOT];
        runtime->scratch_capacity = runtime->graph_plan_capacities[TERMITE_METAL_GRAPH_PLAN_SAMPLE_PROBS_SLOT];
        return 0;
    }
    if (runtime->active_frame_cb != nil) return -2;
    @autoreleasepool {
        if (probs_bytes > runtime->scratch_capacity || runtime->scratch_buffer == nil) {
            id<MTLBuffer> scratch = [runtime->device newBufferWithLength:probs_bytes options:MTLResourceStorageModePrivate];
            if (scratch == nil) return -3;
            runtime->scratch_buffer = scratch;
            runtime->scratch_capacity = probs_bytes;
        }
        return 0;
    }
}

static int termite_metal_decode_runtime_ensure_sample_topk_buffers(
    termite_metal_decode_runtime *runtime,
    size_t partial_bytes
) {
    if (runtime == NULL) return -1;
    if (partial_bytes == 0) return 0;
    const bool values_ready = termite_metal_decode_runtime_ensure_graph_plan_slot(runtime, TERMITE_METAL_GRAPH_PLAN_SAMPLE_TOPK_VALUES_SLOT, partial_bytes) == 0;
    const bool ids_ready = termite_metal_decode_runtime_ensure_graph_plan_slot(runtime, TERMITE_METAL_GRAPH_PLAN_SAMPLE_TOPK_IDS_SLOT, partial_bytes) == 0;
    if (values_ready && ids_ready) {
        runtime->sample_topk_values_buffer = runtime->graph_plan_buffers[TERMITE_METAL_GRAPH_PLAN_SAMPLE_TOPK_VALUES_SLOT];
        runtime->sample_topk_ids_buffer = runtime->graph_plan_buffers[TERMITE_METAL_GRAPH_PLAN_SAMPLE_TOPK_IDS_SLOT];
        runtime->sample_topk_capacity = partial_bytes;
        return 0;
    }
    if (runtime->active_frame_cb != nil) return -2;
    @autoreleasepool {
        if (partial_bytes > runtime->sample_topk_capacity || runtime->sample_topk_values_buffer == nil || runtime->sample_topk_ids_buffer == nil) {
            id<MTLBuffer> values = [runtime->device newBufferWithLength:partial_bytes options:MTLResourceStorageModePrivate];
            id<MTLBuffer> ids = [runtime->device newBufferWithLength:partial_bytes options:MTLResourceStorageModePrivate];
            if (values == nil || ids == nil) return -2;
            runtime->sample_topk_values_buffer = values;
            runtime->sample_topk_ids_buffer = ids;
            runtime->sample_topk_capacity = partial_bytes;
        }
        return 0;
    }
}

static int termite_metal_encode_argmax_logits_on_encoder(
    termite_metal_decode_runtime *runtime,
    id<MTLComputeCommandEncoder> encoder,
    id<MTLBuffer> logits_buffer,
    size_t logits_offset,
    size_t out_dim,
    int failure_code
) {
    if (runtime == NULL || encoder == nil || logits_buffer == nil) return failure_code;
    if (runtime->argmax_logits_pipeline == nil) return failure_code;
    if (out_dim == 0 || out_dim > UINT32_MAX) return failure_code;
    size_t logits_bytes = 0;
    if (!termite_metal_size_mul(out_dim, sizeof(float), &logits_bytes)) return failure_code;
    if (runtime->token_capacity < sizeof(uint32_t) || runtime->token_buffer == nil || runtime->token_buffer.storageMode != MTLStorageModeShared) {
        id<MTLBuffer> token = [runtime->device newBufferWithLength:sizeof(uint32_t) options:MTLResourceStorageModeShared];
        if (token == nil) return failure_code;
        runtime->token_buffer = token;
        runtime->token_capacity = sizeof(uint32_t);
    }

    const uint32_t out_dim_u32 = (uint32_t)out_dim;
    const bool use_parallel = out_dim >= 2048u &&
        runtime->argmax_logits_partials_pipeline != nil &&
        runtime->argmax_logits_reduce_pipeline != nil;
    if (!use_parallel) {
        if (termite_metal_decode_runtime_prepare_planned_compute_unary_accesses(
                runtime,
                logits_buffer,
                logits_offset,
                logits_bytes,
                runtime->token_buffer,
                0,
                sizeof(uint32_t),
                failure_code) != 0)
        {
            return failure_code;
        }
        [encoder setComputePipelineState:runtime->argmax_logits_pipeline];
        [encoder setBuffer:logits_buffer offset:logits_offset atIndex:0];
        [encoder setBuffer:runtime->token_buffer offset:0 atIndex:1];
        [encoder setBytes:&out_dim_u32 length:sizeof(out_dim_u32) atIndex:2];
        [encoder dispatchThreads:MTLSizeMake(1, 1, 1) threadsPerThreadgroup:MTLSizeMake(1, 1, 1)];
        return 0;
    }

    const size_t block_size = 1024u;
    const size_t block_count = (out_dim + block_size - 1u) / block_size;
    if (block_count == 0 || block_count > UINT32_MAX) return failure_code;
    const size_t partial_bytes = block_count * sizeof(float);
    if (termite_metal_decode_runtime_ensure_sample_topk_buffers(runtime, partial_bytes) != 0) return failure_code;
    if (runtime->sample_topk_values_buffer == nil || runtime->sample_topk_ids_buffer == nil) return failure_code;
    termite_metal_planned_encoder_range partial_accesses[3];
    if (termite_metal_planned_range_make(logits_buffer, logits_offset, logits_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &partial_accesses[0], failure_code) != 0 ||
        termite_metal_planned_range_make(runtime->sample_topk_values_buffer, 0, partial_bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &partial_accesses[1], failure_code) != 0 ||
        termite_metal_planned_range_make(runtime->sample_topk_ids_buffer, 0, partial_bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &partial_accesses[2], failure_code) != 0 ||
        termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, partial_accesses, 3, failure_code) != 0)
    {
        return failure_code;
    }

    [encoder setComputePipelineState:runtime->argmax_logits_partials_pipeline];
    [encoder setBuffer:logits_buffer offset:logits_offset atIndex:0];
    [encoder setBuffer:runtime->sample_topk_values_buffer offset:0 atIndex:1];
    [encoder setBuffer:runtime->sample_topk_ids_buffer offset:0 atIndex:2];
    [encoder setBytes:&out_dim_u32 length:sizeof(out_dim_u32) atIndex:3];
    [encoder setThreadgroupMemoryLength:256u * sizeof(float) atIndex:0];
    [encoder setThreadgroupMemoryLength:256u * sizeof(uint32_t) atIndex:1];
    [encoder dispatchThreadgroups:MTLSizeMake(block_count, 1, 1) threadsPerThreadgroup:MTLSizeMake(256, 1, 1)];

    if (runtime->active_planned_compute_encoder == encoder) {
        termite_metal_planned_encoder_range reduce_accesses[3];
        if (termite_metal_planned_range_make(runtime->sample_topk_values_buffer, 0, partial_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &reduce_accesses[0], failure_code) != 0 ||
            termite_metal_planned_range_make(runtime->sample_topk_ids_buffer, 0, partial_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &reduce_accesses[1], failure_code) != 0 ||
            termite_metal_planned_range_make(runtime->token_buffer, 0, sizeof(uint32_t), TERMITE_METAL_PLANNED_RANGE_WRITE, &reduce_accesses[2], failure_code) != 0 ||
            termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, reduce_accesses, 3, failure_code) != 0)
        {
            return failure_code;
        }
    } else {
        [encoder memoryBarrierWithScope:MTLBarrierScopeBuffers];
    }

    const uint32_t block_count_u32 = (uint32_t)block_count;
    [encoder setComputePipelineState:runtime->argmax_logits_reduce_pipeline];
    [encoder setBuffer:runtime->sample_topk_values_buffer offset:0 atIndex:0];
    [encoder setBuffer:runtime->sample_topk_ids_buffer offset:0 atIndex:1];
    [encoder setBuffer:runtime->token_buffer offset:0 atIndex:2];
    [encoder setBytes:&block_count_u32 length:sizeof(block_count_u32) atIndex:3];
    [encoder dispatchThreads:MTLSizeMake(1, 1, 1) threadsPerThreadgroup:MTLSizeMake(1, 1, 1)];
    return 0;
}

static int termite_metal_decode_runtime_ensure_attention_span_slot_buffers(
    termite_metal_decode_runtime *runtime,
    size_t slot,
    size_t encoded_bytes,
    size_t v_bytes
) {
    if (runtime == NULL || slot >= TERMITE_METAL_ATTENTION_SPAN_SLOT_CAPACITY) return -1;
    if (encoded_bytes == 0 || v_bytes == 0) return -2;
    @autoreleasepool {
        if (encoded_bytes > runtime->attention_span_encoded_key_capacities[slot] ||
            runtime->attention_span_encoded_key_buffers[slot] == nil)
        {
            id<MTLBuffer> old_buffer = runtime->attention_span_encoded_key_buffers[slot];
            const size_t old_capacity = runtime->attention_span_encoded_key_capacities[slot];
            id<MTLBuffer> buffer = [runtime->device newBufferWithLength:encoded_bytes options:MTLResourceStorageModePrivate];
            if (buffer == nil) return -5;
            if (old_buffer != nil && old_capacity > 0) {
                bool frame_owned = false;
                id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
                if (command_buffer == nil) return -7;
                if (!frame_owned && termite_metal_decode_runtime_retain_frame_resource(runtime, old_buffer) != 0) return -8;
                id<MTLBlitCommandEncoder> blit = termite_metal_tracked_blit_command_encoder(command_buffer);
                if (blit == nil) return -9;
                [blit copyFromBuffer:old_buffer sourceOffset:0 toBuffer:buffer destinationOffset:0 size:old_capacity];
                [blit endEncoding];
                if (termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -10) != 0) return -10;
            }
            runtime->attention_span_encoded_key_buffers[slot] = buffer;
            runtime->attention_span_encoded_key_capacities[slot] = encoded_bytes;
        }
        if (v_bytes > runtime->attention_span_v_capacities[slot] ||
            runtime->attention_span_v_buffers[slot] == nil)
        {
            id<MTLBuffer> old_buffer = runtime->attention_span_v_buffers[slot];
            const size_t old_capacity = runtime->attention_span_v_capacities[slot];
            id<MTLBuffer> buffer = [runtime->device newBufferWithLength:v_bytes options:MTLResourceStorageModePrivate];
            if (buffer == nil) return -6;
            if (old_buffer != nil && old_capacity > 0) {
                bool frame_owned = false;
                id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
                if (command_buffer == nil) return -11;
                if (!frame_owned && termite_metal_decode_runtime_retain_frame_resource(runtime, old_buffer) != 0) return -12;
                id<MTLBlitCommandEncoder> blit = termite_metal_tracked_blit_command_encoder(command_buffer);
                if (blit == nil) return -13;
                [blit copyFromBuffer:old_buffer sourceOffset:0 toBuffer:buffer destinationOffset:0 size:old_capacity];
                [blit endEncoding];
                if (termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -14) != 0) return -14;
            }
            runtime->attention_span_v_buffers[slot] = buffer;
            runtime->attention_span_v_capacities[slot] = v_bytes;
        }
        if (slot == 0) {
            runtime->attention_span_encoded_key_buffer = runtime->attention_span_encoded_key_buffers[0];
            runtime->attention_span_v_buffer = runtime->attention_span_v_buffers[0];
            runtime->attention_span_encoded_key_capacity = runtime->attention_span_encoded_key_capacities[0];
            runtime->attention_span_v_capacity = runtime->attention_span_v_capacities[0];
        }
        return 0;
    }
}

int termite_metal_decode_runtime_reserve_greedy_tail_scratch(
    termite_metal_decode_runtime *runtime,
    size_t out_dim
) {
    if (runtime == NULL) return -1;
    if (out_dim == 0) return -2;
    const size_t logits_bytes = out_dim * sizeof(float);
    if (termite_metal_decode_runtime_ensure_graph_plan_slot(runtime, TERMITE_METAL_GRAPH_PLAN_SAMPLE_LOGITS_SLOT, logits_bytes) != 0) return -3;
    runtime->sample_logits_buffer = runtime->graph_plan_buffers[TERMITE_METAL_GRAPH_PLAN_SAMPLE_LOGITS_SLOT];
    runtime->sample_logits_capacity = runtime->graph_plan_capacities[TERMITE_METAL_GRAPH_PLAN_SAMPLE_LOGITS_SLOT];
    return 0;
}


int termite_metal_decode_runtime_apply_rms_norm(
    termite_metal_decode_runtime *runtime,
    size_t slot,
    const float *input,
    size_t hidden_size,
    float eps,
    float *output
) {
    if (runtime == NULL || input == NULL || output == NULL) return -1;
    if (runtime->rms_norm_pipeline == nil) return -2;
    if (slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY || hidden_size == 0) return -3;
    if (runtime->rms_norm_slot_prepared[slot] == 0) return -4;
    if (runtime->rms_norm_hidden_sizes[slot] != hidden_size) return -5;
    if (hidden_size > UINT32_MAX) return -6;
    @autoreleasepool {
        const size_t bytes = hidden_size * sizeof(float);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)input length:bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:input length:bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithBytesNoCopy:output length:bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) output_buffer = [runtime->device newBufferWithBytes:output length:bytes options:MTLResourceStorageModeShared];
        if (input_buffer == nil || output_buffer == nil) return -7;
        termite_metal_apply_layer_norm_params params = {
            .hidden_size = (uint32_t)hidden_size,
            .eps = eps,
        };
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -9;
        if (!frame_owned) {
            if (termite_metal_decode_runtime_retain_frame_resource(runtime, input_buffer) != 0) return -8;
            if (termite_metal_decode_runtime_retain_frame_resource(runtime, output_buffer) != 0) return -8;
        }
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_RMS_NORM);
        if (encoder == nil) return -10;
        [encoder setComputePipelineState:runtime->rms_norm_pipeline];
        [encoder setBuffer:input_buffer offset:0 atIndex:0];
        [encoder setBuffer:runtime->rms_norm_weight_buffers[slot] offset:0 atIndex:1];
        [encoder setBuffer:output_buffer offset:0 atIndex:2];
        [encoder setBytes:&params length:sizeof(params) atIndex:3];
        [encoder dispatchThreads:MTLSizeMake(1, 1, 1) threadsPerThreadgroup:MTLSizeMake(1, 1, 1)];
        [encoder endEncoding];
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -11);
    }
}

int termite_metal_decode_runtime_apply_rms_norm_device(
    termite_metal_decode_runtime *runtime,
    size_t slot,
    void *input_handle,
    size_t input_offset,
    size_t hidden_size,
    float eps,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || input_handle == NULL || output_handle == NULL) return -1;
    if (runtime->rms_norm_reduce_pipeline == nil) return -2;
    if (slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY || hidden_size == 0) return -3;
    if (runtime->rms_norm_slot_prepared[slot] == 0) return -4;
    if (runtime->rms_norm_hidden_sizes[slot] != hidden_size) return -5;
    if (hidden_size > UINT32_MAX) return -6;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t bytes = hidden_size * sizeof(float);
        if (input_offset + bytes > input_buffer.length) return -7;
        if (output_offset + bytes > output_buffer.length) return -8;
        const bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLCommandBuffer> command_buffer = frame_owned
            ? termite_metal_new_command_buffer(runtime->queue, __func__)
            : runtime->active_frame_cb;
        if (command_buffer == nil) return -10;
        if (termite_metal_encode_rms_norm_reduce(
                runtime,
                command_buffer,
                input_buffer,
                input_offset,
                slot,
                output_buffer,
                output_offset,
                hidden_size,
                eps,
                -11) != 0) return -11;
        if (!frame_owned) return 0;
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -12;
    }
}

int termite_metal_decode_runtime_apply_rms_norm_scratch_device(
    termite_metal_decode_runtime *runtime,
    size_t slot,
    void *input_handle,
    size_t input_offset,
    size_t hidden_size,
    float eps,
    size_t output_index,
    void **output_handle
) {
    if (runtime == NULL || input_handle == NULL || output_handle == NULL) return -1;
    *output_handle = NULL;
    if (runtime->rms_norm_reduce_pipeline == nil) return -2;
    if (slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY || hidden_size == 0) return -3;
    if (runtime->rms_norm_slot_prepared[slot] == 0) return -4;
    if (runtime->rms_norm_hidden_sizes[slot] != hidden_size) return -5;
    if (hidden_size > UINT32_MAX) return -6;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        const size_t bytes = hidden_size * sizeof(float);
        if (input_offset + bytes > input_buffer.length) return -7;
        if (termite_metal_decode_runtime_ensure_active_layer_projection_buffer(runtime, output_index, bytes) != 0) return -8;
        id<MTLBuffer> output_buffer = runtime->active_layer_projection_buffers[output_index];
        const bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLCommandBuffer> command_buffer = frame_owned
            ? termite_metal_new_command_buffer(runtime->queue, __func__)
            : runtime->active_frame_cb;
        if (command_buffer == nil) return -9;
        if (termite_metal_encode_rms_norm_reduce(
                runtime,
                command_buffer,
                input_buffer,
                input_offset,
                slot,
                output_buffer,
                0,
                hidden_size,
                eps,
                -10) != 0) return -10;
        if (frame_owned) {
            [command_buffer commit];
            [command_buffer waitUntilCompleted];
            if (command_buffer.status != MTLCommandBufferStatusCompleted) return -11;
        }
        *output_handle = (__bridge void *)output_buffer;
        return 0;
    }
}

int termite_metal_decode_runtime_apply_rms_norm_rows_device(
    termite_metal_decode_runtime *runtime,
    size_t slot,
    void *input_handle,
    size_t input_offset,
    size_t rows,
    size_t hidden_size,
    float eps,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || input_handle == NULL || output_handle == NULL) return -1;
    if (runtime->rms_norm_rows_pipeline == nil) return -2;
    if (slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY || rows == 0 || hidden_size == 0) return -3;
    if (runtime->rms_norm_slot_prepared[slot] == 0) return -4;
    if (runtime->rms_norm_hidden_sizes[slot] != hidden_size) return -5;
    if (rows > UINT32_MAX || hidden_size > UINT32_MAX) return -6;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t bytes = rows * hidden_size * sizeof(float);
        if (input_offset + bytes > input_buffer.length) return -7;
        if (output_offset + bytes > output_buffer.length) return -8;
        if (termite_metal_decode_runtime_prepare_planned_compute_norm_accesses(
                runtime,
                input_buffer,
                input_offset,
                bytes,
                runtime->rms_norm_weight_buffers[slot],
                output_buffer,
                output_offset,
                bytes,
                -10) != 0)
        {
            return -10;
        }
        termite_metal_apply_row_norm_params params = {
            .rows = (uint32_t)rows,
            .hidden_size = (uint32_t)hidden_size,
            .eps = eps,
        };
        const bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLCommandBuffer> command_buffer = frame_owned
            ? termite_metal_new_command_buffer(runtime->queue, __func__)
            : runtime->active_frame_cb;
        if (command_buffer == nil) return -9;
        id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
        const BOOL planned_encoder = (encoder != nil);
        if (!planned_encoder) {
            encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_RMS_NORM);
            if (encoder == nil) return -10;
        }
        [encoder setComputePipelineState:runtime->rms_norm_rows_pipeline];
        [encoder setBuffer:input_buffer offset:input_offset atIndex:0];
        [encoder setBuffer:runtime->rms_norm_weight_buffers[slot] offset:0 atIndex:1];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:2];
        [encoder setBytes:&params length:sizeof(params) atIndex:3];
        [encoder dispatchThreads:MTLSizeMake(rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->rms_norm_rows_pipeline, rows), 1, 1)];
        if (!planned_encoder) [encoder endEncoding];
        if (!frame_owned) return 0;
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -11;
    }
}

int termite_metal_decode_runtime_apply_rms_norm_weight_device(
    termite_metal_decode_runtime *runtime,
    void *input_handle,
    size_t input_offset,
    void *weight_handle,
    size_t weight_offset,
    size_t rows,
    size_t hidden_size,
    float eps,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || input_handle == NULL || weight_handle == NULL || output_handle == NULL) return -1;
    if (runtime->rms_norm_rows_pipeline == nil) return -2;
    if (rows == 0 || hidden_size == 0 || rows > UINT32_MAX || hidden_size > UINT32_MAX) return -3;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLBuffer> weight_buffer = (__bridge id<MTLBuffer>)weight_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t input_bytes = rows * hidden_size * sizeof(float);
        const size_t weight_bytes = hidden_size * sizeof(float);
        if (input_offset + input_bytes > input_buffer.length) return -4;
        if (weight_offset + weight_bytes > weight_buffer.length) return -5;
        if (output_offset + input_bytes > output_buffer.length) return -6;
        termite_metal_planned_encoder_range accesses[3];
        if (termite_metal_planned_range_make(input_buffer, input_offset, input_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[0], -8) != 0 ||
            termite_metal_planned_range_make(weight_buffer, weight_offset, weight_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[1], -8) != 0 ||
            termite_metal_planned_range_make(output_buffer, output_offset, input_bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &accesses[2], -8) != 0 ||
            termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, accesses, 3, -8) != 0)
        {
            return -8;
        }
        termite_metal_apply_row_norm_params params = {
            .rows = (uint32_t)rows,
            .hidden_size = (uint32_t)hidden_size,
            .eps = eps,
        };
        const bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLCommandBuffer> command_buffer = frame_owned
            ? termite_metal_new_command_buffer(runtime->queue, __func__)
            : runtime->active_frame_cb;
        if (command_buffer == nil) return -7;
        id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
        const BOOL planned_encoder = (encoder != nil);
        if (!planned_encoder) {
            encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_RMS_NORM);
            if (encoder == nil) return -8;
        }
        [encoder setComputePipelineState:runtime->rms_norm_rows_pipeline];
        [encoder setBuffer:input_buffer offset:input_offset atIndex:0];
        [encoder setBuffer:weight_buffer offset:weight_offset atIndex:1];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:2];
        [encoder setBytes:&params length:sizeof(params) atIndex:3];
        [encoder dispatchThreads:MTLSizeMake(rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->rms_norm_rows_pipeline, rows), 1, 1)];
        if (!planned_encoder) [encoder endEncoding];
        if (!frame_owned) return 0;
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -9;
    }
}

int termite_metal_decode_runtime_apply_rms_norm_weight_scratch_device(
    termite_metal_decode_runtime *runtime,
    void *input_handle,
    size_t input_offset,
    void *weight_handle,
    size_t weight_offset,
    size_t rows,
    size_t hidden_size,
    float eps,
    size_t output_index,
    void **output_handle
) {
    if (runtime == NULL || input_handle == NULL || weight_handle == NULL || output_handle == NULL) return -1;
    *output_handle = NULL;
    if (runtime->rms_norm_rows_pipeline == nil) return -2;
    if (rows == 0 || hidden_size == 0 || rows > UINT32_MAX || hidden_size > UINT32_MAX) return -3;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLBuffer> weight_buffer = (__bridge id<MTLBuffer>)weight_handle;
        const size_t input_bytes = rows * hidden_size * sizeof(float);
        const size_t weight_bytes = hidden_size * sizeof(float);
        if (input_offset + input_bytes > input_buffer.length) return -4;
        if (weight_offset + weight_bytes > weight_buffer.length) return -5;
        if (termite_metal_decode_runtime_ensure_active_layer_projection_buffer(runtime, output_index, input_bytes) != 0) return -6;
        id<MTLBuffer> output_buffer = runtime->active_layer_projection_buffers[output_index];
        termite_metal_planned_encoder_range accesses[3];
        if (termite_metal_planned_range_make(input_buffer, input_offset, input_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[0], -8) != 0 ||
            termite_metal_planned_range_make(weight_buffer, weight_offset, weight_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[1], -8) != 0 ||
            termite_metal_planned_range_make(output_buffer, 0, input_bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &accesses[2], -8) != 0 ||
            termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, accesses, 3, -8) != 0)
        {
            return -8;
        }
        termite_metal_apply_row_norm_params params = {
            .rows = (uint32_t)rows,
            .hidden_size = (uint32_t)hidden_size,
            .eps = eps,
        };
        const bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLCommandBuffer> command_buffer = frame_owned
            ? termite_metal_new_command_buffer(runtime->queue, __func__)
            : runtime->active_frame_cb;
        if (command_buffer == nil) return -7;
        id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
        const BOOL planned_encoder = (encoder != nil);
        if (!planned_encoder) {
            encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_RMS_NORM);
            if (encoder == nil) return -8;
        }
        [encoder setComputePipelineState:runtime->rms_norm_rows_pipeline];
        [encoder setBuffer:input_buffer offset:input_offset atIndex:0];
        [encoder setBuffer:weight_buffer offset:weight_offset atIndex:1];
        [encoder setBuffer:output_buffer offset:0 atIndex:2];
        [encoder setBytes:&params length:sizeof(params) atIndex:3];
        [encoder dispatchThreads:MTLSizeMake(rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->rms_norm_rows_pipeline, rows), 1, 1)];
        if (!planned_encoder) [encoder endEncoding];
        if (frame_owned) {
            [command_buffer commit];
            [command_buffer waitUntilCompleted];
            if (command_buffer.status != MTLCommandBufferStatusCompleted) return -9;
        }
        *output_handle = (__bridge void *)output_buffer;
        return 0;
    }
}

static int termite_metal_encode_head_rms_rope_on_encoder(
    termite_metal_decode_runtime *runtime,
    id<MTLComputeCommandEncoder> encoder,
    id<MTLBuffer> input_buffer,
    size_t input_offset,
    size_t norm_slot,
    size_t total_heads,
    size_t head_dim,
    size_t rope_dim,
    size_t position,
    float theta,
    float freq_scale,
    float eps,
    float value_scale,
    uint32_t consecutive_pairs,
    size_t heads_per_row,
    size_t position_period,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    int failure_code
) {
    if (runtime == NULL || encoder == nil || input_buffer == nil || output_buffer == nil) return failure_code;
    if (runtime->head_rms_rope_pipeline == nil) return failure_code;
    if (norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY || total_heads == 0 || head_dim == 0 || rope_dim == 0 || heads_per_row == 0) return failure_code;
    if (runtime->rms_norm_slot_prepared[norm_slot] == 0 || runtime->rms_norm_hidden_sizes[norm_slot] != head_dim) return failure_code;
    if (total_heads > UINT32_MAX || head_dim > UINT32_MAX || rope_dim > UINT32_MAX || position > UINT32_MAX || heads_per_row > UINT32_MAX || position_period > UINT32_MAX) return failure_code;
    size_t elems = 0;
    size_t bytes = 0;
    if (!termite_metal_size_mul(total_heads, head_dim, &elems) ||
        !termite_metal_size_mul(elems, sizeof(float), &bytes))
    {
        return failure_code;
    }
    if (termite_metal_decode_runtime_prepare_planned_compute_norm_accesses(
            runtime,
            input_buffer,
            input_offset,
            bytes,
            runtime->rms_norm_weight_buffers[norm_slot],
            output_buffer,
            output_offset,
            bytes,
            failure_code) != 0)
    {
        return failure_code;
    }
    termite_metal_head_rms_rope_params params = {
        .total_heads = (uint32_t)total_heads,
        .head_dim = (uint32_t)head_dim,
        .rope_dim = (uint32_t)rope_dim,
        .position = (uint32_t)position,
        .theta = theta,
        .freq_scale = freq_scale,
        .eps = eps,
        .value_scale = value_scale,
        .consecutive_pairs = consecutive_pairs,
        .heads_per_row = (uint32_t)heads_per_row,
        .position_period = (uint32_t)position_period,
    };
    [encoder setComputePipelineState:runtime->head_rms_rope_pipeline];
    [encoder setBuffer:input_buffer offset:input_offset atIndex:0];
    [encoder setBuffer:runtime->rms_norm_weight_buffers[norm_slot] offset:0 atIndex:1];
    [encoder setBuffer:output_buffer offset:output_offset atIndex:2];
    [encoder setBytes:&params length:sizeof(params) atIndex:3];
    [encoder setThreadgroupMemoryLength:256u * sizeof(float) atIndex:0];
    [encoder dispatchThreadgroups:MTLSizeMake(total_heads, 1, 1) threadsPerThreadgroup:MTLSizeMake(256, 1, 1)];
    return 0;
}

int termite_metal_decode_runtime_apply_layer_norm_device(
    termite_metal_decode_runtime *runtime,
    size_t slot,
    void *input_handle,
    size_t input_offset,
    size_t rows,
    size_t hidden_size,
    float eps,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || input_handle == NULL || output_handle == NULL) return -1;
    if (runtime->layer_norm_pipeline == nil) return -2;
    if (slot >= TERMITE_METAL_LAYER_NORM_SLOT_CAPACITY || rows == 0 || hidden_size == 0) return -3;
    if (runtime->layer_norm_slot_prepared[slot] == 0) return -4;
    if (runtime->layer_norm_hidden_sizes[slot] != hidden_size) return -5;
    if (rows > UINT32_MAX || hidden_size > UINT32_MAX) return -6;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t bytes = rows * hidden_size * sizeof(float);
        if (input_offset + bytes > input_buffer.length) return -7;
        if (output_offset + bytes > output_buffer.length) return -8;
        termite_metal_apply_layer_norm_params params = {
            .hidden_size = (uint32_t)hidden_size,
            .eps = eps,
        };
        const bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLCommandBuffer> command_buffer = frame_owned
            ? termite_metal_new_command_buffer(runtime->queue, __func__)
            : runtime->active_frame_cb;
        if (command_buffer == nil) return -10;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_RMS_NORM);
        if (encoder == nil) return -11;
        const BOOL use_rows_pipeline = rows > 1 && hidden_size >= 128 && runtime->layer_norm_rows_pipeline != nil;
        [encoder setComputePipelineState:use_rows_pipeline ? runtime->layer_norm_rows_pipeline : runtime->layer_norm_pipeline];
        [encoder setBuffer:input_buffer offset:input_offset atIndex:0];
        [encoder setBuffer:runtime->layer_norm_weight_buffers[slot] offset:0 atIndex:1];
        [encoder setBuffer:runtime->layer_norm_bias_buffers[slot] offset:0 atIndex:2];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:3];
        [encoder setBytes:&params length:sizeof(params) atIndex:4];
        if (use_rows_pipeline) {
            [encoder setThreadgroupMemoryLength:512u * sizeof(float) atIndex:0];
            [encoder dispatchThreadgroups:MTLSizeMake(rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(256u, 1, 1)];
        } else {
            [encoder dispatchThreads:MTLSizeMake(rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(1, 1, 1)];
        }
        [encoder endEncoding];
        if (!frame_owned) return 0;
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -12;
    }
}

int termite_metal_decode_runtime_apply_add_layer_norm_device(
    termite_metal_decode_runtime *runtime,
    size_t slot,
    void *a_handle,
    size_t a_offset,
    void *b_handle,
    size_t b_offset,
    size_t rows,
    size_t hidden_size,
    float eps,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || a_handle == NULL || b_handle == NULL || output_handle == NULL) return -1;
    if (runtime->layer_norm_add_pipeline == nil) return -2;
    if (slot >= TERMITE_METAL_LAYER_NORM_SLOT_CAPACITY || rows == 0 || hidden_size == 0) return -3;
    if (runtime->layer_norm_slot_prepared[slot] == 0) return -4;
    if (runtime->layer_norm_hidden_sizes[slot] != hidden_size) return -5;
    if (rows > UINT32_MAX || hidden_size > UINT32_MAX) return -6;
    @autoreleasepool {
        id<MTLBuffer> a_buffer = (__bridge id<MTLBuffer>)a_handle;
        id<MTLBuffer> b_buffer = (__bridge id<MTLBuffer>)b_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t bytes = rows * hidden_size * sizeof(float);
        if (a_offset + bytes > a_buffer.length) return -7;
        if (b_offset + bytes > b_buffer.length) return -8;
        if (output_offset + bytes > output_buffer.length) return -9;
        termite_metal_apply_layer_norm_params params = {
            .hidden_size = (uint32_t)hidden_size,
            .eps = eps,
        };
        const bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLCommandBuffer> command_buffer = frame_owned
            ? termite_metal_new_command_buffer(runtime->queue, __func__)
            : runtime->active_frame_cb;
        if (command_buffer == nil) return -10;
        if (!frame_owned) {
            if (termite_metal_decode_runtime_retain_frame_resource(runtime, a_buffer) != 0 ||
                termite_metal_decode_runtime_retain_frame_resource(runtime, b_buffer) != 0 ||
                termite_metal_decode_runtime_retain_frame_resource(runtime, output_buffer) != 0) {
                return -10;
            }
        }
        termite_metal_planned_encoder_range accesses[5];
        if (termite_metal_planned_range_make(a_buffer, a_offset, bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[0], -11) != 0 ||
            termite_metal_planned_range_make(b_buffer, b_offset, bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[1], -11) != 0 ||
            termite_metal_planned_range_make(runtime->layer_norm_weight_buffers[slot], 0, runtime->layer_norm_weight_buffers[slot].length, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[2], -11) != 0 ||
            termite_metal_planned_range_make(runtime->layer_norm_bias_buffers[slot], 0, runtime->layer_norm_bias_buffers[slot].length, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[3], -11) != 0 ||
            termite_metal_planned_range_make(output_buffer, output_offset, bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &accesses[4], -11) != 0 ||
            termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, accesses, 5, -11) != 0)
        {
            return -11;
        }
        BOOL encoder_owned = YES;
        id<MTLComputeCommandEncoder> encoder = termite_metal_scoped_compute_encoder_for(runtime, command_buffer, TERMITE_METAL_COMPUTE_SOURCE_RMS_NORM, &encoder_owned);
        if (encoder == nil) return -11;
        const BOOL use_rows_pipeline = rows > 1 && hidden_size >= 128 && runtime->layer_norm_add_rows_pipeline != nil;
        [encoder setComputePipelineState:use_rows_pipeline ? runtime->layer_norm_add_rows_pipeline : runtime->layer_norm_add_pipeline];
        [encoder setBuffer:a_buffer offset:a_offset atIndex:0];
        [encoder setBuffer:b_buffer offset:b_offset atIndex:1];
        [encoder setBuffer:runtime->layer_norm_weight_buffers[slot] offset:0 atIndex:2];
        [encoder setBuffer:runtime->layer_norm_bias_buffers[slot] offset:0 atIndex:3];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:4];
        [encoder setBytes:&params length:sizeof(params) atIndex:5];
        if (use_rows_pipeline) {
            [encoder setThreadgroupMemoryLength:512u * sizeof(float) atIndex:0];
            [encoder dispatchThreadgroups:MTLSizeMake(rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(256u, 1, 1)];
        } else {
            [encoder dispatchThreads:MTLSizeMake(rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(1, 1, 1)];
        }
        termite_metal_end_scoped_compute_encoder(encoder, encoder_owned);
        if (!frame_owned) return 0;
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -12;
    }
}

int termite_metal_decode_runtime_prepare_linear(
    termite_metal_decode_runtime *runtime,
    size_t slot,
    const float *weight,
    const float *bias,
    size_t in_dim,
    size_t out_dim
) {
    if (runtime == NULL || weight == NULL || bias == NULL) return -1;
    if (runtime->device == nil || runtime->queue == nil || runtime->library == nil) return -2;
    if (slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || in_dim == 0 || out_dim == 0) return -3;
    @autoreleasepool {
        const size_t weight_bytes = in_dim * out_dim * sizeof(float);
        const size_t bias_bytes = out_dim * sizeof(float);
        id<MTLBuffer> weight_buffer = termite_metal_make_private_buffer_with_bytes(runtime->device, runtime->queue, weight, weight_bytes);
        id<MTLBuffer> bias_buffer = termite_metal_make_private_buffer_with_bytes(runtime->device, runtime->queue, bias, bias_bytes);
        if (weight_buffer == nil || bias_buffer == nil) return -4;
        runtime->linear_weight_buffers[slot] = weight_buffer;
        runtime->linear_bias_buffers[slot] = bias_buffer;
        runtime->linear_in_dims[slot] = in_dim;
        runtime->linear_out_dims[slot] = out_dim;
        runtime->linear_slot_prepared[slot] = 1;
        runtime->linear_weight_dtypes[slot] = TERMITE_METAL_DENSE_LINEAR_DTYPE_F32;
        runtime->linear_mps_mm[slot] = nil;
        runtime->linear_mps_mm_rows[slot] = 0;
        termite_metal_decode_runtime_invalidate_dense_qkv_pack_slot(runtime, slot);
        termite_metal_decode_runtime_invalidate_q8_qkv_pack_slot(runtime, slot);
        termite_metal_decode_runtime_invalidate_dense_pair_pack_slot(runtime, slot);
        termite_metal_decode_runtime_clear_quant_linear_descriptor(runtime, slot);
        return 0;
    }
}

int termite_metal_decode_runtime_prepare_linear_bf16(
    termite_metal_decode_runtime *runtime,
    size_t slot,
    const uint8_t *weight,
    size_t weight_bytes,
    const float *bias,
    size_t in_dim,
    size_t out_dim
) {
    if (runtime == NULL || weight == NULL || bias == NULL) return -1;
    if (runtime->device == nil || runtime->queue == nil || runtime->library == nil) return -2;
    if (slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || in_dim == 0 || out_dim == 0) return -3;
    const size_t expected_weight_bytes = in_dim * out_dim * sizeof(uint16_t);
    if (weight_bytes < expected_weight_bytes) return -4;
    @autoreleasepool {
        const size_t bias_bytes = out_dim * sizeof(float);
        id<MTLBuffer> weight_buffer = termite_metal_make_private_buffer_with_bytes(runtime->device, runtime->queue, weight, expected_weight_bytes);
        id<MTLBuffer> bias_buffer = termite_metal_make_private_buffer_with_bytes(runtime->device, runtime->queue, bias, bias_bytes);
        if (weight_buffer == nil || bias_buffer == nil) return -5;
        runtime->linear_weight_buffers[slot] = weight_buffer;
        runtime->linear_bias_buffers[slot] = bias_buffer;
        runtime->linear_in_dims[slot] = in_dim;
        runtime->linear_out_dims[slot] = out_dim;
        runtime->linear_slot_prepared[slot] = 1;
        runtime->linear_weight_dtypes[slot] = TERMITE_METAL_DENSE_LINEAR_DTYPE_BF16;
        runtime->linear_mps_mm[slot] = nil;
        runtime->linear_mps_mm_rows[slot] = 0;
        termite_metal_decode_runtime_invalidate_dense_qkv_pack_slot(runtime, slot);
        termite_metal_decode_runtime_invalidate_q8_qkv_pack_slot(runtime, slot);
        termite_metal_decode_runtime_invalidate_dense_pair_pack_slot(runtime, slot);
        termite_metal_decode_runtime_clear_quant_linear_descriptor(runtime, slot);
        return 0;
    }
}

int termite_metal_decode_runtime_prepare_linear_bf16_no_copy(
    termite_metal_decode_runtime *runtime,
    size_t slot,
    const uint8_t *weight,
    size_t weight_bytes,
    const float *bias,
    size_t in_dim,
    size_t out_dim
) {
    if (runtime == NULL || weight == NULL || bias == NULL) return -1;
    if (runtime->device == nil || runtime->queue == nil || runtime->library == nil) return -2;
    if (slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || in_dim == 0 || out_dim == 0) return -3;
    const size_t expected_weight_bytes = in_dim * out_dim * sizeof(uint16_t);
    if (weight_bytes < expected_weight_bytes) return -4;
    @autoreleasepool {
        const size_t bias_bytes = out_dim * sizeof(float);
        id<MTLBuffer> weight_buffer = [runtime->device
            newBufferWithBytesNoCopy:(void *)weight
                              length:expected_weight_bytes
                             options:MTLResourceStorageModeShared
                         deallocator:nil];
        if (weight_buffer == nil) return -5;
        id<MTLBuffer> bias_buffer = termite_metal_make_private_buffer_with_bytes(runtime->device, runtime->queue, bias, bias_bytes);
        if (bias_buffer == nil) return -6;
        runtime->linear_weight_buffers[slot] = weight_buffer;
        runtime->linear_bias_buffers[slot] = bias_buffer;
        runtime->linear_in_dims[slot] = in_dim;
        runtime->linear_out_dims[slot] = out_dim;
        runtime->linear_slot_prepared[slot] = 1;
        runtime->linear_weight_dtypes[slot] = TERMITE_METAL_DENSE_LINEAR_DTYPE_BF16;
        runtime->linear_mps_mm[slot] = nil;
        runtime->linear_mps_mm_rows[slot] = 0;
        termite_metal_decode_runtime_invalidate_dense_qkv_pack_slot(runtime, slot);
        termite_metal_decode_runtime_invalidate_q8_qkv_pack_slot(runtime, slot);
        termite_metal_decode_runtime_invalidate_dense_pair_pack_slot(runtime, slot);
        termite_metal_decode_runtime_clear_quant_linear_descriptor(runtime, slot);
        return 0;
    }
}

int termite_metal_decode_runtime_prepare_linear_bias(
    termite_metal_decode_runtime *runtime,
    size_t slot,
    const float *bias,
    size_t out_dim
) {
    if (runtime == NULL || bias == NULL) return -1;
    if (runtime->device == nil || runtime->queue == nil || runtime->library == nil) return -2;
    if (slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || out_dim == 0) return -3;
    @autoreleasepool {
        const size_t bias_bytes = out_dim * sizeof(float);
        id<MTLBuffer> bias_buffer = termite_metal_make_private_buffer_with_bytes(runtime->device, runtime->queue, bias, bias_bytes);
        if (bias_buffer == nil) return -4;
        runtime->linear_bias_buffers[slot] = bias_buffer;
        return 0;
    }
}

static int termite_metal_decode_runtime_validate_quantized_linear_slot(
    termite_metal_decode_runtime *runtime,
    uint32_t format,
    size_t slot,
    const uint8_t *weight_raw,
    size_t weight_bytes,
    size_t in_dim,
    size_t out_dim,
    size_t *values_per_block,
    size_t *bytes_per_block
) {
    if (runtime == NULL || weight_raw == NULL) return -1;
    if (runtime->device == nil || runtime->queue == nil || runtime->library == nil) return -2;
    if (slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || in_dim == 0 || out_dim == 0) return -3;
    if (values_per_block == NULL || bytes_per_block == NULL) return -9;
    if (format == TERMITE_METAL_QUANT_FORMAT_TL1 || format == TERMITE_METAL_QUANT_FORMAT_TL2) return -10;
    if (!termite_metal_quant_format_block_layout(format, values_per_block, bytes_per_block)) return -10;
    if (*values_per_block == 0 || *bytes_per_block == 0) return -10;
    if (in_dim % *values_per_block != 0) return -4;
    const size_t row_blocks = in_dim / *values_per_block;
    if (row_blocks > SIZE_MAX / *bytes_per_block) return -5;
    const size_t row_bytes = row_blocks * *bytes_per_block;
    if (out_dim > SIZE_MAX / row_bytes) return -5;
    if (row_bytes * out_dim > weight_bytes) return -5;
    return 0;
}

static void termite_metal_decode_runtime_prepare_quantized_linear_slot_from_buffer(
    termite_metal_decode_runtime *runtime,
    uint32_t format,
    size_t slot,
    id<MTLBuffer> weight_buffer,
    size_t in_dim,
    size_t out_dim,
    size_t values_per_block,
    size_t bytes_per_block
) {
    runtime->quant_linear_weight_buffers[slot] = weight_buffer;
    runtime->quant_linear_in_dims[slot] = in_dim;
    runtime->quant_linear_out_dims[slot] = out_dim;
    runtime->quant_linear_slot_prepared[slot] = 1;
    termite_metal_decode_runtime_set_quant_linear_descriptor(runtime, slot, format, values_per_block, bytes_per_block, in_dim);
    termite_metal_decode_runtime_invalidate_q8_qkv_pack_slot(runtime, slot);
}

int termite_metal_decode_runtime_prepare_quantized_linear_slot(
    termite_metal_decode_runtime *runtime,
    uint32_t format,
    size_t slot,
    const uint8_t *weight_raw,
    size_t weight_bytes,
    size_t in_dim,
    size_t out_dim
) {
    size_t values_per_block = 0;
    size_t bytes_per_block = 0;
    const int validation_rc = termite_metal_decode_runtime_validate_quantized_linear_slot(
        runtime, format, slot, weight_raw, weight_bytes, in_dim, out_dim, &values_per_block, &bytes_per_block);
    if (validation_rc != 0) return validation_rc;
    @autoreleasepool {
        id<MTLBuffer> weight_buffer = termite_metal_make_private_buffer_with_bytes(runtime->device, runtime->queue, weight_raw, weight_bytes);
        if (weight_buffer == nil) return -6;
        termite_metal_decode_runtime_prepare_quantized_linear_slot_from_buffer(
            runtime, format, slot, weight_buffer, in_dim, out_dim, values_per_block, bytes_per_block);
        return 0;
    }
}

int termite_metal_decode_runtime_prepare_quantized_linear_slot_no_copy(
    termite_metal_decode_runtime *runtime,
    uint32_t format,
    size_t slot,
    const uint8_t *weight_raw,
    size_t weight_bytes,
    size_t in_dim,
    size_t out_dim
) {
    size_t values_per_block = 0;
    size_t bytes_per_block = 0;
    const int validation_rc = termite_metal_decode_runtime_validate_quantized_linear_slot(
        runtime, format, slot, weight_raw, weight_bytes, in_dim, out_dim, &values_per_block, &bytes_per_block);
    if (validation_rc != 0) return validation_rc;
    if (!termite_metal_can_make_no_copy_buffer(weight_raw, weight_bytes)) return -8;
    @autoreleasepool {
        id<MTLBuffer> weight_buffer = [runtime->device
            newBufferWithBytesNoCopy:(void *)weight_raw
                              length:weight_bytes
                             options:MTLResourceStorageModeShared
                         deallocator:nil];
        if (weight_buffer == nil) return -6;
        termite_metal_decode_runtime_prepare_quantized_linear_slot_from_buffer(
            runtime, format, slot, weight_buffer, in_dim, out_dim, values_per_block, bytes_per_block);
        return 0;
    }
}

int termite_metal_decode_runtime_prepare_bitnet_tl1_linear_slot(
    termite_metal_decode_runtime *runtime,
    size_t slot,
    const uint8_t *weight_raw,
    size_t weight_bytes,
    size_t in_dim,
    size_t out_dim,
    uint32_t packed_len,
    uint32_t bm,
    uint32_t cfg_by,
    uint32_t bmm
) {
    if (runtime == NULL || weight_raw == NULL) return -1;
    if (runtime->device == nil || runtime->queue == nil || runtime->library == nil) return -2;
    if (slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || in_dim == 0 || out_dim == 0) return -3;
    if (packed_len == 0 || bm == 0 || cfg_by == 0 || bmm == 0) return -4;
    if ((size_t)packed_len + sizeof(float) > weight_bytes) return -5;
    @autoreleasepool {
        id<MTLBuffer> weight_buffer = termite_metal_make_private_buffer_with_bytes(runtime->device, runtime->queue, weight_raw, weight_bytes);
        if (weight_buffer == nil) return -6;
        runtime->quant_linear_weight_buffers[slot] = weight_buffer;
        runtime->quant_linear_in_dims[slot] = in_dim;
        runtime->quant_linear_out_dims[slot] = out_dim;
        runtime->quant_linear_slot_prepared[slot] = 1;
        runtime->quant_linear_tl1_packed_len[slot] = packed_len;
        runtime->quant_linear_tl1_bm[slot] = bm;
        runtime->quant_linear_tl1_cfg_by[slot] = cfg_by;
        runtime->quant_linear_tl1_bmm[slot] = bmm;
        termite_metal_decode_runtime_set_quant_linear_descriptor(runtime, slot, TERMITE_METAL_QUANT_FORMAT_TL1, 1u, 1u, in_dim);
        termite_metal_decode_runtime_invalidate_q8_qkv_pack_slot(runtime, slot);
        return 0;
    }
}

int termite_metal_decode_runtime_prepare_bitnet_tl2_linear_slot(
    termite_metal_decode_runtime *runtime,
    size_t slot,
    const uint8_t *weight_raw,
    size_t weight_bytes,
    size_t in_dim,
    size_t out_dim,
    uint32_t scale_off,
    uint32_t three_value_len,
    uint32_t three_sign_len,
    uint32_t bm,
    uint32_t cfg_by,
    uint32_t bmm,
    uint32_t three_cols,
    uint32_t two_cols
) {
    if (runtime == NULL || weight_raw == NULL) return -1;
    if (runtime->device == nil || runtime->queue == nil || runtime->library == nil) return -2;
    if (slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || in_dim == 0 || out_dim == 0) return -3;
    if (scale_off == 0 || bm == 0 || cfg_by == 0 || bmm == 0) return -4;
    if ((size_t)scale_off + sizeof(float) > weight_bytes) return -5;
    if ((size_t)three_value_len + (size_t)three_sign_len > scale_off) return -6;
    @autoreleasepool {
        id<MTLBuffer> weight_buffer = termite_metal_make_private_buffer_with_bytes(runtime->device, runtime->queue, weight_raw, weight_bytes);
        if (weight_buffer == nil) return -7;
        runtime->quant_linear_weight_buffers[slot] = weight_buffer;
        runtime->quant_linear_in_dims[slot] = in_dim;
        runtime->quant_linear_out_dims[slot] = out_dim;
        runtime->quant_linear_slot_prepared[slot] = 1;
        runtime->quant_linear_tl2_scale_off[slot] = scale_off;
        runtime->quant_linear_tl2_three_value_len[slot] = three_value_len;
        runtime->quant_linear_tl2_three_sign_len[slot] = three_sign_len;
        runtime->quant_linear_tl2_bm[slot] = bm;
        runtime->quant_linear_tl2_cfg_by[slot] = cfg_by;
        runtime->quant_linear_tl2_bmm[slot] = bmm;
        runtime->quant_linear_tl2_three_cols[slot] = three_cols;
        runtime->quant_linear_tl2_two_cols[slot] = two_cols;
        termite_metal_decode_runtime_set_quant_linear_descriptor(runtime, slot, TERMITE_METAL_QUANT_FORMAT_TL2, 1u, 1u, in_dim);
        termite_metal_decode_runtime_invalidate_q8_qkv_pack_slot(runtime, slot);
        return 0;
    }
}

int termite_metal_decode_runtime_apply_linear(
    termite_metal_decode_runtime *runtime,
    size_t slot,
    const float *input,
    size_t in_dim,
    size_t out_dim,
    float *output
) {
    if (runtime == NULL || input == NULL || output == NULL) return -1;
    if (runtime->linear_pipeline == nil || runtime->linear_reduce_pipeline == nil || runtime->linear_bf16_pipeline == nil || runtime->linear_bf16_reduce_pipeline == nil) return -2;
    if (slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || in_dim == 0 || out_dim == 0) return -3;
    if (runtime->linear_slot_prepared[slot] == 0) return -4;
    if (runtime->linear_in_dims[slot] != in_dim || runtime->linear_out_dims[slot] != out_dim) return -5;
    if (in_dim > UINT32_MAX || out_dim > UINT32_MAX) return -6;
    @autoreleasepool {
        const size_t input_bytes = in_dim * sizeof(float);
        const size_t output_bytes = out_dim * sizeof(float);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)input length:input_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:input length:input_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithBytesNoCopy:output length:output_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) output_buffer = [runtime->device newBufferWithBytes:output length:output_bytes options:MTLResourceStorageModeShared];
        if (input_buffer == nil || output_buffer == nil) return -7;
        termite_metal_apply_linear_params params = {
            .in_dim = (uint32_t)in_dim,
            .out_dim = (uint32_t)out_dim,
        };
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -9;
        if (!frame_owned) {
            if (termite_metal_decode_runtime_retain_frame_resource(runtime, input_buffer) != 0) return -8;
            if (termite_metal_decode_runtime_retain_frame_resource(runtime, output_buffer) != 0) return -8;
        }
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_DENSE_LINEAR);
        if (encoder == nil) return -10;
        const BOOL use_reduce = out_dim >= 128;
        const BOOL use_bf16 = runtime->linear_weight_dtypes[slot] == TERMITE_METAL_DENSE_LINEAR_DTYPE_BF16;
        [encoder setComputePipelineState:(use_bf16 ? (use_reduce ? runtime->linear_bf16_reduce_pipeline : runtime->linear_bf16_pipeline) : (use_reduce ? runtime->linear_reduce_pipeline : runtime->linear_pipeline))];
        [encoder setBuffer:input_buffer offset:0 atIndex:0];
        [encoder setBuffer:runtime->linear_weight_buffers[slot] offset:0 atIndex:1];
        [encoder setBuffer:runtime->linear_bias_buffers[slot] offset:0 atIndex:2];
        [encoder setBuffer:output_buffer offset:0 atIndex:3];
        [encoder setBytes:&params length:sizeof(params) atIndex:4];
        if (use_reduce) {
            [encoder dispatchThreads:MTLSizeMake(out_dim * 32u, 1, 1) threadsPerThreadgroup:MTLSizeMake(32, 1, 1)];
        } else {
            id<MTLComputePipelineState> scalar_pipeline = use_bf16 ? runtime->linear_bf16_pipeline : runtime->linear_pipeline;
            NSUInteger thread_width = scalar_pipeline.maxTotalThreadsPerThreadgroup;
            if (thread_width == 0) thread_width = 64;
            if (thread_width > out_dim && out_dim > 0) thread_width = out_dim;
            [encoder dispatchThreads:MTLSizeMake(out_dim, 1, 1) threadsPerThreadgroup:MTLSizeMake(thread_width, 1, 1)];
        }
        [encoder endEncoding];
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -11);
    }
}

int termite_metal_decode_runtime_apply_linear_device(
    termite_metal_decode_runtime *runtime,
    size_t slot,
    void *input_handle,
    size_t input_offset,
    size_t in_dim,
    size_t out_dim,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || input_handle == NULL || output_handle == NULL) return -1;
    if (runtime->linear_pipeline == nil || runtime->linear_reduce_pipeline == nil || runtime->linear_bf16_pipeline == nil || runtime->linear_bf16_reduce_pipeline == nil) return -2;
    if (slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || in_dim == 0 || out_dim == 0) return -3;
    if (runtime->linear_slot_prepared[slot] == 0) return -4;
    if (runtime->linear_in_dims[slot] != in_dim || runtime->linear_out_dims[slot] != out_dim) return -5;
    if (in_dim > UINT32_MAX || out_dim > UINT32_MAX) return -6;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t input_bytes = in_dim * sizeof(float);
        const size_t output_bytes = out_dim * sizeof(float);
        if (input_offset + input_bytes > input_buffer.length) return -7;
        if (output_offset + output_bytes > output_buffer.length) return -8;
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -10;
        if (!frame_owned) {
            if (termite_metal_decode_runtime_retain_frame_resource(runtime, input_buffer) != 0) return -10;
            if (termite_metal_decode_runtime_retain_frame_resource(runtime, output_buffer) != 0) return -10;
        }
        id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
        const BOOL planned_encoder = (encoder != nil);
        if (!planned_encoder) {
            encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_DENSE_LINEAR);
        }
        if (encoder == nil) return -11;
        const int linear_rc = termite_metal_encode_dense_linear_on_encoder(runtime, encoder, slot, input_buffer, input_offset, in_dim, out_dim, output_buffer, output_offset, -11);
        if (!planned_encoder) [encoder endEncoding];
        if (linear_rc != 0) return linear_rc;
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -12);
    }
}

int termite_metal_decode_runtime_apply_linear_multi_row(
    termite_metal_decode_runtime *runtime,
    size_t slot,
    const float *input,
    size_t rows,
    size_t in_dim,
    size_t out_dim,
    float *output
) {
    if (runtime == NULL || input == NULL || output == NULL) return -1;
    if (runtime->linear_multi_row_pipeline == nil || runtime->linear_bf16_multi_row_pipeline == nil ||
        runtime->linear_multi_row_reduce_pipeline == nil || runtime->linear_bf16_multi_row_reduce_pipeline == nil) return -2;
    if (slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || rows == 0 || in_dim == 0 || out_dim == 0) return -3;
    if (runtime->linear_slot_prepared[slot] == 0) return -4;
    if (runtime->linear_in_dims[slot] != in_dim || runtime->linear_out_dims[slot] != out_dim) return -5;
    if (rows > UINT32_MAX || in_dim > UINT32_MAX || out_dim > UINT32_MAX) return -6;
    if (rows == 1) return termite_metal_decode_runtime_apply_linear(runtime, slot, input, in_dim, out_dim, output);
    @autoreleasepool {
        const size_t input_bytes = rows * in_dim * sizeof(float);
        const size_t output_bytes = rows * out_dim * sizeof(float);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)input length:input_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:input length:input_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithBytesNoCopy:output length:output_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) output_buffer = [runtime->device newBufferWithBytes:output length:output_bytes options:MTLResourceStorageModeShared];
        if (input_buffer == nil || output_buffer == nil) return -7;
        termite_metal_linear_params params = {
            .rows = (uint32_t)rows,
            .in_dim = (uint32_t)in_dim,
            .out_dim = (uint32_t)out_dim,
            .row_blocks = 0u,
        };
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -9;
        if (!frame_owned) {
            if (termite_metal_decode_runtime_retain_frame_resource(runtime, input_buffer) != 0) return -8;
            if (termite_metal_decode_runtime_retain_frame_resource(runtime, output_buffer) != 0) return -8;
        }
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_DENSE_LINEAR);
        if (encoder == nil) return -10;
        const BOOL use_bf16 = runtime->linear_weight_dtypes[slot] == TERMITE_METAL_DENSE_LINEAR_DTYPE_BF16;
        const BOOL use_reduce = out_dim >= 128u && in_dim >= 128u;
        const BOOL use_shared_row_reduce = use_bf16 && use_reduce && rows > 1u && rows <= 64u && runtime->linear_bf16_multi_row_shared_reduce_pipeline != nil;
        const BOOL use_tiled32_m16 = !use_bf16 && use_reduce && rows >= 128u && runtime->linear_multi_row_tiled32_m16_f32_pipeline != nil;
        const BOOL use_tiled32 = !use_bf16 && use_reduce && rows >= 128u && runtime->linear_multi_row_tiled32_f32_pipeline != nil;
        const BOOL use_tiled = !use_bf16 && use_reduce && rows >= 128u && runtime->linear_multi_row_tiled_f32_pipeline != nil;
	        id<MTLComputePipelineState> pipeline = use_bf16
	            ? (use_reduce ? (use_shared_row_reduce ? runtime->linear_bf16_multi_row_shared_reduce_pipeline : runtime->linear_bf16_multi_row_reduce_pipeline) : runtime->linear_bf16_multi_row_pipeline)
	            : (use_tiled32_m16 ? runtime->linear_multi_row_tiled32_m16_f32_pipeline : (use_tiled32 ? runtime->linear_multi_row_tiled32_f32_pipeline : (use_tiled ? runtime->linear_multi_row_tiled_f32_pipeline : (use_reduce ? runtime->linear_multi_row_reduce_pipeline : runtime->linear_multi_row_pipeline))));
	        termite_metal_maybe_trace_dense_linear_dispatch(
	            "linear_multi_row",
	            slot,
	            rows,
	            in_dim,
	            out_dim,
	            termite_metal_dense_linear_variant(use_bf16, use_shared_row_reduce, use_tiled32_m16, use_tiled32, use_tiled, use_reduce, false),
	            !frame_owned,
	            false,
	            false,
	            false,
	            false);
	        [encoder setComputePipelineState:pipeline];
        [encoder setBuffer:input_buffer offset:0 atIndex:0];
        [encoder setBuffer:runtime->linear_weight_buffers[slot] offset:0 atIndex:1];
        [encoder setBuffer:runtime->linear_bias_buffers[slot] offset:0 atIndex:2];
        [encoder setBuffer:output_buffer offset:0 atIndex:3];
        [encoder setBytes:&params length:sizeof(params) atIndex:4];
        if (use_tiled32_m16) {
            [encoder dispatchThreadgroups:MTLSizeMake((out_dim + 31u) / 32u, (rows + 15u) / 16u, 1) threadsPerThreadgroup:MTLSizeMake(8u, 16u, 1)];
        } else if (use_tiled32) {
            [encoder dispatchThreadgroups:MTLSizeMake((out_dim + 31u) / 32u, (rows + 7u) / 8u, 1) threadsPerThreadgroup:MTLSizeMake(8u, 8u, 1)];
        } else if (use_tiled) {
            [encoder dispatchThreadgroups:MTLSizeMake((out_dim + 15u) / 16u, (rows + 7u) / 8u, 1) threadsPerThreadgroup:MTLSizeMake(8u, 8u, 1)];
        } else if (use_shared_row_reduce) {
            [encoder setThreadgroupMemoryLength:rows * 32u * sizeof(float) atIndex:0];
            [encoder dispatchThreadgroups:MTLSizeMake(out_dim, 1, 1) threadsPerThreadgroup:MTLSizeMake(32u, 1, 1)];
        } else if (use_reduce) {
            [encoder setThreadgroupMemoryLength:32u * sizeof(float) atIndex:0];
            [encoder dispatchThreadgroups:MTLSizeMake(out_dim, rows, 1) threadsPerThreadgroup:MTLSizeMake(32u, 1, 1)];
        } else {
            const size_t total = rows * out_dim;
            NSUInteger thread_width = pipeline.maxTotalThreadsPerThreadgroup;
            if (thread_width == 0) thread_width = 64;
            if (thread_width > total && total > 0) thread_width = total;
            [encoder dispatchThreads:MTLSizeMake(total, 1, 1) threadsPerThreadgroup:MTLSizeMake(thread_width, 1, 1)];
        }
        [encoder endEncoding];
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -11);
    }
}

int termite_metal_decode_runtime_apply_linear_multi_row_device(
    termite_metal_decode_runtime *runtime,
    size_t slot,
    void *input_handle,
    size_t input_offset,
    size_t rows,
    size_t in_dim,
    size_t out_dim,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || input_handle == NULL || output_handle == NULL) return -1;
    if (runtime->linear_multi_row_pipeline == nil || runtime->linear_bf16_multi_row_pipeline == nil ||
        runtime->linear_multi_row_reduce_pipeline == nil || runtime->linear_bf16_multi_row_reduce_pipeline == nil) return -2;
    if (slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || rows == 0 || in_dim == 0 || out_dim == 0) return -3;
    if (runtime->linear_slot_prepared[slot] == 0) return -4;
    if (runtime->linear_in_dims[slot] != in_dim || runtime->linear_out_dims[slot] != out_dim) return -5;
    if (rows > UINT32_MAX || in_dim > UINT32_MAX || out_dim > UINT32_MAX) return -6;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t input_bytes = rows * in_dim * sizeof(float);
        const size_t output_bytes = rows * out_dim * sizeof(float);
        if (input_offset + input_bytes > input_buffer.length) return -7;
        if (output_offset + output_bytes > output_buffer.length) return -8;
        termite_metal_linear_params params = {
            .rows = (uint32_t)rows,
            .in_dim = (uint32_t)in_dim,
            .out_dim = (uint32_t)out_dim,
            .row_blocks = 0u,
        };
        if (termite_metal_dense_mps_linear_enabled() &&
            runtime->linear_weight_dtypes[slot] == TERMITE_METAL_DENSE_LINEAR_DTYPE_F32 &&
            runtime->linear_weight_buffers[slot] != nil &&
            runtime->linear_bias_buffers[slot] != nil &&
            rows >= 128 &&
            in_dim >= 64 &&
            out_dim >= 64)
        {
            const size_t weight_bytes = out_dim * in_dim * sizeof(float);
            const size_t bias_bytes = out_dim * sizeof(float);
            if (weight_bytes <= runtime->linear_weight_buffers[slot].length &&
                bias_bytes <= runtime->linear_bias_buffers[slot].length)
            {
                const bool frame_owned = (runtime->active_frame_cb == nil);
                id<MTLCommandBuffer> command_buffer = frame_owned
                    ? termite_metal_new_command_buffer(runtime->queue, __func__)
                    : runtime->active_frame_cb;
                if (command_buffer == nil) return -10;
                MPSMatrixDescriptor *left_desc = [MPSMatrixDescriptor matrixDescriptorWithRows:rows
                                                                                       columns:in_dim
                                                                                      rowBytes:in_dim * sizeof(float)
                                                                                      dataType:MPSDataTypeFloat32];
                MPSMatrixDescriptor *right_desc = [MPSMatrixDescriptor matrixDescriptorWithRows:out_dim
                                                                                        columns:in_dim
                                                                                       rowBytes:in_dim * sizeof(float)
                                                                                       dataType:MPSDataTypeFloat32];
                MPSMatrixDescriptor *result_desc = [MPSMatrixDescriptor matrixDescriptorWithRows:rows
                                                                                         columns:out_dim
                                                                                        rowBytes:out_dim * sizeof(float)
                                                                                        dataType:MPSDataTypeFloat32];
                MPSMatrix *left = [[MPSMatrix alloc] initWithBuffer:input_buffer offset:input_offset descriptor:left_desc];
                MPSMatrix *right = [[MPSMatrix alloc] initWithBuffer:runtime->linear_weight_buffers[slot] offset:0 descriptor:right_desc];
                MPSMatrix *result = [[MPSMatrix alloc] initWithBuffer:output_buffer offset:output_offset descriptor:result_desc];
                MPSMatrixMultiplication *mm = termite_metal_decode_runtime_cached_mps_mm(runtime, slot, rows, in_dim, out_dim);
                if (left == nil || right == nil || result == nil || mm == nil) return -10;
	                [mm encodeToCommandBuffer:command_buffer leftMatrix:left rightMatrix:right resultMatrix:result];
	                termite_metal_maybe_trace_dense_linear_dispatch(
	                    "linear_multi_row_device",
	                    slot,
	                    rows,
	                    in_dim,
	                    out_dim,
	                    "mps_matrix",
	                    !frame_owned,
	                    false,
	                    false,
	                    false,
	                    true);

                id<MTLComputeCommandEncoder> bias_encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_DENSE_LINEAR);
                if (bias_encoder == nil) return -11;
                [bias_encoder setComputePipelineState:runtime->linear_bias_pipeline];
                [bias_encoder setBuffer:output_buffer offset:output_offset atIndex:0];
                [bias_encoder setBuffer:runtime->linear_bias_buffers[slot] offset:0 atIndex:1];
                [bias_encoder setBytes:&params length:sizeof(params) atIndex:2];
                const size_t total = rows * out_dim;
                [bias_encoder dispatchThreads:MTLSizeMake(total, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->linear_bias_pipeline, total), 1, 1)];
                [bias_encoder endEncoding];

                if (!frame_owned) {
                    runtime->mps_dense_linear_active_frame_calls += 1;
                    runtime->active_frame_mps_dense_linear_count += 1;
                    return 0;
                }
                runtime->mps_dense_linear_standalone_calls += 1;
                const uint64_t wait_start = termite_metal_clock_monotonic_nanos();
                [command_buffer commit];
                [command_buffer waitUntilCompleted];
                const uint64_t wait_end = termite_metal_clock_monotonic_nanos();
                if (wait_end > wait_start) runtime->mps_dense_linear_standalone_wait_nanos += wait_end - wait_start;
                if (command_buffer.status == MTLCommandBufferStatusCompleted) {
                    runtime->mps_dense_linear_standalone_gpu_nanos += termite_metal_command_buffer_gpu_elapsed_nanos(command_buffer);
                    return 0;
                }
                return -12;
            }
        }
        const bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLCommandBuffer> command_buffer = frame_owned
            ? termite_metal_new_command_buffer(runtime->queue, __func__)
            : runtime->active_frame_cb;
        if (command_buffer == nil) return -10;
        if (!frame_owned) {
            if (termite_metal_decode_runtime_retain_frame_resource(runtime, input_buffer) != 0) return -10;
            if (termite_metal_decode_runtime_retain_frame_resource(runtime, output_buffer) != 0) return -10;
        }
        id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
        const BOOL planned_encoder = (encoder != nil);
        if (!planned_encoder) {
            encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_DENSE_LINEAR);
        }
        if (encoder == nil) return -11;
        if (termite_metal_decode_runtime_prepare_planned_dense_linear_accesses(
                runtime,
                input_buffer,
                input_offset,
                input_bytes,
                runtime->linear_weight_buffers[slot],
                runtime->linear_bias_buffers[slot],
                output_buffer,
                output_offset,
                output_bytes,
                -11) != 0) return -11;
        const BOOL use_bf16 = runtime->linear_weight_dtypes[slot] == TERMITE_METAL_DENSE_LINEAR_DTYPE_BF16;
        const BOOL use_reduce = out_dim >= 128u && in_dim >= 128u;
        const BOOL use_shared_row_reduce = use_bf16 && use_reduce && rows > 1u && rows <= 64u && runtime->linear_bf16_multi_row_shared_reduce_pipeline != nil;
        const BOOL use_tiled32_m16 = !use_bf16 && use_reduce && rows >= 128u && runtime->linear_multi_row_tiled32_m16_f32_pipeline != nil;
        const BOOL use_tiled32 = !use_bf16 && use_reduce && rows >= 128u && runtime->linear_multi_row_tiled32_f32_pipeline != nil;
        const BOOL use_tiled = !use_bf16 && use_reduce && rows >= 128u && runtime->linear_multi_row_tiled_f32_pipeline != nil;
	        id<MTLComputePipelineState> pipeline = use_bf16
	            ? (use_reduce ? (use_shared_row_reduce ? runtime->linear_bf16_multi_row_shared_reduce_pipeline : runtime->linear_bf16_multi_row_reduce_pipeline) : runtime->linear_bf16_multi_row_pipeline)
	            : (use_tiled32_m16 ? runtime->linear_multi_row_tiled32_m16_f32_pipeline : (use_tiled32 ? runtime->linear_multi_row_tiled32_f32_pipeline : (use_tiled ? runtime->linear_multi_row_tiled_f32_pipeline : (use_reduce ? runtime->linear_multi_row_reduce_pipeline : runtime->linear_multi_row_pipeline))));
	        termite_metal_maybe_trace_dense_linear_dispatch(
	            "linear_multi_row_device",
	            slot,
	            rows,
	            in_dim,
	            out_dim,
	            termite_metal_dense_linear_variant(use_bf16, use_shared_row_reduce, use_tiled32_m16, use_tiled32, use_tiled, use_reduce, false),
	            !frame_owned,
	            planned_encoder,
	            false,
	            false,
	            false);
	        [encoder setComputePipelineState:pipeline];
        [encoder setBuffer:input_buffer offset:input_offset atIndex:0];
        [encoder setBuffer:runtime->linear_weight_buffers[slot] offset:0 atIndex:1];
        [encoder setBuffer:runtime->linear_bias_buffers[slot] offset:0 atIndex:2];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:3];
        [encoder setBytes:&params length:sizeof(params) atIndex:4];
        if (use_tiled32_m16) {
            [encoder dispatchThreadgroups:MTLSizeMake((out_dim + 31u) / 32u, (rows + 15u) / 16u, 1) threadsPerThreadgroup:MTLSizeMake(8u, 16u, 1)];
        } else if (use_tiled32) {
            [encoder dispatchThreadgroups:MTLSizeMake((out_dim + 31u) / 32u, (rows + 7u) / 8u, 1) threadsPerThreadgroup:MTLSizeMake(8u, 8u, 1)];
        } else if (use_tiled) {
            [encoder dispatchThreadgroups:MTLSizeMake((out_dim + 15u) / 16u, (rows + 7u) / 8u, 1) threadsPerThreadgroup:MTLSizeMake(8u, 8u, 1)];
        } else if (use_shared_row_reduce) {
            [encoder setThreadgroupMemoryLength:rows * 32u * sizeof(float) atIndex:0];
            [encoder dispatchThreadgroups:MTLSizeMake(out_dim, 1, 1) threadsPerThreadgroup:MTLSizeMake(32u, 1, 1)];
        } else if (use_reduce) {
            [encoder setThreadgroupMemoryLength:32u * sizeof(float) atIndex:0];
            [encoder dispatchThreadgroups:MTLSizeMake(out_dim, rows, 1) threadsPerThreadgroup:MTLSizeMake(32u, 1, 1)];
        } else {
            const size_t total = rows * out_dim;
            NSUInteger thread_width = pipeline.maxTotalThreadsPerThreadgroup;
            if (thread_width == 0) thread_width = 64;
            if (thread_width > total && total > 0) thread_width = total;
            [encoder dispatchThreads:MTLSizeMake(total, 1, 1) threadsPerThreadgroup:MTLSizeMake(thread_width, 1, 1)];
        }
        if (!planned_encoder) [encoder endEncoding];
        if (!frame_owned) return 0;
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -12;
    }
}

int termite_metal_decode_runtime_apply_dense_mlp2_device(
    termite_metal_decode_runtime *runtime,
    size_t first_linear_slot,
    size_t second_linear_slot,
    void *input_handle,
    size_t input_offset,
    size_t rows,
    size_t in_dim,
    size_t hidden_dim,
    size_t out_dim,
    uint32_t activation_kind,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || input_handle == NULL || output_handle == NULL) return -1;
    if (runtime->linear_multi_row_pipeline == nil || runtime->linear_multi_row_reduce_pipeline == nil ||
        runtime->linear_bf16_multi_row_pipeline == nil || runtime->linear_bf16_multi_row_reduce_pipeline == nil ||
        runtime->linear_bf16_multi_row_shared_reduce_pipeline == nil || runtime->activation_pipeline == nil) return -2;
    if (first_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || second_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -3;
    if (rows == 0 || in_dim == 0 || hidden_dim == 0 || out_dim == 0) return -4;
    if (rows > UINT32_MAX || in_dim > UINT32_MAX || hidden_dim > UINT32_MAX || out_dim > UINT32_MAX) return -5;
    if (runtime->linear_slot_prepared[first_linear_slot] == 0 || runtime->linear_slot_prepared[second_linear_slot] == 0) return -6;
    if (runtime->linear_in_dims[first_linear_slot] != in_dim || runtime->linear_out_dims[first_linear_slot] != hidden_dim) return -7;
    if (runtime->linear_in_dims[second_linear_slot] != hidden_dim || runtime->linear_out_dims[second_linear_slot] != out_dim) return -8;
    if (rows > SIZE_MAX / in_dim || rows > SIZE_MAX / hidden_dim || rows > SIZE_MAX / out_dim) return -9;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t input_bytes = rows * in_dim * sizeof(float);
        const size_t hidden_bytes = rows * hidden_dim * sizeof(float);
        const size_t output_bytes = rows * out_dim * sizeof(float);
        if (input_offset + input_bytes > input_buffer.length) return -10;
        if (output_offset + output_bytes > output_buffer.length) return -11;
        id<MTLBuffer> first_output_buffer = nil;
        id<MTLBuffer> activated_buffer = nil;
        if (termite_metal_decode_runtime_ensure_hot_buffers(runtime, output_bytes, hidden_bytes) == 0) {
            first_output_buffer = runtime->hot_intermediate_buffers[0];
            activated_buffer = runtime->hot_intermediate_buffers[1];
        } else {
            first_output_buffer = [runtime->device newBufferWithLength:hidden_bytes options:MTLResourceStorageModePrivate];
            activated_buffer = [runtime->device newBufferWithLength:hidden_bytes options:MTLResourceStorageModePrivate];
        }
        if (first_output_buffer == nil || activated_buffer == nil) return -12;

        termite_metal_linear_params first_params = {
            .rows = (uint32_t)rows,
            .in_dim = (uint32_t)in_dim,
            .out_dim = (uint32_t)hidden_dim,
            .row_blocks = 0u,
        };
        termite_metal_apply_activation_params activation_params = {
            .activation_kind = activation_kind,
            .rows = (uint32_t)rows,
            .dim = (uint32_t)hidden_dim,
        };
        termite_metal_linear_params second_params = {
            .rows = (uint32_t)rows,
            .in_dim = (uint32_t)hidden_dim,
            .out_dim = (uint32_t)out_dim,
            .row_blocks = 0u,
        };

        const bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLCommandBuffer> command_buffer = frame_owned
            ? termite_metal_new_command_buffer(runtime->queue, __func__)
            : runtime->active_frame_cb;
        if (command_buffer == nil) return -13;
        if (!frame_owned) {
            if (termite_metal_decode_runtime_retain_frame_resource(runtime, input_buffer) != 0 ||
                termite_metal_decode_runtime_retain_frame_resource(runtime, output_buffer) != 0 ||
                termite_metal_decode_runtime_retain_frame_resource(runtime, first_output_buffer) != 0 ||
                termite_metal_decode_runtime_retain_frame_resource(runtime, activated_buffer) != 0) return -13;
        }

        BOOL first_encoder_owned = YES;
        id<MTLComputeCommandEncoder> first_encoder = termite_metal_scoped_compute_encoder_for(runtime, command_buffer, TERMITE_METAL_COMPUTE_SOURCE_DENSE_LINEAR, &first_encoder_owned);
        if (first_encoder == nil) return -14;
        const BOOL first_bf16 = runtime->linear_weight_dtypes[first_linear_slot] == TERMITE_METAL_DENSE_LINEAR_DTYPE_BF16;
        const BOOL first_reduce = hidden_dim >= 128u && in_dim >= 128u;
        const BOOL first_shared = first_bf16 && first_reduce && rows > 1u && rows <= 64u && runtime->linear_bf16_multi_row_shared_reduce_pipeline != nil;
        const BOOL first_tiled32_m16 = !first_bf16 && first_reduce && rows >= 128u && runtime->linear_multi_row_tiled32_m16_f32_pipeline != nil;
        const BOOL first_tiled32 = !first_bf16 && first_reduce && rows >= 128u && runtime->linear_multi_row_tiled32_f32_pipeline != nil;
        const BOOL first_tiled = !first_bf16 && first_reduce && rows >= 128u && runtime->linear_multi_row_tiled_f32_pipeline != nil;
	        id<MTLComputePipelineState> first_pipeline = first_bf16
	            ? (first_reduce ? (first_shared ? runtime->linear_bf16_multi_row_shared_reduce_pipeline : runtime->linear_bf16_multi_row_reduce_pipeline) : runtime->linear_bf16_multi_row_pipeline)
	            : (first_tiled32_m16 ? runtime->linear_multi_row_tiled32_m16_f32_pipeline : (first_tiled32 ? runtime->linear_multi_row_tiled32_f32_pipeline : (first_tiled ? runtime->linear_multi_row_tiled_f32_pipeline : (first_reduce ? runtime->linear_multi_row_reduce_pipeline : runtime->linear_multi_row_pipeline))));
	        termite_metal_maybe_trace_dense_linear_dispatch(
	            "dense_mlp2.first",
	            first_linear_slot,
	            rows,
	            in_dim,
	            hidden_dim,
	            termite_metal_dense_linear_variant(first_bf16, first_shared, first_tiled32_m16, first_tiled32, first_tiled, first_reduce, false),
	            !frame_owned,
	            !first_encoder_owned,
	            false,
	            false,
	            false);
	        [first_encoder setComputePipelineState:first_pipeline];
        [first_encoder setBuffer:input_buffer offset:input_offset atIndex:0];
        [first_encoder setBuffer:runtime->linear_weight_buffers[first_linear_slot] offset:0 atIndex:1];
        [first_encoder setBuffer:runtime->linear_bias_buffers[first_linear_slot] offset:0 atIndex:2];
        [first_encoder setBuffer:first_output_buffer offset:0 atIndex:3];
        [first_encoder setBytes:&first_params length:sizeof(first_params) atIndex:4];
        if (first_tiled32_m16) {
            [first_encoder dispatchThreadgroups:MTLSizeMake((hidden_dim + 31u) / 32u, (rows + 15u) / 16u, 1) threadsPerThreadgroup:MTLSizeMake(8u, 16u, 1)];
        } else if (first_tiled32) {
            [first_encoder dispatchThreadgroups:MTLSizeMake((hidden_dim + 31u) / 32u, (rows + 7u) / 8u, 1) threadsPerThreadgroup:MTLSizeMake(8u, 8u, 1)];
        } else if (first_tiled) {
            [first_encoder dispatchThreadgroups:MTLSizeMake((hidden_dim + 15u) / 16u, (rows + 7u) / 8u, 1) threadsPerThreadgroup:MTLSizeMake(8u, 8u, 1)];
        } else if (first_shared) {
            [first_encoder setThreadgroupMemoryLength:rows * 32u * sizeof(float) atIndex:0];
            [first_encoder dispatchThreadgroups:MTLSizeMake(hidden_dim, 1, 1) threadsPerThreadgroup:MTLSizeMake(32u, 1, 1)];
        } else if (first_reduce) {
            [first_encoder setThreadgroupMemoryLength:32u * sizeof(float) atIndex:0];
            [first_encoder dispatchThreadgroups:MTLSizeMake(hidden_dim, rows, 1) threadsPerThreadgroup:MTLSizeMake(32u, 1, 1)];
        } else {
            const size_t first_total = rows * hidden_dim;
            [first_encoder dispatchThreads:MTLSizeMake(first_total, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(first_pipeline, first_total), 1, 1)];
        }
        if (!first_encoder_owned) {
            [first_encoder memoryBarrierWithScope:MTLBarrierScopeBuffers];
        }
        termite_metal_end_scoped_compute_encoder(first_encoder, first_encoder_owned);

        BOOL activation_encoder_owned = YES;
        id<MTLComputeCommandEncoder> activation_encoder = termite_metal_scoped_compute_encoder_for(runtime, command_buffer, TERMITE_METAL_COMPUTE_SOURCE_TAIL, &activation_encoder_owned);
        if (activation_encoder == nil) return -15;
        [activation_encoder setComputePipelineState:runtime->activation_pipeline];
        [activation_encoder setBuffer:first_output_buffer offset:0 atIndex:0];
        [activation_encoder setBuffer:activated_buffer offset:0 atIndex:1];
        [activation_encoder setBytes:&activation_params length:sizeof(activation_params) atIndex:2];
        const size_t activation_total = rows * hidden_dim;
        [activation_encoder dispatchThreads:MTLSizeMake(activation_total, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->activation_pipeline, activation_total), 1, 1)];
        if (!activation_encoder_owned) {
            [activation_encoder memoryBarrierWithScope:MTLBarrierScopeBuffers];
        }
        termite_metal_end_scoped_compute_encoder(activation_encoder, activation_encoder_owned);

        BOOL second_encoder_owned = YES;
        id<MTLComputeCommandEncoder> second_encoder = termite_metal_scoped_compute_encoder_for(runtime, command_buffer, TERMITE_METAL_COMPUTE_SOURCE_DENSE_LINEAR, &second_encoder_owned);
        if (second_encoder == nil) return -16;
        const BOOL second_bf16 = runtime->linear_weight_dtypes[second_linear_slot] == TERMITE_METAL_DENSE_LINEAR_DTYPE_BF16;
        const BOOL second_reduce = out_dim >= 128u && hidden_dim >= 128u;
        const BOOL second_shared = second_bf16 && second_reduce && rows > 1u && rows <= 64u && runtime->linear_bf16_multi_row_shared_reduce_pipeline != nil;
        const BOOL second_tiled32_m16 = !second_bf16 && second_reduce && rows >= 128u && runtime->linear_multi_row_tiled32_m16_f32_pipeline != nil;
        const BOOL second_tiled32 = !second_bf16 && second_reduce && rows >= 128u && runtime->linear_multi_row_tiled32_f32_pipeline != nil;
        const BOOL second_tiled = !second_bf16 && second_reduce && rows >= 128u && runtime->linear_multi_row_tiled_f32_pipeline != nil;
	        id<MTLComputePipelineState> second_pipeline = second_bf16
	            ? (second_reduce ? (second_shared ? runtime->linear_bf16_multi_row_shared_reduce_pipeline : runtime->linear_bf16_multi_row_reduce_pipeline) : runtime->linear_bf16_multi_row_pipeline)
	            : (second_tiled32_m16 ? runtime->linear_multi_row_tiled32_m16_f32_pipeline : (second_tiled32 ? runtime->linear_multi_row_tiled32_f32_pipeline : (second_tiled ? runtime->linear_multi_row_tiled_f32_pipeline : (second_reduce ? runtime->linear_multi_row_reduce_pipeline : runtime->linear_multi_row_pipeline))));
	        termite_metal_maybe_trace_dense_linear_dispatch(
	            "dense_mlp2.second",
	            second_linear_slot,
	            rows,
	            hidden_dim,
	            out_dim,
	            termite_metal_dense_linear_variant(second_bf16, second_shared, second_tiled32_m16, second_tiled32, second_tiled, second_reduce, false),
	            !frame_owned,
	            !second_encoder_owned,
	            false,
	            false,
	            false);
	        [second_encoder setComputePipelineState:second_pipeline];
        [second_encoder setBuffer:activated_buffer offset:0 atIndex:0];
        [second_encoder setBuffer:runtime->linear_weight_buffers[second_linear_slot] offset:0 atIndex:1];
        [second_encoder setBuffer:runtime->linear_bias_buffers[second_linear_slot] offset:0 atIndex:2];
        [second_encoder setBuffer:output_buffer offset:output_offset atIndex:3];
        [second_encoder setBytes:&second_params length:sizeof(second_params) atIndex:4];
        if (second_tiled32_m16) {
            [second_encoder dispatchThreadgroups:MTLSizeMake((out_dim + 31u) / 32u, (rows + 15u) / 16u, 1) threadsPerThreadgroup:MTLSizeMake(8u, 16u, 1)];
        } else if (second_tiled32) {
            [second_encoder dispatchThreadgroups:MTLSizeMake((out_dim + 31u) / 32u, (rows + 7u) / 8u, 1) threadsPerThreadgroup:MTLSizeMake(8u, 8u, 1)];
        } else if (second_tiled) {
            [second_encoder dispatchThreadgroups:MTLSizeMake((out_dim + 15u) / 16u, (rows + 7u) / 8u, 1) threadsPerThreadgroup:MTLSizeMake(8u, 8u, 1)];
        } else if (second_shared) {
            [second_encoder setThreadgroupMemoryLength:rows * 32u * sizeof(float) atIndex:0];
            [second_encoder dispatchThreadgroups:MTLSizeMake(out_dim, 1, 1) threadsPerThreadgroup:MTLSizeMake(32u, 1, 1)];
        } else if (second_reduce) {
            [second_encoder setThreadgroupMemoryLength:32u * sizeof(float) atIndex:0];
            [second_encoder dispatchThreadgroups:MTLSizeMake(out_dim, rows, 1) threadsPerThreadgroup:MTLSizeMake(32u, 1, 1)];
        } else {
            const size_t second_total = rows * out_dim;
            [second_encoder dispatchThreads:MTLSizeMake(second_total, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(second_pipeline, second_total), 1, 1)];
        }
        termite_metal_end_scoped_compute_encoder(second_encoder, second_encoder_owned);

        if (!frame_owned) return 0;
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -17;
    }
}

int termite_metal_decode_runtime_apply_dense_ffn_layer_norm_device(
    termite_metal_decode_runtime *runtime,
    size_t first_linear_slot,
    size_t second_linear_slot,
    size_t layer_norm_slot,
    void *input_handle,
    size_t input_offset,
    void *residual_handle,
    size_t residual_offset,
    size_t rows,
    size_t hidden_size,
    size_t intermediate_size,
    uint32_t activation_kind,
    float eps,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || input_handle == NULL || residual_handle == NULL || output_handle == NULL) return -1;
    runtime->deberta_ffn_fused_calls += 1;
    if (runtime->bias_activation_pipeline == nil || runtime->bias_add_layer_norm_pipeline == nil) {
        runtime->deberta_ffn_fused_fallbacks += 1;
        return -2;
    }
    if (first_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY ||
        second_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY ||
        layer_norm_slot >= TERMITE_METAL_LAYER_NORM_SLOT_CAPACITY) {
        runtime->deberta_ffn_fused_fallbacks += 1;
        return -3;
    }
    if (rows == 0 || hidden_size == 0 || intermediate_size == 0 ||
        rows > UINT32_MAX || hidden_size > UINT32_MAX || intermediate_size > UINT32_MAX) {
        runtime->deberta_ffn_fused_fallbacks += 1;
        return -4;
    }
    if (runtime->linear_slot_prepared[first_linear_slot] == 0 ||
        runtime->linear_slot_prepared[second_linear_slot] == 0 ||
        runtime->layer_norm_slot_prepared[layer_norm_slot] == 0) {
        runtime->deberta_ffn_fused_fallbacks += 1;
        return -5;
    }
    if (runtime->linear_weight_dtypes[first_linear_slot] != TERMITE_METAL_DENSE_LINEAR_DTYPE_F32 ||
        runtime->linear_weight_dtypes[second_linear_slot] != TERMITE_METAL_DENSE_LINEAR_DTYPE_F32) {
        runtime->deberta_ffn_fused_fallbacks += 1;
        return -6;
    }
    if (runtime->linear_in_dims[first_linear_slot] != hidden_size ||
        runtime->linear_out_dims[first_linear_slot] != intermediate_size ||
        runtime->linear_in_dims[second_linear_slot] != intermediate_size ||
        runtime->linear_out_dims[second_linear_slot] != hidden_size ||
        runtime->layer_norm_hidden_sizes[layer_norm_slot] != hidden_size) {
        runtime->deberta_ffn_fused_fallbacks += 1;
        return -7;
    }
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLBuffer> residual_buffer = (__bridge id<MTLBuffer>)residual_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t input_bytes = rows * hidden_size * sizeof(float);
        const size_t intermediate_bytes = rows * intermediate_size * sizeof(float);
        const size_t output_bytes = input_bytes;
        if (input_offset + input_bytes > input_buffer.length ||
            residual_offset + output_bytes > residual_buffer.length ||
            output_offset + output_bytes > output_buffer.length) {
            runtime->deberta_ffn_fused_fallbacks += 1;
            return -8;
        }
        id<MTLBuffer> first_output_buffer = nil;
        id<MTLBuffer> activated_buffer = nil;
        id<MTLBuffer> projected_buffer = nil;
        if (termite_metal_decode_runtime_ensure_hot_buffers(runtime, output_bytes, intermediate_bytes) == 0) {
            first_output_buffer = runtime->hot_intermediate_buffers[0];
            activated_buffer = runtime->hot_intermediate_buffers[1];
            projected_buffer = runtime->hot_hidden_buffers[0];
        } else {
            first_output_buffer = [runtime->device newBufferWithLength:intermediate_bytes options:MTLResourceStorageModePrivate];
            activated_buffer = [runtime->device newBufferWithLength:intermediate_bytes options:MTLResourceStorageModePrivate];
            projected_buffer = [runtime->device newBufferWithLength:output_bytes options:MTLResourceStorageModePrivate];
        }
        if (first_output_buffer == nil || activated_buffer == nil || projected_buffer == nil) {
            runtime->deberta_ffn_fused_fallbacks += 1;
            return -9;
        }

        const bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLCommandBuffer> command_buffer = frame_owned
            ? termite_metal_new_command_buffer(runtime->queue, __func__)
            : runtime->active_frame_cb;
        if (command_buffer == nil) {
            runtime->deberta_ffn_fused_fallbacks += 1;
            return -10;
        }
        if (!frame_owned) {
            if (termite_metal_decode_runtime_retain_frame_resource(runtime, input_buffer) != 0 ||
                termite_metal_decode_runtime_retain_frame_resource(runtime, residual_buffer) != 0 ||
                termite_metal_decode_runtime_retain_frame_resource(runtime, output_buffer) != 0 ||
                termite_metal_decode_runtime_retain_frame_resource(runtime, first_output_buffer) != 0 ||
                termite_metal_decode_runtime_retain_frame_resource(runtime, activated_buffer) != 0 ||
                termite_metal_decode_runtime_retain_frame_resource(runtime, projected_buffer) != 0) {
                runtime->deberta_ffn_fused_fallbacks += 1;
                return -11;
            }
        }

        if (termite_metal_direct_deberta_ffn_enabled()) {
            if (runtime->linear_multi_row_pipeline != nil &&
                runtime->linear_multi_row_reduce_pipeline != nil &&
                runtime->activation_pipeline != nil &&
                runtime->layer_norm_add_pipeline != nil)
            {
                termite_metal_linear_params first_params = {
                    .rows = (uint32_t)rows,
                    .in_dim = (uint32_t)hidden_size,
                    .out_dim = (uint32_t)intermediate_size,
                    .row_blocks = 0u,
                };
                termite_metal_apply_activation_params direct_activation_params = {
                    .activation_kind = activation_kind,
                    .rows = (uint32_t)rows,
                    .dim = (uint32_t)intermediate_size,
                };
                termite_metal_linear_params second_params = {
                    .rows = (uint32_t)rows,
                    .in_dim = (uint32_t)intermediate_size,
                    .out_dim = (uint32_t)hidden_size,
                    .row_blocks = 0u,
                };

	                const BOOL first_reduce = intermediate_size >= 128u && hidden_size >= 128u;
	                const BOOL first_tiled = first_reduce && rows >= 128u && runtime->linear_multi_row_tiled_f32_pipeline != nil;
	                const BOOL first_tiled32_m16 = first_reduce && rows >= 128u && runtime->linear_multi_row_tiled32_m16_f32_pipeline != nil;
	                const BOOL first_tiled32 = first_reduce && rows >= 128u && runtime->linear_multi_row_tiled32_f32_pipeline != nil;
	                const BOOL first_tiled_activation = (first_tiled32_m16 && runtime->linear_multi_row_tiled32_m16_activation_f32_pipeline != nil) ||
	                    (first_tiled32 && runtime->linear_multi_row_tiled32_activation_f32_pipeline != nil) ||
	                    (first_tiled && runtime->linear_multi_row_tiled_activation_f32_pipeline != nil);
                id<MTLBuffer> first_linear_output_buffer = first_tiled_activation ? activated_buffer : first_output_buffer;
                if (termite_metal_decode_runtime_prepare_planned_dense_linear_accesses(
                        runtime,
                        input_buffer,
                        input_offset,
                        input_bytes,
                        runtime->linear_weight_buffers[first_linear_slot],
                        runtime->linear_bias_buffers[first_linear_slot],
                        first_linear_output_buffer,
                        0,
                        intermediate_bytes,
                        -12) != 0) {
                    runtime->deberta_ffn_fused_fallbacks += 1;
                } else {
                    BOOL first_encoder_owned = YES;
                    id<MTLComputeCommandEncoder> first_encoder = termite_metal_scoped_compute_encoder_for(runtime, command_buffer, TERMITE_METAL_COMPUTE_SOURCE_FFN, &first_encoder_owned);
                    if (first_encoder == nil) {
                        runtime->deberta_ffn_fused_fallbacks += 1;
                        return -12;
                    }
		                    id<MTLComputePipelineState> first_pipeline = first_tiled32_m16
		                        ? (first_tiled_activation ? runtime->linear_multi_row_tiled32_m16_activation_f32_pipeline : runtime->linear_multi_row_tiled32_m16_f32_pipeline)
		                        : (first_tiled32 ? (first_tiled_activation ? runtime->linear_multi_row_tiled32_activation_f32_pipeline : runtime->linear_multi_row_tiled32_f32_pipeline)
			                        : (first_tiled ? (first_tiled_activation ? runtime->linear_multi_row_tiled_activation_f32_pipeline : runtime->linear_multi_row_tiled_f32_pipeline)
			                        : (first_reduce ? runtime->linear_multi_row_reduce_pipeline : runtime->linear_multi_row_pipeline)));
	                    termite_metal_maybe_trace_dense_linear_dispatch(
	                        "deberta_ffn_direct.first",
	                        first_linear_slot,
	                        rows,
	                        hidden_size,
	                        intermediate_size,
	                        termite_metal_dense_linear_variant(false, false, first_tiled32_m16, first_tiled32, first_tiled, first_reduce, first_tiled_activation),
	                        !frame_owned,
	                        !first_encoder_owned,
	                        false,
	                        first_tiled_activation,
	                        false);
	                    [first_encoder setComputePipelineState:first_pipeline];
                    [first_encoder setBuffer:input_buffer offset:input_offset atIndex:0];
                    [first_encoder setBuffer:runtime->linear_weight_buffers[first_linear_slot] offset:0 atIndex:1];
                    [first_encoder setBuffer:runtime->linear_bias_buffers[first_linear_slot] offset:0 atIndex:2];
                    [first_encoder setBuffer:first_linear_output_buffer offset:0 atIndex:3];
                    [first_encoder setBytes:&first_params length:sizeof(first_params) atIndex:4];
                    if (first_tiled_activation) {
                        [first_encoder setBytes:&direct_activation_params length:sizeof(direct_activation_params) atIndex:5];
                    }
	                    if (first_tiled32_m16) {
	                        [first_encoder dispatchThreadgroups:MTLSizeMake((intermediate_size + 31u) / 32u, (rows + 15u) / 16u, 1) threadsPerThreadgroup:MTLSizeMake(8u, 16u, 1)];
	                    } else if (first_tiled32) {
	                        [first_encoder dispatchThreadgroups:MTLSizeMake((intermediate_size + 31u) / 32u, (rows + 7u) / 8u, 1) threadsPerThreadgroup:MTLSizeMake(8u, 8u, 1)];
                    } else if (first_tiled) {
                        [first_encoder dispatchThreadgroups:MTLSizeMake((intermediate_size + 15u) / 16u, (rows + 7u) / 8u, 1) threadsPerThreadgroup:MTLSizeMake(8u, 8u, 1)];
                    } else if (first_reduce) {
                        [first_encoder setThreadgroupMemoryLength:32u * sizeof(float) atIndex:0];
                        [first_encoder dispatchThreadgroups:MTLSizeMake(intermediate_size, rows, 1) threadsPerThreadgroup:MTLSizeMake(32u, 1, 1)];
                    } else {
                        const size_t first_total = rows * intermediate_size;
                        [first_encoder dispatchThreads:MTLSizeMake(first_total, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(first_pipeline, first_total), 1, 1)];
                    }
                    if (!first_encoder_owned) {
                        [first_encoder memoryBarrierWithScope:MTLBarrierScopeBuffers];
                    }
                    termite_metal_end_scoped_compute_encoder(first_encoder, first_encoder_owned);

                    if (!first_tiled_activation) {
                        BOOL activation_encoder_owned = YES;
                        id<MTLComputeCommandEncoder> activation_encoder = termite_metal_scoped_compute_encoder_for(runtime, command_buffer, TERMITE_METAL_COMPUTE_SOURCE_FFN, &activation_encoder_owned);
                        if (activation_encoder == nil) {
                            runtime->deberta_ffn_fused_fallbacks += 1;
                            return -13;
                        }
                        [activation_encoder setComputePipelineState:runtime->activation_pipeline];
                        [activation_encoder setBuffer:first_output_buffer offset:0 atIndex:0];
                        [activation_encoder setBuffer:activated_buffer offset:0 atIndex:1];
                        [activation_encoder setBytes:&direct_activation_params length:sizeof(direct_activation_params) atIndex:2];
                        const size_t activation_total = rows * intermediate_size;
                        [activation_encoder dispatchThreads:MTLSizeMake(activation_total, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->activation_pipeline, activation_total), 1, 1)];
                        if (!activation_encoder_owned) {
                            [activation_encoder memoryBarrierWithScope:MTLBarrierScopeBuffers];
                        }
                        termite_metal_end_scoped_compute_encoder(activation_encoder, activation_encoder_owned);
                    }

                    if (termite_metal_decode_runtime_prepare_planned_dense_linear_accesses(
                            runtime,
                            activated_buffer,
                            0,
                            intermediate_bytes,
                            runtime->linear_weight_buffers[second_linear_slot],
                            runtime->linear_bias_buffers[second_linear_slot],
                            projected_buffer,
                            0,
                            output_bytes,
                            -14) != 0) {
                        runtime->deberta_ffn_fused_fallbacks += 1;
                    } else {
                        BOOL second_encoder_owned = YES;
                        id<MTLComputeCommandEncoder> second_encoder = termite_metal_scoped_compute_encoder_for(runtime, command_buffer, TERMITE_METAL_COMPUTE_SOURCE_FFN, &second_encoder_owned);
                        if (second_encoder == nil) {
                            runtime->deberta_ffn_fused_fallbacks += 1;
                            return -14;
                        }
	                        const BOOL second_reduce = hidden_size >= 128u && intermediate_size >= 128u;
	                        const BOOL second_tiled = second_reduce && rows >= 128u && runtime->linear_multi_row_tiled_f32_pipeline != nil;
	                        const BOOL second_tiled32_m16 = second_reduce && rows >= 128u && runtime->linear_multi_row_tiled32_m16_f32_pipeline != nil;
	                        const BOOL second_tiled32 = second_reduce && rows >= 128u && runtime->linear_multi_row_tiled32_f32_pipeline != nil;
		                        id<MTLComputePipelineState> second_pipeline = second_tiled32_m16
		                            ? runtime->linear_multi_row_tiled32_m16_f32_pipeline
		                            : (second_tiled32 ? runtime->linear_multi_row_tiled32_f32_pipeline
			                            : (second_tiled ? runtime->linear_multi_row_tiled_f32_pipeline
			                            : (second_reduce ? runtime->linear_multi_row_reduce_pipeline : runtime->linear_multi_row_pipeline)));
	                        termite_metal_maybe_trace_dense_linear_dispatch(
	                            "deberta_ffn_direct.second",
	                            second_linear_slot,
	                            rows,
	                            intermediate_size,
	                            hidden_size,
	                            termite_metal_dense_linear_variant(false, false, second_tiled32_m16, second_tiled32, second_tiled, second_reduce, false),
	                            !frame_owned,
	                            !second_encoder_owned,
	                            false,
	                            false,
	                            false);
	                        [second_encoder setComputePipelineState:second_pipeline];
                        [second_encoder setBuffer:activated_buffer offset:0 atIndex:0];
                        [second_encoder setBuffer:runtime->linear_weight_buffers[second_linear_slot] offset:0 atIndex:1];
                        [second_encoder setBuffer:runtime->linear_bias_buffers[second_linear_slot] offset:0 atIndex:2];
                        [second_encoder setBuffer:projected_buffer offset:0 atIndex:3];
                        [second_encoder setBytes:&second_params length:sizeof(second_params) atIndex:4];
	                        if (second_tiled32_m16) {
	                            [second_encoder dispatchThreadgroups:MTLSizeMake((hidden_size + 31u) / 32u, (rows + 15u) / 16u, 1) threadsPerThreadgroup:MTLSizeMake(8u, 16u, 1)];
	                        } else if (second_tiled32) {
	                            [second_encoder dispatchThreadgroups:MTLSizeMake((hidden_size + 31u) / 32u, (rows + 7u) / 8u, 1) threadsPerThreadgroup:MTLSizeMake(8u, 8u, 1)];
                        } else if (second_tiled) {
                            [second_encoder dispatchThreadgroups:MTLSizeMake((hidden_size + 15u) / 16u, (rows + 7u) / 8u, 1) threadsPerThreadgroup:MTLSizeMake(8u, 8u, 1)];
                        } else if (second_reduce) {
                            [second_encoder setThreadgroupMemoryLength:32u * sizeof(float) atIndex:0];
                            [second_encoder dispatchThreadgroups:MTLSizeMake(hidden_size, rows, 1) threadsPerThreadgroup:MTLSizeMake(32u, 1, 1)];
                        } else {
                            const size_t second_total = rows * hidden_size;
                            [second_encoder dispatchThreads:MTLSizeMake(second_total, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(second_pipeline, second_total), 1, 1)];
                        }
                        if (!second_encoder_owned) {
                            [second_encoder memoryBarrierWithScope:MTLBarrierScopeBuffers];
                        }
                        termite_metal_end_scoped_compute_encoder(second_encoder, second_encoder_owned);

                        termite_metal_apply_layer_norm_params direct_ln_params = {
                            .hidden_size = (uint32_t)hidden_size,
                            .eps = eps,
                        };
                        BOOL ln_encoder_owned = YES;
                        id<MTLComputeCommandEncoder> ln_encoder = termite_metal_scoped_compute_encoder_for(runtime, command_buffer, TERMITE_METAL_COMPUTE_SOURCE_FFN, &ln_encoder_owned);
                        if (ln_encoder == nil) {
                            runtime->deberta_ffn_fused_fallbacks += 1;
                            return -15;
                        }
                        const BOOL use_ln_rows_pipeline = rows > 1 && hidden_size >= 128 && runtime->layer_norm_add_rows_pipeline != nil;
                        [ln_encoder setComputePipelineState:use_ln_rows_pipeline ? runtime->layer_norm_add_rows_pipeline : runtime->layer_norm_add_pipeline];
                        [ln_encoder setBuffer:projected_buffer offset:0 atIndex:0];
                        [ln_encoder setBuffer:residual_buffer offset:residual_offset atIndex:1];
                        [ln_encoder setBuffer:runtime->layer_norm_weight_buffers[layer_norm_slot] offset:0 atIndex:2];
                        [ln_encoder setBuffer:runtime->layer_norm_bias_buffers[layer_norm_slot] offset:0 atIndex:3];
                        [ln_encoder setBuffer:output_buffer offset:output_offset atIndex:4];
                        [ln_encoder setBytes:&direct_ln_params length:sizeof(direct_ln_params) atIndex:5];
                        if (use_ln_rows_pipeline) {
                            [ln_encoder setThreadgroupMemoryLength:512u * sizeof(float) atIndex:0];
                            [ln_encoder dispatchThreadgroups:MTLSizeMake(rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(256u, 1, 1)];
                        } else {
                            [ln_encoder dispatchThreads:MTLSizeMake(rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(1, 1, 1)];
                        }
                        termite_metal_end_scoped_compute_encoder(ln_encoder, ln_encoder_owned);

                        if (!frame_owned) return 0;
                        [command_buffer commit];
                        [command_buffer waitUntilCompleted];
                        if (command_buffer.status == MTLCommandBufferStatusCompleted) return 0;
                        runtime->deberta_ffn_fused_fallbacks += 1;
                        return -16;
                    }
                }
            } else {
                runtime->deberta_ffn_fused_fallbacks += 1;
            }
        }
        if (!termite_metal_dense_mps_linear_enabled()) {
            runtime->deberta_ffn_fused_fallbacks += 1;
            return -17;
        }

        MPSMatrixDescriptor *first_left_desc = [MPSMatrixDescriptor matrixDescriptorWithRows:rows
                                                                                    columns:hidden_size
                                                                                   rowBytes:hidden_size * sizeof(float)
                                                                                   dataType:MPSDataTypeFloat32];
        MPSMatrixDescriptor *first_right_desc = [MPSMatrixDescriptor matrixDescriptorWithRows:intermediate_size
                                                                                     columns:hidden_size
                                                                                    rowBytes:hidden_size * sizeof(float)
                                                                                    dataType:MPSDataTypeFloat32];
        MPSMatrixDescriptor *first_result_desc = [MPSMatrixDescriptor matrixDescriptorWithRows:rows
                                                                                      columns:intermediate_size
                                                                                     rowBytes:intermediate_size * sizeof(float)
                                                                                     dataType:MPSDataTypeFloat32];
        MPSMatrix *first_left = [[MPSMatrix alloc] initWithBuffer:input_buffer offset:input_offset descriptor:first_left_desc];
        MPSMatrix *first_right = [[MPSMatrix alloc] initWithBuffer:runtime->linear_weight_buffers[first_linear_slot] offset:0 descriptor:first_right_desc];
        MPSMatrix *first_result = [[MPSMatrix alloc] initWithBuffer:first_output_buffer offset:0 descriptor:first_result_desc];
        MPSMatrixMultiplication *first_mm = termite_metal_decode_runtime_cached_mps_mm(runtime, first_linear_slot, rows, hidden_size, intermediate_size);
        if (first_left == nil || first_right == nil || first_result == nil || first_mm == nil) {
            runtime->deberta_ffn_fused_fallbacks += 1;
            return -12;
        }
        [first_mm encodeToCommandBuffer:command_buffer leftMatrix:first_left rightMatrix:first_right resultMatrix:first_result];

        termite_metal_apply_activation_params activation_params = {
            .activation_kind = activation_kind,
            .rows = (uint32_t)rows,
            .dim = (uint32_t)intermediate_size,
        };
        id<MTLComputeCommandEncoder> activation_encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_FFN);
        if (activation_encoder == nil) {
            runtime->deberta_ffn_fused_fallbacks += 1;
            return -13;
        }
        [activation_encoder setComputePipelineState:runtime->bias_activation_pipeline];
        [activation_encoder setBuffer:first_output_buffer offset:0 atIndex:0];
        [activation_encoder setBuffer:runtime->linear_bias_buffers[first_linear_slot] offset:0 atIndex:1];
        [activation_encoder setBuffer:activated_buffer offset:0 atIndex:2];
        [activation_encoder setBytes:&activation_params length:sizeof(activation_params) atIndex:3];
        const size_t activation_total = rows * intermediate_size;
        [activation_encoder dispatchThreads:MTLSizeMake(activation_total, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->bias_activation_pipeline, activation_total), 1, 1)];
        [activation_encoder endEncoding];

        MPSMatrixDescriptor *second_left_desc = [MPSMatrixDescriptor matrixDescriptorWithRows:rows
                                                                                     columns:intermediate_size
                                                                                    rowBytes:intermediate_size * sizeof(float)
                                                                                    dataType:MPSDataTypeFloat32];
        MPSMatrixDescriptor *second_right_desc = [MPSMatrixDescriptor matrixDescriptorWithRows:hidden_size
                                                                                      columns:intermediate_size
                                                                                     rowBytes:intermediate_size * sizeof(float)
                                                                                     dataType:MPSDataTypeFloat32];
        MPSMatrixDescriptor *second_result_desc = [MPSMatrixDescriptor matrixDescriptorWithRows:rows
                                                                                       columns:hidden_size
                                                                                      rowBytes:hidden_size * sizeof(float)
                                                                                      dataType:MPSDataTypeFloat32];
        MPSMatrix *second_left = [[MPSMatrix alloc] initWithBuffer:activated_buffer offset:0 descriptor:second_left_desc];
        MPSMatrix *second_right = [[MPSMatrix alloc] initWithBuffer:runtime->linear_weight_buffers[second_linear_slot] offset:0 descriptor:second_right_desc];
        MPSMatrix *second_result = [[MPSMatrix alloc] initWithBuffer:projected_buffer offset:0 descriptor:second_result_desc];
        MPSMatrixMultiplication *second_mm = termite_metal_decode_runtime_cached_mps_mm(runtime, second_linear_slot, rows, intermediate_size, hidden_size);
        if (second_left == nil || second_right == nil || second_result == nil || second_mm == nil) {
            runtime->deberta_ffn_fused_fallbacks += 1;
            return -14;
        }
        [second_mm encodeToCommandBuffer:command_buffer leftMatrix:second_left rightMatrix:second_right resultMatrix:second_result];

        termite_metal_apply_layer_norm_params ln_params = {
            .hidden_size = (uint32_t)hidden_size,
            .eps = eps,
        };
        id<MTLComputeCommandEncoder> ln_encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_FFN);
        if (ln_encoder == nil) {
            runtime->deberta_ffn_fused_fallbacks += 1;
            return -15;
        }
        const BOOL use_bias_ln_rows_pipeline = rows > 1 && hidden_size >= 128 && runtime->bias_add_layer_norm_rows_pipeline != nil;
        [ln_encoder setComputePipelineState:use_bias_ln_rows_pipeline ? runtime->bias_add_layer_norm_rows_pipeline : runtime->bias_add_layer_norm_pipeline];
        [ln_encoder setBuffer:projected_buffer offset:0 atIndex:0];
        [ln_encoder setBuffer:runtime->linear_bias_buffers[second_linear_slot] offset:0 atIndex:1];
        [ln_encoder setBuffer:residual_buffer offset:residual_offset atIndex:2];
        [ln_encoder setBuffer:runtime->layer_norm_weight_buffers[layer_norm_slot] offset:0 atIndex:3];
        [ln_encoder setBuffer:runtime->layer_norm_bias_buffers[layer_norm_slot] offset:0 atIndex:4];
        [ln_encoder setBuffer:output_buffer offset:output_offset atIndex:5];
        [ln_encoder setBytes:&ln_params length:sizeof(ln_params) atIndex:6];
        if (use_bias_ln_rows_pipeline) {
            [ln_encoder setThreadgroupMemoryLength:512u * sizeof(float) atIndex:0];
            [ln_encoder dispatchThreadgroups:MTLSizeMake(rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(256u, 1, 1)];
        } else {
            [ln_encoder dispatchThreads:MTLSizeMake(rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(1, 1, 1)];
        }
        [ln_encoder endEncoding];

        runtime->deberta_ffn_fused_mps_matmuls += 2;
        if (!frame_owned) {
            runtime->mps_dense_linear_active_frame_calls += 2;
            runtime->active_frame_mps_dense_linear_count += 2;
            return 0;
        }
        runtime->mps_dense_linear_standalone_calls += 2;
        const uint64_t wait_start = termite_metal_clock_monotonic_nanos();
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        const uint64_t wait_end = termite_metal_clock_monotonic_nanos();
        if (wait_end > wait_start) runtime->mps_dense_linear_standalone_wait_nanos += wait_end - wait_start;
        if (command_buffer.status == MTLCommandBufferStatusCompleted) {
            runtime->mps_dense_linear_standalone_gpu_nanos += termite_metal_command_buffer_gpu_elapsed_nanos(command_buffer);
            return 0;
        }
        runtime->deberta_ffn_fused_fallbacks += 1;
        return -16;
    }
}

int termite_metal_decode_runtime_apply_dense_linear_layer_norm_device(
    termite_metal_decode_runtime *runtime,
    size_t linear_slot,
    size_t layer_norm_slot,
    void *input_handle,
    size_t input_offset,
    void *residual_handle,
    size_t residual_offset,
    size_t rows,
    size_t in_dim,
    size_t hidden_size,
    float eps,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || input_handle == NULL || residual_handle == NULL || output_handle == NULL) return -1;
    if (runtime->bias_add_layer_norm_pipeline == nil || runtime->layer_norm_add_pipeline == nil) return -2;
    if (linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY ||
        layer_norm_slot >= TERMITE_METAL_LAYER_NORM_SLOT_CAPACITY) return -3;
    if (rows == 0 || in_dim == 0 || hidden_size == 0 ||
        rows > UINT32_MAX || in_dim > UINT32_MAX || hidden_size > UINT32_MAX) return -4;
    if (runtime->linear_slot_prepared[linear_slot] == 0 ||
        runtime->layer_norm_slot_prepared[layer_norm_slot] == 0) return -5;
    if (runtime->linear_weight_dtypes[linear_slot] != TERMITE_METAL_DENSE_LINEAR_DTYPE_F32) return -6;
    if (runtime->linear_in_dims[linear_slot] != in_dim ||
        runtime->linear_out_dims[linear_slot] != hidden_size ||
        runtime->layer_norm_hidden_sizes[layer_norm_slot] != hidden_size) return -7;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLBuffer> residual_buffer = (__bridge id<MTLBuffer>)residual_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t input_bytes = rows * in_dim * sizeof(float);
        const size_t output_bytes = rows * hidden_size * sizeof(float);
        if (input_offset + input_bytes > input_buffer.length ||
            residual_offset + output_bytes > residual_buffer.length ||
            output_offset + output_bytes > output_buffer.length) return -8;

        id<MTLBuffer> projected_buffer = nil;
        if (termite_metal_decode_runtime_ensure_hot_buffers(runtime, output_bytes, 0) == 0) {
            projected_buffer = runtime->hot_hidden_buffers[3];
        } else {
            projected_buffer = [runtime->device newBufferWithLength:output_bytes options:MTLResourceStorageModePrivate];
        }
        if (projected_buffer == nil) return -9;

        const bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLCommandBuffer> command_buffer = frame_owned
            ? termite_metal_new_command_buffer(runtime->queue, __func__)
            : runtime->active_frame_cb;
        if (command_buffer == nil) return -10;
        if (!frame_owned &&
            (termite_metal_decode_runtime_retain_frame_resource(runtime, input_buffer) != 0 ||
             termite_metal_decode_runtime_retain_frame_resource(runtime, residual_buffer) != 0 ||
             termite_metal_decode_runtime_retain_frame_resource(runtime, output_buffer) != 0 ||
             termite_metal_decode_runtime_retain_frame_resource(runtime, projected_buffer) != 0)) {
            return -11;
        }

        if (!termite_metal_dense_mps_linear_enabled()) {
            if (runtime->linear_multi_row_pipeline != nil &&
                runtime->linear_multi_row_reduce_pipeline != nil &&
                runtime->layer_norm_add_pipeline != nil)
            {
                termite_metal_linear_params linear_params = {
                    .rows = (uint32_t)rows,
                    .in_dim = (uint32_t)in_dim,
                    .out_dim = (uint32_t)hidden_size,
                    .row_blocks = 0u,
                };
                if (termite_metal_decode_runtime_prepare_planned_dense_linear_accesses(
                        runtime,
                        input_buffer,
                        input_offset,
                        input_bytes,
                        runtime->linear_weight_buffers[linear_slot],
                        runtime->linear_bias_buffers[linear_slot],
                        projected_buffer,
                        0,
                        output_bytes,
                        -12) != 0) {
                    return -12;
                }

                BOOL linear_encoder_owned = YES;
                id<MTLComputeCommandEncoder> linear_encoder = termite_metal_scoped_compute_encoder_for(runtime, command_buffer, TERMITE_METAL_COMPUTE_SOURCE_DENSE_LINEAR, &linear_encoder_owned);
                if (linear_encoder == nil) return -12;
                const BOOL reduce = hidden_size >= 128u && in_dim >= 128u;
                const BOOL tiled32_m16 = reduce && rows >= 128u && runtime->linear_multi_row_tiled32_m16_f32_pipeline != nil;
                const BOOL tiled = reduce && rows >= 128u && runtime->linear_multi_row_tiled_f32_pipeline != nil;
                const BOOL tiled32 = reduce && rows >= 128u && runtime->linear_multi_row_tiled32_f32_pipeline != nil;
	                id<MTLComputePipelineState> linear_pipeline = tiled32_m16
	                    ? runtime->linear_multi_row_tiled32_m16_f32_pipeline
	                    : (tiled32 ? runtime->linear_multi_row_tiled32_f32_pipeline
	                    : (tiled ? runtime->linear_multi_row_tiled_f32_pipeline
	                    : (reduce ? runtime->linear_multi_row_reduce_pipeline : runtime->linear_multi_row_pipeline)));
	                termite_metal_maybe_trace_dense_linear_dispatch(
	                    "dense_linear_layer_norm",
	                    linear_slot,
	                    rows,
	                    in_dim,
	                    hidden_size,
	                    termite_metal_dense_linear_variant(false, false, tiled32_m16, tiled32, tiled, reduce, false),
	                    !frame_owned,
	                    !linear_encoder_owned,
	                    false,
	                    false,
	                    false);
	                [linear_encoder setComputePipelineState:linear_pipeline];
                [linear_encoder setBuffer:input_buffer offset:input_offset atIndex:0];
                [linear_encoder setBuffer:runtime->linear_weight_buffers[linear_slot] offset:0 atIndex:1];
                [linear_encoder setBuffer:runtime->linear_bias_buffers[linear_slot] offset:0 atIndex:2];
                [linear_encoder setBuffer:projected_buffer offset:0 atIndex:3];
                [linear_encoder setBytes:&linear_params length:sizeof(linear_params) atIndex:4];
                if (tiled32_m16) {
                    [linear_encoder dispatchThreadgroups:MTLSizeMake((hidden_size + 31u) / 32u, (rows + 15u) / 16u, 1) threadsPerThreadgroup:MTLSizeMake(8u, 16u, 1)];
                } else if (tiled32) {
                    [linear_encoder dispatchThreadgroups:MTLSizeMake((hidden_size + 31u) / 32u, (rows + 7u) / 8u, 1) threadsPerThreadgroup:MTLSizeMake(8u, 8u, 1)];
                } else if (tiled) {
                    [linear_encoder dispatchThreadgroups:MTLSizeMake((hidden_size + 15u) / 16u, (rows + 7u) / 8u, 1) threadsPerThreadgroup:MTLSizeMake(8u, 8u, 1)];
                } else if (reduce) {
                    [linear_encoder setThreadgroupMemoryLength:32u * sizeof(float) atIndex:0];
                    [linear_encoder dispatchThreadgroups:MTLSizeMake(hidden_size, rows, 1) threadsPerThreadgroup:MTLSizeMake(32u, 1, 1)];
                } else {
                    const size_t total = rows * hidden_size;
                    [linear_encoder dispatchThreads:MTLSizeMake(total, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(linear_pipeline, total), 1, 1)];
                }
                if (!linear_encoder_owned) {
                    [linear_encoder memoryBarrierWithScope:MTLBarrierScopeBuffers];
                }
                termite_metal_end_scoped_compute_encoder(linear_encoder, linear_encoder_owned);

                termite_metal_apply_layer_norm_params ln_params = {
                    .hidden_size = (uint32_t)hidden_size,
                    .eps = eps,
                };
                termite_metal_planned_encoder_range ln_accesses[5];
                if (termite_metal_planned_range_make(projected_buffer, 0, output_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &ln_accesses[0], -13) != 0 ||
                    termite_metal_planned_range_make(residual_buffer, residual_offset, output_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &ln_accesses[1], -13) != 0 ||
                    termite_metal_planned_range_make(runtime->layer_norm_weight_buffers[layer_norm_slot], 0, runtime->layer_norm_weight_buffers[layer_norm_slot].length, TERMITE_METAL_PLANNED_RANGE_READ, &ln_accesses[2], -13) != 0 ||
                    termite_metal_planned_range_make(runtime->layer_norm_bias_buffers[layer_norm_slot], 0, runtime->layer_norm_bias_buffers[layer_norm_slot].length, TERMITE_METAL_PLANNED_RANGE_READ, &ln_accesses[3], -13) != 0 ||
                    termite_metal_planned_range_make(output_buffer, output_offset, output_bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &ln_accesses[4], -13) != 0 ||
                    termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, ln_accesses, 5, -13) != 0) {
                    return -13;
                }
                BOOL ln_encoder_owned = YES;
                id<MTLComputeCommandEncoder> ln_encoder = termite_metal_scoped_compute_encoder_for(runtime, command_buffer, TERMITE_METAL_COMPUTE_SOURCE_DENSE_LINEAR, &ln_encoder_owned);
                if (ln_encoder == nil) return -13;
                const BOOL use_add_ln_rows_pipeline = rows > 1 && hidden_size >= 128 && runtime->layer_norm_add_rows_pipeline != nil;
                [ln_encoder setComputePipelineState:use_add_ln_rows_pipeline ? runtime->layer_norm_add_rows_pipeline : runtime->layer_norm_add_pipeline];
                [ln_encoder setBuffer:projected_buffer offset:0 atIndex:0];
                [ln_encoder setBuffer:residual_buffer offset:residual_offset atIndex:1];
                [ln_encoder setBuffer:runtime->layer_norm_weight_buffers[layer_norm_slot] offset:0 atIndex:2];
                [ln_encoder setBuffer:runtime->layer_norm_bias_buffers[layer_norm_slot] offset:0 atIndex:3];
                [ln_encoder setBuffer:output_buffer offset:output_offset atIndex:4];
                [ln_encoder setBytes:&ln_params length:sizeof(ln_params) atIndex:5];
                if (use_add_ln_rows_pipeline) {
                    [ln_encoder setThreadgroupMemoryLength:512u * sizeof(float) atIndex:0];
                    [ln_encoder dispatchThreadgroups:MTLSizeMake(rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(256u, 1, 1)];
                } else {
                    [ln_encoder dispatchThreads:MTLSizeMake(rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(1, 1, 1)];
                }
                termite_metal_end_scoped_compute_encoder(ln_encoder, ln_encoder_owned);

                if (!frame_owned) return 0;
                [command_buffer commit];
                [command_buffer waitUntilCompleted];
                return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -14;
            }
            return -15;
        }

        MPSMatrixDescriptor *left_desc = [MPSMatrixDescriptor matrixDescriptorWithRows:rows
                                                                               columns:in_dim
                                                                              rowBytes:in_dim * sizeof(float)
                                                                              dataType:MPSDataTypeFloat32];
        MPSMatrixDescriptor *right_desc = [MPSMatrixDescriptor matrixDescriptorWithRows:hidden_size
                                                                                columns:in_dim
                                                                               rowBytes:in_dim * sizeof(float)
                                                                               dataType:MPSDataTypeFloat32];
        MPSMatrixDescriptor *result_desc = [MPSMatrixDescriptor matrixDescriptorWithRows:rows
                                                                                 columns:hidden_size
                                                                                rowBytes:hidden_size * sizeof(float)
                                                                                dataType:MPSDataTypeFloat32];
        MPSMatrix *left = [[MPSMatrix alloc] initWithBuffer:input_buffer offset:input_offset descriptor:left_desc];
        MPSMatrix *right = [[MPSMatrix alloc] initWithBuffer:runtime->linear_weight_buffers[linear_slot] offset:0 descriptor:right_desc];
        MPSMatrix *result = [[MPSMatrix alloc] initWithBuffer:projected_buffer offset:0 descriptor:result_desc];
        MPSMatrixMultiplication *mm = termite_metal_decode_runtime_cached_mps_mm(runtime, linear_slot, rows, in_dim, hidden_size);
        if (left == nil || right == nil || result == nil || mm == nil) return -12;
        [mm encodeToCommandBuffer:command_buffer leftMatrix:left rightMatrix:right resultMatrix:result];

        termite_metal_apply_layer_norm_params ln_params = {
            .hidden_size = (uint32_t)hidden_size,
            .eps = eps,
        };
        id<MTLComputeCommandEncoder> ln_encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_DENSE_LINEAR);
        if (ln_encoder == nil) return -13;
        const BOOL use_bias_ln_rows_pipeline = rows > 1 && hidden_size >= 128 && runtime->bias_add_layer_norm_rows_pipeline != nil;
        [ln_encoder setComputePipelineState:use_bias_ln_rows_pipeline ? runtime->bias_add_layer_norm_rows_pipeline : runtime->bias_add_layer_norm_pipeline];
        [ln_encoder setBuffer:projected_buffer offset:0 atIndex:0];
        [ln_encoder setBuffer:runtime->linear_bias_buffers[linear_slot] offset:0 atIndex:1];
        [ln_encoder setBuffer:residual_buffer offset:residual_offset atIndex:2];
        [ln_encoder setBuffer:runtime->layer_norm_weight_buffers[layer_norm_slot] offset:0 atIndex:3];
        [ln_encoder setBuffer:runtime->layer_norm_bias_buffers[layer_norm_slot] offset:0 atIndex:4];
        [ln_encoder setBuffer:output_buffer offset:output_offset atIndex:5];
        [ln_encoder setBytes:&ln_params length:sizeof(ln_params) atIndex:6];
        if (use_bias_ln_rows_pipeline) {
            [ln_encoder setThreadgroupMemoryLength:512u * sizeof(float) atIndex:0];
            [ln_encoder dispatchThreadgroups:MTLSizeMake(rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(256u, 1, 1)];
        } else {
            [ln_encoder dispatchThreads:MTLSizeMake(rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(1, 1, 1)];
        }
        [ln_encoder endEncoding];

        if (!frame_owned) {
            runtime->mps_dense_linear_active_frame_calls += 1;
            runtime->active_frame_mps_dense_linear_count += 1;
            return 0;
        }
        runtime->mps_dense_linear_standalone_calls += 1;
        const uint64_t wait_start = termite_metal_clock_monotonic_nanos();
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        const uint64_t wait_end = termite_metal_clock_monotonic_nanos();
        if (wait_end > wait_start) runtime->mps_dense_linear_standalone_wait_nanos += wait_end - wait_start;
        if (command_buffer.status == MTLCommandBufferStatusCompleted) {
            runtime->mps_dense_linear_standalone_gpu_nanos += termite_metal_command_buffer_gpu_elapsed_nanos(command_buffer);
            return 0;
        }
        return -14;
    }
}

static int termite_metal_decode_runtime_encode_bias_add_layer_norm(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    size_t linear_slot,
    size_t layer_norm_slot,
    id<MTLBuffer> projected_buffer,
    size_t projected_offset,
    id<MTLBuffer> residual_buffer,
    size_t residual_offset,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    size_t rows,
    size_t hidden_size,
    float eps,
    size_t source,
    int failure_code
) {
    if (runtime == NULL || command_buffer == nil || projected_buffer == nil || residual_buffer == nil || output_buffer == nil) return failure_code;
    if (runtime->bias_add_layer_norm_pipeline == nil || runtime->layer_norm_add_pipeline == nil) return failure_code;
    if (linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || layer_norm_slot >= TERMITE_METAL_LAYER_NORM_SLOT_CAPACITY) return failure_code;
    if (runtime->linear_bias_buffers[linear_slot] == nil ||
        runtime->layer_norm_weight_buffers[layer_norm_slot] == nil ||
        runtime->layer_norm_bias_buffers[layer_norm_slot] == nil) return failure_code;
    const size_t output_bytes = rows * hidden_size * sizeof(float);
    termite_metal_apply_layer_norm_params ln_params = {
        .hidden_size = (uint32_t)hidden_size,
        .eps = eps,
    };
    termite_metal_planned_encoder_range ln_accesses[6];
    if (termite_metal_planned_range_make(projected_buffer, projected_offset, output_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &ln_accesses[0], failure_code) != 0 ||
        termite_metal_planned_range_make(runtime->linear_bias_buffers[linear_slot], 0, runtime->linear_bias_buffers[linear_slot].length, TERMITE_METAL_PLANNED_RANGE_READ, &ln_accesses[1], failure_code) != 0 ||
        termite_metal_planned_range_make(residual_buffer, residual_offset, output_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &ln_accesses[2], failure_code) != 0 ||
        termite_metal_planned_range_make(runtime->layer_norm_weight_buffers[layer_norm_slot], 0, runtime->layer_norm_weight_buffers[layer_norm_slot].length, TERMITE_METAL_PLANNED_RANGE_READ, &ln_accesses[3], failure_code) != 0 ||
        termite_metal_planned_range_make(runtime->layer_norm_bias_buffers[layer_norm_slot], 0, runtime->layer_norm_bias_buffers[layer_norm_slot].length, TERMITE_METAL_PLANNED_RANGE_READ, &ln_accesses[4], failure_code) != 0 ||
        termite_metal_planned_range_make(output_buffer, output_offset, output_bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &ln_accesses[5], failure_code) != 0 ||
        termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, ln_accesses, 6, failure_code) != 0) {
        return failure_code;
    }
    if (runtime->active_planned_compute_encoder != nil) {
        [runtime->active_planned_compute_encoder memoryBarrierWithScope:MTLBarrierScopeBuffers];
    }
    BOOL ln_encoder_owned = YES;
    id<MTLComputeCommandEncoder> ln_encoder = termite_metal_scoped_compute_encoder_for(runtime, command_buffer, source, &ln_encoder_owned);
    if (ln_encoder == nil) return failure_code;
    const BOOL use_bias_ln_rows_pipeline = rows > 1 && hidden_size >= 128 && runtime->bias_add_layer_norm_rows_pipeline != nil;
    [ln_encoder setComputePipelineState:use_bias_ln_rows_pipeline ? runtime->bias_add_layer_norm_rows_pipeline : runtime->bias_add_layer_norm_pipeline];
    [ln_encoder setBuffer:projected_buffer offset:projected_offset atIndex:0];
    [ln_encoder setBuffer:runtime->linear_bias_buffers[linear_slot] offset:0 atIndex:1];
    [ln_encoder setBuffer:residual_buffer offset:residual_offset atIndex:2];
    [ln_encoder setBuffer:runtime->layer_norm_weight_buffers[layer_norm_slot] offset:0 atIndex:3];
    [ln_encoder setBuffer:runtime->layer_norm_bias_buffers[layer_norm_slot] offset:0 atIndex:4];
    [ln_encoder setBuffer:output_buffer offset:output_offset atIndex:5];
    [ln_encoder setBytes:&ln_params length:sizeof(ln_params) atIndex:6];
    if (use_bias_ln_rows_pipeline) {
        [ln_encoder setThreadgroupMemoryLength:512u * sizeof(float) atIndex:0];
        [ln_encoder dispatchThreadgroups:MTLSizeMake(rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(256u, 1, 1)];
    } else {
        [ln_encoder dispatchThreads:MTLSizeMake(rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(1, 1, 1)];
    }
    termite_metal_end_scoped_compute_encoder(ln_encoder, ln_encoder_owned);
    return 0;
}

int termite_metal_decode_runtime_apply_quantized_linear_layer_norm_device(
    termite_metal_decode_runtime *runtime,
    uint32_t format,
    size_t linear_slot,
    size_t layer_norm_slot,
    void *input_handle,
    size_t input_offset,
    void *residual_handle,
    size_t residual_offset,
    size_t rows,
    size_t in_dim,
    size_t hidden_size,
    float eps,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || input_handle == NULL || residual_handle == NULL || output_handle == NULL) return -1;
    if (linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || layer_norm_slot >= TERMITE_METAL_LAYER_NORM_SLOT_CAPACITY) return -2;
    if (rows == 0 || in_dim == 0 || hidden_size == 0 ||
        rows > UINT32_MAX || in_dim > UINT32_MAX || hidden_size > UINT32_MAX) return -3;
    termite_metal_quant_linear_slot_view view;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, format, linear_slot, in_dim, hidden_size, &view) != 0) return -4;
    if (runtime->layer_norm_slot_prepared[layer_norm_slot] == 0 ||
        runtime->layer_norm_hidden_sizes[layer_norm_slot] != hidden_size) return -5;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLBuffer> residual_buffer = (__bridge id<MTLBuffer>)residual_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t input_bytes = rows * in_dim * sizeof(float);
        const size_t output_bytes = rows * hidden_size * sizeof(float);
        if (input_offset + input_bytes > input_buffer.length ||
            residual_offset + output_bytes > residual_buffer.length ||
            output_offset + output_bytes > output_buffer.length) return -6;

        id<MTLBuffer> projected_buffer = nil;
        if (termite_metal_decode_runtime_ensure_hot_buffers(runtime, output_bytes, 0) == 0) {
            projected_buffer = runtime->hot_hidden_buffers[3];
        } else {
            projected_buffer = [runtime->device newBufferWithLength:output_bytes options:MTLResourceStorageModePrivate];
        }
        if (projected_buffer == nil) return -7;

        const bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLCommandBuffer> command_buffer = frame_owned
            ? termite_metal_new_command_buffer(runtime->queue, __func__)
            : runtime->active_frame_cb;
        if (command_buffer == nil) return -8;
        if (!frame_owned &&
            (termite_metal_decode_runtime_retain_frame_resource(runtime, input_buffer) != 0 ||
             termite_metal_decode_runtime_retain_frame_resource(runtime, residual_buffer) != 0 ||
             termite_metal_decode_runtime_retain_frame_resource(runtime, output_buffer) != 0 ||
             termite_metal_decode_runtime_retain_frame_resource(runtime, projected_buffer) != 0)) {
            return -9;
        }

        const int linear_rc = termite_metal_decode_runtime_apply_quantized_linear_slot_device(
            runtime,
            format,
            linear_slot,
            input_handle,
            input_offset,
            rows,
            in_dim,
            hidden_size,
            (__bridge void *)projected_buffer,
            0);
        if (linear_rc != 0) return -10;

        const int ln_rc = termite_metal_decode_runtime_encode_bias_add_layer_norm(
            runtime,
            command_buffer,
            linear_slot,
            layer_norm_slot,
            projected_buffer,
            0,
            residual_buffer,
            residual_offset,
            output_buffer,
            output_offset,
            rows,
            hidden_size,
            eps,
            TERMITE_METAL_COMPUTE_SOURCE_DENSE_LINEAR,
            -11);
        if (ln_rc != 0) return ln_rc;

        if (!frame_owned) return 0;
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -12;
    }
}

int termite_metal_decode_runtime_apply_quantized_ffn_layer_norm_device(
    termite_metal_decode_runtime *runtime,
    uint32_t format,
    size_t first_linear_slot,
    size_t second_linear_slot,
    size_t layer_norm_slot,
    void *input_handle,
    size_t input_offset,
    void *residual_handle,
    size_t residual_offset,
    size_t rows,
    size_t hidden_size,
    size_t intermediate_size,
    uint32_t activation_kind,
    float eps,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || input_handle == NULL || residual_handle == NULL || output_handle == NULL) return -1;
    runtime->deberta_ffn_fused_calls += 1;
    if (first_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY ||
        second_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY ||
        layer_norm_slot >= TERMITE_METAL_LAYER_NORM_SLOT_CAPACITY) {
        runtime->deberta_ffn_fused_fallbacks += 1;
        return -2;
    }
    if (rows == 0 || hidden_size == 0 || intermediate_size == 0 ||
        rows > UINT32_MAX || hidden_size > UINT32_MAX || intermediate_size > UINT32_MAX) {
        runtime->deberta_ffn_fused_fallbacks += 1;
        return -3;
    }
    termite_metal_quant_linear_slot_view first_view;
    termite_metal_quant_linear_slot_view second_view;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, format, first_linear_slot, hidden_size, intermediate_size, &first_view) != 0 ||
        termite_metal_decode_runtime_require_quant_linear_slot(runtime, format, second_linear_slot, intermediate_size, hidden_size, &second_view) != 0 ||
        first_view.values_per_block != second_view.values_per_block ||
        first_view.bytes_per_block != second_view.bytes_per_block) {
        runtime->deberta_ffn_fused_fallbacks += 1;
        return -4;
    }
    if (runtime->bias_activation_pipeline == nil ||
        runtime->bias_add_layer_norm_pipeline == nil ||
        runtime->linear_bias_buffers[first_linear_slot] == nil ||
        runtime->linear_bias_buffers[second_linear_slot] == nil ||
        runtime->layer_norm_slot_prepared[layer_norm_slot] == 0 ||
        runtime->layer_norm_hidden_sizes[layer_norm_slot] != hidden_size) {
        runtime->deberta_ffn_fused_fallbacks += 1;
        return -5;
    }
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLBuffer> residual_buffer = (__bridge id<MTLBuffer>)residual_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t input_bytes = rows * hidden_size * sizeof(float);
        const size_t intermediate_bytes = rows * intermediate_size * sizeof(float);
        const size_t output_bytes = input_bytes;
        if (input_offset + input_bytes > input_buffer.length ||
            residual_offset + output_bytes > residual_buffer.length ||
            output_offset + output_bytes > output_buffer.length) {
            runtime->deberta_ffn_fused_fallbacks += 1;
            return -6;
        }

        id<MTLBuffer> first_output_buffer = nil;
        id<MTLBuffer> activated_buffer = nil;
        id<MTLBuffer> projected_buffer = nil;
        if (termite_metal_decode_runtime_ensure_hot_buffers(runtime, output_bytes, intermediate_bytes) == 0) {
            first_output_buffer = runtime->hot_intermediate_buffers[0];
            activated_buffer = runtime->hot_intermediate_buffers[1];
            projected_buffer = runtime->hot_hidden_buffers[0];
        } else {
            first_output_buffer = [runtime->device newBufferWithLength:intermediate_bytes options:MTLResourceStorageModePrivate];
            activated_buffer = [runtime->device newBufferWithLength:intermediate_bytes options:MTLResourceStorageModePrivate];
            projected_buffer = [runtime->device newBufferWithLength:output_bytes options:MTLResourceStorageModePrivate];
        }
        if (first_output_buffer == nil || activated_buffer == nil || projected_buffer == nil) {
            runtime->deberta_ffn_fused_fallbacks += 1;
            return -7;
        }

        const bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLCommandBuffer> command_buffer = frame_owned
            ? termite_metal_new_command_buffer(runtime->queue, __func__)
            : runtime->active_frame_cb;
        if (command_buffer == nil) {
            runtime->deberta_ffn_fused_fallbacks += 1;
            return -8;
        }
        if (!frame_owned &&
            (termite_metal_decode_runtime_retain_frame_resource(runtime, input_buffer) != 0 ||
             termite_metal_decode_runtime_retain_frame_resource(runtime, residual_buffer) != 0 ||
             termite_metal_decode_runtime_retain_frame_resource(runtime, output_buffer) != 0 ||
             termite_metal_decode_runtime_retain_frame_resource(runtime, first_output_buffer) != 0 ||
             termite_metal_decode_runtime_retain_frame_resource(runtime, activated_buffer) != 0 ||
             termite_metal_decode_runtime_retain_frame_resource(runtime, projected_buffer) != 0)) {
            runtime->deberta_ffn_fused_fallbacks += 1;
            return -9;
        }

        const int first_rc = termite_metal_decode_runtime_apply_quantized_linear_slot_device(
            runtime,
            format,
            first_linear_slot,
            input_handle,
            input_offset,
            rows,
            hidden_size,
            intermediate_size,
            (__bridge void *)first_output_buffer,
            0);
        if (first_rc != 0) {
            runtime->deberta_ffn_fused_fallbacks += 1;
            return -10;
        }
        if (runtime->active_planned_compute_encoder != nil) {
            [runtime->active_planned_compute_encoder memoryBarrierWithScope:MTLBarrierScopeBuffers];
        }

        termite_metal_apply_activation_params activation_params = {
            .activation_kind = activation_kind,
            .rows = (uint32_t)rows,
            .dim = (uint32_t)intermediate_size,
        };
        termite_metal_planned_encoder_range activation_accesses[3];
        if (termite_metal_planned_range_make(first_output_buffer, 0, intermediate_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &activation_accesses[0], -11) != 0 ||
            termite_metal_planned_range_make(runtime->linear_bias_buffers[first_linear_slot], 0, runtime->linear_bias_buffers[first_linear_slot].length, TERMITE_METAL_PLANNED_RANGE_READ, &activation_accesses[1], -11) != 0 ||
            termite_metal_planned_range_make(activated_buffer, 0, intermediate_bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &activation_accesses[2], -11) != 0 ||
            termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, activation_accesses, 3, -11) != 0) {
            runtime->deberta_ffn_fused_fallbacks += 1;
            return -11;
        }
        BOOL activation_encoder_owned = YES;
        id<MTLComputeCommandEncoder> activation_encoder = termite_metal_scoped_compute_encoder_for(runtime, command_buffer, TERMITE_METAL_COMPUTE_SOURCE_FFN, &activation_encoder_owned);
        if (activation_encoder == nil) {
            runtime->deberta_ffn_fused_fallbacks += 1;
            return -12;
        }
        [activation_encoder setComputePipelineState:runtime->bias_activation_pipeline];
        [activation_encoder setBuffer:first_output_buffer offset:0 atIndex:0];
        [activation_encoder setBuffer:runtime->linear_bias_buffers[first_linear_slot] offset:0 atIndex:1];
        [activation_encoder setBuffer:activated_buffer offset:0 atIndex:2];
        [activation_encoder setBytes:&activation_params length:sizeof(activation_params) atIndex:3];
        const size_t activation_total = rows * intermediate_size;
        [activation_encoder dispatchThreads:MTLSizeMake(activation_total, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->bias_activation_pipeline, activation_total), 1, 1)];
        termite_metal_end_scoped_compute_encoder(activation_encoder, activation_encoder_owned);

        const int second_rc = termite_metal_decode_runtime_apply_quantized_linear_slot_device(
            runtime,
            format,
            second_linear_slot,
            (__bridge void *)activated_buffer,
            0,
            rows,
            intermediate_size,
            hidden_size,
            (__bridge void *)projected_buffer,
            0);
        if (second_rc != 0) {
            runtime->deberta_ffn_fused_fallbacks += 1;
            return -13;
        }

        const int ln_rc = termite_metal_decode_runtime_encode_bias_add_layer_norm(
            runtime,
            command_buffer,
            second_linear_slot,
            layer_norm_slot,
            projected_buffer,
            0,
            residual_buffer,
            residual_offset,
            output_buffer,
            output_offset,
            rows,
            hidden_size,
            eps,
            TERMITE_METAL_COMPUTE_SOURCE_FFN,
            -14);
        if (ln_rc != 0) {
            runtime->deberta_ffn_fused_fallbacks += 1;
            return ln_rc;
        }

        if (!frame_owned) return 0;
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        if (command_buffer.status == MTLCommandBufferStatusCompleted) return 0;
        runtime->deberta_ffn_fused_fallbacks += 1;
        return -15;
    }
}

int termite_metal_decode_runtime_apply_i2_s_linear_slot(
    termite_metal_decode_runtime *runtime,
    size_t slot,
    const float *input,
    size_t rows,
    size_t in_dim,
    size_t out_dim,
    float *output
) {
    if (runtime == NULL || input == NULL || output == NULL) return -1;
    if (runtime->i2_s_quantize_pipeline == nil || runtime->i2_s_linear_i8_pipeline == nil) return -2;
    if (slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || rows == 0 || in_dim == 0 || out_dim == 0) return -3;
    termite_metal_quant_linear_slot_view view;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_I2_S, slot, in_dim, out_dim, &view) != 0) return -4;
    if (rows > UINT32_MAX || in_dim > UINT32_MAX || out_dim > UINT32_MAX) return -6;
    @autoreleasepool {
        const size_t input_bytes = rows * in_dim * sizeof(float);
        const size_t output_bytes = rows * out_dim * sizeof(float);
        const size_t qinput_bytes = rows * in_dim * sizeof(int8_t);
        const size_t scale_bytes = rows * sizeof(float);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)input length:input_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:input length:input_bytes options:MTLResourceStorageModeShared];
        if (termite_metal_decode_runtime_ensure_i2s_quantize_buffers(runtime, qinput_bytes, scale_bytes) != 0) return -7;
        id<MTLBuffer> qinput_buffer = runtime->i2s_qinput_buffer;
        id<MTLBuffer> scale_buffer = runtime->i2s_scale_buffer;
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithBytesNoCopy:output length:output_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) output_buffer = [runtime->device newBufferWithBytes:output length:output_bytes options:MTLResourceStorageModeShared];
        if (input_buffer == nil || qinput_buffer == nil || scale_buffer == nil || output_buffer == nil) return -7;
        termite_metal_quantize_rows_params quantize_params = {
            .rows = (uint32_t)rows,
            .dim = (uint32_t)in_dim,
        };
        termite_metal_linear_params params = {
            .rows = (uint32_t)rows,
            .in_dim = (uint32_t)in_dim,
            .out_dim = (uint32_t)out_dim,
            .row_blocks = (uint32_t)(in_dim / 128u),
        };
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -9;
        id<MTLComputeCommandEncoder> quantize_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (quantize_encoder == nil) return -10;
        [quantize_encoder setComputePipelineState:runtime->i2_s_quantize_pipeline];
        [quantize_encoder setBuffer:input_buffer offset:0 atIndex:0];
        [quantize_encoder setBuffer:qinput_buffer offset:0 atIndex:1];
        [quantize_encoder setBuffer:scale_buffer offset:0 atIndex:2];
        [quantize_encoder setBytes:&quantize_params length:sizeof(quantize_params) atIndex:3];
        NSUInteger quantize_thread_width = runtime->i2_s_quantize_pipeline.maxTotalThreadsPerThreadgroup;
        if (quantize_thread_width == 0) quantize_thread_width = 1;
        if (quantize_thread_width > rows && rows > 0) quantize_thread_width = rows;
        [quantize_encoder dispatchThreads:MTLSizeMake(rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(quantize_thread_width, 1, 1)];
        [quantize_encoder endEncoding];
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (encoder == nil) return -11;
        [encoder setComputePipelineState:runtime->i2_s_linear_i8_pipeline];
        [encoder setBuffer:qinput_buffer offset:0 atIndex:0];
        [encoder setBuffer:view.weight_buffer offset:0 atIndex:1];
        [encoder setBuffer:scale_buffer offset:0 atIndex:2];
        [encoder setBuffer:output_buffer offset:0 atIndex:3];
        [encoder setBytes:&params length:sizeof(params) atIndex:4];
        NSUInteger thread_width = runtime->i2_s_linear_i8_pipeline.maxTotalThreadsPerThreadgroup;
        if (thread_width == 0) thread_width = 32;
        const size_t total = rows * out_dim;
        if (thread_width > total && total > 0) thread_width = total;
        [encoder dispatchThreads:MTLSizeMake(total, 1, 1) threadsPerThreadgroup:MTLSizeMake(thread_width, 1, 1)];
        [encoder endEncoding];
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -12);
    }
}

int termite_metal_decode_runtime_apply_quantized_linear_slot_device(
    termite_metal_decode_runtime *runtime,
    uint32_t format,
    size_t slot,
    void *input_handle,
    size_t input_offset,
    size_t rows,
    size_t in_dim,
    size_t out_dim,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL) return -1;
    if (slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -2;
    if (runtime->quant_linear_formats[slot] != format) return -3;
    if (format == TERMITE_METAL_QUANT_FORMAT_TL1 || format == TERMITE_METAL_QUANT_FORMAT_TL2) {
        if (input_handle == NULL || output_handle == NULL) return -4;
        if (runtime->quant_linear_slot_prepared[slot] == 0) return -5;
        if (runtime->quant_linear_in_dims[slot] != in_dim || runtime->quant_linear_out_dims[slot] != out_dim) return -6;
        if (rows == 0 || in_dim == 0 || out_dim == 0 || rows > UINT32_MAX || in_dim > UINT32_MAX || out_dim > UINT32_MAX) return -7;
        @autoreleasepool {
            id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
            id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
            id<MTLBuffer> weight_buffer = runtime->quant_linear_weight_buffers[slot];
            id<MTLComputePipelineState> pipeline = format == TERMITE_METAL_QUANT_FORMAT_TL1 ? runtime->tl1_pipeline : runtime->tl2_pipeline;
            if (input_buffer == nil || output_buffer == nil || weight_buffer == nil || pipeline == nil) return -8;
            const size_t input_bytes = rows * in_dim * sizeof(float);
            const size_t output_bytes = rows * out_dim * sizeof(float);
            if (input_offset + input_bytes > input_buffer.length) return -9;
            if (output_offset + output_bytes > output_buffer.length) return -10;
            bool frame_owned = true;
            id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
            if (command_buffer == nil) return -11;
            id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
            const BOOL planned_encoder = (encoder != nil);
            if (!planned_encoder) {
                encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_QUANT_LINEAR);
            }
            if (encoder == nil) return -12;
            termite_metal_planned_encoder_range accesses[3];
            if (termite_metal_planned_range_make(input_buffer, input_offset, input_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[0], -12) != 0 ||
                termite_metal_planned_range_make(weight_buffer, 0, weight_buffer.length, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[1], -12) != 0 ||
                termite_metal_planned_range_make(output_buffer, output_offset, output_bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &accesses[2], -12) != 0 ||
                termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, accesses, 3, -12) != 0)
            {
                return -12;
            }
            [encoder setComputePipelineState:pipeline];
            [encoder setBuffer:input_buffer offset:input_offset atIndex:0];
            [encoder setBuffer:weight_buffer offset:0 atIndex:1];
            [encoder setBuffer:output_buffer offset:output_offset atIndex:2];
            if (format == TERMITE_METAL_QUANT_FORMAT_TL1) {
                termite_metal_tl1_params params = { .rows = (uint32_t)rows, .in_dim = (uint32_t)in_dim, .out_dim = (uint32_t)out_dim, .packed_len = runtime->quant_linear_tl1_packed_len[slot], .bm = runtime->quant_linear_tl1_bm[slot], .cfg_by = runtime->quant_linear_tl1_cfg_by[slot], .bmm = runtime->quant_linear_tl1_bmm[slot] };
                [encoder setBytes:&params length:sizeof(params) atIndex:3];
            } else {
                termite_metal_tl2_params params = { .rows = (uint32_t)rows, .in_dim = (uint32_t)in_dim, .out_dim = (uint32_t)out_dim, .scale_off = runtime->quant_linear_tl2_scale_off[slot], .three_value_len = runtime->quant_linear_tl2_three_value_len[slot], .three_sign_len = runtime->quant_linear_tl2_three_sign_len[slot], .bm = runtime->quant_linear_tl2_bm[slot], .cfg_by = runtime->quant_linear_tl2_cfg_by[slot], .bmm = runtime->quant_linear_tl2_bmm[slot], .three_cols = runtime->quant_linear_tl2_three_cols[slot], .two_cols = runtime->quant_linear_tl2_two_cols[slot] };
                [encoder setBytes:&params length:sizeof(params) atIndex:3];
            }
            [encoder dispatchThreads:MTLSizeMake(out_dim, rows, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(pipeline, out_dim), 1, 1)];
            if (!planned_encoder) [encoder endEncoding];
            return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -13);
        }
    }
    termite_metal_quant_linear_slot_view view;
    if (termite_metal_decode_runtime_quant_linear_slot_view(runtime, format, slot, &view) != 0) return -5;
    return termite_metal_decode_runtime_apply_quantized_linear_slot_device_direct(
        runtime,
        slot,
        input_handle,
        input_offset,
        rows,
        in_dim,
        out_dim,
        output_handle,
        output_offset,
        view.pipeline,
        view.weight_buffer,
        view.prepared,
        view.in_dim,
        view.out_dim,
        view.format,
        view.values_per_block,
        view.bytes_per_block
    );
}

static int termite_metal_decode_runtime_apply_quantized_q_kv_pair_linear_qkv_slots_device(
    termite_metal_decode_runtime *runtime,
    uint32_t q_format,
    uint32_t kv_format,
    size_t q_slot,
    size_t k_slot,
    size_t v_slot,
    void *input_handle,
    size_t input_offset,
    size_t rows,
    size_t in_dim,
    size_t q_out_dim,
    size_t kv_out_dim,
    void *q_output_handle,
    size_t q_output_offset,
    void *k_output_handle,
    size_t k_output_offset,
    void *v_output_handle,
    size_t v_output_offset
);

static id<MTLComputePipelineState> termite_metal_quant_pair_pipeline_for_format(
    termite_metal_decode_runtime *runtime,
    uint32_t format
);

static int termite_metal_encode_quant_q_plus_kv_pair_descriptors(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    uint32_t q_format,
    uint32_t kv_format,
    size_t q_slot,
    size_t k_slot,
    id<MTLBuffer> input_buffer,
    size_t input_offset,
    size_t rows,
    size_t in_dim,
    size_t q_out_dim,
    size_t kv_out_dim,
    const termite_metal_quant_linear_slot_view *q_view,
    const termite_metal_quant_linear_slot_view *k_view,
    const termite_metal_quant_linear_slot_view *v_view,
    id<MTLComputePipelineState> kv_pair_pipeline,
    id<MTLBuffer> q_output_buffer,
    size_t q_output_offset,
    id<MTLBuffer> k_output_buffer,
    size_t k_output_offset,
    id<MTLBuffer> v_output_buffer,
    size_t v_output_offset,
    int q_failure_code,
    int kv_failure_code
);

int termite_metal_decode_runtime_apply_quantized_linear_qkv_slots_device(
    termite_metal_decode_runtime *runtime,
    uint32_t q_format,
    uint32_t k_format,
    uint32_t v_format,
    size_t q_slot,
    size_t k_slot,
    size_t v_slot,
    void *input_handle,
    size_t input_offset,
    size_t rows,
    size_t in_dim,
    size_t q_out_dim,
    size_t kv_out_dim,
    void *q_output_handle,
    size_t q_output_offset,
    void *k_output_handle,
    size_t k_output_offset,
    void *v_output_handle,
    size_t v_output_offset
) {
    if (runtime == NULL || input_handle == NULL || q_output_handle == NULL || k_output_handle == NULL || v_output_handle == NULL) return -1;
    if (q_format != TERMITE_METAL_QUANT_FORMAT_Q8_0 ||
        k_format != TERMITE_METAL_QUANT_FORMAT_Q8_0 ||
        v_format != TERMITE_METAL_QUANT_FORMAT_Q8_0)
    {
        if (k_format != v_format) return -2;
        return termite_metal_decode_runtime_apply_quantized_q_kv_pair_linear_qkv_slots_device(
            runtime,
            q_format,
            k_format,
            q_slot,
            k_slot,
            v_slot,
            input_handle,
            input_offset,
            rows,
            in_dim,
            q_out_dim,
            kv_out_dim,
            q_output_handle,
            q_output_offset,
            k_output_handle,
            k_output_offset,
            v_output_handle,
            v_output_offset);
    }
    if (runtime->q8_0_pipeline == nil || runtime->q8_0_pair_pipeline == nil) return -2;
    if (q_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || k_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || v_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -3;
    if (q_slot == k_slot || q_slot == v_slot || k_slot == v_slot) return -4;
    termite_metal_quant_linear_slot_view q_view;
    termite_metal_quant_linear_slot_view k_view;
    termite_metal_quant_linear_slot_view v_view;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q8_0, q_slot, in_dim, q_out_dim, &q_view) != 0) return -5;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q8_0, k_slot, in_dim, kv_out_dim, &k_view) != 0) return -6;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q8_0, v_slot, in_dim, kv_out_dim, &v_view) != 0) return -7;
    if (rows == 0 || rows > UINT32_MAX || in_dim == 0 || q_out_dim == 0 || kv_out_dim == 0 || in_dim > UINT32_MAX || q_out_dim > UINT32_MAX || kv_out_dim > UINT32_MAX) return -8;
    if (in_dim % 32u != 0) return -9;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLBuffer> q_output_buffer = (__bridge id<MTLBuffer>)q_output_handle;
        id<MTLBuffer> k_output_buffer = (__bridge id<MTLBuffer>)k_output_handle;
        id<MTLBuffer> v_output_buffer = (__bridge id<MTLBuffer>)v_output_handle;
        const size_t input_bytes = rows * in_dim * sizeof(float);
        const size_t q_output_bytes = rows * q_out_dim * sizeof(float);
        const size_t kv_output_bytes = rows * kv_out_dim * sizeof(float);
        if (input_offset + input_bytes > input_buffer.length) return -10;
        if (q_output_offset + q_output_bytes > q_output_buffer.length) return -11;
        if (k_output_offset + kv_output_bytes > k_output_buffer.length) return -12;
        if (v_output_offset + kv_output_bytes > v_output_buffer.length) return -13;

        const bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLCommandBuffer> command_buffer = frame_owned
            ? termite_metal_new_command_buffer(runtime->queue, __func__)
            : runtime->active_frame_cb;
        if (command_buffer == nil) return -14;

        if (rows == 1 && termite_metal_encode_q8_0_qkv_linear(
                runtime,
                command_buffer,
                input_buffer,
                input_offset,
                q_view.weight_buffer,
                k_view.weight_buffer,
                v_view.weight_buffer,
                q_output_buffer,
                q_output_offset,
                k_output_buffer,
                k_output_offset,
                v_output_buffer,
                v_output_offset,
                rows,
                in_dim,
                q_out_dim,
                kv_out_dim,
                -15) != 0) return -15;

        if (rows != 1 && termite_metal_encode_q8_0_linear(
                runtime,
                command_buffer,
                input_buffer,
                input_offset,
                q_view.weight_buffer,
                q_output_buffer,
                q_output_offset,
                rows,
                in_dim,
                q_out_dim,
                -15) != 0) return -15;

        if (rows != 1) {
            if (rows >= 9u && runtime->q8_0_mm_pipeline != nil) {
                if (termite_metal_encode_q8_0_linear(
                        runtime,
                        command_buffer,
                        input_buffer,
                        input_offset,
                        k_view.weight_buffer,
                        k_output_buffer,
                        k_output_offset,
                        rows,
                        in_dim,
                        kv_out_dim,
                        -16) != 0) return -16;
                if (termite_metal_encode_q8_0_linear(
                        runtime,
                        command_buffer,
                        input_buffer,
                        input_offset,
                        v_view.weight_buffer,
                        v_output_buffer,
                        v_output_offset,
                        rows,
                        in_dim,
                        kv_out_dim,
                        -16) != 0) return -16;
	            } else {
	                if (termite_metal_encode_q8_0_pair_linear(
	                    runtime,
	                    command_buffer,
                    input_buffer,
                    input_offset,
                    k_view.weight_buffer,
                    v_view.weight_buffer,
                    k_output_buffer,
                    k_output_offset,
                    v_output_buffer,
                    v_output_offset,
                    rows,
                    in_dim,
	                    kv_out_dim,
	                    -16) != 0) return -16;
	        }
	        }

	        if (!frame_owned) return 0;
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -17;
    }
}

int termite_metal_decode_runtime_apply_quantized_linear_slot_scratch_device(
    termite_metal_decode_runtime *runtime,
    uint32_t format,
    size_t slot,
    void *input_handle,
    size_t input_offset,
    size_t rows,
    size_t in_dim,
    size_t out_dim,
    void **output_handle
) {
    if (runtime == NULL || input_handle == NULL || output_handle == NULL) return -1;
    *output_handle = NULL;
    if (slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -3;
    termite_metal_quant_linear_slot_view view;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, format, slot, in_dim, out_dim, &view) != 0) return -4;
    if (rows == 0 || rows > UINT32_MAX || in_dim == 0 || out_dim == 0 || in_dim > UINT32_MAX || out_dim > UINT32_MAX) return -6;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        const size_t input_bytes = rows * in_dim * sizeof(float);
        const size_t output_bytes = rows * out_dim * sizeof(float);
        if (input_offset + input_bytes > input_buffer.length) return -7;
        if (termite_metal_decode_runtime_ensure_active_layer_projection_buffer(runtime, 0, output_bytes) != 0) return -8;
        id<MTLBuffer> output_buffer = runtime->active_layer_projection_buffers[0];
        bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -9;
        if (format == TERMITE_METAL_QUANT_FORMAT_TL1 || format == TERMITE_METAL_QUANT_FORMAT_TL2) {
            const int linear_rc = termite_metal_decode_runtime_apply_quantized_linear_slot_device(
                runtime,
                format,
                slot,
                input_handle,
                input_offset,
                rows,
                in_dim,
                out_dim,
                (__bridge void *)output_buffer,
                0);
            if (linear_rc != 0) return -10;
        } else {
            termite_metal_quant_matmul_descriptor descriptor = {
                .slot = slot,
                .format = format,
                .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
                .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_NONE,
                .fallback_pipeline = view.pipeline,
                .input_buffer = input_buffer,
                .input_offset = input_offset,
                .weight_buffer = view.weight_buffer,
                .output_buffer = output_buffer,
                .output_offset = 0,
                .rows = rows,
                .in_dim = in_dim,
                .out_dim = out_dim,
                .values_per_block = view.values_per_block,
                .bytes_per_block = view.bytes_per_block,
                .failure_code = -10,
            };
            if (termite_metal_encode_quant_matmul_descriptor(runtime, command_buffer, &descriptor) != 0) return -10;
        }
        const int rc = termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -11);
        if (rc != 0) return rc;
        *output_handle = (__bridge void *)output_buffer;
        return 0;
    }
}

int termite_metal_decode_runtime_apply_prefill_quantized_setup_device(
    termite_metal_decode_runtime *runtime,
    int shares_kv,
    uint32_t q_format,
    uint32_t k_format,
    uint32_t v_format,
    size_t q_slot,
    size_t k_slot,
    size_t v_slot,
    void *input_handle,
    size_t input_offset,
    size_t rows,
    size_t hidden_size,
    size_t attention_input_size,
    size_t kv_dim,
    size_t q_norm_slot,
    size_t k_norm_slot,
    int value_norm_present,
    void *value_norm_weight_handle,
    size_t value_norm_weight_offset,
    size_t num_heads,
    size_t num_kv_heads,
    size_t head_dim,
    size_t rope_dim,
    size_t position,
    float theta,
    float freq_scale,
    float eps,
    float query_value_scale,
    uint32_t consecutive_pairs,
    termite_metal_planned_layer_contract planned_contract,
    int keep_scope_open_after,
    void **q_ready_handle,
    void **k_ready_handle,
    void **v_ready_handle
) {
    if (runtime == NULL || input_handle == NULL || q_ready_handle == NULL || k_ready_handle == NULL || v_ready_handle == NULL) return -1;
    *q_ready_handle = NULL;
    *k_ready_handle = NULL;
    *v_ready_handle = NULL;
    if (rows == 0 || hidden_size == 0 || attention_input_size == 0 || num_heads == 0 || head_dim == 0 || rope_dim == 0) return -2;
    if (attention_input_size != num_heads * head_dim) return -3;
    if (!shares_kv && (kv_dim == 0 || num_kv_heads == 0 || kv_dim != num_kv_heads * head_dim)) return -4;
    if (rows > UINT32_MAX || hidden_size > UINT32_MAX || attention_input_size > UINT32_MAX || kv_dim > UINT32_MAX || num_heads > UINT32_MAX || num_kv_heads > UINT32_MAX || head_dim > UINT32_MAX || rope_dim > UINT32_MAX || position > UINT32_MAX) return -5;
    if (runtime->active_frame_cb == nil) return -7;

    termite_metal_quant_linear_slot_view q_view;
    termite_metal_quant_linear_slot_view k_view;
    termite_metal_quant_linear_slot_view v_view;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, q_format, q_slot, hidden_size, attention_input_size, &q_view) != 0) return -8;
    if (!shares_kv) {
        if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, k_format, k_slot, hidden_size, kv_dim, &k_view) != 0) return -9;
        if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, v_format, v_slot, hidden_size, kv_dim, &v_view) != 0) return -10;
    }
    if (value_norm_present != 0 && (value_norm_weight_handle == NULL || runtime->rms_norm_rows_pipeline == nil)) return -11;

    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLBuffer> value_norm_weight_buffer = value_norm_weight_handle != NULL ? (__bridge id<MTLBuffer>)value_norm_weight_handle : nil;
        const size_t input_bytes = rows * hidden_size * sizeof(float);
        const size_t q_bytes = rows * attention_input_size * sizeof(float);
        const size_t kv_bytes = rows * kv_dim * sizeof(float);
        if (input_offset + input_bytes > input_buffer.length) return -12;
        if (value_norm_present != 0 && value_norm_weight_offset + head_dim * sizeof(float) > value_norm_weight_buffer.length) return -13;
        if (termite_metal_decode_runtime_ensure_active_layer_projection_buffer(runtime, 0, q_bytes) != 0) return -14;
        if (termite_metal_decode_runtime_ensure_active_layer_projection_buffer(runtime, 3, q_bytes) != 0) return -15;
        if (!shares_kv) {
            if (termite_metal_decode_runtime_ensure_active_layer_projection_buffer(runtime, 1, kv_bytes) != 0) return -16;
            if (termite_metal_decode_runtime_ensure_active_layer_projection_buffer(runtime, 2, kv_bytes) != 0) return -17;
            if (termite_metal_decode_runtime_ensure_active_layer_projection_buffer(runtime, 4, kv_bytes) != 0) return -18;
            if (termite_metal_decode_runtime_ensure_active_layer_projection_buffer(runtime, 5, kv_bytes) != 0) return -19;
        }
        id<MTLCommandBuffer> command_buffer = runtime->active_frame_cb;
        id<MTLBuffer> q_projected_buffer = runtime->active_layer_projection_buffers[0];
        id<MTLBuffer> k_projected_buffer = shares_kv ? nil : runtime->active_layer_projection_buffers[1];
        id<MTLBuffer> v_projected_buffer = shares_kv ? nil : runtime->active_layer_projection_buffers[2];
        id<MTLBuffer> q_ready_buffer = runtime->active_layer_projection_buffers[3];
        id<MTLBuffer> k_ready_buffer = shares_kv ? nil : runtime->active_layer_projection_buffers[4];
        id<MTLBuffer> v_ready_buffer = shares_kv ? nil : (value_norm_present != 0 ? runtime->active_layer_projection_buffers[5] : v_projected_buffer);
        if (command_buffer == nil || q_projected_buffer == nil || q_ready_buffer == nil) return -20;
        if (!shares_kv && (k_projected_buffer == nil || v_projected_buffer == nil || k_ready_buffer == nil || v_ready_buffer == nil)) return -21;
        const BOOL reusing_planned_encoder = (runtime->active_planned_compute_encoder != nil);
        if (!reusing_planned_encoder && termite_metal_decode_runtime_begin_planned_compute_scope(runtime, TERMITE_METAL_COMPUTE_SOURCE_LAYER, TERMITE_METAL_COMPUTE_REGION_LAYER) != 0) return -22;
        id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
        termite_metal_planned_layer_cursor planned_cursor = termite_metal_planned_layer_cursor_make(runtime, planned_contract, -23);
        int rc = 0;
        if (encoder == nil) rc = -22;

        if (rc == 0) rc = termite_metal_planned_layer_cursor_before_op(&planned_cursor, TERMITE_METAL_PLAN_OP_DECODE_Q_OR_QKV_LINEAR);
        const uint8_t qkv_planned_dispatch = termite_metal_planned_layer_cursor_quant_dispatch(&planned_cursor);
        if (rc == 0) {
            if (q_format == TERMITE_METAL_QUANT_FORMAT_TL1 || q_format == TERMITE_METAL_QUANT_FORMAT_TL2) {
                rc = termite_metal_decode_runtime_apply_quantized_linear_slot_device(runtime, q_format, q_slot, input_handle, input_offset, rows, hidden_size, attention_input_size, (__bridge void *)q_projected_buffer, 0);
                if (rc != 0) rc = -24;
            } else {
                termite_metal_quant_matmul_descriptor descriptor = {
                    .slot = q_slot,
                    .format = q_format,
                    .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
                    .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_NONE,
                    .fallback_pipeline = q_view.pipeline,
                    .input_buffer = input_buffer,
                    .input_offset = input_offset,
                    .weight_buffer = q_view.weight_buffer,
                    .output_buffer = q_projected_buffer,
                    .rows = rows,
                    .in_dim = hidden_size,
                    .out_dim = attention_input_size,
                    .values_per_block = q_view.values_per_block,
                    .bytes_per_block = q_view.bytes_per_block,
                    .q8_family = TERMITE_METAL_Q8_0_LINEAR_FAMILY_QKV,
                    .use_planned_dispatch = true,
                    .planned_dispatch = qkv_planned_dispatch,
                    .failure_code = -24,
                };
                rc = termite_metal_encode_quant_matmul_descriptor_on_encoder(runtime, encoder, &descriptor);
            }
        }
        if (rc == 0 && !shares_kv) {
            if (k_format == TERMITE_METAL_QUANT_FORMAT_TL1 || k_format == TERMITE_METAL_QUANT_FORMAT_TL2) {
                rc = termite_metal_decode_runtime_apply_quantized_linear_slot_device(runtime, k_format, k_slot, input_handle, input_offset, rows, hidden_size, kv_dim, (__bridge void *)k_projected_buffer, 0);
                if (rc != 0) rc = -25;
            } else {
                termite_metal_quant_matmul_descriptor descriptor = {
                    .slot = k_slot,
                    .format = k_format,
                    .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
                    .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_NONE,
                    .fallback_pipeline = k_view.pipeline,
                    .input_buffer = input_buffer,
                    .input_offset = input_offset,
                    .weight_buffer = k_view.weight_buffer,
                    .output_buffer = k_projected_buffer,
                    .rows = rows,
                    .in_dim = hidden_size,
                    .out_dim = kv_dim,
                    .values_per_block = k_view.values_per_block,
                    .bytes_per_block = k_view.bytes_per_block,
                    .q8_family = TERMITE_METAL_Q8_0_LINEAR_FAMILY_QKV,
                    .use_planned_dispatch = true,
                    .planned_dispatch = qkv_planned_dispatch,
                    .failure_code = -25,
                };
                rc = termite_metal_encode_quant_matmul_descriptor_on_encoder(runtime, encoder, &descriptor);
            }
        }
        if (rc == 0 && !shares_kv) {
            if (v_format == TERMITE_METAL_QUANT_FORMAT_TL1 || v_format == TERMITE_METAL_QUANT_FORMAT_TL2) {
                rc = termite_metal_decode_runtime_apply_quantized_linear_slot_device(runtime, v_format, v_slot, input_handle, input_offset, rows, hidden_size, kv_dim, (__bridge void *)v_projected_buffer, 0);
                if (rc != 0) rc = -26;
            } else {
                termite_metal_quant_matmul_descriptor descriptor = {
                    .slot = v_slot,
                    .format = v_format,
                    .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
                    .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_NONE,
                    .fallback_pipeline = v_view.pipeline,
                    .input_buffer = input_buffer,
                    .input_offset = input_offset,
                    .weight_buffer = v_view.weight_buffer,
                    .output_buffer = v_projected_buffer,
                    .rows = rows,
                    .in_dim = hidden_size,
                    .out_dim = kv_dim,
                    .values_per_block = v_view.values_per_block,
                    .bytes_per_block = v_view.bytes_per_block,
                    .q8_family = TERMITE_METAL_Q8_0_LINEAR_FAMILY_QKV,
                    .use_planned_dispatch = true,
                    .planned_dispatch = qkv_planned_dispatch,
                    .failure_code = -26,
                };
                rc = termite_metal_encode_quant_matmul_descriptor_on_encoder(runtime, encoder, &descriptor);
            }
        }
        if (rc == 0) rc = termite_metal_planned_layer_cursor_fallback_barrier_after_op(&planned_cursor);

        if (rc == 0) rc = termite_metal_planned_layer_cursor_before_op(&planned_cursor, TERMITE_METAL_PLAN_OP_DECODE_Q_HEAD_NORM_ROPE);
        if (rc == 0) {
            rc = termite_metal_encode_head_rms_rope_on_encoder(runtime, encoder, q_projected_buffer, 0, q_norm_slot, rows * num_heads, head_dim, rope_dim, position, theta, freq_scale, 0.0f, query_value_scale, consecutive_pairs, num_heads, 0, q_ready_buffer, 0, -27);
        }
        if (rc == 0) rc = termite_metal_planned_layer_cursor_fallback_barrier_after_op(&planned_cursor);

        if (rc == 0 && !shares_kv) rc = termite_metal_planned_layer_cursor_before_op(&planned_cursor, TERMITE_METAL_PLAN_OP_DECODE_K_HEAD_NORM_ROPE);
        if (rc == 0 && !shares_kv) {
            rc = termite_metal_encode_head_rms_rope_on_encoder(runtime, encoder, k_projected_buffer, 0, k_norm_slot, rows * num_kv_heads, head_dim, rope_dim, position, theta, freq_scale, 0.0f, 1.0f, consecutive_pairs, num_kv_heads, 0, k_ready_buffer, 0, -28);
        }
        if (rc == 0 && !shares_kv) rc = termite_metal_planned_layer_cursor_fallback_barrier_after_op(&planned_cursor);

        if (rc == 0 && !shares_kv && value_norm_present != 0) rc = termite_metal_planned_layer_cursor_before_op(&planned_cursor, TERMITE_METAL_PLAN_OP_DECODE_V_NORM);
        if (rc == 0 && !shares_kv && value_norm_present != 0) {
            const size_t value_norm_weight_bytes = head_dim * sizeof(float);
            if (termite_metal_decode_runtime_prepare_planned_compute_weight_norm_accesses(
                    runtime,
                    v_projected_buffer,
                    0,
                    kv_bytes,
                    value_norm_weight_buffer,
                    value_norm_weight_offset,
                    value_norm_weight_bytes,
                    v_ready_buffer,
                    0,
                    kv_bytes,
                    -28) != 0)
            {
                rc = -28;
            }
            termite_metal_apply_row_norm_params params = {
                .rows = (uint32_t)(rows * num_kv_heads),
                .hidden_size = (uint32_t)head_dim,
                .eps = eps,
            };
            if (rc == 0) {
                [encoder setComputePipelineState:runtime->rms_norm_rows_pipeline];
                [encoder setBuffer:v_projected_buffer offset:0 atIndex:0];
                [encoder setBuffer:value_norm_weight_buffer offset:value_norm_weight_offset atIndex:1];
                [encoder setBuffer:v_ready_buffer offset:0 atIndex:2];
                [encoder setBytes:&params length:sizeof(params) atIndex:3];
                [encoder dispatchThreads:MTLSizeMake(rows * num_kv_heads, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->rms_norm_rows_pipeline, rows * num_kv_heads), 1, 1)];
            }
        }
        const int end_rc = (rc != 0 || keep_scope_open_after == 0)
            ? termite_metal_decode_runtime_end_planned_compute_scope(runtime)
            : 0;
        if (rc != 0) return rc;
        if (end_rc != 0) return -29;
        *q_ready_handle = (__bridge void *)q_ready_buffer;
        if (!shares_kv) {
            *k_ready_handle = (__bridge void *)k_ready_buffer;
            *v_ready_handle = (__bridge void *)v_ready_buffer;
        }
        return 0;
    }
}

int termite_metal_decode_runtime_apply_quantized_linear_qkv_slots_scratch_device(
    termite_metal_decode_runtime *runtime,
    uint32_t q_format,
    uint32_t k_format,
    uint32_t v_format,
    size_t q_slot,
    size_t k_slot,
    size_t v_slot,
    void *input_handle,
    size_t input_offset,
    size_t rows,
    size_t in_dim,
    size_t q_out_dim,
    size_t kv_out_dim,
    void **q_output_handle,
    void **k_output_handle,
    void **v_output_handle
) {
    if (runtime == NULL || input_handle == NULL || q_output_handle == NULL || k_output_handle == NULL || v_output_handle == NULL) return -1;
    *q_output_handle = NULL;
    *k_output_handle = NULL;
    *v_output_handle = NULL;
    if (q_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || k_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || v_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -3;
    if (q_slot == k_slot || q_slot == v_slot || k_slot == v_slot) return -4;
    termite_metal_quant_linear_slot_view q_view;
    termite_metal_quant_linear_slot_view k_view;
    termite_metal_quant_linear_slot_view v_view;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, q_format, q_slot, in_dim, q_out_dim, &q_view) != 0) return -5;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, k_format, k_slot, in_dim, kv_out_dim, &k_view) != 0) return -6;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, v_format, v_slot, in_dim, kv_out_dim, &v_view) != 0) return -7;
    if (rows == 0 || rows > UINT32_MAX || in_dim == 0 || q_out_dim == 0 || kv_out_dim == 0 || in_dim > UINT32_MAX || q_out_dim > UINT32_MAX || kv_out_dim > UINT32_MAX) return -8;
    const BOOL all_q8_0 = q_format == TERMITE_METAL_QUANT_FORMAT_Q8_0 &&
        k_format == TERMITE_METAL_QUANT_FORMAT_Q8_0 &&
        v_format == TERMITE_METAL_QUANT_FORMAT_Q8_0;
    if (all_q8_0 && rows == 1 && runtime->q8_0_qkv_mmv_pipeline == nil) return -2;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        const size_t input_bytes = rows * in_dim * sizeof(float);
        const size_t q_output_bytes = rows * q_out_dim * sizeof(float);
        const size_t kv_output_bytes = rows * kv_out_dim * sizeof(float);
        if (input_offset + input_bytes > input_buffer.length) return -10;
        if (termite_metal_decode_runtime_ensure_active_layer_projection_buffer(runtime, 0, q_output_bytes) != 0) return -11;
        if (termite_metal_decode_runtime_ensure_active_layer_projection_buffer(runtime, 1, kv_output_bytes) != 0) return -12;
        if (termite_metal_decode_runtime_ensure_active_layer_projection_buffer(runtime, 2, kv_output_bytes) != 0) return -13;
        id<MTLBuffer> q_output_buffer = runtime->active_layer_projection_buffers[0];
        id<MTLBuffer> k_output_buffer = runtime->active_layer_projection_buffers[1];
        id<MTLBuffer> v_output_buffer = runtime->active_layer_projection_buffers[2];
        const size_t total_out_dim = q_out_dim + kv_out_dim + kv_out_dim;
        const size_t packed_output_bytes = rows * total_out_dim * sizeof(float);
        bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -14;
        if (all_q8_0 && rows == 1) {
            if (termite_metal_encode_q8_0_qkv_linear(
                    runtime,
                    command_buffer,
                    input_buffer,
                    input_offset,
                    q_view.weight_buffer,
                    k_view.weight_buffer,
                    v_view.weight_buffer,
                    q_output_buffer,
                    0,
                    k_output_buffer,
                    0,
                    v_output_buffer,
                    0,
                    rows,
                    in_dim,
                    q_out_dim,
                    kv_out_dim,
                    -15) != 0) return -15;
        } else if (all_q8_0) {
            const BOOL can_pack_large_qkv =
                rows >= 9u &&
                runtime->q8_0_mm_pipeline != nil &&
                runtime->dense_qkv_split_pipeline != nil &&
                total_out_dim <= UINT32_MAX &&
                termite_metal_decode_runtime_ensure_active_layer_projection_buffer(runtime, 3, packed_output_bytes) == 0 &&
                termite_metal_decode_runtime_ensure_q8_qkv_packed_weight(
                    runtime,
                    command_buffer,
                    q_slot,
                    k_slot,
                    v_slot,
                    in_dim,
                    q_out_dim,
                    kv_out_dim,
                    &q_view,
                    &k_view,
                    &v_view) == 0;
            if (can_pack_large_qkv) {
                id<MTLBuffer> packed_output_buffer = runtime->active_layer_projection_buffers[3];
                if (packed_output_buffer == nil) return -15;
                if (termite_metal_encode_q8_0_linear(
                        runtime,
                        command_buffer,
                        input_buffer,
                        input_offset,
                        runtime->q8_qkv_packed_weight_buffers[q_slot],
                        packed_output_buffer,
                        0,
                        rows,
                        in_dim,
                        total_out_dim,
                        -15) != 0) return -15;

                termite_metal_dense_qkv_split_bias_params split_params = {
                    .rows = (uint32_t)rows,
                    .q_out_dim = (uint32_t)q_out_dim,
                    .kv_out_dim = (uint32_t)kv_out_dim,
                    .total_out_dim = (uint32_t)total_out_dim,
                };
                termite_metal_planned_encoder_range split_accesses[4];
                if (termite_metal_planned_range_make(packed_output_buffer, 0, packed_output_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &split_accesses[0], -16) != 0 ||
                    termite_metal_planned_range_make(q_output_buffer, 0, q_output_bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &split_accesses[1], -16) != 0 ||
                    termite_metal_planned_range_make(k_output_buffer, 0, kv_output_bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &split_accesses[2], -16) != 0 ||
                    termite_metal_planned_range_make(v_output_buffer, 0, kv_output_bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &split_accesses[3], -16) != 0 ||
                    termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, split_accesses, 4, -16) != 0)
                {
                    return -16;
                }
                BOOL split_encoder_owned = YES;
                id<MTLComputeCommandEncoder> split_encoder = termite_metal_scoped_compute_encoder_for(runtime, command_buffer, TERMITE_METAL_COMPUTE_SOURCE_QUANT_QKV, &split_encoder_owned);
                if (split_encoder == nil) return -16;
                [split_encoder setComputePipelineState:runtime->dense_qkv_split_pipeline];
                [split_encoder setBuffer:packed_output_buffer offset:0 atIndex:0];
                [split_encoder setBuffer:q_output_buffer offset:0 atIndex:1];
                [split_encoder setBuffer:k_output_buffer offset:0 atIndex:2];
                [split_encoder setBuffer:v_output_buffer offset:0 atIndex:3];
                [split_encoder setBytes:&split_params length:sizeof(split_params) atIndex:4];
                [split_encoder dispatchThreads:MTLSizeMake(rows * total_out_dim, 1, 1)
                       threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->dense_qkv_split_pipeline, rows * total_out_dim), 1, 1)];
                termite_metal_end_scoped_compute_encoder(split_encoder, split_encoder_owned);
            } else {
                if (termite_metal_encode_q8_0_linear(
                        runtime,
                        command_buffer,
                        input_buffer,
                        input_offset,
                        q_view.weight_buffer,
                        q_output_buffer,
                        0,
                        rows,
                        in_dim,
                        q_out_dim,
                        -15) != 0) return -15;
                if (rows >= 9u && runtime->q8_0_mm_pipeline != nil) {
                    if (termite_metal_encode_q8_0_linear(
                            runtime,
                            command_buffer,
                            input_buffer,
                            input_offset,
                            k_view.weight_buffer,
                            k_output_buffer,
                            0,
                            rows,
                            in_dim,
                            kv_out_dim,
                            -16) != 0) return -16;
                    if (termite_metal_encode_q8_0_linear(
                            runtime,
                            command_buffer,
                            input_buffer,
                            input_offset,
                            v_view.weight_buffer,
                            v_output_buffer,
                            0,
                            rows,
                            in_dim,
                            kv_out_dim,
                            -16) != 0) return -16;
                } else {
                    if (termite_metal_encode_q8_0_pair_linear(
                            runtime,
                            command_buffer,
                            input_buffer,
                            input_offset,
                            k_view.weight_buffer,
                            v_view.weight_buffer,
                            k_output_buffer,
                            0,
                            v_output_buffer,
                            0,
                            rows,
                            in_dim,
                            kv_out_dim,
                            -16) != 0) return -16;
                }
            }
        } else {
            const BOOL any_tl = q_format == TERMITE_METAL_QUANT_FORMAT_TL1 ||
                q_format == TERMITE_METAL_QUANT_FORMAT_TL2 ||
                k_format == TERMITE_METAL_QUANT_FORMAT_TL1 ||
                k_format == TERMITE_METAL_QUANT_FORMAT_TL2 ||
                v_format == TERMITE_METAL_QUANT_FORMAT_TL1 ||
                v_format == TERMITE_METAL_QUANT_FORMAT_TL2;
            const BOOL can_pair_kv = !any_tl && k_format == v_format &&
                termite_metal_quant_pair_pipeline_for_format(runtime, k_format) != nil;
            if (any_tl) {
                if (termite_metal_decode_runtime_apply_quantized_linear_slot_device(runtime, q_format, q_slot, input_handle, input_offset, rows, in_dim, q_out_dim, (__bridge void *)q_output_buffer, 0) != 0) return -15;
                if (termite_metal_decode_runtime_apply_quantized_linear_slot_device(runtime, k_format, k_slot, input_handle, input_offset, rows, in_dim, kv_out_dim, (__bridge void *)k_output_buffer, 0) != 0) return -16;
                if (termite_metal_decode_runtime_apply_quantized_linear_slot_device(runtime, v_format, v_slot, input_handle, input_offset, rows, in_dim, kv_out_dim, (__bridge void *)v_output_buffer, 0) != 0) return -16;
            } else if (can_pair_kv) {
                const int encode_rc = termite_metal_encode_quant_q_plus_kv_pair_descriptors(
                    runtime,
                    command_buffer,
                    q_format,
                    k_format,
                    q_slot,
                    k_slot,
                    input_buffer,
                    input_offset,
                    rows,
                    in_dim,
                    q_out_dim,
                    kv_out_dim,
                    &q_view,
                    &k_view,
                    &v_view,
                    termite_metal_quant_pair_pipeline_for_format(runtime, k_format),
                    q_output_buffer,
                    0,
                    k_output_buffer,
                    0,
                    v_output_buffer,
                    0,
                    -15,
                    -16);
                if (encode_rc != 0) return encode_rc;
            } else {
                termite_metal_quant_matmul_descriptor q_descriptor = {
                    .slot = q_slot,
                    .format = q_format,
                    .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
                    .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_NONE,
                    .fallback_pipeline = q_view.pipeline,
                    .input_buffer = input_buffer,
                    .input_offset = input_offset,
                    .weight_buffer = q_view.weight_buffer,
                    .output_buffer = q_output_buffer,
                    .output_offset = 0,
                    .rows = rows,
                    .in_dim = in_dim,
                    .out_dim = q_out_dim,
                    .values_per_block = q_view.values_per_block,
                    .bytes_per_block = q_view.bytes_per_block,
                    .failure_code = -15,
                };
                if (termite_metal_encode_quant_matmul_descriptor(runtime, command_buffer, &q_descriptor) != 0) return -15;
                termite_metal_quant_matmul_descriptor k_descriptor = {
                    .slot = k_slot,
                    .format = k_format,
                    .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
                    .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_NONE,
                    .fallback_pipeline = k_view.pipeline,
                    .input_buffer = input_buffer,
                    .input_offset = input_offset,
                    .weight_buffer = k_view.weight_buffer,
                    .output_buffer = k_output_buffer,
                    .output_offset = 0,
                    .rows = rows,
                    .in_dim = in_dim,
                    .out_dim = kv_out_dim,
                    .values_per_block = k_view.values_per_block,
                    .bytes_per_block = k_view.bytes_per_block,
                    .failure_code = -16,
                };
                if (termite_metal_encode_quant_matmul_descriptor(runtime, command_buffer, &k_descriptor) != 0) return -16;
                termite_metal_quant_matmul_descriptor v_descriptor = {
                    .slot = v_slot,
                    .format = v_format,
                    .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
                    .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_NONE,
                    .fallback_pipeline = v_view.pipeline,
                    .input_buffer = input_buffer,
                    .input_offset = input_offset,
                    .weight_buffer = v_view.weight_buffer,
                    .output_buffer = v_output_buffer,
                    .output_offset = 0,
                    .rows = rows,
                    .in_dim = in_dim,
                    .out_dim = kv_out_dim,
                    .values_per_block = v_view.values_per_block,
                    .bytes_per_block = v_view.bytes_per_block,
                    .failure_code = -16,
                };
                if (termite_metal_encode_quant_matmul_descriptor(runtime, command_buffer, &v_descriptor) != 0) return -16;
            }
        }
        const int rc = termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -17);
        if (rc != 0) return rc;
        *q_output_handle = (__bridge void *)q_output_buffer;
        *k_output_handle = (__bridge void *)k_output_buffer;
        *v_output_handle = (__bridge void *)v_output_buffer;
        return 0;
    }
}

int termite_metal_decode_runtime_apply_quantized_linear_pair_slots(
    termite_metal_decode_runtime *runtime,
    uint32_t format,
    size_t slot_a,
    size_t slot_b,
    const float *input,
    size_t rows,
    size_t in_dim,
    size_t out_dim,
    float *output_a,
    float *output_b
) {
    if (runtime == NULL || input == NULL || output_a == NULL || output_b == NULL) return -1;
    if (slot_a >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || slot_b >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || slot_a == slot_b) return -3;
    termite_metal_quant_linear_slot_view view_a;
    termite_metal_quant_linear_slot_view view_b;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, format, slot_a, in_dim, out_dim, &view_a) != 0) return -4;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, format, slot_b, in_dim, out_dim, &view_b) != 0) return -5;
    if (view_a.values_per_block != view_b.values_per_block || view_a.bytes_per_block != view_b.bytes_per_block) return -6;
    if (rows == 0 || rows > UINT32_MAX || in_dim > UINT32_MAX || out_dim > UINT32_MAX) return -7;
    @autoreleasepool {
        const size_t input_bytes = rows * in_dim * sizeof(float);
        const size_t output_bytes = rows * out_dim * sizeof(float);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)input length:input_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:input length:input_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> output_a_buffer = [runtime->device newBufferWithBytesNoCopy:output_a length:output_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_a_buffer == nil) output_a_buffer = [runtime->device newBufferWithBytes:output_a length:output_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> output_b_buffer = [runtime->device newBufferWithBytesNoCopy:output_b length:output_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_b_buffer == nil) output_b_buffer = [runtime->device newBufferWithBytes:output_b length:output_bytes options:MTLResourceStorageModeShared];
        if (input_buffer == nil || output_a_buffer == nil || output_b_buffer == nil) return -8;
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -9;
        termite_metal_quant_matmul_descriptor descriptor = {
            .slot = slot_a,
            .format = format,
            .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
            .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_PAIR,
            .fallback_pipeline = nil,
            .input_buffer = input_buffer,
            .input_offset = 0,
            .weight_buffer = view_a.weight_buffer,
            .second_weight_buffer = view_b.weight_buffer,
            .output_buffer = output_a_buffer,
            .output_offset = 0,
            .second_output_buffer = output_b_buffer,
            .second_output_offset = 0,
            .rows = rows,
            .in_dim = in_dim,
            .out_dim = out_dim,
            .values_per_block = view_a.values_per_block,
            .bytes_per_block = view_a.bytes_per_block,
            .failure_code = -10,
        };
        if (termite_metal_encode_quant_matmul_descriptor(runtime, command_buffer, &descriptor) != 0) return -10;
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -11;
    }
}

int termite_metal_decode_runtime_apply_quantized_linear_pair_slots_device(
    termite_metal_decode_runtime *runtime,
    uint32_t format,
    size_t slot_a,
    size_t slot_b,
    void *input_handle,
    size_t input_offset,
    size_t rows,
    size_t in_dim,
    size_t out_dim,
    void *output_a_handle,
    size_t output_a_offset,
    void *output_b_handle,
    size_t output_b_offset
) {
    if (runtime == NULL || input_handle == NULL || output_a_handle == NULL || output_b_handle == NULL) return -1;
    if (slot_a >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || slot_b >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || slot_a == slot_b) return -3;
    termite_metal_quant_linear_slot_view view_a;
    termite_metal_quant_linear_slot_view view_b;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, format, slot_a, in_dim, out_dim, &view_a) != 0) return -4;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, format, slot_b, in_dim, out_dim, &view_b) != 0) return -5;
    if (view_a.values_per_block != view_b.values_per_block || view_a.bytes_per_block != view_b.bytes_per_block) return -6;
    if (rows == 0 || rows > UINT32_MAX || in_dim > UINT32_MAX || out_dim > UINT32_MAX) return -7;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLBuffer> output_a_buffer = (__bridge id<MTLBuffer>)output_a_handle;
        id<MTLBuffer> output_b_buffer = (__bridge id<MTLBuffer>)output_b_handle;
        const size_t input_bytes = rows * in_dim * sizeof(float);
        const size_t output_bytes = rows * out_dim * sizeof(float);
        if (input_offset + input_bytes > input_buffer.length) return -8;
        if (output_a_offset + output_bytes > output_a_buffer.length) return -9;
        if (output_b_offset + output_bytes > output_b_buffer.length) return -10;
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -11;
        termite_metal_quant_matmul_descriptor descriptor = {
            .slot = slot_a,
            .format = format,
            .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
            .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_PAIR,
            .fallback_pipeline = nil,
            .input_buffer = input_buffer,
            .input_offset = input_offset,
            .weight_buffer = view_a.weight_buffer,
            .second_weight_buffer = view_b.weight_buffer,
            .output_buffer = output_a_buffer,
            .output_offset = output_a_offset,
            .second_output_buffer = output_b_buffer,
            .second_output_offset = output_b_offset,
            .rows = rows,
            .in_dim = in_dim,
            .out_dim = out_dim,
            .values_per_block = view_a.values_per_block,
            .bytes_per_block = view_a.bytes_per_block,
            .failure_code = -12,
        };
        if (termite_metal_encode_quant_matmul_descriptor(runtime, command_buffer, &descriptor) != 0) return -12;
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -13);
    }
}

int termite_metal_decode_runtime_apply_linear_bias_device(
    termite_metal_decode_runtime *runtime,
    size_t slot,
    void *output_handle,
    size_t output_offset,
    size_t rows,
    size_t out_dim
) {
    if (runtime == NULL || output_handle == NULL) return -1;
    if (runtime->linear_bias_pipeline == nil) return -2;
    if (slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || rows == 0 || out_dim == 0) return -3;
    if (runtime->linear_bias_buffers[slot] == nil) return -4;
    if (rows > UINT32_MAX || out_dim > UINT32_MAX) return -5;
    @autoreleasepool {
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t output_bytes = rows * out_dim * sizeof(float);
        if (output_offset + output_bytes > output_buffer.length) return -6;
        termite_metal_linear_params params = {
            .rows = (uint32_t)rows,
            .in_dim = (uint32_t)out_dim,
            .out_dim = (uint32_t)out_dim,
            .row_blocks = 0u,
        };
        const bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLCommandBuffer> command_buffer = frame_owned
            ? termite_metal_new_command_buffer(runtime->queue, __func__)
            : runtime->active_frame_cb;
        if (command_buffer == nil) return -7;
        id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
        const BOOL planned_encoder = (encoder != nil);
        if (!planned_encoder) {
            encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_DENSE_LINEAR);
        }
        if (encoder == nil) return -8;
        [encoder setComputePipelineState:runtime->linear_bias_pipeline];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:0];
        [encoder setBuffer:runtime->linear_bias_buffers[slot] offset:0 atIndex:1];
        [encoder setBytes:&params length:sizeof(params) atIndex:2];
        const size_t total = rows * out_dim;
        [encoder dispatchThreads:MTLSizeMake(total, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->linear_bias_pipeline, total), 1, 1)];
        if (!planned_encoder) [encoder endEncoding];
        if (!frame_owned) return 0;
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -9;
    }
}

int termite_metal_decode_runtime_apply_linear_pair_slots(
    termite_metal_decode_runtime *runtime,
    size_t slot_a,
    size_t slot_b,
    const float *input,
    size_t rows,
    size_t in_dim,
    size_t out_dim,
    float *output_a,
    float *output_b
) {
    if (runtime == NULL || input == NULL || output_a == NULL || output_b == NULL) return -1;
    if (runtime->linear_pipeline == nil) return -2;
    if (slot_a >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || slot_b >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -3;
    if (slot_a == slot_b) return -4;
    if (runtime->linear_slot_prepared[slot_a] == 0 || runtime->linear_slot_prepared[slot_b] == 0) return -5;
    if (runtime->linear_in_dims[slot_a] != in_dim || runtime->linear_in_dims[slot_b] != in_dim) return -6;
    if (runtime->linear_out_dims[slot_a] != out_dim || runtime->linear_out_dims[slot_b] != out_dim) return -7;
    if (rows == 0 || rows > UINT32_MAX || in_dim > UINT32_MAX || out_dim > UINT32_MAX) return -8;
    @autoreleasepool {
        const size_t input_bytes = rows * in_dim * sizeof(float);
        const size_t output_bytes = rows * out_dim * sizeof(float);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)input length:input_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:input length:input_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> output_a_buffer = [runtime->device newBufferWithBytesNoCopy:output_a length:output_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_a_buffer == nil) output_a_buffer = [runtime->device newBufferWithBytes:output_a length:output_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> output_b_buffer = [runtime->device newBufferWithBytesNoCopy:output_b length:output_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_b_buffer == nil) output_b_buffer = [runtime->device newBufferWithBytes:output_b length:output_bytes options:MTLResourceStorageModeShared];
        if (input_buffer == nil || output_a_buffer == nil || output_b_buffer == nil) return -9;

        termite_metal_apply_linear_params params = {
            .in_dim = (uint32_t)in_dim,
            .out_dim = (uint32_t)out_dim,
        };

        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -10;

        id<MTLComputeCommandEncoder> encoder_a = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (encoder_a == nil) return -11;
        [encoder_a setComputePipelineState:runtime->linear_pipeline];
        [encoder_a setBuffer:input_buffer offset:0 atIndex:0];
        [encoder_a setBuffer:runtime->linear_weight_buffers[slot_a] offset:0 atIndex:1];
        [encoder_a setBuffer:runtime->linear_bias_buffers[slot_a] offset:0 atIndex:2];
        [encoder_a setBuffer:output_a_buffer offset:0 atIndex:3];
        [encoder_a setBytes:&params length:sizeof(params) atIndex:4];
        const size_t total = rows * out_dim;
        NSUInteger thread_width = runtime->linear_pipeline.maxTotalThreadsPerThreadgroup;
        if (thread_width == 0) thread_width = 64;
        if (thread_width > total && total > 0) thread_width = total;
        [encoder_a dispatchThreads:MTLSizeMake(total, 1, 1) threadsPerThreadgroup:MTLSizeMake(thread_width, 1, 1)];
        [encoder_a endEncoding];

        id<MTLComputeCommandEncoder> encoder_b = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (encoder_b == nil) return -12;
        [encoder_b setComputePipelineState:runtime->linear_pipeline];
        [encoder_b setBuffer:input_buffer offset:0 atIndex:0];
        [encoder_b setBuffer:runtime->linear_weight_buffers[slot_b] offset:0 atIndex:1];
        [encoder_b setBuffer:runtime->linear_bias_buffers[slot_b] offset:0 atIndex:2];
        [encoder_b setBuffer:output_b_buffer offset:0 atIndex:3];
        [encoder_b setBytes:&params length:sizeof(params) atIndex:4];
        [encoder_b dispatchThreads:MTLSizeMake(total, 1, 1) threadsPerThreadgroup:MTLSizeMake(thread_width, 1, 1)];
        [encoder_b endEncoding];

        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -13;
    }
}

int termite_metal_decode_runtime_apply_linear_pair_slots_device(
    termite_metal_decode_runtime *runtime,
    size_t slot_a,
    size_t slot_b,
    void *input_handle,
    size_t input_offset,
    size_t rows,
    size_t in_dim,
    size_t out_dim,
    void *output_a_handle,
    size_t output_a_offset,
    void *output_b_handle,
    size_t output_b_offset
) {
    if (runtime == NULL || input_handle == NULL || output_a_handle == NULL || output_b_handle == NULL) return -1;
    if (runtime->linear_pipeline == nil) return -2;
    if (slot_a >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || slot_b >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -3;
    if (slot_a == slot_b) return -4;
    if (runtime->linear_slot_prepared[slot_a] == 0 || runtime->linear_slot_prepared[slot_b] == 0) return -5;
    if (runtime->linear_in_dims[slot_a] != in_dim || runtime->linear_in_dims[slot_b] != in_dim) return -6;
    if (runtime->linear_out_dims[slot_a] != out_dim || runtime->linear_out_dims[slot_b] != out_dim) return -7;
    if (rows == 0 || rows > UINT32_MAX || in_dim > UINT32_MAX || out_dim > UINT32_MAX) return -8;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLBuffer> output_a_buffer = (__bridge id<MTLBuffer>)output_a_handle;
        id<MTLBuffer> output_b_buffer = (__bridge id<MTLBuffer>)output_b_handle;
        const size_t input_bytes = rows * in_dim * sizeof(float);
        const size_t output_bytes = rows * out_dim * sizeof(float);
        if (input_offset + input_bytes > input_buffer.length) return -9;
        if (output_a_offset + output_bytes > output_a_buffer.length) return -10;
        if (output_b_offset + output_bytes > output_b_buffer.length) return -11;

        termite_metal_apply_linear_params params = {
            .in_dim = (uint32_t)in_dim,
            .out_dim = (uint32_t)out_dim,
        };

        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -12;

        id<MTLComputeCommandEncoder> encoder_a = runtime->active_planned_compute_encoder;
        const BOOL planned_encoder = (encoder_a != nil);
        if (!planned_encoder) {
            encoder_a = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_DENSE_LINEAR);
        }
        if (encoder_a == nil) return -13;
        if (termite_metal_decode_runtime_prepare_planned_dense_linear_accesses(
                runtime,
                input_buffer,
                input_offset,
                input_bytes,
                runtime->linear_weight_buffers[slot_a],
                runtime->linear_bias_buffers[slot_a],
                output_a_buffer,
                output_a_offset,
                output_bytes,
                -13) != 0) return -13;
        [encoder_a setComputePipelineState:runtime->linear_pipeline];
        [encoder_a setBuffer:input_buffer offset:input_offset atIndex:0];
        [encoder_a setBuffer:runtime->linear_weight_buffers[slot_a] offset:0 atIndex:1];
        [encoder_a setBuffer:runtime->linear_bias_buffers[slot_a] offset:0 atIndex:2];
        [encoder_a setBuffer:output_a_buffer offset:output_a_offset atIndex:3];
        [encoder_a setBytes:&params length:sizeof(params) atIndex:4];
        const size_t total = rows * out_dim;
        NSUInteger thread_width = runtime->linear_pipeline.maxTotalThreadsPerThreadgroup;
        if (thread_width == 0) thread_width = 64;
        if (thread_width > total && total > 0) thread_width = total;
        [encoder_a dispatchThreads:MTLSizeMake(total, 1, 1) threadsPerThreadgroup:MTLSizeMake(thread_width, 1, 1)];
        if (!planned_encoder) [encoder_a endEncoding];

        id<MTLComputeCommandEncoder> encoder_b = planned_encoder
            ? encoder_a
            : termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_DENSE_LINEAR);
        if (encoder_b == nil) return -14;
        if (termite_metal_decode_runtime_prepare_planned_dense_linear_accesses(
                runtime,
                input_buffer,
                input_offset,
                input_bytes,
                runtime->linear_weight_buffers[slot_b],
                runtime->linear_bias_buffers[slot_b],
                output_b_buffer,
                output_b_offset,
                output_bytes,
                -14) != 0) return -14;
        [encoder_b setComputePipelineState:runtime->linear_pipeline];
        [encoder_b setBuffer:input_buffer offset:input_offset atIndex:0];
        [encoder_b setBuffer:runtime->linear_weight_buffers[slot_b] offset:0 atIndex:1];
        [encoder_b setBuffer:runtime->linear_bias_buffers[slot_b] offset:0 atIndex:2];
        [encoder_b setBuffer:output_b_buffer offset:output_b_offset atIndex:3];
        [encoder_b setBytes:&params length:sizeof(params) atIndex:4];
        [encoder_b dispatchThreads:MTLSizeMake(total, 1, 1) threadsPerThreadgroup:MTLSizeMake(thread_width, 1, 1)];
        if (!planned_encoder) [encoder_b endEncoding];

        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -15);
    }
}

int termite_metal_decode_runtime_apply_linear_qkv_slots(
    termite_metal_decode_runtime *runtime,
    size_t q_slot,
    size_t k_slot,
    size_t v_slot,
    const float *input,
    size_t rows,
    size_t in_dim,
    size_t q_out_dim,
    size_t kv_out_dim,
    float *q_output,
    float *k_output,
    float *v_output
) {
    if (runtime == NULL || input == NULL || q_output == NULL || k_output == NULL || v_output == NULL) return -1;
    if (runtime->linear_pipeline == nil || runtime->linear_reduce_pipeline == nil) return -2;
    if (q_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || k_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || v_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -3;
    if (q_slot == k_slot || q_slot == v_slot || k_slot == v_slot) return -4;
    if (runtime->linear_slot_prepared[q_slot] == 0 || runtime->linear_slot_prepared[k_slot] == 0 || runtime->linear_slot_prepared[v_slot] == 0) return -5;
    if (runtime->linear_in_dims[q_slot] != in_dim || runtime->linear_in_dims[k_slot] != in_dim || runtime->linear_in_dims[v_slot] != in_dim) return -6;
    if (runtime->linear_out_dims[q_slot] != q_out_dim || runtime->linear_out_dims[k_slot] != kv_out_dim || runtime->linear_out_dims[v_slot] != kv_out_dim) return -7;
    if (rows != 1 || in_dim == 0 || q_out_dim == 0 || kv_out_dim == 0 || in_dim > UINT32_MAX || q_out_dim > UINT32_MAX || kv_out_dim > UINT32_MAX) return -8;
    @autoreleasepool {
        const size_t input_bytes = in_dim * sizeof(float);
        const size_t q_output_bytes = q_out_dim * sizeof(float);
        const size_t kv_output_bytes = kv_out_dim * sizeof(float);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)input length:input_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:input length:input_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> q_output_buffer = [runtime->device newBufferWithBytesNoCopy:q_output length:q_output_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (q_output_buffer == nil) q_output_buffer = [runtime->device newBufferWithBytes:q_output length:q_output_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> k_output_buffer = [runtime->device newBufferWithBytesNoCopy:k_output length:kv_output_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (k_output_buffer == nil) k_output_buffer = [runtime->device newBufferWithBytes:k_output length:kv_output_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> v_output_buffer = [runtime->device newBufferWithBytesNoCopy:v_output length:kv_output_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (v_output_buffer == nil) v_output_buffer = [runtime->device newBufferWithBytes:v_output length:kv_output_bytes options:MTLResourceStorageModeShared];
        if (input_buffer == nil || q_output_buffer == nil || k_output_buffer == nil || v_output_buffer == nil) return -9;

        termite_metal_apply_linear_params q_params = {
            .in_dim = (uint32_t)in_dim,
            .out_dim = (uint32_t)q_out_dim,
        };
        termite_metal_apply_linear_params kv_params = {
            .in_dim = (uint32_t)in_dim,
            .out_dim = (uint32_t)kv_out_dim,
        };

        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -10;

        id<MTLComputeCommandEncoder> q_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (q_encoder == nil) return -11;
        const BOOL use_q_reduce = q_out_dim >= 128;
        [q_encoder setComputePipelineState:(use_q_reduce ? runtime->linear_reduce_pipeline : runtime->linear_pipeline)];
        [q_encoder setBuffer:input_buffer offset:0 atIndex:0];
        [q_encoder setBuffer:runtime->linear_weight_buffers[q_slot] offset:0 atIndex:1];
        [q_encoder setBuffer:runtime->linear_bias_buffers[q_slot] offset:0 atIndex:2];
        [q_encoder setBuffer:q_output_buffer offset:0 atIndex:3];
        [q_encoder setBytes:&q_params length:sizeof(q_params) atIndex:4];
        if (use_q_reduce) {
            [q_encoder dispatchThreads:MTLSizeMake(q_out_dim * 32u, 1, 1) threadsPerThreadgroup:MTLSizeMake(32, 1, 1)];
        } else {
            [q_encoder dispatchThreads:MTLSizeMake(q_out_dim, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->linear_pipeline, q_out_dim), 1, 1)];
        }
        [q_encoder endEncoding];

        id<MTLComputeCommandEncoder> k_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (k_encoder == nil) return -12;
        const BOOL use_kv_reduce = kv_out_dim >= 128;
        [k_encoder setComputePipelineState:(use_kv_reduce ? runtime->linear_reduce_pipeline : runtime->linear_pipeline)];
        [k_encoder setBuffer:input_buffer offset:0 atIndex:0];
        [k_encoder setBuffer:runtime->linear_weight_buffers[k_slot] offset:0 atIndex:1];
        [k_encoder setBuffer:runtime->linear_bias_buffers[k_slot] offset:0 atIndex:2];
        [k_encoder setBuffer:k_output_buffer offset:0 atIndex:3];
        [k_encoder setBytes:&kv_params length:sizeof(kv_params) atIndex:4];
        if (use_kv_reduce) {
            [k_encoder dispatchThreads:MTLSizeMake(kv_out_dim * 32u, 1, 1) threadsPerThreadgroup:MTLSizeMake(32, 1, 1)];
        } else {
            [k_encoder dispatchThreads:MTLSizeMake(kv_out_dim, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->linear_pipeline, kv_out_dim), 1, 1)];
        }
        [k_encoder endEncoding];

        id<MTLComputeCommandEncoder> v_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (v_encoder == nil) return -13;
        [v_encoder setComputePipelineState:(use_kv_reduce ? runtime->linear_reduce_pipeline : runtime->linear_pipeline)];
        [v_encoder setBuffer:input_buffer offset:0 atIndex:0];
        [v_encoder setBuffer:runtime->linear_weight_buffers[v_slot] offset:0 atIndex:1];
        [v_encoder setBuffer:runtime->linear_bias_buffers[v_slot] offset:0 atIndex:2];
        [v_encoder setBuffer:v_output_buffer offset:0 atIndex:3];
        [v_encoder setBytes:&kv_params length:sizeof(kv_params) atIndex:4];
        if (use_kv_reduce) {
            [v_encoder dispatchThreads:MTLSizeMake(kv_out_dim * 32u, 1, 1) threadsPerThreadgroup:MTLSizeMake(32, 1, 1)];
        } else {
            [v_encoder dispatchThreads:MTLSizeMake(kv_out_dim, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->linear_pipeline, kv_out_dim), 1, 1)];
        }
        [v_encoder endEncoding];

        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -14;
    }
}

int termite_metal_decode_runtime_apply_linear_qkv_slots_device(
    termite_metal_decode_runtime *runtime,
    size_t q_slot,
    size_t k_slot,
    size_t v_slot,
    void *input_handle,
    size_t input_offset,
    size_t rows,
    size_t in_dim,
    size_t q_out_dim,
    size_t kv_out_dim,
    void *q_output_handle,
    size_t q_output_offset,
    void *k_output_handle,
    size_t k_output_offset,
    void *v_output_handle,
    size_t v_output_offset
) {
    if (runtime == NULL || input_handle == NULL || q_output_handle == NULL || k_output_handle == NULL || v_output_handle == NULL) return -1;
    if (runtime->linear_pipeline == nil || runtime->linear_reduce_pipeline == nil) return -2;
    if (q_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || k_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || v_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -3;
    if (q_slot == k_slot || q_slot == v_slot || k_slot == v_slot) return -4;
    if (runtime->linear_slot_prepared[q_slot] == 0 || runtime->linear_slot_prepared[k_slot] == 0 || runtime->linear_slot_prepared[v_slot] == 0) return -5;
    if (runtime->linear_in_dims[q_slot] != in_dim || runtime->linear_in_dims[k_slot] != in_dim || runtime->linear_in_dims[v_slot] != in_dim) return -6;
    if (runtime->linear_out_dims[q_slot] != q_out_dim || runtime->linear_out_dims[k_slot] != kv_out_dim || runtime->linear_out_dims[v_slot] != kv_out_dim) return -7;
    if (rows != 1 || in_dim == 0 || q_out_dim == 0 || kv_out_dim == 0 || in_dim > UINT32_MAX || q_out_dim > UINT32_MAX || kv_out_dim > UINT32_MAX) return -8;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLBuffer> q_output_buffer = (__bridge id<MTLBuffer>)q_output_handle;
        id<MTLBuffer> k_output_buffer = (__bridge id<MTLBuffer>)k_output_handle;
        id<MTLBuffer> v_output_buffer = (__bridge id<MTLBuffer>)v_output_handle;
        const size_t input_bytes = in_dim * sizeof(float);
        const size_t q_output_bytes = q_out_dim * sizeof(float);
        const size_t kv_output_bytes = kv_out_dim * sizeof(float);
        if (input_offset + input_bytes > input_buffer.length) return -9;
        if (q_output_offset + q_output_bytes > q_output_buffer.length) return -10;
        if (k_output_offset + kv_output_bytes > k_output_buffer.length) return -11;
        if (v_output_offset + kv_output_bytes > v_output_buffer.length) return -12;

        termite_metal_apply_linear_params q_params = {
            .in_dim = (uint32_t)in_dim,
            .out_dim = (uint32_t)q_out_dim,
        };
        termite_metal_apply_linear_params kv_params = {
            .in_dim = (uint32_t)in_dim,
            .out_dim = (uint32_t)kv_out_dim,
        };

        const bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLCommandBuffer> command_buffer = frame_owned
            ? termite_metal_new_command_buffer(runtime->queue, __func__)
            : runtime->active_frame_cb;
        if (command_buffer == nil) return -13;

        id<MTLComputeCommandEncoder> q_encoder = runtime->active_planned_compute_encoder;
        const BOOL planned_encoder = (q_encoder != nil);
        if (!planned_encoder) {
            q_encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_DENSE_LINEAR);
        }
        if (q_encoder == nil) return -14;
        const BOOL use_q_reduce = q_out_dim >= 128;
        if (termite_metal_decode_runtime_prepare_planned_dense_linear_accesses(
                runtime,
                input_buffer,
                input_offset,
                input_bytes,
                runtime->linear_weight_buffers[q_slot],
                runtime->linear_bias_buffers[q_slot],
                q_output_buffer,
                q_output_offset,
                q_output_bytes,
                -14) != 0) return -14;
        [q_encoder setComputePipelineState:(use_q_reduce ? runtime->linear_reduce_pipeline : runtime->linear_pipeline)];
        [q_encoder setBuffer:input_buffer offset:input_offset atIndex:0];
        [q_encoder setBuffer:runtime->linear_weight_buffers[q_slot] offset:0 atIndex:1];
        [q_encoder setBuffer:runtime->linear_bias_buffers[q_slot] offset:0 atIndex:2];
        [q_encoder setBuffer:q_output_buffer offset:q_output_offset atIndex:3];
        [q_encoder setBytes:&q_params length:sizeof(q_params) atIndex:4];
        if (use_q_reduce) {
            [q_encoder dispatchThreads:MTLSizeMake(q_out_dim * 32u, 1, 1) threadsPerThreadgroup:MTLSizeMake(32, 1, 1)];
        } else {
            [q_encoder dispatchThreads:MTLSizeMake(q_out_dim, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->linear_pipeline, q_out_dim), 1, 1)];
        }
        if (!planned_encoder) [q_encoder endEncoding];

        id<MTLComputeCommandEncoder> k_encoder = planned_encoder
            ? q_encoder
            : termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_DENSE_LINEAR);
        if (k_encoder == nil) return -15;
        const BOOL use_kv_reduce = kv_out_dim >= 128;
        if (termite_metal_decode_runtime_prepare_planned_dense_linear_accesses(
                runtime,
                input_buffer,
                input_offset,
                input_bytes,
                runtime->linear_weight_buffers[k_slot],
                runtime->linear_bias_buffers[k_slot],
                k_output_buffer,
                k_output_offset,
                kv_output_bytes,
                -15) != 0) return -15;
        [k_encoder setComputePipelineState:(use_kv_reduce ? runtime->linear_reduce_pipeline : runtime->linear_pipeline)];
        [k_encoder setBuffer:input_buffer offset:input_offset atIndex:0];
        [k_encoder setBuffer:runtime->linear_weight_buffers[k_slot] offset:0 atIndex:1];
        [k_encoder setBuffer:runtime->linear_bias_buffers[k_slot] offset:0 atIndex:2];
        [k_encoder setBuffer:k_output_buffer offset:k_output_offset atIndex:3];
        [k_encoder setBytes:&kv_params length:sizeof(kv_params) atIndex:4];
        if (use_kv_reduce) {
            [k_encoder dispatchThreads:MTLSizeMake(kv_out_dim * 32u, 1, 1) threadsPerThreadgroup:MTLSizeMake(32, 1, 1)];
        } else {
            [k_encoder dispatchThreads:MTLSizeMake(kv_out_dim, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->linear_pipeline, kv_out_dim), 1, 1)];
        }
        if (!planned_encoder) [k_encoder endEncoding];

        id<MTLComputeCommandEncoder> v_encoder = planned_encoder
            ? q_encoder
            : termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_DENSE_LINEAR);
        if (v_encoder == nil) return -16;
        if (termite_metal_decode_runtime_prepare_planned_dense_linear_accesses(
                runtime,
                input_buffer,
                input_offset,
                input_bytes,
                runtime->linear_weight_buffers[v_slot],
                runtime->linear_bias_buffers[v_slot],
                v_output_buffer,
                v_output_offset,
                kv_output_bytes,
                -16) != 0) return -16;
        [v_encoder setComputePipelineState:(use_kv_reduce ? runtime->linear_reduce_pipeline : runtime->linear_pipeline)];
        [v_encoder setBuffer:input_buffer offset:input_offset atIndex:0];
        [v_encoder setBuffer:runtime->linear_weight_buffers[v_slot] offset:0 atIndex:1];
        [v_encoder setBuffer:runtime->linear_bias_buffers[v_slot] offset:0 atIndex:2];
        [v_encoder setBuffer:v_output_buffer offset:v_output_offset atIndex:3];
        [v_encoder setBytes:&kv_params length:sizeof(kv_params) atIndex:4];
        if (use_kv_reduce) {
            [v_encoder dispatchThreads:MTLSizeMake(kv_out_dim * 32u, 1, 1) threadsPerThreadgroup:MTLSizeMake(32, 1, 1)];
        } else {
            [v_encoder dispatchThreads:MTLSizeMake(kv_out_dim, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->linear_pipeline, kv_out_dim), 1, 1)];
        }
        if (!planned_encoder) [v_encoder endEncoding];

        if (!frame_owned) return 0;
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -17;
    }
}

int termite_metal_decode_runtime_apply_dense_linear_qkv_slots_scratch_device(
    termite_metal_decode_runtime *runtime,
    size_t q_slot,
    size_t k_slot,
    size_t v_slot,
    void *input_handle,
    size_t input_offset,
    size_t rows,
    size_t in_dim,
    size_t q_out_dim,
    size_t kv_out_dim,
    void **q_output_handle,
    void **k_output_handle,
    void **v_output_handle
) {
    if (runtime == NULL || input_handle == NULL || q_output_handle == NULL || k_output_handle == NULL || v_output_handle == NULL) return -1;
    *q_output_handle = NULL;
    *k_output_handle = NULL;
    *v_output_handle = NULL;
    if (runtime->active_frame_cb == nil) return -2;
    if (runtime->dense_qkv_split_bias_pipeline == nil) return -2;
    const bool direct_packed_dense = termite_metal_direct_packed_dense_enabled();
    if (runtime->active_planned_compute_encoder != nil && !direct_packed_dense) return -2;
    if (q_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || k_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || v_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -3;
    if (q_slot == k_slot || q_slot == v_slot || k_slot == v_slot) return -4;
    if (runtime->linear_slot_prepared[q_slot] == 0 || runtime->linear_slot_prepared[k_slot] == 0 || runtime->linear_slot_prepared[v_slot] == 0) return -5;
    if (runtime->linear_weight_dtypes[q_slot] != TERMITE_METAL_DENSE_LINEAR_DTYPE_F32 ||
        runtime->linear_weight_dtypes[k_slot] != TERMITE_METAL_DENSE_LINEAR_DTYPE_F32 ||
        runtime->linear_weight_dtypes[v_slot] != TERMITE_METAL_DENSE_LINEAR_DTYPE_F32)
    {
        return -6;
    }
    if (runtime->linear_in_dims[q_slot] != in_dim || runtime->linear_in_dims[k_slot] != in_dim || runtime->linear_in_dims[v_slot] != in_dim) return -7;
    if (runtime->linear_out_dims[q_slot] != q_out_dim || runtime->linear_out_dims[k_slot] != kv_out_dim || runtime->linear_out_dims[v_slot] != kv_out_dim) return -8;
    if (rows == 0 || in_dim == 0 || q_out_dim == 0 || kv_out_dim == 0 || rows > UINT32_MAX || in_dim > UINT32_MAX || q_out_dim > UINT32_MAX || kv_out_dim > UINT32_MAX) return -9;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLCommandBuffer> command_buffer = runtime->active_frame_cb;
        if (command_buffer == nil) return -10;
        const size_t input_bytes = rows * in_dim * sizeof(float);
        const size_t q_output_bytes = rows * q_out_dim * sizeof(float);
        const size_t kv_output_bytes = rows * kv_out_dim * sizeof(float);
        const size_t total_out_dim = q_out_dim + kv_out_dim + kv_out_dim;
        const size_t packed_output_bytes = rows * total_out_dim * sizeof(float);
        if (input_offset + input_bytes > input_buffer.length) return -10;
        if (termite_metal_decode_runtime_ensure_active_layer_projection_buffer(runtime, 0, q_output_bytes) != 0) return -11;
        if (termite_metal_decode_runtime_ensure_active_layer_projection_buffer(runtime, 1, kv_output_bytes) != 0) return -12;
        if (termite_metal_decode_runtime_ensure_active_layer_projection_buffer(runtime, 2, kv_output_bytes) != 0) return -13;
        if (termite_metal_decode_runtime_ensure_active_layer_projection_buffer(runtime, 3, packed_output_bytes) != 0) return -14;
        id<MTLBuffer> q_output_buffer = runtime->active_layer_projection_buffers[0];
        id<MTLBuffer> k_output_buffer = runtime->active_layer_projection_buffers[1];
        id<MTLBuffer> v_output_buffer = runtime->active_layer_projection_buffers[2];
        id<MTLBuffer> packed_output_buffer = runtime->active_layer_projection_buffers[3];
        if (q_output_buffer == nil || k_output_buffer == nil || v_output_buffer == nil || packed_output_buffer == nil) return -14;
        if (termite_metal_decode_runtime_retain_frame_resource(runtime, input_buffer) != 0) return -14;
        if (direct_packed_dense) {
            if (runtime->linear_multi_row_pipeline == nil ||
                runtime->linear_multi_row_reduce_pipeline == nil ||
                runtime->linear_multi_row_tiled_f32_pipeline == nil ||
                runtime->dense_qkv_split_pipeline == nil) return -15;
            if (termite_metal_decode_runtime_ensure_dense_qkv_packed_weight(
                    runtime,
                    command_buffer,
                    q_slot,
                    k_slot,
                    v_slot,
                    in_dim,
                    q_out_dim,
                    kv_out_dim) != 0) return -15;
	            termite_metal_linear_params linear_params = {
	                .rows = (uint32_t)rows,
	                .in_dim = (uint32_t)in_dim,
	                .out_dim = (uint32_t)total_out_dim,
	                .row_blocks = 0u,
		            };
				            const BOOL use_tiled32_m16 = rows >= 128u && in_dim >= 128u && total_out_dim >= 128u && runtime->linear_multi_row_tiled32_m16_f32_pipeline != nil;
				            const BOOL use_tiled32 = rows >= 128u && in_dim >= 128u && total_out_dim >= 128u && runtime->linear_multi_row_tiled32_f32_pipeline != nil;
				            const BOOL use_tiled = rows >= 128u && in_dim >= 128u && total_out_dim >= 128u && runtime->linear_multi_row_tiled_f32_pipeline != nil;
				            id<MTLComputePipelineState> direct_linear_pipeline = use_tiled32_m16 ? runtime->linear_multi_row_tiled32_m16_f32_pipeline : (use_tiled32 ? runtime->linear_multi_row_tiled32_f32_pipeline : (use_tiled ? runtime->linear_multi_row_tiled_f32_pipeline : runtime->linear_multi_row_reduce_pipeline));
            if (termite_metal_decode_runtime_prepare_planned_dense_linear_accesses(
                    runtime,
                    input_buffer,
                    input_offset,
                    input_bytes,
                    runtime->dense_qkv_packed_weight_buffers[q_slot],
                    runtime->dense_qkv_packed_bias_buffers[q_slot],
                    packed_output_buffer,
                    0,
                    packed_output_bytes,
                    -16) != 0) return -16;
            BOOL linear_encoder_owned = YES;
            id<MTLComputeCommandEncoder> linear_encoder = termite_metal_scoped_compute_encoder_for(runtime, command_buffer, TERMITE_METAL_COMPUTE_SOURCE_DENSE_LINEAR, &linear_encoder_owned);
            if (linear_encoder == nil) return -16;
            termite_metal_maybe_trace_dense_linear_dispatch(
                "dense_qkv_packed",
                q_slot,
                rows,
                in_dim,
                total_out_dim,
                termite_metal_dense_linear_variant(false, false, use_tiled32_m16, use_tiled32, use_tiled, true, false),
                true,
                !linear_encoder_owned,
                true,
                false,
                false);
            [linear_encoder setComputePipelineState:direct_linear_pipeline];
            [linear_encoder setBuffer:input_buffer offset:input_offset atIndex:0];
            [linear_encoder setBuffer:runtime->dense_qkv_packed_weight_buffers[q_slot] offset:0 atIndex:1];
            [linear_encoder setBuffer:runtime->dense_qkv_packed_bias_buffers[q_slot] offset:0 atIndex:2];
            [linear_encoder setBuffer:packed_output_buffer offset:0 atIndex:3];
            [linear_encoder setBytes:&linear_params length:sizeof(linear_params) atIndex:4];
	            if (use_tiled32_m16) {
	                [linear_encoder dispatchThreadgroups:MTLSizeMake((total_out_dim + 31u) / 32u, (rows + 15u) / 16u, 1) threadsPerThreadgroup:MTLSizeMake(8u, 16u, 1)];
	            } else if (use_tiled32) {
	                [linear_encoder dispatchThreadgroups:MTLSizeMake((total_out_dim + 31u) / 32u, (rows + 7u) / 8u, 1) threadsPerThreadgroup:MTLSizeMake(8u, 8u, 1)];
            } else if (use_tiled) {
                [linear_encoder dispatchThreadgroups:MTLSizeMake((total_out_dim + 15u) / 16u, (rows + 7u) / 8u, 1) threadsPerThreadgroup:MTLSizeMake(8u, 8u, 1)];
            } else {
                [linear_encoder setThreadgroupMemoryLength:32u * sizeof(float) atIndex:0];
                [linear_encoder dispatchThreadgroups:MTLSizeMake(total_out_dim, rows, 1) threadsPerThreadgroup:MTLSizeMake(32u, 1, 1)];
            }
            termite_metal_end_scoped_compute_encoder(linear_encoder, linear_encoder_owned);

            termite_metal_dense_qkv_split_bias_params split_params = {
                .rows = (uint32_t)rows,
                .q_out_dim = (uint32_t)q_out_dim,
                .kv_out_dim = (uint32_t)kv_out_dim,
                .total_out_dim = (uint32_t)total_out_dim,
            };
            termite_metal_planned_encoder_range split_accesses[4];
            if (termite_metal_planned_range_make(packed_output_buffer, 0, packed_output_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &split_accesses[0], -17) != 0 ||
                termite_metal_planned_range_make(q_output_buffer, 0, q_output_bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &split_accesses[1], -17) != 0 ||
                termite_metal_planned_range_make(k_output_buffer, 0, kv_output_bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &split_accesses[2], -17) != 0 ||
                termite_metal_planned_range_make(v_output_buffer, 0, kv_output_bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &split_accesses[3], -17) != 0 ||
                termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, split_accesses, 4, -17) != 0)
            {
                return -17;
            }
            BOOL split_encoder_owned = YES;
            id<MTLComputeCommandEncoder> split_encoder = termite_metal_scoped_compute_encoder_for(runtime, command_buffer, TERMITE_METAL_COMPUTE_SOURCE_DENSE_LINEAR, &split_encoder_owned);
            if (split_encoder == nil) return -17;
            [split_encoder setComputePipelineState:runtime->dense_qkv_split_pipeline];
            [split_encoder setBuffer:packed_output_buffer offset:0 atIndex:0];
            [split_encoder setBuffer:q_output_buffer offset:0 atIndex:1];
            [split_encoder setBuffer:k_output_buffer offset:0 atIndex:2];
            [split_encoder setBuffer:v_output_buffer offset:0 atIndex:3];
            [split_encoder setBytes:&split_params length:sizeof(split_params) atIndex:4];
            [split_encoder dispatchThreads:MTLSizeMake(rows * total_out_dim, 1, 1)
                   threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->dense_qkv_split_pipeline, rows * total_out_dim), 1, 1)];
            termite_metal_end_scoped_compute_encoder(split_encoder, split_encoder_owned);

            *q_output_handle = (__bridge void *)q_output_buffer;
            *k_output_handle = (__bridge void *)k_output_buffer;
            *v_output_handle = (__bridge void *)v_output_buffer;
            runtime->dense_qkv_packed_calls += 1;
            return 0;
        }
        if (termite_metal_decode_runtime_ensure_dense_qkv_packed_weight(
                runtime,
                command_buffer,
                q_slot,
                k_slot,
                v_slot,
                in_dim,
                q_out_dim,
                kv_out_dim) != 0) return -15;

        MPSMatrixDescriptor *left_desc = [MPSMatrixDescriptor matrixDescriptorWithRows:rows
                                                                               columns:in_dim
                                                                              rowBytes:in_dim * sizeof(float)
                                                                              dataType:MPSDataTypeFloat32];
        MPSMatrixDescriptor *right_desc = [MPSMatrixDescriptor matrixDescriptorWithRows:total_out_dim
                                                                                columns:in_dim
                                                                               rowBytes:in_dim * sizeof(float)
                                                                               dataType:MPSDataTypeFloat32];
        MPSMatrixDescriptor *result_desc = [MPSMatrixDescriptor matrixDescriptorWithRows:rows
                                                                                 columns:total_out_dim
                                                                                rowBytes:total_out_dim * sizeof(float)
                                                                                dataType:MPSDataTypeFloat32];
        MPSMatrix *left = [[MPSMatrix alloc] initWithBuffer:input_buffer offset:input_offset descriptor:left_desc];
        MPSMatrix *right = [[MPSMatrix alloc] initWithBuffer:runtime->dense_qkv_packed_weight_buffers[q_slot] offset:0 descriptor:right_desc];
        MPSMatrix *result = [[MPSMatrix alloc] initWithBuffer:packed_output_buffer offset:0 descriptor:result_desc];
	        MPSMatrixMultiplication *mm = termite_metal_decode_runtime_cached_dense_qkv_mps_mm(runtime, q_slot, rows, in_dim, total_out_dim);
	        if (left == nil || right == nil || result == nil || mm == nil) return -16;
	        [mm encodeToCommandBuffer:command_buffer leftMatrix:left rightMatrix:right resultMatrix:result];
	        termite_metal_maybe_trace_dense_linear_dispatch(
	            "dense_qkv_packed",
	            q_slot,
	            rows,
	            in_dim,
	            total_out_dim,
	            "mps_matrix",
	            true,
	            false,
	            true,
	            false,
	            true);

        termite_metal_dense_qkv_split_bias_params params = {
            .rows = (uint32_t)rows,
            .q_out_dim = (uint32_t)q_out_dim,
            .kv_out_dim = (uint32_t)kv_out_dim,
            .total_out_dim = (uint32_t)total_out_dim,
        };
        id<MTLComputeCommandEncoder> split_encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_DENSE_LINEAR);
        if (split_encoder == nil) return -17;
        [split_encoder setComputePipelineState:runtime->dense_qkv_split_bias_pipeline];
        [split_encoder setBuffer:packed_output_buffer offset:0 atIndex:0];
        [split_encoder setBuffer:runtime->linear_bias_buffers[q_slot] offset:0 atIndex:1];
        [split_encoder setBuffer:runtime->linear_bias_buffers[k_slot] offset:0 atIndex:2];
        [split_encoder setBuffer:runtime->linear_bias_buffers[v_slot] offset:0 atIndex:3];
        [split_encoder setBuffer:q_output_buffer offset:0 atIndex:4];
        [split_encoder setBuffer:k_output_buffer offset:0 atIndex:5];
        [split_encoder setBuffer:v_output_buffer offset:0 atIndex:6];
        [split_encoder setBytes:&params length:sizeof(params) atIndex:7];
        [split_encoder dispatchThreads:MTLSizeMake(rows * total_out_dim, 1, 1)
                   threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->dense_qkv_split_bias_pipeline, rows * total_out_dim), 1, 1)];
        [split_encoder endEncoding];
        runtime->mps_dense_linear_active_frame_calls += 1;
        runtime->active_frame_mps_dense_linear_count += 1;

        *q_output_handle = (__bridge void *)q_output_buffer;
        *k_output_handle = (__bridge void *)k_output_buffer;
        *v_output_handle = (__bridge void *)v_output_buffer;
        runtime->dense_qkv_packed_calls += 1;
        return 0;
    }
}

int termite_metal_decode_runtime_apply_dense_linear_pair_slots_scratch_device(
    termite_metal_decode_runtime *runtime,
    size_t a_slot,
    size_t b_slot,
    void *input_handle,
    size_t input_offset,
    size_t rows,
    size_t in_dim,
    size_t out_dim,
    void **a_output_handle,
    void **b_output_handle
) {
    if (runtime == NULL || input_handle == NULL || a_output_handle == NULL || b_output_handle == NULL) return -1;
    *a_output_handle = NULL;
    *b_output_handle = NULL;
    if (runtime->active_frame_cb == nil) return -2;
    if (runtime->dense_pair_split_bias_pipeline == nil) return -2;
    const bool direct_packed_dense = termite_metal_direct_packed_dense_enabled();
    if (runtime->active_planned_compute_encoder != nil && !direct_packed_dense) return -2;
    if (a_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || b_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -3;
    if (a_slot == b_slot) return -4;
    if (runtime->linear_slot_prepared[a_slot] == 0 || runtime->linear_slot_prepared[b_slot] == 0) return -5;
    if (runtime->linear_weight_dtypes[a_slot] != TERMITE_METAL_DENSE_LINEAR_DTYPE_F32 ||
        runtime->linear_weight_dtypes[b_slot] != TERMITE_METAL_DENSE_LINEAR_DTYPE_F32)
    {
        return -6;
    }
    if (runtime->linear_in_dims[a_slot] != in_dim || runtime->linear_in_dims[b_slot] != in_dim) return -7;
    if (runtime->linear_out_dims[a_slot] != out_dim || runtime->linear_out_dims[b_slot] != out_dim) return -8;
    if (rows == 0 || in_dim == 0 || out_dim == 0 || rows > UINT32_MAX || in_dim > UINT32_MAX || out_dim > UINT32_MAX) return -9;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLCommandBuffer> command_buffer = runtime->active_frame_cb;
        if (command_buffer == nil) return -10;
        const size_t input_bytes = rows * in_dim * sizeof(float);
        const size_t output_bytes = rows * out_dim * sizeof(float);
        const size_t total_out_dim = out_dim * 2;
        const size_t packed_output_bytes = rows * total_out_dim * sizeof(float);
        if (input_offset + input_bytes > input_buffer.length) return -10;
        if (termite_metal_decode_runtime_ensure_active_layer_projection_buffer(runtime, 4, output_bytes) != 0) return -11;
        if (termite_metal_decode_runtime_ensure_active_layer_projection_buffer(runtime, 5, output_bytes) != 0) return -12;
        if (termite_metal_decode_runtime_ensure_active_layer_projection_buffer(runtime, 3, packed_output_bytes) != 0) return -13;
        id<MTLBuffer> a_output_buffer = runtime->active_layer_projection_buffers[4];
        id<MTLBuffer> b_output_buffer = runtime->active_layer_projection_buffers[5];
        id<MTLBuffer> packed_output_buffer = runtime->active_layer_projection_buffers[3];
        if (a_output_buffer == nil || b_output_buffer == nil || packed_output_buffer == nil) return -13;
	        if (termite_metal_decode_runtime_retain_frame_resource(runtime, input_buffer) != 0) return -13;
	        if (direct_packed_dense) {
	            if (runtime->linear_multi_row_pipeline == nil ||
	                runtime->linear_multi_row_reduce_pipeline == nil ||
	                runtime->linear_multi_row_tiled_f32_pipeline == nil ||
	                runtime->dense_pair_split_pipeline == nil) return -14;
	            if (termite_metal_decode_runtime_ensure_dense_pair_packed_weight(
	                    runtime,
	                    command_buffer,
                    a_slot,
                    b_slot,
                    in_dim,
                    out_dim) != 0) return -14;
            termite_metal_linear_params linear_params = {
                .rows = (uint32_t)rows,
                .in_dim = (uint32_t)in_dim,
                .out_dim = (uint32_t)total_out_dim,
                .row_blocks = 0u,
	            };
			            const BOOL use_tiled32_m16 = rows >= 128u && in_dim >= 128u && total_out_dim >= 128u && runtime->linear_multi_row_tiled32_m16_f32_pipeline != nil;
			            const BOOL use_tiled32 = rows >= 128u && in_dim >= 128u && total_out_dim >= 128u && runtime->linear_multi_row_tiled32_f32_pipeline != nil;
			            const BOOL use_tiled = rows >= 128u && in_dim >= 128u && total_out_dim >= 128u && runtime->linear_multi_row_tiled_f32_pipeline != nil;
			            id<MTLComputePipelineState> direct_linear_pipeline = use_tiled32_m16 ? runtime->linear_multi_row_tiled32_m16_f32_pipeline : (use_tiled32 ? runtime->linear_multi_row_tiled32_f32_pipeline : (use_tiled ? runtime->linear_multi_row_tiled_f32_pipeline : runtime->linear_multi_row_reduce_pipeline));
            if (termite_metal_decode_runtime_prepare_planned_dense_linear_accesses(
                    runtime,
                    input_buffer,
                    input_offset,
                    input_bytes,
                    runtime->dense_pair_packed_weight_buffers[a_slot],
                    runtime->dense_pair_packed_bias_buffers[a_slot],
                    packed_output_buffer,
                    0,
                    packed_output_bytes,
                    -15) != 0) return -15;
            BOOL linear_encoder_owned = YES;
            id<MTLComputeCommandEncoder> linear_encoder = termite_metal_scoped_compute_encoder_for(runtime, command_buffer, TERMITE_METAL_COMPUTE_SOURCE_DENSE_LINEAR, &linear_encoder_owned);
            if (linear_encoder == nil) return -15;
            termite_metal_maybe_trace_dense_linear_dispatch(
                "dense_pair_packed",
                a_slot,
                rows,
                in_dim,
                total_out_dim,
                termite_metal_dense_linear_variant(false, false, use_tiled32_m16, use_tiled32, use_tiled, true, false),
                true,
                !linear_encoder_owned,
                true,
                false,
                false);
            [linear_encoder setComputePipelineState:direct_linear_pipeline];
            [linear_encoder setBuffer:input_buffer offset:input_offset atIndex:0];
            [linear_encoder setBuffer:runtime->dense_pair_packed_weight_buffers[a_slot] offset:0 atIndex:1];
            [linear_encoder setBuffer:runtime->dense_pair_packed_bias_buffers[a_slot] offset:0 atIndex:2];
            [linear_encoder setBuffer:packed_output_buffer offset:0 atIndex:3];
            [linear_encoder setBytes:&linear_params length:sizeof(linear_params) atIndex:4];
	            if (use_tiled32_m16) {
	                [linear_encoder dispatchThreadgroups:MTLSizeMake((total_out_dim + 31u) / 32u, (rows + 15u) / 16u, 1) threadsPerThreadgroup:MTLSizeMake(8u, 16u, 1)];
	            } else if (use_tiled32) {
	                [linear_encoder dispatchThreadgroups:MTLSizeMake((total_out_dim + 31u) / 32u, (rows + 7u) / 8u, 1) threadsPerThreadgroup:MTLSizeMake(8u, 8u, 1)];
            } else if (use_tiled) {
                [linear_encoder dispatchThreadgroups:MTLSizeMake((total_out_dim + 15u) / 16u, (rows + 7u) / 8u, 1) threadsPerThreadgroup:MTLSizeMake(8u, 8u, 1)];
            } else {
                [linear_encoder setThreadgroupMemoryLength:32u * sizeof(float) atIndex:0];
                [linear_encoder dispatchThreadgroups:MTLSizeMake(total_out_dim, rows, 1) threadsPerThreadgroup:MTLSizeMake(32u, 1, 1)];
            }
            termite_metal_end_scoped_compute_encoder(linear_encoder, linear_encoder_owned);

            termite_metal_dense_pair_split_bias_params split_params = {
                .rows = (uint32_t)rows,
                .out_dim = (uint32_t)out_dim,
                .total_out_dim = (uint32_t)total_out_dim,
                .reserved = 0,
            };
            termite_metal_planned_encoder_range split_accesses[3];
            if (termite_metal_planned_range_make(packed_output_buffer, 0, packed_output_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &split_accesses[0], -16) != 0 ||
                termite_metal_planned_range_make(a_output_buffer, 0, output_bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &split_accesses[1], -16) != 0 ||
                termite_metal_planned_range_make(b_output_buffer, 0, output_bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &split_accesses[2], -16) != 0 ||
                termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, split_accesses, 3, -16) != 0)
            {
                return -16;
            }
            BOOL split_encoder_owned = YES;
            id<MTLComputeCommandEncoder> split_encoder = termite_metal_scoped_compute_encoder_for(runtime, command_buffer, TERMITE_METAL_COMPUTE_SOURCE_DENSE_LINEAR, &split_encoder_owned);
            if (split_encoder == nil) return -16;
            [split_encoder setComputePipelineState:runtime->dense_pair_split_pipeline];
            [split_encoder setBuffer:packed_output_buffer offset:0 atIndex:0];
            [split_encoder setBuffer:a_output_buffer offset:0 atIndex:1];
            [split_encoder setBuffer:b_output_buffer offset:0 atIndex:2];
            [split_encoder setBytes:&split_params length:sizeof(split_params) atIndex:3];
            [split_encoder dispatchThreads:MTLSizeMake(rows * total_out_dim, 1, 1)
                   threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->dense_pair_split_pipeline, rows * total_out_dim), 1, 1)];
            termite_metal_end_scoped_compute_encoder(split_encoder, split_encoder_owned);

            *a_output_handle = (__bridge void *)a_output_buffer;
            *b_output_handle = (__bridge void *)b_output_buffer;
            runtime->dense_pair_packed_calls += 1;
            return 0;
        }
        if (termite_metal_decode_runtime_ensure_dense_pair_packed_weight(
                runtime,
                command_buffer,
                a_slot,
                b_slot,
                in_dim,
                out_dim) != 0) return -14;

        MPSMatrixDescriptor *left_desc = [MPSMatrixDescriptor matrixDescriptorWithRows:rows
                                                                               columns:in_dim
                                                                              rowBytes:in_dim * sizeof(float)
                                                                              dataType:MPSDataTypeFloat32];
        MPSMatrixDescriptor *right_desc = [MPSMatrixDescriptor matrixDescriptorWithRows:total_out_dim
                                                                                columns:in_dim
                                                                               rowBytes:in_dim * sizeof(float)
                                                                               dataType:MPSDataTypeFloat32];
        MPSMatrixDescriptor *result_desc = [MPSMatrixDescriptor matrixDescriptorWithRows:rows
                                                                                 columns:total_out_dim
                                                                                rowBytes:total_out_dim * sizeof(float)
                                                                                dataType:MPSDataTypeFloat32];
        MPSMatrix *left = [[MPSMatrix alloc] initWithBuffer:input_buffer offset:input_offset descriptor:left_desc];
        MPSMatrix *right = [[MPSMatrix alloc] initWithBuffer:runtime->dense_pair_packed_weight_buffers[a_slot] offset:0 descriptor:right_desc];
        MPSMatrix *result = [[MPSMatrix alloc] initWithBuffer:packed_output_buffer offset:0 descriptor:result_desc];
        MPSMatrixMultiplication *mm = termite_metal_decode_runtime_cached_dense_pair_mps_mm(runtime, a_slot, rows, in_dim, total_out_dim);
        if (left == nil || right == nil || result == nil || mm == nil) return -15;
        [mm encodeToCommandBuffer:command_buffer leftMatrix:left rightMatrix:right resultMatrix:result];

        termite_metal_dense_pair_split_bias_params params = {
            .rows = (uint32_t)rows,
            .out_dim = (uint32_t)out_dim,
            .total_out_dim = (uint32_t)total_out_dim,
            .reserved = 0,
        };
        id<MTLComputeCommandEncoder> split_encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_DENSE_LINEAR);
        if (split_encoder == nil) return -16;
        [split_encoder setComputePipelineState:runtime->dense_pair_split_bias_pipeline];
        [split_encoder setBuffer:packed_output_buffer offset:0 atIndex:0];
        [split_encoder setBuffer:runtime->linear_bias_buffers[a_slot] offset:0 atIndex:1];
        [split_encoder setBuffer:runtime->linear_bias_buffers[b_slot] offset:0 atIndex:2];
        [split_encoder setBuffer:a_output_buffer offset:0 atIndex:3];
        [split_encoder setBuffer:b_output_buffer offset:0 atIndex:4];
        [split_encoder setBytes:&params length:sizeof(params) atIndex:5];
        [split_encoder dispatchThreads:MTLSizeMake(rows * total_out_dim, 1, 1)
                   threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->dense_pair_split_bias_pipeline, rows * total_out_dim), 1, 1)];
        [split_encoder endEncoding];
        runtime->mps_dense_linear_active_frame_calls += 1;
        runtime->active_frame_mps_dense_linear_count += 1;

        *a_output_handle = (__bridge void *)a_output_buffer;
        *b_output_handle = (__bridge void *)b_output_buffer;
        runtime->dense_pair_packed_calls += 1;
        return 0;
    }
}

static int termite_metal_encode_quant_q_plus_kv_pair_descriptors(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    uint32_t q_format,
    uint32_t kv_format,
    size_t q_slot,
    size_t k_slot,
    id<MTLBuffer> input_buffer,
    size_t input_offset,
    size_t rows,
    size_t in_dim,
    size_t q_out_dim,
    size_t kv_out_dim,
    const termite_metal_quant_linear_slot_view *q_view,
    const termite_metal_quant_linear_slot_view *k_view,
    const termite_metal_quant_linear_slot_view *v_view,
    id<MTLComputePipelineState> kv_pair_pipeline,
    id<MTLBuffer> q_output_buffer,
    size_t q_output_offset,
    id<MTLBuffer> k_output_buffer,
    size_t k_output_offset,
    id<MTLBuffer> v_output_buffer,
    size_t v_output_offset,
    int q_failure_code,
    int kv_failure_code
) {
    if (runtime == NULL || command_buffer == nil || input_buffer == nil ||
        q_view == NULL || k_view == NULL || v_view == NULL ||
        q_output_buffer == nil || k_output_buffer == nil || v_output_buffer == nil) return q_failure_code;
    if (k_view->values_per_block != v_view->values_per_block ||
        k_view->bytes_per_block != v_view->bytes_per_block) return kv_failure_code;

    termite_metal_quant_matmul_descriptor q_descriptor = {
        .slot = q_slot,
        .format = q_format,
        .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
        .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_NONE,
        .fallback_pipeline = q_view->pipeline,
        .input_buffer = input_buffer,
        .input_offset = input_offset,
        .weight_buffer = q_view->weight_buffer,
        .output_buffer = q_output_buffer,
        .output_offset = q_output_offset,
        .rows = rows,
        .in_dim = in_dim,
        .out_dim = q_out_dim,
        .values_per_block = q_view->values_per_block,
        .bytes_per_block = q_view->bytes_per_block,
        .failure_code = q_failure_code,
    };
    if (termite_metal_encode_quant_matmul_descriptor(runtime, command_buffer, &q_descriptor) != 0) return q_failure_code;

    termite_metal_quant_matmul_descriptor kv_descriptor = {
        .slot = k_slot,
        .format = kv_format,
        .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
        .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_PAIR,
        .fallback_pipeline = kv_pair_pipeline,
        .input_buffer = input_buffer,
        .input_offset = input_offset,
        .weight_buffer = k_view->weight_buffer,
        .second_weight_buffer = v_view->weight_buffer,
        .output_buffer = k_output_buffer,
        .output_offset = k_output_offset,
        .second_output_buffer = v_output_buffer,
        .second_output_offset = v_output_offset,
        .rows = rows,
        .in_dim = in_dim,
        .out_dim = kv_out_dim,
        .values_per_block = k_view->values_per_block,
        .bytes_per_block = k_view->bytes_per_block,
        .failure_code = kv_failure_code,
    };
    if (termite_metal_encode_quant_matmul_descriptor(runtime, command_buffer, &kv_descriptor) != 0) return kv_failure_code;
    return 0;
}

static id<MTLComputePipelineState> termite_metal_quant_pair_pipeline_for_format(
    termite_metal_decode_runtime *runtime,
    uint32_t format
) {
    if (runtime == NULL) return nil;
    switch (format) {
        case TERMITE_METAL_QUANT_FORMAT_I2_S:
            return runtime->i2_s_pair_pipeline;
        case TERMITE_METAL_QUANT_FORMAT_Q4_0:
            return runtime->q4_0_pair_pipeline;
        case TERMITE_METAL_QUANT_FORMAT_Q4_K:
            return runtime->q4_k_pair_pipeline;
        case TERMITE_METAL_QUANT_FORMAT_Q6_K:
            return runtime->q6_k_pair_pipeline;
        default:
            return nil;
    }
}

int termite_metal_decode_runtime_apply_quantized_q_kv_pair_linear_qkv_slots(
    termite_metal_decode_runtime *runtime,
    uint32_t q_format,
    uint32_t kv_format,
    size_t q_slot,
    size_t k_slot,
    size_t v_slot,
    const float *input,
    size_t rows,
    size_t in_dim,
    size_t q_out_dim,
    size_t kv_out_dim,
    float *q_output,
    float *k_output,
    float *v_output
) {
    if (runtime == NULL || input == NULL || q_output == NULL || k_output == NULL || v_output == NULL) return -1;
    id<MTLComputePipelineState> kv_pair_pipeline = termite_metal_quant_pair_pipeline_for_format(runtime, kv_format);
    if (kv_pair_pipeline == nil) return -2;
    if (q_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || k_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || v_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -3;
    if (q_slot == k_slot || q_slot == v_slot || k_slot == v_slot) return -4;
    termite_metal_quant_linear_slot_view q_view;
    termite_metal_quant_linear_slot_view k_view;
    termite_metal_quant_linear_slot_view v_view;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, q_format, q_slot, in_dim, q_out_dim, &q_view) != 0) return -5;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, kv_format, k_slot, in_dim, kv_out_dim, &k_view) != 0) return -6;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, kv_format, v_slot, in_dim, kv_out_dim, &v_view) != 0) return -7;
    if (rows == 0 || rows > UINT32_MAX || in_dim > UINT32_MAX || q_out_dim > UINT32_MAX || kv_out_dim > UINT32_MAX) return -8;
    @autoreleasepool {
        const size_t input_bytes = rows * in_dim * sizeof(float);
        const size_t q_output_bytes = rows * q_out_dim * sizeof(float);
        const size_t kv_output_bytes = rows * kv_out_dim * sizeof(float);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)input length:input_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:input length:input_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> q_output_buffer = [runtime->device newBufferWithBytesNoCopy:q_output length:q_output_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (q_output_buffer == nil) q_output_buffer = [runtime->device newBufferWithBytes:q_output length:q_output_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> k_output_buffer = [runtime->device newBufferWithBytesNoCopy:k_output length:kv_output_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (k_output_buffer == nil) k_output_buffer = [runtime->device newBufferWithBytes:k_output length:kv_output_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> v_output_buffer = [runtime->device newBufferWithBytesNoCopy:v_output length:kv_output_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (v_output_buffer == nil) v_output_buffer = [runtime->device newBufferWithBytes:v_output length:kv_output_bytes options:MTLResourceStorageModeShared];
        if (input_buffer == nil || q_output_buffer == nil || k_output_buffer == nil || v_output_buffer == nil) return -9;
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -10;
        const int rc = termite_metal_encode_quant_q_plus_kv_pair_descriptors(
            runtime,
            command_buffer,
            q_format,
            kv_format,
            q_slot,
            k_slot,
            input_buffer,
            0,
            rows,
            in_dim,
            q_out_dim,
            kv_out_dim,
            &q_view,
            &k_view,
            &v_view,
            kv_pair_pipeline,
            q_output_buffer,
            0,
            k_output_buffer,
            0,
            v_output_buffer,
            0,
            -11,
            -12);
        if (rc != 0) return rc;
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -13;
    }
}

static int termite_metal_decode_runtime_apply_quantized_q_kv_pair_linear_qkv_slots_device(
    termite_metal_decode_runtime *runtime,
    uint32_t q_format,
    uint32_t kv_format,
    size_t q_slot,
    size_t k_slot,
    size_t v_slot,
    void *input_handle,
    size_t input_offset,
    size_t rows,
    size_t in_dim,
    size_t q_out_dim,
    size_t kv_out_dim,
    void *q_output_handle,
    size_t q_output_offset,
    void *k_output_handle,
    size_t k_output_offset,
    void *v_output_handle,
    size_t v_output_offset
) {
    if (runtime == NULL || input_handle == NULL || q_output_handle == NULL || k_output_handle == NULL || v_output_handle == NULL) return -1;
    id<MTLComputePipelineState> kv_pair_pipeline = termite_metal_quant_pair_pipeline_for_format(runtime, kv_format);
    if (kv_pair_pipeline == nil) return -2;
    if (q_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || k_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || v_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -3;
    if (q_slot == k_slot || q_slot == v_slot || k_slot == v_slot) return -4;
    termite_metal_quant_linear_slot_view q_view;
    termite_metal_quant_linear_slot_view k_view;
    termite_metal_quant_linear_slot_view v_view;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, q_format, q_slot, in_dim, q_out_dim, &q_view) != 0) return -5;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, kv_format, k_slot, in_dim, kv_out_dim, &k_view) != 0) return -6;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, kv_format, v_slot, in_dim, kv_out_dim, &v_view) != 0) return -7;
    if (rows == 0 || rows > UINT32_MAX || in_dim > UINT32_MAX || q_out_dim > UINT32_MAX || kv_out_dim > UINT32_MAX) return -8;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLBuffer> q_output_buffer = (__bridge id<MTLBuffer>)q_output_handle;
        id<MTLBuffer> k_output_buffer = (__bridge id<MTLBuffer>)k_output_handle;
        id<MTLBuffer> v_output_buffer = (__bridge id<MTLBuffer>)v_output_handle;
        const size_t input_bytes = rows * in_dim * sizeof(float);
        const size_t q_output_bytes = rows * q_out_dim * sizeof(float);
        const size_t kv_output_bytes = rows * kv_out_dim * sizeof(float);
        if (input_offset + input_bytes > input_buffer.length) return -9;
        if (q_output_offset + q_output_bytes > q_output_buffer.length) return -10;
        if (k_output_offset + kv_output_bytes > k_output_buffer.length) return -11;
        if (v_output_offset + kv_output_bytes > v_output_buffer.length) return -12;
        const bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLCommandBuffer> command_buffer = frame_owned
            ? termite_metal_new_command_buffer(runtime->queue, __func__)
            : runtime->active_frame_cb;
        if (command_buffer == nil) return -13;
        const int rc = termite_metal_encode_quant_q_plus_kv_pair_descriptors(
            runtime,
            command_buffer,
            q_format,
            kv_format,
            q_slot,
            k_slot,
            input_buffer,
            input_offset,
            rows,
            in_dim,
            q_out_dim,
            kv_out_dim,
            &q_view,
            &k_view,
            &v_view,
            kv_pair_pipeline,
            q_output_buffer,
            q_output_offset,
            k_output_buffer,
            k_output_offset,
            v_output_buffer,
            v_output_offset,
            -14,
            -15);
        if (rc != 0) return rc;
        if (!frame_owned) return 0;
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -16;
    }
}

static int termite_metal_encode_dense_linear_on_encoder(
    termite_metal_decode_runtime *runtime,
    id<MTLComputeCommandEncoder> encoder,
    size_t slot,
    id<MTLBuffer> input_buffer,
    size_t input_offset,
    size_t in_dim,
    size_t out_dim,
    id<MTLBuffer> output_buffer,
    size_t output_offset,
    int failure_code
) {
    if (runtime == NULL || encoder == nil || input_buffer == nil || output_buffer == nil) return failure_code;
    if (runtime->linear_pipeline == nil || runtime->linear_reduce_pipeline == nil || runtime->linear_bf16_pipeline == nil || runtime->linear_bf16_reduce_pipeline == nil) return failure_code;
    if (slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || in_dim == 0 || out_dim == 0) return failure_code;
    if (runtime->linear_slot_prepared[slot] == 0) return failure_code;
    if (runtime->linear_in_dims[slot] != in_dim || runtime->linear_out_dims[slot] != out_dim) return failure_code;
    if (in_dim > UINT32_MAX || out_dim > UINT32_MAX) return failure_code;
    const size_t input_bytes = in_dim * sizeof(float);
    const size_t output_bytes = out_dim * sizeof(float);
    if (input_offset + input_bytes > input_buffer.length) return failure_code;
    if (output_offset + output_bytes > output_buffer.length) return failure_code;
    if (termite_metal_decode_runtime_prepare_planned_dense_linear_accesses(
            runtime,
            input_buffer,
            input_offset,
            input_bytes,
            runtime->linear_weight_buffers[slot],
            runtime->linear_bias_buffers[slot],
            output_buffer,
            output_offset,
            output_bytes,
            failure_code) != 0) return failure_code;
    termite_metal_apply_linear_params params = {
        .in_dim = (uint32_t)in_dim,
        .out_dim = (uint32_t)out_dim,
    };
    const BOOL use_reduce = out_dim >= 128;
    const BOOL use_bf16 = runtime->linear_weight_dtypes[slot] == TERMITE_METAL_DENSE_LINEAR_DTYPE_BF16;
    [encoder setComputePipelineState:(use_bf16 ? (use_reduce ? runtime->linear_bf16_reduce_pipeline : runtime->linear_bf16_pipeline) : (use_reduce ? runtime->linear_reduce_pipeline : runtime->linear_pipeline))];
    [encoder setBuffer:input_buffer offset:input_offset atIndex:0];
    [encoder setBuffer:runtime->linear_weight_buffers[slot] offset:0 atIndex:1];
    [encoder setBuffer:runtime->linear_bias_buffers[slot] offset:0 atIndex:2];
    [encoder setBuffer:output_buffer offset:output_offset atIndex:3];
    [encoder setBytes:&params length:sizeof(params) atIndex:4];
    if (use_reduce) {
        [encoder dispatchThreads:MTLSizeMake(out_dim * 32u, 1, 1) threadsPerThreadgroup:MTLSizeMake(32, 1, 1)];
    } else {
        id<MTLComputePipelineState> scalar_pipeline = use_bf16 ? runtime->linear_bf16_pipeline : runtime->linear_pipeline;
        NSUInteger thread_width = scalar_pipeline.maxTotalThreadsPerThreadgroup;
        if (thread_width == 0) thread_width = 64;
        if (thread_width > out_dim && out_dim > 0) thread_width = out_dim;
        [encoder dispatchThreads:MTLSizeMake(out_dim, 1, 1) threadsPerThreadgroup:MTLSizeMake(thread_width, 1, 1)];
    }
    return 0;
}

int termite_metal_decode_runtime_apply_linear_argmax(
    termite_metal_decode_runtime *runtime,
    size_t slot,
    const float *input,
    size_t in_dim,
    size_t out_dim,
    uint32_t *output_token_id
) {
    if (runtime == NULL || input == NULL || output_token_id == NULL) return -1;
    if (runtime->linear_pipeline == nil || runtime->linear_reduce_pipeline == nil || runtime->linear_bf16_pipeline == nil || runtime->linear_bf16_reduce_pipeline == nil || runtime->argmax_logits_pipeline == nil) return -2;
    if (slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || in_dim == 0 || out_dim == 0) return -3;
    if (runtime->linear_slot_prepared[slot] == 0) return -4;
    if (runtime->linear_in_dims[slot] != in_dim || runtime->linear_out_dims[slot] != out_dim) return -5;
    if (in_dim > UINT32_MAX || out_dim > UINT32_MAX) return -6;
    @autoreleasepool {
        const size_t input_bytes = in_dim * sizeof(float);
        const size_t logits_bytes = out_dim * sizeof(float);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)input length:input_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:input length:input_bytes options:MTLResourceStorageModeShared];
        if (input_buffer == nil) return -7;
        if (termite_metal_decode_runtime_ensure_sample_logits_buffer(runtime, logits_bytes) != 0) return -8;
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -9;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_TAIL);
        if (encoder == nil) return -10;
        const int linear_rc = termite_metal_encode_dense_linear_on_encoder(runtime, encoder, slot, input_buffer, 0, in_dim, out_dim, runtime->sample_logits_buffer, 0, -10);
        if (linear_rc != 0) return linear_rc;
        [encoder memoryBarrierWithScope:MTLBarrierScopeBuffers];
        const int argmax_rc = termite_metal_encode_argmax_logits_on_encoder(runtime, encoder, runtime->sample_logits_buffer, 0, out_dim, -10);
        [encoder endEncoding];
        if (argmax_rc != 0) return argmax_rc;
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        if (command_buffer.status != MTLCommandBufferStatusCompleted) return -11;
        uint32_t *result = (uint32_t *)runtime->token_buffer.contents;
        if (result == NULL) return -12;
        output_token_id[0] = result[0];
        return 0;
    }
}

int termite_metal_decode_runtime_apply_linear_argmax_device(
    termite_metal_decode_runtime *runtime,
    size_t slot,
    void *input_handle,
    size_t input_offset,
    size_t in_dim,
    size_t out_dim,
    uint32_t *output_token_id
) {
    if (runtime == NULL || input_handle == NULL || output_token_id == NULL) return -1;
    if (runtime->linear_pipeline == nil || runtime->linear_reduce_pipeline == nil || runtime->linear_bf16_pipeline == nil || runtime->linear_bf16_reduce_pipeline == nil || runtime->argmax_logits_pipeline == nil) return -2;
    if (slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || in_dim == 0 || out_dim == 0) return -3;
    if (runtime->linear_slot_prepared[slot] == 0) return -4;
    if (runtime->linear_in_dims[slot] != in_dim || runtime->linear_out_dims[slot] != out_dim) return -5;
    if (in_dim > UINT32_MAX || out_dim > UINT32_MAX) return -6;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        const size_t input_bytes = in_dim * sizeof(float);
        const size_t logits_bytes = out_dim * sizeof(float);
        if (input_offset + input_bytes > input_buffer.length) return -7;
        if (termite_metal_decode_runtime_ensure_sample_logits_buffer(runtime, logits_bytes) != 0) return -8;
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -10;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_TAIL);
        if (encoder == nil) return -11;
        const int linear_rc = termite_metal_encode_dense_linear_on_encoder(runtime, encoder, slot, input_buffer, input_offset, in_dim, out_dim, runtime->sample_logits_buffer, 0, -11);
        if (linear_rc != 0) return linear_rc;
        [encoder memoryBarrierWithScope:MTLBarrierScopeBuffers];
        const int argmax_rc = termite_metal_encode_argmax_logits_on_encoder(runtime, encoder, runtime->sample_logits_buffer, 0, out_dim, -11);
        [encoder endEncoding];
        if (argmax_rc != 0) return argmax_rc;
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        if (command_buffer.status != MTLCommandBufferStatusCompleted) return -12;
        uint32_t *result = (uint32_t *)runtime->token_buffer.contents;
        if (result == NULL) return -13;
        output_token_id[0] = result[0];
        return 0;
    }
}

int termite_metal_decode_runtime_argmax_from_logits_device(
    termite_metal_decode_runtime *runtime,
    void *logits_handle,
    size_t logits_offset,
    size_t out_dim,
    uint32_t *output_token_id
) {
    if (runtime == NULL || logits_handle == NULL || output_token_id == NULL) return -1;
    if (runtime->argmax_logits_pipeline == nil) return -2;
    if (out_dim == 0 || out_dim > UINT32_MAX) return -3;
    @autoreleasepool {
        id<MTLBuffer> logits_buffer = (__bridge id<MTLBuffer>)logits_handle;
        const size_t logits_bytes = out_dim * sizeof(float);
        if (logits_offset + logits_bytes > logits_buffer.length) return -4;
        if (runtime->token_capacity < sizeof(uint32_t) || runtime->token_buffer == nil || runtime->token_buffer.storageMode != MTLStorageModeShared) {
            id<MTLBuffer> token = [runtime->device newBufferWithLength:sizeof(uint32_t) options:MTLResourceStorageModeShared];
            if (token == nil) return -5;
            runtime->token_buffer = token;
            runtime->token_capacity = sizeof(uint32_t);
        }
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -6;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (encoder == nil) return -7;
        const int encode_rc = termite_metal_encode_argmax_logits_on_encoder(runtime, encoder, logits_buffer, logits_offset, out_dim, -7);
        [encoder endEncoding];
        if (encode_rc != 0) return encode_rc;
        if (!frame_owned) return -10;
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        if (command_buffer.status != MTLCommandBufferStatusCompleted) return -8;
        uint32_t *result = (uint32_t *)runtime->token_buffer.contents;
        if (result == NULL) return -9;
        output_token_id[0] = result[0];
        return 0;
    }
}

int termite_metal_decode_runtime_encode_argmax_from_logits_device(
    termite_metal_decode_runtime *runtime,
    void *logits_handle,
    size_t logits_offset,
    size_t out_dim
) {
    if (runtime == NULL || logits_handle == NULL) return -1;
    if (runtime->argmax_logits_pipeline == nil) return -2;
    if (out_dim == 0 || out_dim > UINT32_MAX) return -3;
    @autoreleasepool {
        id<MTLBuffer> logits_buffer = (__bridge id<MTLBuffer>)logits_handle;
        const size_t logits_bytes = out_dim * sizeof(float);
        if (logits_offset + logits_bytes > logits_buffer.length) return -4;
        if (runtime->token_capacity < sizeof(uint32_t) || runtime->token_buffer == nil || runtime->token_buffer.storageMode != MTLStorageModeShared) {
            id<MTLBuffer> token = [runtime->device newBufferWithLength:sizeof(uint32_t) options:MTLResourceStorageModeShared];
            if (token == nil) return -5;
            runtime->token_buffer = token;
            runtime->token_capacity = sizeof(uint32_t);
        }
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -6;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (encoder == nil) return -7;
        const int encode_rc = termite_metal_encode_argmax_logits_on_encoder(runtime, encoder, logits_buffer, logits_offset, out_dim, -7);
        [encoder endEncoding];
        if (encode_rc != 0) return encode_rc;
        if (!frame_owned) return 0;
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        if (command_buffer.status != MTLCommandBufferStatusCompleted) return -8;
        return 0;
    }
}

int termite_metal_decode_runtime_apply_layer_norm_linear_argmax(
    termite_metal_decode_runtime *runtime,
    size_t norm_slot,
    size_t linear_slot,
    const float *input,
    size_t hidden_size,
    float eps,
    size_t out_dim,
    uint32_t *output_token_id
) {
    if (runtime == NULL || input == NULL || output_token_id == NULL) return -1;
    if (runtime->layer_norm_pipeline == nil || runtime->linear_pipeline == nil || runtime->linear_reduce_pipeline == nil || runtime->linear_bf16_pipeline == nil || runtime->linear_bf16_reduce_pipeline == nil || runtime->argmax_logits_pipeline == nil) return -2;
    if (norm_slot >= TERMITE_METAL_LAYER_NORM_SLOT_CAPACITY || linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -3;
    if (hidden_size == 0 || out_dim == 0) return -4;
    if (runtime->layer_norm_slot_prepared[norm_slot] == 0 || runtime->linear_slot_prepared[linear_slot] == 0) return -5;
    if (runtime->layer_norm_hidden_sizes[norm_slot] != hidden_size) return -6;
    if (runtime->linear_in_dims[linear_slot] != hidden_size || runtime->linear_out_dims[linear_slot] != out_dim) return -7;
    if (hidden_size > UINT32_MAX || out_dim > UINT32_MAX) return -8;
    @autoreleasepool {
        const size_t input_bytes = hidden_size * sizeof(float);
        const size_t hidden_bytes = hidden_size * sizeof(float);
        const size_t logits_bytes = out_dim * sizeof(float);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)input length:input_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:input length:input_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> hidden_buffer = [runtime->device newBufferWithLength:hidden_bytes options:MTLResourceStorageModePrivate];
        if (input_buffer == nil || hidden_buffer == nil) return -9;
        termite_metal_apply_layer_norm_params norm_params = {
            .hidden_size = (uint32_t)hidden_size,
            .eps = eps,
        };
        id<MTLBuffer> norm_params_buffer = [runtime->device newBufferWithBytes:&norm_params length:sizeof(norm_params) options:MTLResourceStorageModeShared];
        if (norm_params_buffer == nil) return -10;
        if (termite_metal_decode_runtime_ensure_sample_logits_buffer(runtime, logits_bytes) != 0) return -10;
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -11;

        id<MTLComputeCommandEncoder> norm_encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_RMS_NORM);
        if (norm_encoder == nil) return -12;
        [norm_encoder setComputePipelineState:runtime->layer_norm_pipeline];
        [norm_encoder setBuffer:input_buffer offset:0 atIndex:0];
        [norm_encoder setBuffer:runtime->layer_norm_weight_buffers[norm_slot] offset:0 atIndex:1];
        [norm_encoder setBuffer:runtime->layer_norm_bias_buffers[norm_slot] offset:0 atIndex:2];
        [norm_encoder setBuffer:hidden_buffer offset:0 atIndex:3];
        [norm_encoder setBuffer:norm_params_buffer offset:0 atIndex:4];
        [norm_encoder dispatchThreads:MTLSizeMake(1, 1, 1) threadsPerThreadgroup:MTLSizeMake(1, 1, 1)];
        [norm_encoder endEncoding];

        id<MTLComputeCommandEncoder> linear_encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_TAIL);
        if (linear_encoder == nil) return -13;
        const int linear_rc = termite_metal_encode_dense_linear_on_encoder(runtime, linear_encoder, linear_slot, hidden_buffer, 0, hidden_size, out_dim, runtime->sample_logits_buffer, 0, -13);
        if (linear_rc != 0) return linear_rc;
        [linear_encoder memoryBarrierWithScope:MTLBarrierScopeBuffers];
        const int argmax_rc = termite_metal_encode_argmax_logits_on_encoder(runtime, linear_encoder, runtime->sample_logits_buffer, 0, out_dim, -13);
        [linear_encoder endEncoding];
        if (argmax_rc != 0) return argmax_rc;

        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        if (command_buffer.status != MTLCommandBufferStatusCompleted) return -14;
        uint32_t *result = (uint32_t *)runtime->token_buffer.contents;
        if (result == NULL) return -15;
        output_token_id[0] = result[0];
        return 0;
    }
}

int termite_metal_decode_runtime_apply_layer_norm_linear(
    termite_metal_decode_runtime *runtime,
    size_t norm_slot,
    size_t linear_slot,
    const float *input,
    size_t hidden_size,
    float eps,
    size_t out_dim,
    float *output
) {
    if (runtime == NULL || input == NULL || output == NULL) return -1;
    if (runtime->layer_norm_pipeline == nil || runtime->linear_pipeline == nil || runtime->linear_reduce_pipeline == nil) return -2;
    if (norm_slot >= TERMITE_METAL_LAYER_NORM_SLOT_CAPACITY || linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -3;
    if (hidden_size == 0 || out_dim == 0) return -4;
    if (runtime->layer_norm_slot_prepared[norm_slot] == 0 || runtime->linear_slot_prepared[linear_slot] == 0) return -5;
    if (runtime->layer_norm_hidden_sizes[norm_slot] != hidden_size) return -6;
    if (runtime->linear_in_dims[linear_slot] != hidden_size || runtime->linear_out_dims[linear_slot] != out_dim) return -7;
    if (hidden_size > UINT32_MAX || out_dim > UINT32_MAX) return -8;
    @autoreleasepool {
        const size_t input_bytes = hidden_size * sizeof(float);
        const size_t hidden_bytes = hidden_size * sizeof(float);
        const size_t output_bytes = out_dim * sizeof(float);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)input length:input_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:input length:input_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> hidden_buffer = [runtime->device newBufferWithLength:hidden_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithLength:output_bytes options:MTLResourceStorageModeShared];
        if (input_buffer == nil || hidden_buffer == nil || output_buffer == nil) return -9;
        termite_metal_apply_layer_norm_params norm_params = {
            .hidden_size = (uint32_t)hidden_size,
            .eps = eps,
        };
        termite_metal_apply_linear_params linear_params = {
            .in_dim = (uint32_t)hidden_size,
            .out_dim = (uint32_t)out_dim,
        };
        id<MTLBuffer> norm_params_buffer = [runtime->device newBufferWithBytes:&norm_params length:sizeof(norm_params) options:MTLResourceStorageModeShared];
        id<MTLBuffer> linear_params_buffer = [runtime->device newBufferWithBytes:&linear_params length:sizeof(linear_params) options:MTLResourceStorageModeShared];
        if (norm_params_buffer == nil || linear_params_buffer == nil) return -10;
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -11;

        id<MTLComputeCommandEncoder> norm_encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_RMS_NORM);
        if (norm_encoder == nil) return -12;
        [norm_encoder setComputePipelineState:runtime->layer_norm_pipeline];
        [norm_encoder setBuffer:input_buffer offset:0 atIndex:0];
        [norm_encoder setBuffer:runtime->layer_norm_weight_buffers[norm_slot] offset:0 atIndex:1];
        [norm_encoder setBuffer:runtime->layer_norm_bias_buffers[norm_slot] offset:0 atIndex:2];
        [norm_encoder setBuffer:hidden_buffer offset:0 atIndex:3];
        [norm_encoder setBuffer:norm_params_buffer offset:0 atIndex:4];
        [norm_encoder dispatchThreads:MTLSizeMake(1, 1, 1) threadsPerThreadgroup:MTLSizeMake(1, 1, 1)];
        [norm_encoder endEncoding];

        id<MTLComputeCommandEncoder> linear_encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_TAIL);
        if (linear_encoder == nil) return -13;
        const BOOL use_reduce = out_dim >= 128;
        [linear_encoder setComputePipelineState:(use_reduce ? runtime->linear_reduce_pipeline : runtime->linear_pipeline)];
        [linear_encoder setBuffer:hidden_buffer offset:0 atIndex:0];
        [linear_encoder setBuffer:runtime->linear_weight_buffers[linear_slot] offset:0 atIndex:1];
        [linear_encoder setBuffer:runtime->linear_bias_buffers[linear_slot] offset:0 atIndex:2];
        [linear_encoder setBuffer:output_buffer offset:0 atIndex:3];
        [linear_encoder setBuffer:linear_params_buffer offset:0 atIndex:4];
        if (use_reduce) {
            [linear_encoder dispatchThreads:MTLSizeMake(out_dim * 32u, 1, 1) threadsPerThreadgroup:MTLSizeMake(32, 1, 1)];
        } else {
            NSUInteger thread_width = runtime->linear_pipeline.maxTotalThreadsPerThreadgroup;
            if (thread_width == 0) thread_width = 64;
            if (thread_width > out_dim && out_dim > 0) thread_width = out_dim;
            [linear_encoder dispatchThreads:MTLSizeMake(out_dim, 1, 1) threadsPerThreadgroup:MTLSizeMake(thread_width, 1, 1)];
        }
        [linear_encoder endEncoding];

        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        if (command_buffer.status != MTLCommandBufferStatusCompleted) return -14;
        float *result = (float *)output_buffer.contents;
        if (result == NULL) return -15;
        memcpy(output, result, output_bytes);
        return 0;
    }
}

int termite_metal_decode_runtime_apply_rms_norm_linear_argmax(
    termite_metal_decode_runtime *runtime,
    size_t norm_slot,
    size_t linear_slot,
    const float *input,
    size_t hidden_size,
    float eps,
    size_t out_dim,
    uint32_t *output_token_id
) {
    if (runtime == NULL || input == NULL || output_token_id == NULL) return -1;
    if (runtime->rms_norm_reduce_pipeline == nil || runtime->linear_pipeline == nil || runtime->linear_reduce_pipeline == nil || runtime->linear_bf16_pipeline == nil || runtime->linear_bf16_reduce_pipeline == nil || runtime->argmax_logits_pipeline == nil) return -2;
    if (norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY || linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -3;
    if (hidden_size == 0 || out_dim == 0) return -4;
    if (runtime->rms_norm_slot_prepared[norm_slot] == 0 || runtime->linear_slot_prepared[linear_slot] == 0) return -5;
    if (runtime->rms_norm_hidden_sizes[norm_slot] != hidden_size) return -6;
    if (runtime->linear_in_dims[linear_slot] != hidden_size || runtime->linear_out_dims[linear_slot] != out_dim) return -7;
    if (hidden_size > UINT32_MAX || out_dim > UINT32_MAX) return -8;
    @autoreleasepool {
        const size_t input_bytes = hidden_size * sizeof(float);
        const size_t hidden_bytes = hidden_size * sizeof(float);
        const size_t logits_bytes = out_dim * sizeof(float);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)input length:input_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:input length:input_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> hidden_buffer = [runtime->device newBufferWithLength:hidden_bytes options:MTLResourceStorageModePrivate];
        if (input_buffer == nil || hidden_buffer == nil) return -9;
        if (termite_metal_decode_runtime_ensure_sample_logits_buffer(runtime, logits_bytes) != 0) return -10;
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -11;

        if (termite_metal_encode_rms_norm_reduce(
                runtime,
                command_buffer,
                input_buffer,
                0,
                norm_slot,
                hidden_buffer,
                0,
                hidden_size,
                eps,
                -12) != 0) return -12;

        id<MTLComputeCommandEncoder> linear_encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_TAIL);
        if (linear_encoder == nil) return -13;
        const int linear_rc = termite_metal_encode_dense_linear_on_encoder(runtime, linear_encoder, linear_slot, hidden_buffer, 0, hidden_size, out_dim, runtime->sample_logits_buffer, 0, -13);
        if (linear_rc != 0) return linear_rc;
        [linear_encoder memoryBarrierWithScope:MTLBarrierScopeBuffers];
        const int argmax_rc = termite_metal_encode_argmax_logits_on_encoder(runtime, linear_encoder, runtime->sample_logits_buffer, 0, out_dim, -13);
        [linear_encoder endEncoding];
        if (argmax_rc != 0) return argmax_rc;

        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        if (command_buffer.status != MTLCommandBufferStatusCompleted) return -14;
        uint32_t *result = (uint32_t *)runtime->token_buffer.contents;
        if (result == NULL) return -15;
        output_token_id[0] = result[0];
        return 0;
    }
}

int termite_metal_decode_runtime_encode_rms_norm_linear_argmax_device(
    termite_metal_decode_runtime *runtime,
    size_t norm_slot,
    size_t linear_slot,
    void *input_handle,
    size_t input_offset,
    size_t hidden_size,
    float eps,
    size_t out_dim,
    termite_metal_planned_layer_contract planned_contract
) {
    if (runtime == NULL || input_handle == NULL) return -1;
    if (runtime->rms_norm_reduce_pipeline == nil || runtime->linear_pipeline == nil || runtime->linear_reduce_pipeline == nil || runtime->linear_bf16_pipeline == nil || runtime->linear_bf16_reduce_pipeline == nil || runtime->argmax_logits_pipeline == nil) return -2;
    if (norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY || linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -3;
    if (hidden_size == 0 || out_dim == 0) return -4;
    if (runtime->rms_norm_slot_prepared[norm_slot] == 0 || runtime->linear_slot_prepared[linear_slot] == 0) return -5;
    if (runtime->rms_norm_hidden_sizes[norm_slot] != hidden_size) return -6;
    if (runtime->linear_in_dims[linear_slot] != hidden_size || runtime->linear_out_dims[linear_slot] != out_dim) return -7;
    if (hidden_size > UINT32_MAX || out_dim > UINT32_MAX) return -8;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        const size_t input_bytes = hidden_size * sizeof(float);
        const size_t logits_bytes = out_dim * sizeof(float);
        if (input_offset + input_bytes > input_buffer.length) return -9;
        if (runtime->token_capacity < sizeof(uint32_t) || runtime->token_buffer == nil || runtime->token_buffer.storageMode != MTLStorageModeShared) {
            id<MTLBuffer> token = [runtime->device newBufferWithLength:sizeof(uint32_t) options:MTLResourceStorageModeShared];
            if (token == nil) return -10;
            runtime->token_buffer = token;
            runtime->token_capacity = sizeof(uint32_t);
        }
        if (termite_metal_decode_runtime_ensure_hot_buffers(runtime, hidden_size * sizeof(float), 0) != 0) return -11;
        id<MTLBuffer> hidden_buffer = runtime->hot_hidden_buffers[0];
        if (hidden_buffer == nil) return -11;
        if (termite_metal_decode_runtime_ensure_sample_logits_buffer(runtime, logits_bytes) != 0) return -12;
        const bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLCommandBuffer> command_buffer = frame_owned
            ? termite_metal_new_command_buffer(runtime->queue, __func__)
            : runtime->active_frame_cb;
        if (command_buffer == nil) return -13;

        if (!frame_owned) {
            const BOOL reusing_planned_encoder = (runtime->active_planned_compute_encoder != nil);
            if (!reusing_planned_encoder && termite_metal_decode_runtime_begin_planned_compute_scope(runtime, TERMITE_METAL_COMPUTE_SOURCE_TAIL, TERMITE_METAL_COMPUTE_REGION_TAIL) != 0) return -14;
            id<MTLComputeCommandEncoder> tail_encoder = runtime->active_planned_compute_encoder;
            int rc = 0;
            if (tail_encoder == nil) rc = -14;
            if (rc == 0 && planned_contract.command_ops != NULL) {
                for (size_t op_index = planned_contract.start_index;
                     rc == 0 && op_index < planned_contract.command_op_count;
                     op_index += 1)
                {
                    const termite_metal_runtime_command_op op = planned_contract.command_ops[op_index];
                    termite_metal_decode_runtime_record_planned_command_op(runtime, op);
                    switch (op.kind) {
                        case TERMITE_METAL_PLAN_OP_DECODE_TAIL_FINAL_NORM:
                            rc = termite_metal_encode_rms_norm_reduce(
                                runtime,
                                command_buffer,
                                input_buffer,
                                input_offset,
                                norm_slot,
                                hidden_buffer,
                                0,
                                hidden_size,
                                eps,
                                -14);
                            break;
                        case TERMITE_METAL_PLAN_OP_DECODE_TAIL_LM_HEAD:
                            if (!termite_metal_runtime_operator_is_quant_matmul(op.op)) {
                                rc = -15;
                                break;
                            }
                            if (op.format != TERMITE_METAL_GRAPH_FORMAT_UNKNOWN) {
                                rc = -15;
                                break;
                            }
                            rc = termite_metal_encode_dense_linear_on_encoder(
                                runtime,
                                tail_encoder,
                                linear_slot,
                                hidden_buffer,
                                0,
                                hidden_size,
                                out_dim,
                                runtime->sample_logits_buffer,
                                0,
                                -15);
                            break;
                        case TERMITE_METAL_PLAN_OP_DECODE_TAIL_ARGMAX:
                            rc = termite_metal_encode_argmax_logits_on_encoder(
                                runtime,
                                tail_encoder,
                                runtime->sample_logits_buffer,
                                0,
                                out_dim,
                                -16);
                            break;
                        default:
                            rc = -14;
                            break;
                    }
                }
            } else if (rc == 0) {
                rc = termite_metal_encode_rms_norm_reduce(
                    runtime,
                    command_buffer,
                    input_buffer,
                    input_offset,
                    norm_slot,
                    hidden_buffer,
                    0,
	                    hidden_size,
	                    eps,
	                    -14);
	                if (rc == 0) rc = termite_metal_encode_dense_linear_on_encoder(
                    runtime,
                    tail_encoder,
                    linear_slot,
                    hidden_buffer,
                    0,
                    hidden_size,
                    out_dim,
	                    runtime->sample_logits_buffer,
	                    0,
	                    -15);
	                if (rc == 0) rc = termite_metal_encode_argmax_logits_on_encoder(
                    runtime,
                    tail_encoder,
                    runtime->sample_logits_buffer,
                    0,
                    out_dim,
                    -16);
            }
            const int end_rc = reusing_planned_encoder ? 0 : termite_metal_decode_runtime_end_planned_compute_scope(runtime);
            if (rc != 0) return rc;
            return end_rc == 0 ? 0 : -17;
        }

        if (termite_metal_encode_rms_norm_reduce(
                runtime,
                command_buffer,
                input_buffer,
                input_offset,
                norm_slot,
                hidden_buffer,
                0,
                hidden_size,
                eps,
                -14) != 0) return -14;

        id<MTLComputeCommandEncoder> tail_encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_TAIL);
        if (tail_encoder == nil) return -15;
        const int linear_rc = termite_metal_encode_dense_linear_on_encoder(
            runtime,
            tail_encoder,
            linear_slot,
            hidden_buffer,
            0,
            hidden_size,
            out_dim,
            runtime->sample_logits_buffer,
            0,
            -15);
        if (linear_rc != 0) return linear_rc;
        [tail_encoder memoryBarrierWithScope:MTLBarrierScopeBuffers];
        const int argmax_rc = termite_metal_encode_argmax_logits_on_encoder(
            runtime,
            tail_encoder,
            runtime->sample_logits_buffer,
            0,
            out_dim,
            -16);
        [tail_encoder endEncoding];
        if (argmax_rc != 0) return argmax_rc;

        if (!frame_owned) return 0;
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -16;
    }
}

int termite_metal_decode_runtime_encode_rms_norm_quantized_linear_argmax_device(
    termite_metal_decode_runtime *runtime,
    size_t norm_slot,
    size_t linear_slot,
    uint32_t format,
    void *input_handle,
    size_t input_offset,
    size_t hidden_size,
    float eps,
    size_t out_dim,
    termite_metal_planned_layer_contract planned_contract
) {
    if (runtime == NULL || input_handle == NULL) return -1;
    if (runtime->rms_norm_reduce_pipeline == nil || runtime->argmax_logits_pipeline == nil) return -2;
    if (norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY || linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -3;
    if (hidden_size == 0 || out_dim == 0) return -4;
    termite_metal_quant_linear_slot_view linear_view;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, format, linear_slot, hidden_size, out_dim, &linear_view) != 0) return -5;
    if (runtime->rms_norm_slot_prepared[norm_slot] == 0) return -5;
    if (runtime->rms_norm_hidden_sizes[norm_slot] != hidden_size) return -6;
    if (hidden_size > UINT32_MAX || out_dim > UINT32_MAX) return -8;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        const size_t input_bytes = hidden_size * sizeof(float);
        const size_t logits_bytes = out_dim * sizeof(float);
        if (input_offset + input_bytes > input_buffer.length) return -9;
        if (runtime->token_capacity < sizeof(uint32_t) || runtime->token_buffer == nil || runtime->token_buffer.storageMode != MTLStorageModeShared) {
            id<MTLBuffer> token = [runtime->device newBufferWithLength:sizeof(uint32_t) options:MTLResourceStorageModeShared];
            if (token == nil) return -10;
            runtime->token_buffer = token;
            runtime->token_capacity = sizeof(uint32_t);
        }
        if (termite_metal_decode_runtime_ensure_hot_buffers(runtime, hidden_size * sizeof(float), 0) != 0) return -11;
        id<MTLBuffer> hidden_buffer = runtime->hot_hidden_buffers[0];
        if (hidden_buffer == nil) return -11;
        if (termite_metal_decode_runtime_ensure_sample_logits_buffer(runtime, logits_bytes) != 0) return -12;
        const bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLCommandBuffer> command_buffer = frame_owned
            ? termite_metal_new_command_buffer(runtime->queue, __func__)
            : runtime->active_frame_cb;
        if (command_buffer == nil) return -13;
        if (!frame_owned) {
            const BOOL reusing_planned_encoder = (runtime->active_planned_compute_encoder != nil);
            if (!reusing_planned_encoder && termite_metal_decode_runtime_begin_planned_compute_scope(runtime, TERMITE_METAL_COMPUTE_SOURCE_TAIL, TERMITE_METAL_COMPUTE_REGION_TAIL) != 0) return -14;
            id<MTLComputeCommandEncoder> tail_encoder = runtime->active_planned_compute_encoder;
            int rc = 0;
            if (tail_encoder == nil) rc = -14;
            if (rc == 0 && planned_contract.command_ops == NULL) rc = -14;
            for (size_t op_index = planned_contract.start_index;
                 rc == 0 && op_index < planned_contract.command_op_count;
                 op_index += 1)
            {
                const termite_metal_runtime_command_op op = planned_contract.command_ops[op_index];
                termite_metal_decode_runtime_record_planned_command_op(runtime, op);
                switch (op.kind) {
                    case TERMITE_METAL_PLAN_OP_DECODE_TAIL_FINAL_NORM:
                        rc = termite_metal_encode_rms_norm_reduce(
                            runtime,
                            command_buffer,
                            input_buffer,
                            input_offset,
                            norm_slot,
                            hidden_buffer,
                            0,
                            hidden_size,
                            eps,
                            -14);
                        break;
                    case TERMITE_METAL_PLAN_OP_DECODE_TAIL_LM_HEAD: {
                        if (!termite_metal_runtime_operator_is_quant_matmul(op.op)) {
                            rc = -15;
                            break;
                        }
                        if (!termite_metal_runtime_command_format_matches_quant(op.format, format)) {
                            rc = -15;
                            break;
                        }
                        termite_metal_quant_matmul_descriptor descriptor = {
                            .slot = linear_slot,
                            .format = format,
                            .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
                            .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_NONE,
                            .fallback_pipeline = linear_view.pipeline,
                            .input_buffer = hidden_buffer,
                            .weight_buffer = linear_view.weight_buffer,
                            .output_buffer = runtime->sample_logits_buffer,
                            .rows = 1,
                            .in_dim = hidden_size,
                            .out_dim = out_dim,
                            .values_per_block = linear_view.values_per_block,
                            .bytes_per_block = linear_view.bytes_per_block,
                            .q8_family = TERMITE_METAL_Q8_0_LINEAR_FAMILY_TAIL,
                            .use_planned_dispatch = true,
                            .planned_dispatch = op.quant_dispatch,
                            .failure_code = -15,
                        };
                        rc = termite_metal_encode_quant_matmul_descriptor_on_encoder(runtime, tail_encoder, &descriptor);
                        break;
                    }
                    case TERMITE_METAL_PLAN_OP_DECODE_TAIL_ARGMAX:
                        rc = termite_metal_encode_argmax_logits_on_encoder(
                            runtime,
                            tail_encoder,
                            runtime->sample_logits_buffer,
                            0,
                            out_dim,
                            -16);
                        break;
                    default:
                        rc = -14;
                        break;
                }
            }
            const int end_rc = reusing_planned_encoder ? 0 : termite_metal_decode_runtime_end_planned_compute_scope(runtime);
            if (rc != 0) return rc;
            return end_rc == 0 ? 0 : -17;
        }

        if (termite_metal_encode_rms_norm_reduce(
                runtime,
                command_buffer,
                input_buffer,
                input_offset,
                norm_slot,
                hidden_buffer,
                0,
                hidden_size,
                eps,
                -14) != 0) return -14;

        id<MTLComputeCommandEncoder> argmax_encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_TAIL);
        if (argmax_encoder == nil) return -15;
        termite_metal_quant_matmul_descriptor descriptor = {
            .slot = linear_slot,
            .format = format,
            .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
            .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_NONE,
            .fallback_pipeline = linear_view.pipeline,
            .input_buffer = hidden_buffer,
            .weight_buffer = linear_view.weight_buffer,
            .output_buffer = runtime->sample_logits_buffer,
            .rows = 1,
            .in_dim = hidden_size,
            .out_dim = out_dim,
            .values_per_block = linear_view.values_per_block,
            .bytes_per_block = linear_view.bytes_per_block,
            .q8_family = TERMITE_METAL_Q8_0_LINEAR_FAMILY_TAIL,
            .failure_code = -15,
        };
        const int linear_rc = termite_metal_encode_quant_matmul_descriptor_on_encoder(runtime, argmax_encoder, &descriptor);
        if (linear_rc != 0) {
            [argmax_encoder endEncoding];
            return linear_rc;
        }
        [argmax_encoder memoryBarrierWithScope:MTLBarrierScopeBuffers];
        const int argmax_rc = termite_metal_encode_argmax_logits_on_encoder(
            runtime,
            argmax_encoder,
            runtime->sample_logits_buffer,
            0,
            out_dim,
            -16);
        [argmax_encoder endEncoding];
        if (argmax_rc != 0) return argmax_rc;

        if (!frame_owned) return 0;
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -17;
    }
}

int termite_metal_decode_runtime_encode_rms_norm_quantized_linear_logits_device(
    termite_metal_decode_runtime *runtime,
    size_t norm_slot,
    size_t linear_slot,
    uint32_t format,
    void *input_handle,
    size_t input_offset,
    size_t hidden_size,
    float eps,
    size_t out_dim,
    termite_metal_planned_layer_contract planned_contract,
    void **logits_handle
) {
    if (runtime == NULL || input_handle == NULL || logits_handle == NULL) return -1;
    *logits_handle = NULL;
    if (runtime->rms_norm_reduce_pipeline == nil) return -2;
    if (norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY || linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -3;
    if (hidden_size == 0 || out_dim == 0) return -4;
    termite_metal_quant_linear_slot_view linear_view;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, format, linear_slot, hidden_size, out_dim, &linear_view) != 0) return -5;
    if (runtime->rms_norm_slot_prepared[norm_slot] == 0) return -5;
    if (runtime->rms_norm_hidden_sizes[norm_slot] != hidden_size) return -6;
    if (hidden_size > UINT32_MAX || out_dim > UINT32_MAX) return -8;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        const size_t input_bytes = hidden_size * sizeof(float);
        const size_t logits_bytes = out_dim * sizeof(float);
        if (input_offset + input_bytes > input_buffer.length) return -9;
        if (termite_metal_decode_runtime_ensure_hot_buffers(runtime, hidden_size * sizeof(float), 0) != 0) return -11;
        id<MTLBuffer> hidden_buffer = runtime->hot_hidden_buffers[0];
        if (hidden_buffer == nil) return -11;
        if (termite_metal_decode_runtime_ensure_sample_logits_buffer(runtime, logits_bytes) != 0) return -12;
        const bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLCommandBuffer> command_buffer = frame_owned
            ? termite_metal_new_command_buffer(runtime->queue, __func__)
            : runtime->active_frame_cb;
        if (command_buffer == nil) return -13;
        if (!frame_owned && planned_contract.command_ops != NULL) {
            const BOOL reusing_planned_encoder = (runtime->active_planned_compute_encoder != nil);
            if (!reusing_planned_encoder && termite_metal_decode_runtime_begin_planned_compute_scope(runtime, TERMITE_METAL_COMPUTE_SOURCE_TAIL, TERMITE_METAL_COMPUTE_REGION_TAIL) != 0) return -14;
            id<MTLComputeCommandEncoder> tail_encoder = runtime->active_planned_compute_encoder;
            int rc = 0;
            if (tail_encoder == nil) rc = -14;
            for (size_t op_index = planned_contract.start_index;
                 rc == 0 && op_index < planned_contract.command_op_count;
                 op_index += 1)
            {
                const termite_metal_runtime_command_op op = planned_contract.command_ops[op_index];
                termite_metal_decode_runtime_record_planned_command_op(runtime, op);
                switch (op.kind) {
                    case TERMITE_METAL_PLAN_OP_DECODE_TAIL_FINAL_NORM:
                        rc = termite_metal_encode_rms_norm_reduce(
                            runtime,
                            command_buffer,
                            input_buffer,
                            input_offset,
                            norm_slot,
                            hidden_buffer,
                            0,
                            hidden_size,
                            eps,
                            -14);
                        break;
                    case TERMITE_METAL_PLAN_OP_DECODE_TAIL_LM_HEAD: {
                        if (!termite_metal_runtime_operator_is_quant_matmul(op.op)) {
                            rc = -15;
                            break;
                        }
                        if (!termite_metal_runtime_command_format_matches_quant(op.format, format)) {
                            rc = -15;
                            break;
                        }
                        termite_metal_quant_matmul_descriptor descriptor = {
                            .slot = linear_slot,
                            .format = format,
                            .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
                            .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_NONE,
                            .fallback_pipeline = linear_view.pipeline,
                            .input_buffer = hidden_buffer,
                            .weight_buffer = linear_view.weight_buffer,
                            .output_buffer = runtime->sample_logits_buffer,
                            .rows = 1,
                            .in_dim = hidden_size,
                            .out_dim = out_dim,
                            .values_per_block = linear_view.values_per_block,
                            .bytes_per_block = linear_view.bytes_per_block,
                            .q8_family = TERMITE_METAL_Q8_0_LINEAR_FAMILY_TAIL,
                            .use_planned_dispatch = true,
                            .planned_dispatch = op.quant_dispatch,
                            .failure_code = -15,
                        };
                        rc = termite_metal_encode_quant_matmul_descriptor_on_encoder(runtime, tail_encoder, &descriptor);
                        break;
                    }
                    default:
                        rc = -14;
                        break;
                }
            }
            const int end_rc = reusing_planned_encoder ? 0 : termite_metal_decode_runtime_end_planned_compute_scope(runtime);
            if (rc != 0) return rc;
            if (end_rc != 0) return -17;
            *logits_handle = (__bridge void *)runtime->sample_logits_buffer;
            return 0;
        }

        if (termite_metal_encode_rms_norm_reduce(
                runtime,
                command_buffer,
                input_buffer,
                input_offset,
                norm_slot,
                hidden_buffer,
                0,
                hidden_size,
                eps,
                -14) != 0) return -14;

        id<MTLComputeCommandEncoder> logits_encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_TAIL);
        if (logits_encoder == nil) return -15;
        termite_metal_quant_matmul_descriptor descriptor = {
            .slot = linear_slot,
            .format = format,
            .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
            .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_NONE,
            .fallback_pipeline = linear_view.pipeline,
            .input_buffer = hidden_buffer,
            .weight_buffer = linear_view.weight_buffer,
            .output_buffer = runtime->sample_logits_buffer,
            .rows = 1,
            .in_dim = hidden_size,
            .out_dim = out_dim,
            .values_per_block = linear_view.values_per_block,
            .bytes_per_block = linear_view.bytes_per_block,
            .q8_family = TERMITE_METAL_Q8_0_LINEAR_FAMILY_TAIL,
            .failure_code = -15,
        };
        const int linear_rc = termite_metal_encode_quant_matmul_descriptor_on_encoder(runtime, logits_encoder, &descriptor);
        [logits_encoder endEncoding];
        if (linear_rc != 0) return linear_rc;

        if (frame_owned) {
            [command_buffer commit];
            [command_buffer waitUntilCompleted];
            if (command_buffer.status != MTLCommandBufferStatusCompleted) return -17;
        }
        *logits_handle = (__bridge void *)runtime->sample_logits_buffer;
        return 0;
    }
}

static int termite_metal_encode_ple_residual_q8_0_device(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    void *hidden_handle,
    size_t hidden_offset,
    void *ple_handle,
    size_t ple_offset,
    size_t gate_linear_slot,
    size_t proj_linear_slot,
    size_t post_norm_slot,
    size_t rows,
    size_t hidden_size,
    size_t ple_hidden_size,
    float eps,
    uint32_t activation_kind,
    int output_scale_present,
    float output_scale,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || command_buffer == nil || hidden_handle == NULL || ple_handle == NULL || output_handle == NULL) return -1;
    if (runtime->q8_0_pipeline == nil || runtime->activation_multiply_pipeline == nil || runtime->rms_norm_add_pipeline == nil || runtime->rms_norm_rows_pipeline == nil || runtime->add_pipeline == nil) return -2;
    if (output_scale_present != 0 && (runtime->rms_norm_add_scale_pipeline == nil || runtime->rms_norm_add_scale_rows_pipeline == nil)) return -2;
    if (gate_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || proj_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || post_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return -3;
    if (rows == 0 || hidden_size == 0 || ple_hidden_size == 0 || rows > UINT32_MAX || hidden_size > UINT32_MAX || ple_hidden_size > UINT32_MAX) return -4;
    if (hidden_size % 32u != 0 || ple_hidden_size % 32u != 0) return -5;
    termite_metal_quant_linear_slot_view gate_view;
    termite_metal_quant_linear_slot_view proj_view;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q8_0, gate_linear_slot, hidden_size, ple_hidden_size, &gate_view) != 0) return -6;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q8_0, proj_linear_slot, ple_hidden_size, hidden_size, &proj_view) != 0) return -7;
    if (runtime->rms_norm_slot_prepared[post_norm_slot] == 0) return -8;
    if (runtime->rms_norm_hidden_sizes[post_norm_slot] != hidden_size) return -9;
    if (rows > SIZE_MAX / hidden_size || rows > SIZE_MAX / ple_hidden_size) return -10;
    @autoreleasepool {
        id<MTLBuffer> hidden_buffer = (__bridge id<MTLBuffer>)hidden_handle;
        id<MTLBuffer> ple_buffer = (__bridge id<MTLBuffer>)ple_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t hidden_elems = rows * hidden_size;
        const size_t ple_elems = rows * ple_hidden_size;
        const size_t hidden_bytes = hidden_elems * sizeof(float);
        const size_t ple_bytes = ple_elems * sizeof(float);
        if (hidden_offset + hidden_bytes > hidden_buffer.length) return -10;
        if (ple_offset + ple_bytes > ple_buffer.length) return -11;
        if (output_offset + hidden_bytes > output_buffer.length) return -12;
        if (termite_metal_decode_runtime_ensure_hot_buffers(runtime, hidden_bytes, ple_bytes) != 0) return -13;
        id<MTLBuffer> gate_buffer = runtime->hot_intermediate_buffers[0];
        id<MTLBuffer> gated_buffer = runtime->hot_intermediate_buffers[2];
        id<MTLBuffer> projected_buffer = runtime->hot_hidden_buffers[2];
        id<MTLBuffer> normed_buffer = runtime->hot_hidden_buffers[3];
        if (gate_buffer == nil || gated_buffer == nil || projected_buffer == nil || normed_buffer == nil) return -13;

        termite_metal_apply_activation_params activation_params = {
            .activation_kind = activation_kind,
            .rows = (uint32_t)rows,
            .dim = (uint32_t)ple_hidden_size,
        };
        if (rows == 1 && runtime->active_frame_cb == command_buffer) {
            const int planned_status = termite_metal_encode_q8_0_ple_gate_proj_rms_add_scale_planned_1x(
                runtime,
                command_buffer,
                hidden_buffer,
                hidden_offset,
                ple_buffer,
                ple_offset,
                gate_view.weight_buffer,
                gated_buffer,
                proj_view.weight_buffer,
                projected_buffer,
                hidden_size,
                ple_hidden_size,
                activation_kind,
                post_norm_slot,
                output_buffer,
                output_offset,
                eps,
                output_scale_present,
                output_scale,
                -15);
            if (planned_status != 0) return planned_status;
            return 0;
        }
        const bool fused_gate = rows == 1 && termite_metal_encode_q8_0_activation_multiply(
                runtime,
                command_buffer,
                hidden_buffer,
                hidden_offset,
                gate_view.weight_buffer,
                ple_buffer,
                ple_offset,
                gated_buffer,
                0,
                rows,
                hidden_size,
                ple_hidden_size,
                activation_kind,
                -15) == 0;
        if (!fused_gate) {
            termite_metal_decode_runtime_record_logical_command_op(
                runtime,
                TERMITE_METAL_PLAN_OP_DECODE_PLE_GATE_ACTIVATION,
                TERMITE_METAL_OPERATOR_MUL_MM,
                TERMITE_METAL_QUANT_MATMUL_DISPATCH_MM);
            if (termite_metal_encode_q8_0_linear_family(
                    runtime,
                    command_buffer,
                    hidden_buffer,
                    hidden_offset,
                    gate_view.weight_buffer,
                    gate_buffer,
                    0,
                    rows,
                    hidden_size,
                    ple_hidden_size,
                    TERMITE_METAL_Q8_0_LINEAR_FAMILY_PLE_GATE,
                    -15) != 0) return -15;

            BOOL gated_encoder_owned = YES;
            id<MTLComputeCommandEncoder> gated_encoder = termite_metal_scoped_compute_encoder_for(runtime, command_buffer, TERMITE_METAL_COMPUTE_SOURCE_PLE, &gated_encoder_owned);
            if (gated_encoder == nil) return -16;
            if (termite_metal_decode_runtime_prepare_planned_compute_binary_accesses(
                    runtime,
                    gate_buffer,
                    0,
                    ple_bytes,
                    ple_buffer,
                    ple_offset,
                    ple_bytes,
                    gated_buffer,
                    0,
                    ple_bytes,
                    -16) != 0) return -16;
            [gated_encoder setComputePipelineState:runtime->activation_multiply_pipeline];
            [gated_encoder setBuffer:gate_buffer offset:0 atIndex:0];
            [gated_encoder setBuffer:ple_buffer offset:ple_offset atIndex:1];
            [gated_encoder setBuffer:gated_buffer offset:0 atIndex:2];
            [gated_encoder setBytes:&activation_params length:sizeof(activation_params) atIndex:3];
            [gated_encoder dispatchThreads:MTLSizeMake(ple_elems, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->activation_multiply_pipeline, ple_elems), 1, 1)];
            termite_metal_end_scoped_compute_encoder(gated_encoder, gated_encoder_owned);
        }

        if (rows == 1 && runtime->active_frame_cb == command_buffer) {
            const int planned_status = output_scale_present != 0
                ? termite_metal_encode_q8_0_linear_rms_add_scale_planned_1x(
                    runtime,
                    command_buffer,
                    gated_buffer,
                    0,
                    proj_view.weight_buffer,
                    projected_buffer,
                    0,
                    ple_hidden_size,
                    hidden_size,
                    post_norm_slot,
                    hidden_buffer,
                    hidden_offset,
                    output_buffer,
                    output_offset,
                    eps,
                    output_scale,
                    TERMITE_METAL_COMPUTE_SOURCE_PLE,
                    TERMITE_METAL_COMPUTE_REGION_PLE,
                    -18)
                : termite_metal_encode_q8_0_linear_rms_add_planned_1x(
                    runtime,
                    command_buffer,
                    gated_buffer,
                    0,
                    proj_view.weight_buffer,
                    projected_buffer,
                    0,
                    ple_hidden_size,
                    hidden_size,
                    post_norm_slot,
                    hidden_buffer,
                    hidden_offset,
                    output_buffer,
                    output_offset,
                    eps,
                    TERMITE_METAL_COMPUTE_SOURCE_PLE,
                    TERMITE_METAL_COMPUTE_REGION_PLE,
                    -18);
            if (planned_status != 0) return planned_status;
            return 0;
        }

        termite_metal_decode_runtime_record_logical_command_op(
            runtime,
            TERMITE_METAL_PLAN_OP_DECODE_PLE_PROJECTION,
            TERMITE_METAL_OPERATOR_MUL_MM,
            TERMITE_METAL_QUANT_MATMUL_DISPATCH_MM);
        if (termite_metal_encode_q8_0_linear_family(
                runtime,
                command_buffer,
                gated_buffer,
                0,
                proj_view.weight_buffer,
                projected_buffer,
                0,
                rows,
                ple_hidden_size,
                hidden_size,
                TERMITE_METAL_Q8_0_LINEAR_FAMILY_PLE_PROJECTION,
                -18) != 0) return -18;

        if (rows == 1) {
            termite_metal_decode_runtime_record_logical_command_op(
                runtime,
                TERMITE_METAL_PLAN_OP_DECODE_PLE_POST_NORM_RESIDUAL,
                TERMITE_METAL_OPERATOR_UNKNOWN,
                TERMITE_METAL_QUANT_MATMUL_DISPATCH_UNKNOWN);
            if (output_scale_present != 0) {
                if (termite_metal_encode_rms_norm_add_scale_for(
                        runtime,
                        command_buffer,
                        projected_buffer,
                        0,
                        post_norm_slot,
                        hidden_buffer,
                        hidden_offset,
                        output_buffer,
                        output_offset,
                        hidden_size,
                        eps,
                        output_scale,
                        TERMITE_METAL_COMPUTE_SOURCE_PLE,
                        -19) != 0) return -19;
            } else if (termite_metal_encode_rms_norm_add(
                    runtime,
                    command_buffer,
                    projected_buffer,
                    0,
                    post_norm_slot,
                    hidden_buffer,
                    hidden_offset,
                    output_buffer,
                    output_offset,
                    hidden_size,
                    eps,
                    -19) != 0) {
                return -19;
            }
        } else {
            termite_metal_decode_runtime_record_logical_command_op(
                runtime,
                TERMITE_METAL_PLAN_OP_DECODE_PLE_POST_NORM_RESIDUAL,
                TERMITE_METAL_OPERATOR_UNKNOWN,
                TERMITE_METAL_QUANT_MATMUL_DISPATCH_UNKNOWN);
            if (output_scale_present != 0) {
                if (termite_metal_encode_rms_norm_add_scale_rows_for(
                        runtime,
                        command_buffer,
                        projected_buffer,
                        0,
                        post_norm_slot,
                        hidden_buffer,
                        hidden_offset,
                        output_buffer,
                        output_offset,
                        rows,
                        hidden_size,
                        eps,
                        output_scale,
                        TERMITE_METAL_COMPUTE_SOURCE_PLE,
                        -19) != 0) return -19;
            } else {
                if (termite_metal_encode_rms_norm_rows(
                        runtime,
                        command_buffer,
                        projected_buffer,
                        0,
                        post_norm_slot,
                        normed_buffer,
                        0,
                        rows,
                        hidden_size,
                        eps,
                        -19) != 0) return -19;
                if (termite_metal_encode_add_for(
                        runtime,
                        command_buffer,
                        hidden_buffer,
                        hidden_offset,
                        normed_buffer,
                        0,
                        output_buffer,
                        output_offset,
                        hidden_elems,
                        TERMITE_METAL_COMPUTE_SOURCE_PLE,
                        -20) != 0) return -20;
            }
        }

        return 0;
    }
}

int termite_metal_decode_runtime_apply_ple_residual_q8_0_device(
    termite_metal_decode_runtime *runtime,
    void *hidden_handle,
    size_t hidden_offset,
    void *ple_handle,
    size_t ple_offset,
    size_t gate_linear_slot,
    size_t proj_linear_slot,
    size_t post_norm_slot,
    size_t rows,
    size_t hidden_size,
    size_t ple_hidden_size,
    float eps,
    uint32_t activation_kind,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL) return -1;
    const bool frame_owned = (runtime->active_frame_cb == nil);
    id<MTLCommandBuffer> command_buffer = frame_owned
        ? termite_metal_new_command_buffer(runtime->queue, __func__)
        : runtime->active_frame_cb;
    if (command_buffer == nil) return -14;
    const int rc = termite_metal_encode_ple_residual_q8_0_device(
        runtime,
        command_buffer,
        hidden_handle,
        hidden_offset,
        ple_handle,
        ple_offset,
        gate_linear_slot,
        proj_linear_slot,
        post_norm_slot,
        rows,
        hidden_size,
        ple_hidden_size,
        eps,
        activation_kind,
        0,
        1.0f,
        output_handle,
        output_offset
    );
    if (rc != 0) return rc;
    return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -21);
}

int termite_metal_decode_runtime_read_token_id(
    termite_metal_decode_runtime *runtime,
    uint32_t *output_token_id
) {
    if (runtime == NULL || output_token_id == NULL) return -1;
    if (runtime->token_buffer == nil || runtime->token_capacity < sizeof(uint32_t)) return -2;
    if (runtime->token_buffer.storageMode != MTLStorageModeShared) return -3;
    uint32_t *result = (uint32_t *)runtime->token_buffer.contents;
    if (result == NULL) return -4;
    output_token_id[0] = result[0];
    return 0;
}

int termite_metal_decode_runtime_apply_rms_norm_linear(
    termite_metal_decode_runtime *runtime,
    size_t norm_slot,
    size_t linear_slot,
    const float *input,
    size_t hidden_size,
    float eps,
    size_t out_dim,
    float *output
) {
    if (runtime == NULL || input == NULL || output == NULL) return -1;
    if (runtime->rms_norm_pipeline == nil || runtime->linear_pipeline == nil || runtime->linear_reduce_pipeline == nil) return -2;
    if (norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY || linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -3;
    if (hidden_size == 0 || out_dim == 0) return -4;
    if (runtime->rms_norm_slot_prepared[norm_slot] == 0 || runtime->linear_slot_prepared[linear_slot] == 0) return -5;
    if (runtime->rms_norm_hidden_sizes[norm_slot] != hidden_size) return -6;
    if (runtime->linear_in_dims[linear_slot] != hidden_size || runtime->linear_out_dims[linear_slot] != out_dim) return -7;
    if (hidden_size > UINT32_MAX || out_dim > UINT32_MAX) return -8;
    @autoreleasepool {
        const size_t input_bytes = hidden_size * sizeof(float);
        const size_t hidden_bytes = hidden_size * sizeof(float);
        const size_t output_bytes = out_dim * sizeof(float);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)input length:input_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:input length:input_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> hidden_buffer = [runtime->device newBufferWithLength:hidden_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithLength:output_bytes options:MTLResourceStorageModeShared];
        if (input_buffer == nil || hidden_buffer == nil || output_buffer == nil) return -9;
        termite_metal_apply_layer_norm_params norm_params = {
            .hidden_size = (uint32_t)hidden_size,
            .eps = eps,
        };
        termite_metal_apply_linear_params linear_params = {
            .in_dim = (uint32_t)hidden_size,
            .out_dim = (uint32_t)out_dim,
        };
        id<MTLBuffer> norm_params_buffer = [runtime->device newBufferWithBytes:&norm_params length:sizeof(norm_params) options:MTLResourceStorageModeShared];
        id<MTLBuffer> linear_params_buffer = [runtime->device newBufferWithBytes:&linear_params length:sizeof(linear_params) options:MTLResourceStorageModeShared];
        if (norm_params_buffer == nil || linear_params_buffer == nil) return -10;
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -11;

        id<MTLComputeCommandEncoder> norm_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (norm_encoder == nil) return -12;
        [norm_encoder setComputePipelineState:runtime->rms_norm_pipeline];
        [norm_encoder setBuffer:input_buffer offset:0 atIndex:0];
        [norm_encoder setBuffer:runtime->rms_norm_weight_buffers[norm_slot] offset:0 atIndex:1];
        [norm_encoder setBuffer:hidden_buffer offset:0 atIndex:2];
        [norm_encoder setBuffer:norm_params_buffer offset:0 atIndex:3];
        [norm_encoder dispatchThreads:MTLSizeMake(1, 1, 1) threadsPerThreadgroup:MTLSizeMake(1, 1, 1)];
        [norm_encoder endEncoding];

        id<MTLComputeCommandEncoder> linear_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (linear_encoder == nil) return -13;
        const BOOL use_reduce = out_dim >= 128;
        [linear_encoder setComputePipelineState:(use_reduce ? runtime->linear_reduce_pipeline : runtime->linear_pipeline)];
        [linear_encoder setBuffer:hidden_buffer offset:0 atIndex:0];
        [linear_encoder setBuffer:runtime->linear_weight_buffers[linear_slot] offset:0 atIndex:1];
        [linear_encoder setBuffer:runtime->linear_bias_buffers[linear_slot] offset:0 atIndex:2];
        [linear_encoder setBuffer:output_buffer offset:0 atIndex:3];
        [linear_encoder setBuffer:linear_params_buffer offset:0 atIndex:4];
        if (use_reduce) {
            [linear_encoder dispatchThreads:MTLSizeMake(out_dim * 32u, 1, 1) threadsPerThreadgroup:MTLSizeMake(32, 1, 1)];
        } else {
            NSUInteger thread_width = runtime->linear_pipeline.maxTotalThreadsPerThreadgroup;
            if (thread_width == 0) thread_width = 64;
            if (thread_width > out_dim && out_dim > 0) thread_width = out_dim;
            [linear_encoder dispatchThreads:MTLSizeMake(out_dim, 1, 1) threadsPerThreadgroup:MTLSizeMake(thread_width, 1, 1)];
        }
        [linear_encoder endEncoding];

        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        if (command_buffer.status != MTLCommandBufferStatusCompleted) return -14;
        float *result = (float *)output_buffer.contents;
        if (result == NULL) return -15;
        memcpy(output, result, output_bytes);
        return 0;
    }
}

static int termite_metal_decode_runtime_sample_from_logits(
    termite_metal_decode_runtime *runtime,
    const float *logits,
    size_t out_dim,
    float temperature,
    size_t top_k,
    float top_p,
    float min_p,
    float repetition_penalty,
    float frequency_penalty,
    float presence_penalty,
    const uint32_t *penalty_token_ids,
    const uint32_t *penalty_counts,
    size_t penalty_count,
    uint32_t seed,
    uint32_t *output_token_id
) {
    if (runtime == NULL || logits == NULL || output_token_id == NULL) return -1;
    if (runtime->sample_logits_pipeline == nil) return -2;
    if (out_dim == 0 || out_dim > UINT32_MAX) return -3;
    @autoreleasepool {
        const size_t logits_bytes = out_dim * sizeof(float);
        id<MTLBuffer> logits_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)logits length:logits_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (logits_buffer == nil) logits_buffer = [runtime->device newBufferWithBytes:logits length:logits_bytes options:MTLResourceStorageModeShared];
        if (logits_buffer == nil) return -4;
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -6;
        int rc = termite_metal_decode_runtime_encode_sample_from_logits_buffer(
            runtime,
            command_buffer,
            logits_buffer,
            out_dim,
            temperature,
            top_k,
            top_p,
            min_p,
            repetition_penalty,
            frequency_penalty,
            presence_penalty,
            penalty_token_ids,
            penalty_counts,
            penalty_count,
            seed
        );
        if (rc != 0) return rc;
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        if (command_buffer.status != MTLCommandBufferStatusCompleted) return -8;
        uint32_t *result = (uint32_t *)runtime->token_buffer.contents;
        if (result == NULL) return -9;
        output_token_id[0] = result[0];
        return 0;
    }
}

static int termite_metal_decode_runtime_encode_sample_topk_from_logits_buffer(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    id<MTLBuffer> logits_buffer,
    size_t out_dim,
    float temperature,
    size_t top_k,
    float top_p,
    float min_p,
    float repetition_penalty,
    float frequency_penalty,
    float presence_penalty,
    const uint32_t *penalty_token_ids,
    const uint32_t *penalty_counts,
    size_t penalty_count,
    uint32_t seed
) {
    if (runtime == NULL || command_buffer == nil || logits_buffer == nil) return -1;
    if (runtime->sample_topk_partials_pipeline == nil || runtime->sample_topk_reduce_pipeline == nil) return -2;
    if (out_dim == 0 || out_dim > UINT32_MAX || top_k == 0 || top_k > 64 || penalty_count > UINT32_MAX) return -3;

    const size_t block_size = 512;
    const size_t actual_top_k = top_k < out_dim ? top_k : out_dim;
    const size_t block_count = (out_dim + block_size - 1) / block_size;
    const size_t candidate_count = block_count * actual_top_k;
    if (candidate_count == 0 || candidate_count > UINT32_MAX) return -3;
    const size_t partial_bytes = candidate_count * sizeof(float);
    if (termite_metal_decode_runtime_ensure_sample_topk_buffers(runtime, partial_bytes) != 0) return -4;
    const size_t probs_bytes = actual_top_k * sizeof(float);
    if (termite_metal_decode_runtime_ensure_sample_probs_buffer(runtime, probs_bytes) != 0) return -4;
    if (runtime->token_capacity < sizeof(uint32_t) || runtime->token_buffer == nil || runtime->token_buffer.storageMode != MTLStorageModeShared) {
        id<MTLBuffer> token = [runtime->device newBufferWithLength:sizeof(uint32_t) options:MTLResourceStorageModeShared];
        if (token == nil) return -4;
        runtime->token_buffer = token;
        runtime->token_capacity = sizeof(uint32_t);
    }

    termite_metal_sample_logits_params params = {
        .out_dim = (uint32_t)out_dim,
        .temperature = temperature,
        .top_k = (uint32_t)actual_top_k,
        .top_p = top_p,
        .min_p = min_p,
        .repetition_penalty = repetition_penalty,
        .frequency_penalty = frequency_penalty,
        .presence_penalty = presence_penalty,
        .penalty_count = (uint32_t)penalty_count,
        .seed = seed,
    };
    termite_metal_topk_reduce_params reduce_params = {
        .candidate_count = (uint32_t)candidate_count,
        .top_k = (uint32_t)actual_top_k,
        .top_p = top_p,
        .min_p = min_p,
        .seed = seed,
    };
    termite_metal_penalty_entry *entries = NULL;
    if (penalty_count > 0) {
        entries = calloc(penalty_count, sizeof(termite_metal_penalty_entry));
        if (entries == NULL) return -10;
        for (size_t i = 0; i < penalty_count; ++i) {
            entries[i].token_id = penalty_token_ids[i];
            entries[i].count = penalty_counts[i];
        }
    }

    id<MTLComputeCommandEncoder> partial_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
    if (partial_encoder == nil) {
        if (entries != NULL) free(entries);
        return -7;
    }
    [partial_encoder setComputePipelineState:runtime->sample_topk_partials_pipeline];
    [partial_encoder setBuffer:logits_buffer offset:0 atIndex:0];
    [partial_encoder setBuffer:runtime->sample_topk_values_buffer offset:0 atIndex:1];
    [partial_encoder setBuffer:runtime->sample_topk_ids_buffer offset:0 atIndex:2];
    [partial_encoder setBytes:&params length:sizeof(params) atIndex:3];
    if (entries != NULL) {
        [partial_encoder setBytes:entries length:penalty_count * sizeof(termite_metal_penalty_entry) atIndex:4];
    } else {
        termite_metal_penalty_entry empty_penalty = { 0, 0 };
        [partial_encoder setBytes:&empty_penalty length:sizeof(empty_penalty) atIndex:4];
    }
    [partial_encoder dispatchThreads:MTLSizeMake(block_count, 1, 1) threadsPerThreadgroup:MTLSizeMake(1, 1, 1)];
    [partial_encoder endEncoding];

    id<MTLComputeCommandEncoder> reduce_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
    if (reduce_encoder == nil) {
        if (entries != NULL) free(entries);
        return -7;
    }
    [reduce_encoder setComputePipelineState:runtime->sample_topk_reduce_pipeline];
    [reduce_encoder setBuffer:runtime->sample_topk_values_buffer offset:0 atIndex:0];
    [reduce_encoder setBuffer:runtime->sample_topk_ids_buffer offset:0 atIndex:1];
    [reduce_encoder setBuffer:runtime->scratch_buffer offset:0 atIndex:2];
    [reduce_encoder setBuffer:runtime->token_buffer offset:0 atIndex:3];
    [reduce_encoder setBytes:&reduce_params length:sizeof(reduce_params) atIndex:4];
    [reduce_encoder dispatchThreads:MTLSizeMake(1, 1, 1) threadsPerThreadgroup:MTLSizeMake(1, 1, 1)];
    [reduce_encoder endEncoding];
    if (entries != NULL) free(entries);
    return 0;
}

static int termite_metal_decode_runtime_encode_sample_from_logits_buffer(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    id<MTLBuffer> logits_buffer,
    size_t out_dim,
    float temperature,
    size_t top_k,
    float top_p,
    float min_p,
    float repetition_penalty,
    float frequency_penalty,
    float presence_penalty,
    const uint32_t *penalty_token_ids,
    const uint32_t *penalty_counts,
    size_t penalty_count,
    uint32_t seed
) {
    if (runtime == NULL || command_buffer == nil || logits_buffer == nil) return -1;
    if (runtime->sample_logits_pipeline == nil) return -2;
    if (out_dim == 0 || out_dim > UINT32_MAX) return -3;
    if (top_k > 0 && top_k <= 64) {
        return termite_metal_decode_runtime_encode_sample_topk_from_logits_buffer(
            runtime,
            command_buffer,
            logits_buffer,
            out_dim,
            temperature,
            top_k,
            top_p,
            min_p,
            repetition_penalty,
            frequency_penalty,
            presence_penalty,
            penalty_token_ids,
            penalty_counts,
            penalty_count,
            seed
        );
    }

    const size_t logits_bytes = out_dim * sizeof(float);
    if (termite_metal_decode_runtime_ensure_sample_probs_buffer(runtime, logits_bytes) != 0) return -4;
    if (runtime->token_capacity < sizeof(uint32_t) || runtime->token_buffer == nil || runtime->token_buffer.storageMode != MTLStorageModeShared) {
        id<MTLBuffer> token = [runtime->device newBufferWithLength:sizeof(uint32_t) options:MTLResourceStorageModeShared];
        if (token == nil) return -4;
        runtime->token_buffer = token;
        runtime->token_capacity = sizeof(uint32_t);
    }

    termite_metal_sample_logits_params params = {
        .out_dim = (uint32_t)out_dim,
        .temperature = temperature,
        .top_k = (uint32_t)top_k,
        .top_p = top_p,
        .min_p = min_p,
        .repetition_penalty = repetition_penalty,
        .frequency_penalty = frequency_penalty,
        .presence_penalty = presence_penalty,
        .penalty_count = (uint32_t)penalty_count,
        .seed = seed,
    };
    termite_metal_penalty_entry *entries = NULL;
    if (penalty_count > 0) {
        entries = calloc(penalty_count, sizeof(termite_metal_penalty_entry));
        if (entries == NULL) return -10;
        for (size_t i = 0; i < penalty_count; ++i) {
            entries[i].token_id = penalty_token_ids[i];
            entries[i].count = penalty_counts[i];
        }
    }
    id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
    if (encoder == nil) {
        if (entries != NULL) free(entries);
        return -7;
    }
    [encoder setComputePipelineState:runtime->sample_logits_pipeline];
    [encoder setBuffer:logits_buffer offset:0 atIndex:0];
    [encoder setBuffer:runtime->scratch_buffer offset:0 atIndex:1];
    [encoder setBuffer:runtime->token_buffer offset:0 atIndex:2];
    [encoder setBytes:&params length:sizeof(params) atIndex:3];
    if (entries != NULL) {
        [encoder setBytes:entries length:penalty_count * sizeof(termite_metal_penalty_entry) atIndex:4];
    } else {
        termite_metal_penalty_entry empty_penalty = { 0, 0 };
        [encoder setBytes:&empty_penalty length:sizeof(empty_penalty) atIndex:4];
    }
    [encoder dispatchThreads:MTLSizeMake(1, 1, 1) threadsPerThreadgroup:MTLSizeMake(1, 1, 1)];
    [encoder endEncoding];
    if (entries != NULL) free(entries);
    return 0;
}

int termite_metal_decode_runtime_sample_from_logits_device(
    termite_metal_decode_runtime *runtime,
    void *logits_handle,
    size_t logits_offset,
    size_t out_dim,
    float temperature,
    size_t top_k,
    float top_p,
    float min_p,
    float repetition_penalty,
    float frequency_penalty,
    float presence_penalty,
    const uint32_t *penalty_token_ids,
    const uint32_t *penalty_counts,
    size_t penalty_count,
    uint32_t seed,
    uint32_t *output_token_id
) {
    if (runtime == NULL || logits_handle == NULL || output_token_id == NULL) return -1;
    if (runtime->sample_logits_pipeline == nil) return -2;
    if (out_dim == 0 || out_dim > UINT32_MAX) return -3;
    @autoreleasepool {
        id<MTLBuffer> logits_buffer = (__bridge id<MTLBuffer>)logits_handle;
        const size_t logits_bytes = out_dim * sizeof(float);
        if (logits_offset + logits_bytes > logits_buffer.length) return -4;
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -6;
        int rc = termite_metal_decode_runtime_encode_sample_from_logits_buffer(
            runtime,
            command_buffer,
            logits_buffer,
            out_dim,
            temperature,
            top_k,
            top_p,
            min_p,
            repetition_penalty,
            frequency_penalty,
            presence_penalty,
            penalty_token_ids,
            penalty_counts,
            penalty_count,
            seed
        );
        if (rc != 0) return rc;
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        if (command_buffer.status != MTLCommandBufferStatusCompleted) return -8;
        uint32_t *result = (uint32_t *)runtime->token_buffer.contents;
        if (result == NULL) return -9;
        output_token_id[0] = result[0];
        return 0;
    }
}

int termite_metal_decode_runtime_apply_layer_norm_linear_sample_device(
    termite_metal_decode_runtime *runtime,
    size_t norm_slot,
    size_t linear_slot,
    const float *input,
    size_t hidden_size,
    float eps,
    size_t out_dim,
    float temperature,
    size_t top_k,
    float top_p,
    float min_p,
    float repetition_penalty,
    float frequency_penalty,
    float presence_penalty,
    const uint32_t *penalty_token_ids,
    const uint32_t *penalty_counts,
    size_t penalty_count,
    uint32_t seed,
    uint32_t *output_token_id
) {
    if (runtime == NULL || input == NULL || output_token_id == NULL) return -1;
    if (runtime->layer_norm_pipeline == nil || runtime->linear_pipeline == nil || runtime->linear_reduce_pipeline == nil) return -2;
    if (norm_slot >= TERMITE_METAL_LAYER_NORM_SLOT_CAPACITY || linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -3;
    if (hidden_size == 0 || out_dim == 0) return -4;
    if (runtime->layer_norm_slot_prepared[norm_slot] == 0 || runtime->linear_slot_prepared[linear_slot] == 0) return -5;
    if (runtime->layer_norm_hidden_sizes[norm_slot] != hidden_size) return -6;
    if (runtime->linear_in_dims[linear_slot] != hidden_size || runtime->linear_out_dims[linear_slot] != out_dim) return -7;
    if (hidden_size > UINT32_MAX || out_dim > UINT32_MAX) return -8;
    @autoreleasepool {
        const size_t input_bytes = hidden_size * sizeof(float);
        const size_t hidden_bytes = hidden_size * sizeof(float);
        const size_t logits_bytes = out_dim * sizeof(float);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)input length:input_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:input length:input_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> hidden_buffer = [runtime->device newBufferWithLength:hidden_bytes options:MTLResourceStorageModePrivate];
        if (termite_metal_decode_runtime_ensure_sample_logits_buffer(runtime, logits_bytes) != 0) return -9;
        if (input_buffer == nil || hidden_buffer == nil || runtime->sample_logits_buffer == nil) return -9;
        termite_metal_apply_layer_norm_params norm_params = {
            .hidden_size = (uint32_t)hidden_size,
            .eps = eps,
        };
        termite_metal_apply_linear_params linear_params = {
            .in_dim = (uint32_t)hidden_size,
            .out_dim = (uint32_t)out_dim,
        };
        id<MTLBuffer> norm_params_buffer = [runtime->device newBufferWithBytes:&norm_params length:sizeof(norm_params) options:MTLResourceStorageModeShared];
        id<MTLBuffer> linear_params_buffer = [runtime->device newBufferWithBytes:&linear_params length:sizeof(linear_params) options:MTLResourceStorageModeShared];
        if (norm_params_buffer == nil || linear_params_buffer == nil) return -10;
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -11;

        id<MTLComputeCommandEncoder> norm_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (norm_encoder == nil) return -12;
        [norm_encoder setComputePipelineState:runtime->layer_norm_pipeline];
        [norm_encoder setBuffer:input_buffer offset:0 atIndex:0];
        [norm_encoder setBuffer:runtime->layer_norm_weight_buffers[norm_slot] offset:0 atIndex:1];
        [norm_encoder setBuffer:runtime->layer_norm_bias_buffers[norm_slot] offset:0 atIndex:2];
        [norm_encoder setBuffer:hidden_buffer offset:0 atIndex:3];
        [norm_encoder setBuffer:norm_params_buffer offset:0 atIndex:4];
        [norm_encoder dispatchThreads:MTLSizeMake(1, 1, 1) threadsPerThreadgroup:MTLSizeMake(1, 1, 1)];
        [norm_encoder endEncoding];

        id<MTLComputeCommandEncoder> linear_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (linear_encoder == nil) return -13;
        const BOOL use_reduce = out_dim >= 128;
        [linear_encoder setComputePipelineState:(use_reduce ? runtime->linear_reduce_pipeline : runtime->linear_pipeline)];
        [linear_encoder setBuffer:hidden_buffer offset:0 atIndex:0];
        [linear_encoder setBuffer:runtime->linear_weight_buffers[linear_slot] offset:0 atIndex:1];
        [linear_encoder setBuffer:runtime->linear_bias_buffers[linear_slot] offset:0 atIndex:2];
        [linear_encoder setBuffer:runtime->sample_logits_buffer offset:0 atIndex:3];
        [linear_encoder setBuffer:linear_params_buffer offset:0 atIndex:4];
        if (use_reduce) {
            [linear_encoder dispatchThreads:MTLSizeMake(out_dim * 32u, 1, 1) threadsPerThreadgroup:MTLSizeMake(32, 1, 1)];
        } else {
            NSUInteger thread_width = runtime->linear_pipeline.maxTotalThreadsPerThreadgroup;
            if (thread_width == 0) thread_width = 64;
            if (thread_width > out_dim && out_dim > 0) thread_width = out_dim;
            [linear_encoder dispatchThreads:MTLSizeMake(out_dim, 1, 1) threadsPerThreadgroup:MTLSizeMake(thread_width, 1, 1)];
        }
        [linear_encoder endEncoding];

        int rc = termite_metal_decode_runtime_encode_sample_from_logits_buffer(
            runtime,
            command_buffer,
            runtime->sample_logits_buffer,
            out_dim,
            temperature,
            top_k,
            top_p,
            min_p,
            repetition_penalty,
            frequency_penalty,
            presence_penalty,
            penalty_token_ids,
            penalty_counts,
            penalty_count,
            seed
        );
        if (rc != 0) return rc;
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        if (command_buffer.status != MTLCommandBufferStatusCompleted) return -14;
        uint32_t *result = (uint32_t *)runtime->token_buffer.contents;
        if (result == NULL) return -15;
        output_token_id[0] = result[0];
        return 0;
    }
}

int termite_metal_decode_runtime_apply_rms_norm_linear_sample_device(
    termite_metal_decode_runtime *runtime,
    size_t norm_slot,
    size_t linear_slot,
    const float *input,
    size_t hidden_size,
    float eps,
    size_t out_dim,
    float temperature,
    size_t top_k,
    float top_p,
    float min_p,
    float repetition_penalty,
    float frequency_penalty,
    float presence_penalty,
    const uint32_t *penalty_token_ids,
    const uint32_t *penalty_counts,
    size_t penalty_count,
    uint32_t seed,
    uint32_t *output_token_id
) {
    if (runtime == NULL || input == NULL || output_token_id == NULL) return -1;
    if (runtime->rms_norm_pipeline == nil || runtime->linear_pipeline == nil || runtime->linear_reduce_pipeline == nil) return -2;
    if (norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY || linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -3;
    if (hidden_size == 0 || out_dim == 0) return -4;
    if (runtime->rms_norm_slot_prepared[norm_slot] == 0 || runtime->linear_slot_prepared[linear_slot] == 0) return -5;
    if (runtime->rms_norm_hidden_sizes[norm_slot] != hidden_size) return -6;
    if (runtime->linear_in_dims[linear_slot] != hidden_size || runtime->linear_out_dims[linear_slot] != out_dim) return -7;
    if (hidden_size > UINT32_MAX || out_dim > UINT32_MAX) return -8;
    @autoreleasepool {
        const size_t input_bytes = hidden_size * sizeof(float);
        const size_t hidden_bytes = hidden_size * sizeof(float);
        const size_t logits_bytes = out_dim * sizeof(float);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)input length:input_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:input length:input_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> hidden_buffer = [runtime->device newBufferWithLength:hidden_bytes options:MTLResourceStorageModePrivate];
        if (termite_metal_decode_runtime_ensure_sample_logits_buffer(runtime, logits_bytes) != 0) return -9;
        if (input_buffer == nil || hidden_buffer == nil || runtime->sample_logits_buffer == nil) return -9;
        termite_metal_apply_layer_norm_params norm_params = {
            .hidden_size = (uint32_t)hidden_size,
            .eps = eps,
        };
        termite_metal_apply_linear_params linear_params = {
            .in_dim = (uint32_t)hidden_size,
            .out_dim = (uint32_t)out_dim,
        };
        id<MTLBuffer> norm_params_buffer = [runtime->device newBufferWithBytes:&norm_params length:sizeof(norm_params) options:MTLResourceStorageModeShared];
        id<MTLBuffer> linear_params_buffer = [runtime->device newBufferWithBytes:&linear_params length:sizeof(linear_params) options:MTLResourceStorageModeShared];
        if (norm_params_buffer == nil || linear_params_buffer == nil) return -10;
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -11;

        id<MTLComputeCommandEncoder> norm_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (norm_encoder == nil) return -12;
        [norm_encoder setComputePipelineState:runtime->rms_norm_pipeline];
        [norm_encoder setBuffer:input_buffer offset:0 atIndex:0];
        [norm_encoder setBuffer:runtime->rms_norm_weight_buffers[norm_slot] offset:0 atIndex:1];
        [norm_encoder setBuffer:hidden_buffer offset:0 atIndex:2];
        [norm_encoder setBuffer:norm_params_buffer offset:0 atIndex:3];
        [norm_encoder dispatchThreads:MTLSizeMake(1, 1, 1) threadsPerThreadgroup:MTLSizeMake(1, 1, 1)];
        [norm_encoder endEncoding];

        id<MTLComputeCommandEncoder> linear_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (linear_encoder == nil) return -13;
        const BOOL use_reduce = out_dim >= 128;
        [linear_encoder setComputePipelineState:(use_reduce ? runtime->linear_reduce_pipeline : runtime->linear_pipeline)];
        [linear_encoder setBuffer:hidden_buffer offset:0 atIndex:0];
        [linear_encoder setBuffer:runtime->linear_weight_buffers[linear_slot] offset:0 atIndex:1];
        [linear_encoder setBuffer:runtime->linear_bias_buffers[linear_slot] offset:0 atIndex:2];
        [linear_encoder setBuffer:runtime->sample_logits_buffer offset:0 atIndex:3];
        [linear_encoder setBuffer:linear_params_buffer offset:0 atIndex:4];
        if (use_reduce) {
            [linear_encoder dispatchThreads:MTLSizeMake(out_dim * 32u, 1, 1) threadsPerThreadgroup:MTLSizeMake(32, 1, 1)];
        } else {
            NSUInteger thread_width = runtime->linear_pipeline.maxTotalThreadsPerThreadgroup;
            if (thread_width == 0) thread_width = 64;
            if (thread_width > out_dim && out_dim > 0) thread_width = out_dim;
            [linear_encoder dispatchThreads:MTLSizeMake(out_dim, 1, 1) threadsPerThreadgroup:MTLSizeMake(thread_width, 1, 1)];
        }
        [linear_encoder endEncoding];

        int rc = termite_metal_decode_runtime_encode_sample_from_logits_buffer(
            runtime,
            command_buffer,
            runtime->sample_logits_buffer,
            out_dim,
            temperature,
            top_k,
            top_p,
            min_p,
            repetition_penalty,
            frequency_penalty,
            presence_penalty,
            penalty_token_ids,
            penalty_counts,
            penalty_count,
            seed
        );
        if (rc != 0) return rc;
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        if (command_buffer.status != MTLCommandBufferStatusCompleted) return -14;
        uint32_t *result = (uint32_t *)runtime->token_buffer.contents;
        if (result == NULL) return -15;
        output_token_id[0] = result[0];
        return 0;
    }
}

int termite_metal_decode_runtime_apply_activation(
    termite_metal_decode_runtime *runtime,
    uint32_t activation_kind,
    const float *input,
    size_t dim,
    float *output
) {
    if (runtime == NULL || input == NULL || output == NULL) return -1;
    if (runtime->activation_pipeline == nil) return -2;
    if (dim == 0 || dim > UINT32_MAX) return -3;
    @autoreleasepool {
        const size_t bytes = dim * sizeof(float);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)input length:bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:input length:bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithBytesNoCopy:output length:bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) output_buffer = [runtime->device newBufferWithBytes:output length:bytes options:MTLResourceStorageModeShared];
        if (input_buffer == nil || output_buffer == nil) return -4;
        termite_metal_apply_activation_params params = {
            .activation_kind = activation_kind,
            .rows = 1,
            .dim = (uint32_t)dim,
        };
        id<MTLBuffer> params_buffer = [runtime->device newBufferWithBytes:&params length:sizeof(params) options:MTLResourceStorageModeShared];
        if (params_buffer == nil) return -5;
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -6;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (encoder == nil) return -7;
        [encoder setComputePipelineState:runtime->activation_pipeline];
        [encoder setBuffer:input_buffer offset:0 atIndex:0];
        [encoder setBuffer:output_buffer offset:0 atIndex:1];
        [encoder setBuffer:params_buffer offset:0 atIndex:2];
        NSUInteger thread_width = runtime->activation_pipeline.maxTotalThreadsPerThreadgroup;
        if (thread_width == 0) thread_width = 64;
        if (thread_width > dim && dim > 0) thread_width = dim;
        [encoder dispatchThreads:MTLSizeMake(dim, 1, 1) threadsPerThreadgroup:MTLSizeMake(thread_width, 1, 1)];
        [encoder endEncoding];
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -8;
    }
}

int termite_metal_decode_runtime_apply_activation_device(
    termite_metal_decode_runtime *runtime,
    uint32_t activation_kind,
    void *input_handle,
    size_t input_offset,
    size_t rows,
    size_t dim,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || input_handle == NULL || output_handle == NULL) return -1;
    if (runtime->activation_pipeline == nil) return -2;
    if (rows == 0 || dim == 0 || rows > UINT32_MAX || dim > UINT32_MAX) return -3;
    if (rows > SIZE_MAX / dim) return -4;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t total = rows * dim;
        if (total > UINT32_MAX) return -5;
        const size_t bytes = total * sizeof(float);
        if (input_offset + bytes > input_buffer.length) return -6;
        if (output_offset + bytes > output_buffer.length) return -7;
        if (termite_metal_decode_runtime_prepare_planned_compute_unary_accesses(
                runtime,
                input_buffer,
                input_offset,
                bytes,
                output_buffer,
                output_offset,
                bytes,
                -10) != 0)
        {
            return -10;
        }
        termite_metal_apply_activation_params params = {
            .activation_kind = activation_kind,
            .rows = (uint32_t)rows,
            .dim = (uint32_t)dim,
        };
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -9;
        id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
        const BOOL planned_encoder = (encoder != nil);
        if (!planned_encoder) {
            encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        }
        if (encoder == nil) return -10;
        [encoder setComputePipelineState:runtime->activation_pipeline];
        [encoder setBuffer:input_buffer offset:input_offset atIndex:0];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:1];
        [encoder setBytes:&params length:sizeof(params) atIndex:2];
        NSUInteger thread_width = runtime->activation_pipeline.maxTotalThreadsPerThreadgroup;
        if (thread_width == 0) thread_width = 64;
        if (thread_width > total && total > 0) thread_width = total;
        [encoder dispatchThreads:MTLSizeMake(total, 1, 1) threadsPerThreadgroup:MTLSizeMake(thread_width, 1, 1)];
        if (!planned_encoder) [encoder endEncoding];
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -11);
    }
}

int termite_metal_decode_runtime_apply_softmax_device(
    termite_metal_decode_runtime *runtime,
    void *input_handle,
    size_t input_offset,
    size_t rows,
    size_t dim,
    uint32_t log_softmax,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || input_handle == NULL || output_handle == NULL) return -1;
    if (runtime->softmax_pipeline == nil) return -2;
    if (rows == 0 || dim == 0 || rows > UINT32_MAX || dim > UINT32_MAX) return -3;
    if (rows > SIZE_MAX / dim) return -4;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t total = rows * dim;
        if (total > UINT32_MAX) return -5;
        const size_t bytes = total * sizeof(float);
        if (input_offset + bytes > input_buffer.length) return -6;
        if (output_offset + bytes > output_buffer.length) return -7;
        termite_metal_apply_softmax_params params = {
            .rows = (uint32_t)rows,
            .dim = (uint32_t)dim,
            .log_softmax = log_softmax != 0 ? 1u : 0u,
            .reserved = 0,
        };
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -9;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (encoder == nil) return -10;
        [encoder setComputePipelineState:runtime->softmax_pipeline];
        [encoder setBuffer:input_buffer offset:input_offset atIndex:0];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:1];
        [encoder setBytes:&params length:sizeof(params) atIndex:2];
        [encoder setThreadgroupMemoryLength:256u * sizeof(float) atIndex:0];
        [encoder dispatchThreadgroups:MTLSizeMake(rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(256, 1, 1)];
        [encoder endEncoding];
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -11);
    }
}

int termite_metal_decode_runtime_reduce_last_dim_device(
    termite_metal_decode_runtime *runtime,
    void *input_handle,
    size_t input_offset,
    size_t rows,
    size_t dim,
    uint32_t kind,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || input_handle == NULL || output_handle == NULL) return -1;
    if (runtime->reduce_last_dim_pipeline == nil) return -2;
    if (rows == 0 || dim == 0 || rows > UINT32_MAX || dim > UINT32_MAX || kind > 2u) return -3;
    if (rows > SIZE_MAX / dim) return -4;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t total = rows * dim;
        if (total > UINT32_MAX) return -5;
        const size_t input_bytes = total * sizeof(float);
        const size_t output_bytes = rows * sizeof(float);
        if (input_offset + input_bytes > input_buffer.length) return -6;
        if (output_offset + output_bytes > output_buffer.length) return -7;
        termite_metal_reduce_last_dim_params params = {
            .rows = (uint32_t)rows,
            .dim = (uint32_t)dim,
            .kind = kind,
            .reserved = 0,
        };
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -9;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (encoder == nil) return -10;
        [encoder setComputePipelineState:runtime->reduce_last_dim_pipeline];
        [encoder setBuffer:input_buffer offset:input_offset atIndex:0];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:1];
        [encoder setBytes:&params length:sizeof(params) atIndex:2];
        [encoder setThreadgroupMemoryLength:256u * sizeof(float) atIndex:0];
        [encoder dispatchThreadgroups:MTLSizeMake(rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(256, 1, 1)];
        [encoder endEncoding];
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -11);
    }
}

int termite_metal_decode_runtime_multiply_reduce_last_dim_device(
    termite_metal_decode_runtime *runtime,
    void *lhs_handle,
    size_t lhs_offset,
    void *rhs_handle,
    size_t rhs_offset,
    size_t rows,
    size_t dim,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || lhs_handle == NULL || rhs_handle == NULL || output_handle == NULL) return -1;
    if (runtime->multiply_reduce_last_dim_pipeline == nil) return -2;
    if (rows == 0 || dim == 0 || rows > UINT32_MAX || dim > UINT32_MAX) return -3;
    if (rows > SIZE_MAX / dim) return -4;
    @autoreleasepool {
        id<MTLBuffer> lhs_buffer = (__bridge id<MTLBuffer>)lhs_handle;
        id<MTLBuffer> rhs_buffer = (__bridge id<MTLBuffer>)rhs_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t total = rows * dim;
        if (total > UINT32_MAX) return -5;
        const size_t input_bytes = total * sizeof(float);
        const size_t output_bytes = rows * sizeof(float);
        if (lhs_offset + input_bytes > lhs_buffer.length) return -6;
        if (rhs_offset + input_bytes > rhs_buffer.length) return -7;
        if (output_offset + output_bytes > output_buffer.length) return -8;
        termite_metal_reduce_last_dim_params params = {
            .rows = (uint32_t)rows,
            .dim = (uint32_t)dim,
            .kind = 0,
            .reserved = 0,
        };
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -9;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (encoder == nil) return -10;
        [encoder setComputePipelineState:runtime->multiply_reduce_last_dim_pipeline];
        [encoder setBuffer:lhs_buffer offset:lhs_offset atIndex:0];
        [encoder setBuffer:rhs_buffer offset:rhs_offset atIndex:1];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:2];
        [encoder setBytes:&params length:sizeof(params) atIndex:3];
        [encoder setThreadgroupMemoryLength:256u * sizeof(float) atIndex:0];
        [encoder dispatchThreadgroups:MTLSizeMake(rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(256, 1, 1)];
        [encoder endEncoding];
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -11);
    }
}

int termite_metal_decode_runtime_reduce_axis_f32_device(
    termite_metal_decode_runtime *runtime,
    void *input_handle,
    size_t input_offset,
    const uint32_t *out_strides,
    const uint32_t *input_strides_for_out,
    size_t rank,
    size_t input_elems,
    size_t output_elems,
    size_t reduce_dim,
    size_t reduce_stride,
    uint32_t kind,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || input_handle == NULL || output_handle == NULL || out_strides == NULL || input_strides_for_out == NULL) return -1;
    if (runtime->reduce_axis_f32_pipeline == nil) return -2;
    if (rank == 0 || rank > 8 || input_elems == 0 || output_elems == 0 || reduce_dim == 0 || reduce_stride == 0 || kind > 2u) return -3;
    if (input_elems > UINT32_MAX || output_elems > UINT32_MAX || reduce_dim > UINT32_MAX || reduce_stride > UINT32_MAX) return -4;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t input_bytes = input_elems * sizeof(float);
        const size_t output_bytes = output_elems * sizeof(float);
        if (input_offset + input_bytes > input_buffer.length) return -5;
        if (output_offset + output_bytes > output_buffer.length) return -6;
        termite_metal_reduce_axis_f32_params params;
        memset(&params, 0, sizeof(params));
        params.rank = (uint32_t)rank;
        params.out_total = (uint32_t)output_elems;
        params.reduce_dim = (uint32_t)reduce_dim;
        params.reduce_stride = (uint32_t)reduce_stride;
        params.kind = kind;
        for (size_t i = 0; i < rank; ++i) {
            params.out_strides[i] = out_strides[i];
            params.input_strides_for_out[i] = input_strides_for_out[i];
        }
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -9;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (encoder == nil) return -10;
        [encoder setComputePipelineState:runtime->reduce_axis_f32_pipeline];
        [encoder setBuffer:input_buffer offset:input_offset atIndex:0];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:1];
        [encoder setBytes:&params length:sizeof(params) atIndex:2];
        [encoder dispatchThreads:MTLSizeMake(output_elems, 1, 1)
           threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->reduce_axis_f32_pipeline, output_elems), 1, 1)];
        [encoder endEncoding];
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -11);
    }
}

int termite_metal_decode_runtime_broadcast_last_dim_device(
    termite_metal_decode_runtime *runtime,
    void *input_handle,
    size_t input_offset,
    size_t rows,
    size_t in_dim,
    size_t out_dim,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || input_handle == NULL || output_handle == NULL) return -1;
    if (runtime->broadcast_last_dim_pipeline == nil) return -2;
    if (rows == 0 || in_dim == 0 || out_dim == 0 || rows > UINT32_MAX || in_dim > UINT32_MAX || out_dim > UINT32_MAX) return -3;
    if (in_dim != 1 && in_dim != out_dim) return -4;
    if (rows > SIZE_MAX / out_dim || rows > SIZE_MAX / in_dim) return -5;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t input_total = rows * in_dim;
        const size_t output_total = rows * out_dim;
        if (output_total > UINT32_MAX) return -6;
        const size_t input_bytes = input_total * sizeof(float);
        const size_t output_bytes = output_total * sizeof(float);
        if (input_offset + input_bytes > input_buffer.length) return -7;
        if (output_offset + output_bytes > output_buffer.length) return -8;
        termite_metal_broadcast_last_dim_params params = {
            .rows = (uint32_t)rows,
            .in_dim = (uint32_t)in_dim,
            .out_dim = (uint32_t)out_dim,
            .reserved = 0,
        };
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -9;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (encoder == nil) return -10;
        [encoder setComputePipelineState:runtime->broadcast_last_dim_pipeline];
        [encoder setBuffer:input_buffer offset:input_offset atIndex:0];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:1];
        [encoder setBytes:&params length:sizeof(params) atIndex:2];
        [encoder dispatchThreads:MTLSizeMake(output_total, 1, 1)
           threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->broadcast_last_dim_pipeline, output_total), 1, 1)];
        [encoder endEncoding];
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -11);
    }
}

int termite_metal_decode_runtime_broadcast_f32_device(
    termite_metal_decode_runtime *runtime,
    void *input_handle,
    size_t input_offset,
    const uint32_t *out_strides,
    const uint32_t *input_strides_for_out,
    size_t rank,
    size_t input_elems,
    size_t output_elems,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || input_handle == NULL || output_handle == NULL || out_strides == NULL || input_strides_for_out == NULL) return -1;
    if (runtime->broadcast_f32_pipeline == nil) return -2;
    if (rank == 0 || rank > 8 || input_elems == 0 || output_elems == 0 || input_elems > UINT32_MAX || output_elems > UINT32_MAX) return -3;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t input_bytes = input_elems * sizeof(float);
        const size_t output_bytes = output_elems * sizeof(float);
        if (input_offset + input_bytes > input_buffer.length) return -4;
        if (output_offset + output_bytes > output_buffer.length) return -5;
        termite_metal_broadcast_f32_params params;
        memset(&params, 0, sizeof(params));
        params.rank = (uint32_t)rank;
        params.total = (uint32_t)output_elems;
        for (size_t i = 0; i < rank; ++i) {
            params.out_strides[i] = out_strides[i];
            params.input_strides_for_out[i] = input_strides_for_out[i];
        }
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -9;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (encoder == nil) return -10;
        [encoder setComputePipelineState:runtime->broadcast_f32_pipeline];
        [encoder setBuffer:input_buffer offset:input_offset atIndex:0];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:1];
        [encoder setBytes:&params length:sizeof(params) atIndex:2];
        [encoder dispatchThreads:MTLSizeMake(output_elems, 1, 1)
           threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->broadcast_f32_pipeline, output_elems), 1, 1)];
        [encoder endEncoding];
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -11);
    }
}

int termite_metal_decode_runtime_gather_axis0_f32_2d_device(
    termite_metal_decode_runtime *runtime,
    void *input_handle,
    size_t input_offset,
    void *indices_handle,
    size_t indices_offset,
    size_t rows,
    size_t cols,
    size_t index_count,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || input_handle == NULL || indices_handle == NULL || output_handle == NULL) return -1;
    if (runtime->gather_axis0_f32_2d_pipeline == nil) return -2;
    if (rows == 0 || cols == 0 || index_count == 0 || rows > UINT32_MAX || cols > UINT32_MAX || index_count > UINT32_MAX) return -3;
    if (rows > SIZE_MAX / cols || index_count > SIZE_MAX / cols) return -4;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLBuffer> indices_buffer = (__bridge id<MTLBuffer>)indices_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t input_bytes = rows * cols * sizeof(float);
        const size_t indices_bytes = index_count * sizeof(float);
        const size_t output_total = index_count * cols;
        const size_t output_bytes = output_total * sizeof(float);
        if (output_total > UINT32_MAX) return -5;
        if (input_offset + input_bytes > input_buffer.length) return -6;
        if (indices_offset + indices_bytes > indices_buffer.length) return -7;
        if (output_offset + output_bytes > output_buffer.length) return -8;
        termite_metal_gather_axis0_f32_2d_params params = {
            .rows = (uint32_t)rows,
            .cols = (uint32_t)cols,
            .index_count = (uint32_t)index_count,
            .reserved = 0,
        };
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -9;
        termite_metal_planned_encoder_range accesses[3];
        if (termite_metal_planned_range_make(input_buffer, input_offset, input_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[0], -10) != 0 ||
            termite_metal_planned_range_make(indices_buffer, indices_offset, indices_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[1], -10) != 0 ||
            termite_metal_planned_range_make(output_buffer, output_offset, output_bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &accesses[2], -10) != 0 ||
            termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, accesses, 3, -10) != 0)
        {
            return -10;
        }
        BOOL encoder_owned = YES;
        id<MTLComputeCommandEncoder> encoder = termite_metal_scoped_compute_encoder_for(runtime, command_buffer, TERMITE_METAL_COMPUTE_SOURCE_EMBEDDING, &encoder_owned);
        if (encoder == nil) return -10;
        [encoder setComputePipelineState:runtime->gather_axis0_f32_2d_pipeline];
        [encoder setBuffer:input_buffer offset:input_offset atIndex:0];
        [encoder setBuffer:indices_buffer offset:indices_offset atIndex:1];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:2];
        [encoder setBytes:&params length:sizeof(params) atIndex:3];
        [encoder dispatchThreads:MTLSizeMake(output_total, 1, 1)
           threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->gather_axis0_f32_2d_pipeline, output_total), 1, 1)];
        termite_metal_end_scoped_compute_encoder(encoder, encoder_owned);
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -11);
    }
}

int termite_metal_decode_runtime_concat_lastdim_f32_2d_device(
    termite_metal_decode_runtime *runtime,
    void *a_handle,
    size_t a_offset,
    void *b_handle,
    size_t b_offset,
    size_t rows,
    size_t dim_a,
    size_t dim_b,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || a_handle == NULL || b_handle == NULL || output_handle == NULL) return -1;
    if (runtime->concat_lastdim_f32_2d_pipeline == nil) return -2;
    if (rows == 0 || dim_a == 0 || dim_b == 0 || rows > UINT32_MAX || dim_a > UINT32_MAX || dim_b > UINT32_MAX) return -3;
    if (rows > SIZE_MAX / dim_a || rows > SIZE_MAX / dim_b || dim_a > SIZE_MAX - dim_b) return -4;
    const size_t out_dim = dim_a + dim_b;
    if (rows > SIZE_MAX / out_dim) return -5;
    @autoreleasepool {
        id<MTLBuffer> a_buffer = (__bridge id<MTLBuffer>)a_handle;
        id<MTLBuffer> b_buffer = (__bridge id<MTLBuffer>)b_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t a_count = rows * dim_a;
        const size_t b_count = rows * dim_b;
        const size_t output_total = rows * out_dim;
        if (output_total > UINT32_MAX) return -6;
        if (a_offset + a_count * sizeof(float) > a_buffer.length) return -7;
        if (b_offset + b_count * sizeof(float) > b_buffer.length) return -8;
        if (output_offset + output_total * sizeof(float) > output_buffer.length) return -9;
        termite_metal_concat_lastdim_f32_2d_params params = {
            .rows = (uint32_t)rows,
            .dim_a = (uint32_t)dim_a,
            .dim_b = (uint32_t)dim_b,
            .reserved = 0,
        };
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -10;
        termite_metal_planned_encoder_range accesses[3];
        if (termite_metal_planned_range_make(a_buffer, a_offset, a_count * sizeof(float), TERMITE_METAL_PLANNED_RANGE_READ, &accesses[0], -11) != 0 ||
            termite_metal_planned_range_make(b_buffer, b_offset, b_count * sizeof(float), TERMITE_METAL_PLANNED_RANGE_READ, &accesses[1], -11) != 0 ||
            termite_metal_planned_range_make(output_buffer, output_offset, output_total * sizeof(float), TERMITE_METAL_PLANNED_RANGE_WRITE, &accesses[2], -11) != 0 ||
            termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, accesses, 3, -11) != 0)
        {
            return -11;
        }
        BOOL encoder_owned = YES;
        id<MTLComputeCommandEncoder> encoder = termite_metal_scoped_compute_encoder_for(runtime, command_buffer, TERMITE_METAL_COMPUTE_SOURCE_TAIL, &encoder_owned);
        if (encoder == nil) return -11;
        [encoder setComputePipelineState:runtime->concat_lastdim_f32_2d_pipeline];
        [encoder setBuffer:a_buffer offset:a_offset atIndex:0];
        [encoder setBuffer:b_buffer offset:b_offset atIndex:1];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:2];
        [encoder setBytes:&params length:sizeof(params) atIndex:3];
        [encoder dispatchThreads:MTLSizeMake(output_total, 1, 1)
           threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->concat_lastdim_f32_2d_pipeline, output_total), 1, 1)];
        termite_metal_end_scoped_compute_encoder(encoder, encoder_owned);
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -12);
    }
}

int termite_metal_decode_runtime_gliner_word_embeddings_f32_device(
    termite_metal_decode_runtime *runtime,
    void *hidden_handle,
    size_t hidden_offset,
    void *words_mask_handle,
    size_t words_mask_offset,
    size_t batch,
    size_t seq_len,
    size_t hidden_size,
    size_t num_words,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || hidden_handle == NULL || words_mask_handle == NULL || output_handle == NULL) return -1;
    if (runtime->gliner_word_embeddings_f32_pipeline == nil) return -2;
    if (batch == 0 || seq_len == 0 || hidden_size == 0 || num_words == 0 ||
        batch > UINT32_MAX || seq_len > UINT32_MAX || hidden_size > UINT32_MAX || num_words > UINT32_MAX) return -3;
    if (batch > SIZE_MAX / seq_len || batch * seq_len > SIZE_MAX / hidden_size) return -4;
    if (batch > SIZE_MAX / num_words || batch * num_words > SIZE_MAX / hidden_size) return -5;
    @autoreleasepool {
        id<MTLBuffer> hidden_buffer = (__bridge id<MTLBuffer>)hidden_handle;
        id<MTLBuffer> mask_buffer = (__bridge id<MTLBuffer>)words_mask_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t token_count = batch * seq_len;
        const size_t hidden_total = token_count * hidden_size;
        const size_t output_total = batch * num_words * hidden_size;
        if (output_total > UINT32_MAX) return -6;
        if (hidden_offset + hidden_total * sizeof(float) > hidden_buffer.length) return -7;
        if (words_mask_offset + token_count * sizeof(float) > mask_buffer.length) return -8;
        if (output_offset + output_total * sizeof(float) > output_buffer.length) return -9;
        termite_metal_gliner_word_embeddings_f32_params params = {
            .batch = (uint32_t)batch,
            .seq_len = (uint32_t)seq_len,
            .hidden_size = (uint32_t)hidden_size,
            .num_words = (uint32_t)num_words,
        };
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -10;
        termite_metal_planned_encoder_range accesses[3];
        if (termite_metal_planned_range_make(hidden_buffer, hidden_offset, hidden_total * sizeof(float), TERMITE_METAL_PLANNED_RANGE_READ, &accesses[0], -11) != 0 ||
            termite_metal_planned_range_make(mask_buffer, words_mask_offset, token_count * sizeof(float), TERMITE_METAL_PLANNED_RANGE_READ, &accesses[1], -11) != 0 ||
            termite_metal_planned_range_make(output_buffer, output_offset, output_total * sizeof(float), TERMITE_METAL_PLANNED_RANGE_WRITE, &accesses[2], -11) != 0 ||
            termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, accesses, 3, -11) != 0)
        {
            return -11;
        }
        BOOL encoder_owned = YES;
        id<MTLComputeCommandEncoder> encoder = termite_metal_scoped_compute_encoder_for(runtime, command_buffer, TERMITE_METAL_COMPUTE_SOURCE_EMBEDDING, &encoder_owned);
        if (encoder == nil) return -11;
        [encoder setComputePipelineState:runtime->gliner_word_embeddings_f32_pipeline];
        [encoder setBuffer:hidden_buffer offset:hidden_offset atIndex:0];
        [encoder setBuffer:mask_buffer offset:words_mask_offset atIndex:1];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:2];
        [encoder setBytes:&params length:sizeof(params) atIndex:3];
        [encoder dispatchThreads:MTLSizeMake(output_total, 1, 1)
           threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->gliner_word_embeddings_f32_pipeline, output_total), 1, 1)];
        termite_metal_end_scoped_compute_encoder(encoder, encoder_owned);
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -12);
    }
}

int termite_metal_decode_runtime_gliner_gru_combine_f32_device(
    termite_metal_decode_runtime *runtime,
    void *label_handle,
    size_t label_offset,
    void *gi_handle,
    size_t gi_offset,
    void *gh_handle,
    size_t gh_offset,
    size_t rows,
    size_t dim,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || label_handle == NULL || gi_handle == NULL || gh_handle == NULL || output_handle == NULL) return -1;
    if (runtime->gliner_gru_combine_f32_pipeline == nil) return -2;
    if (rows == 0 || dim == 0 || rows > UINT32_MAX || dim > UINT32_MAX) return -3;
    if (rows > SIZE_MAX / dim || rows * dim > SIZE_MAX / 3) return -4;
    @autoreleasepool {
        id<MTLBuffer> label_buffer = (__bridge id<MTLBuffer>)label_handle;
        id<MTLBuffer> gi_buffer = (__bridge id<MTLBuffer>)gi_handle;
        id<MTLBuffer> gh_buffer = (__bridge id<MTLBuffer>)gh_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t count = rows * dim;
        const size_t gate_count = count * 3;
        if (count > UINT32_MAX) return -5;
        if (label_offset + count * sizeof(float) > label_buffer.length) return -6;
        if (gi_offset + gate_count * sizeof(float) > gi_buffer.length) return -7;
        if (gh_offset + gate_count * sizeof(float) > gh_buffer.length) return -8;
        if (output_offset + count * sizeof(float) > output_buffer.length) return -9;
        termite_metal_gliner_gru_combine_f32_params params = {
            .rows = (uint32_t)rows,
            .dim = (uint32_t)dim,
            .reserved0 = 0,
            .reserved1 = 0,
        };
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -10;
        termite_metal_planned_encoder_range accesses[4];
        if (termite_metal_planned_range_make(label_buffer, label_offset, count * sizeof(float), TERMITE_METAL_PLANNED_RANGE_READ, &accesses[0], -11) != 0 ||
            termite_metal_planned_range_make(gi_buffer, gi_offset, gate_count * sizeof(float), TERMITE_METAL_PLANNED_RANGE_READ, &accesses[1], -11) != 0 ||
            termite_metal_planned_range_make(gh_buffer, gh_offset, gate_count * sizeof(float), TERMITE_METAL_PLANNED_RANGE_READ, &accesses[2], -11) != 0 ||
            termite_metal_planned_range_make(output_buffer, output_offset, count * sizeof(float), TERMITE_METAL_PLANNED_RANGE_WRITE, &accesses[3], -11) != 0 ||
            termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, accesses, 4, -11) != 0)
        {
            return -11;
        }
        BOOL encoder_owned = YES;
        id<MTLComputeCommandEncoder> encoder = termite_metal_scoped_compute_encoder_for(runtime, command_buffer, TERMITE_METAL_COMPUTE_SOURCE_TAIL, &encoder_owned);
        if (encoder == nil) return -11;
        [encoder setComputePipelineState:runtime->gliner_gru_combine_f32_pipeline];
        [encoder setBuffer:label_buffer offset:label_offset atIndex:0];
        [encoder setBuffer:gi_buffer offset:gi_offset atIndex:1];
        [encoder setBuffer:gh_buffer offset:gh_offset atIndex:2];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:3];
        [encoder setBytes:&params length:sizeof(params) atIndex:4];
        [encoder dispatchThreads:MTLSizeMake(count, 1, 1)
           threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->gliner_gru_combine_f32_pipeline, count), 1, 1)];
        termite_metal_end_scoped_compute_encoder(encoder, encoder_owned);
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -12);
    }
}

int termite_metal_decode_runtime_argmax_axis_f32_device(
    termite_metal_decode_runtime *runtime,
    void *input_handle,
    size_t input_offset,
    size_t outer,
    size_t axis_dim,
    size_t inner,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || input_handle == NULL || output_handle == NULL) return -1;
    if (runtime->argmax_axis_f32_pipeline == nil) return -2;
    if (outer == 0 || axis_dim == 0 || inner == 0 || outer > UINT32_MAX || axis_dim > UINT32_MAX || inner > UINT32_MAX) return -3;
    if (outer > SIZE_MAX / axis_dim || outer * axis_dim > SIZE_MAX / inner || outer > SIZE_MAX / inner) return -4;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t input_total = outer * axis_dim * inner;
        const size_t output_total = outer * inner;
        if (output_total > UINT32_MAX) return -5;
        if (input_offset + input_total * sizeof(float) > input_buffer.length) return -6;
        if (output_offset + output_total * sizeof(float) > output_buffer.length) return -7;
        termite_metal_argmax_axis_f32_params params = {
            .outer = (uint32_t)outer,
            .axis_dim = (uint32_t)axis_dim,
            .inner = (uint32_t)inner,
            .reserved = 0,
        };
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -9;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (encoder == nil) return -10;
        [encoder setComputePipelineState:runtime->argmax_axis_f32_pipeline];
        [encoder setBuffer:input_buffer offset:input_offset atIndex:0];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:1];
        [encoder setBytes:&params length:sizeof(params) atIndex:2];
        [encoder dispatchThreads:MTLSizeMake(output_total, 1, 1)
           threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->argmax_axis_f32_pipeline, output_total), 1, 1)];
        [encoder endEncoding];
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -11);
    }
}

int termite_metal_decode_runtime_convert_dtype_f32_device(
    termite_metal_decode_runtime *runtime,
    void *input_handle,
    size_t input_offset,
    size_t elem_count,
    uint32_t kind,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || input_handle == NULL || output_handle == NULL) return -1;
    if (runtime->convert_dtype_f32_pipeline == nil) return -2;
    if (elem_count == 0 || elem_count > UINT32_MAX || kind > 2u) return -3;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t bytes = elem_count * sizeof(float);
        if (input_offset + bytes > input_buffer.length) return -4;
        if (output_offset + bytes > output_buffer.length) return -5;
        termite_metal_convert_dtype_f32_params params = {
            .elem_count = (uint32_t)elem_count,
            .kind = kind,
            .reserved0 = 0,
            .reserved1 = 0,
        };
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -9;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (encoder == nil) return -10;
        [encoder setComputePipelineState:runtime->convert_dtype_f32_pipeline];
        [encoder setBuffer:input_buffer offset:input_offset atIndex:0];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:1];
        [encoder setBytes:&params length:sizeof(params) atIndex:2];
        [encoder dispatchThreads:MTLSizeMake(elem_count, 1, 1)
           threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->convert_dtype_f32_pipeline, elem_count), 1, 1)];
        [encoder endEncoding];
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -11);
    }
}

int termite_metal_decode_runtime_sdpa_f32_device(
    termite_metal_decode_runtime *runtime,
    void *q_handle,
    size_t q_offset,
    void *k_handle,
    size_t k_offset,
    void *v_handle,
    size_t v_offset,
    void *bias_handle,
    size_t bias_offset,
    void *mask_handle,
    size_t mask_offset,
    size_t batch,
    size_t seq_len,
    size_t num_heads,
    size_t head_dim,
    uint32_t bias_mode,
    uint32_t has_mask,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || q_handle == NULL || k_handle == NULL || v_handle == NULL || output_handle == NULL) return -1;
    if (runtime->sdpa_f32_pipeline == nil) return -2;
    if (batch == 0 || seq_len == 0 || num_heads == 0 || head_dim == 0 || bias_mode > 3u || has_mask > 1u) return -3;
    if (bias_mode != 0u && bias_handle == NULL) return -4;
    if (has_mask != 0u && mask_handle == NULL) return -5;
    if (batch > UINT32_MAX || seq_len > UINT32_MAX || num_heads > UINT32_MAX || head_dim > UINT32_MAX) return -6;
    if (batch > SIZE_MAX / num_heads || batch * num_heads > SIZE_MAX / seq_len || batch * num_heads * seq_len > SIZE_MAX / head_dim) return -7;
    @autoreleasepool {
        id<MTLBuffer> q_buffer = (__bridge id<MTLBuffer>)q_handle;
        id<MTLBuffer> k_buffer = (__bridge id<MTLBuffer>)k_handle;
        id<MTLBuffer> v_buffer = (__bridge id<MTLBuffer>)v_handle;
        id<MTLBuffer> bias_buffer = bias_handle == NULL ? nil : (__bridge id<MTLBuffer>)bias_handle;
        id<MTLBuffer> mask_buffer = mask_handle == NULL ? nil : (__bridge id<MTLBuffer>)mask_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        id<MTLBuffer> optional_dummy_buffer = nil;
        if (bias_buffer == nil || mask_buffer == nil) {
            optional_dummy_buffer = [runtime->device newBufferWithLength:sizeof(float) options:MTLResourceStorageModeShared];
            if (optional_dummy_buffer == nil) return -17;
        }
        if (bias_buffer == nil) bias_buffer = optional_dummy_buffer;
        if (mask_buffer == nil) mask_buffer = optional_dummy_buffer;
        const size_t total = batch * num_heads * seq_len * head_dim;
        const size_t bytes = total * sizeof(float);
        if (total > UINT32_MAX) return -8;
        if (q_offset + bytes > q_buffer.length || k_offset + bytes > k_buffer.length || v_offset + bytes > v_buffer.length) return -9;
        if (output_offset + bytes > output_buffer.length) return -10;
        if (bias_mode != 0u) {
            const size_t bias_heads = bias_mode == 1u ? num_heads : (bias_mode == 2u ? batch * num_heads : batch);
            if (bias_heads > SIZE_MAX / seq_len || bias_heads * seq_len > SIZE_MAX / seq_len) return -11;
            if (bias_offset + bias_heads * seq_len * seq_len * sizeof(float) > bias_buffer.length) return -12;
        }
        if (has_mask != 0u && mask_offset + batch * seq_len * sizeof(float) > mask_buffer.length) return -13;
        termite_metal_sdpa_f32_params params = {
            .batch = (uint32_t)batch,
            .seq_len = (uint32_t)seq_len,
            .num_heads = (uint32_t)num_heads,
            .head_dim = (uint32_t)head_dim,
            .bias_mode = bias_mode,
            .has_mask = has_mask,
            .reserved0 = 0,
            .reserved1 = 0,
        };
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -14;
        const size_t bias_bytes = bias_mode != 0u
            ? ((bias_mode == 1u ? num_heads : (bias_mode == 2u ? batch * num_heads : batch)) * seq_len * seq_len * sizeof(float))
            : sizeof(float);
        const size_t mask_bytes = has_mask != 0u ? batch * seq_len * sizeof(float) : sizeof(float);
        termite_metal_planned_encoder_range accesses[6];
        if (termite_metal_planned_range_make(q_buffer, q_offset, bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[0], -15) != 0 ||
            termite_metal_planned_range_make(k_buffer, k_offset, bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[1], -15) != 0 ||
            termite_metal_planned_range_make(v_buffer, v_offset, bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[2], -15) != 0 ||
            termite_metal_planned_range_make(bias_buffer, bias_offset, bias_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[3], -15) != 0 ||
            termite_metal_planned_range_make(mask_buffer, mask_offset, mask_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[4], -15) != 0 ||
            termite_metal_planned_range_make(output_buffer, output_offset, bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &accesses[5], -15) != 0 ||
            termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, accesses, 6, -15) != 0)
        {
            return -15;
        }
        BOOL encoder_owned = YES;
        id<MTLComputeCommandEncoder> encoder = termite_metal_scoped_compute_encoder_for(runtime, command_buffer, TERMITE_METAL_COMPUTE_SOURCE_ATTENTION, &encoder_owned);
        if (encoder == nil) return -15;
        [encoder setComputePipelineState:runtime->sdpa_f32_pipeline];
        [encoder setBuffer:q_buffer offset:q_offset atIndex:0];
        [encoder setBuffer:k_buffer offset:k_offset atIndex:1];
        [encoder setBuffer:v_buffer offset:v_offset atIndex:2];
        [encoder setBuffer:bias_buffer offset:bias_offset atIndex:3];
        [encoder setBuffer:mask_buffer offset:mask_offset atIndex:4];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:5];
        [encoder setBytes:&params length:sizeof(params) atIndex:6];
        [encoder dispatchThreads:MTLSizeMake(total, 1, 1)
           threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->sdpa_f32_pipeline, total), 1, 1)];
        termite_metal_end_scoped_compute_encoder(encoder, encoder_owned);
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -16);
    }
}

int termite_metal_decode_runtime_disentangled_relative_attention_f32_device(
    termite_metal_decode_runtime *runtime,
    void *q_handle,
    size_t q_offset,
    void *k_handle,
    size_t k_offset,
    void *v_handle,
    size_t v_offset,
    void *q_r_handle,
    size_t q_r_offset,
    void *k_r_handle,
    size_t k_r_offset,
    void *mask_handle,
    size_t mask_offset,
    size_t batch,
    size_t seq_len,
    size_t num_heads,
    size_t head_dim,
    uint32_t has_mask,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || q_handle == NULL || k_handle == NULL || v_handle == NULL || q_r_handle == NULL || k_r_handle == NULL || output_handle == NULL) return -1;
    if (runtime->disentangled_relative_attention_f32_pipeline == nil) return -2;
    if (batch == 0 || seq_len == 0 || num_heads == 0 || head_dim == 0 || has_mask > 1u) return -3;
    if (has_mask != 0u && mask_handle == NULL) return -4;
    if (batch > UINT32_MAX || seq_len > UINT32_MAX || num_heads > UINT32_MAX || head_dim > UINT32_MAX) return -5;
    if (batch > SIZE_MAX / seq_len || num_heads > SIZE_MAX / head_dim) return -6;
    if (seq_len > SIZE_MAX / 2u) return -8;
    const size_t hidden = num_heads * head_dim;
    if (batch * seq_len > SIZE_MAX / hidden) return -7;
    const size_t total = batch * seq_len * hidden;
    const size_t rel_len = seq_len * 2u - 1u;
    if (rel_len > SIZE_MAX / hidden) return -9;
    const size_t rel_total = rel_len * hidden;
    if (total > UINT32_MAX) return -10;
    @autoreleasepool {
        id<MTLBuffer> q_buffer = (__bridge id<MTLBuffer>)q_handle;
        id<MTLBuffer> k_buffer = (__bridge id<MTLBuffer>)k_handle;
        id<MTLBuffer> v_buffer = (__bridge id<MTLBuffer>)v_handle;
        id<MTLBuffer> q_r_buffer = (__bridge id<MTLBuffer>)q_r_handle;
        id<MTLBuffer> k_r_buffer = (__bridge id<MTLBuffer>)k_r_handle;
        id<MTLBuffer> mask_buffer = mask_handle == NULL ? nil : (__bridge id<MTLBuffer>)mask_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        id<MTLBuffer> optional_dummy_buffer = nil;
        if (mask_buffer == nil) {
            optional_dummy_buffer = [runtime->device newBufferWithLength:sizeof(float) options:MTLResourceStorageModeShared];
            if (optional_dummy_buffer == nil) return -18;
            mask_buffer = optional_dummy_buffer;
        }
        const size_t bytes = total * sizeof(float);
        const size_t rel_bytes = rel_total * sizeof(float);
        if (q_offset + bytes > q_buffer.length || k_offset + bytes > k_buffer.length || v_offset + bytes > v_buffer.length) return -11;
        if (q_r_offset + rel_bytes > q_r_buffer.length || k_r_offset + rel_bytes > k_r_buffer.length) return -12;
        if (output_offset + bytes > output_buffer.length) return -13;
        if (has_mask != 0u && mask_offset + batch * seq_len * sizeof(float) > mask_buffer.length) return -14;
        termite_metal_disentangled_relative_attention_f32_params params = {
            .batch = (uint32_t)batch,
            .seq_len = (uint32_t)seq_len,
            .num_heads = (uint32_t)num_heads,
            .head_dim = (uint32_t)head_dim,
            .has_mask = has_mask,
            .reserved0 = 0,
            .reserved1 = 0,
            .reserved2 = 0,
        };
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -15;
        if (!frame_owned) {
            if (termite_metal_decode_runtime_retain_frame_resource(runtime, q_buffer) != 0 ||
                termite_metal_decode_runtime_retain_frame_resource(runtime, k_buffer) != 0 ||
                termite_metal_decode_runtime_retain_frame_resource(runtime, v_buffer) != 0 ||
                termite_metal_decode_runtime_retain_frame_resource(runtime, q_r_buffer) != 0 ||
                termite_metal_decode_runtime_retain_frame_resource(runtime, k_r_buffer) != 0 ||
                termite_metal_decode_runtime_retain_frame_resource(runtime, mask_buffer) != 0 ||
                termite_metal_decode_runtime_retain_frame_resource(runtime, output_buffer) != 0) {
                return -15;
            }
        }
        termite_metal_planned_encoder_range accesses[7];
        if (termite_metal_planned_range_make(q_buffer, q_offset, bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[0], -16) != 0 ||
            termite_metal_planned_range_make(k_buffer, k_offset, bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[1], -16) != 0 ||
            termite_metal_planned_range_make(v_buffer, v_offset, bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[2], -16) != 0 ||
            termite_metal_planned_range_make(q_r_buffer, q_r_offset, rel_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[3], -16) != 0 ||
            termite_metal_planned_range_make(k_r_buffer, k_r_offset, rel_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[4], -16) != 0 ||
            termite_metal_planned_range_make(mask_buffer, mask_offset, has_mask != 0u ? batch * seq_len * sizeof(float) : sizeof(float), TERMITE_METAL_PLANNED_RANGE_READ, &accesses[5], -16) != 0 ||
            termite_metal_planned_range_make(output_buffer, output_offset, bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &accesses[6], -16) != 0 ||
            termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, accesses, 7, -16) != 0)
        {
            return -16;
        }
        const BOOL gemm_attention_requested = getenv("TERMITE_METAL_DISABLE_DEBERTA_GEMM_ATTENTION") == NULL &&
            ((batch == 1 && seq_len >= 384) || (batch >= 2 && batch <= 8 && seq_len >= 256));
        const BOOL flash4_attention_requested = getenv("TERMITE_METAL_DISABLE_DEBERTA_FLASH_ATTENTION") == NULL &&
            batch >= 1 && batch <= 8 && seq_len >= 384 && seq_len <= 512 && head_dim <= 256;
        if (flash4_attention_requested &&
            runtime->disentangled_relative_attention_f32_flash4_pipeline != nil)
        {
            BOOL flash_encoder_owned = YES;
            id<MTLComputeCommandEncoder> flash_encoder = termite_metal_scoped_compute_encoder_for(runtime, command_buffer, TERMITE_METAL_COMPUTE_SOURCE_ATTENTION, &flash_encoder_owned);
            if (flash_encoder == nil) return -16;
            [flash_encoder setComputePipelineState:runtime->disentangled_relative_attention_f32_flash4_pipeline];
            [flash_encoder setBuffer:q_buffer offset:q_offset atIndex:0];
            [flash_encoder setBuffer:k_buffer offset:k_offset atIndex:1];
            [flash_encoder setBuffer:v_buffer offset:v_offset atIndex:2];
            [flash_encoder setBuffer:q_r_buffer offset:q_r_offset atIndex:3];
            [flash_encoder setBuffer:k_r_buffer offset:k_r_offset atIndex:4];
            [flash_encoder setBuffer:mask_buffer offset:mask_offset atIndex:5];
            [flash_encoder setBuffer:output_buffer offset:output_offset atIndex:6];
            [flash_encoder setBytes:&params length:sizeof(params) atIndex:7];
            const NSUInteger flash_width = 64u;
            const NSUInteger flash_query_block = 4u;
            const NSUInteger scratch_floats = flash_query_block * seq_len + flash_query_block + flash_query_block * flash_width;
            [flash_encoder setThreadgroupMemoryLength:termite_metal_threadgroup_memory_16(scratch_floats * sizeof(float)) atIndex:0];
            [flash_encoder dispatchThreadgroups:MTLSizeMake((seq_len + flash_query_block - 1u) / flash_query_block, num_heads, batch)
                       threadsPerThreadgroup:MTLSizeMake(flash_width, 1, 1)];
            runtime->deberta_attention_flash_calls += 1;
            termite_metal_end_scoped_compute_encoder(flash_encoder, flash_encoder_owned);
            return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -17);
        }
        if (gemm_attention_requested &&
            runtime->deberta_relative_score_gemm_f32_pipeline != nil &&
            runtime->deberta_relative_score_softmax_pv_f32_pipeline != nil &&
            seq_len <= 1024 &&
            head_dim <= 1024)
        {
            const size_t rel_score_count = batch * num_heads * seq_len * rel_len;
            const size_t cc_score_count = batch * num_heads * seq_len * seq_len;
            if (rel_score_count <= SIZE_MAX / sizeof(float) && cc_score_count <= SIZE_MAX / sizeof(float)) {
                const size_t cc_score_bytes = cc_score_count * sizeof(float);
                const size_t rel_score_bytes = rel_score_count * sizeof(float);
                const size_t gemm_slots[3] = {
                    TERMITE_METAL_GRAPH_PLAN_DEBERTA_CC_SCORE_SLOT,
                    TERMITE_METAL_GRAPH_PLAN_DEBERTA_C2P_SCORE_SLOT,
                    TERMITE_METAL_GRAPH_PLAN_DEBERTA_P2C_SCORE_SLOT,
                };
                const size_t gemm_bytes[3] = { cc_score_bytes, rel_score_bytes, rel_score_bytes };
                if (termite_metal_decode_runtime_reserve_graph_plan_slots(runtime, gemm_slots, gemm_bytes, 3) == 0) {
                    id<MTLBuffer> cc_buffer = runtime->graph_plan_buffers[TERMITE_METAL_GRAPH_PLAN_DEBERTA_CC_SCORE_SLOT];
                    id<MTLBuffer> c2p_buffer = runtime->graph_plan_buffers[TERMITE_METAL_GRAPH_PLAN_DEBERTA_C2P_SCORE_SLOT];
                    id<MTLBuffer> p2c_buffer = runtime->graph_plan_buffers[TERMITE_METAL_GRAPH_PLAN_DEBERTA_P2C_SCORE_SLOT];
                    BOOL gemm_encoder_owned = YES;
                    id<MTLComputeCommandEncoder> gemm_encoder = termite_metal_scoped_compute_encoder_for(runtime, command_buffer, TERMITE_METAL_COMPUTE_SOURCE_ATTENTION, &gemm_encoder_owned);
                    if (gemm_encoder == nil) return -16;
                    termite_metal_deberta_relative_score_gemm_f32_params score_params = {
                        .batch = (uint32_t)batch,
                        .seq_len = (uint32_t)seq_len,
                        .rel_len = (uint32_t)rel_len,
                        .num_heads = (uint32_t)num_heads,
                        .head_dim = (uint32_t)head_dim,
                        .mode = 0,
                        .reserved0 = 0,
                        .reserved1 = 0,
                    };
                    [gemm_encoder setComputePipelineState:runtime->deberta_relative_score_gemm_f32_pipeline];
                    [gemm_encoder setBuffer:q_buffer offset:q_offset atIndex:0];
                    [gemm_encoder setBuffer:k_buffer offset:k_offset atIndex:1];
                    [gemm_encoder setBuffer:q_r_buffer offset:q_r_offset atIndex:2];
                    [gemm_encoder setBuffer:k_r_buffer offset:k_r_offset atIndex:3];
                    [gemm_encoder setBuffer:cc_buffer offset:0 atIndex:4];
                    [gemm_encoder setBytes:&score_params length:sizeof(score_params) atIndex:5];
                    [gemm_encoder dispatchThreads:MTLSizeMake(cc_score_count, 1, 1)
                           threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->deberta_relative_score_gemm_f32_pipeline, cc_score_count), 1, 1)];
                    score_params.mode = 1;
                    [gemm_encoder setBuffer:c2p_buffer offset:0 atIndex:4];
                    [gemm_encoder setBytes:&score_params length:sizeof(score_params) atIndex:5];
                    [gemm_encoder dispatchThreads:MTLSizeMake(rel_score_count, 1, 1)
                           threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->deberta_relative_score_gemm_f32_pipeline, rel_score_count), 1, 1)];
                    score_params.mode = 2;
                    [gemm_encoder setBuffer:p2c_buffer offset:0 atIndex:4];
                    [gemm_encoder setBytes:&score_params length:sizeof(score_params) atIndex:5];
                    [gemm_encoder dispatchThreads:MTLSizeMake(rel_score_count, 1, 1)
                           threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->deberta_relative_score_gemm_f32_pipeline, rel_score_count), 1, 1)];
                    [gemm_encoder setComputePipelineState:runtime->deberta_relative_score_softmax_pv_f32_pipeline];
                    [gemm_encoder setBuffer:cc_buffer offset:0 atIndex:0];
                    [gemm_encoder setBuffer:c2p_buffer offset:0 atIndex:1];
                    [gemm_encoder setBuffer:p2c_buffer offset:0 atIndex:2];
                    [gemm_encoder setBuffer:v_buffer offset:v_offset atIndex:3];
                    [gemm_encoder setBuffer:mask_buffer offset:mask_offset atIndex:4];
                    [gemm_encoder setBuffer:output_buffer offset:output_offset atIndex:5];
                    [gemm_encoder setBytes:&params length:sizeof(params) atIndex:6];
                    const NSUInteger softmax_width = 64u;
                    [gemm_encoder setThreadgroupMemoryLength:termite_metal_threadgroup_memory_16((seq_len + softmax_width) * sizeof(float)) atIndex:0];
                    [gemm_encoder dispatchThreadgroups:MTLSizeMake(seq_len, num_heads, batch)
                           threadsPerThreadgroup:MTLSizeMake(softmax_width, 1, 1)];
                    runtime->deberta_attention_gemm_calls += 1;
                    termite_metal_end_scoped_compute_encoder(gemm_encoder, gemm_encoder_owned);
                    return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -17);
                }
            }
            runtime->deberta_attention_gemm_fallbacks += 1;
        }
        if (getenv("TERMITE_METAL_DISABLE_DEBERTA_TG_ATTENTION") == NULL &&
            runtime->disentangled_relative_attention_f32_tg_pipeline != nil &&
            seq_len <= 512 &&
            head_dim <= 256)
        {
            BOOL tg_encoder_owned = YES;
            id<MTLComputeCommandEncoder> tg_encoder = termite_metal_scoped_compute_encoder_for(runtime, command_buffer, TERMITE_METAL_COMPUTE_SOURCE_ATTENTION, &tg_encoder_owned);
            if (tg_encoder == nil) return -16;
            [tg_encoder setComputePipelineState:runtime->disentangled_relative_attention_f32_tg_pipeline];
            [tg_encoder setBuffer:q_buffer offset:q_offset atIndex:0];
            [tg_encoder setBuffer:k_buffer offset:k_offset atIndex:1];
            [tg_encoder setBuffer:v_buffer offset:v_offset atIndex:2];
            [tg_encoder setBuffer:q_r_buffer offset:q_r_offset atIndex:3];
            [tg_encoder setBuffer:k_r_buffer offset:k_r_offset atIndex:4];
            [tg_encoder setBuffer:mask_buffer offset:mask_offset atIndex:5];
            [tg_encoder setBuffer:output_buffer offset:output_offset atIndex:6];
            [tg_encoder setBytes:&params length:sizeof(params) atIndex:7];
            const NSUInteger tg_width = 64u;
            [tg_encoder setThreadgroupMemoryLength:termite_metal_threadgroup_memory_16((seq_len + tg_width) * sizeof(float)) atIndex:0];
            [tg_encoder dispatchThreadgroups:MTLSizeMake(seq_len, num_heads, batch)
                       threadsPerThreadgroup:MTLSizeMake(tg_width, 1, 1)];
            runtime->deberta_attention_legacy_calls += 1;
            termite_metal_end_scoped_compute_encoder(tg_encoder, tg_encoder_owned);
            return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -17);
        }
        BOOL encoder_owned = YES;
        id<MTLComputeCommandEncoder> encoder = termite_metal_scoped_compute_encoder_for(runtime, command_buffer, TERMITE_METAL_COMPUTE_SOURCE_ATTENTION, &encoder_owned);
        if (encoder == nil) return -16;
        [encoder setComputePipelineState:runtime->disentangled_relative_attention_f32_pipeline];
        [encoder setBuffer:q_buffer offset:q_offset atIndex:0];
        [encoder setBuffer:k_buffer offset:k_offset atIndex:1];
        [encoder setBuffer:v_buffer offset:v_offset atIndex:2];
        [encoder setBuffer:q_r_buffer offset:q_r_offset atIndex:3];
        [encoder setBuffer:k_r_buffer offset:k_r_offset atIndex:4];
        [encoder setBuffer:mask_buffer offset:mask_offset atIndex:5];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:6];
        [encoder setBytes:&params length:sizeof(params) atIndex:7];
        [encoder dispatchThreads:MTLSizeMake(total, 1, 1)
           threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->disentangled_relative_attention_f32_pipeline, total), 1, 1)];
        runtime->deberta_attention_legacy_calls += 1;
        termite_metal_end_scoped_compute_encoder(encoder, encoder_owned);
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -17);
    }
}

int termite_metal_decode_runtime_transpose_f32_device(
    termite_metal_decode_runtime *runtime,
    void *input_handle,
    size_t input_offset,
    const uint32_t *dims,
    const uint32_t *in_strides,
    const uint32_t *out_strides,
    const uint32_t *perm,
    size_t rank,
    size_t total,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || input_handle == NULL || output_handle == NULL || dims == NULL || in_strides == NULL || out_strides == NULL || perm == NULL) return -1;
    if (runtime->transpose_f32_pipeline == nil) return -2;
    if (rank == 0 || rank > 8 || total == 0 || total > UINT32_MAX) return -3;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t bytes = total * sizeof(float);
        if (input_offset + bytes > input_buffer.length) return -4;
        if (output_offset + bytes > output_buffer.length) return -5;
        termite_metal_transpose_f32_params params = { .rank = (uint32_t)rank, .total = (uint32_t)total, .reserved0 = 0, .reserved1 = 0 };
        for (size_t i = 0; i < 8; ++i) {
            params.dims[i] = i < rank ? dims[i] : 1u;
            params.in_strides[i] = i < rank ? in_strides[i] : 0u;
            params.out_strides[i] = i < rank ? out_strides[i] : 0u;
            params.perm[i] = i < rank ? perm[i] : 0u;
        }
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -6;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (encoder == nil) return -7;
        [encoder setComputePipelineState:runtime->transpose_f32_pipeline];
        [encoder setBuffer:input_buffer offset:input_offset atIndex:0];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:1];
        [encoder setBytes:&params length:sizeof(params) atIndex:2];
        [encoder dispatchThreads:MTLSizeMake(total, 1, 1)
           threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->transpose_f32_pipeline, total), 1, 1)];
        [encoder endEncoding];
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -8);
    }
}

int termite_metal_decode_runtime_dot_general_2d_f32_device(
    termite_metal_decode_runtime *runtime,
    void *lhs_handle,
    size_t lhs_offset,
    void *rhs_handle,
    size_t rhs_offset,
    size_t m,
    size_t n,
    size_t k,
    uint32_t rhs_contract_axis,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || lhs_handle == NULL || rhs_handle == NULL || output_handle == NULL) return -1;
    if (runtime->dot_general_2d_f32_pipeline == nil) return -2;
    if (m == 0 || n == 0 || k == 0 || m > UINT32_MAX || n > UINT32_MAX || k > UINT32_MAX || rhs_contract_axis > 1u) return -3;
    if (m > SIZE_MAX / k || m > SIZE_MAX / n || n > SIZE_MAX / k) return -4;
    @autoreleasepool {
        id<MTLBuffer> lhs_buffer = (__bridge id<MTLBuffer>)lhs_handle;
        id<MTLBuffer> rhs_buffer = (__bridge id<MTLBuffer>)rhs_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t lhs_bytes = m * k * sizeof(float);
        const size_t rhs_bytes = n * k * sizeof(float);
        const size_t total = m * n;
        const size_t output_bytes = total * sizeof(float);
        if (total > UINT32_MAX) return -5;
        if (lhs_offset + lhs_bytes > lhs_buffer.length || rhs_offset + rhs_bytes > rhs_buffer.length || output_offset + output_bytes > output_buffer.length) return -6;
        termite_metal_dot_general_2d_f32_params params = {
            .m = (uint32_t)m,
            .n = (uint32_t)n,
            .k = (uint32_t)k,
            .rhs_contract_axis = rhs_contract_axis,
        };
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -7;
        termite_metal_planned_encoder_range accesses[3];
        if (termite_metal_planned_range_make(lhs_buffer, lhs_offset, lhs_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[0], -8) != 0 ||
            termite_metal_planned_range_make(rhs_buffer, rhs_offset, rhs_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[1], -8) != 0 ||
            termite_metal_planned_range_make(output_buffer, output_offset, output_bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &accesses[2], -8) != 0 ||
            termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, accesses, 3, -8) != 0)
        {
            return -8;
        }
        BOOL encoder_owned = YES;
        id<MTLComputeCommandEncoder> encoder = termite_metal_scoped_compute_encoder_for(runtime, command_buffer, TERMITE_METAL_COMPUTE_SOURCE_TAIL, &encoder_owned);
        if (encoder == nil) return -8;
        [encoder setComputePipelineState:runtime->dot_general_2d_f32_pipeline];
        [encoder setBuffer:lhs_buffer offset:lhs_offset atIndex:0];
        [encoder setBuffer:rhs_buffer offset:rhs_offset atIndex:1];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:2];
        [encoder setBytes:&params length:sizeof(params) atIndex:3];
        [encoder dispatchThreads:MTLSizeMake(total, 1, 1)
           threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->dot_general_2d_f32_pipeline, total), 1, 1)];
        termite_metal_end_scoped_compute_encoder(encoder, encoder_owned);
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -9);
    }
}

int termite_metal_decode_runtime_dot_general_batched_f32_device(
    termite_metal_decode_runtime *runtime,
    void *lhs_handle,
    size_t lhs_offset,
    void *rhs_handle,
    size_t rhs_offset,
    size_t batch_count,
    size_t m,
    size_t n,
    size_t k,
    uint32_t rhs_contract_axis,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || lhs_handle == NULL || rhs_handle == NULL || output_handle == NULL) return -1;
    if (runtime->dot_general_batched_f32_pipeline == nil) return -2;
    if (batch_count == 0 || m == 0 || n == 0 || k == 0 || batch_count > UINT32_MAX || m > UINT32_MAX || n > UINT32_MAX || k > UINT32_MAX || rhs_contract_axis > 1u) return -3;
    if (batch_count > SIZE_MAX / m || batch_count * m > SIZE_MAX / k || batch_count > SIZE_MAX / n || batch_count * n > SIZE_MAX / k || batch_count * m > SIZE_MAX / n) return -4;
    @autoreleasepool {
        id<MTLBuffer> lhs_buffer = (__bridge id<MTLBuffer>)lhs_handle;
        id<MTLBuffer> rhs_buffer = (__bridge id<MTLBuffer>)rhs_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t lhs_bytes = batch_count * m * k * sizeof(float);
        const size_t rhs_bytes = batch_count * n * k * sizeof(float);
        const size_t total = batch_count * m * n;
        if (total > UINT32_MAX) return -5;
        if (lhs_offset + lhs_bytes > lhs_buffer.length || rhs_offset + rhs_bytes > rhs_buffer.length || output_offset + total * sizeof(float) > output_buffer.length) return -6;
        termite_metal_dot_general_batched_f32_params params = {
            .batch_count = (uint32_t)batch_count,
            .m = (uint32_t)m,
            .n = (uint32_t)n,
            .k = (uint32_t)k,
            .rhs_contract_axis = rhs_contract_axis,
            .reserved0 = 0,
            .reserved1 = 0,
            .reserved2 = 0,
        };
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -7;
        termite_metal_planned_encoder_range accesses[3];
        if (termite_metal_planned_range_make(lhs_buffer, lhs_offset, lhs_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[0], -8) != 0 ||
            termite_metal_planned_range_make(rhs_buffer, rhs_offset, rhs_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[1], -8) != 0 ||
            termite_metal_planned_range_make(output_buffer, output_offset, total * sizeof(float), TERMITE_METAL_PLANNED_RANGE_WRITE, &accesses[2], -8) != 0 ||
            termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, accesses, 3, -8) != 0)
        {
            return -8;
        }
        BOOL encoder_owned = YES;
        id<MTLComputeCommandEncoder> encoder = termite_metal_scoped_compute_encoder_for(runtime, command_buffer, TERMITE_METAL_COMPUTE_SOURCE_TAIL, &encoder_owned);
        if (encoder == nil) return -8;
        [encoder setComputePipelineState:runtime->dot_general_batched_f32_pipeline];
        [encoder setBuffer:lhs_buffer offset:lhs_offset atIndex:0];
        [encoder setBuffer:rhs_buffer offset:rhs_offset atIndex:1];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:2];
        [encoder setBytes:&params length:sizeof(params) atIndex:3];
        [encoder dispatchThreads:MTLSizeMake(total, 1, 1)
           threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->dot_general_batched_f32_pipeline, total), 1, 1)];
        termite_metal_end_scoped_compute_encoder(encoder, encoder_owned);
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -9);
    }
}

int termite_metal_decode_runtime_conv1d_f32_device(
    termite_metal_decode_runtime *runtime,
    void *input_handle,
    size_t input_offset,
    void *weight_handle,
    size_t weight_offset,
    void *bias_handle,
    size_t bias_offset,
    size_t batch,
    size_t in_channels,
    size_t out_channels,
    size_t time_steps,
    size_t kernel_size,
    size_t stride,
    size_t padding,
    size_t out_time,
    uint32_t has_bias,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || input_handle == NULL || weight_handle == NULL || output_handle == NULL) return -1;
    if (runtime->conv1d_f32_pipeline == nil) return -2;
    if (has_bias != 0u && bias_handle == NULL) return -3;
    if (batch == 0 || in_channels == 0 || out_channels == 0 || time_steps == 0 || kernel_size == 0 || stride == 0 || out_time == 0) return -4;
    if (batch > UINT32_MAX || in_channels > UINT32_MAX || out_channels > UINT32_MAX || time_steps > UINT32_MAX || kernel_size > UINT32_MAX || stride > UINT32_MAX || padding > UINT32_MAX || out_time > UINT32_MAX) return -5;
    if (batch > SIZE_MAX / in_channels || batch * in_channels > SIZE_MAX / time_steps || out_channels > SIZE_MAX / in_channels || out_channels * in_channels > SIZE_MAX / kernel_size || batch > SIZE_MAX / out_channels || batch * out_channels > SIZE_MAX / out_time) return -6;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLBuffer> weight_buffer = (__bridge id<MTLBuffer>)weight_handle;
        id<MTLBuffer> bias_buffer = bias_handle == NULL ? nil : (__bridge id<MTLBuffer>)bias_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t input_bytes = batch * in_channels * time_steps * sizeof(float);
        const size_t weight_bytes = out_channels * in_channels * kernel_size * sizeof(float);
        const size_t total = batch * out_channels * out_time;
        if (total > UINT32_MAX) return -7;
        if (input_offset + input_bytes > input_buffer.length || weight_offset + weight_bytes > weight_buffer.length || output_offset + total * sizeof(float) > output_buffer.length) return -8;
        if (has_bias != 0u && bias_offset + out_channels * sizeof(float) > bias_buffer.length) return -9;
        termite_metal_conv1d_f32_params params = {
            .batch = (uint32_t)batch, .in_channels = (uint32_t)in_channels, .out_channels = (uint32_t)out_channels, .time_steps = (uint32_t)time_steps,
            .kernel_size = (uint32_t)kernel_size, .stride = (uint32_t)stride, .padding = (uint32_t)padding, .out_time = (uint32_t)out_time,
            .has_bias = has_bias, .reserved0 = 0, .reserved1 = 0, .reserved2 = 0,
        };
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -10;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (encoder == nil) return -11;
        [encoder setComputePipelineState:runtime->conv1d_f32_pipeline];
        [encoder setBuffer:input_buffer offset:input_offset atIndex:0];
        [encoder setBuffer:weight_buffer offset:weight_offset atIndex:1];
        [encoder setBuffer:bias_buffer offset:bias_offset atIndex:2];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:3];
        [encoder setBytes:&params length:sizeof(params) atIndex:4];
        [encoder dispatchThreads:MTLSizeMake(total, 1, 1)
           threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->conv1d_f32_pipeline, total), 1, 1)];
        [encoder endEncoding];
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -12);
    }
}

int termite_metal_decode_runtime_conv2d_f32_device(
    termite_metal_decode_runtime *runtime,
    void *input_handle,
    size_t input_offset,
    void *weight_handle,
    size_t weight_offset,
    void *bias_handle,
    size_t bias_offset,
    size_t batch,
    size_t in_channels,
    size_t out_channels,
    size_t height,
    size_t width,
    size_t kernel_h,
    size_t kernel_w,
    size_t stride_h,
    size_t stride_w,
    size_t padding_h,
    size_t padding_w,
    size_t groups,
    size_t out_h,
    size_t out_w,
    uint32_t has_bias,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || input_handle == NULL || weight_handle == NULL || output_handle == NULL) return -1;
    if (runtime->conv2d_f32_pipeline == nil) return -2;
    if (has_bias != 0u && bias_handle == NULL) return -3;
    if (batch == 0 || in_channels == 0 || out_channels == 0 || height == 0 || width == 0 || kernel_h == 0 || kernel_w == 0 || stride_h == 0 || stride_w == 0 || groups == 0 || out_h == 0 || out_w == 0) return -4;
    if (in_channels % groups != 0 || out_channels % groups != 0) return -5;
    if (batch > UINT32_MAX || in_channels > UINT32_MAX || out_channels > UINT32_MAX || height > UINT32_MAX || width > UINT32_MAX || kernel_h > UINT32_MAX || kernel_w > UINT32_MAX || stride_h > UINT32_MAX || stride_w > UINT32_MAX || padding_h > UINT32_MAX || padding_w > UINT32_MAX || groups > UINT32_MAX || out_h > UINT32_MAX || out_w > UINT32_MAX) return -6;
    const size_t ic_per_group = in_channels / groups;
    if (batch > SIZE_MAX / in_channels || batch * in_channels > SIZE_MAX / height || batch * in_channels * height > SIZE_MAX / width) return -7;
    if (out_channels > SIZE_MAX / ic_per_group || out_channels * ic_per_group > SIZE_MAX / kernel_h || out_channels * ic_per_group * kernel_h > SIZE_MAX / kernel_w) return -8;
    if (batch > SIZE_MAX / out_channels || batch * out_channels > SIZE_MAX / out_h || batch * out_channels * out_h > SIZE_MAX / out_w) return -9;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLBuffer> weight_buffer = (__bridge id<MTLBuffer>)weight_handle;
        id<MTLBuffer> bias_buffer = bias_handle == NULL ? nil : (__bridge id<MTLBuffer>)bias_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t input_bytes = batch * in_channels * height * width * sizeof(float);
        const size_t weight_bytes = out_channels * ic_per_group * kernel_h * kernel_w * sizeof(float);
        const size_t total = batch * out_channels * out_h * out_w;
        if (total > UINT32_MAX) return -10;
        if (input_offset + input_bytes > input_buffer.length || weight_offset + weight_bytes > weight_buffer.length || output_offset + total * sizeof(float) > output_buffer.length) return -11;
        if (has_bias != 0u && bias_offset + out_channels * sizeof(float) > bias_buffer.length) return -12;
        termite_metal_conv2d_f32_params params = {
            .batch = (uint32_t)batch, .in_channels = (uint32_t)in_channels, .out_channels = (uint32_t)out_channels, .height = (uint32_t)height, .width = (uint32_t)width,
            .kernel_h = (uint32_t)kernel_h, .kernel_w = (uint32_t)kernel_w, .stride_h = (uint32_t)stride_h, .stride_w = (uint32_t)stride_w, .padding_h = (uint32_t)padding_h, .padding_w = (uint32_t)padding_w,
            .groups = (uint32_t)groups, .out_h = (uint32_t)out_h, .out_w = (uint32_t)out_w, .has_bias = has_bias, .reserved0 = 0,
        };
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -13;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (encoder == nil) return -14;
        [encoder setComputePipelineState:runtime->conv2d_f32_pipeline];
        [encoder setBuffer:input_buffer offset:input_offset atIndex:0];
        [encoder setBuffer:weight_buffer offset:weight_offset atIndex:1];
        [encoder setBuffer:bias_buffer offset:bias_offset atIndex:2];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:3];
        [encoder setBytes:&params length:sizeof(params) atIndex:4];
        [encoder dispatchThreads:MTLSizeMake(total, 1, 1)
           threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->conv2d_f32_pipeline, total), 1, 1)];
        [encoder endEncoding];
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -15);
    }
}

int termite_metal_decode_runtime_apply_activation_multiply_device(
    termite_metal_decode_runtime *runtime,
    uint32_t activation_kind,
    void *gate_handle,
    size_t gate_offset,
    void *up_handle,
    size_t up_offset,
    size_t rows,
    size_t dim,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || gate_handle == NULL || up_handle == NULL || output_handle == NULL) return -1;
    if (runtime->activation_multiply_pipeline == nil) return -2;
    if (rows == 0 || dim == 0 || rows > UINT32_MAX || dim > UINT32_MAX) return -3;
    if (rows > SIZE_MAX / dim) return -4;
    @autoreleasepool {
        id<MTLBuffer> gate_buffer = (__bridge id<MTLBuffer>)gate_handle;
        id<MTLBuffer> up_buffer = (__bridge id<MTLBuffer>)up_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t total = rows * dim;
        if (total > UINT32_MAX) return -5;
        const size_t bytes = total * sizeof(float);
        if (gate_offset + bytes > gate_buffer.length) return -6;
        if (up_offset + bytes > up_buffer.length) return -7;
        if (output_offset + bytes > output_buffer.length) return -8;
        termite_metal_apply_activation_params params = {
            .activation_kind = activation_kind,
            .rows = (uint32_t)rows,
            .dim = (uint32_t)dim,
        };
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -10;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (encoder == nil) return -11;
        [encoder setComputePipelineState:runtime->activation_multiply_pipeline];
        [encoder setBuffer:gate_buffer offset:gate_offset atIndex:0];
        [encoder setBuffer:up_buffer offset:up_offset atIndex:1];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:2];
        [encoder setBytes:&params length:sizeof(params) atIndex:3];
        NSUInteger thread_width = runtime->activation_multiply_pipeline.maxTotalThreadsPerThreadgroup;
        if (thread_width == 0) thread_width = 64;
        if (thread_width > total && total > 0) thread_width = total;
        [encoder dispatchThreads:MTLSizeMake(total, 1, 1) threadsPerThreadgroup:MTLSizeMake(thread_width, 1, 1)];
        [encoder endEncoding];
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -12);
    }
}

int termite_metal_decode_runtime_apply_add(
    termite_metal_decode_runtime *runtime,
    const float *lhs,
    const float *rhs,
    size_t dim,
    float *output
) {
    if (runtime == NULL || lhs == NULL || rhs == NULL || output == NULL) return -1;
    if (runtime->add_pipeline == nil) return -2;
    if (dim == 0 || dim > UINT32_MAX) return -3;
    @autoreleasepool {
        const size_t bytes = dim * sizeof(float);
        id<MTLBuffer> lhs_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)lhs length:bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (lhs_buffer == nil) lhs_buffer = [runtime->device newBufferWithBytes:lhs length:bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> rhs_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)rhs length:bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (rhs_buffer == nil) rhs_buffer = [runtime->device newBufferWithBytes:rhs length:bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithBytesNoCopy:output length:bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) output_buffer = [runtime->device newBufferWithBytes:output length:bytes options:MTLResourceStorageModeShared];
        if (lhs_buffer == nil || rhs_buffer == nil || output_buffer == nil) return -4;
        termite_metal_apply_add_params params = {
            .rows = 1,
            .dim = (uint32_t)dim,
        };
        id<MTLBuffer> params_buffer = [runtime->device newBufferWithBytes:&params length:sizeof(params) options:MTLResourceStorageModeShared];
        if (params_buffer == nil) return -5;
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -6;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (encoder == nil) return -7;
        [encoder setComputePipelineState:runtime->add_pipeline];
        [encoder setBuffer:lhs_buffer offset:0 atIndex:0];
        [encoder setBuffer:rhs_buffer offset:0 atIndex:1];
        [encoder setBuffer:output_buffer offset:0 atIndex:2];
        [encoder setBuffer:params_buffer offset:0 atIndex:3];
        NSUInteger thread_width = runtime->add_pipeline.maxTotalThreadsPerThreadgroup;
        if (thread_width == 0) thread_width = 64;
        if (thread_width > dim && dim > 0) thread_width = dim;
        [encoder dispatchThreads:MTLSizeMake(dim, 1, 1) threadsPerThreadgroup:MTLSizeMake(thread_width, 1, 1)];
        [encoder endEncoding];
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -8;
    }
}

int termite_metal_decode_runtime_training_accumulate_f32(
    termite_metal_decode_runtime *runtime,
    void *accum_handle,
    size_t accum_offset,
    void *grad_handle,
    size_t grad_offset,
    size_t elem_count,
    float scale,
    uint32_t first
) {
    if (runtime == NULL || accum_handle == NULL || grad_handle == NULL) return -1;
    if (runtime->training_accumulate_pipeline == nil) return -2;
    if (elem_count == 0 || elem_count > UINT32_MAX) return -3;
    @autoreleasepool {
        id<MTLBuffer> accum_buffer = (__bridge id<MTLBuffer>)accum_handle;
        id<MTLBuffer> grad_buffer = (__bridge id<MTLBuffer>)grad_handle;
        const size_t bytes = elem_count * sizeof(float);
        if (accum_offset + bytes > accum_buffer.length) return -4;
        if (grad_offset + bytes > grad_buffer.length) return -5;
        termite_metal_training_accumulate_params params = {
            .elem_count = (uint32_t)elem_count,
            .scale = scale,
            .first = first != 0u ? 1u : 0u,
            .reserved = 0u,
        };
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -6;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (encoder == nil) return -7;
        [encoder setComputePipelineState:runtime->training_accumulate_pipeline];
        [encoder setBuffer:accum_buffer offset:accum_offset atIndex:0];
        [encoder setBuffer:grad_buffer offset:grad_offset atIndex:1];
        [encoder setBytes:&params length:sizeof(params) atIndex:2];
        [encoder dispatchThreads:MTLSizeMake(elem_count, 1, 1)
           threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->training_accumulate_pipeline, elem_count), 1, 1)];
        [encoder endEncoding];
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -8);
    }
}

int termite_metal_decode_runtime_training_adamw_f32(
    termite_metal_decode_runtime *runtime,
    void *weight_handle,
    size_t weight_offset,
    void *grad_handle,
    size_t grad_offset,
    void *m_handle,
    size_t m_offset,
    void *v_handle,
    size_t v_offset,
    size_t elem_count,
    float lr,
    float beta1,
    float beta2,
    float eps,
    float weight_decay,
    float bias_correction1,
    float bias_correction2,
    float grad_scale
) {
    if (runtime == NULL || weight_handle == NULL || grad_handle == NULL || m_handle == NULL || v_handle == NULL) return -1;
    if (runtime->training_adamw_pipeline == nil) return -2;
    if (elem_count == 0 || elem_count > UINT32_MAX) return -3;
    if (bias_correction1 == 0.0f || bias_correction2 == 0.0f) return -4;
    @autoreleasepool {
        id<MTLBuffer> weight_buffer = (__bridge id<MTLBuffer>)weight_handle;
        id<MTLBuffer> grad_buffer = (__bridge id<MTLBuffer>)grad_handle;
        id<MTLBuffer> m_buffer = (__bridge id<MTLBuffer>)m_handle;
        id<MTLBuffer> v_buffer = (__bridge id<MTLBuffer>)v_handle;
        const size_t bytes = elem_count * sizeof(float);
        if (weight_offset + bytes > weight_buffer.length) return -5;
        if (grad_offset + bytes > grad_buffer.length) return -6;
        if (m_offset + bytes > m_buffer.length) return -7;
        if (v_offset + bytes > v_buffer.length) return -8;
        termite_metal_training_adamw_params params = {
            .elem_count = (uint32_t)elem_count,
            .lr = lr,
            .beta1 = beta1,
            .beta2 = beta2,
            .eps = eps,
            .weight_decay = weight_decay,
            .bias_correction1 = bias_correction1,
            .bias_correction2 = bias_correction2,
            .grad_scale = grad_scale,
            .reserved0 = 0u,
            .reserved1 = 0u,
            .reserved2 = 0u,
        };
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -9;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (encoder == nil) return -10;
        [encoder setComputePipelineState:runtime->training_adamw_pipeline];
        [encoder setBuffer:weight_buffer offset:weight_offset atIndex:0];
        [encoder setBuffer:grad_buffer offset:grad_offset atIndex:1];
        [encoder setBuffer:m_buffer offset:m_offset atIndex:2];
        [encoder setBuffer:v_buffer offset:v_offset atIndex:3];
        [encoder setBytes:&params length:sizeof(params) atIndex:4];
        [encoder dispatchThreads:MTLSizeMake(elem_count, 1, 1)
           threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->training_adamw_pipeline, elem_count), 1, 1)];
        [encoder endEncoding];
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -11);
    }
}

int termite_metal_decode_runtime_training_sumsq_f32(
    termite_metal_decode_runtime *runtime,
    void *input_handle,
    size_t input_offset,
    size_t elem_count,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || input_handle == NULL || output_handle == NULL) return -1;
    if (runtime->training_sumsq_pipeline == nil) return -2;
    if (elem_count == 0 || elem_count > UINT32_MAX) return -3;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t bytes = elem_count * sizeof(float);
        if (input_offset + bytes > input_buffer.length) return -4;
        if (output_offset + sizeof(float) > output_buffer.length) return -5;
        termite_metal_training_sumsq_params params = {
            .elem_count = (uint32_t)elem_count,
            .reserved0 = 0u,
            .reserved1 = 0u,
            .reserved2 = 0u,
        };
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -6;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (encoder == nil) return -7;
        [encoder setComputePipelineState:runtime->training_sumsq_pipeline];
        [encoder setBuffer:input_buffer offset:input_offset atIndex:0];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:1];
        [encoder setBytes:&params length:sizeof(params) atIndex:2];
        [encoder dispatchThreads:MTLSizeMake(1, 1, 1) threadsPerThreadgroup:MTLSizeMake(1, 1, 1)];
        [encoder endEncoding];
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -8);
    }
}

int termite_metal_decode_runtime_apply_add_device(
    termite_metal_decode_runtime *runtime,
    void *lhs_handle,
    size_t lhs_offset,
    void *rhs_handle,
    size_t rhs_offset,
    size_t dim,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || lhs_handle == NULL || rhs_handle == NULL || output_handle == NULL) return -1;
    if (runtime->add_pipeline == nil) return -2;
    if (dim == 0 || dim > UINT32_MAX) return -3;
    @autoreleasepool {
        id<MTLBuffer> lhs_buffer = (__bridge id<MTLBuffer>)lhs_handle;
        id<MTLBuffer> rhs_buffer = (__bridge id<MTLBuffer>)rhs_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t bytes = dim * sizeof(float);
        if (lhs_offset + bytes > lhs_buffer.length) return -4;
        if (rhs_offset + bytes > rhs_buffer.length) return -5;
        if (output_offset + bytes > output_buffer.length) return -6;
        if (termite_metal_decode_runtime_prepare_planned_compute_binary_accesses(
                runtime,
                lhs_buffer,
                lhs_offset,
                bytes,
                rhs_buffer,
                rhs_offset,
                bytes,
                output_buffer,
                output_offset,
                bytes,
                -9) != 0)
        {
            return -9;
        }
        termite_metal_apply_add_params params = {
            .rows = 1,
            .dim = (uint32_t)dim,
        };
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -8;
        id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
        const BOOL planned_encoder = (encoder != nil);
        if (!planned_encoder) {
            encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        }
        if (encoder == nil) return -9;
        [encoder setComputePipelineState:runtime->add_pipeline];
        [encoder setBuffer:lhs_buffer offset:lhs_offset atIndex:0];
        [encoder setBuffer:rhs_buffer offset:rhs_offset atIndex:1];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:2];
        [encoder setBytes:&params length:sizeof(params) atIndex:3];
        NSUInteger thread_width = runtime->add_pipeline.maxTotalThreadsPerThreadgroup;
        if (thread_width == 0) thread_width = 64;
        if (thread_width > dim && dim > 0) thread_width = dim;
        [encoder dispatchThreads:MTLSizeMake(dim, 1, 1) threadsPerThreadgroup:MTLSizeMake(thread_width, 1, 1)];
        if (!planned_encoder) [encoder endEncoding];
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -10);
    }
}

int termite_metal_decode_runtime_apply_rms_norm_add_device(
    termite_metal_decode_runtime *runtime,
    void *input_handle,
    size_t input_offset,
    size_t norm_slot,
    void *residual_handle,
    size_t residual_offset,
    size_t rows,
    size_t hidden_size,
    float eps,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || input_handle == NULL || residual_handle == NULL || output_handle == NULL) return -1;
    if (rows == 0 || hidden_size == 0 || rows > UINT32_MAX || hidden_size > UINT32_MAX) return -2;
    if (norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return -3;
    if (runtime->rms_norm_slot_prepared[norm_slot] == 0 || runtime->rms_norm_hidden_sizes[norm_slot] != hidden_size) return -4;
    if (runtime->rms_norm_add_scale_pipeline == nil || runtime->rms_norm_add_scale_rows_pipeline == nil) return -5;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLBuffer> residual_buffer = (__bridge id<MTLBuffer>)residual_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        if (rows > SIZE_MAX / hidden_size) return -6;
        const size_t bytes = rows * hidden_size * sizeof(float);
        if (input_offset + bytes > input_buffer.length) return -7;
        if (residual_offset + bytes > residual_buffer.length) return -8;
        if (output_offset + bytes > output_buffer.length) return -9;
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -10;
        const int rc = rows == 1
            ? termite_metal_encode_rms_norm_add_scale_for(
                runtime,
                command_buffer,
                input_buffer,
                input_offset,
                norm_slot,
                residual_buffer,
                residual_offset,
                output_buffer,
                output_offset,
                hidden_size,
                eps,
                1.0f,
                TERMITE_METAL_COMPUTE_SOURCE_RMS_NORM,
                -11)
            : termite_metal_encode_rms_norm_add_scale_rows_for(
                runtime,
                command_buffer,
                input_buffer,
                input_offset,
                norm_slot,
                residual_buffer,
                residual_offset,
                output_buffer,
                output_offset,
                rows,
                hidden_size,
                eps,
                1.0f,
                TERMITE_METAL_COMPUTE_SOURCE_RMS_NORM,
                -12);
        if (rc != 0) return rc;
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -13);
    }
}

int termite_metal_decode_runtime_apply_add_scale_device(
    termite_metal_decode_runtime *runtime,
    void *lhs_handle,
    size_t lhs_offset,
    void *rhs_handle,
    size_t rhs_offset,
    size_t dim,
    float scale,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || lhs_handle == NULL || rhs_handle == NULL || output_handle == NULL) return -1;
    if (runtime->add_scale_pipeline == nil) return -2;
    if (dim == 0 || dim > UINT32_MAX) return -3;
    @autoreleasepool {
        id<MTLBuffer> lhs_buffer = (__bridge id<MTLBuffer>)lhs_handle;
        id<MTLBuffer> rhs_buffer = (__bridge id<MTLBuffer>)rhs_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t bytes = dim * sizeof(float);
        if (lhs_offset + bytes > lhs_buffer.length) return -4;
        if (rhs_offset + bytes > rhs_buffer.length) return -5;
        if (output_offset + bytes > output_buffer.length) return -6;
        if (termite_metal_decode_runtime_prepare_planned_compute_binary_accesses(
                runtime,
                lhs_buffer,
                lhs_offset,
                bytes,
                rhs_buffer,
                rhs_offset,
                bytes,
                output_buffer,
                output_offset,
                bytes,
                -9) != 0)
        {
            return -9;
        }
        termite_metal_apply_add_params params = {
            .rows = 1,
            .dim = (uint32_t)dim,
        };
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -8;
        id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
        const BOOL planned_encoder = (encoder != nil);
        if (!planned_encoder) {
            encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_PLE);
        }
        if (encoder == nil) return -9;
        [encoder setComputePipelineState:runtime->add_scale_pipeline];
        [encoder setBuffer:lhs_buffer offset:lhs_offset atIndex:0];
        [encoder setBuffer:rhs_buffer offset:rhs_offset atIndex:1];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:2];
        [encoder setBytes:&params length:sizeof(params) atIndex:3];
        [encoder setBytes:&scale length:sizeof(scale) atIndex:4];
        [encoder dispatchThreads:MTLSizeMake(dim, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->add_scale_pipeline, dim), 1, 1)];
        if (!planned_encoder) [encoder endEncoding];
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -10);
    }
}

int termite_metal_decode_runtime_apply_scaled_add_scale_device(
    termite_metal_decode_runtime *runtime,
    void *lhs_handle,
    size_t lhs_offset,
    void *rhs_handle,
    size_t rhs_offset,
    size_t dim,
    float lhs_scale,
    float output_scale,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || lhs_handle == NULL || rhs_handle == NULL || output_handle == NULL) return -1;
    if (runtime->scaled_add_scale_pipeline == nil) return -2;
    if (dim == 0 || dim > UINT32_MAX) return -3;
    @autoreleasepool {
        id<MTLBuffer> lhs_buffer = (__bridge id<MTLBuffer>)lhs_handle;
        id<MTLBuffer> rhs_buffer = (__bridge id<MTLBuffer>)rhs_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t bytes = dim * sizeof(float);
        if (lhs_offset + bytes > lhs_buffer.length) return -4;
        if (rhs_offset + bytes > rhs_buffer.length) return -5;
        if (output_offset + bytes > output_buffer.length) return -6;
        if (termite_metal_decode_runtime_prepare_planned_compute_binary_accesses(
                runtime,
                lhs_buffer,
                lhs_offset,
                bytes,
                rhs_buffer,
                rhs_offset,
                bytes,
                output_buffer,
                output_offset,
                bytes,
                -9) != 0)
        {
            return -9;
        }
        termite_metal_apply_add_params params = {
            .rows = 1,
            .dim = (uint32_t)dim,
        };
        const float scales[2] = { lhs_scale, output_scale };
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -8;
        id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
        const BOOL planned_encoder = (encoder != nil);
        if (!planned_encoder) {
            encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_PLE);
        }
        if (encoder == nil) return -9;
        [encoder setComputePipelineState:runtime->scaled_add_scale_pipeline];
        [encoder setBuffer:lhs_buffer offset:lhs_offset atIndex:0];
        [encoder setBuffer:rhs_buffer offset:rhs_offset atIndex:1];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:2];
        [encoder setBytes:&params length:sizeof(params) atIndex:3];
        [encoder setBytes:scales length:sizeof(scales) atIndex:4];
        [encoder dispatchThreads:MTLSizeMake(dim, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->scaled_add_scale_pipeline, dim), 1, 1)];
        if (!planned_encoder) [encoder endEncoding];
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -10);
    }
}

static int termite_metal_decode_runtime_apply_binary_device(
    termite_metal_decode_runtime *runtime,
    id<MTLComputePipelineState> pipeline,
    void *lhs_handle,
    size_t lhs_offset,
    void *rhs_handle,
    size_t rhs_offset,
    size_t dim,
    bool lhs_scalar,
    bool rhs_scalar,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || lhs_handle == NULL || rhs_handle == NULL || output_handle == NULL) return -1;
    if (pipeline == nil) return -2;
    if (dim == 0 || dim > UINT32_MAX) return -3;
    @autoreleasepool {
        id<MTLBuffer> lhs_buffer = (__bridge id<MTLBuffer>)lhs_handle;
        id<MTLBuffer> rhs_buffer = (__bridge id<MTLBuffer>)rhs_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t bytes = dim * sizeof(float);
        const size_t lhs_bytes = lhs_scalar ? sizeof(float) : bytes;
        const size_t rhs_bytes = rhs_scalar ? sizeof(float) : bytes;
        if (lhs_offset + lhs_bytes > lhs_buffer.length) return -4;
        if (rhs_offset + rhs_bytes > rhs_buffer.length) return -5;
        if (output_offset + bytes > output_buffer.length) return -6;
        if (termite_metal_decode_runtime_prepare_planned_compute_binary_accesses(
                runtime,
                lhs_buffer,
                lhs_offset,
                lhs_bytes,
                rhs_buffer,
                rhs_offset,
                rhs_bytes,
                output_buffer,
                output_offset,
                bytes,
                -9) != 0)
        {
            return -9;
        }
        termite_metal_apply_add_params params = {
            .rows = 1,
            .dim = (uint32_t)dim,
            .flags = (lhs_scalar ? 1u : 0u) | (rhs_scalar ? 2u : 0u),
        };
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -8;
        id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
        const BOOL planned_encoder = (encoder != nil);
        if (!planned_encoder) {
            encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        }
        if (encoder == nil) return -9;
        [encoder setComputePipelineState:pipeline];
        [encoder setBuffer:lhs_buffer offset:lhs_offset atIndex:0];
        [encoder setBuffer:rhs_buffer offset:rhs_offset atIndex:1];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:2];
        [encoder setBytes:&params length:sizeof(params) atIndex:3];
        [encoder dispatchThreads:MTLSizeMake(dim, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(pipeline, dim), 1, 1)];
        if (!planned_encoder) [encoder endEncoding];
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -10);
    }
}

int termite_metal_decode_runtime_apply_subtract_device(
    termite_metal_decode_runtime *runtime,
    void *lhs_handle,
    size_t lhs_offset,
    void *rhs_handle,
    size_t rhs_offset,
    size_t dim,
    void *output_handle,
    size_t output_offset
) {
    return termite_metal_decode_runtime_apply_binary_device(runtime, runtime != NULL ? runtime->subtract_pipeline : nil, lhs_handle, lhs_offset, rhs_handle, rhs_offset, dim, false, false, output_handle, output_offset);
}

int termite_metal_decode_runtime_apply_subtract_device_broadcast(
    termite_metal_decode_runtime *runtime,
    void *lhs_handle,
    size_t lhs_offset,
    void *rhs_handle,
    size_t rhs_offset,
    size_t dim,
    int lhs_scalar,
    int rhs_scalar,
    void *output_handle,
    size_t output_offset
) {
    return termite_metal_decode_runtime_apply_binary_device(runtime, runtime != NULL ? runtime->subtract_pipeline : nil, lhs_handle, lhs_offset, rhs_handle, rhs_offset, dim, lhs_scalar != 0, rhs_scalar != 0, output_handle, output_offset);
}

int termite_metal_decode_runtime_apply_divide_device(
    termite_metal_decode_runtime *runtime,
    void *lhs_handle,
    size_t lhs_offset,
    void *rhs_handle,
    size_t rhs_offset,
    size_t dim,
    void *output_handle,
    size_t output_offset
) {
    return termite_metal_decode_runtime_apply_binary_device(runtime, runtime != NULL ? runtime->divide_pipeline : nil, lhs_handle, lhs_offset, rhs_handle, rhs_offset, dim, false, false, output_handle, output_offset);
}

int termite_metal_decode_runtime_apply_divide_device_broadcast(
    termite_metal_decode_runtime *runtime,
    void *lhs_handle,
    size_t lhs_offset,
    void *rhs_handle,
    size_t rhs_offset,
    size_t dim,
    int lhs_scalar,
    int rhs_scalar,
    void *output_handle,
    size_t output_offset
) {
    return termite_metal_decode_runtime_apply_binary_device(runtime, runtime != NULL ? runtime->divide_pipeline : nil, lhs_handle, lhs_offset, rhs_handle, rhs_offset, dim, lhs_scalar != 0, rhs_scalar != 0, output_handle, output_offset);
}

int termite_metal_decode_runtime_apply_divide_device_rhs_repeat(
    termite_metal_decode_runtime *runtime,
    void *lhs_handle,
    size_t lhs_offset,
    void *rhs_handle,
    size_t rhs_offset,
    size_t dim,
    size_t rhs_period,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || lhs_handle == NULL || rhs_handle == NULL || output_handle == NULL) return -1;
    if (runtime->divide_pipeline == nil) return -2;
    if (dim == 0 || rhs_period == 0 || dim % rhs_period != 0 || dim > UINT32_MAX || rhs_period > UINT32_MAX) return -3;
    @autoreleasepool {
        id<MTLBuffer> lhs_buffer = (__bridge id<MTLBuffer>)lhs_handle;
        id<MTLBuffer> rhs_buffer = (__bridge id<MTLBuffer>)rhs_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t bytes = dim * sizeof(float);
        const size_t rhs_bytes = rhs_period * sizeof(float);
        if (lhs_offset + bytes > lhs_buffer.length) return -4;
        if (rhs_offset + rhs_bytes > rhs_buffer.length) return -5;
        if (output_offset + bytes > output_buffer.length) return -6;
        if (termite_metal_decode_runtime_prepare_planned_compute_binary_accesses(
                runtime,
                lhs_buffer,
                lhs_offset,
                bytes,
                rhs_buffer,
                rhs_offset,
                rhs_bytes,
                output_buffer,
                output_offset,
                bytes,
                -9) != 0)
        {
            return -9;
        }
        termite_metal_apply_add_params params = {
            .rows = (uint32_t)(dim / rhs_period),
            .dim = (uint32_t)rhs_period,
            .flags = 4u,
        };
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -8;
        id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
        const BOOL planned_encoder = (encoder != nil);
        if (!planned_encoder) {
            encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        }
        if (encoder == nil) return -9;
        [encoder setComputePipelineState:runtime->divide_pipeline];
        [encoder setBuffer:lhs_buffer offset:lhs_offset atIndex:0];
        [encoder setBuffer:rhs_buffer offset:rhs_offset atIndex:1];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:2];
        [encoder setBytes:&params length:sizeof(params) atIndex:3];
        [encoder dispatchThreads:MTLSizeMake(dim, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->divide_pipeline, dim), 1, 1)];
        if (!planned_encoder) [encoder endEncoding];
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -10);
    }
}

int termite_metal_decode_runtime_apply_less_than_device(
    termite_metal_decode_runtime *runtime,
    void *lhs_handle,
    size_t lhs_offset,
    void *rhs_handle,
    size_t rhs_offset,
    size_t dim,
    void *output_handle,
    size_t output_offset
) {
    return termite_metal_decode_runtime_apply_binary_device(runtime, runtime != NULL ? runtime->less_than_pipeline : nil, lhs_handle, lhs_offset, rhs_handle, rhs_offset, dim, false, false, output_handle, output_offset);
}

int termite_metal_decode_runtime_apply_less_than_device_broadcast(
    termite_metal_decode_runtime *runtime,
    void *lhs_handle,
    size_t lhs_offset,
    void *rhs_handle,
    size_t rhs_offset,
    size_t dim,
    int lhs_scalar,
    int rhs_scalar,
    void *output_handle,
    size_t output_offset
) {
    return termite_metal_decode_runtime_apply_binary_device(runtime, runtime != NULL ? runtime->less_than_pipeline : nil, lhs_handle, lhs_offset, rhs_handle, rhs_offset, dim, lhs_scalar != 0, rhs_scalar != 0, output_handle, output_offset);
}

int termite_metal_decode_runtime_apply_where_select_device_broadcast(
    termite_metal_decode_runtime *runtime,
    void *cond_handle,
    size_t cond_offset,
    void *true_handle,
    size_t true_offset,
    void *false_handle,
    size_t false_offset,
    size_t dim,
    int cond_scalar,
    int true_scalar,
    int false_scalar,
    void *output_handle,
    size_t output_offset
);

int termite_metal_decode_runtime_apply_where_select_device(
    termite_metal_decode_runtime *runtime,
    void *cond_handle,
    size_t cond_offset,
    void *true_handle,
    size_t true_offset,
    void *false_handle,
    size_t false_offset,
    size_t dim,
    void *output_handle,
    size_t output_offset
) {
    return termite_metal_decode_runtime_apply_where_select_device_broadcast(runtime, cond_handle, cond_offset, true_handle, true_offset, false_handle, false_offset, dim, false, false, false, output_handle, output_offset);
}

int termite_metal_decode_runtime_apply_where_select_device_broadcast(
    termite_metal_decode_runtime *runtime,
    void *cond_handle,
    size_t cond_offset,
    void *true_handle,
    size_t true_offset,
    void *false_handle,
    size_t false_offset,
    size_t dim,
    int cond_scalar,
    int true_scalar,
    int false_scalar,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || cond_handle == NULL || true_handle == NULL || false_handle == NULL || output_handle == NULL) return -1;
    if (runtime->where_select_pipeline == nil) return -2;
    if (dim == 0 || dim > UINT32_MAX) return -3;
    @autoreleasepool {
        id<MTLBuffer> cond_buffer = (__bridge id<MTLBuffer>)cond_handle;
        id<MTLBuffer> true_buffer = (__bridge id<MTLBuffer>)true_handle;
        id<MTLBuffer> false_buffer = (__bridge id<MTLBuffer>)false_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t bytes = dim * sizeof(float);
        const size_t cond_bytes = cond_scalar != 0 ? sizeof(float) : bytes;
        const size_t true_bytes = true_scalar != 0 ? sizeof(float) : bytes;
        const size_t false_bytes = false_scalar != 0 ? sizeof(float) : bytes;
        if (cond_offset + cond_bytes > cond_buffer.length) return -4;
        if (true_offset + true_bytes > true_buffer.length) return -5;
        if (false_offset + false_bytes > false_buffer.length) return -6;
        if (output_offset + bytes > output_buffer.length) return -7;
        if (termite_metal_decode_runtime_prepare_planned_compute_ternary_accesses(
                runtime,
                cond_buffer,
                cond_offset,
                cond_bytes,
                true_buffer,
                true_offset,
                true_bytes,
                false_buffer,
                false_offset,
                false_bytes,
                output_buffer,
                output_offset,
                bytes,
                -10) != 0)
        {
            return -10;
        }
        termite_metal_apply_add_params params = {
            .rows = 1,
            .dim = (uint32_t)dim,
            .flags = (cond_scalar != 0 ? 1u : 0u) | (true_scalar != 0 ? 2u : 0u) | (false_scalar != 0 ? 4u : 0u),
        };
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -9;
        id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
        const BOOL planned_encoder = (encoder != nil);
        if (!planned_encoder) {
            encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        }
        if (encoder == nil) return -10;
        [encoder setComputePipelineState:runtime->where_select_pipeline];
        [encoder setBuffer:cond_buffer offset:cond_offset atIndex:0];
        [encoder setBuffer:true_buffer offset:true_offset atIndex:1];
        [encoder setBuffer:false_buffer offset:false_offset atIndex:2];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:3];
        [encoder setBytes:&params length:sizeof(params) atIndex:4];
        [encoder dispatchThreads:MTLSizeMake(dim, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->where_select_pipeline, dim), 1, 1)];
        if (!planned_encoder) [encoder endEncoding];
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -11);
    }
}

int termite_metal_decode_runtime_apply_multiply(
    termite_metal_decode_runtime *runtime,
    const float *lhs,
    const float *rhs,
    size_t dim,
    float *output
) {
    if (runtime == NULL || lhs == NULL || rhs == NULL || output == NULL) return -1;
    if (runtime->multiply_pipeline == nil) return -2;
    if (dim == 0 || dim > UINT32_MAX) return -3;
    @autoreleasepool {
        const size_t bytes = dim * sizeof(float);
        id<MTLBuffer> lhs_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)lhs length:bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (lhs_buffer == nil) lhs_buffer = [runtime->device newBufferWithBytes:lhs length:bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> rhs_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)rhs length:bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (rhs_buffer == nil) rhs_buffer = [runtime->device newBufferWithBytes:rhs length:bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithBytesNoCopy:output length:bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) output_buffer = [runtime->device newBufferWithBytes:output length:bytes options:MTLResourceStorageModeShared];
        if (lhs_buffer == nil || rhs_buffer == nil || output_buffer == nil) return -4;
        termite_metal_apply_add_params params = {
            .rows = 1,
            .dim = (uint32_t)dim,
        };
        id<MTLBuffer> params_buffer = [runtime->device newBufferWithBytes:&params length:sizeof(params) options:MTLResourceStorageModeShared];
        if (params_buffer == nil) return -5;
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -6;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (encoder == nil) return -7;
        [encoder setComputePipelineState:runtime->multiply_pipeline];
        [encoder setBuffer:lhs_buffer offset:0 atIndex:0];
        [encoder setBuffer:rhs_buffer offset:0 atIndex:1];
        [encoder setBuffer:output_buffer offset:0 atIndex:2];
        [encoder setBuffer:params_buffer offset:0 atIndex:3];
        NSUInteger thread_width = runtime->multiply_pipeline.maxTotalThreadsPerThreadgroup;
        if (thread_width == 0) thread_width = 64;
        if (thread_width > dim && dim > 0) thread_width = dim;
        [encoder dispatchThreads:MTLSizeMake(dim, 1, 1) threadsPerThreadgroup:MTLSizeMake(thread_width, 1, 1)];
        [encoder endEncoding];
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -8;
    }
}

int termite_metal_decode_runtime_apply_multiply_device(
    termite_metal_decode_runtime *runtime,
    void *lhs_handle,
    size_t lhs_offset,
    void *rhs_handle,
    size_t rhs_offset,
    size_t dim,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || lhs_handle == NULL || rhs_handle == NULL || output_handle == NULL) return -1;
    if (runtime->multiply_pipeline == nil) return -2;
    if (dim == 0 || dim > UINT32_MAX) return -3;
    @autoreleasepool {
        id<MTLBuffer> lhs_buffer = (__bridge id<MTLBuffer>)lhs_handle;
        id<MTLBuffer> rhs_buffer = (__bridge id<MTLBuffer>)rhs_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t bytes = dim * sizeof(float);
        if (lhs_offset + bytes > lhs_buffer.length) return -4;
        if (rhs_offset + bytes > rhs_buffer.length) return -5;
        if (output_offset + bytes > output_buffer.length) return -6;
        if (termite_metal_decode_runtime_prepare_planned_compute_binary_accesses(
                runtime,
                lhs_buffer,
                lhs_offset,
                bytes,
                rhs_buffer,
                rhs_offset,
                bytes,
                output_buffer,
                output_offset,
                bytes,
                -9) != 0)
        {
            return -9;
        }
        termite_metal_apply_add_params params = {
            .rows = 1,
            .dim = (uint32_t)dim,
        };
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -8;
        id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
        const BOOL planned_encoder = (encoder != nil);
        if (!planned_encoder) {
            encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        }
        if (encoder == nil) return -9;
        [encoder setComputePipelineState:runtime->multiply_pipeline];
        [encoder setBuffer:lhs_buffer offset:lhs_offset atIndex:0];
        [encoder setBuffer:rhs_buffer offset:rhs_offset atIndex:1];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:2];
        [encoder setBytes:&params length:sizeof(params) atIndex:3];
        NSUInteger thread_width = runtime->multiply_pipeline.maxTotalThreadsPerThreadgroup;
        if (thread_width == 0) thread_width = 64;
        if (thread_width > dim && dim > 0) thread_width = dim;
        [encoder dispatchThreads:MTLSizeMake(dim, 1, 1) threadsPerThreadgroup:MTLSizeMake(thread_width, 1, 1)];
        if (!planned_encoder) [encoder endEncoding];
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -10);
    }
}

int termite_metal_decode_runtime_apply_multiply_device_rhs_repeat(
    termite_metal_decode_runtime *runtime,
    void *lhs_handle,
    size_t lhs_offset,
    void *rhs_handle,
    size_t rhs_offset,
    size_t dim,
    size_t rhs_period,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || lhs_handle == NULL || rhs_handle == NULL || output_handle == NULL) return -1;
    if (runtime->multiply_pipeline == nil) return -2;
    if (dim == 0 || rhs_period == 0 || dim % rhs_period != 0 || dim > UINT32_MAX || rhs_period > UINT32_MAX) return -3;
    @autoreleasepool {
        id<MTLBuffer> lhs_buffer = (__bridge id<MTLBuffer>)lhs_handle;
        id<MTLBuffer> rhs_buffer = (__bridge id<MTLBuffer>)rhs_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t bytes = dim * sizeof(float);
        const size_t rhs_bytes = rhs_period * sizeof(float);
        if (lhs_offset + bytes > lhs_buffer.length) return -4;
        if (rhs_offset + rhs_bytes > rhs_buffer.length) return -5;
        if (output_offset + bytes > output_buffer.length) return -6;
        if (termite_metal_decode_runtime_prepare_planned_compute_binary_accesses(
                runtime,
                lhs_buffer,
                lhs_offset,
                bytes,
                rhs_buffer,
                rhs_offset,
                rhs_bytes,
                output_buffer,
                output_offset,
                bytes,
                -9) != 0)
        {
            return -9;
        }
        termite_metal_apply_add_params params = {
            .rows = (uint32_t)(dim / rhs_period),
            .dim = (uint32_t)rhs_period,
            .flags = 4u,
        };
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -8;
        id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
        const BOOL planned_encoder = (encoder != nil);
        if (!planned_encoder) {
            encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        }
        if (encoder == nil) return -9;
        [encoder setComputePipelineState:runtime->multiply_pipeline];
        [encoder setBuffer:lhs_buffer offset:lhs_offset atIndex:0];
        [encoder setBuffer:rhs_buffer offset:rhs_offset atIndex:1];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:2];
        [encoder setBytes:&params length:sizeof(params) atIndex:3];
        [encoder dispatchThreads:MTLSizeMake(dim, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->multiply_pipeline, dim), 1, 1)];
        if (!planned_encoder) [encoder endEncoding];
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -10);
    }
}

int termite_metal_decode_runtime_apply_scale_device(
    termite_metal_decode_runtime *runtime,
    void *input_handle,
    size_t input_offset,
    size_t dim,
    float scale,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || input_handle == NULL || output_handle == NULL) return -1;
    if (runtime->scale_pipeline == nil) return -2;
    if (dim == 0 || dim > UINT32_MAX) return -3;
    @autoreleasepool {
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        const size_t bytes = dim * sizeof(float);
        if (input_offset + bytes > input_buffer.length) return -4;
        if (output_offset + bytes > output_buffer.length) return -5;
        if (termite_metal_decode_runtime_prepare_planned_compute_unary_accesses(
                runtime,
                input_buffer,
                input_offset,
                bytes,
                output_buffer,
                output_offset,
                bytes,
                -7) != 0)
        {
            return -7;
        }
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -6;
        id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
        const BOOL planned_encoder = (encoder != nil);
        if (!planned_encoder) {
            encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        }
        if (encoder == nil) return -7;
        [encoder setComputePipelineState:runtime->scale_pipeline];
        [encoder setBuffer:input_buffer offset:input_offset atIndex:0];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:1];
        [encoder setBytes:&scale length:sizeof(scale) atIndex:2];
        [encoder dispatchThreads:MTLSizeMake(dim, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->scale_pipeline, dim), 1, 1)];
        if (!planned_encoder) [encoder endEncoding];
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -8);
    }
}

static int termite_metal_decode_runtime_encode_attention_quantized_residual_tl1(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    id<MTLBuffer> input_buffer,
    size_t attention_pre_linear_rms_norm_slot,
    const uint8_t *weight_raw,
    size_t weight_bytes,
    uint32_t packed_len,
    uint32_t bm,
    uint32_t cfg_by,
    uint32_t bmm,
    size_t attention_post_linear_rms_norm_slot,
    id<MTLBuffer> residual_buffer,
    size_t attention_input_size,
    size_t hidden_size,
    float eps,
    id<MTLBuffer> output_buffer
) {
    if (runtime == NULL || command_buffer == nil || input_buffer == nil || weight_raw == NULL || residual_buffer == nil || output_buffer == nil) return -1;
    if (runtime->rms_norm_pipeline == nil || runtime->tl1_pipeline == nil || runtime->add_pipeline == nil) return -2;
    if (attention_input_size == 0 || attention_input_size > UINT32_MAX || hidden_size == 0 || hidden_size > UINT32_MAX) return -3;
    if (attention_pre_linear_rms_norm_slot != SIZE_MAX) {
        if (attention_pre_linear_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return -4;
        if (runtime->rms_norm_slot_prepared[attention_pre_linear_rms_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[attention_pre_linear_rms_norm_slot] != hidden_size) return -5;
    }
    if (attention_post_linear_rms_norm_slot != SIZE_MAX) {
        if (attention_post_linear_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return -6;
        if (runtime->rms_norm_slot_prepared[attention_post_linear_rms_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[attention_post_linear_rms_norm_slot] != hidden_size) return -7;
    }
    @autoreleasepool {
        const size_t attention_input_bytes = attention_input_size * sizeof(float);
        const size_t hidden_bytes = hidden_size * sizeof(float);
        id<MTLBuffer> weight_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)weight_raw length:weight_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (weight_buffer == nil) weight_buffer = [runtime->device newBufferWithBytes:weight_raw length:weight_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> tmp_a = [runtime->device newBufferWithLength:attention_input_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> tmp_b = [runtime->device newBufferWithLength:hidden_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> projected_buffer = [runtime->device newBufferWithLength:hidden_bytes options:MTLResourceStorageModePrivate];
        if (weight_buffer == nil || tmp_a == nil || tmp_b == nil || projected_buffer == nil) return -8;

        id<MTLBuffer> current_buffer = input_buffer;
        if (attention_pre_linear_rms_norm_slot != SIZE_MAX) {
            termite_metal_apply_layer_norm_params params = { .hidden_size = (uint32_t)attention_input_size, .eps = eps };
            id<MTLBuffer> params_buffer = [runtime->device newBufferWithBytes:&params length:sizeof(params) options:MTLResourceStorageModeShared];
            if (params_buffer == nil) return -9;
            id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_TAIL);
            if (encoder == nil) return -10;
            [encoder setComputePipelineState:runtime->rms_norm_pipeline];
            [encoder setBuffer:current_buffer offset:0 atIndex:0];
            [encoder setBuffer:runtime->rms_norm_weight_buffers[attention_pre_linear_rms_norm_slot] offset:0 atIndex:1];
            [encoder setBuffer:tmp_a offset:0 atIndex:2];
            [encoder setBuffer:params_buffer offset:0 atIndex:3];
            [encoder dispatchThreads:MTLSizeMake(attention_input_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->rms_norm_pipeline, attention_input_size), 1, 1)];
            [encoder endEncoding];
            current_buffer = tmp_a;
        }

        {
            termite_metal_tl1_params params = {
                .rows = 1,
                .in_dim = (uint32_t)attention_input_size,
                .out_dim = (uint32_t)hidden_size,
                .packed_len = packed_len,
                .bm = bm,
                .cfg_by = cfg_by,
                .bmm = bmm,
            };
            id<MTLBuffer> params_buffer = [runtime->device newBufferWithBytes:&params length:sizeof(params) options:MTLResourceStorageModeShared];
            if (params_buffer == nil) return -11;
            id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
            if (encoder == nil) return -12;
            [encoder setComputePipelineState:runtime->tl1_pipeline];
            [encoder setBuffer:current_buffer offset:0 atIndex:0];
            [encoder setBuffer:weight_buffer offset:0 atIndex:1];
            [encoder setBuffer:projected_buffer offset:0 atIndex:2];
            [encoder setBuffer:params_buffer offset:0 atIndex:3];
            [encoder dispatchThreads:MTLSizeMake(hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->tl1_pipeline, hidden_size), 1, 1)];
            [encoder endEncoding];
            current_buffer = projected_buffer;
        }

        if (attention_post_linear_rms_norm_slot != SIZE_MAX) {
            termite_metal_apply_layer_norm_params params = { .hidden_size = (uint32_t)hidden_size, .eps = eps };
            id<MTLBuffer> params_buffer = [runtime->device newBufferWithBytes:&params length:sizeof(params) options:MTLResourceStorageModeShared];
            if (params_buffer == nil) return -13;
            id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
            if (encoder == nil) return -14;
            [encoder setComputePipelineState:runtime->rms_norm_pipeline];
            [encoder setBuffer:current_buffer offset:0 atIndex:0];
            [encoder setBuffer:runtime->rms_norm_weight_buffers[attention_post_linear_rms_norm_slot] offset:0 atIndex:1];
            [encoder setBuffer:tmp_b offset:0 atIndex:2];
            [encoder setBuffer:params_buffer offset:0 atIndex:3];
            [encoder dispatchThreads:MTLSizeMake(hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->rms_norm_pipeline, hidden_size), 1, 1)];
            [encoder endEncoding];
            current_buffer = tmp_b;
        }

        {
            termite_metal_apply_add_params params = { .rows = 1, .dim = (uint32_t)hidden_size };
            id<MTLBuffer> params_buffer = [runtime->device newBufferWithBytes:&params length:sizeof(params) options:MTLResourceStorageModeShared];
            if (params_buffer == nil) return -15;
            id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
            if (encoder == nil) return -16;
            [encoder setComputePipelineState:runtime->add_pipeline];
            [encoder setBuffer:current_buffer offset:0 atIndex:0];
            [encoder setBuffer:residual_buffer offset:0 atIndex:1];
            [encoder setBuffer:output_buffer offset:0 atIndex:2];
            [encoder setBuffer:params_buffer offset:0 atIndex:3];
            [encoder dispatchThreads:MTLSizeMake(hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->add_pipeline, hidden_size), 1, 1)];
            [encoder endEncoding];
        }
        return 0;
    }
}

static int termite_metal_decode_runtime_encode_attention_quantized_residual_tl2(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    id<MTLBuffer> input_buffer,
    size_t attention_pre_linear_rms_norm_slot,
    const uint8_t *weight_raw,
    size_t weight_bytes,
    uint32_t scale_off,
    uint32_t three_value_len,
    uint32_t three_sign_len,
    uint32_t bm,
    uint32_t cfg_by,
    uint32_t bmm,
    uint32_t three_cols,
    uint32_t two_cols,
    size_t attention_post_linear_rms_norm_slot,
    id<MTLBuffer> residual_buffer,
    size_t attention_input_size,
    size_t hidden_size,
    float eps,
    id<MTLBuffer> output_buffer
) {
    if (runtime == NULL || command_buffer == nil || input_buffer == nil || weight_raw == NULL || residual_buffer == nil || output_buffer == nil) return -1;
    if (runtime->rms_norm_pipeline == nil || runtime->tl2_pipeline == nil || runtime->add_pipeline == nil) return -2;
    if (attention_input_size == 0 || attention_input_size > UINT32_MAX || hidden_size == 0 || hidden_size > UINT32_MAX) return -3;
    if (attention_pre_linear_rms_norm_slot != SIZE_MAX) {
        if (attention_pre_linear_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return -4;
        if (runtime->rms_norm_slot_prepared[attention_pre_linear_rms_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[attention_pre_linear_rms_norm_slot] != hidden_size) return -5;
    }
    if (attention_post_linear_rms_norm_slot != SIZE_MAX) {
        if (attention_post_linear_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return -6;
        if (runtime->rms_norm_slot_prepared[attention_post_linear_rms_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[attention_post_linear_rms_norm_slot] != hidden_size) return -7;
    }
    @autoreleasepool {
        const size_t attention_input_bytes = attention_input_size * sizeof(float);
        const size_t hidden_bytes = hidden_size * sizeof(float);
        id<MTLBuffer> weight_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)weight_raw length:weight_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (weight_buffer == nil) weight_buffer = [runtime->device newBufferWithBytes:weight_raw length:weight_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> tmp_a = [runtime->device newBufferWithLength:attention_input_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> tmp_b = [runtime->device newBufferWithLength:hidden_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> projected_buffer = [runtime->device newBufferWithLength:hidden_bytes options:MTLResourceStorageModePrivate];
        if (weight_buffer == nil || tmp_a == nil || tmp_b == nil || projected_buffer == nil) return -8;

        id<MTLBuffer> current_buffer = input_buffer;
        if (attention_pre_linear_rms_norm_slot != SIZE_MAX) {
            termite_metal_apply_layer_norm_params params = { .hidden_size = (uint32_t)attention_input_size, .eps = eps };
            id<MTLBuffer> params_buffer = [runtime->device newBufferWithBytes:&params length:sizeof(params) options:MTLResourceStorageModeShared];
            if (params_buffer == nil) return -9;
            id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
            if (encoder == nil) return -10;
            [encoder setComputePipelineState:runtime->rms_norm_pipeline];
            [encoder setBuffer:current_buffer offset:0 atIndex:0];
            [encoder setBuffer:runtime->rms_norm_weight_buffers[attention_pre_linear_rms_norm_slot] offset:0 atIndex:1];
            [encoder setBuffer:tmp_a offset:0 atIndex:2];
            [encoder setBuffer:params_buffer offset:0 atIndex:3];
            [encoder dispatchThreads:MTLSizeMake(attention_input_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->rms_norm_pipeline, attention_input_size), 1, 1)];
            [encoder endEncoding];
            current_buffer = tmp_a;
        }

        {
            termite_metal_tl2_params params = {
                .rows = 1,
                .in_dim = (uint32_t)attention_input_size,
                .out_dim = (uint32_t)hidden_size,
                .scale_off = scale_off,
                .three_value_len = three_value_len,
                .three_sign_len = three_sign_len,
                .bm = bm,
                .cfg_by = cfg_by,
                .bmm = bmm,
                .three_cols = three_cols,
                .two_cols = two_cols,
            };
            id<MTLBuffer> params_buffer = [runtime->device newBufferWithBytes:&params length:sizeof(params) options:MTLResourceStorageModeShared];
            if (params_buffer == nil) return -11;
            id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
            if (encoder == nil) return -12;
            [encoder setComputePipelineState:runtime->tl2_pipeline];
            [encoder setBuffer:current_buffer offset:0 atIndex:0];
            [encoder setBuffer:weight_buffer offset:0 atIndex:1];
            [encoder setBuffer:projected_buffer offset:0 atIndex:2];
            [encoder setBuffer:params_buffer offset:0 atIndex:3];
            [encoder dispatchThreads:MTLSizeMake(hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->tl2_pipeline, hidden_size), 1, 1)];
            [encoder endEncoding];
            current_buffer = projected_buffer;
        }

        if (attention_post_linear_rms_norm_slot != SIZE_MAX) {
            termite_metal_apply_layer_norm_params params = { .hidden_size = (uint32_t)hidden_size, .eps = eps };
            id<MTLBuffer> params_buffer = [runtime->device newBufferWithBytes:&params length:sizeof(params) options:MTLResourceStorageModeShared];
            if (params_buffer == nil) return -13;
            id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
            if (encoder == nil) return -14;
            [encoder setComputePipelineState:runtime->rms_norm_pipeline];
            [encoder setBuffer:current_buffer offset:0 atIndex:0];
            [encoder setBuffer:runtime->rms_norm_weight_buffers[attention_post_linear_rms_norm_slot] offset:0 atIndex:1];
            [encoder setBuffer:tmp_b offset:0 atIndex:2];
            [encoder setBuffer:params_buffer offset:0 atIndex:3];
            [encoder dispatchThreads:MTLSizeMake(hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->rms_norm_pipeline, hidden_size), 1, 1)];
            [encoder endEncoding];
            current_buffer = tmp_b;
        }

        {
            termite_metal_apply_add_params params = { .rows = 1, .dim = (uint32_t)hidden_size };
            id<MTLBuffer> params_buffer = [runtime->device newBufferWithBytes:&params length:sizeof(params) options:MTLResourceStorageModeShared];
            if (params_buffer == nil) return -15;
            id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
            if (encoder == nil) return -16;
            [encoder setComputePipelineState:runtime->add_pipeline];
            [encoder setBuffer:current_buffer offset:0 atIndex:0];
            [encoder setBuffer:residual_buffer offset:0 atIndex:1];
            [encoder setBuffer:output_buffer offset:0 atIndex:2];
            [encoder setBuffer:params_buffer offset:0 atIndex:3];
            [encoder dispatchThreads:MTLSizeMake(hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->add_pipeline, hidden_size), 1, 1)];
            [encoder endEncoding];
        }
        return 0;
    }
}

static int termite_metal_decode_runtime_encode_attention_quantized_residual_i2_s_slot(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    id<MTLBuffer> input_buffer,
    size_t attention_pre_linear_rms_norm_slot,
    size_t attention_linear_slot,
    size_t attention_post_linear_rms_norm_slot,
    id<MTLBuffer> residual_buffer,
    size_t residual_offset,
    size_t attention_input_size,
    size_t hidden_size,
    float eps,
    id<MTLBuffer> output_buffer
) {
    if (runtime == NULL || command_buffer == nil || input_buffer == nil || residual_buffer == nil || output_buffer == nil) return -1;
    if (runtime->rms_norm_pipeline == nil || runtime->i2_s_pipeline == nil || runtime->add_pipeline == nil) return -2;
    if (attention_input_size == 0 || attention_input_size > UINT32_MAX || hidden_size == 0 || hidden_size > UINT32_MAX) return -3;
    if (attention_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -4;
    termite_metal_quant_linear_slot_view attention_view;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_I2_S, attention_linear_slot, attention_input_size, hidden_size, &attention_view) != 0) return -5;
    if (attention_pre_linear_rms_norm_slot != SIZE_MAX) {
        if (attention_pre_linear_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return -6;
        if (runtime->rms_norm_slot_prepared[attention_pre_linear_rms_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[attention_pre_linear_rms_norm_slot] != hidden_size) return -7;
    }
    if (attention_post_linear_rms_norm_slot != SIZE_MAX) {
        if (attention_post_linear_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return -8;
        if (runtime->rms_norm_slot_prepared[attention_post_linear_rms_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[attention_post_linear_rms_norm_slot] != hidden_size) return -9;
    }
    @autoreleasepool {
        const size_t attention_input_bytes = attention_input_size * sizeof(float);
        const size_t hidden_bytes = hidden_size * sizeof(float);
        const size_t max_hidden_bytes = attention_input_bytes > hidden_bytes ? attention_input_bytes : hidden_bytes;
        if (termite_metal_decode_runtime_ensure_hot_buffers(runtime, max_hidden_bytes, 0) != 0) return -10;
        id<MTLBuffer> tmp_a = runtime->hot_hidden_buffers[1];
        id<MTLBuffer> tmp_b = runtime->hot_hidden_buffers[2];
        id<MTLBuffer> projected_buffer = runtime->hot_hidden_buffers[3];
        if (tmp_a == nil || tmp_b == nil || projected_buffer == nil) return -10;

        id<MTLBuffer> current_buffer = input_buffer;
        if (attention_pre_linear_rms_norm_slot != SIZE_MAX) {
            termite_metal_apply_layer_norm_params params = { .hidden_size = (uint32_t)attention_input_size, .eps = eps };
            id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
            if (encoder == nil) return -12;
            [encoder setComputePipelineState:runtime->rms_norm_pipeline];
            [encoder setBuffer:current_buffer offset:0 atIndex:0];
            [encoder setBuffer:runtime->rms_norm_weight_buffers[attention_pre_linear_rms_norm_slot] offset:0 atIndex:1];
            [encoder setBuffer:tmp_a offset:0 atIndex:2];
            [encoder setBytes:&params length:sizeof(params) atIndex:3];
            [encoder dispatchThreads:MTLSizeMake(attention_input_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->rms_norm_pipeline, attention_input_size), 1, 1)];
            [encoder endEncoding];
            current_buffer = tmp_a;
        }

        {
            termite_metal_linear_params params = {
                .rows = 1,
                .in_dim = (uint32_t)attention_input_size,
                .out_dim = (uint32_t)hidden_size,
                .row_blocks = (uint32_t)(attention_input_size / 128u),
            };
            id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
            if (encoder == nil) return -14;
            [encoder setComputePipelineState:runtime->i2_s_pipeline];
            [encoder setBuffer:current_buffer offset:0 atIndex:0];
            [encoder setBuffer:attention_view.weight_buffer offset:0 atIndex:1];
            [encoder setBuffer:projected_buffer offset:0 atIndex:2];
            [encoder setBytes:&params length:sizeof(params) atIndex:3];
            [encoder dispatchThreads:MTLSizeMake(hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->i2_s_pipeline, hidden_size), 1, 1)];
            [encoder endEncoding];
            current_buffer = projected_buffer;
        }

        if (attention_post_linear_rms_norm_slot != SIZE_MAX) {
            termite_metal_apply_layer_norm_params params = { .hidden_size = (uint32_t)hidden_size, .eps = eps };
            id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
            if (encoder == nil) return -16;
            [encoder setComputePipelineState:runtime->rms_norm_pipeline];
            [encoder setBuffer:current_buffer offset:0 atIndex:0];
            [encoder setBuffer:runtime->rms_norm_weight_buffers[attention_post_linear_rms_norm_slot] offset:0 atIndex:1];
            [encoder setBuffer:tmp_b offset:0 atIndex:2];
            [encoder setBytes:&params length:sizeof(params) atIndex:3];
            [encoder dispatchThreads:MTLSizeMake(hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->rms_norm_pipeline, hidden_size), 1, 1)];
            [encoder endEncoding];
            current_buffer = tmp_b;
        }

        {
            termite_metal_apply_add_params params = { .rows = 1, .dim = (uint32_t)hidden_size };
            id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
            if (encoder == nil) return -18;
            [encoder setComputePipelineState:runtime->add_pipeline];
            [encoder setBuffer:current_buffer offset:0 atIndex:0];
            [encoder setBuffer:residual_buffer offset:residual_offset atIndex:1];
            [encoder setBuffer:output_buffer offset:0 atIndex:2];
            [encoder setBytes:&params length:sizeof(params) atIndex:3];
            [encoder dispatchThreads:MTLSizeMake(hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->add_pipeline, hidden_size), 1, 1)];
            [encoder endEncoding];
        }
        return 0;
    }
}

static int termite_metal_decode_runtime_encode_attention_quantized_residual_q4_k_slot(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    id<MTLBuffer> input_buffer,
    size_t attention_pre_linear_rms_norm_slot,
    size_t attention_linear_slot,
    size_t attention_post_linear_rms_norm_slot,
    id<MTLBuffer> residual_buffer,
    size_t residual_offset,
    size_t attention_input_size,
    size_t hidden_size,
    float eps,
    id<MTLBuffer> output_buffer
) {
    if (runtime == NULL || command_buffer == nil || input_buffer == nil || residual_buffer == nil || output_buffer == nil) return -1;
    if (runtime->rms_norm_pipeline == nil || runtime->q4_k_pipeline == nil || runtime->add_pipeline == nil) return -2;
    if (attention_input_size == 0 || attention_input_size > UINT32_MAX || hidden_size == 0 || hidden_size > UINT32_MAX) return -3;
    if (attention_input_size % 256u != 0) return -4;
    if (attention_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -5;
    termite_metal_quant_linear_slot_view attention_view;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q4_K, attention_linear_slot, attention_input_size, hidden_size, &attention_view) != 0) return -6;
    if (attention_pre_linear_rms_norm_slot != SIZE_MAX) {
        if (attention_pre_linear_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return -7;
        if (runtime->rms_norm_slot_prepared[attention_pre_linear_rms_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[attention_pre_linear_rms_norm_slot] != attention_input_size) return -8;
    }
    if (attention_post_linear_rms_norm_slot != SIZE_MAX) {
        if (attention_post_linear_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return -9;
        if (runtime->rms_norm_slot_prepared[attention_post_linear_rms_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[attention_post_linear_rms_norm_slot] != hidden_size) return -10;
    }
    @autoreleasepool {
        const size_t attention_input_bytes = attention_input_size * sizeof(float);
        const size_t hidden_bytes = hidden_size * sizeof(float);
        const size_t max_hidden_bytes = attention_input_bytes > hidden_bytes ? attention_input_bytes : hidden_bytes;
        if (termite_metal_decode_runtime_ensure_hot_buffers(runtime, max_hidden_bytes, 0) != 0) return -11;
        id<MTLBuffer> tmp_a = runtime->hot_hidden_buffers[1];
        id<MTLBuffer> tmp_b = runtime->hot_hidden_buffers[2];
        id<MTLBuffer> projected_buffer = runtime->hot_hidden_buffers[3];
        if (tmp_a == nil || tmp_b == nil || projected_buffer == nil) return -11;

        id<MTLBuffer> current_buffer = input_buffer;
        if (attention_pre_linear_rms_norm_slot != SIZE_MAX) {
            termite_metal_apply_layer_norm_params params = { .hidden_size = (uint32_t)attention_input_size, .eps = eps };
            id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
            if (encoder == nil) return -12;
            [encoder setComputePipelineState:runtime->rms_norm_pipeline];
            [encoder setBuffer:current_buffer offset:0 atIndex:0];
            [encoder setBuffer:runtime->rms_norm_weight_buffers[attention_pre_linear_rms_norm_slot] offset:0 atIndex:1];
            [encoder setBuffer:tmp_a offset:0 atIndex:2];
            [encoder setBytes:&params length:sizeof(params) atIndex:3];
            [encoder dispatchThreads:MTLSizeMake(attention_input_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->rms_norm_pipeline, attention_input_size), 1, 1)];
            [encoder endEncoding];
            current_buffer = tmp_a;
        }

        {
            termite_metal_linear_params params = {
                .rows = 1,
                .in_dim = (uint32_t)attention_input_size,
                .out_dim = (uint32_t)hidden_size,
                .row_blocks = (uint32_t)(attention_input_size / 256u),
            };
            id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
            if (encoder == nil) return -13;
            [encoder setComputePipelineState:runtime->q4_k_pipeline];
            [encoder setBuffer:current_buffer offset:0 atIndex:0];
            [encoder setBuffer:attention_view.weight_buffer offset:0 atIndex:1];
            [encoder setBuffer:projected_buffer offset:0 atIndex:2];
            [encoder setBytes:&params length:sizeof(params) atIndex:3];
            [encoder dispatchThreads:MTLSizeMake(hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->q4_k_pipeline, hidden_size), 1, 1)];
            [encoder endEncoding];
            current_buffer = projected_buffer;
        }

        if (attention_post_linear_rms_norm_slot != SIZE_MAX) {
            return termite_metal_encode_rms_norm_add(
                runtime,
                command_buffer,
                current_buffer,
                0,
                attention_post_linear_rms_norm_slot,
                residual_buffer,
                residual_offset,
                output_buffer,
                0,
                hidden_size,
                eps,
                -14
            );
        } else {
            termite_metal_apply_add_params params = { .rows = 1, .dim = (uint32_t)hidden_size };
            id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
            if (encoder == nil) return -15;
            [encoder setComputePipelineState:runtime->add_pipeline];
            [encoder setBuffer:current_buffer offset:0 atIndex:0];
            [encoder setBuffer:residual_buffer offset:residual_offset atIndex:1];
            [encoder setBuffer:output_buffer offset:0 atIndex:2];
            [encoder setBytes:&params length:sizeof(params) atIndex:3];
            [encoder dispatchThreads:MTLSizeMake(hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->add_pipeline, hidden_size), 1, 1)];
            [encoder endEncoding];
        }
        return 0;
    }
}

static int termite_metal_decode_runtime_encode_attention_quantized_residual_q5_k_slot(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    id<MTLBuffer> input_buffer,
    size_t attention_pre_linear_rms_norm_slot,
    size_t attention_linear_slot,
    size_t attention_post_linear_rms_norm_slot,
    id<MTLBuffer> residual_buffer,
    size_t residual_offset,
    size_t attention_input_size,
    size_t hidden_size,
    float eps,
    id<MTLBuffer> output_buffer
) {
    if (runtime == NULL || command_buffer == nil || input_buffer == nil || residual_buffer == nil || output_buffer == nil) return -1;
    if (runtime->rms_norm_pipeline == nil || runtime->q5_k_pipeline == nil || runtime->add_pipeline == nil) return -2;
    if (attention_input_size == 0 || attention_input_size > UINT32_MAX || hidden_size == 0 || hidden_size > UINT32_MAX) return -3;
    if (attention_input_size % 256u != 0) return -4;
    if (attention_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -5;
    termite_metal_quant_linear_slot_view attention_view;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q5_K, attention_linear_slot, attention_input_size, hidden_size, &attention_view) != 0) return -6;
    if (attention_pre_linear_rms_norm_slot != SIZE_MAX) {
        if (attention_pre_linear_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return -7;
        if (runtime->rms_norm_slot_prepared[attention_pre_linear_rms_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[attention_pre_linear_rms_norm_slot] != attention_input_size) return -8;
    }
    if (attention_post_linear_rms_norm_slot != SIZE_MAX) {
        if (attention_post_linear_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return -9;
        if (runtime->rms_norm_slot_prepared[attention_post_linear_rms_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[attention_post_linear_rms_norm_slot] != hidden_size) return -10;
    }
    @autoreleasepool {
        const size_t attention_input_bytes = attention_input_size * sizeof(float);
        const size_t hidden_bytes = hidden_size * sizeof(float);
        const size_t max_hidden_bytes = attention_input_bytes > hidden_bytes ? attention_input_bytes : hidden_bytes;
        if (termite_metal_decode_runtime_ensure_hot_buffers(runtime, max_hidden_bytes, 0) != 0) return -11;
        id<MTLBuffer> tmp_a = runtime->hot_hidden_buffers[1];
        id<MTLBuffer> tmp_b = runtime->hot_hidden_buffers[2];
        id<MTLBuffer> projected_buffer = runtime->hot_hidden_buffers[3];
        if (tmp_a == nil || tmp_b == nil || projected_buffer == nil) return -11;

        id<MTLBuffer> current_buffer = input_buffer;
        if (attention_pre_linear_rms_norm_slot != SIZE_MAX) {
            termite_metal_apply_layer_norm_params params = { .hidden_size = (uint32_t)attention_input_size, .eps = eps };
            id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
            if (encoder == nil) return -12;
            [encoder setComputePipelineState:runtime->rms_norm_pipeline];
            [encoder setBuffer:current_buffer offset:0 atIndex:0];
            [encoder setBuffer:runtime->rms_norm_weight_buffers[attention_pre_linear_rms_norm_slot] offset:0 atIndex:1];
            [encoder setBuffer:tmp_a offset:0 atIndex:2];
            [encoder setBytes:&params length:sizeof(params) atIndex:3];
            [encoder dispatchThreads:MTLSizeMake(attention_input_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->rms_norm_pipeline, attention_input_size), 1, 1)];
            [encoder endEncoding];
            current_buffer = tmp_a;
        }

        {
            termite_metal_linear_params params = {
                .rows = 1,
                .in_dim = (uint32_t)attention_input_size,
                .out_dim = (uint32_t)hidden_size,
                .row_blocks = (uint32_t)(attention_input_size / 256u),
            };
            id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
            if (encoder == nil) return -13;
            [encoder setComputePipelineState:runtime->q5_k_pipeline];
            [encoder setBuffer:current_buffer offset:0 atIndex:0];
            [encoder setBuffer:attention_view.weight_buffer offset:0 atIndex:1];
            [encoder setBuffer:projected_buffer offset:0 atIndex:2];
            [encoder setBytes:&params length:sizeof(params) atIndex:3];
            [encoder dispatchThreads:MTLSizeMake(hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->q5_k_pipeline, hidden_size), 1, 1)];
            [encoder endEncoding];
            current_buffer = projected_buffer;
        }

        if (attention_post_linear_rms_norm_slot != SIZE_MAX) {
            termite_metal_apply_layer_norm_params params = { .hidden_size = (uint32_t)hidden_size, .eps = eps };
            id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
            if (encoder == nil) return -14;
            [encoder setComputePipelineState:runtime->rms_norm_pipeline];
            [encoder setBuffer:current_buffer offset:0 atIndex:0];
            [encoder setBuffer:runtime->rms_norm_weight_buffers[attention_post_linear_rms_norm_slot] offset:0 atIndex:1];
            [encoder setBuffer:tmp_b offset:0 atIndex:2];
            [encoder setBytes:&params length:sizeof(params) atIndex:3];
            [encoder dispatchThreads:MTLSizeMake(hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->rms_norm_pipeline, hidden_size), 1, 1)];
            [encoder endEncoding];
            current_buffer = tmp_b;
        }

        {
            termite_metal_apply_add_params params = { .rows = 1, .dim = (uint32_t)hidden_size };
            id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
            if (encoder == nil) return -15;
            [encoder setComputePipelineState:runtime->add_pipeline];
            [encoder setBuffer:current_buffer offset:0 atIndex:0];
            [encoder setBuffer:residual_buffer offset:residual_offset atIndex:1];
            [encoder setBuffer:output_buffer offset:0 atIndex:2];
            [encoder setBytes:&params length:sizeof(params) atIndex:3];
            [encoder dispatchThreads:MTLSizeMake(hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->add_pipeline, hidden_size), 1, 1)];
            [encoder endEncoding];
        }
        return 0;
    }
}

static int termite_metal_decode_runtime_encode_attention_quantized_residual_q8_0_slot(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    id<MTLBuffer> input_buffer,
    size_t attention_pre_linear_rms_norm_slot,
    size_t attention_linear_slot,
    size_t attention_post_linear_rms_norm_slot,
    id<MTLBuffer> residual_buffer,
    size_t residual_offset,
    size_t rows,
    size_t attention_input_size,
    size_t hidden_size,
    float eps,
    id<MTLBuffer> output_buffer
) {
    if (runtime == NULL || command_buffer == nil || input_buffer == nil || residual_buffer == nil || output_buffer == nil) return -1;
    if (runtime->rms_norm_reduce_pipeline == nil || runtime->rms_norm_rows_pipeline == nil || runtime->q8_0_pipeline == nil || runtime->add_pipeline == nil) return -2;
    if (rows == 0 || rows > UINT32_MAX || attention_input_size == 0 || attention_input_size > UINT32_MAX || hidden_size == 0 || hidden_size > UINT32_MAX) return -3;
    if (attention_input_size % 32u != 0) return -4;
    if (attention_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -5;
    termite_metal_quant_linear_slot_view attention_view;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q8_0, attention_linear_slot, attention_input_size, hidden_size, &attention_view) != 0) return -6;
    if (attention_pre_linear_rms_norm_slot != SIZE_MAX) {
        if (attention_pre_linear_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return -7;
        if (runtime->rms_norm_slot_prepared[attention_pre_linear_rms_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[attention_pre_linear_rms_norm_slot] != attention_input_size) return -8;
    }
    if (attention_post_linear_rms_norm_slot != SIZE_MAX) {
        if (attention_post_linear_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return -9;
        if (runtime->rms_norm_slot_prepared[attention_post_linear_rms_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[attention_post_linear_rms_norm_slot] != hidden_size) return -10;
    }
    @autoreleasepool {
        if (rows > SIZE_MAX / attention_input_size || rows > SIZE_MAX / hidden_size) return -11;
        const size_t attention_input_bytes = rows * attention_input_size * sizeof(float);
        const size_t hidden_bytes = rows * hidden_size * sizeof(float);
        const size_t max_hidden_bytes = attention_input_bytes > hidden_bytes ? attention_input_bytes : hidden_bytes;
        if (termite_metal_decode_runtime_ensure_hot_buffers(runtime, max_hidden_bytes, 0) != 0) return -11;
        id<MTLBuffer> tmp_a = runtime->hot_hidden_buffers[1];
        id<MTLBuffer> tmp_b = runtime->hot_hidden_buffers[2];
        id<MTLBuffer> projected_buffer = runtime->hot_hidden_buffers[3];
        if (tmp_a == nil || tmp_b == nil || projected_buffer == nil) return -11;

        id<MTLBuffer> current_buffer = input_buffer;
        if (attention_pre_linear_rms_norm_slot != SIZE_MAX) {
            if (rows == 1) {
                if (termite_metal_encode_rms_norm_reduce(
                        runtime,
                        command_buffer,
                        current_buffer,
                        0,
                        attention_pre_linear_rms_norm_slot,
                        tmp_a,
                        0,
                        attention_input_size,
                        eps,
                        -12) != 0) return -12;
            } else {
                if (termite_metal_encode_rms_norm_rows(
                        runtime,
                        command_buffer,
                        current_buffer,
                        0,
                        attention_pre_linear_rms_norm_slot,
                        tmp_a,
                        0,
                        rows,
                        attention_input_size,
                        eps,
                        -12) != 0) return -12;
            }
            current_buffer = tmp_a;
        }

        if (attention_post_linear_rms_norm_slot != SIZE_MAX && rows == 1 && runtime->active_frame_cb == command_buffer) {
            const int planned_status = termite_metal_encode_q8_0_linear_rms_add_planned_1x(
                runtime,
                command_buffer,
                current_buffer,
                0,
                attention_view.weight_buffer,
                projected_buffer,
                0,
                attention_input_size,
                hidden_size,
                attention_post_linear_rms_norm_slot,
                residual_buffer,
                residual_offset,
                output_buffer,
                0,
                eps,
                TERMITE_METAL_COMPUTE_SOURCE_ATTENTION,
                TERMITE_METAL_COMPUTE_REGION_ATTENTION_PROJECT,
                -13);
            if (planned_status != 0) return planned_status;
            return 0;
        }

        termite_metal_decode_runtime_record_logical_command_op(
            runtime,
            TERMITE_METAL_PLAN_OP_DECODE_ATTENTION_OUTPUT_LINEAR,
            TERMITE_METAL_OPERATOR_MUL_MM,
            TERMITE_METAL_QUANT_MATMUL_DISPATCH_MM);
        if (termite_metal_encode_q8_0_linear_family(
                runtime,
                command_buffer,
                current_buffer,
                0,
                attention_view.weight_buffer,
                projected_buffer,
                0,
                rows,
                attention_input_size,
                hidden_size,
                TERMITE_METAL_Q8_0_LINEAR_FAMILY_ATTENTION_OUTPUT,
                -13) != 0) return -13;
        current_buffer = projected_buffer;

        if (attention_post_linear_rms_norm_slot != SIZE_MAX) {
            termite_metal_decode_runtime_record_logical_command_op(
                runtime,
                TERMITE_METAL_PLAN_OP_DECODE_ATTENTION_POST_NORM_RESIDUAL,
                TERMITE_METAL_OPERATOR_UNKNOWN,
                TERMITE_METAL_QUANT_MATMUL_DISPATCH_UNKNOWN);
            if (rows == 1) {
                if (termite_metal_encode_rms_norm_add(
                        runtime,
                        command_buffer,
                        current_buffer,
                        0,
                        attention_post_linear_rms_norm_slot,
                        residual_buffer,
                        residual_offset,
                        output_buffer,
                        0,
                        hidden_size,
                        eps,
                        -14) != 0) return -14;
            } else {
                if (termite_metal_encode_rms_norm_add_rows_for(
                        runtime,
                        command_buffer,
                        current_buffer,
                        0,
                        attention_post_linear_rms_norm_slot,
                        residual_buffer,
                        residual_offset,
                        output_buffer,
                        0,
                        rows,
                        hidden_size,
                        eps,
                        TERMITE_METAL_COMPUTE_SOURCE_ATTENTION,
                        -14) != 0) return -14;
            }
        } else {
            termite_metal_apply_add_params params = { .rows = (uint32_t)rows, .dim = (uint32_t)hidden_size };
            id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_ATTENTION);
            if (encoder == nil) return -15;
            [encoder setComputePipelineState:runtime->add_pipeline];
            [encoder setBuffer:current_buffer offset:0 atIndex:0];
            [encoder setBuffer:residual_buffer offset:residual_offset atIndex:1];
            [encoder setBuffer:output_buffer offset:0 atIndex:2];
            [encoder setBytes:&params length:sizeof(params) atIndex:3];
            [encoder dispatchThreads:MTLSizeMake(rows * hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->add_pipeline, rows * hidden_size), 1, 1)];
            [encoder endEncoding];
        }
        return 0;
    }
}

int termite_metal_decode_runtime_apply_attention_residual_tl1(
    termite_metal_decode_runtime *runtime,
    const float *attention_input,
    size_t attention_pre_linear_rms_norm_slot,
    const uint8_t *weight_raw,
    size_t weight_bytes,
    uint32_t packed_len,
    uint32_t bm,
    uint32_t cfg_by,
    uint32_t bmm,
    size_t attention_post_linear_rms_norm_slot,
    const float *residual,
    size_t attention_input_size,
    size_t hidden_size,
    float eps,
    float *output
) {
    if (runtime == NULL || attention_input == NULL || weight_raw == NULL || residual == NULL || output == NULL) return -1;
    @autoreleasepool {
        const size_t attention_input_bytes = attention_input_size * sizeof(float);
        const size_t hidden_bytes = hidden_size * sizeof(float);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)attention_input length:attention_input_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:attention_input length:attention_input_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> residual_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)residual length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (residual_buffer == nil) residual_buffer = [runtime->device newBufferWithBytes:residual length:hidden_bytes options:MTLResourceStorageModeShared];
        BOOL output_borrowed = YES;
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithBytesNoCopy:output length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) {
            output_borrowed = NO;
            output_buffer = [runtime->device newBufferWithBytes:output length:hidden_bytes options:MTLResourceStorageModeShared];
        }
        if (input_buffer == nil || residual_buffer == nil || output_buffer == nil) return -2;
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -3;
        int rc = termite_metal_decode_runtime_encode_attention_quantized_residual_tl1(
            runtime, command_buffer, input_buffer, attention_pre_linear_rms_norm_slot, weight_raw, weight_bytes, packed_len, bm, cfg_by, bmm, attention_post_linear_rms_norm_slot, residual_buffer, attention_input_size, hidden_size, eps, output_buffer
        );
        if (rc != 0) return rc;
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -4;
    }
}

int termite_metal_decode_runtime_apply_attention_residual_tl2(
    termite_metal_decode_runtime *runtime,
    const float *attention_input,
    size_t attention_pre_linear_rms_norm_slot,
    const uint8_t *weight_raw,
    size_t weight_bytes,
    uint32_t scale_off,
    uint32_t three_value_len,
    uint32_t three_sign_len,
    uint32_t bm,
    uint32_t cfg_by,
    uint32_t bmm,
    uint32_t three_cols,
    uint32_t two_cols,
    size_t attention_post_linear_rms_norm_slot,
    const float *residual,
    size_t attention_input_size,
    size_t hidden_size,
    float eps,
    float *output
) {
    if (runtime == NULL || attention_input == NULL || weight_raw == NULL || residual == NULL || output == NULL) return -1;
    @autoreleasepool {
        const size_t attention_input_bytes = attention_input_size * sizeof(float);
        const size_t hidden_bytes = hidden_size * sizeof(float);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)attention_input length:attention_input_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:attention_input length:attention_input_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> residual_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)residual length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (residual_buffer == nil) residual_buffer = [runtime->device newBufferWithBytes:residual length:hidden_bytes options:MTLResourceStorageModeShared];
        BOOL output_borrowed = YES;
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithBytesNoCopy:output length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) {
            output_borrowed = NO;
            output_buffer = [runtime->device newBufferWithBytes:output length:hidden_bytes options:MTLResourceStorageModeShared];
        }
        if (input_buffer == nil || residual_buffer == nil || output_buffer == nil) return -2;
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -3;
        int rc = termite_metal_decode_runtime_encode_attention_quantized_residual_tl2(
            runtime, command_buffer, input_buffer, attention_pre_linear_rms_norm_slot, weight_raw, weight_bytes, scale_off, three_value_len, three_sign_len, bm, cfg_by, bmm, three_cols, two_cols, attention_post_linear_rms_norm_slot, residual_buffer, attention_input_size, hidden_size, eps, output_buffer
        );
        if (rc != 0) return rc;
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -4;
    }
}

int termite_metal_decode_runtime_apply_attention_residual_i2_s_slot(
    termite_metal_decode_runtime *runtime,
    const float *attention_input,
    size_t attention_pre_linear_rms_norm_slot,
    size_t attention_linear_slot,
    size_t attention_post_linear_rms_norm_slot,
    const float *residual,
    size_t attention_input_size,
    size_t hidden_size,
    float eps,
    float *output
) {
    if (runtime == NULL || attention_input == NULL || residual == NULL || output == NULL) return -1;
    @autoreleasepool {
        const size_t attention_input_bytes = attention_input_size * sizeof(float);
        const size_t hidden_bytes = hidden_size * sizeof(float);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)attention_input length:attention_input_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:attention_input length:attention_input_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> residual_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)residual length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (residual_buffer == nil) residual_buffer = [runtime->device newBufferWithBytes:residual length:hidden_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithBytesNoCopy:output length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) output_buffer = [runtime->device newBufferWithBytes:output length:hidden_bytes options:MTLResourceStorageModeShared];
        if (input_buffer == nil || residual_buffer == nil || output_buffer == nil) return -2;
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -3;
        int rc = termite_metal_decode_runtime_encode_attention_quantized_residual_i2_s_slot(
            runtime, command_buffer, input_buffer, attention_pre_linear_rms_norm_slot, attention_linear_slot, attention_post_linear_rms_norm_slot, residual_buffer, 0, attention_input_size, hidden_size, eps, output_buffer
        );
        if (rc != 0) return rc;
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -4;
    }
}

int termite_metal_decode_runtime_apply_attention_residual_q4_k_slot(
    termite_metal_decode_runtime *runtime,
    const float *attention_input,
    size_t attention_pre_linear_rms_norm_slot,
    size_t attention_linear_slot,
    size_t attention_post_linear_rms_norm_slot,
    const float *residual,
    size_t attention_input_size,
    size_t hidden_size,
    float eps,
    float *output
) {
    if (runtime == NULL || attention_input == NULL || residual == NULL || output == NULL) return -1;
    @autoreleasepool {
        const size_t attention_input_bytes = attention_input_size * sizeof(float);
        const size_t hidden_bytes = hidden_size * sizeof(float);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)attention_input length:attention_input_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:attention_input length:attention_input_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> residual_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)residual length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (residual_buffer == nil) residual_buffer = [runtime->device newBufferWithBytes:residual length:hidden_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithBytesNoCopy:output length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) output_buffer = [runtime->device newBufferWithBytes:output length:hidden_bytes options:MTLResourceStorageModeShared];
        if (input_buffer == nil || residual_buffer == nil || output_buffer == nil) return -2;
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -3;
        int rc = termite_metal_decode_runtime_encode_attention_quantized_residual_q4_k_slot(
            runtime, command_buffer, input_buffer, attention_pre_linear_rms_norm_slot, attention_linear_slot, attention_post_linear_rms_norm_slot, residual_buffer, 0, attention_input_size, hidden_size, eps, output_buffer
        );
        if (rc != 0) return rc;
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -4;
    }
}

int termite_metal_decode_runtime_apply_attention_residual_q5_k_slot(
    termite_metal_decode_runtime *runtime,
    const float *attention_input,
    size_t attention_pre_linear_rms_norm_slot,
    size_t attention_linear_slot,
    size_t attention_post_linear_rms_norm_slot,
    const float *residual,
    size_t attention_input_size,
    size_t hidden_size,
    float eps,
    float *output
) {
    if (runtime == NULL || attention_input == NULL || residual == NULL || output == NULL) return -1;
    @autoreleasepool {
        const size_t attention_input_bytes = attention_input_size * sizeof(float);
        const size_t hidden_bytes = hidden_size * sizeof(float);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)attention_input length:attention_input_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:attention_input length:attention_input_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> residual_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)residual length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (residual_buffer == nil) residual_buffer = [runtime->device newBufferWithBytes:residual length:hidden_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithBytesNoCopy:output length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) output_buffer = [runtime->device newBufferWithBytes:output length:hidden_bytes options:MTLResourceStorageModeShared];
        if (input_buffer == nil || residual_buffer == nil || output_buffer == nil) return -2;
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -3;
        int rc = termite_metal_decode_runtime_encode_attention_quantized_residual_q5_k_slot(
            runtime, command_buffer, input_buffer, attention_pre_linear_rms_norm_slot, attention_linear_slot, attention_post_linear_rms_norm_slot, residual_buffer, 0, attention_input_size, hidden_size, eps, output_buffer
        );
        if (rc != 0) return rc;
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -4;
    }
}

int termite_metal_decode_runtime_apply_attention_residual_q8_0_slot(
    termite_metal_decode_runtime *runtime,
    const float *attention_input,
    size_t attention_pre_linear_rms_norm_slot,
    size_t attention_linear_slot,
    size_t attention_post_linear_rms_norm_slot,
    const float *residual,
    size_t attention_input_size,
    size_t hidden_size,
    float eps,
    float *output
) {
    if (runtime == NULL || attention_input == NULL || residual == NULL || output == NULL) return -1;
    @autoreleasepool {
        const size_t attention_input_bytes = attention_input_size * sizeof(float);
        const size_t hidden_bytes = hidden_size * sizeof(float);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)attention_input length:attention_input_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:attention_input length:attention_input_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> residual_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)residual length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (residual_buffer == nil) residual_buffer = [runtime->device newBufferWithBytes:residual length:hidden_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithBytesNoCopy:output length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) output_buffer = [runtime->device newBufferWithBytes:output length:hidden_bytes options:MTLResourceStorageModeShared];
        if (input_buffer == nil || residual_buffer == nil || output_buffer == nil) return -2;
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -3;
        int rc = termite_metal_decode_runtime_encode_attention_quantized_residual_q8_0_slot(
            runtime, command_buffer, input_buffer, attention_pre_linear_rms_norm_slot, attention_linear_slot, attention_post_linear_rms_norm_slot, residual_buffer, 0, 1, attention_input_size, hidden_size, eps, output_buffer
        );
        if (rc != 0) return rc;
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -4;
    }
}

static int termite_metal_decode_runtime_encode_attention_quantized_residual_slot(
    termite_metal_decode_runtime *runtime,
    uint32_t format,
    id<MTLCommandBuffer> command_buffer,
    id<MTLBuffer> input_buffer,
    size_t attention_pre_linear_rms_norm_slot,
    size_t attention_linear_slot,
    size_t attention_post_linear_rms_norm_slot,
    id<MTLBuffer> residual_buffer,
    size_t residual_offset,
    size_t rows,
    size_t attention_input_size,
    size_t hidden_size,
    float eps,
    id<MTLBuffer> output_buffer
) {
    switch (format) {
        case TERMITE_METAL_QUANT_FORMAT_Q8_0:
            return termite_metal_decode_runtime_encode_attention_quantized_residual_q8_0_slot(
                runtime,
                command_buffer,
                input_buffer,
                attention_pre_linear_rms_norm_slot,
                attention_linear_slot,
                attention_post_linear_rms_norm_slot,
                residual_buffer,
                residual_offset,
                rows,
                attention_input_size,
                hidden_size,
                eps,
                output_buffer
            );
        case TERMITE_METAL_QUANT_FORMAT_I2_S:
            if (rows != 1u) return -101;
            return termite_metal_decode_runtime_encode_attention_quantized_residual_i2_s_slot(
                runtime,
                command_buffer,
                input_buffer,
                attention_pre_linear_rms_norm_slot,
                attention_linear_slot,
                attention_post_linear_rms_norm_slot,
                residual_buffer,
                residual_offset,
                attention_input_size,
                hidden_size,
                eps,
                output_buffer
            );
        case TERMITE_METAL_QUANT_FORMAT_Q4_K:
            if (rows != 1u) return -102;
            return termite_metal_decode_runtime_encode_attention_quantized_residual_q4_k_slot(
                runtime,
                command_buffer,
                input_buffer,
                attention_pre_linear_rms_norm_slot,
                attention_linear_slot,
                attention_post_linear_rms_norm_slot,
                residual_buffer,
                residual_offset,
                attention_input_size,
                hidden_size,
                eps,
                output_buffer
            );
        case TERMITE_METAL_QUANT_FORMAT_Q5_K:
            if (rows != 1u) return -103;
            return termite_metal_decode_runtime_encode_attention_quantized_residual_q5_k_slot(
                runtime,
                command_buffer,
                input_buffer,
                attention_pre_linear_rms_norm_slot,
                attention_linear_slot,
                attention_post_linear_rms_norm_slot,
                residual_buffer,
                residual_offset,
                attention_input_size,
                hidden_size,
                eps,
                output_buffer
            );
        default:
            return -100;
    }
}

int termite_metal_decode_runtime_apply_attention_output_residual_device(
    termite_metal_decode_runtime *runtime,
    uint32_t format,
    void *attention_output_handle,
    size_t attention_output_offset,
    void *residual_handle,
    size_t residual_offset,
    size_t rows,
    size_t attention_input_size,
    size_t hidden_size,
    size_t attention_linear_slot,
    size_t attention_pre_linear_rms_norm_slot,
    size_t attention_post_linear_rms_norm_slot,
    float eps,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || attention_output_handle == NULL || residual_handle == NULL || output_handle == NULL) return -1;
    if (rows == 0 || attention_input_size == 0 || hidden_size == 0) return -2;
    if (attention_output_offset != 0 || output_offset != 0) return -3;
    @autoreleasepool {
        id<MTLBuffer> attention_output_buffer = (__bridge id<MTLBuffer>)attention_output_handle;
        id<MTLBuffer> residual_buffer = (__bridge id<MTLBuffer>)residual_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        if (attention_output_buffer == nil || residual_buffer == nil || output_buffer == nil) return -4;
        if (rows > SIZE_MAX / attention_input_size || rows > SIZE_MAX / hidden_size) return -5;
        const size_t attention_input_bytes = rows * attention_input_size * sizeof(float);
        const size_t hidden_bytes = rows * hidden_size * sizeof(float);
        if (attention_input_bytes > attention_output_buffer.length ||
            residual_offset + hidden_bytes > residual_buffer.length ||
            hidden_bytes > output_buffer.length) return -6;
        if (runtime->active_frame_cb != nil) {
            if (termite_metal_decode_runtime_retain_frame_resource(runtime, attention_output_buffer) != 0) return -7;
            if (termite_metal_decode_runtime_retain_frame_resource(runtime, residual_buffer) != 0) return -7;
            if (termite_metal_decode_runtime_retain_frame_resource(runtime, output_buffer) != 0) return -7;
        }
        bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -8;
        const int rc = termite_metal_decode_runtime_encode_attention_quantized_residual_slot(
            runtime,
            format,
            command_buffer,
            attention_output_buffer,
            attention_pre_linear_rms_norm_slot,
            attention_linear_slot,
            attention_post_linear_rms_norm_slot,
            residual_buffer,
            residual_offset,
            rows,
            attention_input_size,
            hidden_size,
            eps,
            output_buffer
        );
        if (rc != 0) return rc;
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -9);
    }
}

int termite_metal_decode_runtime_apply_attention_span_residual_tl1(
    termite_metal_decode_runtime *runtime,
    uint32_t format,
    const float *q,
    const uint8_t *encoded_key,
    const float *v,
    size_t kv_tokens,
    size_t num_heads,
    size_t num_kv_heads,
    size_t head_dim,
    size_t key_row_bytes,
    size_t v_row_stride,
    size_t base_key_row_bytes,
    size_t query_position,
    size_t kv_position_offset,
    size_t sliding_window,
    size_t attention_pre_linear_rms_norm_slot,
    const uint8_t *weight_raw,
    size_t weight_bytes,
    uint32_t packed_len,
    uint32_t bm,
    uint32_t cfg_by,
    uint32_t bmm,
    size_t attention_post_linear_rms_norm_slot,
    const float *residual,
    size_t attention_input_size,
    size_t hidden_size,
    float eps,
    float *output
) {
    if (runtime == NULL || q == NULL || encoded_key == NULL || v == NULL || weight_raw == NULL || residual == NULL || output == NULL) return -1;
    @autoreleasepool {
        const size_t q_bytes = num_heads * head_dim * sizeof(float);
        const size_t attention_input_bytes = attention_input_size * sizeof(float);
        const size_t hidden_bytes = hidden_size * sizeof(float);
        id<MTLBuffer> q_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)q length:q_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (q_buffer == nil) q_buffer = [runtime->device newBufferWithBytes:q length:q_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> residual_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)residual length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (residual_buffer == nil) residual_buffer = [runtime->device newBufferWithBytes:residual length:hidden_bytes options:MTLResourceStorageModeShared];
        const size_t max_hidden_bytes = attention_input_bytes > hidden_bytes ? attention_input_bytes : hidden_bytes;
        if (termite_metal_decode_runtime_ensure_hot_buffers(runtime, max_hidden_bytes, 0) != 0) return -2;
        id<MTLBuffer> attention_output_buffer = runtime->hot_hidden_buffers[0];
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithBytesNoCopy:output length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) output_buffer = [runtime->device newBufferWithBytes:output length:hidden_bytes options:MTLResourceStorageModeShared];
        if (q_buffer == nil || residual_buffer == nil || attention_output_buffer == nil || output_buffer == nil) return -2;
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -3;
        int rc = termite_metal_decode_runtime_encode_replace_attention_span(
            runtime, command_buffer, encoded_key, v, kv_tokens, key_row_bytes, v_row_stride, kv_position_offset
        );
        if (rc == 0) {
            rc = termite_metal_decode_runtime_encode_attention_span(
                runtime, command_buffer, format, q_buffer, 0, kv_tokens, num_heads, num_kv_heads, head_dim, key_row_bytes, base_key_row_bytes, query_position, kv_position_offset, sliding_window, attention_output_buffer, 0
            );
        }
        if (rc == 0) {
            rc = termite_metal_decode_runtime_encode_attention_quantized_residual_tl1(
                runtime, command_buffer, attention_output_buffer, attention_pre_linear_rms_norm_slot, weight_raw, weight_bytes, packed_len, bm, cfg_by, bmm, attention_post_linear_rms_norm_slot, residual_buffer, attention_input_size, hidden_size, eps, output_buffer
            );
        }
        if (rc == 0) {
            [command_buffer commit];
            [command_buffer waitUntilCompleted];
            rc = command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -4;
        }
        return rc;
    }
}

int termite_metal_decode_runtime_apply_attention_span_residual_tl2(
    termite_metal_decode_runtime *runtime,
    uint32_t format,
    const float *q,
    const uint8_t *encoded_key,
    const float *v,
    size_t kv_tokens,
    size_t num_heads,
    size_t num_kv_heads,
    size_t head_dim,
    size_t key_row_bytes,
    size_t v_row_stride,
    size_t base_key_row_bytes,
    size_t query_position,
    size_t kv_position_offset,
    size_t sliding_window,
    size_t attention_pre_linear_rms_norm_slot,
    const uint8_t *weight_raw,
    size_t weight_bytes,
    uint32_t scale_off,
    uint32_t three_value_len,
    uint32_t three_sign_len,
    uint32_t bm,
    uint32_t cfg_by,
    uint32_t bmm,
    uint32_t three_cols,
    uint32_t two_cols,
    size_t attention_post_linear_rms_norm_slot,
    const float *residual,
    size_t attention_input_size,
    size_t hidden_size,
    float eps,
    float *output
) {
    if (runtime == NULL || q == NULL || encoded_key == NULL || v == NULL || weight_raw == NULL || residual == NULL || output == NULL) return -1;
    @autoreleasepool {
        const size_t q_bytes = num_heads * head_dim * sizeof(float);
        const size_t attention_input_bytes = attention_input_size * sizeof(float);
        const size_t hidden_bytes = hidden_size * sizeof(float);
        id<MTLBuffer> q_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)q length:q_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (q_buffer == nil) q_buffer = [runtime->device newBufferWithBytes:q length:q_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> residual_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)residual length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (residual_buffer == nil) residual_buffer = [runtime->device newBufferWithBytes:residual length:hidden_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> attention_output_buffer = [runtime->device newBufferWithLength:attention_input_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithBytesNoCopy:output length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) output_buffer = [runtime->device newBufferWithBytes:output length:hidden_bytes options:MTLResourceStorageModeShared];
        if (q_buffer == nil || residual_buffer == nil || attention_output_buffer == nil || output_buffer == nil) return -2;
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -3;
        int rc = termite_metal_decode_runtime_encode_replace_attention_span(
            runtime, command_buffer, encoded_key, v, kv_tokens, key_row_bytes, v_row_stride, kv_position_offset
        );
        if (rc == 0) {
            rc = termite_metal_decode_runtime_encode_attention_span(
                runtime, command_buffer, format, q_buffer, 0, kv_tokens, num_heads, num_kv_heads, head_dim, key_row_bytes, base_key_row_bytes, query_position, kv_position_offset, sliding_window, attention_output_buffer, 0
            );
        }
        if (rc == 0) {
            rc = termite_metal_decode_runtime_encode_attention_quantized_residual_tl2(
                runtime, command_buffer, attention_output_buffer, attention_pre_linear_rms_norm_slot, weight_raw, weight_bytes, scale_off, three_value_len, three_sign_len, bm, cfg_by, bmm, three_cols, two_cols, attention_post_linear_rms_norm_slot, residual_buffer, attention_input_size, hidden_size, eps, output_buffer
            );
        }
        if (rc == 0) {
            [command_buffer commit];
            [command_buffer waitUntilCompleted];
            rc = command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -4;
        }
        return rc;
    }
}

int termite_metal_decode_runtime_apply_attention_span_residual_i2_s_slot(
    termite_metal_decode_runtime *runtime,
    uint32_t format,
    const float *q,
    const uint8_t *encoded_key,
    const float *v,
    size_t kv_tokens,
    size_t num_heads,
    size_t num_kv_heads,
    size_t head_dim,
    size_t key_row_bytes,
    size_t v_row_stride,
    size_t base_key_row_bytes,
    size_t query_position,
    size_t kv_position_offset,
    size_t sliding_window,
    size_t attention_pre_linear_rms_norm_slot,
    size_t attention_linear_slot,
    size_t attention_post_linear_rms_norm_slot,
    const float *residual,
    size_t attention_input_size,
    size_t hidden_size,
    float eps,
    float *output
) {
    if (runtime == NULL || q == NULL || encoded_key == NULL || v == NULL || residual == NULL || output == NULL) return -1;
    @autoreleasepool {
        const size_t q_bytes = num_heads * head_dim * sizeof(float);
        const size_t attention_input_bytes = attention_input_size * sizeof(float);
        const size_t hidden_bytes = hidden_size * sizeof(float);
        id<MTLBuffer> q_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)q length:q_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (q_buffer == nil) q_buffer = [runtime->device newBufferWithBytes:q length:q_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> residual_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)residual length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (residual_buffer == nil) residual_buffer = [runtime->device newBufferWithBytes:residual length:hidden_bytes options:MTLResourceStorageModeShared];
        const size_t max_hidden_bytes = attention_input_bytes > hidden_bytes ? attention_input_bytes : hidden_bytes;
        if (termite_metal_decode_runtime_ensure_hot_buffers(runtime, max_hidden_bytes, 0) != 0) return -2;
        id<MTLBuffer> attention_output_buffer = runtime->hot_hidden_buffers[0];
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithBytesNoCopy:output length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) output_buffer = [runtime->device newBufferWithBytes:output length:hidden_bytes options:MTLResourceStorageModeShared];
        if (q_buffer == nil || residual_buffer == nil || attention_output_buffer == nil || output_buffer == nil) return -2;
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -3;
        int rc = termite_metal_decode_runtime_encode_replace_attention_span(
            runtime, command_buffer, encoded_key, v, kv_tokens, key_row_bytes, v_row_stride, kv_position_offset
        );
        if (rc == 0) {
            rc = termite_metal_decode_runtime_encode_attention_span(
                runtime, command_buffer, format, q_buffer, 0, kv_tokens, num_heads, num_kv_heads, head_dim, key_row_bytes, base_key_row_bytes, query_position, kv_position_offset, sliding_window, attention_output_buffer, 0
            );
        }
        if (rc == 0) {
            rc = termite_metal_decode_runtime_encode_attention_quantized_residual_i2_s_slot(
                runtime, command_buffer, attention_output_buffer, attention_pre_linear_rms_norm_slot, attention_linear_slot, attention_post_linear_rms_norm_slot, residual_buffer, 0, attention_input_size, hidden_size, eps, output_buffer
            );
        }
        if (rc == 0) {
            [command_buffer commit];
            [command_buffer waitUntilCompleted];
            rc = command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -4;
        }
        return rc;
    }
}

int termite_metal_decode_runtime_apply_attention_f32_span_residual_q8_0_slot(
    termite_metal_decode_runtime *runtime,
    uint32_t format,
    const float *q,
    const float *k,
    const float *v,
    size_t kv_tokens,
    size_t num_heads,
    size_t num_kv_heads,
    size_t head_dim,
    size_t key_row_bytes,
    size_t v_row_stride,
    size_t base_key_row_bytes,
    size_t query_position,
    size_t kv_position_offset,
    size_t sliding_window,
    size_t attention_pre_linear_rms_norm_slot,
    size_t attention_linear_slot,
    size_t attention_post_linear_rms_norm_slot,
    const float *residual,
    size_t attention_input_size,
    size_t hidden_size,
    float eps,
    size_t layer_index,
    float *output
) {
    if (runtime == NULL || q == NULL || k == NULL || v == NULL || residual == NULL || output == NULL) return -1;
    if (layer_index >= TERMITE_METAL_ATTENTION_SPAN_SLOT_CAPACITY) return -2;
    @autoreleasepool {
        const size_t q_bytes = num_heads * head_dim * sizeof(float);
        const size_t attention_input_bytes = attention_input_size * sizeof(float);
        const size_t hidden_bytes = hidden_size * sizeof(float);
        id<MTLBuffer> q_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)q length:q_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (q_buffer == nil) q_buffer = [runtime->device newBufferWithBytes:q length:q_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> residual_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)residual length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (residual_buffer == nil) residual_buffer = [runtime->device newBufferWithBytes:residual length:hidden_bytes options:MTLResourceStorageModeShared];
        const size_t max_hidden_bytes = attention_input_bytes > hidden_bytes ? attention_input_bytes : hidden_bytes;
        if (termite_metal_decode_runtime_ensure_hot_buffers(runtime, max_hidden_bytes, 0) != 0) return -3;
        id<MTLBuffer> attention_output_buffer = runtime->hot_hidden_buffers[0];
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithBytesNoCopy:output length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) output_buffer = [runtime->device newBufferWithBytes:output length:hidden_bytes options:MTLResourceStorageModeShared];
        if (q_buffer == nil || residual_buffer == nil || attention_output_buffer == nil || output_buffer == nil) return -3;
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -4;
        int rc = termite_metal_decode_runtime_encode_replace_attention_span_from_f32_key_slot(
            runtime,
            command_buffer,
            layer_index,
            format,
            k,
            v,
            kv_tokens,
            num_kv_heads,
            head_dim,
            key_row_bytes,
            base_key_row_bytes,
            v_row_stride,
            kv_position_offset
        );
        if (rc == 0) {
            rc = termite_metal_decode_runtime_encode_attention_span_slot(
                runtime,
                command_buffer,
                layer_index,
                format,
                q_buffer,
                0,
                kv_tokens,
                num_heads,
                num_kv_heads,
                head_dim,
                key_row_bytes,
                base_key_row_bytes,
                query_position,
                kv_position_offset,
                sliding_window,
                attention_output_buffer,
                0
            );
        }
        if (rc == 0) {
            rc = termite_metal_decode_runtime_encode_attention_quantized_residual_q8_0_slot(
                runtime,
                command_buffer,
                attention_output_buffer,
                attention_pre_linear_rms_norm_slot,
                attention_linear_slot,
                attention_post_linear_rms_norm_slot,
                residual_buffer,
                0,
                1,
                attention_input_size,
                hidden_size,
                eps,
                output_buffer
            );
        }
        if (rc == 0) {
            [command_buffer commit];
            [command_buffer waitUntilCompleted];
            rc = command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -5;
        }
        return rc;
    }
}

int termite_metal_decode_runtime_apply_gated_ffn_residual_tl1(
    termite_metal_decode_runtime *runtime,
    const float *input,
    const float *residual,
    size_t attention_input_size,
    size_t hidden_size,
    size_t intermediate_size,
    uint32_t activation_kind,
    const uint8_t *gate_weight_raw,
    size_t gate_weight_bytes,
    uint32_t gate_packed_len,
    uint32_t gate_bm,
    uint32_t gate_cfg_by,
    uint32_t gate_bmm,
    const uint8_t *up_weight_raw,
    size_t up_weight_bytes,
    uint32_t up_packed_len,
    uint32_t up_bm,
    uint32_t up_cfg_by,
    uint32_t up_bmm,
    size_t post_gate_rms_norm_slot,
    const uint8_t *down_weight_raw,
    size_t down_weight_bytes,
    uint32_t down_packed_len,
    uint32_t down_bm,
    uint32_t down_cfg_by,
    uint32_t down_bmm,
    float *output
) {
    if (runtime == NULL || input == NULL || residual == NULL || gate_weight_raw == NULL || up_weight_raw == NULL || down_weight_raw == NULL || output == NULL) return -1;
    if (runtime->tl1_pipeline == nil || runtime->activation_pipeline == nil || runtime->multiply_pipeline == nil || runtime->add_pipeline == nil) return -2;
    if (hidden_size == 0 || intermediate_size == 0 || hidden_size > UINT32_MAX || intermediate_size > UINT32_MAX) return -3;
    if (post_gate_rms_norm_slot != SIZE_MAX) {
        if (post_gate_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return -4;
        if (runtime->rms_norm_slot_prepared[post_gate_rms_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[post_gate_rms_norm_slot] != intermediate_size) return -5;
    }
    @autoreleasepool {
        const size_t hidden_bytes = hidden_size * sizeof(float);
        const size_t intermediate_bytes = intermediate_size * sizeof(float);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)input length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:input length:hidden_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> residual_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)residual length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (residual_buffer == nil) residual_buffer = [runtime->device newBufferWithBytes:residual length:hidden_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> gate_weight_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)gate_weight_raw length:gate_weight_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (gate_weight_buffer == nil) gate_weight_buffer = [runtime->device newBufferWithBytes:gate_weight_raw length:gate_weight_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> up_weight_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)up_weight_raw length:up_weight_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (up_weight_buffer == nil) up_weight_buffer = [runtime->device newBufferWithBytes:up_weight_raw length:up_weight_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> down_weight_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)down_weight_raw length:down_weight_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (down_weight_buffer == nil) down_weight_buffer = [runtime->device newBufferWithBytes:down_weight_raw length:down_weight_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> gate_output_buffer = [runtime->device newBufferWithLength:intermediate_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> up_output_buffer = [runtime->device newBufferWithLength:intermediate_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> activated_buffer = [runtime->device newBufferWithLength:intermediate_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> gated_buffer = [runtime->device newBufferWithLength:intermediate_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> normed_gated_buffer = post_gate_rms_norm_slot != SIZE_MAX ? [runtime->device newBufferWithLength:intermediate_bytes options:MTLResourceStorageModePrivate] : nil;
        id<MTLBuffer> projected_buffer = [runtime->device newBufferWithLength:hidden_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithBytesNoCopy:output length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) output_buffer = [runtime->device newBufferWithBytes:output length:hidden_bytes options:MTLResourceStorageModeShared];
        if (input_buffer == nil || residual_buffer == nil || gate_weight_buffer == nil || up_weight_buffer == nil || down_weight_buffer == nil || gate_output_buffer == nil || up_output_buffer == nil || activated_buffer == nil || gated_buffer == nil || projected_buffer == nil || output_buffer == nil || (post_gate_rms_norm_slot != SIZE_MAX && normed_gated_buffer == nil)) return -6;

        termite_metal_tl1_params gate_params = { .rows = 1, .in_dim = (uint32_t)hidden_size, .out_dim = (uint32_t)intermediate_size, .packed_len = gate_packed_len, .bm = gate_bm, .cfg_by = gate_cfg_by, .bmm = gate_bmm };
        termite_metal_tl1_params up_params = { .rows = 1, .in_dim = (uint32_t)hidden_size, .out_dim = (uint32_t)intermediate_size, .packed_len = up_packed_len, .bm = up_bm, .cfg_by = up_cfg_by, .bmm = up_bmm };
        termite_metal_tl1_params down_params = { .rows = 1, .in_dim = (uint32_t)intermediate_size, .out_dim = (uint32_t)hidden_size, .packed_len = down_packed_len, .bm = down_bm, .cfg_by = down_cfg_by, .bmm = down_bmm };
        termite_metal_apply_activation_params activation_params = { .activation_kind = activation_kind, .rows = 1, .dim = (uint32_t)intermediate_size };
        termite_metal_apply_add_params multiply_params = { .rows = 1, .dim = (uint32_t)intermediate_size };
        termite_metal_apply_layer_norm_params rms_params = { .hidden_size = (uint32_t)intermediate_size, .eps = 0.0f };
        termite_metal_apply_add_params add_params = { .rows = 1, .dim = (uint32_t)hidden_size };
        id<MTLBuffer> gate_params_buffer = [runtime->device newBufferWithBytes:&gate_params length:sizeof(gate_params) options:MTLResourceStorageModeShared];
        id<MTLBuffer> up_params_buffer = [runtime->device newBufferWithBytes:&up_params length:sizeof(up_params) options:MTLResourceStorageModeShared];
        id<MTLBuffer> down_params_buffer = [runtime->device newBufferWithBytes:&down_params length:sizeof(down_params) options:MTLResourceStorageModeShared];
        id<MTLBuffer> activation_params_buffer = [runtime->device newBufferWithBytes:&activation_params length:sizeof(activation_params) options:MTLResourceStorageModeShared];
        id<MTLBuffer> multiply_params_buffer = [runtime->device newBufferWithBytes:&multiply_params length:sizeof(multiply_params) options:MTLResourceStorageModeShared];
        id<MTLBuffer> rms_params_buffer = post_gate_rms_norm_slot != SIZE_MAX ? [runtime->device newBufferWithBytes:&rms_params length:sizeof(rms_params) options:MTLResourceStorageModeShared] : nil;
        id<MTLBuffer> add_params_buffer = [runtime->device newBufferWithBytes:&add_params length:sizeof(add_params) options:MTLResourceStorageModeShared];
        if (gate_params_buffer == nil || up_params_buffer == nil || down_params_buffer == nil || activation_params_buffer == nil || multiply_params_buffer == nil || add_params_buffer == nil || (post_gate_rms_norm_slot != SIZE_MAX && rms_params_buffer == nil)) return -7;

        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -8;

        id<MTLComputeCommandEncoder> gate_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (gate_encoder == nil) return -9;
        [gate_encoder setComputePipelineState:runtime->tl1_pipeline];
        [gate_encoder setBuffer:input_buffer offset:0 atIndex:0];
        [gate_encoder setBuffer:gate_weight_buffer offset:0 atIndex:1];
        [gate_encoder setBuffer:gate_output_buffer offset:0 atIndex:2];
        [gate_encoder setBuffer:gate_params_buffer offset:0 atIndex:3];
        [gate_encoder dispatchThreads:MTLSizeMake(intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->tl1_pipeline, intermediate_size), 1, 1)];
        [gate_encoder endEncoding];

        id<MTLComputeCommandEncoder> up_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (up_encoder == nil) return -10;
        [up_encoder setComputePipelineState:runtime->tl1_pipeline];
        [up_encoder setBuffer:input_buffer offset:0 atIndex:0];
        [up_encoder setBuffer:up_weight_buffer offset:0 atIndex:1];
        [up_encoder setBuffer:up_output_buffer offset:0 atIndex:2];
        [up_encoder setBuffer:up_params_buffer offset:0 atIndex:3];
        [up_encoder dispatchThreads:MTLSizeMake(intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->tl1_pipeline, intermediate_size), 1, 1)];
        [up_encoder endEncoding];

        id<MTLComputeCommandEncoder> activation_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (activation_encoder == nil) return -11;
        [activation_encoder setComputePipelineState:runtime->activation_pipeline];
        [activation_encoder setBuffer:gate_output_buffer offset:0 atIndex:0];
        [activation_encoder setBuffer:activated_buffer offset:0 atIndex:1];
        [activation_encoder setBuffer:activation_params_buffer offset:0 atIndex:2];
        [activation_encoder dispatchThreads:MTLSizeMake(intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->activation_pipeline, intermediate_size), 1, 1)];
        [activation_encoder endEncoding];

        id<MTLComputeCommandEncoder> multiply_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (multiply_encoder == nil) return -12;
        [multiply_encoder setComputePipelineState:runtime->multiply_pipeline];
        [multiply_encoder setBuffer:activated_buffer offset:0 atIndex:0];
        [multiply_encoder setBuffer:up_output_buffer offset:0 atIndex:1];
        [multiply_encoder setBuffer:gated_buffer offset:0 atIndex:2];
        [multiply_encoder setBuffer:multiply_params_buffer offset:0 atIndex:3];
        [multiply_encoder dispatchThreads:MTLSizeMake(intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->multiply_pipeline, intermediate_size), 1, 1)];
        [multiply_encoder endEncoding];

        id<MTLBuffer> down_input_buffer = gated_buffer;
        if (post_gate_rms_norm_slot != SIZE_MAX) {
            id<MTLComputeCommandEncoder> rms_encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_RMS_NORM);
            if (rms_encoder == nil) return -13;
            [rms_encoder setComputePipelineState:runtime->rms_norm_pipeline];
            [rms_encoder setBuffer:gated_buffer offset:0 atIndex:0];
            [rms_encoder setBuffer:runtime->rms_norm_weight_buffers[post_gate_rms_norm_slot] offset:0 atIndex:1];
            [rms_encoder setBuffer:normed_gated_buffer offset:0 atIndex:2];
            [rms_encoder setBuffer:rms_params_buffer offset:0 atIndex:3];
            [rms_encoder dispatchThreads:MTLSizeMake(intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->rms_norm_pipeline, intermediate_size), 1, 1)];
            [rms_encoder endEncoding];
            down_input_buffer = normed_gated_buffer;
        }

        id<MTLComputeCommandEncoder> down_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (down_encoder == nil) return -14;
        [down_encoder setComputePipelineState:runtime->tl1_pipeline];
        [down_encoder setBuffer:down_input_buffer offset:0 atIndex:0];
        [down_encoder setBuffer:down_weight_buffer offset:0 atIndex:1];
        [down_encoder setBuffer:projected_buffer offset:0 atIndex:2];
        [down_encoder setBuffer:down_params_buffer offset:0 atIndex:3];
        [down_encoder dispatchThreads:MTLSizeMake(hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->tl1_pipeline, hidden_size), 1, 1)];
        [down_encoder endEncoding];

        id<MTLComputeCommandEncoder> add_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (add_encoder == nil) return -15;
        [add_encoder setComputePipelineState:runtime->add_pipeline];
        [add_encoder setBuffer:projected_buffer offset:0 atIndex:0];
        [add_encoder setBuffer:residual_buffer offset:0 atIndex:1];
        [add_encoder setBuffer:output_buffer offset:0 atIndex:2];
        [add_encoder setBuffer:add_params_buffer offset:0 atIndex:3];
        [add_encoder dispatchThreads:MTLSizeMake(hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->add_pipeline, hidden_size), 1, 1)];
        [add_encoder endEncoding];

        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -16;
    }
}

int termite_metal_decode_runtime_apply_gated_ffn_residual_i2_s(
    termite_metal_decode_runtime *runtime,
    const float *input,
    const float *residual,
    size_t hidden_size,
    size_t intermediate_size,
    uint32_t activation_kind,
    const uint8_t *gate_weight_raw,
    size_t gate_weight_bytes,
    const uint8_t *up_weight_raw,
    size_t up_weight_bytes,
    size_t post_gate_rms_norm_slot,
    const uint8_t *down_weight_raw,
    size_t down_weight_bytes,
    float *output
) {
    if (runtime == NULL || input == NULL || residual == NULL || gate_weight_raw == NULL || up_weight_raw == NULL || down_weight_raw == NULL || output == NULL) return -1;
    if (runtime->i2_s_pipeline == nil || runtime->i2_s_pair_pipeline == nil || runtime->activation_pipeline == nil || runtime->multiply_pipeline == nil || runtime->add_pipeline == nil) return -2;
    if (hidden_size == 0 || intermediate_size == 0 || hidden_size > UINT32_MAX || intermediate_size > UINT32_MAX) return -3;
    if (hidden_size % 128u != 0 || intermediate_size % 128u != 0) return -4;
    if (post_gate_rms_norm_slot != SIZE_MAX) {
        if (post_gate_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return -5;
        if (runtime->rms_norm_slot_prepared[post_gate_rms_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[post_gate_rms_norm_slot] != intermediate_size) return -6;
    }
    const size_t gate_row_blocks = hidden_size / 128u;
    const size_t down_row_blocks = intermediate_size / 128u;
    if (gate_row_blocks * 32u * intermediate_size > gate_weight_bytes || gate_row_blocks * 32u * intermediate_size > up_weight_bytes || down_row_blocks * 32u * hidden_size > down_weight_bytes) return -7;
    @autoreleasepool {
        const size_t hidden_bytes = hidden_size * sizeof(float);
        const size_t intermediate_bytes = intermediate_size * sizeof(float);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)input length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:input length:hidden_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> residual_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)residual length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (residual_buffer == nil) residual_buffer = [runtime->device newBufferWithBytes:residual length:hidden_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> gate_weight_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)gate_weight_raw length:gate_weight_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (gate_weight_buffer == nil) gate_weight_buffer = [runtime->device newBufferWithBytes:gate_weight_raw length:gate_weight_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> up_weight_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)up_weight_raw length:up_weight_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (up_weight_buffer == nil) up_weight_buffer = [runtime->device newBufferWithBytes:up_weight_raw length:up_weight_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> down_weight_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)down_weight_raw length:down_weight_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (down_weight_buffer == nil) down_weight_buffer = [runtime->device newBufferWithBytes:down_weight_raw length:down_weight_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> gate_output_buffer = [runtime->device newBufferWithLength:intermediate_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> up_output_buffer = [runtime->device newBufferWithLength:intermediate_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> activated_buffer = [runtime->device newBufferWithLength:intermediate_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> gated_buffer = [runtime->device newBufferWithLength:intermediate_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> normed_gated_buffer = post_gate_rms_norm_slot != SIZE_MAX ? [runtime->device newBufferWithLength:intermediate_bytes options:MTLResourceStorageModePrivate] : nil;
        id<MTLBuffer> projected_buffer = [runtime->device newBufferWithLength:hidden_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithBytesNoCopy:output length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) output_buffer = [runtime->device newBufferWithBytes:output length:hidden_bytes options:MTLResourceStorageModeShared];
        if (input_buffer == nil || residual_buffer == nil || gate_weight_buffer == nil || up_weight_buffer == nil || down_weight_buffer == nil || gate_output_buffer == nil || up_output_buffer == nil || activated_buffer == nil || gated_buffer == nil || projected_buffer == nil || output_buffer == nil || (post_gate_rms_norm_slot != SIZE_MAX && normed_gated_buffer == nil)) return -8;

        termite_metal_linear_params pair_params = { .rows = 1, .in_dim = (uint32_t)hidden_size, .out_dim = (uint32_t)intermediate_size, .row_blocks = (uint32_t)gate_row_blocks };
        termite_metal_linear_params down_params = { .rows = 1, .in_dim = (uint32_t)intermediate_size, .out_dim = (uint32_t)hidden_size, .row_blocks = (uint32_t)down_row_blocks };
        termite_metal_apply_activation_params activation_params = { .activation_kind = activation_kind, .rows = 1, .dim = (uint32_t)intermediate_size };
        termite_metal_apply_add_params multiply_params = { .rows = 1, .dim = (uint32_t)intermediate_size };
        termite_metal_apply_layer_norm_params rms_params = { .hidden_size = (uint32_t)intermediate_size, .eps = 0.0f };
        termite_metal_apply_add_params add_params = { .rows = 1, .dim = (uint32_t)hidden_size };
        id<MTLBuffer> pair_params_buffer = [runtime->device newBufferWithBytes:&pair_params length:sizeof(pair_params) options:MTLResourceStorageModeShared];
        id<MTLBuffer> down_params_buffer = [runtime->device newBufferWithBytes:&down_params length:sizeof(down_params) options:MTLResourceStorageModeShared];
        id<MTLBuffer> activation_params_buffer = [runtime->device newBufferWithBytes:&activation_params length:sizeof(activation_params) options:MTLResourceStorageModeShared];
        id<MTLBuffer> multiply_params_buffer = [runtime->device newBufferWithBytes:&multiply_params length:sizeof(multiply_params) options:MTLResourceStorageModeShared];
        id<MTLBuffer> rms_params_buffer = post_gate_rms_norm_slot != SIZE_MAX ? [runtime->device newBufferWithBytes:&rms_params length:sizeof(rms_params) options:MTLResourceStorageModeShared] : nil;
        id<MTLBuffer> add_params_buffer = [runtime->device newBufferWithBytes:&add_params length:sizeof(add_params) options:MTLResourceStorageModeShared];
        if (pair_params_buffer == nil || down_params_buffer == nil || activation_params_buffer == nil || multiply_params_buffer == nil || add_params_buffer == nil || (post_gate_rms_norm_slot != SIZE_MAX && rms_params_buffer == nil)) return -9;

        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -10;

        id<MTLComputeCommandEncoder> pair_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (pair_encoder == nil) return -11;
        [pair_encoder setComputePipelineState:runtime->i2_s_pair_pipeline];
        [pair_encoder setBuffer:input_buffer offset:0 atIndex:0];
        [pair_encoder setBuffer:gate_weight_buffer offset:0 atIndex:1];
        [pair_encoder setBuffer:up_weight_buffer offset:0 atIndex:2];
        [pair_encoder setBuffer:gate_output_buffer offset:0 atIndex:3];
        [pair_encoder setBuffer:up_output_buffer offset:0 atIndex:4];
        [pair_encoder setBuffer:pair_params_buffer offset:0 atIndex:5];
        [pair_encoder dispatchThreads:MTLSizeMake(intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->i2_s_pair_pipeline, intermediate_size), 1, 1)];
        [pair_encoder endEncoding];

        id<MTLComputeCommandEncoder> activation_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (activation_encoder == nil) return -12;
        [activation_encoder setComputePipelineState:runtime->activation_pipeline];
        [activation_encoder setBuffer:gate_output_buffer offset:0 atIndex:0];
        [activation_encoder setBuffer:activated_buffer offset:0 atIndex:1];
        [activation_encoder setBuffer:activation_params_buffer offset:0 atIndex:2];
        [activation_encoder dispatchThreads:MTLSizeMake(intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->activation_pipeline, intermediate_size), 1, 1)];
        [activation_encoder endEncoding];

        id<MTLComputeCommandEncoder> multiply_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (multiply_encoder == nil) return -13;
        [multiply_encoder setComputePipelineState:runtime->multiply_pipeline];
        [multiply_encoder setBuffer:activated_buffer offset:0 atIndex:0];
        [multiply_encoder setBuffer:up_output_buffer offset:0 atIndex:1];
        [multiply_encoder setBuffer:gated_buffer offset:0 atIndex:2];
        [multiply_encoder setBuffer:multiply_params_buffer offset:0 atIndex:3];
        [multiply_encoder dispatchThreads:MTLSizeMake(intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->multiply_pipeline, intermediate_size), 1, 1)];
        [multiply_encoder endEncoding];

        id<MTLBuffer> down_input_buffer = gated_buffer;
        if (post_gate_rms_norm_slot != SIZE_MAX) {
            id<MTLComputeCommandEncoder> rms_encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_RMS_NORM);
            if (rms_encoder == nil) return -14;
            [rms_encoder setComputePipelineState:runtime->rms_norm_pipeline];
            [rms_encoder setBuffer:gated_buffer offset:0 atIndex:0];
            [rms_encoder setBuffer:runtime->rms_norm_weight_buffers[post_gate_rms_norm_slot] offset:0 atIndex:1];
            [rms_encoder setBuffer:normed_gated_buffer offset:0 atIndex:2];
            [rms_encoder setBuffer:rms_params_buffer offset:0 atIndex:3];
            [rms_encoder dispatchThreads:MTLSizeMake(intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->rms_norm_pipeline, intermediate_size), 1, 1)];
            [rms_encoder endEncoding];
            down_input_buffer = normed_gated_buffer;
        }

        id<MTLComputeCommandEncoder> down_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (down_encoder == nil) return -15;
        [down_encoder setComputePipelineState:runtime->i2_s_pipeline];
        [down_encoder setBuffer:down_input_buffer offset:0 atIndex:0];
        [down_encoder setBuffer:down_weight_buffer offset:0 atIndex:1];
        [down_encoder setBuffer:projected_buffer offset:0 atIndex:2];
        [down_encoder setBuffer:down_params_buffer offset:0 atIndex:3];
        [down_encoder dispatchThreads:MTLSizeMake(hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->i2_s_pipeline, hidden_size), 1, 1)];
        [down_encoder endEncoding];

        id<MTLComputeCommandEncoder> add_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (add_encoder == nil) return -16;
        [add_encoder setComputePipelineState:runtime->add_pipeline];
        [add_encoder setBuffer:projected_buffer offset:0 atIndex:0];
        [add_encoder setBuffer:residual_buffer offset:0 atIndex:1];
        [add_encoder setBuffer:output_buffer offset:0 atIndex:2];
        [add_encoder setBuffer:add_params_buffer offset:0 atIndex:3];
        [add_encoder dispatchThreads:MTLSizeMake(hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->add_pipeline, hidden_size), 1, 1)];
        [add_encoder endEncoding];

        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -17;
    }
}

int termite_metal_decode_runtime_apply_gated_ffn_residual_i2_s_slots(
    termite_metal_decode_runtime *runtime,
    const float *input,
    const float *residual,
    size_t rows,
    size_t hidden_size,
    size_t intermediate_size,
    uint32_t activation_kind,
    float eps,
    size_t gate_linear_slot,
    size_t up_linear_slot,
    size_t post_gate_rms_norm_slot,
    size_t down_linear_slot,
    float *output
) {
    if (runtime == NULL || input == NULL || residual == NULL || output == NULL) return -1;
    if (runtime->i2_s_pipeline == nil || runtime->i2_s_pair_pipeline == nil || runtime->activation_pipeline == nil || runtime->multiply_pipeline == nil || runtime->add_pipeline == nil) return -2;
    if (rows == 0 || hidden_size == 0 || intermediate_size == 0 || rows > UINT32_MAX || hidden_size > UINT32_MAX || intermediate_size > UINT32_MAX) return -3;
    if (hidden_size % 128u != 0 || intermediate_size % 128u != 0) return -4;
    if (gate_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || up_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || down_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -5;
    termite_metal_quant_linear_slot_view gate_view;
    termite_metal_quant_linear_slot_view up_view;
    termite_metal_quant_linear_slot_view down_view;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_I2_S, gate_linear_slot, hidden_size, intermediate_size, &gate_view) != 0) return -6;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_I2_S, up_linear_slot, hidden_size, intermediate_size, &up_view) != 0) return -7;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_I2_S, down_linear_slot, intermediate_size, hidden_size, &down_view) != 0) return -8;
    if (post_gate_rms_norm_slot != SIZE_MAX) {
        if (post_gate_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return -9;
        if (runtime->rms_norm_slot_prepared[post_gate_rms_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[post_gate_rms_norm_slot] != intermediate_size) return -10;
    }
    @autoreleasepool {
        const size_t hidden_bytes = rows * hidden_size * sizeof(float);
        const size_t intermediate_bytes = rows * intermediate_size * sizeof(float);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)input length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:input length:hidden_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> residual_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)residual length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (residual_buffer == nil) residual_buffer = [runtime->device newBufferWithBytes:residual length:hidden_bytes options:MTLResourceStorageModeShared];
        if (termite_metal_decode_runtime_ensure_hot_buffers(runtime, hidden_bytes, intermediate_bytes) != 0) return -11;
        id<MTLBuffer> gate_output_buffer = runtime->hot_intermediate_buffers[0];
        id<MTLBuffer> up_output_buffer = runtime->hot_intermediate_buffers[1];
        id<MTLBuffer> activated_buffer = runtime->hot_intermediate_buffers[2];
        id<MTLBuffer> gated_buffer = runtime->hot_intermediate_buffers[3];
        id<MTLBuffer> normed_gated_buffer = post_gate_rms_norm_slot != SIZE_MAX ? runtime->hot_intermediate_buffers[4] : nil;
        id<MTLBuffer> projected_buffer = runtime->hot_hidden_buffers[0];
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithBytesNoCopy:output length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) output_buffer = [runtime->device newBufferWithBytes:output length:hidden_bytes options:MTLResourceStorageModeShared];
        if (input_buffer == nil || residual_buffer == nil || gate_output_buffer == nil || up_output_buffer == nil || activated_buffer == nil || gated_buffer == nil || projected_buffer == nil || output_buffer == nil || (post_gate_rms_norm_slot != SIZE_MAX && normed_gated_buffer == nil)) return -11;

        termite_metal_linear_params pair_params = { .rows = (uint32_t)rows, .in_dim = (uint32_t)hidden_size, .out_dim = (uint32_t)intermediate_size, .row_blocks = (uint32_t)(hidden_size / 128u) };
        termite_metal_linear_params down_params = { .rows = (uint32_t)rows, .in_dim = (uint32_t)intermediate_size, .out_dim = (uint32_t)hidden_size, .row_blocks = (uint32_t)(intermediate_size / 128u) };
        termite_metal_apply_activation_params activation_params = { .activation_kind = activation_kind, .rows = (uint32_t)rows, .dim = (uint32_t)intermediate_size };
        termite_metal_apply_add_params multiply_params = { .rows = (uint32_t)rows, .dim = (uint32_t)intermediate_size };
        termite_metal_apply_row_norm_params rms_params = { .rows = (uint32_t)rows, .hidden_size = (uint32_t)intermediate_size, .eps = 0.0f };
        termite_metal_apply_row_norm_params post_down_rms_params = { .rows = (uint32_t)rows, .hidden_size = (uint32_t)hidden_size, .eps = 0.0f };
        termite_metal_apply_add_params add_params = { .rows = (uint32_t)rows, .dim = (uint32_t)hidden_size };
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -13;

        id<MTLComputeCommandEncoder> pair_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (pair_encoder == nil) return -14;
        [pair_encoder setComputePipelineState:runtime->i2_s_pair_pipeline];
        [pair_encoder setBuffer:input_buffer offset:0 atIndex:0];
        [pair_encoder setBuffer:gate_view.weight_buffer offset:0 atIndex:1];
        [pair_encoder setBuffer:up_view.weight_buffer offset:0 atIndex:2];
        [pair_encoder setBuffer:gate_output_buffer offset:0 atIndex:3];
        [pair_encoder setBuffer:up_output_buffer offset:0 atIndex:4];
        [pair_encoder setBytes:&pair_params length:sizeof(pair_params) atIndex:5];
        [pair_encoder dispatchThreads:MTLSizeMake(rows * intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->i2_s_pair_pipeline, rows * intermediate_size), 1, 1)];
        [pair_encoder endEncoding];

        id<MTLComputeCommandEncoder> activation_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (activation_encoder == nil) return -15;
        [activation_encoder setComputePipelineState:runtime->activation_pipeline];
        [activation_encoder setBuffer:gate_output_buffer offset:0 atIndex:0];
        [activation_encoder setBuffer:activated_buffer offset:0 atIndex:1];
        [activation_encoder setBytes:&activation_params length:sizeof(activation_params) atIndex:2];
        [activation_encoder dispatchThreads:MTLSizeMake(rows * intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->activation_pipeline, rows * intermediate_size), 1, 1)];
        [activation_encoder endEncoding];

        id<MTLComputeCommandEncoder> multiply_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (multiply_encoder == nil) return -16;
        [multiply_encoder setComputePipelineState:runtime->multiply_pipeline];
        [multiply_encoder setBuffer:activated_buffer offset:0 atIndex:0];
        [multiply_encoder setBuffer:up_output_buffer offset:0 atIndex:1];
        [multiply_encoder setBuffer:gated_buffer offset:0 atIndex:2];
        [multiply_encoder setBytes:&multiply_params length:sizeof(multiply_params) atIndex:3];
        [multiply_encoder dispatchThreads:MTLSizeMake(rows * intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->multiply_pipeline, rows * intermediate_size), 1, 1)];
        [multiply_encoder endEncoding];

        id<MTLBuffer> down_input_buffer = gated_buffer;
        if (post_gate_rms_norm_slot != SIZE_MAX) {
            id<MTLComputeCommandEncoder> rms_encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_RMS_NORM);
            if (rms_encoder == nil) return -17;
            [rms_encoder setComputePipelineState:runtime->rms_norm_rows_pipeline];
            [rms_encoder setBuffer:gated_buffer offset:0 atIndex:0];
            [rms_encoder setBuffer:runtime->rms_norm_weight_buffers[post_gate_rms_norm_slot] offset:0 atIndex:1];
            [rms_encoder setBuffer:normed_gated_buffer offset:0 atIndex:2];
            [rms_encoder setBytes:&rms_params length:sizeof(rms_params) atIndex:3];
            [rms_encoder dispatchThreads:MTLSizeMake(rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->rms_norm_rows_pipeline, rows), 1, 1)];
            [rms_encoder endEncoding];
            down_input_buffer = normed_gated_buffer;
        }

        id<MTLComputeCommandEncoder> down_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (down_encoder == nil) return -18;
        [down_encoder setComputePipelineState:runtime->i2_s_pipeline];
        [down_encoder setBuffer:down_input_buffer offset:0 atIndex:0];
        [down_encoder setBuffer:down_view.weight_buffer offset:0 atIndex:1];
        [down_encoder setBuffer:projected_buffer offset:0 atIndex:2];
        [down_encoder setBytes:&down_params length:sizeof(down_params) atIndex:3];
        [down_encoder dispatchThreads:MTLSizeMake(rows * hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->i2_s_pipeline, rows * hidden_size), 1, 1)];
        [down_encoder endEncoding];

        id<MTLComputeCommandEncoder> add_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (add_encoder == nil) return -19;
        [add_encoder setComputePipelineState:runtime->add_pipeline];
        [add_encoder setBuffer:projected_buffer offset:0 atIndex:0];
        [add_encoder setBuffer:residual_buffer offset:0 atIndex:1];
        [add_encoder setBuffer:output_buffer offset:0 atIndex:2];
        [add_encoder setBytes:&add_params length:sizeof(add_params) atIndex:3];
        [add_encoder dispatchThreads:MTLSizeMake(rows * hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->add_pipeline, rows * hidden_size), 1, 1)];
        [add_encoder endEncoding];

        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -20;
    }
}

int termite_metal_decode_runtime_apply_gated_ffn_residual_q4_k_slots(
    termite_metal_decode_runtime *runtime,
    const float *input,
    const float *residual,
    size_t rows,
    size_t hidden_size,
    size_t intermediate_size,
    uint32_t activation_kind,
    size_t gate_linear_slot,
    size_t up_linear_slot,
    size_t post_gate_rms_norm_slot,
    size_t post_down_rms_norm_slot,
    size_t down_linear_slot,
    float *output
) {
    if (runtime == NULL || input == NULL || residual == NULL || output == NULL) return -1;
    if (runtime->q4_k_pipeline == nil || runtime->q4_k_pair_pipeline == nil || runtime->activation_pipeline == nil || runtime->multiply_pipeline == nil || runtime->add_pipeline == nil) return -2;
    if (rows == 0 || hidden_size == 0 || intermediate_size == 0 || rows > UINT32_MAX || hidden_size > UINT32_MAX || intermediate_size > UINT32_MAX) return -3;
    const float eps = 0.0f;
    if (hidden_size % 256u != 0 || intermediate_size % 256u != 0) return -4;
    if (gate_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || up_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || down_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -5;
    termite_metal_quant_linear_slot_view gate_view;
    termite_metal_quant_linear_slot_view up_view;
    termite_metal_quant_linear_slot_view down_view;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q4_K, gate_linear_slot, hidden_size, intermediate_size, &gate_view) != 0) return -6;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q4_K, up_linear_slot, hidden_size, intermediate_size, &up_view) != 0) return -7;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q4_K, down_linear_slot, intermediate_size, hidden_size, &down_view) != 0) return -8;
    if (post_gate_rms_norm_slot != SIZE_MAX) {
        if (post_gate_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return -9;
        if (runtime->rms_norm_slot_prepared[post_gate_rms_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[post_gate_rms_norm_slot] != intermediate_size) return -10;
    }
    if (post_down_rms_norm_slot != SIZE_MAX) {
        if (post_down_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return -11;
        if (runtime->rms_norm_slot_prepared[post_down_rms_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[post_down_rms_norm_slot] != hidden_size) return -12;
    }
    @autoreleasepool {
        const size_t hidden_bytes = rows * hidden_size * sizeof(float);
        const size_t intermediate_bytes = rows * intermediate_size * sizeof(float);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)input length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:input length:hidden_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> residual_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)residual length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (residual_buffer == nil) residual_buffer = [runtime->device newBufferWithBytes:residual length:hidden_bytes options:MTLResourceStorageModeShared];
        if (termite_metal_decode_runtime_ensure_hot_buffers(runtime, hidden_bytes, intermediate_bytes) != 0) return -13;
        id<MTLBuffer> gate_output_buffer = runtime->hot_intermediate_buffers[0];
        id<MTLBuffer> up_output_buffer = runtime->hot_intermediate_buffers[1];
        id<MTLBuffer> activated_buffer = runtime->hot_intermediate_buffers[2];
        id<MTLBuffer> gated_buffer = runtime->hot_intermediate_buffers[3];
        id<MTLBuffer> normed_gated_buffer = post_gate_rms_norm_slot != SIZE_MAX ? runtime->hot_intermediate_buffers[4] : nil;
        id<MTLBuffer> projected_buffer = runtime->hot_hidden_buffers[0];
        id<MTLBuffer> post_down_buffer = post_down_rms_norm_slot != SIZE_MAX ? runtime->hot_hidden_buffers[1] : nil;
        BOOL output_borrowed = YES;
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithBytesNoCopy:output length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) {
            output_borrowed = NO;
            output_buffer = [runtime->device newBufferWithBytes:output length:hidden_bytes options:MTLResourceStorageModeShared];
        }
        if (input_buffer == nil || residual_buffer == nil || gate_output_buffer == nil || up_output_buffer == nil || activated_buffer == nil || gated_buffer == nil || projected_buffer == nil || output_buffer == nil || (post_gate_rms_norm_slot != SIZE_MAX && normed_gated_buffer == nil) || (post_down_rms_norm_slot != SIZE_MAX && post_down_buffer == nil)) return -13;

        termite_metal_linear_params down_params = { .rows = (uint32_t)rows, .in_dim = (uint32_t)intermediate_size, .out_dim = (uint32_t)hidden_size, .row_blocks = (uint32_t)(intermediate_size / 256u) };
        termite_metal_apply_activation_params activation_params = { .activation_kind = activation_kind, .rows = (uint32_t)rows, .dim = (uint32_t)intermediate_size };
        termite_metal_apply_add_params multiply_params = { .rows = (uint32_t)rows, .dim = (uint32_t)intermediate_size };
        termite_metal_apply_row_norm_params rms_params = { .rows = (uint32_t)rows, .hidden_size = (uint32_t)intermediate_size, .eps = eps };
        termite_metal_apply_row_norm_params post_down_rms_params = { .rows = (uint32_t)rows, .hidden_size = (uint32_t)hidden_size, .eps = eps };
        termite_metal_apply_add_params add_params = { .rows = (uint32_t)rows, .dim = (uint32_t)hidden_size };
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -15;

        id<MTLComputeCommandEncoder> pair_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (pair_encoder == nil) return -16;
        termite_metal_quant_matmul_descriptor pair_descriptor = {
            .slot = gate_linear_slot,
            .format = TERMITE_METAL_QUANT_FORMAT_Q4_K,
            .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
            .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_PAIR,
            .fallback_pipeline = runtime->q4_k_pair_pipeline,
            .input_buffer = input_buffer,
            .weight_buffer = gate_view.weight_buffer,
            .second_weight_buffer = up_view.weight_buffer,
            .output_buffer = gate_output_buffer,
            .second_output_buffer = up_output_buffer,
            .rows = rows,
            .in_dim = hidden_size,
            .out_dim = intermediate_size,
            .values_per_block = gate_view.values_per_block,
            .bytes_per_block = gate_view.bytes_per_block,
            .failure_code = -16,
        };
        if (termite_metal_encode_quant_matmul_descriptor_on_encoder(runtime, pair_encoder, &pair_descriptor) != 0) return -16;
        [pair_encoder endEncoding];

        id<MTLComputeCommandEncoder> activation_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (activation_encoder == nil) return -17;
        [activation_encoder setComputePipelineState:runtime->activation_pipeline];
        [activation_encoder setBuffer:gate_output_buffer offset:0 atIndex:0];
        [activation_encoder setBuffer:activated_buffer offset:0 atIndex:1];
        [activation_encoder setBytes:&activation_params length:sizeof(activation_params) atIndex:2];
        [activation_encoder dispatchThreads:MTLSizeMake(rows * intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->activation_pipeline, rows * intermediate_size), 1, 1)];
        [activation_encoder endEncoding];

        id<MTLComputeCommandEncoder> multiply_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (multiply_encoder == nil) return -18;
        [multiply_encoder setComputePipelineState:runtime->multiply_pipeline];
        [multiply_encoder setBuffer:activated_buffer offset:0 atIndex:0];
        [multiply_encoder setBuffer:up_output_buffer offset:0 atIndex:1];
        [multiply_encoder setBuffer:gated_buffer offset:0 atIndex:2];
        [multiply_encoder setBytes:&multiply_params length:sizeof(multiply_params) atIndex:3];
        [multiply_encoder dispatchThreads:MTLSizeMake(rows * intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->multiply_pipeline, rows * intermediate_size), 1, 1)];
        [multiply_encoder endEncoding];

        id<MTLBuffer> down_input_buffer = gated_buffer;
        if (post_gate_rms_norm_slot != SIZE_MAX) {
            if (rows == 1) {
                if (termite_metal_encode_rms_norm_reduce(
                        runtime,
                        command_buffer,
                        gated_buffer,
                        0,
                        post_gate_rms_norm_slot,
                        normed_gated_buffer,
                        0,
                        intermediate_size,
                        0.0f,
                        -19) != 0) return -19;
            } else {
                id<MTLComputeCommandEncoder> rms_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
                if (rms_encoder == nil) return -19;
                [rms_encoder setComputePipelineState:runtime->rms_norm_rows_pipeline];
                [rms_encoder setBuffer:gated_buffer offset:0 atIndex:0];
                [rms_encoder setBuffer:runtime->rms_norm_weight_buffers[post_gate_rms_norm_slot] offset:0 atIndex:1];
                [rms_encoder setBuffer:normed_gated_buffer offset:0 atIndex:2];
                [rms_encoder setBytes:&rms_params length:sizeof(rms_params) atIndex:3];
                [rms_encoder dispatchThreads:MTLSizeMake(rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->rms_norm_rows_pipeline, rows), 1, 1)];
                [rms_encoder endEncoding];
            }
            down_input_buffer = normed_gated_buffer;
        }

        id<MTLComputeCommandEncoder> down_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (down_encoder == nil) return -20;
        [down_encoder setComputePipelineState:runtime->q4_k_pipeline];
        [down_encoder setBuffer:down_input_buffer offset:0 atIndex:0];
        [down_encoder setBuffer:down_view.weight_buffer offset:0 atIndex:1];
        [down_encoder setBuffer:projected_buffer offset:0 atIndex:2];
        [down_encoder setBytes:&down_params length:sizeof(down_params) atIndex:3];
        [down_encoder dispatchThreads:MTLSizeMake(rows * hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->q4_k_pipeline, rows * hidden_size), 1, 1)];
        [down_encoder endEncoding];

        id<MTLBuffer> add_input_buffer = projected_buffer;
        if (post_down_rms_norm_slot != SIZE_MAX) {
            id<MTLComputeCommandEncoder> post_down_encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_RMS_NORM);
            if (post_down_encoder == nil) return -21;
            [post_down_encoder setComputePipelineState:runtime->rms_norm_rows_pipeline];
            [post_down_encoder setBuffer:projected_buffer offset:0 atIndex:0];
            [post_down_encoder setBuffer:runtime->rms_norm_weight_buffers[post_down_rms_norm_slot] offset:0 atIndex:1];
            [post_down_encoder setBuffer:post_down_buffer offset:0 atIndex:2];
            [post_down_encoder setBytes:&post_down_rms_params length:sizeof(post_down_rms_params) atIndex:3];
            [post_down_encoder dispatchThreads:MTLSizeMake(rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->rms_norm_rows_pipeline, rows), 1, 1)];
            [post_down_encoder endEncoding];
            add_input_buffer = post_down_buffer;
        }

        id<MTLComputeCommandEncoder> add_encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_FFN);
        if (add_encoder == nil) return -22;
        [add_encoder setComputePipelineState:runtime->add_pipeline];
        [add_encoder setBuffer:add_input_buffer offset:0 atIndex:0];
        [add_encoder setBuffer:residual_buffer offset:0 atIndex:1];
        [add_encoder setBuffer:output_buffer offset:0 atIndex:2];
        [add_encoder setBytes:&add_params length:sizeof(add_params) atIndex:3];
        [add_encoder dispatchThreads:MTLSizeMake(rows * hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->add_pipeline, rows * hidden_size), 1, 1)];
        [add_encoder endEncoding];

        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        if (command_buffer.status != MTLCommandBufferStatusCompleted) return -23;
        if (!output_borrowed) {
            memcpy(output, [output_buffer contents], hidden_bytes);
        }
        return 0;
    }
}

int termite_metal_decode_runtime_apply_rms_norm_i2_s_linear_residual(
    termite_metal_decode_runtime *runtime,
    const float *input,
    const float *residual,
    size_t intermediate_size,
    size_t hidden_size,
    size_t post_gate_rms_norm_slot,
    const uint8_t *down_weight_raw,
    size_t down_weight_bytes,
    float *output
) {
    if (runtime == NULL || input == NULL || residual == NULL || down_weight_raw == NULL || output == NULL) return -1;
    if (runtime->rms_norm_pipeline == nil || runtime->i2_s_pipeline == nil || runtime->add_pipeline == nil) return -2;
    if (intermediate_size == 0 || hidden_size == 0 || intermediate_size > UINT32_MAX || hidden_size > UINT32_MAX) return -3;
    if (intermediate_size % 128u != 0) return -4;
    if (post_gate_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return -5;
    if (runtime->rms_norm_slot_prepared[post_gate_rms_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[post_gate_rms_norm_slot] != intermediate_size) return -6;
    const size_t down_row_blocks = intermediate_size / 128u;
    if (down_row_blocks * 32u * hidden_size > down_weight_bytes) return -7;
    @autoreleasepool {
        const size_t hidden_bytes = hidden_size * sizeof(float);
        const size_t intermediate_bytes = intermediate_size * sizeof(float);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)input length:intermediate_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:input length:intermediate_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> residual_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)residual length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (residual_buffer == nil) residual_buffer = [runtime->device newBufferWithBytes:residual length:hidden_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> down_weight_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)down_weight_raw length:down_weight_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (down_weight_buffer == nil) down_weight_buffer = [runtime->device newBufferWithBytes:down_weight_raw length:down_weight_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> normed_input_buffer = [runtime->device newBufferWithLength:intermediate_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> projected_buffer = [runtime->device newBufferWithLength:hidden_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithBytesNoCopy:output length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) output_buffer = [runtime->device newBufferWithBytes:output length:hidden_bytes options:MTLResourceStorageModeShared];
        if (input_buffer == nil || residual_buffer == nil || down_weight_buffer == nil || normed_input_buffer == nil || projected_buffer == nil || output_buffer == nil) return -8;

        termite_metal_apply_layer_norm_params rms_params = { .hidden_size = (uint32_t)intermediate_size, .eps = 0.0f };
        termite_metal_linear_params down_params = { .rows = 1, .in_dim = (uint32_t)intermediate_size, .out_dim = (uint32_t)hidden_size, .row_blocks = (uint32_t)down_row_blocks };
        termite_metal_apply_add_params add_params = { .rows = 1, .dim = (uint32_t)hidden_size };
        id<MTLBuffer> rms_params_buffer = [runtime->device newBufferWithBytes:&rms_params length:sizeof(rms_params) options:MTLResourceStorageModeShared];
        id<MTLBuffer> down_params_buffer = [runtime->device newBufferWithBytes:&down_params length:sizeof(down_params) options:MTLResourceStorageModeShared];
        id<MTLBuffer> add_params_buffer = [runtime->device newBufferWithBytes:&add_params length:sizeof(add_params) options:MTLResourceStorageModeShared];
        if (rms_params_buffer == nil || down_params_buffer == nil || add_params_buffer == nil) return -9;

        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -10;

        id<MTLComputeCommandEncoder> rms_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (rms_encoder == nil) return -11;
        [rms_encoder setComputePipelineState:runtime->rms_norm_pipeline];
        [rms_encoder setBuffer:input_buffer offset:0 atIndex:0];
        [rms_encoder setBuffer:runtime->rms_norm_weight_buffers[post_gate_rms_norm_slot] offset:0 atIndex:1];
        [rms_encoder setBuffer:normed_input_buffer offset:0 atIndex:2];
        [rms_encoder setBuffer:rms_params_buffer offset:0 atIndex:3];
        [rms_encoder dispatchThreads:MTLSizeMake(intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->rms_norm_pipeline, intermediate_size), 1, 1)];
        [rms_encoder endEncoding];

        id<MTLComputeCommandEncoder> down_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (down_encoder == nil) return -12;
        [down_encoder setComputePipelineState:runtime->i2_s_pipeline];
        [down_encoder setBuffer:normed_input_buffer offset:0 atIndex:0];
        [down_encoder setBuffer:down_weight_buffer offset:0 atIndex:1];
        [down_encoder setBuffer:projected_buffer offset:0 atIndex:2];
        [down_encoder setBuffer:down_params_buffer offset:0 atIndex:3];
        [down_encoder dispatchThreads:MTLSizeMake(hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->i2_s_pipeline, hidden_size), 1, 1)];
        [down_encoder endEncoding];

        id<MTLComputeCommandEncoder> add_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (add_encoder == nil) return -13;
        [add_encoder setComputePipelineState:runtime->add_pipeline];
        [add_encoder setBuffer:projected_buffer offset:0 atIndex:0];
        [add_encoder setBuffer:residual_buffer offset:0 atIndex:1];
        [add_encoder setBuffer:output_buffer offset:0 atIndex:2];
        [add_encoder setBuffer:add_params_buffer offset:0 atIndex:3];
        [add_encoder dispatchThreads:MTLSizeMake(hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->add_pipeline, hidden_size), 1, 1)];
        [add_encoder endEncoding];

        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -14;
    }
}

int termite_metal_decode_runtime_apply_rms_norm_i2_s_linear_residual_slot(
    termite_metal_decode_runtime *runtime,
    const float *input,
    const float *residual,
    size_t intermediate_size,
    size_t hidden_size,
    size_t post_gate_rms_norm_slot,
    size_t down_linear_slot,
    float *output
) {
    if (runtime == NULL || input == NULL || residual == NULL || output == NULL) return -1;
    if (runtime->rms_norm_pipeline == nil || runtime->i2_s_pipeline == nil || runtime->add_pipeline == nil) return -2;
    if (down_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -3;
    termite_metal_quant_linear_slot_view down_view;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_I2_S, down_linear_slot, intermediate_size, hidden_size, &down_view) != 0) return -4;
    if (intermediate_size == 0 || hidden_size == 0 || intermediate_size > UINT32_MAX || hidden_size > UINT32_MAX) return -6;
    if (post_gate_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return -7;
    if (runtime->rms_norm_slot_prepared[post_gate_rms_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[post_gate_rms_norm_slot] != intermediate_size) return -8;
    @autoreleasepool {
        const size_t hidden_bytes = hidden_size * sizeof(float);
        const size_t intermediate_bytes = intermediate_size * sizeof(float);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)input length:intermediate_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:input length:intermediate_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> residual_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)residual length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (residual_buffer == nil) residual_buffer = [runtime->device newBufferWithBytes:residual length:hidden_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> normed_input_buffer = [runtime->device newBufferWithLength:intermediate_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> projected_buffer = [runtime->device newBufferWithLength:hidden_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithBytesNoCopy:output length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) output_buffer = [runtime->device newBufferWithBytes:output length:hidden_bytes options:MTLResourceStorageModeShared];
        if (input_buffer == nil || residual_buffer == nil || normed_input_buffer == nil || projected_buffer == nil || output_buffer == nil) return -9;

        termite_metal_apply_layer_norm_params rms_params = { .hidden_size = (uint32_t)intermediate_size, .eps = 0.0f };
        termite_metal_linear_params down_params = { .rows = 1, .in_dim = (uint32_t)intermediate_size, .out_dim = (uint32_t)hidden_size, .row_blocks = (uint32_t)(intermediate_size / 128u) };
        termite_metal_apply_add_params add_params = { .rows = 1, .dim = (uint32_t)hidden_size };
        id<MTLBuffer> rms_params_buffer = [runtime->device newBufferWithBytes:&rms_params length:sizeof(rms_params) options:MTLResourceStorageModeShared];
        id<MTLBuffer> down_params_buffer = [runtime->device newBufferWithBytes:&down_params length:sizeof(down_params) options:MTLResourceStorageModeShared];
        id<MTLBuffer> add_params_buffer = [runtime->device newBufferWithBytes:&add_params length:sizeof(add_params) options:MTLResourceStorageModeShared];
        if (rms_params_buffer == nil || down_params_buffer == nil || add_params_buffer == nil) return -10;

        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -11;

        id<MTLComputeCommandEncoder> rms_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (rms_encoder == nil) return -12;
        [rms_encoder setComputePipelineState:runtime->rms_norm_pipeline];
        [rms_encoder setBuffer:input_buffer offset:0 atIndex:0];
        [rms_encoder setBuffer:runtime->rms_norm_weight_buffers[post_gate_rms_norm_slot] offset:0 atIndex:1];
        [rms_encoder setBuffer:normed_input_buffer offset:0 atIndex:2];
        [rms_encoder setBuffer:rms_params_buffer offset:0 atIndex:3];
        [rms_encoder dispatchThreads:MTLSizeMake(intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->rms_norm_pipeline, intermediate_size), 1, 1)];
        [rms_encoder endEncoding];

        id<MTLComputeCommandEncoder> down_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (down_encoder == nil) return -13;
        [down_encoder setComputePipelineState:runtime->i2_s_pipeline];
        [down_encoder setBuffer:normed_input_buffer offset:0 atIndex:0];
        [down_encoder setBuffer:down_view.weight_buffer offset:0 atIndex:1];
        [down_encoder setBuffer:projected_buffer offset:0 atIndex:2];
        [down_encoder setBuffer:down_params_buffer offset:0 atIndex:3];
        [down_encoder dispatchThreads:MTLSizeMake(hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->i2_s_pipeline, hidden_size), 1, 1)];
        [down_encoder endEncoding];

        id<MTLComputeCommandEncoder> add_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (add_encoder == nil) return -14;
        [add_encoder setComputePipelineState:runtime->add_pipeline];
        [add_encoder setBuffer:projected_buffer offset:0 atIndex:0];
        [add_encoder setBuffer:residual_buffer offset:0 atIndex:1];
        [add_encoder setBuffer:output_buffer offset:0 atIndex:2];
        [add_encoder setBuffer:add_params_buffer offset:0 atIndex:3];
        [add_encoder dispatchThreads:MTLSizeMake(hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->add_pipeline, hidden_size), 1, 1)];
        [add_encoder endEncoding];

        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -15;
    }
}

int termite_metal_decode_runtime_apply_gated_ffn_residual_tl2(
    termite_metal_decode_runtime *runtime,
    const float *input,
    const float *residual,
    size_t hidden_size,
    size_t intermediate_size,
    uint32_t activation_kind,
    const uint8_t *gate_weight_raw,
    size_t gate_weight_bytes,
    uint32_t gate_scale_off,
    uint32_t gate_three_value_len,
    uint32_t gate_three_sign_len,
    uint32_t gate_bm,
    uint32_t gate_cfg_by,
    uint32_t gate_bmm,
    uint32_t gate_three_cols,
    uint32_t gate_two_cols,
    const uint8_t *up_weight_raw,
    size_t up_weight_bytes,
    uint32_t up_scale_off,
    uint32_t up_three_value_len,
    uint32_t up_three_sign_len,
    uint32_t up_bm,
    uint32_t up_cfg_by,
    uint32_t up_bmm,
    uint32_t up_three_cols,
    uint32_t up_two_cols,
    size_t post_gate_rms_norm_slot,
    const uint8_t *down_weight_raw,
    size_t down_weight_bytes,
    uint32_t down_scale_off,
    uint32_t down_three_value_len,
    uint32_t down_three_sign_len,
    uint32_t down_bm,
    uint32_t down_cfg_by,
    uint32_t down_bmm,
    uint32_t down_three_cols,
    uint32_t down_two_cols,
    float *output
) {
    if (runtime == NULL || input == NULL || residual == NULL || gate_weight_raw == NULL || up_weight_raw == NULL || down_weight_raw == NULL || output == NULL) return -1;
    if (runtime->tl2_pipeline == nil || runtime->activation_pipeline == nil || runtime->multiply_pipeline == nil || runtime->add_pipeline == nil) return -2;
    if (hidden_size == 0 || intermediate_size == 0 || hidden_size > UINT32_MAX || intermediate_size > UINT32_MAX) return -3;
    if (post_gate_rms_norm_slot != SIZE_MAX) {
        if (post_gate_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return -4;
        if (runtime->rms_norm_slot_prepared[post_gate_rms_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[post_gate_rms_norm_slot] != intermediate_size) return -5;
    }
    @autoreleasepool {
        const size_t hidden_bytes = hidden_size * sizeof(float);
        const size_t intermediate_bytes = intermediate_size * sizeof(float);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)input length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:input length:hidden_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> residual_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)residual length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (residual_buffer == nil) residual_buffer = [runtime->device newBufferWithBytes:residual length:hidden_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> gate_weight_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)gate_weight_raw length:gate_weight_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (gate_weight_buffer == nil) gate_weight_buffer = [runtime->device newBufferWithBytes:gate_weight_raw length:gate_weight_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> up_weight_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)up_weight_raw length:up_weight_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (up_weight_buffer == nil) up_weight_buffer = [runtime->device newBufferWithBytes:up_weight_raw length:up_weight_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> down_weight_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)down_weight_raw length:down_weight_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (down_weight_buffer == nil) down_weight_buffer = [runtime->device newBufferWithBytes:down_weight_raw length:down_weight_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> gate_output_buffer = [runtime->device newBufferWithLength:intermediate_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> up_output_buffer = [runtime->device newBufferWithLength:intermediate_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> activated_buffer = [runtime->device newBufferWithLength:intermediate_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> gated_buffer = [runtime->device newBufferWithLength:intermediate_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> normed_gated_buffer = post_gate_rms_norm_slot != SIZE_MAX ? [runtime->device newBufferWithLength:intermediate_bytes options:MTLResourceStorageModePrivate] : nil;
        id<MTLBuffer> projected_buffer = [runtime->device newBufferWithLength:hidden_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithBytesNoCopy:output length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) output_buffer = [runtime->device newBufferWithBytes:output length:hidden_bytes options:MTLResourceStorageModeShared];
        if (input_buffer == nil || residual_buffer == nil || gate_weight_buffer == nil || up_weight_buffer == nil || down_weight_buffer == nil || gate_output_buffer == nil || up_output_buffer == nil || activated_buffer == nil || gated_buffer == nil || projected_buffer == nil || output_buffer == nil || (post_gate_rms_norm_slot != SIZE_MAX && normed_gated_buffer == nil)) return -6;

        termite_metal_tl2_params gate_params = { .rows = 1, .in_dim = (uint32_t)hidden_size, .out_dim = (uint32_t)intermediate_size, .scale_off = gate_scale_off, .three_value_len = gate_three_value_len, .three_sign_len = gate_three_sign_len, .bm = gate_bm, .cfg_by = gate_cfg_by, .bmm = gate_bmm, .three_cols = gate_three_cols, .two_cols = gate_two_cols };
        termite_metal_tl2_params up_params = { .rows = 1, .in_dim = (uint32_t)hidden_size, .out_dim = (uint32_t)intermediate_size, .scale_off = up_scale_off, .three_value_len = up_three_value_len, .three_sign_len = up_three_sign_len, .bm = up_bm, .cfg_by = up_cfg_by, .bmm = up_bmm, .three_cols = up_three_cols, .two_cols = up_two_cols };
        termite_metal_tl2_params down_params = { .rows = 1, .in_dim = (uint32_t)intermediate_size, .out_dim = (uint32_t)hidden_size, .scale_off = down_scale_off, .three_value_len = down_three_value_len, .three_sign_len = down_three_sign_len, .bm = down_bm, .cfg_by = down_cfg_by, .bmm = down_bmm, .three_cols = down_three_cols, .two_cols = down_two_cols };
        termite_metal_apply_activation_params activation_params = { .activation_kind = activation_kind, .rows = 1, .dim = (uint32_t)intermediate_size };
        termite_metal_apply_add_params multiply_params = { .rows = 1, .dim = (uint32_t)intermediate_size };
        termite_metal_apply_layer_norm_params rms_params = { .hidden_size = (uint32_t)intermediate_size, .eps = 0.0f };
        termite_metal_apply_add_params add_params = { .rows = 1, .dim = (uint32_t)hidden_size };
        id<MTLBuffer> gate_params_buffer = [runtime->device newBufferWithBytes:&gate_params length:sizeof(gate_params) options:MTLResourceStorageModeShared];
        id<MTLBuffer> up_params_buffer = [runtime->device newBufferWithBytes:&up_params length:sizeof(up_params) options:MTLResourceStorageModeShared];
        id<MTLBuffer> down_params_buffer = [runtime->device newBufferWithBytes:&down_params length:sizeof(down_params) options:MTLResourceStorageModeShared];
        id<MTLBuffer> activation_params_buffer = [runtime->device newBufferWithBytes:&activation_params length:sizeof(activation_params) options:MTLResourceStorageModeShared];
        id<MTLBuffer> multiply_params_buffer = [runtime->device newBufferWithBytes:&multiply_params length:sizeof(multiply_params) options:MTLResourceStorageModeShared];
        id<MTLBuffer> rms_params_buffer = post_gate_rms_norm_slot != SIZE_MAX ? [runtime->device newBufferWithBytes:&rms_params length:sizeof(rms_params) options:MTLResourceStorageModeShared] : nil;
        id<MTLBuffer> add_params_buffer = [runtime->device newBufferWithBytes:&add_params length:sizeof(add_params) options:MTLResourceStorageModeShared];
        if (gate_params_buffer == nil || up_params_buffer == nil || down_params_buffer == nil || activation_params_buffer == nil || multiply_params_buffer == nil || add_params_buffer == nil || (post_gate_rms_norm_slot != SIZE_MAX && rms_params_buffer == nil)) return -7;

        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -8;

        id<MTLComputeCommandEncoder> gate_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (gate_encoder == nil) return -9;
        [gate_encoder setComputePipelineState:runtime->tl2_pipeline];
        [gate_encoder setBuffer:input_buffer offset:0 atIndex:0];
        [gate_encoder setBuffer:gate_weight_buffer offset:0 atIndex:1];
        [gate_encoder setBuffer:gate_output_buffer offset:0 atIndex:2];
        [gate_encoder setBuffer:gate_params_buffer offset:0 atIndex:3];
        [gate_encoder dispatchThreads:MTLSizeMake(intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->tl2_pipeline, intermediate_size), 1, 1)];
        [gate_encoder endEncoding];

        id<MTLComputeCommandEncoder> up_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (up_encoder == nil) return -10;
        [up_encoder setComputePipelineState:runtime->tl2_pipeline];
        [up_encoder setBuffer:input_buffer offset:0 atIndex:0];
        [up_encoder setBuffer:up_weight_buffer offset:0 atIndex:1];
        [up_encoder setBuffer:up_output_buffer offset:0 atIndex:2];
        [up_encoder setBuffer:up_params_buffer offset:0 atIndex:3];
        [up_encoder dispatchThreads:MTLSizeMake(intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->tl2_pipeline, intermediate_size), 1, 1)];
        [up_encoder endEncoding];

        id<MTLComputeCommandEncoder> activation_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (activation_encoder == nil) return -11;
        [activation_encoder setComputePipelineState:runtime->activation_pipeline];
        [activation_encoder setBuffer:gate_output_buffer offset:0 atIndex:0];
        [activation_encoder setBuffer:activated_buffer offset:0 atIndex:1];
        [activation_encoder setBuffer:activation_params_buffer offset:0 atIndex:2];
        [activation_encoder dispatchThreads:MTLSizeMake(intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->activation_pipeline, intermediate_size), 1, 1)];
        [activation_encoder endEncoding];

        id<MTLComputeCommandEncoder> multiply_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (multiply_encoder == nil) return -12;
        [multiply_encoder setComputePipelineState:runtime->multiply_pipeline];
        [multiply_encoder setBuffer:activated_buffer offset:0 atIndex:0];
        [multiply_encoder setBuffer:up_output_buffer offset:0 atIndex:1];
        [multiply_encoder setBuffer:gated_buffer offset:0 atIndex:2];
        [multiply_encoder setBuffer:multiply_params_buffer offset:0 atIndex:3];
        [multiply_encoder dispatchThreads:MTLSizeMake(intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->multiply_pipeline, intermediate_size), 1, 1)];
        [multiply_encoder endEncoding];

        id<MTLBuffer> down_input_buffer = gated_buffer;
        if (post_gate_rms_norm_slot != SIZE_MAX) {
            id<MTLComputeCommandEncoder> rms_encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_RMS_NORM);
            if (rms_encoder == nil) return -13;
            [rms_encoder setComputePipelineState:runtime->rms_norm_pipeline];
            [rms_encoder setBuffer:gated_buffer offset:0 atIndex:0];
            [rms_encoder setBuffer:runtime->rms_norm_weight_buffers[post_gate_rms_norm_slot] offset:0 atIndex:1];
            [rms_encoder setBuffer:normed_gated_buffer offset:0 atIndex:2];
            [rms_encoder setBuffer:rms_params_buffer offset:0 atIndex:3];
            [rms_encoder dispatchThreads:MTLSizeMake(intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->rms_norm_pipeline, intermediate_size), 1, 1)];
            [rms_encoder endEncoding];
            down_input_buffer = normed_gated_buffer;
        }

        id<MTLComputeCommandEncoder> down_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (down_encoder == nil) return -14;
        [down_encoder setComputePipelineState:runtime->tl2_pipeline];
        [down_encoder setBuffer:down_input_buffer offset:0 atIndex:0];
        [down_encoder setBuffer:down_weight_buffer offset:0 atIndex:1];
        [down_encoder setBuffer:projected_buffer offset:0 atIndex:2];
        [down_encoder setBuffer:down_params_buffer offset:0 atIndex:3];
        [down_encoder dispatchThreads:MTLSizeMake(hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->tl2_pipeline, hidden_size), 1, 1)];
        [down_encoder endEncoding];

        id<MTLComputeCommandEncoder> add_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (add_encoder == nil) return -15;
        [add_encoder setComputePipelineState:runtime->add_pipeline];
        [add_encoder setBuffer:projected_buffer offset:0 atIndex:0];
        [add_encoder setBuffer:residual_buffer offset:0 atIndex:1];
        [add_encoder setBuffer:output_buffer offset:0 atIndex:2];
        [add_encoder setBuffer:add_params_buffer offset:0 atIndex:3];
        [add_encoder dispatchThreads:MTLSizeMake(hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->add_pipeline, hidden_size), 1, 1)];
        [add_encoder endEncoding];

        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -16;
    }
}

int termite_metal_decode_runtime_apply_linear_activation_linear_residual(
    termite_metal_decode_runtime *runtime,
    size_t first_linear_slot,
    size_t second_linear_slot,
    const float *input,
    const float *residual,
    size_t hidden_size,
    size_t intermediate_size,
    uint32_t activation_kind,
    float *output
) {
    if (runtime == NULL || input == NULL || residual == NULL || output == NULL) return -1;
    if (runtime->linear_pipeline == nil || runtime->activation_pipeline == nil || runtime->add_pipeline == nil) return -2;
    if (first_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || second_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -3;
    if (hidden_size == 0 || intermediate_size == 0) return -4;
    if (runtime->linear_slot_prepared[first_linear_slot] == 0 || runtime->linear_slot_prepared[second_linear_slot] == 0) return -5;
    if (runtime->linear_in_dims[first_linear_slot] != hidden_size || runtime->linear_out_dims[first_linear_slot] != intermediate_size) return -6;
    if (runtime->linear_in_dims[second_linear_slot] != intermediate_size || runtime->linear_out_dims[second_linear_slot] != hidden_size) return -7;
    if (hidden_size > UINT32_MAX || intermediate_size > UINT32_MAX) return -8;
    @autoreleasepool {
        const size_t hidden_bytes = hidden_size * sizeof(float);
        const size_t intermediate_bytes = intermediate_size * sizeof(float);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)input length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:input length:hidden_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> residual_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)residual length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (residual_buffer == nil) residual_buffer = [runtime->device newBufferWithBytes:residual length:hidden_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> first_output_buffer = [runtime->device newBufferWithLength:intermediate_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> activated_buffer = [runtime->device newBufferWithLength:intermediate_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> second_output_buffer = [runtime->device newBufferWithLength:hidden_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithBytesNoCopy:output length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        bool output_needs_copyback = false;
        if (output_buffer == nil) {
            output_buffer = [runtime->device newBufferWithLength:hidden_bytes options:MTLResourceStorageModeShared];
            output_needs_copyback = output_buffer != nil;
        }
        if (input_buffer == nil || residual_buffer == nil || first_output_buffer == nil || activated_buffer == nil || second_output_buffer == nil || output_buffer == nil) return -9;

        termite_metal_apply_linear_params first_linear_params = {
            .in_dim = (uint32_t)hidden_size,
            .out_dim = (uint32_t)intermediate_size,
        };
        termite_metal_apply_activation_params activation_params = {
            .activation_kind = activation_kind,
            .rows = 1,
            .dim = (uint32_t)intermediate_size,
        };
        termite_metal_apply_linear_params second_linear_params = {
            .in_dim = (uint32_t)intermediate_size,
            .out_dim = (uint32_t)hidden_size,
        };
        termite_metal_apply_add_params add_params = {
            .rows = 1,
            .dim = (uint32_t)hidden_size,
        };
        id<MTLBuffer> first_linear_params_buffer = [runtime->device newBufferWithBytes:&first_linear_params length:sizeof(first_linear_params) options:MTLResourceStorageModeShared];
        id<MTLBuffer> activation_params_buffer = [runtime->device newBufferWithBytes:&activation_params length:sizeof(activation_params) options:MTLResourceStorageModeShared];
        id<MTLBuffer> second_linear_params_buffer = [runtime->device newBufferWithBytes:&second_linear_params length:sizeof(second_linear_params) options:MTLResourceStorageModeShared];
        id<MTLBuffer> add_params_buffer = [runtime->device newBufferWithBytes:&add_params length:sizeof(add_params) options:MTLResourceStorageModeShared];
        if (first_linear_params_buffer == nil || activation_params_buffer == nil || second_linear_params_buffer == nil || add_params_buffer == nil) return -10;

        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -11;

        id<MTLComputeCommandEncoder> first_linear_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (first_linear_encoder == nil) return -12;
        [first_linear_encoder setComputePipelineState:runtime->linear_pipeline];
        [first_linear_encoder setBuffer:input_buffer offset:0 atIndex:0];
        [first_linear_encoder setBuffer:runtime->linear_weight_buffers[first_linear_slot] offset:0 atIndex:1];
        [first_linear_encoder setBuffer:runtime->linear_bias_buffers[first_linear_slot] offset:0 atIndex:2];
        [first_linear_encoder setBuffer:first_output_buffer offset:0 atIndex:3];
        [first_linear_encoder setBuffer:first_linear_params_buffer offset:0 atIndex:4];
        NSUInteger first_linear_thread_width = runtime->linear_pipeline.maxTotalThreadsPerThreadgroup;
        if (first_linear_thread_width == 0) first_linear_thread_width = 64;
        if (first_linear_thread_width > intermediate_size) first_linear_thread_width = intermediate_size;
        [first_linear_encoder dispatchThreads:MTLSizeMake(intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(first_linear_thread_width, 1, 1)];
        [first_linear_encoder endEncoding];

        id<MTLComputeCommandEncoder> activation_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (activation_encoder == nil) return -13;
        [activation_encoder setComputePipelineState:runtime->activation_pipeline];
        [activation_encoder setBuffer:first_output_buffer offset:0 atIndex:0];
        [activation_encoder setBuffer:activated_buffer offset:0 atIndex:1];
        [activation_encoder setBuffer:activation_params_buffer offset:0 atIndex:2];
        NSUInteger activation_thread_width = runtime->activation_pipeline.maxTotalThreadsPerThreadgroup;
        if (activation_thread_width == 0) activation_thread_width = 64;
        if (activation_thread_width > intermediate_size) activation_thread_width = intermediate_size;
        [activation_encoder dispatchThreads:MTLSizeMake(intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(activation_thread_width, 1, 1)];
        [activation_encoder endEncoding];

        id<MTLComputeCommandEncoder> second_linear_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (second_linear_encoder == nil) return -14;
        [second_linear_encoder setComputePipelineState:runtime->linear_pipeline];
        [second_linear_encoder setBuffer:activated_buffer offset:0 atIndex:0];
        [second_linear_encoder setBuffer:runtime->linear_weight_buffers[second_linear_slot] offset:0 atIndex:1];
        [second_linear_encoder setBuffer:runtime->linear_bias_buffers[second_linear_slot] offset:0 atIndex:2];
        [second_linear_encoder setBuffer:second_output_buffer offset:0 atIndex:3];
        [second_linear_encoder setBuffer:second_linear_params_buffer offset:0 atIndex:4];
        NSUInteger second_linear_thread_width = runtime->linear_pipeline.maxTotalThreadsPerThreadgroup;
        if (second_linear_thread_width == 0) second_linear_thread_width = 64;
        if (second_linear_thread_width > hidden_size) second_linear_thread_width = hidden_size;
        [second_linear_encoder dispatchThreads:MTLSizeMake(hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(second_linear_thread_width, 1, 1)];
        [second_linear_encoder endEncoding];

        id<MTLComputeCommandEncoder> add_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (add_encoder == nil) return -15;
        [add_encoder setComputePipelineState:runtime->add_pipeline];
        [add_encoder setBuffer:second_output_buffer offset:0 atIndex:0];
        [add_encoder setBuffer:residual_buffer offset:0 atIndex:1];
        [add_encoder setBuffer:output_buffer offset:0 atIndex:2];
        [add_encoder setBuffer:add_params_buffer offset:0 atIndex:3];
        NSUInteger add_thread_width = runtime->add_pipeline.maxTotalThreadsPerThreadgroup;
        if (add_thread_width == 0) add_thread_width = 64;
        if (add_thread_width > hidden_size) add_thread_width = hidden_size;
        [add_encoder dispatchThreads:MTLSizeMake(hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(add_thread_width, 1, 1)];
        [add_encoder endEncoding];

        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        if (command_buffer.status != MTLCommandBufferStatusCompleted) return -16;
        if (output_needs_copyback) {
            memcpy(output, [output_buffer contents], hidden_bytes);
        }
        return 0;
    }
}

int termite_metal_decode_runtime_apply_linear_pair_activation_multiply_linear_residual(
    termite_metal_decode_runtime *runtime,
    size_t gate_linear_slot,
    size_t up_linear_slot,
    size_t down_linear_slot,
    size_t post_down_rms_norm_slot,
    const float *input,
    const float *residual,
    size_t hidden_size,
    size_t intermediate_size,
    uint32_t activation_kind,
    float *output
) {
    if (runtime == NULL || input == NULL || residual == NULL || output == NULL) return -1;
    if (runtime->linear_pipeline == nil || runtime->linear_reduce_pipeline == nil || runtime->linear_pair_reduce_pipeline == nil || runtime->activation_pipeline == nil || runtime->multiply_pipeline == nil || runtime->add_pipeline == nil) return -2;
    if (gate_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || up_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || down_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -3;
    if (hidden_size == 0 || intermediate_size == 0) return -4;
    if (runtime->linear_slot_prepared[gate_linear_slot] == 0 || runtime->linear_slot_prepared[up_linear_slot] == 0 || runtime->linear_slot_prepared[down_linear_slot] == 0) return -5;
    if (runtime->linear_in_dims[gate_linear_slot] != hidden_size || runtime->linear_out_dims[gate_linear_slot] != intermediate_size) return -6;
    if (runtime->linear_in_dims[up_linear_slot] != hidden_size || runtime->linear_out_dims[up_linear_slot] != intermediate_size) return -7;
    if (runtime->linear_in_dims[down_linear_slot] != intermediate_size || runtime->linear_out_dims[down_linear_slot] != hidden_size) return -8;
    if (hidden_size > UINT32_MAX || intermediate_size > UINT32_MAX) return -9;
    if (post_down_rms_norm_slot != SIZE_MAX) {
        if (runtime->rms_norm_pipeline == nil) return -10;
        if (post_down_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return -11;
        if (runtime->rms_norm_slot_prepared[post_down_rms_norm_slot] == 0) return -12;
        if (runtime->rms_norm_hidden_sizes[post_down_rms_norm_slot] != hidden_size) return -13;
    }
    @autoreleasepool {
        const size_t hidden_bytes = hidden_size * sizeof(float);
        const size_t intermediate_bytes = intermediate_size * sizeof(float);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)input length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:input length:hidden_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> residual_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)residual length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (residual_buffer == nil) residual_buffer = [runtime->device newBufferWithBytes:residual length:hidden_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> gate_output_buffer = [runtime->device newBufferWithLength:intermediate_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> up_output_buffer = [runtime->device newBufferWithLength:intermediate_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> activated_buffer = [runtime->device newBufferWithLength:intermediate_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> gated_buffer = [runtime->device newBufferWithLength:intermediate_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> projected_buffer = [runtime->device newBufferWithLength:hidden_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> normed_projected_buffer = post_down_rms_norm_slot != SIZE_MAX ? [runtime->device newBufferWithLength:hidden_bytes options:MTLResourceStorageModePrivate] : nil;
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithBytesNoCopy:output length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) output_buffer = [runtime->device newBufferWithBytes:output length:hidden_bytes options:MTLResourceStorageModeShared];
        if (input_buffer == nil || residual_buffer == nil || gate_output_buffer == nil || up_output_buffer == nil || activated_buffer == nil || gated_buffer == nil || projected_buffer == nil || output_buffer == nil || (post_down_rms_norm_slot != SIZE_MAX && normed_projected_buffer == nil)) return -14;

        termite_metal_apply_linear_params up_linear_params = {
            .in_dim = (uint32_t)hidden_size,
            .out_dim = (uint32_t)intermediate_size,
        };
        termite_metal_apply_activation_params activation_params = {
            .activation_kind = activation_kind,
            .rows = 1,
            .dim = (uint32_t)intermediate_size,
        };
        termite_metal_apply_add_params multiply_params = {
            .rows = 1,
            .dim = (uint32_t)intermediate_size,
        };
        termite_metal_apply_linear_params down_linear_params = {
            .in_dim = (uint32_t)intermediate_size,
            .out_dim = (uint32_t)hidden_size,
        };
        termite_metal_apply_layer_norm_params post_down_rms_params = {
            .hidden_size = (uint32_t)hidden_size,
            .eps = 0.0f,
        };
        termite_metal_apply_add_params add_params = {
            .rows = 1,
            .dim = (uint32_t)hidden_size,
        };
        id<MTLBuffer> pair_linear_params_buffer = [runtime->device newBufferWithBytes:&up_linear_params length:sizeof(up_linear_params) options:MTLResourceStorageModeShared];
        id<MTLBuffer> activation_params_buffer = [runtime->device newBufferWithBytes:&activation_params length:sizeof(activation_params) options:MTLResourceStorageModeShared];
        id<MTLBuffer> multiply_params_buffer = [runtime->device newBufferWithBytes:&multiply_params length:sizeof(multiply_params) options:MTLResourceStorageModeShared];
        id<MTLBuffer> down_linear_params_buffer = [runtime->device newBufferWithBytes:&down_linear_params length:sizeof(down_linear_params) options:MTLResourceStorageModeShared];
        id<MTLBuffer> post_down_rms_params_buffer = post_down_rms_norm_slot != SIZE_MAX ? [runtime->device newBufferWithBytes:&post_down_rms_params length:sizeof(post_down_rms_params) options:MTLResourceStorageModeShared] : nil;
        id<MTLBuffer> add_params_buffer = [runtime->device newBufferWithBytes:&add_params length:sizeof(add_params) options:MTLResourceStorageModeShared];
        if (pair_linear_params_buffer == nil || activation_params_buffer == nil || multiply_params_buffer == nil || down_linear_params_buffer == nil || add_params_buffer == nil || (post_down_rms_norm_slot != SIZE_MAX && post_down_rms_params_buffer == nil)) return -15;

        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -16;

        id<MTLComputeCommandEncoder> gate_linear_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (gate_linear_encoder == nil) return -17;
        [gate_linear_encoder setComputePipelineState:runtime->linear_pipeline];
        [gate_linear_encoder setBuffer:input_buffer offset:0 atIndex:0];
        [gate_linear_encoder setBuffer:runtime->linear_weight_buffers[gate_linear_slot] offset:0 atIndex:1];
        [gate_linear_encoder setBuffer:runtime->linear_bias_buffers[gate_linear_slot] offset:0 atIndex:2];
        [gate_linear_encoder setBuffer:gate_output_buffer offset:0 atIndex:3];
        [gate_linear_encoder setBuffer:pair_linear_params_buffer offset:0 atIndex:4];
        NSUInteger pair_linear_thread_width = runtime->linear_pipeline.maxTotalThreadsPerThreadgroup;
        if (pair_linear_thread_width == 0) pair_linear_thread_width = 64;
        if (pair_linear_thread_width > intermediate_size) pair_linear_thread_width = intermediate_size;
        [gate_linear_encoder dispatchThreads:MTLSizeMake(intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(pair_linear_thread_width, 1, 1)];
        [gate_linear_encoder endEncoding];

        id<MTLComputeCommandEncoder> up_linear_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (up_linear_encoder == nil) return -18;
        [up_linear_encoder setComputePipelineState:runtime->linear_pipeline];
        [up_linear_encoder setBuffer:input_buffer offset:0 atIndex:0];
        [up_linear_encoder setBuffer:runtime->linear_weight_buffers[up_linear_slot] offset:0 atIndex:1];
        [up_linear_encoder setBuffer:runtime->linear_bias_buffers[up_linear_slot] offset:0 atIndex:2];
        [up_linear_encoder setBuffer:up_output_buffer offset:0 atIndex:3];
        [up_linear_encoder setBuffer:pair_linear_params_buffer offset:0 atIndex:4];
        [up_linear_encoder dispatchThreads:MTLSizeMake(intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(pair_linear_thread_width, 1, 1)];
        [up_linear_encoder endEncoding];

        id<MTLComputeCommandEncoder> activation_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (activation_encoder == nil) return -19;
        [activation_encoder setComputePipelineState:runtime->activation_pipeline];
        [activation_encoder setBuffer:gate_output_buffer offset:0 atIndex:0];
        [activation_encoder setBuffer:activated_buffer offset:0 atIndex:1];
        [activation_encoder setBuffer:activation_params_buffer offset:0 atIndex:2];
        NSUInteger activation_thread_width = runtime->activation_pipeline.maxTotalThreadsPerThreadgroup;
        if (activation_thread_width == 0) activation_thread_width = 64;
        if (activation_thread_width > intermediate_size) activation_thread_width = intermediate_size;
        [activation_encoder dispatchThreads:MTLSizeMake(intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(activation_thread_width, 1, 1)];
        [activation_encoder endEncoding];

        id<MTLComputeCommandEncoder> multiply_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (multiply_encoder == nil) return -20;
        [multiply_encoder setComputePipelineState:runtime->multiply_pipeline];
        [multiply_encoder setBuffer:activated_buffer offset:0 atIndex:0];
        [multiply_encoder setBuffer:up_output_buffer offset:0 atIndex:1];
        [multiply_encoder setBuffer:gated_buffer offset:0 atIndex:2];
        [multiply_encoder setBuffer:multiply_params_buffer offset:0 atIndex:3];
        NSUInteger multiply_thread_width = runtime->multiply_pipeline.maxTotalThreadsPerThreadgroup;
        if (multiply_thread_width == 0) multiply_thread_width = 64;
        if (multiply_thread_width > intermediate_size) multiply_thread_width = intermediate_size;
        [multiply_encoder dispatchThreads:MTLSizeMake(intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(multiply_thread_width, 1, 1)];
        [multiply_encoder endEncoding];

        id<MTLComputeCommandEncoder> down_linear_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (down_linear_encoder == nil) return -21;
        [down_linear_encoder setComputePipelineState:runtime->linear_pipeline];
        [down_linear_encoder setBuffer:gated_buffer offset:0 atIndex:0];
        [down_linear_encoder setBuffer:runtime->linear_weight_buffers[down_linear_slot] offset:0 atIndex:1];
        [down_linear_encoder setBuffer:runtime->linear_bias_buffers[down_linear_slot] offset:0 atIndex:2];
        [down_linear_encoder setBuffer:projected_buffer offset:0 atIndex:3];
        [down_linear_encoder setBuffer:down_linear_params_buffer offset:0 atIndex:4];
        NSUInteger down_linear_thread_width = runtime->linear_pipeline.maxTotalThreadsPerThreadgroup;
        if (down_linear_thread_width == 0) down_linear_thread_width = 64;
        if (down_linear_thread_width > hidden_size) down_linear_thread_width = hidden_size;
        [down_linear_encoder dispatchThreads:MTLSizeMake(hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(down_linear_thread_width, 1, 1)];
        [down_linear_encoder endEncoding];

        id<MTLBuffer> add_lhs_buffer = projected_buffer;
        if (post_down_rms_norm_slot != SIZE_MAX) {
            id<MTLComputeCommandEncoder> rms_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
            if (rms_encoder == nil) return -22;
            [rms_encoder setComputePipelineState:runtime->rms_norm_pipeline];
            [rms_encoder setBuffer:projected_buffer offset:0 atIndex:0];
            [rms_encoder setBuffer:runtime->rms_norm_weight_buffers[post_down_rms_norm_slot] offset:0 atIndex:1];
            [rms_encoder setBuffer:normed_projected_buffer offset:0 atIndex:2];
            [rms_encoder setBuffer:post_down_rms_params_buffer offset:0 atIndex:3];
            NSUInteger rms_thread_width = runtime->rms_norm_pipeline.maxTotalThreadsPerThreadgroup;
            if (rms_thread_width == 0) rms_thread_width = 64;
            if (rms_thread_width > hidden_size) rms_thread_width = hidden_size;
            [rms_encoder dispatchThreads:MTLSizeMake(hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(rms_thread_width, 1, 1)];
            [rms_encoder endEncoding];
            add_lhs_buffer = normed_projected_buffer;
        }

        id<MTLComputeCommandEncoder> add_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (add_encoder == nil) return -23;
        [add_encoder setComputePipelineState:runtime->add_pipeline];
        [add_encoder setBuffer:add_lhs_buffer offset:0 atIndex:0];
        [add_encoder setBuffer:residual_buffer offset:0 atIndex:1];
        [add_encoder setBuffer:output_buffer offset:0 atIndex:2];
        [add_encoder setBuffer:add_params_buffer offset:0 atIndex:3];
        NSUInteger add_thread_width = runtime->add_pipeline.maxTotalThreadsPerThreadgroup;
        if (add_thread_width == 0) add_thread_width = 64;
        if (add_thread_width > hidden_size) add_thread_width = hidden_size;
        [add_encoder dispatchThreads:MTLSizeMake(hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(add_thread_width, 1, 1)];
        [add_encoder endEncoding];

        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -24;
    }
}

int termite_metal_decode_runtime_apply_gated_ffn_residual_q4_k_pair_q5_k_down_slots(
    termite_metal_decode_runtime *runtime,
    const float *input,
    const float *residual,
    size_t rows,
    size_t hidden_size,
    size_t intermediate_size,
    uint32_t activation_kind,
    size_t gate_linear_slot,
    size_t up_linear_slot,
    size_t post_gate_rms_norm_slot,
    size_t post_down_rms_norm_slot,
    size_t down_linear_slot,
    float *output
) {
    if (runtime == NULL || input == NULL || residual == NULL || output == NULL) return -1;
    if (runtime->q4_k_pair_pipeline == nil || runtime->q5_k_pipeline == nil || runtime->activation_pipeline == nil || runtime->multiply_pipeline == nil || runtime->add_pipeline == nil) return -2;
    if (rows == 0 || hidden_size == 0 || intermediate_size == 0 || rows > UINT32_MAX || hidden_size > UINT32_MAX || intermediate_size > UINT32_MAX) return -3;
    if (hidden_size % 256u != 0 || intermediate_size % 256u != 0) return -4;
    if (gate_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || up_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || down_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -5;
    termite_metal_quant_linear_slot_view gate_view;
    termite_metal_quant_linear_slot_view up_view;
    termite_metal_quant_linear_slot_view down_view;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q4_K, gate_linear_slot, hidden_size, intermediate_size, &gate_view) != 0) return -6;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q4_K, up_linear_slot, hidden_size, intermediate_size, &up_view) != 0) return -7;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q5_K, down_linear_slot, intermediate_size, hidden_size, &down_view) != 0) return -8;
    if (post_gate_rms_norm_slot != SIZE_MAX) {
        if (post_gate_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return -9;
        if (runtime->rms_norm_slot_prepared[post_gate_rms_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[post_gate_rms_norm_slot] != intermediate_size) return -10;
    }
    if (post_down_rms_norm_slot != SIZE_MAX) {
        if (post_down_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return -11;
        if (runtime->rms_norm_slot_prepared[post_down_rms_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[post_down_rms_norm_slot] != hidden_size) return -12;
    }
    @autoreleasepool {
        const size_t hidden_bytes = rows * hidden_size * sizeof(float);
        const size_t intermediate_bytes = rows * intermediate_size * sizeof(float);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)input length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:input length:hidden_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> residual_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)residual length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (residual_buffer == nil) residual_buffer = [runtime->device newBufferWithBytes:residual length:hidden_bytes options:MTLResourceStorageModeShared];
        if (termite_metal_decode_runtime_ensure_hot_buffers(runtime, hidden_bytes, intermediate_bytes) != 0) return -13;
        id<MTLBuffer> gate_output_buffer = runtime->hot_intermediate_buffers[0];
        id<MTLBuffer> up_output_buffer = runtime->hot_intermediate_buffers[1];
        id<MTLBuffer> activated_buffer = runtime->hot_intermediate_buffers[2];
        id<MTLBuffer> gated_buffer = runtime->hot_intermediate_buffers[3];
        id<MTLBuffer> normed_gated_buffer = post_gate_rms_norm_slot != SIZE_MAX ? runtime->hot_intermediate_buffers[4] : nil;
        id<MTLBuffer> projected_buffer = runtime->hot_hidden_buffers[0];
        id<MTLBuffer> post_down_buffer = post_down_rms_norm_slot != SIZE_MAX ? runtime->hot_hidden_buffers[1] : nil;
        BOOL output_borrowed = YES;
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithBytesNoCopy:output length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) {
            output_borrowed = NO;
            output_buffer = [runtime->device newBufferWithBytes:output length:hidden_bytes options:MTLResourceStorageModeShared];
        }
        if (input_buffer == nil || residual_buffer == nil || gate_output_buffer == nil || up_output_buffer == nil || activated_buffer == nil || gated_buffer == nil || projected_buffer == nil || output_buffer == nil || (post_gate_rms_norm_slot != SIZE_MAX && normed_gated_buffer == nil) || (post_down_rms_norm_slot != SIZE_MAX && post_down_buffer == nil)) return -13;

        termite_metal_linear_params down_params = { .rows = (uint32_t)rows, .in_dim = (uint32_t)intermediate_size, .out_dim = (uint32_t)hidden_size, .row_blocks = (uint32_t)(intermediate_size / 256u) };
        termite_metal_apply_activation_params activation_params = { .activation_kind = activation_kind, .rows = (uint32_t)rows, .dim = (uint32_t)intermediate_size };
        termite_metal_apply_add_params multiply_params = { .rows = (uint32_t)rows, .dim = (uint32_t)intermediate_size };
        termite_metal_apply_row_norm_params rms_params = { .rows = (uint32_t)rows, .hidden_size = (uint32_t)intermediate_size, .eps = 0.0f };
        termite_metal_apply_row_norm_params post_down_rms_params = { .rows = (uint32_t)rows, .hidden_size = (uint32_t)hidden_size, .eps = 0.0f };
        termite_metal_apply_add_params add_params = { .rows = (uint32_t)rows, .dim = (uint32_t)hidden_size };
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -15;

        id<MTLComputeCommandEncoder> pair_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (pair_encoder == nil) return -16;
        termite_metal_quant_matmul_descriptor pair_descriptor = {
            .slot = gate_linear_slot,
            .format = TERMITE_METAL_QUANT_FORMAT_Q4_K,
            .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
            .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_PAIR,
            .fallback_pipeline = runtime->q4_k_pair_pipeline,
            .input_buffer = input_buffer,
            .weight_buffer = gate_view.weight_buffer,
            .second_weight_buffer = up_view.weight_buffer,
            .output_buffer = gate_output_buffer,
            .second_output_buffer = up_output_buffer,
            .rows = rows,
            .in_dim = hidden_size,
            .out_dim = intermediate_size,
            .values_per_block = gate_view.values_per_block,
            .bytes_per_block = gate_view.bytes_per_block,
            .failure_code = -16,
        };
        if (termite_metal_encode_quant_matmul_descriptor_on_encoder(runtime, pair_encoder, &pair_descriptor) != 0) return -16;
        [pair_encoder endEncoding];

        id<MTLComputeCommandEncoder> activation_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (activation_encoder == nil) return -17;
        [activation_encoder setComputePipelineState:runtime->activation_pipeline];
        [activation_encoder setBuffer:gate_output_buffer offset:0 atIndex:0];
        [activation_encoder setBuffer:activated_buffer offset:0 atIndex:1];
        [activation_encoder setBytes:&activation_params length:sizeof(activation_params) atIndex:2];
        [activation_encoder dispatchThreads:MTLSizeMake(rows * intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->activation_pipeline, rows * intermediate_size), 1, 1)];
        [activation_encoder endEncoding];

        id<MTLComputeCommandEncoder> multiply_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (multiply_encoder == nil) return -18;
        [multiply_encoder setComputePipelineState:runtime->multiply_pipeline];
        [multiply_encoder setBuffer:activated_buffer offset:0 atIndex:0];
        [multiply_encoder setBuffer:up_output_buffer offset:0 atIndex:1];
        [multiply_encoder setBuffer:gated_buffer offset:0 atIndex:2];
        [multiply_encoder setBytes:&multiply_params length:sizeof(multiply_params) atIndex:3];
        [multiply_encoder dispatchThreads:MTLSizeMake(rows * intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->multiply_pipeline, rows * intermediate_size), 1, 1)];
        [multiply_encoder endEncoding];

        id<MTLBuffer> down_input_buffer = gated_buffer;
        if (post_gate_rms_norm_slot != SIZE_MAX) {
            id<MTLComputeCommandEncoder> rms_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
            if (rms_encoder == nil) return -19;
            [rms_encoder setComputePipelineState:runtime->rms_norm_rows_pipeline];
            [rms_encoder setBuffer:gated_buffer offset:0 atIndex:0];
            [rms_encoder setBuffer:runtime->rms_norm_weight_buffers[post_gate_rms_norm_slot] offset:0 atIndex:1];
            [rms_encoder setBuffer:normed_gated_buffer offset:0 atIndex:2];
            [rms_encoder setBytes:&rms_params length:sizeof(rms_params) atIndex:3];
            [rms_encoder dispatchThreads:MTLSizeMake(rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->rms_norm_rows_pipeline, rows), 1, 1)];
            [rms_encoder endEncoding];
            down_input_buffer = normed_gated_buffer;
        }

        id<MTLComputeCommandEncoder> down_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (down_encoder == nil) return -20;
        [down_encoder setComputePipelineState:runtime->q5_k_pipeline];
        [down_encoder setBuffer:down_input_buffer offset:0 atIndex:0];
        [down_encoder setBuffer:down_view.weight_buffer offset:0 atIndex:1];
        [down_encoder setBuffer:projected_buffer offset:0 atIndex:2];
        [down_encoder setBytes:&down_params length:sizeof(down_params) atIndex:3];
        [down_encoder dispatchThreads:MTLSizeMake(rows * hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->q5_k_pipeline, rows * hidden_size), 1, 1)];
        [down_encoder endEncoding];

        id<MTLBuffer> add_lhs_buffer = projected_buffer;
        if (post_down_rms_norm_slot != SIZE_MAX) {
            id<MTLComputeCommandEncoder> post_down_encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_RMS_NORM);
            if (post_down_encoder == nil) return -21;
            [post_down_encoder setComputePipelineState:runtime->rms_norm_rows_pipeline];
            [post_down_encoder setBuffer:projected_buffer offset:0 atIndex:0];
            [post_down_encoder setBuffer:runtime->rms_norm_weight_buffers[post_down_rms_norm_slot] offset:0 atIndex:1];
            [post_down_encoder setBuffer:post_down_buffer offset:0 atIndex:2];
            [post_down_encoder setBytes:&post_down_rms_params length:sizeof(post_down_rms_params) atIndex:3];
            [post_down_encoder dispatchThreads:MTLSizeMake(rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->rms_norm_rows_pipeline, rows), 1, 1)];
            [post_down_encoder endEncoding];
            add_lhs_buffer = post_down_buffer;
        }

        id<MTLComputeCommandEncoder> add_encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_FFN);
        if (add_encoder == nil) return -22;
        [add_encoder setComputePipelineState:runtime->add_pipeline];
        [add_encoder setBuffer:add_lhs_buffer offset:0 atIndex:0];
        [add_encoder setBuffer:residual_buffer offset:0 atIndex:1];
        [add_encoder setBuffer:output_buffer offset:0 atIndex:2];
        [add_encoder setBytes:&add_params length:sizeof(add_params) atIndex:3];
        [add_encoder dispatchThreads:MTLSizeMake(rows * hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->add_pipeline, rows * hidden_size), 1, 1)];
        [add_encoder endEncoding];

        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        if (command_buffer.status != MTLCommandBufferStatusCompleted) return -23;
        if (!output_borrowed) {
            memcpy(output, [output_buffer contents], hidden_bytes);
        }
        return 0;
    }
}

int termite_metal_decode_runtime_apply_gated_ffn_residual_q4_k_pair_q6_k_down_slots(
    termite_metal_decode_runtime *runtime,
    const float *input,
    const float *residual,
    size_t rows,
    size_t hidden_size,
    size_t intermediate_size,
    uint32_t activation_kind,
    size_t gate_linear_slot,
    size_t up_linear_slot,
    size_t post_gate_rms_norm_slot,
    size_t post_down_rms_norm_slot,
    size_t down_linear_slot,
    float *output
) {
    if (runtime == NULL || input == NULL || residual == NULL || output == NULL) return -1;
    if (runtime->q4_k_pair_pipeline == nil || runtime->q6_k_pipeline == nil || runtime->activation_pipeline == nil || runtime->multiply_pipeline == nil || runtime->add_pipeline == nil) return -2;
    if (rows == 0 || hidden_size == 0 || intermediate_size == 0 || rows > UINT32_MAX || hidden_size > UINT32_MAX || intermediate_size > UINT32_MAX) return -3;
    const float eps = 0.0f;
    if (hidden_size % 256u != 0 || intermediate_size % 256u != 0) return -4;
    if (gate_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || up_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || down_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -5;
    termite_metal_quant_linear_slot_view gate_view;
    termite_metal_quant_linear_slot_view up_view;
    termite_metal_quant_linear_slot_view down_view;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q4_K, gate_linear_slot, hidden_size, intermediate_size, &gate_view) != 0) return -6;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q4_K, up_linear_slot, hidden_size, intermediate_size, &up_view) != 0) return -7;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q6_K, down_linear_slot, intermediate_size, hidden_size, &down_view) != 0) return -8;
    if (post_gate_rms_norm_slot != SIZE_MAX) {
        if (post_gate_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return -9;
        if (runtime->rms_norm_slot_prepared[post_gate_rms_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[post_gate_rms_norm_slot] != intermediate_size) return -10;
    }
    if (post_down_rms_norm_slot != SIZE_MAX) {
        if (post_down_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return -11;
        if (runtime->rms_norm_slot_prepared[post_down_rms_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[post_down_rms_norm_slot] != hidden_size) return -12;
    }
    @autoreleasepool {
        const size_t hidden_bytes = rows * hidden_size * sizeof(float);
        const size_t intermediate_bytes = rows * intermediate_size * sizeof(float);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)input length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:input length:hidden_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> residual_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)residual length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (residual_buffer == nil) residual_buffer = [runtime->device newBufferWithBytes:residual length:hidden_bytes options:MTLResourceStorageModeShared];
        if (termite_metal_decode_runtime_ensure_hot_buffers(runtime, hidden_bytes, intermediate_bytes) != 0) return -13;
        id<MTLBuffer> gate_output_buffer = runtime->hot_intermediate_buffers[0];
        id<MTLBuffer> up_output_buffer = runtime->hot_intermediate_buffers[1];
        id<MTLBuffer> activated_buffer = runtime->hot_intermediate_buffers[2];
        id<MTLBuffer> gated_buffer = runtime->hot_intermediate_buffers[3];
        id<MTLBuffer> normed_gated_buffer = post_gate_rms_norm_slot != SIZE_MAX ? runtime->hot_intermediate_buffers[4] : nil;
        id<MTLBuffer> projected_buffer = runtime->hot_hidden_buffers[0];
        id<MTLBuffer> post_down_buffer = post_down_rms_norm_slot != SIZE_MAX ? runtime->hot_hidden_buffers[1] : nil;
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithBytesNoCopy:output length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) output_buffer = [runtime->device newBufferWithBytes:output length:hidden_bytes options:MTLResourceStorageModeShared];
        if (input_buffer == nil || residual_buffer == nil || gate_output_buffer == nil || up_output_buffer == nil || activated_buffer == nil || gated_buffer == nil || projected_buffer == nil || output_buffer == nil || (post_gate_rms_norm_slot != SIZE_MAX && normed_gated_buffer == nil) || (post_down_rms_norm_slot != SIZE_MAX && post_down_buffer == nil)) return -13;

        termite_metal_linear_params down_params = { .rows = (uint32_t)rows, .in_dim = (uint32_t)intermediate_size, .out_dim = (uint32_t)hidden_size, .row_blocks = (uint32_t)(intermediate_size / 256u) };
        termite_metal_apply_activation_params activation_params = { .activation_kind = activation_kind, .rows = (uint32_t)rows, .dim = (uint32_t)intermediate_size };
        termite_metal_apply_add_params multiply_params = { .rows = (uint32_t)rows, .dim = (uint32_t)intermediate_size };
        termite_metal_apply_row_norm_params rms_params = { .rows = (uint32_t)rows, .hidden_size = (uint32_t)intermediate_size, .eps = 0.0f };
        termite_metal_apply_row_norm_params post_down_rms_params = { .rows = (uint32_t)rows, .hidden_size = (uint32_t)hidden_size, .eps = 0.0f };
        termite_metal_apply_add_params add_params = { .rows = (uint32_t)rows, .dim = (uint32_t)hidden_size };
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -15;

        id<MTLComputeCommandEncoder> pair_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (pair_encoder == nil) return -16;
        termite_metal_quant_matmul_descriptor pair_descriptor = {
            .slot = gate_linear_slot,
            .format = TERMITE_METAL_QUANT_FORMAT_Q4_K,
            .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
            .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_PAIR,
            .fallback_pipeline = runtime->q4_k_pair_pipeline,
            .input_buffer = input_buffer,
            .weight_buffer = gate_view.weight_buffer,
            .second_weight_buffer = up_view.weight_buffer,
            .output_buffer = gate_output_buffer,
            .second_output_buffer = up_output_buffer,
            .rows = rows,
            .in_dim = hidden_size,
            .out_dim = intermediate_size,
            .values_per_block = gate_view.values_per_block,
            .bytes_per_block = gate_view.bytes_per_block,
            .failure_code = -16,
        };
        if (termite_metal_encode_quant_matmul_descriptor_on_encoder(runtime, pair_encoder, &pair_descriptor) != 0) return -16;
        [pair_encoder endEncoding];

        id<MTLComputeCommandEncoder> activation_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (activation_encoder == nil) return -17;
        [activation_encoder setComputePipelineState:runtime->activation_pipeline];
        [activation_encoder setBuffer:gate_output_buffer offset:0 atIndex:0];
        [activation_encoder setBuffer:activated_buffer offset:0 atIndex:1];
        [activation_encoder setBytes:&activation_params length:sizeof(activation_params) atIndex:2];
        [activation_encoder dispatchThreads:MTLSizeMake(rows * intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->activation_pipeline, rows * intermediate_size), 1, 1)];
        [activation_encoder endEncoding];

        id<MTLComputeCommandEncoder> multiply_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (multiply_encoder == nil) return -18;
        [multiply_encoder setComputePipelineState:runtime->multiply_pipeline];
        [multiply_encoder setBuffer:activated_buffer offset:0 atIndex:0];
        [multiply_encoder setBuffer:up_output_buffer offset:0 atIndex:1];
        [multiply_encoder setBuffer:gated_buffer offset:0 atIndex:2];
        [multiply_encoder setBytes:&multiply_params length:sizeof(multiply_params) atIndex:3];
        [multiply_encoder dispatchThreads:MTLSizeMake(rows * intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->multiply_pipeline, rows * intermediate_size), 1, 1)];
        [multiply_encoder endEncoding];

        id<MTLBuffer> down_input_buffer = gated_buffer;
        if (post_gate_rms_norm_slot != SIZE_MAX) {
            id<MTLComputeCommandEncoder> rms_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
            if (rms_encoder == nil) return -19;
            [rms_encoder setComputePipelineState:runtime->rms_norm_rows_pipeline];
            [rms_encoder setBuffer:gated_buffer offset:0 atIndex:0];
            [rms_encoder setBuffer:runtime->rms_norm_weight_buffers[post_gate_rms_norm_slot] offset:0 atIndex:1];
            [rms_encoder setBuffer:normed_gated_buffer offset:0 atIndex:2];
            [rms_encoder setBytes:&rms_params length:sizeof(rms_params) atIndex:3];
            [rms_encoder dispatchThreads:MTLSizeMake(rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->rms_norm_rows_pipeline, rows), 1, 1)];
            [rms_encoder endEncoding];
            down_input_buffer = normed_gated_buffer;
        }

        id<MTLComputeCommandEncoder> down_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (down_encoder == nil) return -20;
        [down_encoder setComputePipelineState:runtime->q6_k_pipeline];
        [down_encoder setBuffer:down_input_buffer offset:0 atIndex:0];
        [down_encoder setBuffer:down_view.weight_buffer offset:0 atIndex:1];
        [down_encoder setBuffer:projected_buffer offset:0 atIndex:2];
        [down_encoder setBytes:&down_params length:sizeof(down_params) atIndex:3];
        [down_encoder dispatchThreads:MTLSizeMake(rows * hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->q6_k_pipeline, rows * hidden_size), 1, 1)];
        [down_encoder endEncoding];

        id<MTLBuffer> add_lhs_buffer = projected_buffer;
        if (post_down_rms_norm_slot != SIZE_MAX) {
            id<MTLComputeCommandEncoder> post_down_encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_RMS_NORM);
            if (post_down_encoder == nil) return -21;
            [post_down_encoder setComputePipelineState:runtime->rms_norm_rows_pipeline];
            [post_down_encoder setBuffer:projected_buffer offset:0 atIndex:0];
            [post_down_encoder setBuffer:runtime->rms_norm_weight_buffers[post_down_rms_norm_slot] offset:0 atIndex:1];
            [post_down_encoder setBuffer:post_down_buffer offset:0 atIndex:2];
            [post_down_encoder setBytes:&post_down_rms_params length:sizeof(post_down_rms_params) atIndex:3];
            [post_down_encoder dispatchThreads:MTLSizeMake(rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->rms_norm_rows_pipeline, rows), 1, 1)];
            [post_down_encoder endEncoding];
            add_lhs_buffer = post_down_buffer;
        }

        id<MTLComputeCommandEncoder> add_encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_FFN);
        if (add_encoder == nil) return -22;
        [add_encoder setComputePipelineState:runtime->add_pipeline];
        [add_encoder setBuffer:add_lhs_buffer offset:0 atIndex:0];
        [add_encoder setBuffer:residual_buffer offset:0 atIndex:1];
        [add_encoder setBuffer:output_buffer offset:0 atIndex:2];
        [add_encoder setBytes:&add_params length:sizeof(add_params) atIndex:3];
        [add_encoder dispatchThreads:MTLSizeMake(rows * hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->add_pipeline, rows * hidden_size), 1, 1)];
        [add_encoder endEncoding];

        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -23;
    }
}

int termite_metal_decode_runtime_apply_gated_ffn_residual_q6_k_slots(
    termite_metal_decode_runtime *runtime,
    const float *input,
    const float *residual,
    size_t rows,
    size_t hidden_size,
    size_t intermediate_size,
    uint32_t activation_kind,
    size_t gate_linear_slot,
    size_t up_linear_slot,
    size_t post_gate_rms_norm_slot,
    size_t post_down_rms_norm_slot,
    size_t down_linear_slot,
    float *output
) {
    if (runtime == NULL || input == NULL || residual == NULL || output == NULL) return -1;
    if (runtime->q6_k_pipeline == nil || runtime->q6_k_pair_pipeline == nil || runtime->activation_pipeline == nil || runtime->multiply_pipeline == nil || runtime->add_pipeline == nil) return -2;
    if (rows == 0 || hidden_size == 0 || intermediate_size == 0 || rows > UINT32_MAX || hidden_size > UINT32_MAX || intermediate_size > UINT32_MAX) return -3;
    if (hidden_size % 256u != 0 || intermediate_size % 256u != 0) return -4;
    if (gate_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || up_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || down_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -5;
    termite_metal_quant_linear_slot_view gate_view;
    termite_metal_quant_linear_slot_view up_view;
    termite_metal_quant_linear_slot_view down_view;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q6_K, gate_linear_slot, hidden_size, intermediate_size, &gate_view) != 0) return -6;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q6_K, up_linear_slot, hidden_size, intermediate_size, &up_view) != 0) return -7;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q6_K, down_linear_slot, intermediate_size, hidden_size, &down_view) != 0) return -8;
    if (post_gate_rms_norm_slot != SIZE_MAX) {
        if (post_gate_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return -9;
        if (runtime->rms_norm_slot_prepared[post_gate_rms_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[post_gate_rms_norm_slot] != intermediate_size) return -10;
    }
    if (post_down_rms_norm_slot != SIZE_MAX) {
        if (post_down_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return -11;
        if (runtime->rms_norm_slot_prepared[post_down_rms_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[post_down_rms_norm_slot] != hidden_size) return -12;
    }
    @autoreleasepool {
        const size_t hidden_bytes = rows * hidden_size * sizeof(float);
        const size_t intermediate_bytes = rows * intermediate_size * sizeof(float);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)input length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:input length:hidden_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> residual_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)residual length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (residual_buffer == nil) residual_buffer = [runtime->device newBufferWithBytes:residual length:hidden_bytes options:MTLResourceStorageModeShared];
        if (termite_metal_decode_runtime_ensure_hot_buffers(runtime, hidden_bytes, intermediate_bytes) != 0) return -13;
        id<MTLBuffer> gate_output_buffer = runtime->hot_intermediate_buffers[0];
        id<MTLBuffer> up_output_buffer = runtime->hot_intermediate_buffers[1];
        id<MTLBuffer> activated_buffer = runtime->hot_intermediate_buffers[2];
        id<MTLBuffer> gated_buffer = runtime->hot_intermediate_buffers[3];
        id<MTLBuffer> normed_gated_buffer = post_gate_rms_norm_slot != SIZE_MAX ? runtime->hot_intermediate_buffers[4] : nil;
        id<MTLBuffer> projected_buffer = runtime->hot_hidden_buffers[0];
        id<MTLBuffer> post_down_buffer = post_down_rms_norm_slot != SIZE_MAX ? runtime->hot_hidden_buffers[1] : nil;
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithBytesNoCopy:output length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) output_buffer = [runtime->device newBufferWithBytes:output length:hidden_bytes options:MTLResourceStorageModeShared];
        if (input_buffer == nil || residual_buffer == nil || gate_output_buffer == nil || up_output_buffer == nil || activated_buffer == nil || gated_buffer == nil || projected_buffer == nil || output_buffer == nil || (post_gate_rms_norm_slot != SIZE_MAX && normed_gated_buffer == nil) || (post_down_rms_norm_slot != SIZE_MAX && post_down_buffer == nil)) return -13;

        termite_metal_linear_params down_params = { .rows = (uint32_t)rows, .in_dim = (uint32_t)intermediate_size, .out_dim = (uint32_t)hidden_size, .row_blocks = (uint32_t)(intermediate_size / 256u) };
        termite_metal_apply_activation_params activation_params = { .activation_kind = activation_kind, .rows = (uint32_t)rows, .dim = (uint32_t)intermediate_size };
        termite_metal_apply_add_params multiply_params = { .rows = (uint32_t)rows, .dim = (uint32_t)intermediate_size };
        termite_metal_apply_row_norm_params rms_params = { .rows = (uint32_t)rows, .hidden_size = (uint32_t)intermediate_size, .eps = 0.0f };
        termite_metal_apply_row_norm_params post_down_rms_params = { .rows = (uint32_t)rows, .hidden_size = (uint32_t)hidden_size, .eps = 0.0f };
        termite_metal_apply_add_params add_params = { .rows = (uint32_t)rows, .dim = (uint32_t)hidden_size };
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -15;

        id<MTLComputeCommandEncoder> pair_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (pair_encoder == nil) return -16;
        termite_metal_quant_matmul_descriptor pair_descriptor = {
            .slot = gate_linear_slot,
            .format = TERMITE_METAL_QUANT_FORMAT_Q6_K,
            .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
            .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_PAIR,
            .fallback_pipeline = runtime->q6_k_pair_pipeline,
            .input_buffer = input_buffer,
            .weight_buffer = gate_view.weight_buffer,
            .second_weight_buffer = up_view.weight_buffer,
            .output_buffer = gate_output_buffer,
            .second_output_buffer = up_output_buffer,
            .rows = rows,
            .in_dim = hidden_size,
            .out_dim = intermediate_size,
            .values_per_block = gate_view.values_per_block,
            .bytes_per_block = gate_view.bytes_per_block,
            .failure_code = -16,
        };
        if (termite_metal_encode_quant_matmul_descriptor_on_encoder(runtime, pair_encoder, &pair_descriptor) != 0) return -16;
        [pair_encoder endEncoding];

        id<MTLComputeCommandEncoder> activation_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (activation_encoder == nil) return -17;
        [activation_encoder setComputePipelineState:runtime->activation_pipeline];
        [activation_encoder setBuffer:gate_output_buffer offset:0 atIndex:0];
        [activation_encoder setBuffer:activated_buffer offset:0 atIndex:1];
        [activation_encoder setBytes:&activation_params length:sizeof(activation_params) atIndex:2];
        [activation_encoder dispatchThreads:MTLSizeMake(rows * intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->activation_pipeline, rows * intermediate_size), 1, 1)];
        [activation_encoder endEncoding];

        id<MTLComputeCommandEncoder> multiply_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (multiply_encoder == nil) return -18;
        [multiply_encoder setComputePipelineState:runtime->multiply_pipeline];
        [multiply_encoder setBuffer:activated_buffer offset:0 atIndex:0];
        [multiply_encoder setBuffer:up_output_buffer offset:0 atIndex:1];
        [multiply_encoder setBuffer:gated_buffer offset:0 atIndex:2];
        [multiply_encoder setBytes:&multiply_params length:sizeof(multiply_params) atIndex:3];
        [multiply_encoder dispatchThreads:MTLSizeMake(rows * intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->multiply_pipeline, rows * intermediate_size), 1, 1)];
        [multiply_encoder endEncoding];

        id<MTLBuffer> down_input_buffer = gated_buffer;
        if (post_gate_rms_norm_slot != SIZE_MAX) {
            id<MTLComputeCommandEncoder> rms_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
            if (rms_encoder == nil) return -19;
            [rms_encoder setComputePipelineState:runtime->rms_norm_rows_pipeline];
            [rms_encoder setBuffer:gated_buffer offset:0 atIndex:0];
            [rms_encoder setBuffer:runtime->rms_norm_weight_buffers[post_gate_rms_norm_slot] offset:0 atIndex:1];
            [rms_encoder setBuffer:normed_gated_buffer offset:0 atIndex:2];
            [rms_encoder setBytes:&rms_params length:sizeof(rms_params) atIndex:3];
            [rms_encoder dispatchThreads:MTLSizeMake(rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->rms_norm_rows_pipeline, rows), 1, 1)];
            [rms_encoder endEncoding];
            down_input_buffer = normed_gated_buffer;
        }

        id<MTLComputeCommandEncoder> down_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (down_encoder == nil) return -20;
        [down_encoder setComputePipelineState:runtime->q6_k_pipeline];
        [down_encoder setBuffer:down_input_buffer offset:0 atIndex:0];
        [down_encoder setBuffer:down_view.weight_buffer offset:0 atIndex:1];
        [down_encoder setBuffer:projected_buffer offset:0 atIndex:2];
        [down_encoder setBytes:&down_params length:sizeof(down_params) atIndex:3];
        [down_encoder dispatchThreads:MTLSizeMake(rows * hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->q6_k_pipeline, rows * hidden_size), 1, 1)];
        [down_encoder endEncoding];

        id<MTLBuffer> add_lhs_buffer = projected_buffer;
        if (post_down_rms_norm_slot != SIZE_MAX) {
            id<MTLComputeCommandEncoder> post_down_encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_RMS_NORM);
            if (post_down_encoder == nil) return -21;
            [post_down_encoder setComputePipelineState:runtime->rms_norm_rows_pipeline];
            [post_down_encoder setBuffer:projected_buffer offset:0 atIndex:0];
            [post_down_encoder setBuffer:runtime->rms_norm_weight_buffers[post_down_rms_norm_slot] offset:0 atIndex:1];
            [post_down_encoder setBuffer:post_down_buffer offset:0 atIndex:2];
            [post_down_encoder setBytes:&post_down_rms_params length:sizeof(post_down_rms_params) atIndex:3];
            [post_down_encoder dispatchThreads:MTLSizeMake(rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->rms_norm_rows_pipeline, rows), 1, 1)];
            [post_down_encoder endEncoding];
            add_lhs_buffer = post_down_buffer;
        }

        id<MTLComputeCommandEncoder> add_encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_FFN);
        if (add_encoder == nil) return -22;
        [add_encoder setComputePipelineState:runtime->add_pipeline];
        [add_encoder setBuffer:add_lhs_buffer offset:0 atIndex:0];
        [add_encoder setBuffer:residual_buffer offset:0 atIndex:1];
        [add_encoder setBuffer:output_buffer offset:0 atIndex:2];
        [add_encoder setBytes:&add_params length:sizeof(add_params) atIndex:3];
        [add_encoder dispatchThreads:MTLSizeMake(rows * hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->add_pipeline, rows * hidden_size), 1, 1)];
        [add_encoder endEncoding];

        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -23;
    }
}

int termite_metal_decode_runtime_apply_gated_ffn_residual_q4_0_pair_q8_0_down_slots(
    termite_metal_decode_runtime *runtime,
    const float *input,
    const float *residual,
    size_t rows,
    size_t hidden_size,
    size_t intermediate_size,
    uint32_t activation_kind,
    size_t gate_linear_slot,
    size_t up_linear_slot,
    size_t post_gate_rms_norm_slot,
    size_t post_down_rms_norm_slot,
    size_t down_linear_slot,
    float *output
) {
    if (runtime == NULL || input == NULL || residual == NULL || output == NULL) return -1;
    if (runtime->q4_0_pair_pipeline == nil || runtime->q8_0_pipeline == nil || runtime->activation_pipeline == nil || runtime->multiply_pipeline == nil || runtime->add_pipeline == nil) return -2;
    if (rows == 0 || hidden_size == 0 || intermediate_size == 0 || rows > UINT32_MAX || hidden_size > UINT32_MAX || intermediate_size > UINT32_MAX) return -3;
    if (hidden_size % 32u != 0 || intermediate_size % 32u != 0) return -4;
    if (gate_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || up_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || down_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -5;
    termite_metal_quant_linear_slot_view gate_view;
    termite_metal_quant_linear_slot_view up_view;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q4_0, gate_linear_slot, hidden_size, intermediate_size, &gate_view) != 0) return -6;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q4_0, up_linear_slot, hidden_size, intermediate_size, &up_view) != 0) return -7;
    termite_metal_quant_linear_slot_view down_view;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q8_0, down_linear_slot, intermediate_size, hidden_size, &down_view) != 0) return -8;
    if (post_gate_rms_norm_slot != SIZE_MAX) {
        if (post_gate_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return -9;
        if (runtime->rms_norm_slot_prepared[post_gate_rms_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[post_gate_rms_norm_slot] != intermediate_size) return -10;
    }
    if (post_down_rms_norm_slot != SIZE_MAX) {
        if (post_down_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return -11;
        if (runtime->rms_norm_slot_prepared[post_down_rms_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[post_down_rms_norm_slot] != hidden_size) return -12;
    }
    @autoreleasepool {
        const size_t hidden_bytes = rows * hidden_size * sizeof(float);
        const size_t intermediate_bytes = rows * intermediate_size * sizeof(float);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)input length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:input length:hidden_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> residual_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)residual length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (residual_buffer == nil) residual_buffer = [runtime->device newBufferWithBytes:residual length:hidden_bytes options:MTLResourceStorageModeShared];
        if (termite_metal_decode_runtime_ensure_hot_buffers(runtime, hidden_bytes, intermediate_bytes) != 0) return -13;
        id<MTLBuffer> gate_output_buffer = runtime->hot_intermediate_buffers[0];
        id<MTLBuffer> up_output_buffer = runtime->hot_intermediate_buffers[1];
        id<MTLBuffer> activated_buffer = runtime->hot_intermediate_buffers[2];
        id<MTLBuffer> gated_buffer = runtime->hot_intermediate_buffers[3];
        id<MTLBuffer> normed_gated_buffer = post_gate_rms_norm_slot != SIZE_MAX ? runtime->hot_intermediate_buffers[4] : nil;
        id<MTLBuffer> projected_buffer = runtime->hot_hidden_buffers[0];
        id<MTLBuffer> ffn_scalar_or_post_down_buffer = runtime->hot_hidden_buffers[1];
        id<MTLBuffer> post_down_buffer = post_down_rms_norm_slot != SIZE_MAX ? ffn_scalar_or_post_down_buffer : nil;
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithBytesNoCopy:output length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) output_buffer = [runtime->device newBufferWithBytes:output length:hidden_bytes options:MTLResourceStorageModeShared];
        if (input_buffer == nil || residual_buffer == nil || gate_output_buffer == nil || up_output_buffer == nil || activated_buffer == nil || gated_buffer == nil || projected_buffer == nil || output_buffer == nil || (post_gate_rms_norm_slot != SIZE_MAX && normed_gated_buffer == nil) || (post_down_rms_norm_slot != SIZE_MAX && post_down_buffer == nil)) return -13;

        termite_metal_apply_activation_params activation_params = { .activation_kind = activation_kind, .rows = (uint32_t)rows, .dim = (uint32_t)intermediate_size };
        termite_metal_apply_add_params multiply_params = { .rows = (uint32_t)rows, .dim = (uint32_t)intermediate_size };
        termite_metal_apply_row_norm_params rms_params = { .rows = (uint32_t)rows, .hidden_size = (uint32_t)intermediate_size, .eps = 0.0f };
        termite_metal_apply_row_norm_params post_down_rms_params = { .rows = (uint32_t)rows, .hidden_size = (uint32_t)hidden_size, .eps = 0.0f };
        termite_metal_apply_add_params add_params = { .rows = (uint32_t)rows, .dim = (uint32_t)hidden_size };
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -15;

        id<MTLComputeCommandEncoder> pair_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (pair_encoder == nil) return -16;
        termite_metal_quant_matmul_descriptor pair_descriptor = {
            .slot = gate_linear_slot,
            .format = TERMITE_METAL_QUANT_FORMAT_Q4_0,
            .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
            .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_PAIR,
            .fallback_pipeline = runtime->q4_0_pair_pipeline,
            .input_buffer = input_buffer,
            .weight_buffer = gate_view.weight_buffer,
            .second_weight_buffer = up_view.weight_buffer,
            .output_buffer = gate_output_buffer,
            .second_output_buffer = up_output_buffer,
            .rows = rows,
            .in_dim = hidden_size,
            .out_dim = intermediate_size,
            .values_per_block = gate_view.values_per_block,
            .bytes_per_block = gate_view.bytes_per_block,
            .failure_code = -16,
        };
        if (termite_metal_encode_quant_matmul_descriptor_on_encoder(runtime, pair_encoder, &pair_descriptor) != 0) return -16;
        [pair_encoder endEncoding];

        id<MTLComputeCommandEncoder> activation_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (activation_encoder == nil) return -17;
        [activation_encoder setComputePipelineState:runtime->activation_pipeline];
        [activation_encoder setBuffer:gate_output_buffer offset:0 atIndex:0];
        [activation_encoder setBuffer:activated_buffer offset:0 atIndex:1];
        [activation_encoder setBytes:&activation_params length:sizeof(activation_params) atIndex:2];
        [activation_encoder dispatchThreads:MTLSizeMake(rows * intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->activation_pipeline, rows * intermediate_size), 1, 1)];
        [activation_encoder endEncoding];

        id<MTLComputeCommandEncoder> multiply_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (multiply_encoder == nil) return -18;
        [multiply_encoder setComputePipelineState:runtime->multiply_pipeline];
        [multiply_encoder setBuffer:activated_buffer offset:0 atIndex:0];
        [multiply_encoder setBuffer:up_output_buffer offset:0 atIndex:1];
        [multiply_encoder setBuffer:gated_buffer offset:0 atIndex:2];
        [multiply_encoder setBytes:&multiply_params length:sizeof(multiply_params) atIndex:3];
        [multiply_encoder dispatchThreads:MTLSizeMake(rows * intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->multiply_pipeline, rows * intermediate_size), 1, 1)];
        [multiply_encoder endEncoding];

        id<MTLBuffer> down_input_buffer = gated_buffer;
        BOOL down_projection_done = NO;
        if (post_gate_rms_norm_slot != SIZE_MAX) {
            if (rows == 1 && ffn_scalar_or_post_down_buffer != nil && runtime->rms_inv_scale_pipeline != nil && runtime->q8_0_rms_scale_mmv_pipeline != nil) {
                const int scale_status = termite_metal_encode_rms_inv_scale_1x(
                    runtime,
                    command_buffer,
                    gated_buffer,
                    0,
                    ffn_scalar_or_post_down_buffer,
                    0,
                    intermediate_size,
                    0.0f,
                    -19);
                const int down_status = scale_status == 0 ? termite_metal_encode_q8_0_linear_rms_scale_1x(
                    runtime,
                    command_buffer,
                    gated_buffer,
                    0,
                    post_gate_rms_norm_slot,
                    ffn_scalar_or_post_down_buffer,
                    0,
                    down_view.weight_buffer,
                    projected_buffer,
                    0,
                    intermediate_size,
                    hidden_size,
                    -20) : -20;
                down_projection_done = (scale_status == 0 && down_status == 0);
            }
            if (!down_projection_done) {
                id<MTLComputeCommandEncoder> rms_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
                if (rms_encoder == nil) return -19;
                [rms_encoder setComputePipelineState:runtime->rms_norm_rows_pipeline];
                [rms_encoder setBuffer:gated_buffer offset:0 atIndex:0];
                [rms_encoder setBuffer:runtime->rms_norm_weight_buffers[post_gate_rms_norm_slot] offset:0 atIndex:1];
                [rms_encoder setBuffer:normed_gated_buffer offset:0 atIndex:2];
                [rms_encoder setBytes:&rms_params length:sizeof(rms_params) atIndex:3];
                [rms_encoder dispatchThreads:MTLSizeMake(rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->rms_norm_rows_pipeline, rows), 1, 1)];
                [rms_encoder endEncoding];
                down_input_buffer = normed_gated_buffer;
            }
        }

        if (!down_projection_done && termite_metal_encode_q8_0_linear(
                runtime,
                command_buffer,
                down_input_buffer,
                0,
                down_view.weight_buffer,
                projected_buffer,
                0,
                rows,
                intermediate_size,
                hidden_size,
                -20) != 0) return -20;

        id<MTLBuffer> add_lhs_buffer = projected_buffer;
        if (post_down_rms_norm_slot != SIZE_MAX) {
            id<MTLComputeCommandEncoder> post_down_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
            if (post_down_encoder == nil) return -21;
            [post_down_encoder setComputePipelineState:runtime->rms_norm_rows_pipeline];
            [post_down_encoder setBuffer:projected_buffer offset:0 atIndex:0];
            [post_down_encoder setBuffer:runtime->rms_norm_weight_buffers[post_down_rms_norm_slot] offset:0 atIndex:1];
            [post_down_encoder setBuffer:post_down_buffer offset:0 atIndex:2];
            [post_down_encoder setBytes:&post_down_rms_params length:sizeof(post_down_rms_params) atIndex:3];
            [post_down_encoder dispatchThreads:MTLSizeMake(rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->rms_norm_rows_pipeline, rows), 1, 1)];
            [post_down_encoder endEncoding];
            add_lhs_buffer = post_down_buffer;
        }

        id<MTLComputeCommandEncoder> add_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (add_encoder == nil) return -22;
        [add_encoder setComputePipelineState:runtime->add_pipeline];
        [add_encoder setBuffer:add_lhs_buffer offset:0 atIndex:0];
        [add_encoder setBuffer:residual_buffer offset:0 atIndex:1];
        [add_encoder setBuffer:output_buffer offset:0 atIndex:2];
        [add_encoder setBytes:&add_params length:sizeof(add_params) atIndex:3];
        [add_encoder dispatchThreads:MTLSizeMake(rows * hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->add_pipeline, rows * hidden_size), 1, 1)];
        [add_encoder endEncoding];

        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -23;
    }
}

int termite_metal_decode_runtime_apply_gated_ffn_residual_q8_0_slots(
    termite_metal_decode_runtime *runtime,
    const float *input,
    const float *residual,
    size_t rows,
    size_t hidden_size,
    size_t intermediate_size,
    uint32_t activation_kind,
    float eps,
    size_t gate_linear_slot,
    size_t up_linear_slot,
    size_t post_gate_rms_norm_slot,
    size_t post_down_rms_norm_slot,
    size_t down_linear_slot,
    float *output
) {
    if (runtime == NULL || input == NULL || residual == NULL || output == NULL) return -1;
    if (runtime->q8_0_pipeline == nil || runtime->q8_0_pair_pipeline == nil || runtime->rms_norm_rows_pipeline == nil || runtime->activation_multiply_pipeline == nil || runtime->add_pipeline == nil) return -2;
    if (rows == 0 || hidden_size == 0 || intermediate_size == 0 || rows > UINT32_MAX || hidden_size > UINT32_MAX || intermediate_size > UINT32_MAX) return -3;
    if (hidden_size % 32u != 0 || intermediate_size % 32u != 0) return -4;
    if (gate_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || up_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || down_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -5;
    termite_metal_quant_linear_slot_view gate_view;
    termite_metal_quant_linear_slot_view up_view;
    termite_metal_quant_linear_slot_view down_view;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q8_0, gate_linear_slot, hidden_size, intermediate_size, &gate_view) != 0) return -6;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q8_0, up_linear_slot, hidden_size, intermediate_size, &up_view) != 0) return -7;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q8_0, down_linear_slot, intermediate_size, hidden_size, &down_view) != 0) return -8;
    if (post_gate_rms_norm_slot != SIZE_MAX) {
        if (post_gate_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return -9;
        if (runtime->rms_norm_slot_prepared[post_gate_rms_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[post_gate_rms_norm_slot] != intermediate_size) return -10;
    }
    if (post_down_rms_norm_slot != SIZE_MAX) {
        if (post_down_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return -11;
        if (runtime->rms_norm_slot_prepared[post_down_rms_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[post_down_rms_norm_slot] != hidden_size) return -12;
    }
    @autoreleasepool {
        const size_t hidden_bytes = rows * hidden_size * sizeof(float);
        const size_t intermediate_bytes = rows * intermediate_size * sizeof(float);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)input length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:input length:hidden_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> residual_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)residual length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (residual_buffer == nil) residual_buffer = [runtime->device newBufferWithBytes:residual length:hidden_bytes options:MTLResourceStorageModeShared];
        if (termite_metal_decode_runtime_ensure_hot_buffers(runtime, hidden_bytes, intermediate_bytes) != 0) return -13;
        id<MTLBuffer> gate_output_buffer = runtime->hot_intermediate_buffers[0];
        id<MTLBuffer> up_output_buffer = runtime->hot_intermediate_buffers[1];
        id<MTLBuffer> activated_buffer = runtime->hot_intermediate_buffers[2];
        id<MTLBuffer> gated_buffer = runtime->hot_intermediate_buffers[3];
        id<MTLBuffer> normed_gated_buffer = post_gate_rms_norm_slot != SIZE_MAX ? runtime->hot_intermediate_buffers[4] : nil;
        id<MTLBuffer> projected_buffer = runtime->hot_hidden_buffers[0];
        id<MTLBuffer> ffn_scalar_or_post_down_buffer = runtime->hot_hidden_buffers[1];
        id<MTLBuffer> post_down_buffer = post_down_rms_norm_slot != SIZE_MAX ? ffn_scalar_or_post_down_buffer : nil;
        BOOL output_borrowed = YES;
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithBytesNoCopy:output length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) {
            output_borrowed = NO;
            output_buffer = [runtime->device newBufferWithBytes:output length:hidden_bytes options:MTLResourceStorageModeShared];
        }
        if (input_buffer == nil || residual_buffer == nil || gate_output_buffer == nil || up_output_buffer == nil || activated_buffer == nil || gated_buffer == nil || projected_buffer == nil || output_buffer == nil || (post_gate_rms_norm_slot != SIZE_MAX && normed_gated_buffer == nil) || (post_down_rms_norm_slot != SIZE_MAX && post_down_buffer == nil)) return -14;

        termite_metal_apply_activation_params activation_params = { .activation_kind = activation_kind, .rows = (uint32_t)rows, .dim = (uint32_t)intermediate_size };
        termite_metal_apply_add_params multiply_params = { .rows = (uint32_t)rows, .dim = (uint32_t)intermediate_size };
        termite_metal_apply_row_norm_params rms_params = { .rows = (uint32_t)rows, .hidden_size = (uint32_t)intermediate_size, .eps = eps };
        termite_metal_apply_row_norm_params post_down_rms_params = { .rows = (uint32_t)rows, .hidden_size = (uint32_t)hidden_size, .eps = eps };
        termite_metal_apply_add_params add_params = { .rows = (uint32_t)rows, .dim = (uint32_t)hidden_size };
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -15;

        const bool split_gate_up_linear = (rows >= 9u && runtime->q8_0_pair_activation_mm_pipeline == nil && runtime->q8_0_mm_pipeline != nil);
        const bool fused_gate_up = !split_gate_up_linear && termite_metal_encode_q8_0_pair_activation_multiply(
            runtime,
            command_buffer,
            input_buffer,
            0,
            gate_view.weight_buffer,
            up_view.weight_buffer,
            gated_buffer,
            0,
            rows,
            hidden_size,
            intermediate_size,
            activation_kind,
            -16) == 0;
        if (!fused_gate_up) {
            if (split_gate_up_linear) {
                if (termite_metal_encode_q8_0_linear(
                        runtime,
                        command_buffer,
                        input_buffer,
                        0,
                        gate_view.weight_buffer,
                        gate_output_buffer,
                        0,
                        rows,
                        hidden_size,
                        intermediate_size,
                        -16) != 0) return -16;
                if (termite_metal_encode_q8_0_linear(
                        runtime,
                        command_buffer,
                        input_buffer,
                        0,
                        up_view.weight_buffer,
                        up_output_buffer,
                        0,
                        rows,
                        hidden_size,
                        intermediate_size,
                        -16) != 0) return -16;
            } else {
                if (termite_metal_encode_q8_0_pair_linear(
                        runtime,
                        command_buffer,
                        input_buffer,
                        0,
                        gate_view.weight_buffer,
                        up_view.weight_buffer,
                        gate_output_buffer,
                        0,
                        up_output_buffer,
                        0,
                        rows,
                        hidden_size,
                        intermediate_size,
                        -16) != 0) return -16;
            }

            BOOL gated_encoder_owned = YES;
            id<MTLComputeCommandEncoder> gated_encoder = termite_metal_scoped_compute_encoder_for(runtime, command_buffer, TERMITE_METAL_COMPUTE_SOURCE_FFN, &gated_encoder_owned);
            if (gated_encoder == nil) return -17;
            if (termite_metal_decode_runtime_prepare_planned_compute_binary_accesses(
                    runtime,
                    gate_output_buffer,
                    0,
                    intermediate_bytes,
                    up_output_buffer,
                    0,
                    intermediate_bytes,
                    gated_buffer,
                    0,
                    intermediate_bytes,
                    -17) != 0) return -17;
            [gated_encoder setComputePipelineState:runtime->activation_multiply_pipeline];
            [gated_encoder setBuffer:gate_output_buffer offset:0 atIndex:0];
            [gated_encoder setBuffer:up_output_buffer offset:0 atIndex:1];
            [gated_encoder setBuffer:gated_buffer offset:0 atIndex:2];
            [gated_encoder setBytes:&activation_params length:sizeof(activation_params) atIndex:3];
            [gated_encoder dispatchThreads:MTLSizeMake(rows * intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->activation_multiply_pipeline, rows * intermediate_size), 1, 1)];
            termite_metal_end_scoped_compute_encoder(gated_encoder, gated_encoder_owned);
        }

        id<MTLBuffer> down_input_buffer = gated_buffer;
        BOOL down_projection_done = NO;
        if (post_gate_rms_norm_slot != SIZE_MAX) {
            if (rows == 1 && ffn_scalar_or_post_down_buffer != nil && runtime->rms_inv_scale_pipeline != nil && runtime->q8_0_rms_scale_mmv_pipeline != nil) {
                const int scale_status = termite_metal_encode_rms_inv_scale_1x(
                    runtime,
                    command_buffer,
                    gated_buffer,
                    0,
                    ffn_scalar_or_post_down_buffer,
                    0,
                    intermediate_size,
                    0.0f,
                    -19);
                const int down_status = scale_status == 0 ? termite_metal_encode_q8_0_linear_rms_scale_1x(
                    runtime,
                    command_buffer,
                    gated_buffer,
                    0,
                    post_gate_rms_norm_slot,
                    ffn_scalar_or_post_down_buffer,
                    0,
                    down_view.weight_buffer,
                    projected_buffer,
                    0,
                    intermediate_size,
                    hidden_size,
                    -20) : -20;
                down_projection_done = (scale_status == 0 && down_status == 0);
            }
            if (!down_projection_done) {
                id<MTLComputeCommandEncoder> rms_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
                if (rms_encoder == nil) return -19;
                [rms_encoder setComputePipelineState:runtime->rms_norm_rows_pipeline];
                [rms_encoder setBuffer:gated_buffer offset:0 atIndex:0];
                [rms_encoder setBuffer:runtime->rms_norm_weight_buffers[post_gate_rms_norm_slot] offset:0 atIndex:1];
                [rms_encoder setBuffer:normed_gated_buffer offset:0 atIndex:2];
                [rms_encoder setBytes:&rms_params length:sizeof(rms_params) atIndex:3];
                [rms_encoder dispatchThreads:MTLSizeMake(rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->rms_norm_rows_pipeline, rows), 1, 1)];
                [rms_encoder endEncoding];
                down_input_buffer = normed_gated_buffer;
            }
        }

        if (!down_projection_done && termite_metal_encode_q8_0_linear(
                runtime,
                command_buffer,
                down_input_buffer,
                0,
                down_view.weight_buffer,
                projected_buffer,
                0,
                rows,
                intermediate_size,
                hidden_size,
                -20) != 0) return -20;

        id<MTLBuffer> add_lhs_buffer = projected_buffer;
        if (post_down_rms_norm_slot != SIZE_MAX) {
            id<MTLComputeCommandEncoder> post_down_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
            if (post_down_encoder == nil) return -21;
            [post_down_encoder setComputePipelineState:runtime->rms_norm_rows_pipeline];
            [post_down_encoder setBuffer:projected_buffer offset:0 atIndex:0];
            [post_down_encoder setBuffer:runtime->rms_norm_weight_buffers[post_down_rms_norm_slot] offset:0 atIndex:1];
            [post_down_encoder setBuffer:post_down_buffer offset:0 atIndex:2];
            [post_down_encoder setBytes:&post_down_rms_params length:sizeof(post_down_rms_params) atIndex:3];
            [post_down_encoder dispatchThreads:MTLSizeMake(rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->rms_norm_rows_pipeline, rows), 1, 1)];
            [post_down_encoder endEncoding];
            add_lhs_buffer = post_down_buffer;
        }

        id<MTLComputeCommandEncoder> add_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (add_encoder == nil) return -22;
        [add_encoder setComputePipelineState:runtime->add_pipeline];
        [add_encoder setBuffer:add_lhs_buffer offset:0 atIndex:0];
        [add_encoder setBuffer:residual_buffer offset:0 atIndex:1];
        [add_encoder setBuffer:output_buffer offset:0 atIndex:2];
        [add_encoder setBytes:&add_params length:sizeof(add_params) atIndex:3];
        [add_encoder dispatchThreads:MTLSizeMake(rows * hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->add_pipeline, rows * hidden_size), 1, 1)];
        [add_encoder endEncoding];

        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        if (command_buffer.status != MTLCommandBufferStatusCompleted) return -23;
        if (!output_borrowed) {
            memcpy(output, [output_buffer contents], hidden_bytes);
        }
        return 0;
    }
}

static int termite_metal_decode_runtime_apply_gated_ffn_residual_q8_0_slots_device_impl(
    termite_metal_decode_runtime *runtime,
    void *input_handle,
    size_t input_offset,
    void *residual_handle,
    size_t residual_offset,
    size_t rows,
    size_t hidden_size,
    size_t intermediate_size,
    uint32_t activation_kind,
    size_t gate_linear_slot,
    size_t up_linear_slot,
    size_t pre_gate_rms_norm_slot,
    size_t post_gate_rms_norm_slot,
    size_t post_down_rms_norm_slot,
    float eps,
    size_t down_linear_slot,
    void *output_handle,
    size_t output_offset,
    termite_metal_planned_layer_contract planned_contract
) {
    if (runtime == NULL || input_handle == NULL || residual_handle == NULL || output_handle == NULL) return -1;
    if (runtime->q8_0_pipeline == nil || runtime->q8_0_pair_pipeline == nil || runtime->rms_norm_rows_pipeline == nil || runtime->activation_multiply_pipeline == nil || runtime->add_pipeline == nil) return -2;
    if (rows == 0 || hidden_size == 0 || intermediate_size == 0 || rows > UINT32_MAX || hidden_size > UINT32_MAX || intermediate_size > UINT32_MAX) return -3;
    if (hidden_size % 32u != 0 || intermediate_size % 32u != 0) return -4;
    if (gate_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || up_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || down_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -5;
    termite_metal_quant_linear_slot_view gate_view;
    termite_metal_quant_linear_slot_view up_view;
    termite_metal_quant_linear_slot_view down_view;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q8_0, gate_linear_slot, hidden_size, intermediate_size, &gate_view) != 0) return -6;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q8_0, up_linear_slot, hidden_size, intermediate_size, &up_view) != 0) return -7;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q8_0, down_linear_slot, intermediate_size, hidden_size, &down_view) != 0) return -8;
    if (pre_gate_rms_norm_slot != SIZE_MAX) {
        if (pre_gate_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return -9;
        if (runtime->rms_norm_slot_prepared[pre_gate_rms_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[pre_gate_rms_norm_slot] != hidden_size) return -10;
    }
    if (post_gate_rms_norm_slot != SIZE_MAX) {
        if (post_gate_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return -11;
        if (runtime->rms_norm_slot_prepared[post_gate_rms_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[post_gate_rms_norm_slot] != intermediate_size) return -12;
    }
    if (post_down_rms_norm_slot != SIZE_MAX) {
        if (post_down_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return -13;
        if (runtime->rms_norm_slot_prepared[post_down_rms_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[post_down_rms_norm_slot] != hidden_size) return -14;
    }
    @autoreleasepool {
        const size_t hidden_bytes = rows * hidden_size * sizeof(float);
        const size_t intermediate_bytes = rows * intermediate_size * sizeof(float);
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)input_handle;
        id<MTLBuffer> residual_buffer = (__bridge id<MTLBuffer>)residual_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        if (termite_metal_decode_runtime_ensure_hot_buffers(runtime, hidden_bytes, intermediate_bytes) != 0) return -13;
        id<MTLBuffer> gate_output_buffer = runtime->hot_intermediate_buffers[0];
        id<MTLBuffer> up_output_buffer = runtime->hot_intermediate_buffers[1];
        id<MTLBuffer> activated_buffer = runtime->hot_intermediate_buffers[2];
        id<MTLBuffer> gated_buffer = runtime->hot_intermediate_buffers[3];
        id<MTLBuffer> normed_gated_buffer = post_gate_rms_norm_slot != SIZE_MAX ? runtime->hot_intermediate_buffers[4] : nil;
        id<MTLBuffer> projected_buffer = runtime->hot_hidden_buffers[0];
        id<MTLBuffer> ffn_scalar_or_hidden_buffer = runtime->hot_hidden_buffers[1];
        id<MTLBuffer> pre_norm_input_buffer = pre_gate_rms_norm_slot != SIZE_MAX ? ffn_scalar_or_hidden_buffer : nil;
        if (input_buffer == nil || residual_buffer == nil || output_buffer == nil || gate_output_buffer == nil || up_output_buffer == nil || activated_buffer == nil || gated_buffer == nil || projected_buffer == nil || (post_gate_rms_norm_slot != SIZE_MAX && normed_gated_buffer == nil)) return -14;

        termite_metal_apply_activation_params activation_params = { .activation_kind = activation_kind, .rows = (uint32_t)rows, .dim = (uint32_t)intermediate_size };
        termite_metal_apply_row_norm_params rms_params = { .rows = (uint32_t)rows, .hidden_size = (uint32_t)intermediate_size, .eps = eps };
        termite_metal_apply_add_params add_params = { .rows = (uint32_t)rows, .dim = (uint32_t)hidden_size };
        const bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLCommandBuffer> command_buffer = frame_owned
            ? termite_metal_new_command_buffer(runtime->queue, __func__)
            : runtime->active_frame_cb;
        if (command_buffer == nil) return -15;

        const bool split_gate_up_linear = (rows >= 9u && runtime->q8_0_pair_activation_mm_pipeline == nil && runtime->q8_0_mm_pipeline != nil);
        if (pre_gate_rms_norm_slot != SIZE_MAX &&
            !split_gate_up_linear &&
            rows == 1 &&
            post_gate_rms_norm_slot == SIZE_MAX &&
            post_down_rms_norm_slot != SIZE_MAX &&
            pre_norm_input_buffer != nil &&
            runtime->rms_inv_scale_pipeline != nil &&
            runtime->q8_0_pair_activation_rms_scale_mmv_pipeline != nil &&
            runtime->active_frame_cb == command_buffer) {
            const int planned_status = termite_metal_encode_q8_0_ffn_pregate_pair_down_rms_add_planned_1x(
                runtime,
                command_buffer,
                input_buffer,
                input_offset,
                pre_gate_rms_norm_slot,
                pre_norm_input_buffer,
                gate_view.weight_buffer,
                up_view.weight_buffer,
                gated_buffer,
                down_view.weight_buffer,
                projected_buffer,
                hidden_size,
                intermediate_size,
                activation_kind,
                post_down_rms_norm_slot,
                residual_buffer,
                residual_offset,
                output_buffer,
                output_offset,
                eps,
                -16);
            if (planned_status != 0) return planned_status;
            return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -23);
        }

        BOOL pre_gate_pair_done = NO;
        id<MTLBuffer> gate_input_buffer = input_buffer;
        size_t gate_input_offset = input_offset;
        if (pre_gate_rms_norm_slot != SIZE_MAX) {
            termite_metal_decode_runtime_record_logical_command_op(
                runtime,
                TERMITE_METAL_PLAN_OP_DECODE_FFN_PRE_NORM_SCALE,
                TERMITE_METAL_OPERATOR_UNKNOWN,
                TERMITE_METAL_QUANT_MATMUL_DISPATCH_UNKNOWN);
            if (pre_norm_input_buffer == nil) return -16;
            if (rows == 1 && runtime->rms_inv_scale_pipeline != nil && runtime->q8_0_pair_activation_rms_scale_mmv_pipeline != nil) {
                const int scale_status = termite_metal_encode_rms_inv_scale_1x(
                    runtime,
                    command_buffer,
                    input_buffer,
                    input_offset,
                    pre_norm_input_buffer,
                    0,
                    hidden_size,
                    eps,
                    -16);
                const int pair_status = scale_status == 0 ? termite_metal_encode_q8_0_pair_activation_multiply_rms_scale_1x(
                    runtime,
                    command_buffer,
                    input_buffer,
                    input_offset,
                    pre_gate_rms_norm_slot,
                    pre_norm_input_buffer,
                    0,
                    gate_view.weight_buffer,
                    up_view.weight_buffer,
                    gated_buffer,
                    0,
                    hidden_size,
                    intermediate_size,
                    activation_kind,
                    -16) : -16;
                pre_gate_pair_done = (scale_status == 0 && pair_status == 0);
            }
            if (!pre_gate_pair_done && rows == 1) {
                if (termite_metal_encode_rms_norm_reduce(
                        runtime,
                        command_buffer,
                        input_buffer,
                        input_offset,
                        pre_gate_rms_norm_slot,
                        pre_norm_input_buffer,
                        0,
                        hidden_size,
                        eps,
                        -16) != 0) return -16;
            } else if (!pre_gate_pair_done) {
                if (termite_metal_encode_rms_norm_rows(
                        runtime,
                        command_buffer,
                        input_buffer,
                        input_offset,
                        pre_gate_rms_norm_slot,
                        pre_norm_input_buffer,
                        0,
                        rows,
                        hidden_size,
                        eps,
                        -16) != 0) return -16;
            }
            if (!pre_gate_pair_done) {
                gate_input_buffer = pre_norm_input_buffer;
                gate_input_offset = 0;
            }
        }

        if (!pre_gate_pair_done &&
            !split_gate_up_linear &&
            rows == 1 &&
            post_gate_rms_norm_slot == SIZE_MAX &&
            post_down_rms_norm_slot != SIZE_MAX &&
            runtime->active_frame_cb == command_buffer) {
            const int planned_status = termite_metal_encode_q8_0_ffn_pair_down_rms_add_planned_1x(
                runtime,
                command_buffer,
                gate_input_buffer,
                gate_input_offset,
                gate_view.weight_buffer,
                up_view.weight_buffer,
                gated_buffer,
                down_view.weight_buffer,
                projected_buffer,
                hidden_size,
                intermediate_size,
                activation_kind,
                post_down_rms_norm_slot,
                residual_buffer,
                residual_offset,
                output_buffer,
                output_offset,
                eps,
                -16);
            if (planned_status != 0) return planned_status;
            return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -23);
        }
        termite_metal_decode_runtime_record_contract_or_logical_command_op(
            runtime,
            planned_contract,
            TERMITE_METAL_PLAN_OP_DECODE_FFN_GATE_UP_ACTIVATION,
            TERMITE_METAL_OPERATOR_MUL_MM,
            TERMITE_METAL_QUANT_MATMUL_DISPATCH_MM);
        termite_metal_runtime_command_op gate_up_op;
        const bool has_planned_gate_up = termite_metal_planned_layer_contract_find_command_op(
            planned_contract,
            TERMITE_METAL_PLAN_OP_DECODE_FFN_GATE_UP_ACTIVATION,
            &gate_up_op);
        int gate_up_status = -16;
        if (!pre_gate_pair_done && !split_gate_up_linear) {
            const termite_metal_quant_matmul_activation_dtype gate_up_output_dtype =
                (has_planned_gate_up && post_gate_rms_norm_slot == SIZE_MAX)
                    ? termite_metal_quant_matmul_dtype_from_command(gate_up_op.output_dtype)
                    : TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32;
            termite_metal_quant_matmul_descriptor descriptor = {
                .slot = gate_linear_slot,
                .format = TERMITE_METAL_QUANT_FORMAT_Q8_0,
                .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
                .input_dtype = has_planned_gate_up ? termite_metal_quant_matmul_dtype_from_command(gate_up_op.input_dtype) : TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
                .output_dtype = gate_up_output_dtype,
                .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_PAIR_ACTIVATION_MUL,
                .fallback_pipeline = runtime->q8_0_pair_activation_reduce_pipeline,
                .input_buffer = gate_input_buffer,
                .input_offset = gate_input_offset,
                .weight_buffer = gate_view.weight_buffer,
                .second_weight_buffer = up_view.weight_buffer,
                .output_buffer = gated_buffer,
                .rows = rows,
                .in_dim = hidden_size,
                .out_dim = intermediate_size,
                .values_per_block = 32u,
                .bytes_per_block = 34u,
                .activation_kind = activation_kind,
                .q8_family = TERMITE_METAL_Q8_0_LINEAR_FAMILY_PAIR_ACTIVATION,
                .use_planned_dispatch = has_planned_gate_up,
                .planned_dispatch = has_planned_gate_up ? gate_up_op.quant_dispatch : TERMITE_METAL_QUANT_MATMUL_DISPATCH_UNKNOWN,
                .failure_code = -16,
            };
            gate_up_status = termite_metal_encode_quant_matmul_descriptor(runtime, command_buffer, &descriptor);
            if (gate_up_status != 0 && has_planned_gate_up &&
                (gate_up_op.input_dtype != TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32 ||
                    gate_up_op.output_dtype != TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32))
            {
                descriptor.input_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32;
                descriptor.output_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32;
                descriptor.use_planned_dispatch = false;
                descriptor.planned_dispatch = TERMITE_METAL_QUANT_MATMUL_DISPATCH_UNKNOWN;
                gate_up_status = termite_metal_encode_quant_matmul_descriptor(runtime, command_buffer, &descriptor);
            }
        }
        const bool fused_gate_up = pre_gate_pair_done || (!split_gate_up_linear && gate_up_status == 0);
        if (!fused_gate_up) {
            if (split_gate_up_linear) {
                if (termite_metal_encode_q8_0_linear(
                        runtime,
                        command_buffer,
                        gate_input_buffer,
                        gate_input_offset,
                        gate_view.weight_buffer,
                        gate_output_buffer,
                        0,
                        rows,
                        hidden_size,
                        intermediate_size,
                        -16) != 0) return -16;
                if (termite_metal_encode_q8_0_linear(
                        runtime,
                        command_buffer,
                        gate_input_buffer,
                        gate_input_offset,
                        up_view.weight_buffer,
                        up_output_buffer,
                        0,
                        rows,
                        hidden_size,
                        intermediate_size,
                        -16) != 0) return -16;
            } else {
                if (termite_metal_encode_q8_0_pair_linear(
                        runtime,
                        command_buffer,
                        gate_input_buffer,
                        gate_input_offset,
                        gate_view.weight_buffer,
                        up_view.weight_buffer,
                        gate_output_buffer,
                        0,
                        up_output_buffer,
                        0,
                        rows,
                        hidden_size,
                        intermediate_size,
                        -16) != 0) return -16;
            }

            id<MTLComputeCommandEncoder> gated_encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_FFN);
            if (gated_encoder == nil) return -17;
            [gated_encoder setComputePipelineState:runtime->activation_multiply_pipeline];
            [gated_encoder setBuffer:gate_output_buffer offset:0 atIndex:0];
            [gated_encoder setBuffer:up_output_buffer offset:0 atIndex:1];
            [gated_encoder setBuffer:gated_buffer offset:0 atIndex:2];
            [gated_encoder setBytes:&activation_params length:sizeof(activation_params) atIndex:3];
            [gated_encoder dispatchThreads:MTLSizeMake(rows * intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->activation_multiply_pipeline, rows * intermediate_size), 1, 1)];
            [gated_encoder endEncoding];
        }

        id<MTLBuffer> down_input_buffer = gated_buffer;
        BOOL down_projection_done = NO;
        if (post_gate_rms_norm_slot != SIZE_MAX) {
            if (rows == 1 && ffn_scalar_or_hidden_buffer != nil && runtime->rms_inv_scale_pipeline != nil && runtime->q8_0_rms_scale_mmv_pipeline != nil) {
                const int scale_status = termite_metal_encode_rms_inv_scale_1x(
                    runtime,
                    command_buffer,
                    gated_buffer,
                    0,
                    ffn_scalar_or_hidden_buffer,
                    0,
                    intermediate_size,
                    eps,
                    -19);
                const int down_status = scale_status == 0 ? termite_metal_encode_q8_0_linear_rms_scale_1x(
                    runtime,
                    command_buffer,
                    gated_buffer,
                    0,
                    post_gate_rms_norm_slot,
                    ffn_scalar_or_hidden_buffer,
                    0,
                    down_view.weight_buffer,
                    projected_buffer,
                    0,
                    intermediate_size,
                    hidden_size,
                    -20) : -20;
                down_projection_done = (scale_status == 0 && down_status == 0);
            }
            if (!down_projection_done) {
                BOOL rms_encoder_owned = YES;
                id<MTLComputeCommandEncoder> rms_encoder = termite_metal_scoped_compute_encoder_for(runtime, command_buffer, TERMITE_METAL_COMPUTE_SOURCE_RMS_NORM, &rms_encoder_owned);
                if (rms_encoder == nil) return -19;
                if (termite_metal_decode_runtime_prepare_planned_compute_norm_accesses(
                        runtime,
                        gated_buffer,
                        0,
                        intermediate_bytes,
                        runtime->rms_norm_weight_buffers[post_gate_rms_norm_slot],
                        normed_gated_buffer,
                        0,
                        intermediate_bytes,
                        -19) != 0) return -19;
                [rms_encoder setComputePipelineState:runtime->rms_norm_rows_pipeline];
                [rms_encoder setBuffer:gated_buffer offset:0 atIndex:0];
                [rms_encoder setBuffer:runtime->rms_norm_weight_buffers[post_gate_rms_norm_slot] offset:0 atIndex:1];
                [rms_encoder setBuffer:normed_gated_buffer offset:0 atIndex:2];
                [rms_encoder setBytes:&rms_params length:sizeof(rms_params) atIndex:3];
                [rms_encoder dispatchThreads:MTLSizeMake(rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->rms_norm_rows_pipeline, rows), 1, 1)];
                termite_metal_end_scoped_compute_encoder(rms_encoder, rms_encoder_owned);
                down_input_buffer = normed_gated_buffer;
            }
        }

        if (!down_projection_done && post_down_rms_norm_slot != SIZE_MAX && rows == 1 && runtime->active_frame_cb == command_buffer) {
            const int planned_status = termite_metal_encode_q8_0_linear_rms_add_planned_1x(
                runtime,
                command_buffer,
                down_input_buffer,
                0,
                down_view.weight_buffer,
                projected_buffer,
                0,
                intermediate_size,
                hidden_size,
                post_down_rms_norm_slot,
                residual_buffer,
                residual_offset,
                output_buffer,
                output_offset,
                eps,
                TERMITE_METAL_COMPUTE_SOURCE_FFN,
                TERMITE_METAL_COMPUTE_REGION_FFN,
                -20);
            if (planned_status != 0) return planned_status;
            return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -23);
        }

        if (!down_projection_done) {
            termite_metal_decode_runtime_record_contract_or_logical_command_op(
                runtime,
                planned_contract,
                TERMITE_METAL_PLAN_OP_DECODE_FFN_DOWN_LINEAR,
                TERMITE_METAL_OPERATOR_MUL_MM,
                TERMITE_METAL_QUANT_MATMUL_DISPATCH_MM);
        }
        if (!down_projection_done) {
            termite_metal_runtime_command_op down_op;
            const bool has_planned_down = termite_metal_planned_layer_contract_find_command_op(
                planned_contract,
                TERMITE_METAL_PLAN_OP_DECODE_FFN_DOWN_LINEAR,
                &down_op);
            termite_metal_quant_matmul_descriptor descriptor = {
                .slot = down_linear_slot,
                .format = TERMITE_METAL_QUANT_FORMAT_Q8_0,
                .activation_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
                .input_dtype = (has_planned_down && post_gate_rms_norm_slot == SIZE_MAX && !pre_gate_pair_done) ? termite_metal_quant_matmul_dtype_from_command(down_op.input_dtype) : TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
                .output_dtype = has_planned_down ? termite_metal_quant_matmul_dtype_from_command(down_op.output_dtype) : TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32,
                .epilogue = TERMITE_METAL_QUANT_MATMUL_EPILOGUE_NONE,
                .fallback_pipeline = runtime->q8_0_pipeline,
                .input_buffer = down_input_buffer,
                .weight_buffer = down_view.weight_buffer,
                .output_buffer = projected_buffer,
                .rows = rows,
                .in_dim = intermediate_size,
                .out_dim = hidden_size,
                .values_per_block = 32u,
                .bytes_per_block = 34u,
                .q8_family = TERMITE_METAL_Q8_0_LINEAR_FAMILY_FFN_DOWN,
                .use_planned_dispatch = has_planned_down,
                .planned_dispatch = has_planned_down ? down_op.quant_dispatch : TERMITE_METAL_QUANT_MATMUL_DISPATCH_UNKNOWN,
                .failure_code = -20,
            };
            int down_status = termite_metal_encode_quant_matmul_descriptor(runtime, command_buffer, &descriptor);
            if (down_status != 0 && has_planned_down &&
                (down_op.input_dtype != TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32 ||
                    down_op.output_dtype != TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32))
            {
                descriptor.input_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32;
                descriptor.output_dtype = TERMITE_METAL_QUANT_MATMUL_ACTIVATION_F32;
                descriptor.use_planned_dispatch = false;
                descriptor.planned_dispatch = TERMITE_METAL_QUANT_MATMUL_DISPATCH_UNKNOWN;
                down_status = termite_metal_encode_quant_matmul_descriptor(runtime, command_buffer, &descriptor);
            }
            if (down_status != 0) return -20;
        }

        if (post_down_rms_norm_slot != SIZE_MAX && rows == 1) {
            termite_metal_decode_runtime_record_logical_command_op(
                runtime,
                TERMITE_METAL_PLAN_OP_DECODE_FFN_POST_NORM_RESIDUAL,
                TERMITE_METAL_OPERATOR_UNKNOWN,
                TERMITE_METAL_QUANT_MATMUL_DISPATCH_UNKNOWN);
            if (termite_metal_encode_rms_norm_add(
                    runtime,
                    command_buffer,
                    projected_buffer,
                    0,
                    post_down_rms_norm_slot,
                    residual_buffer,
                    residual_offset,
                    output_buffer,
                    output_offset,
                    hidden_size,
                    eps,
                    -21) != 0) return -21;
            return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -23);
        }

        if (post_down_rms_norm_slot != SIZE_MAX) {
            termite_metal_decode_runtime_record_logical_command_op(
                runtime,
                TERMITE_METAL_PLAN_OP_DECODE_FFN_POST_NORM_RESIDUAL,
                TERMITE_METAL_OPERATOR_UNKNOWN,
                TERMITE_METAL_QUANT_MATMUL_DISPATCH_UNKNOWN);
            if (termite_metal_encode_rms_norm_add_rows_for(
                    runtime,
                    command_buffer,
                    projected_buffer,
                    0,
                    post_down_rms_norm_slot,
                    residual_buffer,
                    residual_offset,
                    output_buffer,
                    output_offset,
                    rows,
                    hidden_size,
                    eps,
                    TERMITE_METAL_COMPUTE_SOURCE_FFN,
                    -21) != 0) return -21;
            return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -23);
        }

        id<MTLBuffer> add_lhs_buffer = projected_buffer;
        BOOL add_encoder_owned = YES;
        id<MTLComputeCommandEncoder> add_encoder = termite_metal_scoped_compute_encoder_for(runtime, command_buffer, TERMITE_METAL_COMPUTE_SOURCE_FFN, &add_encoder_owned);
        if (add_encoder == nil) return -22;
        if (termite_metal_decode_runtime_prepare_planned_compute_binary_accesses(
                runtime,
                add_lhs_buffer,
                0,
                hidden_bytes,
                residual_buffer,
                residual_offset,
                hidden_bytes,
                output_buffer,
                output_offset,
                hidden_bytes,
                -22) != 0) return -22;
        [add_encoder setComputePipelineState:runtime->add_pipeline];
        [add_encoder setBuffer:add_lhs_buffer offset:0 atIndex:0];
        [add_encoder setBuffer:residual_buffer offset:residual_offset atIndex:1];
        [add_encoder setBuffer:output_buffer offset:output_offset atIndex:2];
        [add_encoder setBytes:&add_params length:sizeof(add_params) atIndex:3];
        [add_encoder dispatchThreads:MTLSizeMake(rows * hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->add_pipeline, rows * hidden_size), 1, 1)];
        termite_metal_end_scoped_compute_encoder(add_encoder, add_encoder_owned);

        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -23);
    }
}

int termite_metal_decode_runtime_apply_gated_ffn_residual_q8_0_slots_device(
    termite_metal_decode_runtime *runtime,
    void *input_handle,
    size_t input_offset,
    void *residual_handle,
    size_t residual_offset,
    size_t rows,
    size_t hidden_size,
    size_t intermediate_size,
    uint32_t activation_kind,
    size_t gate_linear_slot,
    size_t up_linear_slot,
    size_t post_gate_rms_norm_slot,
    size_t post_down_rms_norm_slot,
    float eps,
    size_t down_linear_slot,
    void *output_handle,
    size_t output_offset
) {
    return termite_metal_decode_runtime_apply_gated_ffn_residual_q8_0_slots_device_impl(
        runtime,
        input_handle,
        input_offset,
        residual_handle,
        residual_offset,
        rows,
        hidden_size,
        intermediate_size,
        activation_kind,
        gate_linear_slot,
        up_linear_slot,
        SIZE_MAX,
        post_gate_rms_norm_slot,
        post_down_rms_norm_slot,
        eps,
        down_linear_slot,
        output_handle,
        output_offset,
        (termite_metal_planned_layer_contract){0}
    );
}

int termite_metal_decode_runtime_apply_gated_ffn_residual_q8_0(
    termite_metal_decode_runtime *runtime,
    const float *input,
    const float *residual,
    size_t rows,
    size_t hidden_size,
    size_t intermediate_size,
    uint32_t activation_kind,
    const uint8_t *gate_weight_raw,
    size_t gate_weight_bytes,
    const uint8_t *up_weight_raw,
    size_t up_weight_bytes,
    size_t post_gate_rms_norm_slot,
    size_t post_down_rms_norm_slot,
    const uint8_t *down_weight_raw,
    size_t down_weight_bytes,
    float *output
) {
    if (runtime == NULL || input == NULL || residual == NULL || gate_weight_raw == NULL || up_weight_raw == NULL || down_weight_raw == NULL || output == NULL) return -1;
    if (runtime->q8_0_pipeline == nil || runtime->q8_0_pair_pipeline == nil || runtime->rms_norm_rows_pipeline == nil || runtime->activation_multiply_pipeline == nil || runtime->add_pipeline == nil) return -2;
    if (rows == 0 || hidden_size == 0 || intermediate_size == 0 || rows > UINT32_MAX || hidden_size > UINT32_MAX || intermediate_size > UINT32_MAX) return -3;
    if (hidden_size % 32u != 0 || intermediate_size % 32u != 0) return -4;
    const size_t gate_row_blocks = hidden_size / 32u;
    const size_t down_row_blocks = intermediate_size / 32u;
    if (gate_weight_bytes < intermediate_size * gate_row_blocks * 34u) return -5;
    if (up_weight_bytes < intermediate_size * gate_row_blocks * 34u) return -6;
    if (down_weight_bytes < hidden_size * down_row_blocks * 34u) return -7;
    if (post_gate_rms_norm_slot != SIZE_MAX) {
        if (post_gate_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return -8;
        if (runtime->rms_norm_slot_prepared[post_gate_rms_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[post_gate_rms_norm_slot] != intermediate_size) return -9;
    }
    if (post_down_rms_norm_slot != SIZE_MAX) {
        if (post_down_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return -10;
        if (runtime->rms_norm_slot_prepared[post_down_rms_norm_slot] == 0 || runtime->rms_norm_hidden_sizes[post_down_rms_norm_slot] != hidden_size) return -11;
    }
    @autoreleasepool {
        const size_t hidden_bytes = rows * hidden_size * sizeof(float);
        const size_t intermediate_bytes = rows * intermediate_size * sizeof(float);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)input length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:input length:hidden_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> residual_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)residual length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (residual_buffer == nil) residual_buffer = [runtime->device newBufferWithBytes:residual length:hidden_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> gate_weight_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)gate_weight_raw length:gate_weight_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (gate_weight_buffer == nil) gate_weight_buffer = [runtime->device newBufferWithBytes:gate_weight_raw length:gate_weight_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> up_weight_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)up_weight_raw length:up_weight_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (up_weight_buffer == nil) up_weight_buffer = [runtime->device newBufferWithBytes:up_weight_raw length:up_weight_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> down_weight_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)down_weight_raw length:down_weight_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (down_weight_buffer == nil) down_weight_buffer = [runtime->device newBufferWithBytes:down_weight_raw length:down_weight_bytes options:MTLResourceStorageModeShared];
        if (termite_metal_decode_runtime_ensure_hot_buffers(runtime, hidden_bytes, intermediate_bytes) != 0) return -12;
        id<MTLBuffer> gate_output_buffer = runtime->hot_intermediate_buffers[0];
        id<MTLBuffer> up_output_buffer = runtime->hot_intermediate_buffers[1];
        id<MTLBuffer> activated_buffer = runtime->hot_intermediate_buffers[2];
        id<MTLBuffer> gated_buffer = runtime->hot_intermediate_buffers[3];
        id<MTLBuffer> normed_gated_buffer = post_gate_rms_norm_slot != SIZE_MAX ? runtime->hot_intermediate_buffers[4] : nil;
        id<MTLBuffer> projected_buffer = runtime->hot_hidden_buffers[0];
        id<MTLBuffer> post_down_buffer = post_down_rms_norm_slot != SIZE_MAX ? runtime->hot_hidden_buffers[1] : nil;
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithBytesNoCopy:output length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) output_buffer = [runtime->device newBufferWithBytes:output length:hidden_bytes options:MTLResourceStorageModeShared];
        if (input_buffer == nil || residual_buffer == nil || gate_weight_buffer == nil || up_weight_buffer == nil || down_weight_buffer == nil || gate_output_buffer == nil || up_output_buffer == nil || activated_buffer == nil || gated_buffer == nil || projected_buffer == nil || output_buffer == nil || (post_gate_rms_norm_slot != SIZE_MAX && normed_gated_buffer == nil) || (post_down_rms_norm_slot != SIZE_MAX && post_down_buffer == nil)) return -13;

        termite_metal_apply_activation_params activation_params = { .activation_kind = activation_kind, .rows = (uint32_t)rows, .dim = (uint32_t)intermediate_size };
        termite_metal_apply_add_params multiply_params = { .rows = (uint32_t)rows, .dim = (uint32_t)intermediate_size };
        termite_metal_apply_row_norm_params rms_params = { .rows = (uint32_t)rows, .hidden_size = (uint32_t)intermediate_size, .eps = 0.0f };
        termite_metal_apply_row_norm_params post_down_rms_params = { .rows = (uint32_t)rows, .hidden_size = (uint32_t)hidden_size, .eps = 0.0f };
        termite_metal_apply_add_params add_params = { .rows = (uint32_t)rows, .dim = (uint32_t)hidden_size };
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -14;

        const bool split_gate_up_linear = (rows >= 9u && runtime->q8_0_pair_activation_mm_pipeline == nil && runtime->q8_0_mm_pipeline != nil);
        const bool fused_gate_up = !split_gate_up_linear && termite_metal_encode_q8_0_pair_activation_multiply(
            runtime,
            command_buffer,
            input_buffer,
            0,
            gate_weight_buffer,
            up_weight_buffer,
            gated_buffer,
            0,
            rows,
            hidden_size,
            intermediate_size,
            activation_kind,
            -15) == 0;
        if (!fused_gate_up) {
            if (split_gate_up_linear) {
                if (termite_metal_encode_q8_0_linear(
                        runtime,
                        command_buffer,
                        input_buffer,
                        0,
                        gate_weight_buffer,
                        gate_output_buffer,
                        0,
                        rows,
                        hidden_size,
                        intermediate_size,
                        -15) != 0) return -15;
                if (termite_metal_encode_q8_0_linear(
                        runtime,
                        command_buffer,
                        input_buffer,
                        0,
                        up_weight_buffer,
                        up_output_buffer,
                        0,
                        rows,
                        hidden_size,
                        intermediate_size,
                        -15) != 0) return -15;
            } else {
                if (termite_metal_encode_q8_0_pair_linear(
                        runtime,
                        command_buffer,
                        input_buffer,
                        0,
                        gate_weight_buffer,
                        up_weight_buffer,
                        gate_output_buffer,
                        0,
                        up_output_buffer,
                        0,
                        rows,
                        hidden_size,
                        intermediate_size,
                        -15) != 0) return -15;
            }

            id<MTLComputeCommandEncoder> gated_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
            if (gated_encoder == nil) return -16;
            [gated_encoder setComputePipelineState:runtime->activation_multiply_pipeline];
            [gated_encoder setBuffer:gate_output_buffer offset:0 atIndex:0];
            [gated_encoder setBuffer:up_output_buffer offset:0 atIndex:1];
            [gated_encoder setBuffer:gated_buffer offset:0 atIndex:2];
            [gated_encoder setBytes:&activation_params length:sizeof(activation_params) atIndex:3];
            [gated_encoder dispatchThreads:MTLSizeMake(rows * intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->activation_multiply_pipeline, rows * intermediate_size), 1, 1)];
            [gated_encoder endEncoding];
        }

        id<MTLBuffer> down_input_buffer = gated_buffer;
        if (post_gate_rms_norm_slot != SIZE_MAX) {
            id<MTLComputeCommandEncoder> rms_encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_RMS_NORM);
            if (rms_encoder == nil) return -18;
            [rms_encoder setComputePipelineState:runtime->rms_norm_rows_pipeline];
            [rms_encoder setBuffer:gated_buffer offset:0 atIndex:0];
            [rms_encoder setBuffer:runtime->rms_norm_weight_buffers[post_gate_rms_norm_slot] offset:0 atIndex:1];
            [rms_encoder setBuffer:normed_gated_buffer offset:0 atIndex:2];
            [rms_encoder setBytes:&rms_params length:sizeof(rms_params) atIndex:3];
            [rms_encoder dispatchThreads:MTLSizeMake(rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->rms_norm_rows_pipeline, rows), 1, 1)];
            [rms_encoder endEncoding];
            down_input_buffer = normed_gated_buffer;
        }

        if (termite_metal_encode_q8_0_linear(
                runtime,
                command_buffer,
                down_input_buffer,
                0,
                down_weight_buffer,
                projected_buffer,
                0,
                rows,
                intermediate_size,
                hidden_size,
                -19) != 0) return -19;

        id<MTLBuffer> add_lhs_buffer = projected_buffer;
        if (post_down_rms_norm_slot != SIZE_MAX) {
            id<MTLComputeCommandEncoder> post_down_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
            if (post_down_encoder == nil) return -20;
            [post_down_encoder setComputePipelineState:runtime->rms_norm_rows_pipeline];
            [post_down_encoder setBuffer:projected_buffer offset:0 atIndex:0];
            [post_down_encoder setBuffer:runtime->rms_norm_weight_buffers[post_down_rms_norm_slot] offset:0 atIndex:1];
            [post_down_encoder setBuffer:post_down_buffer offset:0 atIndex:2];
            [post_down_encoder setBytes:&post_down_rms_params length:sizeof(post_down_rms_params) atIndex:3];
            [post_down_encoder dispatchThreads:MTLSizeMake(rows, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->rms_norm_rows_pipeline, rows), 1, 1)];
            [post_down_encoder endEncoding];
            add_lhs_buffer = post_down_buffer;
        }

        id<MTLComputeCommandEncoder> add_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (add_encoder == nil) return -21;
        [add_encoder setComputePipelineState:runtime->add_pipeline];
        [add_encoder setBuffer:add_lhs_buffer offset:0 atIndex:0];
        [add_encoder setBuffer:residual_buffer offset:0 atIndex:1];
        [add_encoder setBuffer:output_buffer offset:0 atIndex:2];
        [add_encoder setBytes:&add_params length:sizeof(add_params) atIndex:3];
        [add_encoder dispatchThreads:MTLSizeMake(rows * hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->add_pipeline, rows * hidden_size), 1, 1)];
        [add_encoder endEncoding];

        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -22;
    }
}

int termite_metal_decode_runtime_update_attention_span(
    termite_metal_decode_runtime *runtime,
    const uint8_t *encoded_key,
    const float *v,
    size_t kv_tokens,
    size_t key_row_bytes,
    size_t v_row_stride,
    size_t position_offset
) {
    if (runtime == NULL || encoded_key == NULL || v == NULL) return -1;
    if (kv_tokens == 0 || key_row_bytes == 0 || v_row_stride == 0) return -2;
    @autoreleasepool {
        const size_t encoded_bytes = kv_tokens * key_row_bytes;
        const size_t v_bytes = kv_tokens * v_row_stride * sizeof(float);
        if (termite_metal_decode_runtime_ensure_attention_span_slot_buffers(runtime, 0, encoded_bytes, v_bytes) != 0) return -3;
        if (termite_metal_upload_private_buffer(runtime->device, runtime->queue, runtime->attention_span_encoded_key_buffer, encoded_key, encoded_bytes) != 0) return -5;
        if (termite_metal_upload_private_buffer(runtime->device, runtime->queue, runtime->attention_span_v_buffer, v, v_bytes) != 0) return -6;
        runtime->attention_span_tokens = kv_tokens;
        runtime->attention_span_key_row_bytes = key_row_bytes;
        runtime->attention_span_v_row_stride = v_row_stride;
        runtime->attention_span_position_offset = position_offset;
        runtime->attention_span_encoded_key_buffers[0] = runtime->attention_span_encoded_key_buffer;
        runtime->attention_span_v_buffers[0] = runtime->attention_span_v_buffer;
        runtime->attention_span_encoded_key_capacities[0] = runtime->attention_span_encoded_key_capacity;
        runtime->attention_span_v_capacities[0] = runtime->attention_span_v_capacity;
        runtime->attention_span_slot_tokens[0] = kv_tokens;
        runtime->attention_span_slot_key_row_bytes[0] = key_row_bytes;
        runtime->attention_span_slot_v_row_stride[0] = v_row_stride;
        runtime->attention_span_slot_position_offset[0] = position_offset;
        return 0;
    }
}

int termite_metal_decode_runtime_update_attention_span_from_f32_key_device(
    termite_metal_decode_runtime *runtime,
    uint32_t format,
    void *k_handle,
    size_t k_offset,
    void *v_handle,
    size_t v_offset,
    size_t kv_tokens,
    size_t num_kv_heads,
    size_t head_dim,
    size_t key_row_bytes,
    size_t base_key_row_bytes,
    size_t v_row_stride,
    size_t kv_position_offset
) {
    if (runtime == NULL || k_handle == NULL || v_handle == NULL) return -1;
    @autoreleasepool {
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -2;
        int rc = termite_metal_decode_runtime_encode_replace_attention_span_from_f32_key_device_slot(
            runtime,
            command_buffer,
            0,
            format,
            k_handle,
            k_offset,
            v_handle,
            v_offset,
            kv_tokens,
            num_kv_heads,
            head_dim,
            key_row_bytes,
            base_key_row_bytes,
            v_row_stride,
            kv_position_offset
        );
        if (rc == 0) {
            [command_buffer commit];
            [command_buffer waitUntilCompleted];
            rc = command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -3;
        }
        return rc;
    }
}

/// Slot-addressable variant of `update_attention_span_from_f32_key_device`.
/// Encodes `kv_tokens`-worth of device-resident f32 KV into the slot's
/// attention span buffers, doing incremental append when slot state matches
/// (same key_row_bytes, v_row_stride, position_offset, and tokens ≤ kv_tokens).
/// Owns the command buffer + commit/wait; caller only hands in device handles.
///
/// Phase 6 KV device write path: this is the kernel MetalKvStorage calls
/// instead of materializing device bytes on the host for the KvPool.
int termite_metal_decode_runtime_update_attention_span_from_f32_key_device_slot(
    termite_metal_decode_runtime *runtime,
    size_t slot,
    uint32_t format,
    void *k_handle,
    size_t k_offset,
    void *v_handle,
    size_t v_offset,
    size_t kv_tokens,
    size_t num_kv_heads,
    size_t head_dim,
    size_t key_row_bytes,
    size_t base_key_row_bytes,
    size_t v_row_stride,
    size_t kv_position_offset
) {
    if (runtime == NULL || k_handle == NULL || v_handle == NULL) return -1;
    if (slot >= TERMITE_METAL_ATTENTION_SPAN_SLOT_CAPACITY) return -2;
    @autoreleasepool {
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -3;
        int rc = termite_metal_decode_runtime_encode_replace_attention_span_from_f32_key_device_slot(
            runtime,
            command_buffer,
            slot,
            format,
            k_handle,
            k_offset,
            v_handle,
            v_offset,
            kv_tokens,
            num_kv_heads,
            head_dim,
            key_row_bytes,
            base_key_row_bytes,
            v_row_stride,
            kv_position_offset
        );
        if (rc == 0) {
            [command_buffer commit];
            [command_buffer waitUntilCompleted];
            rc = command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -4;
        }
        return rc;
    }
}

static int termite_metal_decode_runtime_encode_attention_paged_update_from_f32_key_device_slot(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    size_t slot,
    uint32_t format,
    void *k_handle,
    size_t k_offset,
    void *v_handle,
    size_t v_offset,
    size_t total_tokens,
    size_t suffix_tokens,
    size_t num_kv_heads,
    size_t head_dim,
    size_t key_row_bytes,
    size_t base_key_row_bytes,
    size_t v_row_stride,
    size_t kv_position_offset,
    const uint32_t *block_table,
    size_t block_count,
    size_t page_size
) {
    if (runtime == NULL || command_buffer == nil || k_handle == NULL || v_handle == NULL || block_table == NULL) return -1;
    if (slot >= TERMITE_METAL_ATTENTION_SPAN_SLOT_CAPACITY) return -2;
    if (total_tokens == 0 || suffix_tokens == 0) return suffix_tokens == 0 ? 0 : -3;
    if (suffix_tokens > total_tokens || num_kv_heads == 0 || head_dim == 0 || key_row_bytes == 0 || base_key_row_bytes == 0 || v_row_stride == 0) return -4;
    if (block_count == 0 || page_size == 0) return -5;
    if (total_tokens > UINT32_MAX || suffix_tokens > UINT32_MAX || num_kv_heads > UINT32_MAX || head_dim > UINT32_MAX || key_row_bytes > UINT32_MAX || base_key_row_bytes > UINT32_MAX || page_size > UINT32_MAX) return -6;
    switch (format) {
        case 0u:
        case 1u:
        case 2u:
        case 3u:
        case 4u:
            break;
        default:
            return -7;
    }

    const size_t last_logical_block = (total_tokens - 1u) / page_size;
    if (last_logical_block >= block_count) return -8;
    size_t capacity_tokens = 0;
    for (size_t i = 0; i < block_count; i++) {
        const size_t block_start = (size_t)block_table[i];
        if (block_start > SIZE_MAX - page_size) return -9;
        const size_t block_end = block_start + page_size;
        if (block_end > capacity_tokens) capacity_tokens = block_end;
    }
    if (capacity_tokens == 0) return -10;
    const size_t encoded_bytes = capacity_tokens * key_row_bytes;
    const size_t v_element_bytes = (format == 3u) ? sizeof(uint16_t) : sizeof(float);
    const size_t v_bytes = capacity_tokens * v_row_stride * v_element_bytes;
    if (termite_metal_decode_runtime_ensure_attention_span_slot_buffers(runtime, slot, encoded_bytes, v_bytes) != 0) return -11;

    @autoreleasepool {
        id<MTLBuffer> k_buffer = (__bridge id<MTLBuffer>)k_handle;
        id<MTLBuffer> v_buffer = (__bridge id<MTLBuffer>)v_handle;
        if (k_buffer == nil || v_buffer == nil) return -12;

        const size_t values_per_row = num_kv_heads * head_dim;
        const size_t suffix_start = total_tokens - suffix_tokens;
        const size_t raw_source_row_bytes = values_per_row * sizeof(float);
        const size_t raw_v_source_row_bytes = v_row_stride * sizeof(float);

        if (format == 2u) {
            BOOL encoder_owned = YES;
            id<MTLComputeCommandEncoder> encoder = termite_metal_scoped_compute_encoder_for(runtime, command_buffer, TERMITE_METAL_COMPUTE_SOURCE_ATTENTION, &encoder_owned);
            if (encoder == nil) return -14;
            const int rc = termite_metal_encode_paged_f32_kv_seed_on_encoder(
                runtime,
                encoder,
                slot,
                k_buffer,
                k_offset,
                v_buffer,
                v_offset,
                total_tokens,
                suffix_tokens,
                num_kv_heads,
                head_dim,
                key_row_bytes,
                v_row_stride,
                kv_position_offset,
                block_table,
                block_count,
                page_size,
                -14);
            termite_metal_end_scoped_compute_encoder(encoder, encoder_owned);
            if (rc != 0) return rc;
        } else if (format == 3u) {
            id<MTLComputePipelineState> pipeline = runtime->encode_f16_convert_pipeline;
            if (pipeline == nil) return -15;
            termite_metal_encode_key_params params = {
                .rows = 1u,
                .num_kv_heads = (uint32_t)num_kv_heads,
                .head_dim = (uint32_t)head_dim,
                .key_row_bytes = (uint32_t)key_row_bytes,
                .base_key_row_bytes = (uint32_t)base_key_row_bytes,
            };
            id<MTLBuffer> params_buffer = [runtime->device newBufferWithBytes:&params length:sizeof(params) options:MTLResourceStorageModeShared];
            if (params_buffer == nil) return -16;
            const size_t tg_width = termite_metal_thread_width(pipeline, values_per_row);
            for (size_t logical = suffix_start; logical < total_tokens; logical++) {
                const size_t logical_block = logical / page_size;
                const size_t token_in_block = logical - logical_block * page_size;
                const size_t physical_token = (size_t)block_table[logical_block] + token_in_block;
                const size_t source_row = logical - suffix_start;
                id<MTLComputeCommandEncoder> k_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
                if (k_encoder == nil) return -17;
                [k_encoder setComputePipelineState:pipeline];
                [k_encoder setBuffer:k_buffer offset:(k_offset + source_row * raw_source_row_bytes) atIndex:0];
                [k_encoder setBuffer:runtime->attention_span_encoded_key_buffers[slot] offset:(physical_token * key_row_bytes) atIndex:1];
                [k_encoder setBuffer:params_buffer offset:0 atIndex:2];
                [k_encoder dispatchThreads:MTLSizeMake(values_per_row, 1, 1) threadsPerThreadgroup:MTLSizeMake(tg_width, 1, 1)];
                [k_encoder endEncoding];

                id<MTLComputeCommandEncoder> v_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
                if (v_encoder == nil) return -18;
                [v_encoder setComputePipelineState:pipeline];
                [v_encoder setBuffer:v_buffer offset:(v_offset + source_row * raw_v_source_row_bytes) atIndex:0];
                [v_encoder setBuffer:runtime->attention_span_v_buffers[slot] offset:(physical_token * v_row_stride * sizeof(uint16_t)) atIndex:1];
                [v_encoder setBuffer:params_buffer offset:0 atIndex:2];
                [v_encoder dispatchThreads:MTLSizeMake(values_per_row, 1, 1) threadsPerThreadgroup:MTLSizeMake(tg_width, 1, 1)];
                [v_encoder endEncoding];
            }
        } else {
            id<MTLComputePipelineState> pipeline = nil;
            switch (format) {
                case 0u: pipeline = runtime->encode_polar4_key_pipeline; break;
                case 1u: pipeline = runtime->encode_turbo3_key_pipeline; break;
                case 4u: pipeline = runtime->encode_int8_per_head_pipeline; break;
                default: return -19;
            }
            if (pipeline == nil) return -20;
            termite_metal_encode_key_params params = {
                .rows = 1u,
                .num_kv_heads = (uint32_t)num_kv_heads,
                .head_dim = (uint32_t)head_dim,
                .key_row_bytes = (uint32_t)key_row_bytes,
                .base_key_row_bytes = (uint32_t)base_key_row_bytes,
            };
            id<MTLBuffer> params_buffer = [runtime->device newBufferWithBytes:&params length:sizeof(params) options:MTLResourceStorageModeShared];
            if (params_buffer == nil) return -21;
            size_t int8_tg_size = 1u;
            while (int8_tg_size < head_dim && int8_tg_size < 256u) int8_tg_size <<= 1u;
            if (format == 4u && int8_tg_size < head_dim) return -22;
            const size_t residual_bytes = (num_kv_heads * 32u + 7u) / 8u;
            const size_t compressed_work_items = format == 0u ? base_key_row_bytes : (base_key_row_bytes + residual_bytes);
            for (size_t logical = suffix_start; logical < total_tokens; logical++) {
                const size_t logical_block = logical / page_size;
                const size_t token_in_block = logical - logical_block * page_size;
                const size_t physical_token = (size_t)block_table[logical_block] + token_in_block;
                const size_t source_row = logical - suffix_start;
                id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
                if (encoder == nil) return -23;
                [encoder setComputePipelineState:pipeline];
                [encoder setBuffer:k_buffer offset:(k_offset + source_row * raw_source_row_bytes) atIndex:0];
                [encoder setBuffer:runtime->attention_span_encoded_key_buffers[slot] offset:(physical_token * key_row_bytes) atIndex:1];
                [encoder setBuffer:params_buffer offset:0 atIndex:2];
                if (format == 4u) {
                    [encoder dispatchThreads:MTLSizeMake(int8_tg_size, num_kv_heads, 1)
                          threadsPerThreadgroup:MTLSizeMake(int8_tg_size, 1, 1)];
                } else {
                    [encoder dispatchThreads:MTLSizeMake(compressed_work_items, 1, 1)
                          threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(pipeline, compressed_work_items), 1, 1)];
                }
                [encoder endEncoding];
            }

            termite_metal_record_active_frame_blit_source(command_buffer, TERMITE_METAL_BLIT_SOURCE_ATTENTION_SPAN);
            id<MTLBlitCommandEncoder> blit = termite_metal_tracked_blit_command_encoder(command_buffer);
            if (blit == nil) return -24;
            for (size_t logical = suffix_start; logical < total_tokens; logical++) {
                const size_t logical_block = logical / page_size;
                const size_t token_in_block = logical - logical_block * page_size;
                const size_t physical_token = (size_t)block_table[logical_block] + token_in_block;
                const size_t source_row = logical - suffix_start;
                [blit copyFromBuffer:v_buffer
                        sourceOffset:(v_offset + source_row * raw_v_source_row_bytes)
                            toBuffer:runtime->attention_span_v_buffers[slot]
                   destinationOffset:(physical_token * v_row_stride * sizeof(float))
                                size:raw_v_source_row_bytes];
            }
            [blit endEncoding];
        }

        runtime->attention_span_slot_tokens[slot] = capacity_tokens;
        runtime->attention_span_slot_key_row_bytes[slot] = key_row_bytes;
        runtime->attention_span_slot_v_row_stride[slot] = v_row_stride;
        runtime->attention_span_slot_position_offset[slot] = kv_position_offset;
        if (slot == 0) {
            runtime->attention_span_encoded_key_buffer = runtime->attention_span_encoded_key_buffers[slot];
            runtime->attention_span_v_buffer = runtime->attention_span_v_buffers[slot];
            runtime->attention_span_encoded_key_capacity = runtime->attention_span_encoded_key_capacities[slot];
            runtime->attention_span_v_capacity = runtime->attention_span_v_capacities[slot];
            runtime->attention_span_tokens = capacity_tokens;
            runtime->attention_span_key_row_bytes = key_row_bytes;
            runtime->attention_span_v_row_stride = v_row_stride;
            runtime->attention_span_position_offset = kv_position_offset;
        }

        return 0;
    }
}

static int termite_metal_encode_paged_quantized_kv_seed_on_encoder(
    termite_metal_decode_runtime *runtime,
    id<MTLComputeCommandEncoder> encoder,
    size_t slot,
    uint32_t format,
    id<MTLBuffer> k_buffer,
    size_t k_offset,
    id<MTLBuffer> v_buffer,
    size_t v_offset,
    size_t total_tokens,
    size_t suffix_tokens,
    size_t num_kv_heads,
    size_t head_dim,
    size_t key_row_bytes,
    size_t base_key_row_bytes,
    size_t v_row_stride,
    size_t kv_position_offset,
    const uint32_t *block_table,
    size_t block_count,
    size_t page_size,
    int failure_code
) {
    if (runtime == NULL || encoder == nil || k_buffer == nil || v_buffer == nil || block_table == NULL) return failure_code;
    if (slot >= TERMITE_METAL_ATTENTION_SPAN_SLOT_CAPACITY) return failure_code;
    if (total_tokens == 0 || suffix_tokens == 0) return suffix_tokens == 0 ? 0 : failure_code;
    if (suffix_tokens > total_tokens || num_kv_heads == 0 || head_dim == 0 || key_row_bytes == 0 || base_key_row_bytes == 0 || v_row_stride == 0) return failure_code;
    if (block_count == 0 || page_size == 0) return failure_code;
    if (total_tokens > UINT32_MAX || suffix_tokens > UINT32_MAX || num_kv_heads > UINT32_MAX || head_dim > UINT32_MAX || key_row_bytes > UINT32_MAX || base_key_row_bytes > UINT32_MAX || page_size > UINT32_MAX) return failure_code;
    if (format != 0u && format != 1u && format != 3u && format != 4u) return failure_code;
    const BOOL f16_kv = format == 3u;
    id<MTLComputePipelineState> key_pipeline = nil;
    if (!f16_kv) {
        switch (format) {
            case 0u: key_pipeline = runtime->encode_polar4_key_pipeline; break;
            case 1u: key_pipeline = runtime->encode_turbo3_key_pipeline; break;
            case 4u: key_pipeline = runtime->encode_int8_per_head_pipeline; break;
            default: break;
        }
        if (key_pipeline == nil || runtime->paged_f32_v_seed_pipeline == nil) return failure_code;
    } else {
        if (runtime->paged_f16_kv_seed_pipeline == nil) return failure_code;
        if (key_row_bytes % sizeof(uint16_t) != 0) return failure_code;
    }

    const size_t last_logical_block = (total_tokens - 1u) / page_size;
    if (last_logical_block >= block_count) return failure_code;
    size_t capacity_tokens = 0;
    for (size_t i = 0; i < block_count; i++) {
        const size_t block_start = (size_t)block_table[i];
        if (block_start > SIZE_MAX - page_size) return failure_code;
        const size_t block_end = block_start + page_size;
        if (block_end > capacity_tokens) capacity_tokens = block_end;
    }
    if (capacity_tokens == 0) return failure_code;
    const size_t encoded_bytes = capacity_tokens * key_row_bytes;
    const size_t v_element_bytes = f16_kv ? sizeof(uint16_t) : sizeof(float);
    const size_t v_bytes = capacity_tokens * v_row_stride * v_element_bytes;
    if (termite_metal_decode_runtime_ensure_attention_span_slot_buffers(runtime, slot, encoded_bytes, v_bytes) != 0) return failure_code;

    const size_t values_per_row = num_kv_heads * head_dim;
    if (values_per_row == 0 || values_per_row > UINT32_MAX || v_row_stride > UINT32_MAX) return failure_code;
    const size_t suffix_start = total_tokens - suffix_tokens;
    const size_t raw_source_row_bytes = values_per_row * sizeof(float);
    const size_t raw_v_source_row_bytes = v_row_stride * sizeof(float);
    if (k_offset > k_buffer.length || suffix_tokens * raw_source_row_bytes > k_buffer.length - k_offset) return failure_code;
    if (v_offset > v_buffer.length || suffix_tokens * raw_v_source_row_bytes > v_buffer.length - v_offset) return failure_code;
    if (block_count > SIZE_MAX / sizeof(uint32_t)) return failure_code;
    const size_t block_table_bytes = block_count * sizeof(uint32_t);
    id<MTLBuffer> block_table_buffer = nil;
    termite_metal_planned_encoder_range accesses[4];
    if (termite_metal_planned_range_make(k_buffer, k_offset, suffix_tokens * raw_source_row_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[0], failure_code) != 0 ||
        termite_metal_planned_range_make(v_buffer, v_offset, suffix_tokens * raw_v_source_row_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[1], failure_code) != 0 ||
        termite_metal_planned_range_make(runtime->attention_span_encoded_key_buffers[slot], 0, runtime->attention_span_encoded_key_buffers[slot].length, TERMITE_METAL_PLANNED_RANGE_WRITE, &accesses[2], failure_code) != 0 ||
        termite_metal_planned_range_make(runtime->attention_span_v_buffers[slot], 0, runtime->attention_span_v_buffers[slot].length, TERMITE_METAL_PLANNED_RANGE_WRITE, &accesses[3], failure_code) != 0 ||
        termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, accesses, 4, failure_code) != 0)
    {
        return failure_code;
    }

    if (f16_kv) {
        const size_t key_row_halfs = key_row_bytes / sizeof(uint16_t);
        if (key_row_halfs < values_per_row || key_row_halfs > UINT32_MAX || v_row_stride < values_per_row) return failure_code;
        termite_metal_paged_f32_kv_seed_params params = {
            .total_tokens = (uint32_t)total_tokens,
            .suffix_tokens = (uint32_t)suffix_tokens,
            .values_per_row = (uint32_t)values_per_row,
            .page_size = (uint32_t)page_size,
            .key_row_floats = (uint32_t)key_row_halfs,
            .v_row_stride = (uint32_t)v_row_stride,
        };
        [encoder setComputePipelineState:runtime->paged_f16_kv_seed_pipeline];
        [encoder setBuffer:k_buffer offset:k_offset atIndex:0];
        [encoder setBuffer:v_buffer offset:v_offset atIndex:1];
        [encoder setBuffer:runtime->attention_span_encoded_key_buffers[slot] offset:0 atIndex:2];
        [encoder setBuffer:runtime->attention_span_v_buffers[slot] offset:0 atIndex:3];
        if (block_table_bytes <= 4096u) {
            [encoder setBytes:block_table length:block_table_bytes atIndex:4];
        } else {
            block_table_buffer = [runtime->device newBufferWithBytes:block_table length:block_table_bytes options:MTLResourceStorageModeShared];
            if (block_table_buffer == nil) return failure_code;
            if (runtime->active_frame_cb != nil && termite_metal_decode_runtime_retain_frame_resource(runtime, block_table_buffer) != 0) return failure_code;
            [encoder setBuffer:block_table_buffer offset:0 atIndex:4];
        }
        [encoder setBytes:&params length:sizeof(params) atIndex:5];
        const size_t total = suffix_tokens * (values_per_row > v_row_stride ? values_per_row : v_row_stride);
        [encoder dispatchThreads:MTLSizeMake(total, 1, 1)
           threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->paged_f16_kv_seed_pipeline, total), 1, 1)];
    } else {

        termite_metal_encode_key_params key_params = {
            .rows = 1u,
            .num_kv_heads = (uint32_t)num_kv_heads,
            .head_dim = (uint32_t)head_dim,
            .key_row_bytes = (uint32_t)key_row_bytes,
            .base_key_row_bytes = (uint32_t)base_key_row_bytes,
        };
        size_t int8_tg_size = 1u;
        while (int8_tg_size < head_dim && int8_tg_size < 256u) int8_tg_size <<= 1u;
        if (format == 4u && int8_tg_size < head_dim) return failure_code;
        const size_t residual_bytes = (num_kv_heads * 32u + 7u) / 8u;
        const size_t compressed_work_items = format == 0u ? base_key_row_bytes : (base_key_row_bytes + residual_bytes);

        for (size_t logical = suffix_start; logical < total_tokens; logical++) {
            const size_t logical_block = logical / page_size;
            const size_t token_in_block = logical - logical_block * page_size;
            const size_t physical_token = (size_t)block_table[logical_block] + token_in_block;
            const size_t source_row = logical - suffix_start;
            [encoder setComputePipelineState:key_pipeline];
            [encoder setBuffer:k_buffer offset:(k_offset + source_row * raw_source_row_bytes) atIndex:0];
            [encoder setBuffer:runtime->attention_span_encoded_key_buffers[slot] offset:(physical_token * key_row_bytes) atIndex:1];
            [encoder setBytes:&key_params length:sizeof(key_params) atIndex:2];
            if (format == 4u) {
                [encoder dispatchThreads:MTLSizeMake(int8_tg_size, num_kv_heads, 1)
                      threadsPerThreadgroup:MTLSizeMake(int8_tg_size, 1, 1)];
            } else {
                [encoder dispatchThreads:MTLSizeMake(compressed_work_items, 1, 1)
                      threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(key_pipeline, compressed_work_items), 1, 1)];
            }
        }

        termite_metal_paged_f32_kv_seed_params v_params = {
            .total_tokens = (uint32_t)total_tokens,
            .suffix_tokens = (uint32_t)suffix_tokens,
            .values_per_row = (uint32_t)values_per_row,
            .page_size = (uint32_t)page_size,
            .key_row_floats = 0u,
            .v_row_stride = (uint32_t)v_row_stride,
        };
        [encoder setComputePipelineState:runtime->paged_f32_v_seed_pipeline];
        [encoder setBuffer:v_buffer offset:v_offset atIndex:0];
        [encoder setBuffer:runtime->attention_span_v_buffers[slot] offset:0 atIndex:1];
        if (block_table_bytes <= 4096u) {
            [encoder setBytes:block_table length:block_table_bytes atIndex:2];
        } else {
            block_table_buffer = [runtime->device newBufferWithBytes:block_table length:block_table_bytes options:MTLResourceStorageModeShared];
            if (block_table_buffer == nil) return failure_code;
            if (runtime->active_frame_cb != nil && termite_metal_decode_runtime_retain_frame_resource(runtime, block_table_buffer) != 0) return failure_code;
            [encoder setBuffer:block_table_buffer offset:0 atIndex:2];
        }
        [encoder setBytes:&v_params length:sizeof(v_params) atIndex:3];
        const size_t v_total = suffix_tokens * v_row_stride;
        [encoder dispatchThreads:MTLSizeMake(v_total, 1, 1)
           threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->paged_f32_v_seed_pipeline, v_total), 1, 1)];
    }

    runtime->attention_span_slot_tokens[slot] = capacity_tokens;
    runtime->attention_span_slot_key_row_bytes[slot] = key_row_bytes;
    runtime->attention_span_slot_v_row_stride[slot] = v_row_stride;
    runtime->attention_span_slot_position_offset[slot] = kv_position_offset;
    if (slot == 0) {
        runtime->attention_span_encoded_key_buffer = runtime->attention_span_encoded_key_buffers[slot];
        runtime->attention_span_v_buffer = runtime->attention_span_v_buffers[slot];
        runtime->attention_span_encoded_key_capacity = runtime->attention_span_encoded_key_capacities[slot];
        runtime->attention_span_v_capacity = runtime->attention_span_v_capacities[slot];
        runtime->attention_span_tokens = capacity_tokens;
        runtime->attention_span_key_row_bytes = key_row_bytes;
        runtime->attention_span_v_row_stride = v_row_stride;
        runtime->attention_span_position_offset = kv_position_offset;
    }
    (void)block_table_buffer;
    return 0;
}

static int termite_metal_encode_paged_f32_kv_seed_on_encoder(
    termite_metal_decode_runtime *runtime,
    id<MTLComputeCommandEncoder> encoder,
    size_t slot,
    id<MTLBuffer> k_buffer,
    size_t k_offset,
    id<MTLBuffer> v_buffer,
    size_t v_offset,
    size_t total_tokens,
    size_t suffix_tokens,
    size_t num_kv_heads,
    size_t head_dim,
    size_t key_row_bytes,
    size_t v_row_stride,
    size_t kv_position_offset,
    const uint32_t *block_table,
    size_t block_count,
    size_t page_size,
    int failure_code
) {
    if (runtime == NULL || encoder == nil || k_buffer == nil || v_buffer == nil || block_table == NULL) return failure_code;
    if (slot >= TERMITE_METAL_ATTENTION_SPAN_SLOT_CAPACITY) return failure_code;
    if (runtime->paged_f32_kv_seed_pipeline == nil) return failure_code;
    if (total_tokens == 0 || suffix_tokens == 0) return suffix_tokens == 0 ? 0 : failure_code;
    if (suffix_tokens > total_tokens || num_kv_heads == 0 || head_dim == 0 || key_row_bytes == 0 || v_row_stride == 0) return failure_code;
    if (block_count == 0 || page_size == 0 || key_row_bytes % sizeof(float) != 0) return failure_code;
    if (total_tokens > UINT32_MAX || suffix_tokens > UINT32_MAX || page_size > UINT32_MAX) return failure_code;
    if (num_kv_heads > UINT32_MAX || head_dim > UINT32_MAX || key_row_bytes > UINT32_MAX || v_row_stride > UINT32_MAX) return failure_code;
    const size_t values_per_row = num_kv_heads * head_dim;
    if (values_per_row == 0 || values_per_row > UINT32_MAX) return failure_code;
    const size_t key_row_floats = key_row_bytes / sizeof(float);
    if (key_row_floats < values_per_row || key_row_floats > UINT32_MAX || v_row_stride < values_per_row) return failure_code;
    if (k_offset % sizeof(float) != 0 || v_offset % sizeof(float) != 0) return failure_code;
    if (block_count > SIZE_MAX / sizeof(uint32_t)) return failure_code;

    const size_t last_logical_block = (total_tokens - 1u) / page_size;
    if (last_logical_block >= block_count) return failure_code;
    size_t capacity_tokens = 0;
    for (size_t i = 0; i < block_count; i++) {
        const size_t block_start = (size_t)block_table[i];
        if (block_start > SIZE_MAX - page_size) return failure_code;
        const size_t block_end = block_start + page_size;
        if (block_end > capacity_tokens) capacity_tokens = block_end;
    }
    if (capacity_tokens == 0) return failure_code;

    const size_t raw_k_source_bytes = suffix_tokens * values_per_row * sizeof(float);
    const size_t raw_v_source_bytes = suffix_tokens * v_row_stride * sizeof(float);
    if (k_offset > k_buffer.length || raw_k_source_bytes > k_buffer.length - k_offset) return failure_code;
    if (v_offset > v_buffer.length || raw_v_source_bytes > v_buffer.length - v_offset) return failure_code;

    const size_t encoded_bytes = capacity_tokens * key_row_bytes;
    const size_t v_bytes = capacity_tokens * v_row_stride * sizeof(float);
    if (termite_metal_decode_runtime_ensure_attention_span_slot_buffers(runtime, slot, encoded_bytes, v_bytes) != 0) return failure_code;
    termite_metal_planned_encoder_range accesses[4];
    if (termite_metal_planned_range_make(k_buffer, k_offset, raw_k_source_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[0], failure_code) != 0 ||
        termite_metal_planned_range_make(v_buffer, v_offset, raw_v_source_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[1], failure_code) != 0 ||
        termite_metal_planned_range_make(runtime->attention_span_encoded_key_buffers[slot], 0, runtime->attention_span_encoded_key_buffers[slot].length, TERMITE_METAL_PLANNED_RANGE_WRITE, &accesses[2], failure_code) != 0 ||
        termite_metal_planned_range_make(runtime->attention_span_v_buffers[slot], 0, runtime->attention_span_v_buffers[slot].length, TERMITE_METAL_PLANNED_RANGE_WRITE, &accesses[3], failure_code) != 0 ||
        termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, accesses, 4, failure_code) != 0)
    {
        return failure_code;
    }

    termite_metal_paged_f32_kv_seed_params params = {
        .total_tokens = (uint32_t)total_tokens,
        .suffix_tokens = (uint32_t)suffix_tokens,
        .values_per_row = (uint32_t)values_per_row,
        .page_size = (uint32_t)page_size,
        .key_row_floats = (uint32_t)key_row_floats,
        .v_row_stride = (uint32_t)v_row_stride,
    };
    const size_t block_table_bytes = block_count * sizeof(uint32_t);
    id<MTLBuffer> block_table_buffer = nil;

    [encoder setComputePipelineState:runtime->paged_f32_kv_seed_pipeline];
    [encoder setBuffer:k_buffer offset:k_offset atIndex:0];
    [encoder setBuffer:v_buffer offset:v_offset atIndex:1];
    [encoder setBuffer:runtime->attention_span_encoded_key_buffers[slot] offset:0 atIndex:2];
    [encoder setBuffer:runtime->attention_span_v_buffers[slot] offset:0 atIndex:3];
    if (block_table_bytes <= 4096u) {
        [encoder setBytes:block_table length:block_table_bytes atIndex:4];
    } else {
        block_table_buffer = [runtime->device newBufferWithBytes:block_table length:block_table_bytes options:MTLResourceStorageModeShared];
        if (block_table_buffer == nil) return failure_code;
        if (runtime->active_frame_cb != nil && termite_metal_decode_runtime_retain_frame_resource(runtime, block_table_buffer) != 0) return failure_code;
        [encoder setBuffer:block_table_buffer offset:0 atIndex:4];
    }
    [encoder setBytes:&params length:sizeof(params) atIndex:5];
    const size_t total = suffix_tokens * (values_per_row > v_row_stride ? values_per_row : v_row_stride);
    [encoder dispatchThreads:MTLSizeMake(total, 1, 1)
       threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->paged_f32_kv_seed_pipeline, total), 1, 1)];

    runtime->attention_span_slot_tokens[slot] = capacity_tokens;
    runtime->attention_span_slot_key_row_bytes[slot] = key_row_bytes;
    runtime->attention_span_slot_v_row_stride[slot] = v_row_stride;
    runtime->attention_span_slot_position_offset[slot] = kv_position_offset;
    if (slot == 0) {
        runtime->attention_span_encoded_key_buffer = runtime->attention_span_encoded_key_buffers[slot];
        runtime->attention_span_v_buffer = runtime->attention_span_v_buffers[slot];
        runtime->attention_span_encoded_key_capacity = runtime->attention_span_encoded_key_capacities[slot];
        runtime->attention_span_v_capacity = runtime->attention_span_v_capacities[slot];
        runtime->attention_span_tokens = capacity_tokens;
        runtime->attention_span_key_row_bytes = key_row_bytes;
        runtime->attention_span_v_row_stride = v_row_stride;
        runtime->attention_span_position_offset = kv_position_offset;
    }
    (void)block_table_buffer;
    return 0;
}

int termite_metal_decode_runtime_update_attention_paged_from_f32_key_device_slot(
    termite_metal_decode_runtime *runtime,
    size_t slot,
    uint32_t format,
    void *k_handle,
    size_t k_offset,
    void *v_handle,
    size_t v_offset,
    size_t total_tokens,
    size_t suffix_tokens,
    size_t num_kv_heads,
    size_t head_dim,
    size_t key_row_bytes,
    size_t base_key_row_bytes,
    size_t v_row_stride,
    size_t kv_position_offset,
    const uint32_t *block_table,
    size_t block_count,
    size_t page_size
) {
    if (runtime == NULL) return -1;
    @autoreleasepool {
        const bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLCommandBuffer> command_buffer = frame_owned
            ? termite_metal_new_command_buffer(runtime->queue, __func__)
            : runtime->active_frame_cb;
        if (command_buffer == nil) return -13;
        int rc = termite_metal_decode_runtime_encode_attention_paged_update_from_f32_key_device_slot(
            runtime,
            command_buffer,
            slot,
            format,
            k_handle,
            k_offset,
            v_handle,
            v_offset,
            total_tokens,
            suffix_tokens,
            num_kv_heads,
            head_dim,
            key_row_bytes,
            base_key_row_bytes,
            v_row_stride,
            kv_position_offset,
            block_table,
            block_count,
            page_size
        );
        if (rc == 0 && frame_owned) {
            [command_buffer commit];
            [command_buffer waitUntilCompleted];
            rc = command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -25;
        }
        return rc;
    }
}

/// Reset an attention-span slot's metadata so subsequent writes re-encode
/// from scratch rather than trying to append to stale state. The backing
/// MTLBuffers are retained — their capacity is reusable by the next tenant
/// of this slot. Used by MetalKvStorage when reclaiming slots from a
/// released sequence.
int termite_metal_decode_runtime_reset_attention_span_slot(
    termite_metal_decode_runtime *runtime,
    size_t slot
) {
    if (runtime == NULL) return -1;
    if (slot >= TERMITE_METAL_ATTENTION_SPAN_SLOT_CAPACITY) return -2;
    runtime->attention_span_slot_tokens[slot] = 0;
    runtime->attention_span_slot_key_row_bytes[slot] = 0;
    runtime->attention_span_slot_v_row_stride[slot] = 0;
    runtime->attention_span_slot_position_offset[slot] = 0;
    if (slot == 0) {
        runtime->attention_span_tokens = 0;
        runtime->attention_span_key_row_bytes = 0;
        runtime->attention_span_v_row_stride = 0;
        runtime->attention_span_position_offset = 0;
    }
    return 0;
}

/// Reserve backing buffers for a persistent attention-span slot without
/// marking any logical tokens as written. MetalKvStorage uses this at
/// sequence-growth boundaries so suffix writes do not have to reallocate or
/// copy old slot contents on the attention hot path.
int termite_metal_decode_runtime_reserve_attention_span_slot_buffers(
    termite_metal_decode_runtime *runtime,
    size_t slot,
    uint32_t format,
    size_t token_capacity,
    size_t key_row_bytes,
    size_t v_row_stride
) {
    if (runtime == NULL) return -1;
    if (slot >= TERMITE_METAL_ATTENTION_SPAN_SLOT_CAPACITY) return -2;
    if (token_capacity == 0 || key_row_bytes == 0 || v_row_stride == 0) return -3;
    size_t v_element_bytes = sizeof(float);
    switch (format) {
        case 0u:
        case 1u:
        case 2u:
        case 4u:
            v_element_bytes = sizeof(float);
            break;
        case 3u:
            v_element_bytes = sizeof(uint16_t);
            break;
        default:
            return -4;
    }
    if (token_capacity > SIZE_MAX / key_row_bytes) return -5;
    const size_t encoded_bytes = token_capacity * key_row_bytes;
    if (v_row_stride > SIZE_MAX / v_element_bytes) return -6;
    const size_t v_row_bytes = v_row_stride * v_element_bytes;
    if (token_capacity > SIZE_MAX / v_row_bytes) return -7;
    const size_t v_bytes = token_capacity * v_row_bytes;
    return termite_metal_decode_runtime_ensure_attention_span_slot_buffers(runtime, slot, encoded_bytes, v_bytes);
}

/// Snapshot the current state + buffer handles for an attention-span slot so a
/// caller can download the bytes and decode them on the host. Used by
/// MetalKvStorage.gatherLayerKv to materialize device-resident KV into host
/// f32 without a parallel CPU shadow. Any of the out-pointers may be NULL.
int termite_metal_decode_runtime_attention_span_slot_info(
    const termite_metal_decode_runtime *runtime,
    size_t slot,
    void **encoded_key_handle_out,
    size_t *encoded_key_capacity_out,
    void **v_handle_out,
    size_t *v_capacity_out,
    size_t *tokens_out,
    size_t *key_row_bytes_out,
    size_t *v_row_stride_out,
    size_t *position_offset_out
) {
    if (runtime == NULL) return -1;
    if (slot >= TERMITE_METAL_ATTENTION_SPAN_SLOT_CAPACITY) return -2;
    if (encoded_key_handle_out) *encoded_key_handle_out = (__bridge void *)runtime->attention_span_encoded_key_buffers[slot];
    if (encoded_key_capacity_out) *encoded_key_capacity_out = runtime->attention_span_encoded_key_capacities[slot];
    if (v_handle_out) *v_handle_out = (__bridge void *)runtime->attention_span_v_buffers[slot];
    if (v_capacity_out) *v_capacity_out = runtime->attention_span_v_capacities[slot];
    if (tokens_out) *tokens_out = runtime->attention_span_slot_tokens[slot];
    if (key_row_bytes_out) *key_row_bytes_out = runtime->attention_span_slot_key_row_bytes[slot];
    if (v_row_stride_out) *v_row_stride_out = runtime->attention_span_slot_v_row_stride[slot];
    if (position_offset_out) *position_offset_out = runtime->attention_span_slot_position_offset[slot];
    return 0;
}

int termite_metal_decode_runtime_append_attention_span(
    termite_metal_decode_runtime *runtime,
    const uint8_t *encoded_key_suffix,
    const float *v_suffix,
    size_t kv_tokens,
    size_t suffix_tokens,
    size_t key_row_bytes,
    size_t v_row_stride,
    size_t position_offset
) {
    if (runtime == NULL || encoded_key_suffix == NULL || v_suffix == NULL) return -1;
    if (kv_tokens == 0 || suffix_tokens == 0 || suffix_tokens > kv_tokens || key_row_bytes == 0 || v_row_stride == 0) return -2;
    if (runtime->attention_span_tokens == 0) return 1;
    if (runtime->attention_span_key_row_bytes != key_row_bytes || runtime->attention_span_v_row_stride != v_row_stride) return 1;
    if (position_offset < runtime->attention_span_position_offset) return 1;
    @autoreleasepool {
        // Private resident buffers do not expose CPU-accessible contents. Let
        // the caller fall back to a full-span upload when incremental append
        // cannot be expressed safely through the current runtime contract.
        (void)encoded_key_suffix;
        (void)v_suffix;
        (void)kv_tokens;
        (void)suffix_tokens;
        (void)position_offset;
        return 1;
    }
}

static int termite_metal_decode_runtime_encode_replace_attention_span(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    const uint8_t *encoded_key,
    const float *v,
    size_t kv_tokens,
    size_t key_row_bytes,
    size_t v_row_stride,
    size_t position_offset
) {
    return termite_metal_decode_runtime_encode_replace_attention_span_slot(
        runtime,
        command_buffer,
        0,
        encoded_key,
        v,
        kv_tokens,
        key_row_bytes,
        v_row_stride,
        position_offset
    );
}

static int termite_metal_decode_runtime_encode_replace_attention_span_slot(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    size_t slot,
    const uint8_t *encoded_key,
    const float *v,
    size_t kv_tokens,
    size_t key_row_bytes,
    size_t v_row_stride,
    size_t position_offset
) {
    if (runtime == NULL || command_buffer == nil || encoded_key == NULL || v == NULL) return -1;
    if (kv_tokens == 0 || key_row_bytes == 0 || v_row_stride == 0) return -2;
    if (slot >= TERMITE_METAL_ATTENTION_SPAN_SLOT_CAPACITY) return -3;
    const size_t encoded_bytes = kv_tokens * key_row_bytes;
    const size_t v_bytes = kv_tokens * v_row_stride * sizeof(float);
    if (termite_metal_decode_runtime_ensure_attention_span_slot_buffers(runtime, slot, encoded_bytes, v_bytes) != 0) return -4;
    id<MTLBuffer> encoded_staging = [runtime->device newBufferWithBytes:encoded_key length:encoded_bytes options:MTLResourceStorageModeShared];
    id<MTLBuffer> v_staging = [runtime->device newBufferWithBytes:v length:v_bytes options:MTLResourceStorageModeShared];
    if (encoded_staging == nil || v_staging == nil) return -6;
    termite_metal_record_active_frame_blit_source(command_buffer, TERMITE_METAL_BLIT_SOURCE_ATTENTION_SPAN);
    id<MTLBlitCommandEncoder> blit = termite_metal_tracked_blit_command_encoder(command_buffer);
    if (blit == nil) return -7;
    [blit copyFromBuffer:encoded_staging sourceOffset:0 toBuffer:runtime->attention_span_encoded_key_buffers[slot] destinationOffset:0 size:encoded_bytes];
    [blit copyFromBuffer:v_staging sourceOffset:0 toBuffer:runtime->attention_span_v_buffers[slot] destinationOffset:0 size:v_bytes];
    [blit endEncoding];
    runtime->attention_span_slot_tokens[slot] = kv_tokens;
    runtime->attention_span_slot_key_row_bytes[slot] = key_row_bytes;
    runtime->attention_span_slot_v_row_stride[slot] = v_row_stride;
    runtime->attention_span_slot_position_offset[slot] = position_offset;
    if (slot == 0) {
        runtime->attention_span_encoded_key_buffer = runtime->attention_span_encoded_key_buffers[slot];
        runtime->attention_span_v_buffer = runtime->attention_span_v_buffers[slot];
        runtime->attention_span_encoded_key_capacity = runtime->attention_span_encoded_key_capacities[slot];
        runtime->attention_span_v_capacity = runtime->attention_span_v_capacities[slot];
        runtime->attention_span_tokens = kv_tokens;
        runtime->attention_span_key_row_bytes = key_row_bytes;
        runtime->attention_span_v_row_stride = v_row_stride;
        runtime->attention_span_position_offset = position_offset;
    }
    return 0;
}

static int termite_metal_decode_runtime_encode_replace_attention_span_from_f32_key_slot(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    size_t slot,
    uint32_t format,
    const float *k,
    const float *v,
    size_t kv_tokens,
    size_t num_kv_heads,
    size_t head_dim,
    size_t key_row_bytes,
    size_t base_key_row_bytes,
    size_t v_row_stride,
    size_t position_offset
);

static int termite_metal_decode_runtime_encode_replace_attention_span_from_f32_key(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    uint32_t format,
    const float *k,
    const float *v,
    size_t kv_tokens,
    size_t num_kv_heads,
    size_t head_dim,
    size_t key_row_bytes,
    size_t base_key_row_bytes,
    size_t v_row_stride,
    size_t position_offset
) {
    return termite_metal_decode_runtime_encode_replace_attention_span_from_f32_key_slot(
        runtime,
        command_buffer,
        0,
        format,
        k,
        v,
        kv_tokens,
        num_kv_heads,
        head_dim,
        key_row_bytes,
        base_key_row_bytes,
        v_row_stride,
        position_offset
    );
}

static int termite_metal_decode_runtime_encode_replace_attention_span_from_f32_key_slot(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    size_t slot,
    uint32_t format,
    const float *k,
    const float *v,
    size_t kv_tokens,
    size_t num_kv_heads,
    size_t head_dim,
    size_t key_row_bytes,
    size_t base_key_row_bytes,
    size_t v_row_stride,
    size_t position_offset
) {
    if (runtime == NULL || command_buffer == nil || k == NULL || v == NULL) return -1;
    if (kv_tokens == 0 || num_kv_heads == 0 || head_dim == 0 || key_row_bytes == 0 || base_key_row_bytes == 0 || v_row_stride == 0) return -2;
    if (kv_tokens > UINT32_MAX || num_kv_heads > UINT32_MAX || head_dim > UINT32_MAX || key_row_bytes > UINT32_MAX || base_key_row_bytes > UINT32_MAX) return -3;
    if (slot >= TERMITE_METAL_ATTENTION_SPAN_SLOT_CAPACITY) return -4;
    size_t suffix_start = 0;
    if (runtime->attention_span_slot_tokens[slot] > 0 &&
        runtime->attention_span_slot_tokens[slot] <= kv_tokens &&
        runtime->attention_span_slot_key_row_bytes[slot] == key_row_bytes &&
        runtime->attention_span_slot_v_row_stride[slot] == v_row_stride &&
        runtime->attention_span_slot_position_offset[slot] == position_offset)
    {
        suffix_start = runtime->attention_span_slot_tokens[slot];
        if (suffix_start == kv_tokens) return 0;
    }
    const size_t suffix_tokens = kv_tokens - suffix_start;
    const size_t encoded_bytes = kv_tokens * key_row_bytes;
    const size_t suffix_k_values = suffix_tokens * num_kv_heads * head_dim;
    const size_t k_bytes = suffix_k_values * sizeof(float);
    const size_t v_bytes = kv_tokens * v_row_stride * sizeof(float);
    const size_t suffix_v_bytes = suffix_tokens * v_row_stride * sizeof(float);
    if (termite_metal_decode_runtime_ensure_attention_span_slot_buffers(runtime, slot, encoded_bytes, v_bytes) != 0) return -5;

    id<MTLComputePipelineState> pipeline = nil;
    switch (format) {
        case 0u: pipeline = runtime->encode_polar4_key_pipeline; break;
        case 1u: pipeline = runtime->encode_turbo3_key_pipeline; break;
        default: return -7;
    }
    if (pipeline == nil) return -8;

    const float *k_suffix = k + suffix_start * num_kv_heads * head_dim;
    const float *v_suffix = v + suffix_start * v_row_stride;
    id<MTLBuffer> k_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)k_suffix length:k_bytes options:MTLResourceStorageModeShared deallocator:nil];
    if (k_buffer == nil) k_buffer = [runtime->device newBufferWithBytes:k_suffix length:k_bytes options:MTLResourceStorageModeShared];
    id<MTLBuffer> v_staging = [runtime->device newBufferWithBytes:v_suffix length:suffix_v_bytes options:MTLResourceStorageModeShared];
    if (k_buffer == nil || v_staging == nil) return -9;

    termite_metal_encode_key_params params = {
        .rows = (uint32_t)suffix_tokens,
        .num_kv_heads = (uint32_t)num_kv_heads,
        .head_dim = (uint32_t)head_dim,
        .key_row_bytes = (uint32_t)key_row_bytes,
        .base_key_row_bytes = (uint32_t)base_key_row_bytes,
    };
    id<MTLBuffer> params_buffer = [runtime->device newBufferWithBytes:&params length:sizeof(params) options:MTLResourceStorageModeShared];
    if (params_buffer == nil) return -10;

    id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
    if (encoder == nil) return -11;
    [encoder setComputePipelineState:pipeline];
    [encoder setBuffer:k_buffer offset:0 atIndex:0];
    [encoder setBuffer:runtime->attention_span_encoded_key_buffers[slot] offset:(suffix_start * key_row_bytes) atIndex:1];
    [encoder setBuffer:params_buffer offset:0 atIndex:2];
    const size_t residual_bytes = (num_kv_heads * 32u + 7u) / 8u;
    const size_t work_items = format == 0u ? suffix_tokens * base_key_row_bytes : suffix_tokens * (base_key_row_bytes + residual_bytes);
    [encoder dispatchThreads:MTLSizeMake(work_items, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(pipeline, work_items), 1, 1)];
    [encoder endEncoding];

    termite_metal_record_active_frame_blit_source(command_buffer, TERMITE_METAL_BLIT_SOURCE_ATTENTION_SPAN);
    id<MTLBlitCommandEncoder> blit = termite_metal_tracked_blit_command_encoder(command_buffer);
    if (blit == nil) return -12;
    [blit copyFromBuffer:v_staging sourceOffset:0 toBuffer:runtime->attention_span_v_buffers[slot] destinationOffset:(suffix_start * v_row_stride * sizeof(float)) size:suffix_v_bytes];
    [blit endEncoding];

    runtime->attention_span_slot_tokens[slot] = kv_tokens;
    runtime->attention_span_slot_key_row_bytes[slot] = key_row_bytes;
    runtime->attention_span_slot_v_row_stride[slot] = v_row_stride;
    runtime->attention_span_slot_position_offset[slot] = position_offset;
    if (slot == 0) {
        runtime->attention_span_encoded_key_buffer = runtime->attention_span_encoded_key_buffers[slot];
        runtime->attention_span_v_buffer = runtime->attention_span_v_buffers[slot];
        runtime->attention_span_encoded_key_capacity = runtime->attention_span_encoded_key_capacities[slot];
        runtime->attention_span_v_capacity = runtime->attention_span_v_capacities[slot];
        runtime->attention_span_tokens = kv_tokens;
        runtime->attention_span_key_row_bytes = key_row_bytes;
        runtime->attention_span_v_row_stride = v_row_stride;
        runtime->attention_span_position_offset = position_offset;
    }
    return 0;
}

static int termite_metal_decode_runtime_encode_replace_attention_span_from_f32_key_device_slot(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    size_t slot,
    uint32_t format,
    void *k_handle,
    size_t k_offset,
    void *v_handle,
    size_t v_offset,
    size_t kv_tokens,
    size_t num_kv_heads,
    size_t head_dim,
    size_t key_row_bytes,
    size_t base_key_row_bytes,
    size_t v_row_stride,
    size_t position_offset
) {
    if (runtime == NULL || command_buffer == nil || k_handle == NULL || v_handle == NULL) return -1;
    if (kv_tokens == 0 || num_kv_heads == 0 || head_dim == 0 || key_row_bytes == 0 || base_key_row_bytes == 0 || v_row_stride == 0) return -2;
    if (kv_tokens > UINT32_MAX || num_kv_heads > UINT32_MAX || head_dim > UINT32_MAX || key_row_bytes > UINT32_MAX || base_key_row_bytes > UINT32_MAX) return -3;
    if (slot >= TERMITE_METAL_ATTENTION_SPAN_SLOT_CAPACITY) return -4;
    size_t suffix_start = 0;
    if (runtime->attention_span_slot_tokens[slot] > 0 &&
        runtime->attention_span_slot_tokens[slot] <= kv_tokens &&
        runtime->attention_span_slot_key_row_bytes[slot] == key_row_bytes &&
        runtime->attention_span_slot_v_row_stride[slot] == v_row_stride &&
        runtime->attention_span_slot_position_offset[slot] == position_offset)
    {
        suffix_start = runtime->attention_span_slot_tokens[slot];
        if (suffix_start == kv_tokens) return 0;
    }
    const size_t suffix_tokens = kv_tokens - suffix_start;
    const size_t encoded_bytes = kv_tokens * key_row_bytes;
    const size_t v_bytes = kv_tokens * v_row_stride * sizeof(float);
    const size_t suffix_v_bytes = suffix_tokens * v_row_stride * sizeof(float);
    if (termite_metal_decode_runtime_ensure_attention_span_slot_buffers(runtime, slot, encoded_bytes, v_bytes) != 0) return -5;

    id<MTLBuffer> k_buffer = (__bridge id<MTLBuffer>)k_handle;
    id<MTLBuffer> v_buffer = (__bridge id<MTLBuffer>)v_handle;
    if (k_buffer == nil || v_buffer == nil) return -9;

    if (format == 2u) {
        uint32_t contiguous_block_table[1] = { 0u };
        BOOL encoder_owned = YES;
        id<MTLComputeCommandEncoder> encoder = termite_metal_scoped_compute_encoder_for(runtime, command_buffer, TERMITE_METAL_COMPUTE_SOURCE_ATTENTION, &encoder_owned);
        if (encoder == nil) return -12;
        const int rc = termite_metal_encode_paged_f32_kv_seed_on_encoder(
            runtime,
            encoder,
            slot,
            k_buffer,
            k_offset,
            v_buffer,
            v_offset,
            kv_tokens,
            suffix_tokens,
            num_kv_heads,
            head_dim,
            key_row_bytes,
            v_row_stride,
            position_offset,
            contiguous_block_table,
            1,
            kv_tokens,
            -12);
        termite_metal_end_scoped_compute_encoder(encoder, encoder_owned);
        if (rc != 0) return rc;
    } else if (format == 4u) {
        // int8 per-head path — keys are quantized into a (scale_f32 + int8[head_dim])
        // layout per head via threadgroup tree reduction; values are raw f32
        // (same as polar4/turbo3 layout so existing attention/gather paths
        // continue to work unchanged).
        id<MTLComputePipelineState> pipeline = runtime->encode_int8_per_head_pipeline;
        if (pipeline == nil) return -8;
        const size_t values_per_row = num_kv_heads * head_dim;
        // Threadgroup size = next power of 2 >= head_dim, capped at 256.
        size_t tg_size = 1u;
        while (tg_size < head_dim && tg_size < 256u) tg_size <<= 1u;
        if (tg_size < head_dim) return -13;
        termite_metal_encode_key_params params = {
            .rows = (uint32_t)suffix_tokens,
            .num_kv_heads = (uint32_t)num_kv_heads,
            .head_dim = (uint32_t)head_dim,
            .key_row_bytes = (uint32_t)key_row_bytes,
            .base_key_row_bytes = (uint32_t)base_key_row_bytes,
        };
        id<MTLBuffer> params_buffer = [runtime->device newBufferWithBytes:&params length:sizeof(params) options:MTLResourceStorageModeShared];
        if (params_buffer == nil) return -10;

        id<MTLComputeCommandEncoder> k_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (k_encoder == nil) return -11;
        [k_encoder setComputePipelineState:pipeline];
        [k_encoder setBuffer:k_buffer offset:k_offset atIndex:0];
        [k_encoder setBuffer:runtime->attention_span_encoded_key_buffers[slot] offset:(suffix_start * key_row_bytes) atIndex:1];
        [k_encoder setBuffer:params_buffer offset:0 atIndex:2];
        [k_encoder dispatchThreads:MTLSizeMake(tg_size, suffix_tokens * num_kv_heads, 1)
              threadsPerThreadgroup:MTLSizeMake(tg_size, 1, 1)];
        [k_encoder endEncoding];

        // Values: raw f32 blit matching the polar4/turbo3 path.
        termite_metal_record_active_frame_blit_source(command_buffer, TERMITE_METAL_BLIT_SOURCE_ATTENTION_SPAN);
        id<MTLBlitCommandEncoder> blit = termite_metal_tracked_blit_command_encoder(command_buffer);
        if (blit == nil) return -12;
        [blit copyFromBuffer:v_buffer
                sourceOffset:v_offset
                    toBuffer:runtime->attention_span_v_buffers[slot]
           destinationOffset:(suffix_start * v_row_stride * sizeof(float))
                        size:suffix_v_bytes];
        [blit endEncoding];
    } else if (format == 3u) {
        // f16 path — dispatch the f32→half converter twice (once for keys,
        // once for values). Slot buffers are sized for `key_row_bytes` and
        // `v_row_stride * sizeof(half)` respectively; caller is responsible
        // for passing key_row_bytes = num_kv_heads*head_dim*sizeof(half).
        id<MTLComputePipelineState> pipeline = runtime->encode_f16_convert_pipeline;
        if (pipeline == nil) return -8;
        const size_t values_per_row = num_kv_heads * head_dim;
        termite_metal_encode_key_params params = {
            .rows = (uint32_t)suffix_tokens,
            .num_kv_heads = (uint32_t)num_kv_heads,
            .head_dim = (uint32_t)head_dim,
            .key_row_bytes = (uint32_t)key_row_bytes,
            .base_key_row_bytes = (uint32_t)base_key_row_bytes,
        };
        id<MTLBuffer> params_buffer = [runtime->device newBufferWithBytes:&params length:sizeof(params) options:MTLResourceStorageModeShared];
        if (params_buffer == nil) return -10;
        const size_t work_items = suffix_tokens * values_per_row;
        const size_t tg_width = termite_metal_thread_width(pipeline, work_items);

        id<MTLComputeCommandEncoder> k_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (k_encoder == nil) return -11;
        [k_encoder setComputePipelineState:pipeline];
        [k_encoder setBuffer:k_buffer offset:k_offset atIndex:0];
        [k_encoder setBuffer:runtime->attention_span_encoded_key_buffers[slot] offset:(suffix_start * key_row_bytes) atIndex:1];
        [k_encoder setBuffer:params_buffer offset:0 atIndex:2];
        [k_encoder dispatchThreads:MTLSizeMake(work_items, 1, 1) threadsPerThreadgroup:MTLSizeMake(tg_width, 1, 1)];
        [k_encoder endEncoding];

        // Values: slot holds v_row_stride halfs per row. Suffix write offset
        // is in *half* units, i.e. v_row_stride * 2 bytes per row.
        id<MTLComputeCommandEncoder> v_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (v_encoder == nil) return -11;
        [v_encoder setComputePipelineState:pipeline];
        [v_encoder setBuffer:v_buffer offset:v_offset atIndex:0];
        [v_encoder setBuffer:runtime->attention_span_v_buffers[slot] offset:(suffix_start * v_row_stride * sizeof(uint16_t)) atIndex:1];
        [v_encoder setBuffer:params_buffer offset:0 atIndex:2];
        [v_encoder dispatchThreads:MTLSizeMake(work_items, 1, 1) threadsPerThreadgroup:MTLSizeMake(tg_width, 1, 1)];
        [v_encoder endEncoding];
    } else {
        id<MTLComputePipelineState> pipeline = nil;
        switch (format) {
            case 0u: pipeline = runtime->encode_polar4_key_pipeline; break;
            case 1u: pipeline = runtime->encode_turbo3_key_pipeline; break;
            default: return -7;
        }
        if (pipeline == nil) return -8;

        termite_metal_encode_key_params params = {
            .rows = (uint32_t)suffix_tokens,
            .num_kv_heads = (uint32_t)num_kv_heads,
            .head_dim = (uint32_t)head_dim,
            .key_row_bytes = (uint32_t)key_row_bytes,
            .base_key_row_bytes = (uint32_t)base_key_row_bytes,
        };
        id<MTLBuffer> params_buffer = [runtime->device newBufferWithBytes:&params length:sizeof(params) options:MTLResourceStorageModeShared];
        if (params_buffer == nil) return -10;

        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (encoder == nil) return -11;
        [encoder setComputePipelineState:pipeline];
        [encoder setBuffer:k_buffer offset:k_offset atIndex:0];
        [encoder setBuffer:runtime->attention_span_encoded_key_buffers[slot] offset:(suffix_start * key_row_bytes) atIndex:1];
        [encoder setBuffer:params_buffer offset:0 atIndex:2];
        const size_t residual_bytes = (num_kv_heads * 32u + 7u) / 8u;
        const size_t work_items = format == 0u ? suffix_tokens * base_key_row_bytes : suffix_tokens * (base_key_row_bytes + residual_bytes);
        [encoder dispatchThreads:MTLSizeMake(work_items, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(pipeline, work_items), 1, 1)];
        [encoder endEncoding];

        termite_metal_record_active_frame_blit_source(command_buffer, TERMITE_METAL_BLIT_SOURCE_ATTENTION_SPAN);
        id<MTLBlitCommandEncoder> blit = termite_metal_tracked_blit_command_encoder(command_buffer);
        if (blit == nil) return -12;
        [blit copyFromBuffer:v_buffer sourceOffset:v_offset toBuffer:runtime->attention_span_v_buffers[slot] destinationOffset:(suffix_start * v_row_stride * sizeof(float)) size:suffix_v_bytes];
        [blit endEncoding];
    }

    runtime->attention_span_slot_tokens[slot] = kv_tokens;
    runtime->attention_span_slot_key_row_bytes[slot] = key_row_bytes;
    runtime->attention_span_slot_v_row_stride[slot] = v_row_stride;
    runtime->attention_span_slot_position_offset[slot] = position_offset;
    if (slot == 0) {
        runtime->attention_span_encoded_key_buffer = runtime->attention_span_encoded_key_buffers[slot];
        runtime->attention_span_v_buffer = runtime->attention_span_v_buffers[slot];
        runtime->attention_span_encoded_key_capacity = runtime->attention_span_encoded_key_capacities[slot];
        runtime->attention_span_v_capacity = runtime->attention_span_v_capacities[slot];
        runtime->attention_span_tokens = kv_tokens;
        runtime->attention_span_key_row_bytes = key_row_bytes;
        runtime->attention_span_v_row_stride = v_row_stride;
        runtime->attention_span_position_offset = position_offset;
    }
    return 0;
}

int termite_metal_decode_runtime_attention_span(
    termite_metal_decode_runtime *runtime,
    uint32_t format,
    const float *q,
    size_t kv_tokens,
    size_t num_heads,
    size_t num_kv_heads,
    size_t head_dim,
    size_t key_row_bytes,
    size_t base_key_row_bytes,
    size_t query_position,
    size_t kv_position_offset,
    size_t sliding_window,
    float *output
) {
    if (runtime == NULL || q == NULL || output == NULL) return -1;
    if (kv_tokens == 0 || num_heads == 0 || num_kv_heads == 0 || head_dim == 0) return -2;
    if (num_heads > UINT32_MAX || num_kv_heads > UINT32_MAX || head_dim > UINT32_MAX || kv_tokens > UINT32_MAX) return -3;
    if (key_row_bytes > UINT32_MAX || base_key_row_bytes > UINT32_MAX || query_position > UINT32_MAX || kv_position_offset > UINT32_MAX || sliding_window > UINT32_MAX) return -4;
    if (runtime->attention_span_encoded_key_buffer == nil || runtime->attention_span_v_buffer == nil) return -5;
    if (runtime->attention_span_tokens != kv_tokens || runtime->attention_span_key_row_bytes != key_row_bytes || runtime->attention_span_position_offset != kv_position_offset) return -6;
    id<MTLComputePipelineState> pipeline = nil;
    switch (format) {
        case 0u: pipeline = runtime->polar4_attention_span_pipeline; break;
        case 1u: pipeline = runtime->turbo3_attention_span_pipeline; break;
        default: return -7;
    }
    if (pipeline == nil) return -8;
    @autoreleasepool {
        const size_t q_bytes = num_heads * head_dim * sizeof(float);
        const size_t output_bytes = num_heads * head_dim * sizeof(float);
        id<MTLBuffer> q_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)q length:q_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (q_buffer == nil) q_buffer = [runtime->device newBufferWithBytes:q length:q_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithBytesNoCopy:output length:output_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) output_buffer = [runtime->device newBufferWithBytes:output length:output_bytes options:MTLResourceStorageModeShared];
        if (q_buffer == nil || output_buffer == nil) return -9;
        termite_metal_attention_span_params params = {
            .kv_tokens = (uint32_t)kv_tokens,
            .num_heads = (uint32_t)num_heads,
            .num_kv_heads = (uint32_t)num_kv_heads,
            .head_dim = (uint32_t)head_dim,
            .key_row_bytes = (uint32_t)key_row_bytes,
            .base_key_row_bytes = (uint32_t)base_key_row_bytes,
            .query_position = (uint32_t)query_position,
            .kv_position_offset = (uint32_t)kv_position_offset,
            .sliding_window = (uint32_t)sliding_window,
            .v_row_stride = (uint32_t)runtime->attention_span_v_row_stride,
        };
        id<MTLBuffer> params_buffer = [runtime->device newBufferWithBytes:&params length:sizeof(params) options:MTLResourceStorageModeShared];
        if (params_buffer == nil) return -10;
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -11;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (encoder == nil) return -12;
        [encoder setComputePipelineState:pipeline];
        [encoder setBuffer:q_buffer offset:0 atIndex:0];
        [encoder setBuffer:runtime->attention_span_encoded_key_buffer offset:0 atIndex:1];
        [encoder setBuffer:runtime->attention_span_v_buffer offset:0 atIndex:2];
        [encoder setBuffer:output_buffer offset:0 atIndex:3];
        [encoder setBuffer:params_buffer offset:0 atIndex:4];
        NSUInteger thread_width = pipeline.maxTotalThreadsPerThreadgroup;
        if (thread_width == 0) thread_width = 64;
        if (thread_width > num_heads && num_heads > 0) thread_width = num_heads;
        [encoder dispatchThreads:MTLSizeMake(num_heads, 1, 1) threadsPerThreadgroup:MTLSizeMake(thread_width, 1, 1)];
        [encoder endEncoding];
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -13;
    }
}

int termite_metal_decode_runtime_attention_span_device(
    termite_metal_decode_runtime *runtime,
    uint32_t format,
    void *q_handle,
    size_t q_offset,
    size_t kv_tokens,
    size_t num_heads,
    size_t num_kv_heads,
    size_t head_dim,
    size_t key_row_bytes,
    size_t base_key_row_bytes,
    size_t query_position,
    size_t kv_position_offset,
    size_t sliding_window,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || q_handle == NULL || output_handle == NULL) return -1;
    if (kv_tokens == 0 || num_heads == 0 || num_kv_heads == 0 || head_dim == 0) return -2;
    if (num_heads > UINT32_MAX || num_kv_heads > UINT32_MAX || head_dim > UINT32_MAX || kv_tokens > UINT32_MAX) return -3;
    if (key_row_bytes > UINT32_MAX || base_key_row_bytes > UINT32_MAX || query_position > UINT32_MAX || kv_position_offset > UINT32_MAX || sliding_window > UINT32_MAX) return -4;
    if (runtime->attention_span_encoded_key_buffer == nil || runtime->attention_span_v_buffer == nil) return -5;
    if (runtime->attention_span_tokens != kv_tokens || runtime->attention_span_key_row_bytes != key_row_bytes || runtime->attention_span_position_offset != kv_position_offset) return -6;
    id<MTLComputePipelineState> pipeline = nil;
    switch (format) {
        case 0u: pipeline = runtime->polar4_attention_span_pipeline; break;
        case 1u: pipeline = runtime->turbo3_attention_span_pipeline; break;
        default: return -7;
    }
    if (pipeline == nil) return -8;
    @autoreleasepool {
        id<MTLBuffer> q_buffer = (__bridge id<MTLBuffer>)q_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        if (q_buffer == nil || output_buffer == nil) return -9;
        termite_metal_attention_span_params params = {
            .kv_tokens = (uint32_t)kv_tokens,
            .num_heads = (uint32_t)num_heads,
            .num_kv_heads = (uint32_t)num_kv_heads,
            .head_dim = (uint32_t)head_dim,
            .key_row_bytes = (uint32_t)key_row_bytes,
            .base_key_row_bytes = (uint32_t)base_key_row_bytes,
            .query_position = (uint32_t)query_position,
            .kv_position_offset = (uint32_t)kv_position_offset,
            .sliding_window = (uint32_t)sliding_window,
            .v_row_stride = (uint32_t)runtime->attention_span_v_row_stride,
        };
        id<MTLBuffer> params_buffer = [runtime->device newBufferWithBytes:&params length:sizeof(params) options:MTLResourceStorageModeShared];
        if (params_buffer == nil) return -10;
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -11;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (encoder == nil) return -12;
        [encoder setComputePipelineState:pipeline];
        [encoder setBuffer:q_buffer offset:q_offset atIndex:0];
        [encoder setBuffer:runtime->attention_span_encoded_key_buffer offset:0 atIndex:1];
        [encoder setBuffer:runtime->attention_span_v_buffer offset:0 atIndex:2];
        [encoder setBuffer:output_buffer offset:output_offset atIndex:3];
        [encoder setBuffer:params_buffer offset:0 atIndex:4];
        NSUInteger thread_width = pipeline.maxTotalThreadsPerThreadgroup;
        if (thread_width == 0) thread_width = 64;
        if (thread_width > num_heads && num_heads > 0) thread_width = num_heads;
        [encoder dispatchThreads:MTLSizeMake(num_heads, 1, 1) threadsPerThreadgroup:MTLSizeMake(thread_width, 1, 1)];
        [encoder endEncoding];
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -13;
    }
}

static int termite_metal_decode_runtime_encode_paged_attention_slot(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    size_t slot,
    uint32_t format,
    id<MTLBuffer> q_buffer,
    size_t q_offset,
    const uint32_t *block_table,
    size_t block_count,
    size_t page_size,
    size_t q_len,
    size_t kv_tokens,
    size_t num_heads,
    size_t num_kv_heads,
    size_t head_dim,
    size_t key_row_bytes,
    size_t base_key_row_bytes,
    size_t query_position_offset,
    size_t kv_position_offset,
    size_t sliding_window,
    float softcap,
    const float *sinks,
    size_t sink_count,
    id<MTLBuffer> output_buffer,
    size_t output_offset
) {
    if (runtime == NULL || command_buffer == nil || q_buffer == nil || block_table == NULL || output_buffer == nil) return -1;
    if (slot >= TERMITE_METAL_ATTENTION_SPAN_SLOT_CAPACITY) return -2;
    if (q_len == 0 || kv_tokens == 0 || num_heads == 0 || num_kv_heads == 0 || head_dim == 0 || page_size == 0 || block_count == 0) return -3;
    if (num_heads % num_kv_heads != 0) return -4;
    if (q_len > UINT32_MAX || kv_tokens > UINT32_MAX || num_heads > UINT32_MAX || num_kv_heads > UINT32_MAX || head_dim > UINT32_MAX) return -5;
    if (key_row_bytes > UINT32_MAX || base_key_row_bytes > UINT32_MAX || query_position_offset > UINT32_MAX || kv_position_offset > UINT32_MAX || sliding_window > UINT32_MAX || page_size > UINT32_MAX || block_count > UINT32_MAX) return -6;
    if (runtime->attention_paged_pipeline == nil) return -7;
    if (runtime->attention_span_encoded_key_buffers[slot] == nil || runtime->attention_span_v_buffers[slot] == nil) return -8;
    if (runtime->attention_span_slot_tokens[slot] < kv_tokens || runtime->attention_span_slot_key_row_bytes[slot] != key_row_bytes || runtime->attention_span_slot_position_offset[slot] != kv_position_offset) return -9;
    if (runtime->attention_span_slot_v_row_stride[slot] > UINT32_MAX) return -10;
    if (sinks != NULL && sink_count < num_heads) return -11;
    if (block_count > SIZE_MAX / sizeof(uint32_t)) return -12;
    const uint32_t v_element_bytes = (format == 3u) ? (uint32_t)sizeof(uint16_t) : (uint32_t)sizeof(float);
    switch (format) {
        case 0u:
        case 1u:
        case 2u:
        case 3u:
        case 4u:
            break;
        default:
            return -13;
    }

    termite_metal_paged_attention_params params = {
        .q_len = (uint32_t)q_len,
        .kv_tokens = (uint32_t)kv_tokens,
        .num_heads = (uint32_t)num_heads,
        .num_kv_heads = (uint32_t)num_kv_heads,
        .head_dim = (uint32_t)head_dim,
        .key_row_bytes = (uint32_t)key_row_bytes,
        .base_key_row_bytes = (uint32_t)base_key_row_bytes,
        .query_position_offset = (uint32_t)query_position_offset,
        .kv_position_offset = (uint32_t)kv_position_offset,
        .sliding_window = (uint32_t)sliding_window,
        .v_row_stride = (uint32_t)runtime->attention_span_slot_v_row_stride[slot],
        .page_size = (uint32_t)page_size,
        .block_count = (uint32_t)block_count,
        .format = format,
        .v_element_bytes = v_element_bytes,
        .has_sinks = sinks != NULL ? 1u : 0u,
        .softcap = softcap,
    };
    id<MTLBuffer> params_buffer = [runtime->device newBufferWithBytes:&params length:sizeof(params) options:MTLResourceStorageModeShared];
    id<MTLBuffer> block_table_buffer = [runtime->device newBufferWithBytes:block_table length:block_count * sizeof(uint32_t) options:MTLResourceStorageModeShared];
    const float zero_sink = 0.0f;
    id<MTLBuffer> sinks_buffer = sinks != NULL
        ? [runtime->device newBufferWithBytes:sinks length:num_heads * sizeof(float) options:MTLResourceStorageModeShared]
        : [runtime->device newBufferWithBytes:&zero_sink length:sizeof(zero_sink) options:MTLResourceStorageModeShared];
    if (params_buffer == nil || block_table_buffer == nil || sinks_buffer == nil) return -14;
    if (runtime->active_frame_cb != nil) {
        if (termite_metal_decode_runtime_retain_frame_resource(runtime, params_buffer) != 0) return -14;
        if (termite_metal_decode_runtime_retain_frame_resource(runtime, block_table_buffer) != 0) return -14;
        if (termite_metal_decode_runtime_retain_frame_resource(runtime, sinks_buffer) != 0) return -14;
    }

    id<MTLComputeCommandEncoder> encoder = runtime->active_planned_compute_encoder;
    const BOOL planned_encoder = (encoder != nil);
    if (!planned_encoder) {
        encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_ATTENTION);
        if (encoder == nil) return -15;
    }
    size_t q_bytes = 0;
    if (!termite_metal_size_mul3(q_len, num_heads, head_dim, &q_bytes) ||
        !termite_metal_size_mul(q_bytes, sizeof(float), &q_bytes))
    {
        return -15;
    }
    termite_metal_planned_encoder_range accesses[4];
    if (termite_metal_planned_range_make(q_buffer, q_offset, q_bytes, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[0], -15) != 0 ||
        termite_metal_planned_range_make(runtime->attention_span_encoded_key_buffers[slot], 0, runtime->attention_span_encoded_key_buffers[slot].length, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[1], -15) != 0 ||
        termite_metal_planned_range_make(runtime->attention_span_v_buffers[slot], 0, runtime->attention_span_v_buffers[slot].length, TERMITE_METAL_PLANNED_RANGE_READ, &accesses[2], -15) != 0 ||
        termite_metal_planned_range_make(output_buffer, output_offset, q_bytes, TERMITE_METAL_PLANNED_RANGE_WRITE, &accesses[3], -15) != 0 ||
        termite_metal_decode_runtime_prepare_planned_compute_accesses(runtime, accesses, 4, -15) != 0)
    {
        return -15;
    }
    [encoder setComputePipelineState:runtime->attention_paged_pipeline];
    [encoder setBuffer:q_buffer offset:q_offset atIndex:0];
    [encoder setBuffer:runtime->attention_span_encoded_key_buffers[slot] offset:0 atIndex:1];
    [encoder setBuffer:runtime->attention_span_v_buffers[slot] offset:0 atIndex:2];
    [encoder setBuffer:block_table_buffer offset:0 atIndex:3];
    [encoder setBuffer:sinks_buffer offset:0 atIndex:4];
    [encoder setBuffer:output_buffer offset:output_offset atIndex:5];
    [encoder setBuffer:params_buffer offset:0 atIndex:6];
    const size_t total = q_len * num_heads;
    [encoder dispatchThreads:MTLSizeMake(total, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->attention_paged_pipeline, total), 1, 1)];
    if (!planned_encoder) [encoder endEncoding];
    return 0;
}

int termite_metal_decode_runtime_attention_paged_slot_device(
    termite_metal_decode_runtime *runtime,
    size_t slot,
    uint32_t format,
    void *q_handle,
    size_t q_offset,
    const uint32_t *block_table,
    size_t block_count,
    size_t page_size,
    size_t q_len,
    size_t kv_tokens,
    size_t num_heads,
    size_t num_kv_heads,
    size_t head_dim,
    size_t key_row_bytes,
    size_t base_key_row_bytes,
    size_t query_position_offset,
    size_t kv_position_offset,
    size_t sliding_window,
    float softcap,
    const float *sinks,
    size_t sink_count,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || q_handle == NULL || output_handle == NULL) return -1;
    @autoreleasepool {
        id<MTLBuffer> q_buffer = (__bridge id<MTLBuffer>)q_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        if (q_buffer == nil || output_buffer == nil) return -2;
        const bool frame_active = runtime->active_frame_cb != nil;
        id<MTLCommandBuffer> command_buffer = frame_active ? runtime->active_frame_cb : termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -3;
        const int rc = termite_metal_decode_runtime_encode_paged_attention_slot(
            runtime,
            command_buffer,
            slot,
            format,
            q_buffer,
            q_offset,
            block_table,
            block_count,
            page_size,
            q_len,
            kv_tokens,
            num_heads,
            num_kv_heads,
            head_dim,
            key_row_bytes,
            base_key_row_bytes,
            query_position_offset,
            kv_position_offset,
            sliding_window,
            softcap,
            sinks,
            sink_count,
            output_buffer,
            output_offset
        );
        if (rc != 0) return rc;
        if (frame_active) return 0;
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -4;
    }
}

static int termite_metal_decode_runtime_encode_rms_norm_quantized_linear_slot(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    size_t norm_slot,
    const float *input,
    size_t hidden_size,
    float eps,
    size_t linear_slot,
    id<MTLComputePipelineState> linear_pipeline,
    id<MTLBuffer> linear_weight_buffer,
    size_t out_dim,
    size_t row_blocks,
    float *output
) {
    if (runtime == NULL || command_buffer == nil || input == NULL || output == NULL) return -1;
    if (runtime->rms_norm_pipeline == nil || linear_pipeline == nil || linear_weight_buffer == nil) return -2;
    if (hidden_size == 0 || hidden_size > UINT32_MAX || out_dim == 0 || out_dim > UINT32_MAX) return -3;
    if (termite_metal_decode_runtime_ensure_hot_buffers(runtime, hidden_size * sizeof(float), 0) != 0) return -4;
    @autoreleasepool {
        const size_t input_bytes = hidden_size * sizeof(float);
        const size_t output_bytes = out_dim * sizeof(float);
        id<MTLBuffer> input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)input length:input_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [runtime->device newBufferWithBytes:input length:input_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> output_buffer = [runtime->device newBufferWithBytesNoCopy:output length:output_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) output_buffer = [runtime->device newBufferWithBytes:output length:output_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> normed_buffer = runtime->hot_hidden_buffers[0];
        if (input_buffer == nil || output_buffer == nil || normed_buffer == nil) return -5;

        termite_metal_apply_layer_norm_params norm_params = {
            .hidden_size = (uint32_t)hidden_size,
            .eps = eps,
        };
        id<MTLComputeCommandEncoder> norm_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (norm_encoder == nil) return -6;
        [norm_encoder setComputePipelineState:runtime->rms_norm_pipeline];
        [norm_encoder setBuffer:input_buffer offset:0 atIndex:0];
        [norm_encoder setBuffer:runtime->rms_norm_weight_buffers[norm_slot] offset:0 atIndex:1];
        [norm_encoder setBuffer:normed_buffer offset:0 atIndex:2];
        [norm_encoder setBytes:&norm_params length:sizeof(norm_params) atIndex:3];
        [norm_encoder dispatchThreads:MTLSizeMake(hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->rms_norm_pipeline, hidden_size), 1, 1)];
        [norm_encoder endEncoding];

        termite_metal_linear_params linear_params = {
            .rows = 1,
            .in_dim = (uint32_t)hidden_size,
            .out_dim = (uint32_t)out_dim,
            .row_blocks = (uint32_t)row_blocks,
        };
        id<MTLComputeCommandEncoder> linear_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (linear_encoder == nil) return -7;
        [linear_encoder setComputePipelineState:linear_pipeline];
        [linear_encoder setBuffer:normed_buffer offset:0 atIndex:0];
        [linear_encoder setBuffer:linear_weight_buffer offset:0 atIndex:1];
        [linear_encoder setBuffer:output_buffer offset:0 atIndex:2];
        [linear_encoder setBytes:&linear_params length:sizeof(linear_params) atIndex:3];
        [linear_encoder dispatchThreads:MTLSizeMake(out_dim, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(linear_pipeline, out_dim), 1, 1)];
        [linear_encoder endEncoding];
        return 0;
    }
}

int termite_metal_decode_runtime_apply_rms_norm_q4_k_linear_slot(
    termite_metal_decode_runtime *runtime,
    size_t norm_slot,
    size_t linear_slot,
    const float *input,
    size_t hidden_size,
    float eps,
    size_t out_dim,
    float *output
) {
    if (runtime == NULL || input == NULL || output == NULL) return -1;
    if (runtime->q4_k_pipeline == nil) return -2;
    if (norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY || linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -3;
    if (runtime->rms_norm_slot_prepared[norm_slot] == 0 || runtime->rms_norm_hidden_sizes[norm_slot] != hidden_size) return -4;
    termite_metal_quant_linear_slot_view linear_view;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q4_K, linear_slot, hidden_size, out_dim, &linear_view) != 0) return -5;
    if (hidden_size % 256u != 0) return -6;
    @autoreleasepool {
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -7;
        int rc = termite_metal_decode_runtime_encode_rms_norm_quantized_linear_slot(
            runtime,
            command_buffer,
            norm_slot,
            input,
            hidden_size,
            eps,
            linear_slot,
            linear_view.pipeline,
            linear_view.weight_buffer,
            out_dim,
            hidden_size / 256u,
            output
        );
        if (rc != 0) return rc;
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -8;
    }
}

int termite_metal_decode_runtime_apply_rms_norm_q5_k_linear_slot(
    termite_metal_decode_runtime *runtime,
    size_t norm_slot,
    size_t linear_slot,
    const float *input,
    size_t hidden_size,
    float eps,
    size_t out_dim,
    float *output
) {
    if (runtime == NULL || input == NULL || output == NULL) return -1;
    if (runtime->q5_k_pipeline == nil) return -2;
    if (norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY || linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -3;
    if (runtime->rms_norm_slot_prepared[norm_slot] == 0 || runtime->rms_norm_hidden_sizes[norm_slot] != hidden_size) return -4;
    termite_metal_quant_linear_slot_view linear_view;
    if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q5_K, linear_slot, hidden_size, out_dim, &linear_view) != 0) return -5;
    if (hidden_size % 256u != 0) return -6;
    @autoreleasepool {
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -7;
        int rc = termite_metal_decode_runtime_encode_rms_norm_quantized_linear_slot(
            runtime,
            command_buffer,
            norm_slot,
            input,
            hidden_size,
            eps,
            linear_slot,
            linear_view.pipeline,
            linear_view.weight_buffer,
            out_dim,
            hidden_size / 256u,
            output
        );
        if (rc != 0) return rc;
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -8;
    }
}

static int termite_metal_decode_runtime_encode_attention_span(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    uint32_t format,
    id<MTLBuffer> q_buffer,
    size_t q_offset,
    size_t kv_tokens,
    size_t num_heads,
    size_t num_kv_heads,
    size_t head_dim,
    size_t key_row_bytes,
    size_t base_key_row_bytes,
    size_t query_position,
    size_t kv_position_offset,
    size_t sliding_window,
    id<MTLBuffer> output_buffer,
    size_t output_offset
) {
    return termite_metal_decode_runtime_encode_attention_span_slot(
        runtime,
        command_buffer,
        0,
        format,
        q_buffer,
        q_offset,
        kv_tokens,
        num_heads,
        num_kv_heads,
        head_dim,
        key_row_bytes,
        base_key_row_bytes,
        query_position,
        kv_position_offset,
        sliding_window,
        output_buffer,
        output_offset
    );
}

static int termite_metal_decode_runtime_encode_attention_span_slot(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    size_t slot,
    uint32_t format,
    id<MTLBuffer> q_buffer,
    size_t q_offset,
    size_t kv_tokens,
    size_t num_heads,
    size_t num_kv_heads,
    size_t head_dim,
    size_t key_row_bytes,
    size_t base_key_row_bytes,
    size_t query_position,
    size_t kv_position_offset,
    size_t sliding_window,
    id<MTLBuffer> output_buffer,
    size_t output_offset
) {
    if (runtime == NULL || command_buffer == nil || q_buffer == nil || output_buffer == nil) return -1;
    if (kv_tokens == 0 || num_heads == 0 || num_kv_heads == 0 || head_dim == 0) return -2;
    if (num_heads > UINT32_MAX || num_kv_heads > UINT32_MAX || head_dim > UINT32_MAX || kv_tokens > UINT32_MAX) return -3;
    if (key_row_bytes > UINT32_MAX || base_key_row_bytes > UINT32_MAX || query_position > UINT32_MAX || kv_position_offset > UINT32_MAX || sliding_window > UINT32_MAX) return -4;
    if (slot >= TERMITE_METAL_ATTENTION_SPAN_SLOT_CAPACITY) return -5;
    if (runtime->attention_span_encoded_key_buffers[slot] == nil || runtime->attention_span_v_buffers[slot] == nil) return -6;
    if (runtime->attention_span_slot_tokens[slot] != kv_tokens || runtime->attention_span_slot_key_row_bytes[slot] != key_row_bytes || runtime->attention_span_slot_position_offset[slot] != kv_position_offset) return -7;
    id<MTLComputePipelineState> pipeline = nil;
    switch (format) {
        case 0u: pipeline = runtime->polar4_attention_span_pipeline; break;
        case 1u: pipeline = runtime->turbo3_attention_span_pipeline; break;
        default: return -8;
    }
    if (pipeline == nil) return -9;
    termite_metal_attention_span_params params = {
        .kv_tokens = (uint32_t)kv_tokens,
        .num_heads = (uint32_t)num_heads,
        .num_kv_heads = (uint32_t)num_kv_heads,
        .head_dim = (uint32_t)head_dim,
        .key_row_bytes = (uint32_t)key_row_bytes,
        .base_key_row_bytes = (uint32_t)base_key_row_bytes,
        .query_position = (uint32_t)query_position,
        .kv_position_offset = (uint32_t)kv_position_offset,
        .sliding_window = (uint32_t)sliding_window,
        .v_row_stride = (uint32_t)runtime->attention_span_slot_v_row_stride[slot],
    };
    id<MTLBuffer> params_buffer = [runtime->device newBufferWithBytes:&params length:sizeof(params) options:MTLResourceStorageModeShared];
    if (params_buffer == nil) return -10;
    id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
    if (encoder == nil) return -11;
    [encoder setComputePipelineState:pipeline];
    [encoder setBuffer:q_buffer offset:q_offset atIndex:0];
    [encoder setBuffer:runtime->attention_span_encoded_key_buffers[slot] offset:0 atIndex:1];
    [encoder setBuffer:runtime->attention_span_v_buffers[slot] offset:0 atIndex:2];
    [encoder setBuffer:output_buffer offset:output_offset atIndex:3];
    [encoder setBuffer:params_buffer offset:0 atIndex:4];
    [encoder dispatchThreads:MTLSizeMake(num_heads, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(pipeline, num_heads), 1, 1)];
    [encoder endEncoding];
    return 0;
}

static int termite_metal_apply_optional_norm(
    termite_metal_decode_runtime *runtime,
    size_t layer_norm_slot,
    size_t rms_norm_slot,
    const float *input,
    size_t hidden_size,
    float eps,
    float *output
) {
    if (layer_norm_slot != SIZE_MAX) {
        return termite_metal_decode_runtime_apply_layer_norm(
            runtime,
            layer_norm_slot,
            input,
            1,
            hidden_size,
            eps,
            output
        );
    }
    if (rms_norm_slot != SIZE_MAX) {
        return termite_metal_decode_runtime_apply_rms_norm(
            runtime,
            rms_norm_slot,
            input,
            hidden_size,
            eps,
            output
        );
    }
    if (input != output) {
        memcpy(output, input, hidden_size * sizeof(float));
    }
    return 0;
}

static NSUInteger termite_metal_thread_width(id<MTLComputePipelineState> pipeline, size_t dim) {
    NSUInteger thread_width = pipeline.maxTotalThreadsPerThreadgroup;
    if (thread_width == 0) thread_width = 64;
    if (dim > 0 && thread_width > dim) thread_width = dim;
    return thread_width;
}

static NSUInteger termite_metal_threadgroup_memory_16(NSUInteger bytes) {
    return (bytes + 15u) & ~(NSUInteger)15u;
}

static int termite_metal_decode_runtime_encode_attention_block_prefix(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    id<MTLBuffer> attention_input_buffer,
    size_t attention_input_offset,
    size_t attention_pre_linear_rms_norm_slot,
    size_t attention_linear_slot,
    size_t attention_post_linear_rms_norm_slot,
    id<MTLBuffer> residual_buffer,
    size_t residual_offset,
    size_t attention_input_size,
    size_t hidden_size,
    float eps,
    size_t ffn_layer_norm_slot,
    size_t ffn_rms_norm_slot,
    id<MTLBuffer> attn_added_buffer,
    size_t attn_added_offset,
    id<MTLBuffer> ffn_input_buffer,
    size_t ffn_input_offset
) {
    if (runtime == NULL || command_buffer == nil || attention_input_buffer == nil || residual_buffer == nil || attn_added_buffer == nil || ffn_input_buffer == nil) return -1;
    if (runtime->linear_pipeline == nil || runtime->linear_reduce_pipeline == nil || runtime->add_pipeline == nil) return -2;
    if (attention_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || runtime->linear_slot_prepared[attention_linear_slot] == 0) return -3;
    if (runtime->linear_in_dims[attention_linear_slot] != attention_input_size || runtime->linear_out_dims[attention_linear_slot] != hidden_size) return -4;
    if (attention_input_size == 0 || hidden_size == 0 || attention_input_size > UINT32_MAX || hidden_size > UINT32_MAX) return -5;
    @autoreleasepool {
        const size_t hidden_bytes = hidden_size * sizeof(float);
        const size_t attention_input_bytes = attention_input_size * sizeof(float);
        const size_t scratch_bytes = attention_input_bytes > hidden_bytes ? attention_input_bytes : hidden_bytes;
        id<MTLBuffer> tmp_a = [runtime->device newBufferWithLength:scratch_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> tmp_b = [runtime->device newBufferWithLength:scratch_bytes options:MTLResourceStorageModePrivate];
        if (tmp_a == nil || tmp_b == nil) return -6;

        id<MTLBuffer> current_buffer = attention_input_buffer;
        NSUInteger current_offset = attention_input_offset;
        if (attention_pre_linear_rms_norm_slot != SIZE_MAX) {
            termite_metal_apply_layer_norm_params params = { .hidden_size = (uint32_t)attention_input_size, .eps = eps };
            id<MTLBuffer> params_buffer = [runtime->device newBufferWithBytes:&params length:sizeof(params) options:MTLResourceStorageModeShared];
            if (params_buffer == nil) return -7;
            id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_ATTENTION);
            if (encoder == nil) return -8;
            [encoder setComputePipelineState:runtime->rms_norm_pipeline];
            [encoder setBuffer:current_buffer offset:current_offset atIndex:0];
            [encoder setBuffer:runtime->rms_norm_weight_buffers[attention_pre_linear_rms_norm_slot] offset:0 atIndex:1];
            [encoder setBuffer:tmp_a offset:0 atIndex:2];
            [encoder setBuffer:params_buffer offset:0 atIndex:3];
            [encoder dispatchThreads:MTLSizeMake(1, 1, 1) threadsPerThreadgroup:MTLSizeMake(1, 1, 1)];
            [encoder endEncoding];
            current_buffer = tmp_a;
            current_offset = 0;
        }

        {
            termite_metal_apply_linear_params params = { .in_dim = (uint32_t)attention_input_size, .out_dim = (uint32_t)hidden_size };
            id<MTLBuffer> params_buffer = [runtime->device newBufferWithBytes:&params length:sizeof(params) options:MTLResourceStorageModeShared];
            if (params_buffer == nil) return -9;
            id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
            if (encoder == nil) return -10;
            const BOOL use_reduce = hidden_size >= 128;
            [encoder setComputePipelineState:(use_reduce ? runtime->linear_reduce_pipeline : runtime->linear_pipeline)];
            [encoder setBuffer:current_buffer offset:current_offset atIndex:0];
            [encoder setBuffer:runtime->linear_weight_buffers[attention_linear_slot] offset:0 atIndex:1];
            [encoder setBuffer:runtime->linear_bias_buffers[attention_linear_slot] offset:0 atIndex:2];
            [encoder setBuffer:tmp_b offset:0 atIndex:3];
            [encoder setBuffer:params_buffer offset:0 atIndex:4];
            if (use_reduce) {
                [encoder dispatchThreads:MTLSizeMake(hidden_size * 32u, 1, 1) threadsPerThreadgroup:MTLSizeMake(32, 1, 1)];
            } else {
                [encoder dispatchThreads:MTLSizeMake(hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->linear_pipeline, hidden_size), 1, 1)];
            }
            [encoder endEncoding];
            current_buffer = tmp_b;
            current_offset = 0;
        }

        if (attention_post_linear_rms_norm_slot != SIZE_MAX) {
            termite_metal_apply_layer_norm_params params = { .hidden_size = (uint32_t)hidden_size, .eps = eps };
            id<MTLBuffer> params_buffer = [runtime->device newBufferWithBytes:&params length:sizeof(params) options:MTLResourceStorageModeShared];
            if (params_buffer == nil) return -11;
            id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
            if (encoder == nil) return -12;
            [encoder setComputePipelineState:runtime->rms_norm_pipeline];
            [encoder setBuffer:current_buffer offset:current_offset atIndex:0];
            [encoder setBuffer:runtime->rms_norm_weight_buffers[attention_post_linear_rms_norm_slot] offset:0 atIndex:1];
            [encoder setBuffer:tmp_a offset:0 atIndex:2];
            [encoder setBuffer:params_buffer offset:0 atIndex:3];
            [encoder dispatchThreads:MTLSizeMake(1, 1, 1) threadsPerThreadgroup:MTLSizeMake(1, 1, 1)];
            [encoder endEncoding];
            current_buffer = tmp_a;
            current_offset = 0;
        }

        {
            termite_metal_apply_add_params params = { .rows = 1, .dim = (uint32_t)hidden_size };
            id<MTLBuffer> params_buffer = [runtime->device newBufferWithBytes:&params length:sizeof(params) options:MTLResourceStorageModeShared];
            if (params_buffer == nil) return -13;
            id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
            if (encoder == nil) return -14;
            [encoder setComputePipelineState:runtime->add_pipeline];
            [encoder setBuffer:current_buffer offset:current_offset atIndex:0];
            [encoder setBuffer:residual_buffer offset:residual_offset atIndex:1];
            [encoder setBuffer:attn_added_buffer offset:attn_added_offset atIndex:2];
            [encoder setBuffer:params_buffer offset:0 atIndex:3];
            [encoder dispatchThreads:MTLSizeMake(hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->add_pipeline, hidden_size), 1, 1)];
            [encoder endEncoding];
        }

        if (ffn_layer_norm_slot != SIZE_MAX) {
            termite_metal_apply_layer_norm_params params = { .hidden_size = (uint32_t)hidden_size, .eps = eps };
            id<MTLBuffer> params_buffer = [runtime->device newBufferWithBytes:&params length:sizeof(params) options:MTLResourceStorageModeShared];
            if (params_buffer == nil) return -15;
            id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
            if (encoder == nil) return -16;
            [encoder setComputePipelineState:runtime->layer_norm_pipeline];
            [encoder setBuffer:attn_added_buffer offset:attn_added_offset atIndex:0];
            [encoder setBuffer:runtime->layer_norm_weight_buffers[ffn_layer_norm_slot] offset:0 atIndex:1];
            [encoder setBuffer:runtime->layer_norm_bias_buffers[ffn_layer_norm_slot] offset:0 atIndex:2];
            [encoder setBuffer:ffn_input_buffer offset:ffn_input_offset atIndex:3];
            [encoder setBuffer:params_buffer offset:0 atIndex:4];
            [encoder dispatchThreads:MTLSizeMake(1, 1, 1) threadsPerThreadgroup:MTLSizeMake(1, 1, 1)];
            [encoder endEncoding];
        } else if (ffn_rms_norm_slot != SIZE_MAX) {
            termite_metal_apply_layer_norm_params params = { .hidden_size = (uint32_t)hidden_size, .eps = eps };
            id<MTLBuffer> params_buffer = [runtime->device newBufferWithBytes:&params length:sizeof(params) options:MTLResourceStorageModeShared];
            if (params_buffer == nil) return -17;
            id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
            if (encoder == nil) return -18;
            [encoder setComputePipelineState:runtime->rms_norm_pipeline];
            [encoder setBuffer:attn_added_buffer offset:attn_added_offset atIndex:0];
            [encoder setBuffer:runtime->rms_norm_weight_buffers[ffn_rms_norm_slot] offset:0 atIndex:1];
            [encoder setBuffer:ffn_input_buffer offset:ffn_input_offset atIndex:2];
            [encoder setBuffer:params_buffer offset:0 atIndex:3];
            [encoder dispatchThreads:MTLSizeMake(1, 1, 1) threadsPerThreadgroup:MTLSizeMake(1, 1, 1)];
            [encoder endEncoding];
        } else {
            termite_metal_record_active_frame_blit_source(command_buffer, TERMITE_METAL_BLIT_SOURCE_FFN_COPY);
            id<MTLBlitCommandEncoder> blit_encoder = termite_metal_tracked_blit_command_encoder(command_buffer);
            if (blit_encoder == nil) return -19;
            [blit_encoder copyFromBuffer:attn_added_buffer sourceOffset:attn_added_offset toBuffer:ffn_input_buffer destinationOffset:ffn_input_offset size:hidden_bytes];
            [blit_encoder endEncoding];
        }
        return 0;
    }
}

int termite_metal_decode_runtime_apply_attention_block_prefix(
    termite_metal_decode_runtime *runtime,
    const float *attention_input,
    size_t attention_pre_linear_rms_norm_slot,
    size_t attention_linear_slot,
    size_t attention_post_linear_rms_norm_slot,
    const float *residual,
    size_t attention_input_size,
    size_t hidden_size,
    float eps,
    size_t ffn_layer_norm_slot,
    size_t ffn_rms_norm_slot,
    float *attn_added,
    float *ffn_input
) {
    if (runtime == NULL || attention_input == NULL || residual == NULL || attn_added == NULL || ffn_input == NULL) return -1;
    if (attention_input_size == 0 || hidden_size == 0) return -2;
    @autoreleasepool {
        const size_t attention_input_bytes = attention_input_size * sizeof(float);
        const size_t hidden_bytes = hidden_size * sizeof(float);
        id<MTLBuffer> attention_input_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)attention_input length:attention_input_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (attention_input_buffer == nil) attention_input_buffer = [runtime->device newBufferWithBytes:attention_input length:attention_input_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> residual_buffer = [runtime->device newBufferWithBytesNoCopy:(void *)residual length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (residual_buffer == nil) residual_buffer = [runtime->device newBufferWithBytes:residual length:hidden_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> attn_added_buffer = [runtime->device newBufferWithBytesNoCopy:attn_added length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (attn_added_buffer == nil) attn_added_buffer = [runtime->device newBufferWithBytes:attn_added length:hidden_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> ffn_input_buffer = [runtime->device newBufferWithBytesNoCopy:ffn_input length:hidden_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (ffn_input_buffer == nil) ffn_input_buffer = [runtime->device newBufferWithBytes:ffn_input length:hidden_bytes options:MTLResourceStorageModeShared];
        if (attention_input_buffer == nil || residual_buffer == nil || attn_added_buffer == nil || ffn_input_buffer == nil) return -3;

        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -4;

        const int rc = termite_metal_decode_runtime_encode_attention_block_prefix(
            runtime,
            command_buffer,
            attention_input_buffer,
            0,
            attention_pre_linear_rms_norm_slot,
            attention_linear_slot,
            attention_post_linear_rms_norm_slot,
            residual_buffer,
            0,
            attention_input_size,
            hidden_size,
            eps,
            ffn_layer_norm_slot,
            ffn_rms_norm_slot,
            attn_added_buffer,
            0,
            ffn_input_buffer,
            0
        );
        if (rc != 0) return rc;

        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -5;
    }
}

static int termite_metal_decode_runtime_encode_dense_ffn_residual(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    size_t first_linear_slot,
    size_t second_linear_slot,
    id<MTLBuffer> input_buffer,
    id<MTLBuffer> residual_buffer,
    size_t hidden_size,
    size_t intermediate_size,
    uint32_t activation_kind,
    id<MTLBuffer> output_buffer
) {
    if (runtime == NULL || command_buffer == nil || input_buffer == nil || residual_buffer == nil || output_buffer == nil) return -1;
    if (runtime->linear_pipeline == nil || runtime->activation_pipeline == nil || runtime->add_pipeline == nil) return -2;
    if (first_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || second_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -3;
    if (hidden_size == 0 || intermediate_size == 0) return -4;
    if (runtime->linear_slot_prepared[first_linear_slot] == 0 || runtime->linear_slot_prepared[second_linear_slot] == 0) return -5;
    if (runtime->linear_in_dims[first_linear_slot] != hidden_size || runtime->linear_out_dims[first_linear_slot] != intermediate_size) return -6;
    if (runtime->linear_in_dims[second_linear_slot] != intermediate_size || runtime->linear_out_dims[second_linear_slot] != hidden_size) return -7;
    if (hidden_size > UINT32_MAX || intermediate_size > UINT32_MAX) return -8;
    @autoreleasepool {
        const size_t hidden_bytes = hidden_size * sizeof(float);
        const size_t intermediate_bytes = intermediate_size * sizeof(float);
        id<MTLBuffer> first_output_buffer = [runtime->device newBufferWithLength:intermediate_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> activated_buffer = [runtime->device newBufferWithLength:intermediate_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> second_output_buffer = [runtime->device newBufferWithLength:hidden_bytes options:MTLResourceStorageModePrivate];
        if (first_output_buffer == nil || activated_buffer == nil || second_output_buffer == nil) return -9;
        termite_metal_apply_linear_params first_linear_params = { .in_dim = (uint32_t)hidden_size, .out_dim = (uint32_t)intermediate_size };
        termite_metal_apply_activation_params activation_params = { .activation_kind = activation_kind, .rows = 1, .dim = (uint32_t)intermediate_size };
        termite_metal_apply_linear_params second_linear_params = { .in_dim = (uint32_t)intermediate_size, .out_dim = (uint32_t)hidden_size };
        termite_metal_apply_add_params add_params = { .rows = 1, .dim = (uint32_t)hidden_size };
        id<MTLBuffer> first_linear_params_buffer = [runtime->device newBufferWithBytes:&first_linear_params length:sizeof(first_linear_params) options:MTLResourceStorageModeShared];
        id<MTLBuffer> activation_params_buffer = [runtime->device newBufferWithBytes:&activation_params length:sizeof(activation_params) options:MTLResourceStorageModeShared];
        id<MTLBuffer> second_linear_params_buffer = [runtime->device newBufferWithBytes:&second_linear_params length:sizeof(second_linear_params) options:MTLResourceStorageModeShared];
        id<MTLBuffer> add_params_buffer = [runtime->device newBufferWithBytes:&add_params length:sizeof(add_params) options:MTLResourceStorageModeShared];
        if (first_linear_params_buffer == nil || activation_params_buffer == nil || second_linear_params_buffer == nil || add_params_buffer == nil) return -10;

        id<MTLComputeCommandEncoder> first_linear_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (first_linear_encoder == nil) return -11;
        [first_linear_encoder setComputePipelineState:runtime->linear_pipeline];
        [first_linear_encoder setBuffer:input_buffer offset:0 atIndex:0];
        [first_linear_encoder setBuffer:runtime->linear_weight_buffers[first_linear_slot] offset:0 atIndex:1];
        [first_linear_encoder setBuffer:runtime->linear_bias_buffers[first_linear_slot] offset:0 atIndex:2];
        [first_linear_encoder setBuffer:first_output_buffer offset:0 atIndex:3];
        [first_linear_encoder setBuffer:first_linear_params_buffer offset:0 atIndex:4];
        [first_linear_encoder dispatchThreads:MTLSizeMake(intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->linear_pipeline, intermediate_size), 1, 1)];
        [first_linear_encoder endEncoding];

        id<MTLComputeCommandEncoder> activation_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (activation_encoder == nil) return -12;
        [activation_encoder setComputePipelineState:runtime->activation_pipeline];
        [activation_encoder setBuffer:first_output_buffer offset:0 atIndex:0];
        [activation_encoder setBuffer:activated_buffer offset:0 atIndex:1];
        [activation_encoder setBuffer:activation_params_buffer offset:0 atIndex:2];
        [activation_encoder dispatchThreads:MTLSizeMake(intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->activation_pipeline, intermediate_size), 1, 1)];
        [activation_encoder endEncoding];

        id<MTLComputeCommandEncoder> second_linear_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (second_linear_encoder == nil) return -13;
        [second_linear_encoder setComputePipelineState:runtime->linear_pipeline];
        [second_linear_encoder setBuffer:activated_buffer offset:0 atIndex:0];
        [second_linear_encoder setBuffer:runtime->linear_weight_buffers[second_linear_slot] offset:0 atIndex:1];
        [second_linear_encoder setBuffer:runtime->linear_bias_buffers[second_linear_slot] offset:0 atIndex:2];
        [second_linear_encoder setBuffer:second_output_buffer offset:0 atIndex:3];
        [second_linear_encoder setBuffer:second_linear_params_buffer offset:0 atIndex:4];
        [second_linear_encoder dispatchThreads:MTLSizeMake(hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->linear_pipeline, hidden_size), 1, 1)];
        [second_linear_encoder endEncoding];

        id<MTLComputeCommandEncoder> add_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (add_encoder == nil) return -14;
        [add_encoder setComputePipelineState:runtime->add_pipeline];
        [add_encoder setBuffer:second_output_buffer offset:0 atIndex:0];
        [add_encoder setBuffer:residual_buffer offset:0 atIndex:1];
        [add_encoder setBuffer:output_buffer offset:0 atIndex:2];
        [add_encoder setBuffer:add_params_buffer offset:0 atIndex:3];
        [add_encoder dispatchThreads:MTLSizeMake(hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->add_pipeline, hidden_size), 1, 1)];
        [add_encoder endEncoding];
        return 0;
    }
}

static int termite_metal_decode_runtime_encode_gated_ffn_residual(
    termite_metal_decode_runtime *runtime,
    id<MTLCommandBuffer> command_buffer,
    size_t gate_linear_slot,
    size_t up_linear_slot,
    size_t down_linear_slot,
    size_t post_gate_rms_norm_slot,
    id<MTLBuffer> input_buffer,
    size_t input_offset,
    id<MTLBuffer> residual_buffer,
    size_t residual_offset,
    size_t hidden_size,
    size_t intermediate_size,
    uint32_t activation_kind,
    id<MTLBuffer> output_buffer,
    size_t output_offset
) {
    if (runtime == NULL || command_buffer == nil || input_buffer == nil || residual_buffer == nil || output_buffer == nil) return -1;
    if (runtime->linear_pipeline == nil || runtime->activation_pipeline == nil || runtime->multiply_pipeline == nil || runtime->add_pipeline == nil) return -2;
    if (gate_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || up_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY || down_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -3;
    if (hidden_size == 0 || intermediate_size == 0) return -4;
    if (runtime->linear_slot_prepared[gate_linear_slot] == 0 || runtime->linear_slot_prepared[up_linear_slot] == 0 || runtime->linear_slot_prepared[down_linear_slot] == 0) return -5;
    if (runtime->linear_in_dims[gate_linear_slot] != hidden_size || runtime->linear_out_dims[gate_linear_slot] != intermediate_size) return -6;
    if (runtime->linear_in_dims[up_linear_slot] != hidden_size || runtime->linear_out_dims[up_linear_slot] != intermediate_size) return -7;
    if (runtime->linear_in_dims[down_linear_slot] != intermediate_size || runtime->linear_out_dims[down_linear_slot] != hidden_size) return -8;
    if (post_gate_rms_norm_slot != SIZE_MAX) {
        if (post_gate_rms_norm_slot >= TERMITE_METAL_RMS_NORM_SLOT_CAPACITY) return -9;
        if (runtime->rms_norm_slot_prepared[post_gate_rms_norm_slot] == 0) return -10;
        if (runtime->rms_norm_hidden_sizes[post_gate_rms_norm_slot] != intermediate_size) return -11;
    }
    if (hidden_size > UINT32_MAX || intermediate_size > UINT32_MAX) return -9;
    @autoreleasepool {
        const size_t hidden_bytes = hidden_size * sizeof(float);
        const size_t intermediate_bytes = intermediate_size * sizeof(float);
        id<MTLBuffer> gate_output_buffer = [runtime->device newBufferWithLength:intermediate_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> up_output_buffer = [runtime->device newBufferWithLength:intermediate_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> activated_buffer = [runtime->device newBufferWithLength:intermediate_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> gated_buffer = [runtime->device newBufferWithLength:intermediate_bytes options:MTLResourceStorageModePrivate];
        id<MTLBuffer> normed_gated_buffer = post_gate_rms_norm_slot != SIZE_MAX ? [runtime->device newBufferWithLength:intermediate_bytes options:MTLResourceStorageModePrivate] : nil;
        id<MTLBuffer> projected_buffer = [runtime->device newBufferWithLength:hidden_bytes options:MTLResourceStorageModePrivate];
        if (gate_output_buffer == nil || up_output_buffer == nil || activated_buffer == nil || gated_buffer == nil || projected_buffer == nil || (post_gate_rms_norm_slot != SIZE_MAX && normed_gated_buffer == nil)) return -12;
        termite_metal_apply_linear_params pair_linear_params = { .in_dim = (uint32_t)hidden_size, .out_dim = (uint32_t)intermediate_size };
        termite_metal_apply_activation_params activation_params = { .activation_kind = activation_kind, .rows = 1, .dim = (uint32_t)intermediate_size };
        termite_metal_apply_add_params multiply_params = { .rows = 1, .dim = (uint32_t)intermediate_size };
        termite_metal_apply_layer_norm_params gated_rms_norm_params = { .hidden_size = (uint32_t)intermediate_size, .eps = 0.0f };
        termite_metal_apply_linear_params down_linear_params = { .in_dim = (uint32_t)intermediate_size, .out_dim = (uint32_t)hidden_size };
        termite_metal_apply_add_params add_params = { .rows = 1, .dim = (uint32_t)hidden_size };
        id<MTLBuffer> pair_linear_params_buffer = [runtime->device newBufferWithBytes:&pair_linear_params length:sizeof(pair_linear_params) options:MTLResourceStorageModeShared];
        id<MTLBuffer> activation_params_buffer = [runtime->device newBufferWithBytes:&activation_params length:sizeof(activation_params) options:MTLResourceStorageModeShared];
        id<MTLBuffer> multiply_params_buffer = [runtime->device newBufferWithBytes:&multiply_params length:sizeof(multiply_params) options:MTLResourceStorageModeShared];
        id<MTLBuffer> gated_rms_norm_params_buffer = post_gate_rms_norm_slot != SIZE_MAX ? [runtime->device newBufferWithBytes:&gated_rms_norm_params length:sizeof(gated_rms_norm_params) options:MTLResourceStorageModeShared] : nil;
        id<MTLBuffer> down_linear_params_buffer = [runtime->device newBufferWithBytes:&down_linear_params length:sizeof(down_linear_params) options:MTLResourceStorageModeShared];
        id<MTLBuffer> add_params_buffer = [runtime->device newBufferWithBytes:&add_params length:sizeof(add_params) options:MTLResourceStorageModeShared];
        if (pair_linear_params_buffer == nil || activation_params_buffer == nil || multiply_params_buffer == nil || down_linear_params_buffer == nil || add_params_buffer == nil || (post_gate_rms_norm_slot != SIZE_MAX && gated_rms_norm_params_buffer == nil)) return -13;

        const BOOL use_gate_reduce = intermediate_size >= 128;
        if (use_gate_reduce) {
            id<MTLComputeCommandEncoder> pair_linear_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
            if (pair_linear_encoder == nil) return -12;
            [pair_linear_encoder setComputePipelineState:runtime->linear_pair_reduce_pipeline];
            [pair_linear_encoder setBuffer:input_buffer offset:input_offset atIndex:0];
            [pair_linear_encoder setBuffer:runtime->linear_weight_buffers[gate_linear_slot] offset:0 atIndex:1];
            [pair_linear_encoder setBuffer:runtime->linear_weight_buffers[up_linear_slot] offset:0 atIndex:2];
            [pair_linear_encoder setBuffer:runtime->linear_bias_buffers[gate_linear_slot] offset:0 atIndex:3];
            [pair_linear_encoder setBuffer:runtime->linear_bias_buffers[up_linear_slot] offset:0 atIndex:4];
            [pair_linear_encoder setBuffer:gate_output_buffer offset:0 atIndex:5];
            [pair_linear_encoder setBuffer:up_output_buffer offset:0 atIndex:6];
            [pair_linear_encoder setBuffer:pair_linear_params_buffer offset:0 atIndex:7];
            [pair_linear_encoder dispatchThreads:MTLSizeMake(intermediate_size * 32u, 1, 1) threadsPerThreadgroup:MTLSizeMake(32, 1, 1)];
            [pair_linear_encoder endEncoding];
        } else {
            id<MTLComputeCommandEncoder> gate_linear_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
            if (gate_linear_encoder == nil) return -12;
            [gate_linear_encoder setComputePipelineState:runtime->linear_pipeline];
            [gate_linear_encoder setBuffer:input_buffer offset:input_offset atIndex:0];
            [gate_linear_encoder setBuffer:runtime->linear_weight_buffers[gate_linear_slot] offset:0 atIndex:1];
            [gate_linear_encoder setBuffer:runtime->linear_bias_buffers[gate_linear_slot] offset:0 atIndex:2];
            [gate_linear_encoder setBuffer:gate_output_buffer offset:0 atIndex:3];
            [gate_linear_encoder setBuffer:pair_linear_params_buffer offset:0 atIndex:4];
            [gate_linear_encoder dispatchThreads:MTLSizeMake(intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->linear_pipeline, intermediate_size), 1, 1)];
            [gate_linear_encoder endEncoding];

            id<MTLComputeCommandEncoder> up_linear_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
            if (up_linear_encoder == nil) return -13;
            [up_linear_encoder setComputePipelineState:runtime->linear_pipeline];
            [up_linear_encoder setBuffer:input_buffer offset:input_offset atIndex:0];
            [up_linear_encoder setBuffer:runtime->linear_weight_buffers[up_linear_slot] offset:0 atIndex:1];
            [up_linear_encoder setBuffer:runtime->linear_bias_buffers[up_linear_slot] offset:0 atIndex:2];
            [up_linear_encoder setBuffer:up_output_buffer offset:0 atIndex:3];
            [up_linear_encoder setBuffer:pair_linear_params_buffer offset:0 atIndex:4];
            [up_linear_encoder dispatchThreads:MTLSizeMake(intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->linear_pipeline, intermediate_size), 1, 1)];
            [up_linear_encoder endEncoding];
        }

        id<MTLComputeCommandEncoder> activation_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (activation_encoder == nil) return -14;
        [activation_encoder setComputePipelineState:runtime->activation_pipeline];
        [activation_encoder setBuffer:gate_output_buffer offset:0 atIndex:0];
        [activation_encoder setBuffer:activated_buffer offset:0 atIndex:1];
        [activation_encoder setBuffer:activation_params_buffer offset:0 atIndex:2];
        [activation_encoder dispatchThreads:MTLSizeMake(intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->activation_pipeline, intermediate_size), 1, 1)];
        [activation_encoder endEncoding];

        id<MTLComputeCommandEncoder> multiply_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (multiply_encoder == nil) return -15;
        [multiply_encoder setComputePipelineState:runtime->multiply_pipeline];
        [multiply_encoder setBuffer:activated_buffer offset:0 atIndex:0];
        [multiply_encoder setBuffer:up_output_buffer offset:0 atIndex:1];
        [multiply_encoder setBuffer:gated_buffer offset:0 atIndex:2];
        [multiply_encoder setBuffer:multiply_params_buffer offset:0 atIndex:3];
        [multiply_encoder dispatchThreads:MTLSizeMake(intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->multiply_pipeline, intermediate_size), 1, 1)];
        [multiply_encoder endEncoding];

        id<MTLBuffer> down_input_buffer = gated_buffer;
        if (post_gate_rms_norm_slot != SIZE_MAX) {
            if (runtime->rms_norm_pipeline == nil) return -16;
            id<MTLComputeCommandEncoder> gated_rms_norm_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
            if (gated_rms_norm_encoder == nil) return -17;
            [gated_rms_norm_encoder setComputePipelineState:runtime->rms_norm_pipeline];
            [gated_rms_norm_encoder setBuffer:gated_buffer offset:0 atIndex:0];
            [gated_rms_norm_encoder setBuffer:runtime->rms_norm_weight_buffers[post_gate_rms_norm_slot] offset:0 atIndex:1];
            [gated_rms_norm_encoder setBuffer:normed_gated_buffer offset:0 atIndex:2];
            [gated_rms_norm_encoder setBuffer:gated_rms_norm_params_buffer offset:0 atIndex:3];
            [gated_rms_norm_encoder dispatchThreads:MTLSizeMake(intermediate_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->rms_norm_pipeline, intermediate_size), 1, 1)];
            [gated_rms_norm_encoder endEncoding];
            down_input_buffer = normed_gated_buffer;
        }

        id<MTLComputeCommandEncoder> down_linear_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (down_linear_encoder == nil) return -18;
        const BOOL use_down_reduce = hidden_size >= 128;
        [down_linear_encoder setComputePipelineState:(use_down_reduce ? runtime->linear_reduce_pipeline : runtime->linear_pipeline)];
        [down_linear_encoder setBuffer:down_input_buffer offset:0 atIndex:0];
        [down_linear_encoder setBuffer:runtime->linear_weight_buffers[down_linear_slot] offset:0 atIndex:1];
        [down_linear_encoder setBuffer:runtime->linear_bias_buffers[down_linear_slot] offset:0 atIndex:2];
        [down_linear_encoder setBuffer:projected_buffer offset:0 atIndex:3];
        [down_linear_encoder setBuffer:down_linear_params_buffer offset:0 atIndex:4];
        if (use_down_reduce) {
            [down_linear_encoder dispatchThreads:MTLSizeMake(hidden_size * 32u, 1, 1) threadsPerThreadgroup:MTLSizeMake(32, 1, 1)];
        } else {
            [down_linear_encoder dispatchThreads:MTLSizeMake(hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->linear_pipeline, hidden_size), 1, 1)];
        }
        [down_linear_encoder endEncoding];

        id<MTLComputeCommandEncoder> add_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (add_encoder == nil) return -19;
        [add_encoder setComputePipelineState:runtime->add_pipeline];
        [add_encoder setBuffer:projected_buffer offset:0 atIndex:0];
        [add_encoder setBuffer:residual_buffer offset:residual_offset atIndex:1];
        [add_encoder setBuffer:output_buffer offset:output_offset atIndex:2];
        [add_encoder setBuffer:add_params_buffer offset:0 atIndex:3];
        [add_encoder dispatchThreads:MTLSizeMake(hidden_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->add_pipeline, hidden_size), 1, 1)];
        [add_encoder endEncoding];
        return 0;
    }
}

int termite_metal_decode_runtime_apply_attention_dense_block(
    termite_metal_decode_runtime *runtime,
    uint32_t format,
    const float *q,
    const uint8_t *encoded_key,
    const float *v,
    size_t kv_tokens,
    size_t num_heads,
    size_t num_kv_heads,
    size_t head_dim,
    size_t key_row_bytes,
    size_t v_row_stride,
    size_t base_key_row_bytes,
    size_t query_position,
    size_t kv_position_offset,
    size_t sliding_window,
    size_t attention_linear_slot,
    size_t attention_pre_linear_rms_norm_slot,
    size_t attention_post_linear_rms_norm_slot,
    const float *residual,
    size_t attention_input_size,
    size_t hidden_size,
    float eps,
    size_t ffn_layer_norm_slot,
    size_t ffn_rms_norm_slot,
    size_t first_ffn_linear_slot,
    size_t second_ffn_linear_slot,
    size_t intermediate_size,
    uint32_t activation_kind,
    float *output
) {
    if (runtime == NULL || q == NULL || encoded_key == NULL || v == NULL || residual == NULL || output == NULL) return -1;
    @autoreleasepool {
        const size_t q_bytes = num_heads * head_dim * sizeof(float);
        const size_t hidden_bytes = hidden_size * sizeof(float);
        if (termite_metal_decode_runtime_ensure_direct_block_hidden_buffers(runtime, hidden_bytes) != 0) return -2;
        if (termite_metal_decode_runtime_ensure_direct_block_shared_buffer(runtime, 0, q_bytes) != 0) return -2;
        if (termite_metal_decode_runtime_ensure_direct_block_shared_buffer(runtime, 1, hidden_bytes) != 0) return -2;
        if (termite_metal_decode_runtime_ensure_direct_block_shared_buffer(runtime, 2, hidden_bytes) != 0) return -2;
        id<MTLBuffer> q_buffer = runtime->direct_block_shared_buffers[0];
        id<MTLBuffer> residual_buffer = runtime->direct_block_shared_buffers[1];
        id<MTLBuffer> attention_output_buffer = runtime->direct_block_hidden_buffers[0];
        id<MTLBuffer> attn_added_buffer = runtime->direct_block_hidden_buffers[1];
        id<MTLBuffer> ffn_input_buffer = runtime->direct_block_hidden_buffers[2];
        id<MTLBuffer> output_buffer = runtime->direct_block_shared_buffers[2];
        if (q_buffer == nil || residual_buffer == nil || attention_output_buffer == nil || attn_added_buffer == nil || ffn_input_buffer == nil || output_buffer == nil) return -2;
        memcpy(q_buffer.contents, q, q_bytes);
        memcpy(residual_buffer.contents, residual, hidden_bytes);
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -3;
        int rc = termite_metal_decode_runtime_encode_replace_attention_span(
            runtime,
            command_buffer,
            encoded_key,
            v,
            kv_tokens,
            key_row_bytes,
            v_row_stride,
            kv_position_offset
        );
        if (rc == 0) {
            rc = termite_metal_decode_runtime_encode_attention_span(
                runtime,
                command_buffer,
                format,
                q_buffer,
                0,
                kv_tokens,
                num_heads,
                num_kv_heads,
                head_dim,
                key_row_bytes,
                base_key_row_bytes,
                query_position,
                kv_position_offset,
                sliding_window,
                attention_output_buffer,
                0
            );
        }
        if (rc == 0) {
            rc = termite_metal_decode_runtime_encode_attention_block_prefix(
                runtime,
                command_buffer,
                attention_output_buffer,
                0,
                attention_pre_linear_rms_norm_slot,
                attention_linear_slot,
                attention_post_linear_rms_norm_slot,
                residual_buffer,
                0,
                attention_input_size,
                hidden_size,
                eps,
                ffn_layer_norm_slot,
                ffn_rms_norm_slot,
                attn_added_buffer,
                0,
                ffn_input_buffer,
                0
            );
        }
        if (rc == 0) {
            rc = termite_metal_decode_runtime_encode_dense_ffn_residual(
                runtime,
                command_buffer,
                first_ffn_linear_slot,
                second_ffn_linear_slot,
                ffn_input_buffer,
                attn_added_buffer,
                hidden_size,
                intermediate_size,
                activation_kind,
                output_buffer
            );
        }
        if (rc == 0) {
            [command_buffer commit];
            [command_buffer waitUntilCompleted];
            rc = command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -4;
            if (rc == 0) memcpy(output, output_buffer.contents, hidden_bytes);
        }
        return rc;
    }
}

int termite_metal_decode_runtime_apply_attention_dense_block_device(
    termite_metal_decode_runtime *runtime,
    uint32_t format,
    void *q_handle,
    size_t q_offset,
    const uint8_t *encoded_key,
    const float *v,
    size_t kv_tokens,
    size_t num_heads,
    size_t num_kv_heads,
    size_t head_dim,
    size_t key_row_bytes,
    size_t v_row_stride,
    size_t base_key_row_bytes,
    size_t query_position,
    size_t kv_position_offset,
    size_t sliding_window,
    size_t attention_linear_slot,
    size_t attention_pre_linear_rms_norm_slot,
    size_t attention_post_linear_rms_norm_slot,
    void *residual_handle,
    size_t residual_offset,
    size_t attention_input_size,
    size_t hidden_size,
    float eps,
    size_t ffn_layer_norm_slot,
    size_t ffn_rms_norm_slot,
    size_t first_ffn_linear_slot,
    size_t second_ffn_linear_slot,
    size_t intermediate_size,
    uint32_t activation_kind,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || q_handle == NULL || encoded_key == NULL || v == NULL || residual_handle == NULL || output_handle == NULL) return -1;
    @autoreleasepool {
        const size_t hidden_bytes = hidden_size * sizeof(float);
        if (termite_metal_decode_runtime_ensure_direct_block_hidden_buffers(runtime, hidden_bytes) != 0) return -2;
        id<MTLBuffer> q_buffer = (__bridge id<MTLBuffer>)q_handle;
        id<MTLBuffer> residual_buffer = (__bridge id<MTLBuffer>)residual_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        id<MTLBuffer> attention_output_buffer = runtime->direct_block_hidden_buffers[0];
        id<MTLBuffer> attn_added_buffer = runtime->direct_block_hidden_buffers[1];
        id<MTLBuffer> ffn_input_buffer = runtime->direct_block_hidden_buffers[2];
        if (q_buffer == nil || residual_buffer == nil || output_buffer == nil || attention_output_buffer == nil || attn_added_buffer == nil || ffn_input_buffer == nil) return -2;
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -3;
        int rc = termite_metal_decode_runtime_encode_replace_attention_span(
            runtime,
            command_buffer,
            encoded_key,
            v,
            kv_tokens,
            key_row_bytes,
            v_row_stride,
            kv_position_offset
        );
        if (rc == 0) {
            rc = termite_metal_decode_runtime_encode_attention_span(
                runtime,
                command_buffer,
                format,
                q_buffer,
                q_offset,
                kv_tokens,
                num_heads,
                num_kv_heads,
                head_dim,
                key_row_bytes,
                base_key_row_bytes,
                query_position,
                kv_position_offset,
                sliding_window,
                attention_output_buffer,
                0
            );
        }
        if (rc == 0) {
            rc = termite_metal_decode_runtime_encode_attention_block_prefix(
                runtime,
                command_buffer,
                attention_output_buffer,
                0,
                attention_pre_linear_rms_norm_slot,
                attention_linear_slot,
                attention_post_linear_rms_norm_slot,
                residual_buffer,
                residual_offset,
                attention_input_size,
                hidden_size,
                eps,
                ffn_layer_norm_slot,
                ffn_rms_norm_slot,
                attn_added_buffer,
                0,
                ffn_input_buffer,
                0
            );
        }
        if (rc == 0) {
            rc = termite_metal_decode_runtime_encode_dense_ffn_residual(
                runtime,
                command_buffer,
                first_ffn_linear_slot,
                second_ffn_linear_slot,
                ffn_input_buffer,
                attn_added_buffer,
                hidden_size,
                intermediate_size,
                activation_kind,
                output_buffer
            );
        }
        if (rc == 0) {
            [command_buffer commit];
            [command_buffer waitUntilCompleted];
            rc = command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -4;
        }
        return rc;
    }
}

int termite_metal_decode_runtime_apply_attention_dense_block_device_kv_device(
    termite_metal_decode_runtime *runtime,
    uint32_t format,
    void *q_handle,
    size_t q_offset,
    void *k_handle,
    size_t k_offset,
    void *v_handle,
    size_t v_offset,
    size_t kv_tokens,
    size_t num_heads,
    size_t num_kv_heads,
    size_t head_dim,
    size_t key_row_bytes,
    size_t v_row_stride,
    size_t base_key_row_bytes,
    size_t query_position,
    size_t kv_position_offset,
    size_t sliding_window,
    size_t attention_linear_slot,
    size_t attention_pre_linear_rms_norm_slot,
    size_t attention_post_linear_rms_norm_slot,
    void *residual_handle,
    size_t residual_offset,
    size_t attention_input_size,
    size_t hidden_size,
    float eps,
    size_t ffn_layer_norm_slot,
    size_t ffn_rms_norm_slot,
    size_t first_ffn_linear_slot,
    size_t second_ffn_linear_slot,
    size_t intermediate_size,
    uint32_t activation_kind,
    void *output_handle,
    size_t output_offset
) {
    if (runtime == NULL || q_handle == NULL || k_handle == NULL || v_handle == NULL || residual_handle == NULL || output_handle == NULL) return -1;
    @autoreleasepool {
        const size_t hidden_bytes = hidden_size * sizeof(float);
        if (termite_metal_decode_runtime_ensure_direct_block_hidden_buffers(runtime, hidden_bytes) != 0) return -2;
        id<MTLBuffer> q_buffer = (__bridge id<MTLBuffer>)q_handle;
        id<MTLBuffer> residual_buffer = (__bridge id<MTLBuffer>)residual_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        id<MTLBuffer> attention_output_buffer = runtime->direct_block_hidden_buffers[0];
        id<MTLBuffer> attn_added_buffer = runtime->direct_block_hidden_buffers[1];
        id<MTLBuffer> ffn_input_buffer = runtime->direct_block_hidden_buffers[2];
        if (q_buffer == nil || residual_buffer == nil || output_buffer == nil || attention_output_buffer == nil || attn_added_buffer == nil || ffn_input_buffer == nil) return -2;
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -3;
        int rc = termite_metal_decode_runtime_encode_replace_attention_span_from_f32_key_device_slot(
            runtime,
            command_buffer,
            0,
            format,
            k_handle,
            k_offset,
            v_handle,
            v_offset,
            kv_tokens,
            num_kv_heads,
            head_dim,
            key_row_bytes,
            base_key_row_bytes,
            v_row_stride,
            kv_position_offset
        );
        if (rc == 0) {
            rc = termite_metal_decode_runtime_encode_attention_span(
                runtime,
                command_buffer,
                format,
                q_buffer,
                q_offset,
                kv_tokens,
                num_heads,
                num_kv_heads,
                head_dim,
                key_row_bytes,
                base_key_row_bytes,
                query_position,
                kv_position_offset,
                sliding_window,
                attention_output_buffer,
                0
            );
        }
        if (rc == 0) {
            rc = termite_metal_decode_runtime_encode_attention_block_prefix(
                runtime,
                command_buffer,
                attention_output_buffer,
                0,
                attention_pre_linear_rms_norm_slot,
                attention_linear_slot,
                attention_post_linear_rms_norm_slot,
                residual_buffer,
                residual_offset,
                attention_input_size,
                hidden_size,
                eps,
                ffn_layer_norm_slot,
                ffn_rms_norm_slot,
                attn_added_buffer,
                0,
                ffn_input_buffer,
                0
            );
        }
        if (rc == 0) {
            rc = termite_metal_decode_runtime_encode_dense_ffn_residual(
                runtime,
                command_buffer,
                first_ffn_linear_slot,
                second_ffn_linear_slot,
                ffn_input_buffer,
                attn_added_buffer,
                hidden_size,
                intermediate_size,
                activation_kind,
                output_buffer
            );
        }
        if (rc == 0) {
            [command_buffer commit];
            [command_buffer waitUntilCompleted];
            rc = command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -4;
        }
        return rc;
    }
}

int termite_metal_decode_runtime_apply_attention_gated_block(
    termite_metal_decode_runtime *runtime,
    uint32_t format,
    const float *q,
    const float *k,
    const float *v,
    size_t kv_tokens,
    size_t num_heads,
    size_t num_kv_heads,
    size_t head_dim,
    size_t key_row_bytes,
    size_t v_row_stride,
    size_t base_key_row_bytes,
    size_t query_position,
    size_t kv_position_offset,
    size_t sliding_window,
    size_t attention_linear_slot,
    size_t attention_pre_linear_rms_norm_slot,
    size_t attention_post_linear_rms_norm_slot,
    const float *residual,
    size_t attention_input_size,
    size_t hidden_size,
    float eps,
    size_t ffn_layer_norm_slot,
    size_t ffn_rms_norm_slot,
    size_t ffn_post_gate_rms_norm_slot,
    size_t gate_ffn_linear_slot,
    size_t up_ffn_linear_slot,
    size_t down_ffn_linear_slot,
    size_t intermediate_size,
    uint32_t activation_kind,
    size_t layer_index,
    float *output,
    termite_metal_attention_gated_block_timing *timing
) {
    if (runtime == NULL || q == NULL || k == NULL || v == NULL || residual == NULL || output == NULL) return -1;
    if (layer_index >= TERMITE_METAL_ATTENTION_SPAN_SLOT_CAPACITY) return -2;
    if (timing != NULL) memset(timing, 0, sizeof(*timing));
    @autoreleasepool {
        const size_t q_bytes = num_heads * head_dim * sizeof(float);
        const size_t hidden_bytes = hidden_size * sizeof(float);
        if (termite_metal_decode_runtime_ensure_direct_block_hidden_buffers(runtime, hidden_bytes) != 0) return -2;
        if (termite_metal_decode_runtime_ensure_direct_block_shared_buffer(runtime, 0, q_bytes) != 0) return -2;
        if (termite_metal_decode_runtime_ensure_direct_block_shared_buffer(runtime, 1, hidden_bytes) != 0) return -2;
        if (termite_metal_decode_runtime_ensure_direct_block_shared_buffer(runtime, 2, hidden_bytes) != 0) return -2;
        id<MTLBuffer> q_buffer = runtime->direct_block_shared_buffers[0];
        id<MTLBuffer> residual_buffer = runtime->direct_block_shared_buffers[1];
        id<MTLBuffer> attention_output_buffer = runtime->direct_block_hidden_buffers[0];
        id<MTLBuffer> attn_added_buffer = runtime->direct_block_hidden_buffers[1];
        id<MTLBuffer> ffn_input_buffer = runtime->direct_block_hidden_buffers[2];
        id<MTLBuffer> output_buffer = runtime->direct_block_shared_buffers[2];
        if (q_buffer == nil || residual_buffer == nil || attention_output_buffer == nil || attn_added_buffer == nil || ffn_input_buffer == nil || output_buffer == nil) return -2;
        memcpy(q_buffer.contents, q, q_bytes);
        memcpy(residual_buffer.contents, residual, hidden_bytes);
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -3;
        uint64_t stage_started_at = termite_metal_clock_monotonic_nanos();
        int rc = termite_metal_decode_runtime_encode_replace_attention_span_from_f32_key_slot(
            runtime,
            command_buffer,
            layer_index,
            format,
            k,
            v,
            kv_tokens,
            num_kv_heads,
            head_dim,
            key_row_bytes,
            base_key_row_bytes,
            v_row_stride,
            kv_position_offset
        );
        if (rc != 0 && timing != NULL) {
            timing->failure_stage = 1;
            timing->failure_code = rc;
        }
        if (timing != NULL) timing->replace_span_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;
        if (rc == 0) {
            stage_started_at = termite_metal_clock_monotonic_nanos();
            rc = termite_metal_decode_runtime_encode_attention_span_slot(
                runtime,
                command_buffer,
                layer_index,
                format,
                q_buffer,
                0,
                kv_tokens,
                num_heads,
                num_kv_heads,
                head_dim,
                key_row_bytes,
                base_key_row_bytes,
                query_position,
                kv_position_offset,
                sliding_window,
                attention_output_buffer,
                0
            );
            if (rc != 0 && timing != NULL) {
                timing->failure_stage = 2;
                timing->failure_code = rc;
            }
            if (timing != NULL) timing->attention_span_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;
        }
        if (rc == 0) {
            stage_started_at = termite_metal_clock_monotonic_nanos();
            rc = termite_metal_decode_runtime_encode_attention_block_prefix(
                runtime,
                command_buffer,
                attention_output_buffer,
                0,
                attention_pre_linear_rms_norm_slot,
                attention_linear_slot,
                attention_post_linear_rms_norm_slot,
                residual_buffer,
                0,
                attention_input_size,
                hidden_size,
                eps,
                ffn_layer_norm_slot,
                ffn_rms_norm_slot,
                attn_added_buffer,
                0,
                ffn_input_buffer,
                0
            );
            if (rc != 0 && timing != NULL) {
                timing->failure_stage = 3;
                timing->failure_code = rc;
            }
            if (timing != NULL) timing->attention_prefix_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;
        }
        if (rc == 0) {
            stage_started_at = termite_metal_clock_monotonic_nanos();
            rc = termite_metal_decode_runtime_encode_gated_ffn_residual(
                runtime,
                command_buffer,
                gate_ffn_linear_slot,
                up_ffn_linear_slot,
                down_ffn_linear_slot,
                ffn_post_gate_rms_norm_slot,
                ffn_input_buffer,
                0,
                attn_added_buffer,
                0,
                hidden_size,
                intermediate_size,
                activation_kind,
                output_buffer,
                0
            );
            if (rc != 0 && timing != NULL) {
                timing->failure_stage = 4;
                timing->failure_code = rc;
            }
            if (timing != NULL) timing->gated_ffn_residual_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;
        }
        if (rc == 0) {
            stage_started_at = termite_metal_clock_monotonic_nanos();
            [command_buffer commit];
            [command_buffer waitUntilCompleted];
            if (timing != NULL) timing->command_wait_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;
            rc = command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -4;
            if (rc == 0) {
                if (termite_metal_debug_direct_block_finite(layer_index)) {
                    termite_metal_debug_dump_buffer_finite(runtime, "direct-attention", layer_index, attention_output_buffer, 0, attention_input_size);
                    termite_metal_debug_dump_buffer_finite(runtime, "direct-attn-added", layer_index, attn_added_buffer, 0, hidden_size);
                    termite_metal_debug_dump_buffer_finite(runtime, "direct-ffn-input", layer_index, ffn_input_buffer, 0, hidden_size);
                    termite_metal_debug_dump_buffer_finite(runtime, "direct-output", layer_index, output_buffer, 0, hidden_size);
                }
                memcpy(output, output_buffer.contents, hidden_bytes);
            }
        }
        return rc;
    }
}

int termite_metal_decode_runtime_apply_attention_gated_block_q_slot(
    termite_metal_decode_runtime *runtime,
    uint32_t format,
    const float *attention_input,
    size_t q_linear_slot,
    const float *k,
    const float *v,
    size_t kv_tokens,
    size_t num_heads,
    size_t num_kv_heads,
    size_t head_dim,
    size_t key_row_bytes,
    size_t v_row_stride,
    size_t base_key_row_bytes,
    size_t query_position,
    size_t kv_position_offset,
    size_t sliding_window,
    size_t attention_linear_slot,
    size_t attention_pre_linear_rms_norm_slot,
    size_t attention_post_linear_rms_norm_slot,
    const float *residual,
    size_t hidden_size,
    size_t attention_input_size,
    float eps,
    size_t ffn_layer_norm_slot,
    size_t ffn_rms_norm_slot,
    size_t ffn_post_gate_rms_norm_slot,
    size_t gate_ffn_linear_slot,
    size_t up_ffn_linear_slot,
    size_t down_ffn_linear_slot,
    size_t intermediate_size,
    uint32_t activation_kind,
    size_t layer_index,
    float *output,
    termite_metal_attention_gated_block_timing *timing
) {
    if (runtime == NULL || attention_input == NULL || k == NULL || v == NULL || residual == NULL || output == NULL) return -1;
    if (layer_index >= TERMITE_METAL_ATTENTION_SPAN_SLOT_CAPACITY) return -2;
    if (timing != NULL) memset(timing, 0, sizeof(*timing));
    if (q_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -2;
    if (runtime->linear_pipeline == nil || runtime->linear_reduce_pipeline == nil) return -3;
    if (runtime->linear_slot_prepared[q_linear_slot] == 0) return -4;
    if (runtime->linear_in_dims[q_linear_slot] != hidden_size || runtime->linear_out_dims[q_linear_slot] != attention_input_size) return -5;
    if (hidden_size > UINT32_MAX || attention_input_size > UINT32_MAX) return -6;
    @autoreleasepool {
        const size_t input_bytes = hidden_size * sizeof(float);
        const size_t q_bytes = attention_input_size * sizeof(float);
        const size_t hidden_bytes = hidden_size * sizeof(float);
        if (termite_metal_decode_runtime_ensure_direct_block_hidden_buffers(runtime, hidden_bytes) != 0) return -7;
        if (termite_metal_decode_runtime_ensure_direct_block_q_buffer(runtime, q_bytes) != 0) return -8;
        if (termite_metal_decode_runtime_ensure_direct_block_shared_buffer(runtime, 0, input_bytes) != 0) return -9;
        if (termite_metal_decode_runtime_ensure_direct_block_shared_buffer(runtime, 1, hidden_bytes) != 0) return -9;
        if (termite_metal_decode_runtime_ensure_direct_block_shared_buffer(runtime, 2, hidden_bytes) != 0) return -9;
        id<MTLBuffer> input_buffer = runtime->direct_block_shared_buffers[0];
        id<MTLBuffer> residual_buffer = runtime->direct_block_shared_buffers[1];
        id<MTLBuffer> q_buffer = runtime->direct_block_q_buffer;
        id<MTLBuffer> attention_output_buffer = runtime->direct_block_hidden_buffers[0];
        id<MTLBuffer> attn_added_buffer = runtime->direct_block_hidden_buffers[1];
        id<MTLBuffer> ffn_input_buffer = runtime->direct_block_hidden_buffers[2];
        id<MTLBuffer> output_buffer = runtime->direct_block_shared_buffers[2];
        if (input_buffer == nil || residual_buffer == nil || q_buffer == nil || attention_output_buffer == nil || attn_added_buffer == nil || ffn_input_buffer == nil || output_buffer == nil) return -9;
        memcpy(input_buffer.contents, attention_input, input_bytes);
        memcpy(residual_buffer.contents, residual, hidden_bytes);

        termite_metal_apply_linear_params q_params = {
            .in_dim = (uint32_t)hidden_size,
            .out_dim = (uint32_t)attention_input_size,
        };

        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -10;

        uint64_t stage_started_at = termite_metal_clock_monotonic_nanos();
        id<MTLComputeCommandEncoder> q_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (q_encoder == nil) return -11;
        const BOOL use_q_reduce = attention_input_size >= 128;
        [q_encoder setComputePipelineState:(use_q_reduce ? runtime->linear_reduce_pipeline : runtime->linear_pipeline)];
        [q_encoder setBuffer:input_buffer offset:0 atIndex:0];
        [q_encoder setBuffer:runtime->linear_weight_buffers[q_linear_slot] offset:0 atIndex:1];
        [q_encoder setBuffer:runtime->linear_bias_buffers[q_linear_slot] offset:0 atIndex:2];
        [q_encoder setBuffer:q_buffer offset:0 atIndex:3];
        [q_encoder setBytes:&q_params length:sizeof(q_params) atIndex:4];
        if (use_q_reduce) {
            [q_encoder dispatchThreads:MTLSizeMake(attention_input_size * 32u, 1, 1) threadsPerThreadgroup:MTLSizeMake(32, 1, 1)];
        } else {
            NSUInteger q_thread_width = runtime->linear_pipeline.maxTotalThreadsPerThreadgroup;
            if (q_thread_width == 0) q_thread_width = 64;
            if (q_thread_width > attention_input_size && attention_input_size > 0) q_thread_width = attention_input_size;
            [q_encoder dispatchThreads:MTLSizeMake(attention_input_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(q_thread_width, 1, 1)];
        }
        [q_encoder endEncoding];
        if (timing != NULL) timing->attention_span_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;

        stage_started_at = termite_metal_clock_monotonic_nanos();
        int rc = termite_metal_decode_runtime_encode_replace_attention_span_from_f32_key_slot(
            runtime,
            command_buffer,
            layer_index,
            format,
            k,
            v,
            kv_tokens,
            num_kv_heads,
            head_dim,
            key_row_bytes,
            base_key_row_bytes,
            v_row_stride,
            kv_position_offset
        );
        if (rc != 0 && timing != NULL) {
            timing->failure_stage = 1;
            timing->failure_code = rc;
        }
        if (timing != NULL) timing->replace_span_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;
        if (rc == 0) {
            stage_started_at = termite_metal_clock_monotonic_nanos();
            rc = termite_metal_decode_runtime_encode_attention_span_slot(
                runtime,
                command_buffer,
                layer_index,
                format,
                q_buffer,
                0,
                kv_tokens,
                num_heads,
                num_kv_heads,
                head_dim,
                key_row_bytes,
                base_key_row_bytes,
                query_position,
                kv_position_offset,
                sliding_window,
                attention_output_buffer,
                0
            );
            if (rc != 0 && timing != NULL) {
                timing->failure_stage = 2;
                timing->failure_code = rc;
            }
            if (timing != NULL) timing->attention_span_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;
        }
        if (rc == 0) {
            stage_started_at = termite_metal_clock_monotonic_nanos();
            rc = termite_metal_decode_runtime_encode_attention_block_prefix(
                runtime,
                command_buffer,
                attention_output_buffer,
                0,
                attention_pre_linear_rms_norm_slot,
                attention_linear_slot,
                attention_post_linear_rms_norm_slot,
                residual_buffer,
                0,
                attention_input_size,
                hidden_size,
                eps,
                ffn_layer_norm_slot,
                ffn_rms_norm_slot,
                attn_added_buffer,
                0,
                ffn_input_buffer,
                0
            );
            if (rc != 0 && timing != NULL) {
                timing->failure_stage = 3;
                timing->failure_code = rc;
            }
            if (timing != NULL) timing->attention_prefix_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;
        }
        if (rc == 0) {
            stage_started_at = termite_metal_clock_monotonic_nanos();
            rc = termite_metal_decode_runtime_encode_gated_ffn_residual(
                runtime,
                command_buffer,
                gate_ffn_linear_slot,
                up_ffn_linear_slot,
                down_ffn_linear_slot,
                ffn_post_gate_rms_norm_slot,
                ffn_input_buffer,
                0,
                attn_added_buffer,
                0,
                hidden_size,
                intermediate_size,
                activation_kind,
                output_buffer,
                0
            );
            if (rc != 0 && timing != NULL) {
                timing->failure_stage = 4;
                timing->failure_code = rc;
            }
            if (timing != NULL) timing->gated_ffn_residual_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;
        }
        if (rc == 0) {
            stage_started_at = termite_metal_clock_monotonic_nanos();
            [command_buffer commit];
            [command_buffer waitUntilCompleted];
            if (timing != NULL) timing->command_wait_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;
            rc = command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -12;
            if (rc == 0) {
                if (termite_metal_debug_direct_block_finite(layer_index)) {
                    termite_metal_debug_dump_buffer_finite(runtime, "direct-q", layer_index, q_buffer, 0, attention_input_size);
                    termite_metal_debug_dump_buffer_finite(runtime, "direct-attention", layer_index, attention_output_buffer, 0, attention_input_size);
                    termite_metal_debug_dump_buffer_finite(runtime, "direct-attn-added", layer_index, attn_added_buffer, 0, hidden_size);
                    termite_metal_debug_dump_buffer_finite(runtime, "direct-ffn-input", layer_index, ffn_input_buffer, 0, hidden_size);
                    termite_metal_debug_dump_buffer_finite(runtime, "direct-output", layer_index, output_buffer, 0, hidden_size);
                }
                memcpy(output, output_buffer.contents, hidden_bytes);
            }
        }
        return rc;
    }
}

int termite_metal_decode_runtime_apply_attention_gated_block_device(
    termite_metal_decode_runtime *runtime,
    uint32_t format,
    void *q_handle,
    size_t q_offset,
    const float *k,
    const float *v,
    size_t kv_tokens,
    size_t num_heads,
    size_t num_kv_heads,
    size_t head_dim,
    size_t key_row_bytes,
    size_t v_row_stride,
    size_t base_key_row_bytes,
    size_t query_position,
    size_t kv_position_offset,
    size_t sliding_window,
    size_t attention_linear_slot,
    size_t attention_pre_linear_rms_norm_slot,
    size_t attention_post_linear_rms_norm_slot,
    void *residual_handle,
    size_t residual_offset,
    size_t attention_input_size,
    size_t hidden_size,
    float eps,
    size_t ffn_layer_norm_slot,
    size_t ffn_rms_norm_slot,
    size_t ffn_post_gate_rms_norm_slot,
    size_t gate_ffn_linear_slot,
    size_t up_ffn_linear_slot,
    size_t down_ffn_linear_slot,
    size_t intermediate_size,
    uint32_t activation_kind,
    size_t layer_index,
    void *output_handle,
    size_t output_offset,
    termite_metal_attention_gated_block_timing *timing
) {
    if (runtime == NULL || q_handle == NULL || k == NULL || v == NULL || residual_handle == NULL || output_handle == NULL) return -1;
    if (layer_index >= TERMITE_METAL_ATTENTION_SPAN_SLOT_CAPACITY) return -2;
    if (timing != NULL) memset(timing, 0, sizeof(*timing));
    @autoreleasepool {
        const size_t hidden_bytes = hidden_size * sizeof(float);
        if (termite_metal_decode_runtime_ensure_direct_block_hidden_buffers(runtime, hidden_bytes) != 0) return -2;
        id<MTLBuffer> q_buffer = (__bridge id<MTLBuffer>)q_handle;
        id<MTLBuffer> residual_buffer = (__bridge id<MTLBuffer>)residual_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        id<MTLBuffer> attention_output_buffer = runtime->direct_block_hidden_buffers[0];
        id<MTLBuffer> attn_added_buffer = runtime->direct_block_hidden_buffers[1];
        id<MTLBuffer> ffn_input_buffer = runtime->direct_block_hidden_buffers[2];
        if (q_buffer == nil || residual_buffer == nil || output_buffer == nil || attention_output_buffer == nil || attn_added_buffer == nil || ffn_input_buffer == nil) return -2;
        const bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLCommandBuffer> command_buffer = frame_owned
            ? termite_metal_new_command_buffer(runtime->queue, __func__)
            : runtime->active_frame_cb;
        if (command_buffer == nil) return -3;
        uint64_t stage_started_at = termite_metal_clock_monotonic_nanos();
        int rc = termite_metal_decode_runtime_encode_replace_attention_span_from_f32_key_slot(
            runtime,
            command_buffer,
            layer_index,
            format,
            k,
            v,
            kv_tokens,
            num_kv_heads,
            head_dim,
            key_row_bytes,
            base_key_row_bytes,
            v_row_stride,
            kv_position_offset
        );
        if (rc != 0 && timing != NULL) {
            timing->failure_stage = 1;
            timing->failure_code = rc;
        }
        if (timing != NULL) timing->replace_span_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;
        if (rc == 0) {
            stage_started_at = termite_metal_clock_monotonic_nanos();
            rc = termite_metal_decode_runtime_encode_attention_span_slot(
                runtime,
                command_buffer,
                layer_index,
                format,
                q_buffer,
                q_offset,
                kv_tokens,
                num_heads,
                num_kv_heads,
                head_dim,
                key_row_bytes,
                base_key_row_bytes,
                query_position,
                kv_position_offset,
                sliding_window,
                attention_output_buffer,
                0
            );
            if (rc != 0 && timing != NULL) {
                timing->failure_stage = 2;
                timing->failure_code = rc;
            }
            if (timing != NULL) timing->attention_span_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;
        }
        if (rc == 0) {
            stage_started_at = termite_metal_clock_monotonic_nanos();
            rc = termite_metal_decode_runtime_encode_attention_block_prefix(
                runtime,
                command_buffer,
                attention_output_buffer,
                0,
                attention_pre_linear_rms_norm_slot,
                attention_linear_slot,
                attention_post_linear_rms_norm_slot,
                residual_buffer,
                residual_offset,
                attention_input_size,
                hidden_size,
                eps,
                ffn_layer_norm_slot,
                ffn_rms_norm_slot,
                attn_added_buffer,
                0,
                ffn_input_buffer,
                0
            );
            if (rc != 0 && timing != NULL) {
                timing->failure_stage = 3;
                timing->failure_code = rc;
            }
            if (timing != NULL) timing->attention_prefix_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;
        }
        if (rc == 0) {
            stage_started_at = termite_metal_clock_monotonic_nanos();
            rc = termite_metal_decode_runtime_encode_gated_ffn_residual(
                runtime,
                command_buffer,
                gate_ffn_linear_slot,
                up_ffn_linear_slot,
                down_ffn_linear_slot,
                ffn_post_gate_rms_norm_slot,
                ffn_input_buffer,
                0,
                attn_added_buffer,
                0,
                hidden_size,
                intermediate_size,
                activation_kind,
                output_buffer,
                output_offset
            );
            if (rc != 0 && timing != NULL) {
                timing->failure_stage = 4;
                timing->failure_code = rc;
            }
            if (timing != NULL) timing->gated_ffn_residual_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;
        }
        if (rc == 0 && frame_owned) {
            stage_started_at = termite_metal_clock_monotonic_nanos();
            [command_buffer commit];
            [command_buffer waitUntilCompleted];
            if (timing != NULL) timing->command_wait_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;
            rc = command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -4;
        }
        if (rc == 0 && frame_owned && termite_metal_debug_direct_block_finite(layer_index)) {
            termite_metal_debug_dump_buffer_finite(runtime, "direct-attention", layer_index, attention_output_buffer, 0, attention_input_size);
            termite_metal_debug_dump_buffer_finite(runtime, "direct-attn-added", layer_index, attn_added_buffer, 0, hidden_size);
            termite_metal_debug_dump_buffer_finite(runtime, "direct-ffn-input", layer_index, ffn_input_buffer, 0, hidden_size);
            termite_metal_debug_dump_buffer_finite(runtime, "direct-output", layer_index, output_buffer, output_offset, hidden_size);
        }
        return rc;
    }
}

int termite_metal_decode_runtime_apply_attention_gated_block_device_kv_device(
    termite_metal_decode_runtime *runtime,
    uint32_t format,
    void *q_handle,
    size_t q_offset,
    void *k_handle,
    size_t k_offset,
    void *v_handle,
    size_t v_offset,
    size_t kv_tokens,
    size_t num_heads,
    size_t num_kv_heads,
    size_t head_dim,
    size_t key_row_bytes,
    size_t v_row_stride,
    size_t base_key_row_bytes,
    size_t query_position,
    size_t kv_position_offset,
    size_t sliding_window,
    size_t attention_linear_slot,
    size_t attention_pre_linear_rms_norm_slot,
    size_t attention_post_linear_rms_norm_slot,
    void *residual_handle,
    size_t residual_offset,
    size_t attention_input_size,
    size_t hidden_size,
    float eps,
    size_t ffn_layer_norm_slot,
    size_t ffn_rms_norm_slot,
    size_t ffn_post_gate_rms_norm_slot,
    size_t gate_ffn_linear_slot,
    size_t up_ffn_linear_slot,
    size_t down_ffn_linear_slot,
    size_t intermediate_size,
    uint32_t activation_kind,
    size_t layer_index,
    void *output_handle,
    size_t output_offset,
    termite_metal_attention_gated_block_timing *timing
) {
    if (runtime == NULL || q_handle == NULL || k_handle == NULL || v_handle == NULL || residual_handle == NULL || output_handle == NULL) return -1;
    if (layer_index >= TERMITE_METAL_ATTENTION_SPAN_SLOT_CAPACITY) return -2;
    if (timing != NULL) memset(timing, 0, sizeof(*timing));
    @autoreleasepool {
        const size_t hidden_bytes = hidden_size * sizeof(float);
        if (termite_metal_decode_runtime_ensure_direct_block_hidden_buffers(runtime, hidden_bytes) != 0) return -2;
        id<MTLBuffer> q_buffer = (__bridge id<MTLBuffer>)q_handle;
        id<MTLBuffer> residual_buffer = (__bridge id<MTLBuffer>)residual_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        id<MTLBuffer> attention_output_buffer = runtime->direct_block_hidden_buffers[0];
        id<MTLBuffer> attn_added_buffer = runtime->direct_block_hidden_buffers[1];
        id<MTLBuffer> ffn_input_buffer = runtime->direct_block_hidden_buffers[2];
        if (q_buffer == nil || residual_buffer == nil || output_buffer == nil || attention_output_buffer == nil || attn_added_buffer == nil || ffn_input_buffer == nil) return -2;
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -3;
        uint64_t stage_started_at = termite_metal_clock_monotonic_nanos();
        int rc = termite_metal_decode_runtime_encode_replace_attention_span_from_f32_key_device_slot(
            runtime,
            command_buffer,
            layer_index,
            format,
            k_handle,
            k_offset,
            v_handle,
            v_offset,
            kv_tokens,
            num_kv_heads,
            head_dim,
            key_row_bytes,
            base_key_row_bytes,
            v_row_stride,
            kv_position_offset
        );
        if (rc != 0 && timing != NULL) {
            timing->failure_stage = 1;
            timing->failure_code = rc;
        }
        if (timing != NULL) timing->replace_span_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;
        if (rc == 0) {
            stage_started_at = termite_metal_clock_monotonic_nanos();
            rc = termite_metal_decode_runtime_encode_attention_span_slot(
                runtime,
                command_buffer,
                layer_index,
                format,
                q_buffer,
                q_offset,
                kv_tokens,
                num_heads,
                num_kv_heads,
                head_dim,
                key_row_bytes,
                base_key_row_bytes,
                query_position,
                kv_position_offset,
                sliding_window,
                attention_output_buffer,
                0
            );
            if (rc != 0 && timing != NULL) {
                timing->failure_stage = 2;
                timing->failure_code = rc;
            }
            if (timing != NULL) timing->attention_span_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;
        }
        if (rc == 0) {
            stage_started_at = termite_metal_clock_monotonic_nanos();
            rc = termite_metal_decode_runtime_encode_attention_block_prefix(
                runtime,
                command_buffer,
                attention_output_buffer,
                0,
                attention_pre_linear_rms_norm_slot,
                attention_linear_slot,
                attention_post_linear_rms_norm_slot,
                residual_buffer,
                residual_offset,
                attention_input_size,
                hidden_size,
                eps,
                ffn_layer_norm_slot,
                ffn_rms_norm_slot,
                attn_added_buffer,
                0,
                ffn_input_buffer,
                0
            );
            if (rc != 0 && timing != NULL) {
                timing->failure_stage = 3;
                timing->failure_code = rc;
            }
            if (timing != NULL) timing->attention_prefix_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;
        }
        if (rc == 0) {
            stage_started_at = termite_metal_clock_monotonic_nanos();
            rc = termite_metal_decode_runtime_encode_gated_ffn_residual(
                runtime,
                command_buffer,
                gate_ffn_linear_slot,
                up_ffn_linear_slot,
                down_ffn_linear_slot,
                ffn_post_gate_rms_norm_slot,
                ffn_input_buffer,
                0,
                attn_added_buffer,
                0,
                hidden_size,
                intermediate_size,
                activation_kind,
                output_buffer,
                output_offset
            );
            if (rc != 0 && timing != NULL) {
                timing->failure_stage = 4;
                timing->failure_code = rc;
            }
            if (timing != NULL) timing->gated_ffn_residual_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;
        }
        if (rc == 0) {
            stage_started_at = termite_metal_clock_monotonic_nanos();
            [command_buffer commit];
            [command_buffer waitUntilCompleted];
            if (timing != NULL) timing->command_wait_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;
            rc = command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -4;
        }
        if (rc == 0 && termite_metal_debug_direct_block_finite(layer_index)) {
            termite_metal_debug_dump_buffer_finite(runtime, "direct-attention", layer_index, attention_output_buffer, 0, attention_input_size);
            termite_metal_debug_dump_buffer_finite(runtime, "direct-attn-added", layer_index, attn_added_buffer, 0, hidden_size);
            termite_metal_debug_dump_buffer_finite(runtime, "direct-ffn-input", layer_index, ffn_input_buffer, 0, hidden_size);
            termite_metal_debug_dump_buffer_finite(runtime, "direct-output", layer_index, output_buffer, output_offset, hidden_size);
        }
        return rc;
    }
}

int termite_metal_decode_runtime_apply_attention_gated_block_q_slot_device(
    termite_metal_decode_runtime *runtime,
    uint32_t format,
    void *attention_input_handle,
    size_t attention_input_offset,
    size_t q_linear_slot,
    const float *k,
    const float *v,
    size_t kv_tokens,
    size_t num_heads,
    size_t num_kv_heads,
    size_t head_dim,
    size_t key_row_bytes,
    size_t v_row_stride,
    size_t base_key_row_bytes,
    size_t query_position,
    size_t kv_position_offset,
    size_t sliding_window,
    size_t attention_linear_slot,
    size_t attention_pre_linear_rms_norm_slot,
    size_t attention_post_linear_rms_norm_slot,
    void *residual_handle,
    size_t residual_offset,
    size_t hidden_size,
    size_t attention_input_size,
    float eps,
    size_t ffn_layer_norm_slot,
    size_t ffn_rms_norm_slot,
    size_t ffn_post_gate_rms_norm_slot,
    size_t gate_ffn_linear_slot,
    size_t up_ffn_linear_slot,
    size_t down_ffn_linear_slot,
    size_t intermediate_size,
    uint32_t activation_kind,
    size_t layer_index,
    void *output_handle,
    size_t output_offset,
    termite_metal_attention_gated_block_timing *timing
) {
    if (runtime == NULL || attention_input_handle == NULL || k == NULL || v == NULL || residual_handle == NULL || output_handle == NULL) return -1;
    if (layer_index >= TERMITE_METAL_ATTENTION_SPAN_SLOT_CAPACITY) return -2;
    if (timing != NULL) memset(timing, 0, sizeof(*timing));
    if (q_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -2;
    if (runtime->linear_pipeline == nil || runtime->linear_reduce_pipeline == nil) return -3;
    if (runtime->linear_slot_prepared[q_linear_slot] == 0) return -4;
    if (runtime->linear_in_dims[q_linear_slot] != hidden_size || runtime->linear_out_dims[q_linear_slot] != attention_input_size) return -5;
    if (hidden_size > UINT32_MAX || attention_input_size > UINT32_MAX) return -6;
    @autoreleasepool {
        const size_t q_bytes = attention_input_size * sizeof(float);
        const size_t hidden_bytes = hidden_size * sizeof(float);
        if (termite_metal_decode_runtime_ensure_direct_block_hidden_buffers(runtime, hidden_bytes) != 0) return -7;
        if (termite_metal_decode_runtime_ensure_direct_block_q_buffer(runtime, q_bytes) != 0) return -8;
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)attention_input_handle;
        id<MTLBuffer> residual_buffer = (__bridge id<MTLBuffer>)residual_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        id<MTLBuffer> q_buffer = runtime->direct_block_q_buffer;
        id<MTLBuffer> attention_output_buffer = runtime->direct_block_hidden_buffers[0];
        id<MTLBuffer> attn_added_buffer = runtime->direct_block_hidden_buffers[1];
        id<MTLBuffer> ffn_input_buffer = runtime->direct_block_hidden_buffers[2];
        if (input_buffer == nil || residual_buffer == nil || output_buffer == nil || q_buffer == nil || attention_output_buffer == nil || attn_added_buffer == nil || ffn_input_buffer == nil) return -9;

        termite_metal_apply_linear_params q_params = {
            .in_dim = (uint32_t)hidden_size,
            .out_dim = (uint32_t)attention_input_size,
        };

        const bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLCommandBuffer> command_buffer = frame_owned
            ? termite_metal_new_command_buffer(runtime->queue, __func__)
            : runtime->active_frame_cb;
        if (command_buffer == nil) return -10;

        uint64_t stage_started_at = termite_metal_clock_monotonic_nanos();
        id<MTLComputeCommandEncoder> q_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (q_encoder == nil) return -11;
        const BOOL use_q_reduce = attention_input_size >= 128;
        [q_encoder setComputePipelineState:(use_q_reduce ? runtime->linear_reduce_pipeline : runtime->linear_pipeline)];
        [q_encoder setBuffer:input_buffer offset:attention_input_offset atIndex:0];
        [q_encoder setBuffer:runtime->linear_weight_buffers[q_linear_slot] offset:0 atIndex:1];
        [q_encoder setBuffer:runtime->linear_bias_buffers[q_linear_slot] offset:0 atIndex:2];
        [q_encoder setBuffer:q_buffer offset:0 atIndex:3];
        [q_encoder setBytes:&q_params length:sizeof(q_params) atIndex:4];
        if (use_q_reduce) {
            [q_encoder dispatchThreads:MTLSizeMake(attention_input_size * 32u, 1, 1) threadsPerThreadgroup:MTLSizeMake(32, 1, 1)];
        } else {
            NSUInteger q_thread_width = runtime->linear_pipeline.maxTotalThreadsPerThreadgroup;
            if (q_thread_width == 0) q_thread_width = 64;
            if (q_thread_width > attention_input_size && attention_input_size > 0) q_thread_width = attention_input_size;
            [q_encoder dispatchThreads:MTLSizeMake(attention_input_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(q_thread_width, 1, 1)];
        }
        [q_encoder endEncoding];
        if (timing != NULL) timing->attention_span_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;

        stage_started_at = termite_metal_clock_monotonic_nanos();
        int rc = termite_metal_decode_runtime_encode_replace_attention_span_from_f32_key_slot(
            runtime,
            command_buffer,
            layer_index,
            format,
            k,
            v,
            kv_tokens,
            num_kv_heads,
            head_dim,
            key_row_bytes,
            base_key_row_bytes,
            v_row_stride,
            kv_position_offset
        );
        if (rc != 0 && timing != NULL) {
            timing->failure_stage = 1;
            timing->failure_code = rc;
        }
        if (timing != NULL) timing->replace_span_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;
        if (rc == 0) {
            stage_started_at = termite_metal_clock_monotonic_nanos();
            rc = termite_metal_decode_runtime_encode_attention_span_slot(
                runtime,
                command_buffer,
                layer_index,
                format,
                q_buffer,
                0,
                kv_tokens,
                num_heads,
                num_kv_heads,
                head_dim,
                key_row_bytes,
                base_key_row_bytes,
                query_position,
                kv_position_offset,
                sliding_window,
                attention_output_buffer,
                0
            );
            if (rc != 0 && timing != NULL) {
                timing->failure_stage = 2;
                timing->failure_code = rc;
            }
            if (timing != NULL) timing->attention_span_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;
        }
        if (rc == 0) {
            stage_started_at = termite_metal_clock_monotonic_nanos();
            rc = termite_metal_decode_runtime_encode_attention_block_prefix(
                runtime,
                command_buffer,
                attention_output_buffer,
                0,
                attention_pre_linear_rms_norm_slot,
                attention_linear_slot,
                attention_post_linear_rms_norm_slot,
                residual_buffer,
                residual_offset,
                attention_input_size,
                hidden_size,
                eps,
                ffn_layer_norm_slot,
                ffn_rms_norm_slot,
                attn_added_buffer,
                0,
                ffn_input_buffer,
                0
            );
            if (rc != 0 && timing != NULL) {
                timing->failure_stage = 3;
                timing->failure_code = rc;
            }
            if (timing != NULL) timing->attention_prefix_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;
        }
        if (rc == 0) {
            stage_started_at = termite_metal_clock_monotonic_nanos();
            rc = termite_metal_decode_runtime_encode_gated_ffn_residual(
                runtime,
                command_buffer,
                gate_ffn_linear_slot,
                up_ffn_linear_slot,
                down_ffn_linear_slot,
                ffn_post_gate_rms_norm_slot,
                ffn_input_buffer,
                0,
                attn_added_buffer,
                0,
                hidden_size,
                intermediate_size,
                activation_kind,
                output_buffer,
                output_offset
            );
            if (rc != 0 && timing != NULL) {
                timing->failure_stage = 4;
                timing->failure_code = rc;
            }
            if (timing != NULL) timing->gated_ffn_residual_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;
        }
        if (rc == 0 && frame_owned) {
            stage_started_at = termite_metal_clock_monotonic_nanos();
            [command_buffer commit];
            [command_buffer waitUntilCompleted];
            if (timing != NULL) timing->command_wait_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;
            rc = command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -12;
        }
        if (rc == 0 && frame_owned && termite_metal_debug_direct_block_finite(layer_index)) {
            termite_metal_debug_dump_buffer_finite(runtime, "direct-q", layer_index, q_buffer, 0, attention_input_size);
            termite_metal_debug_dump_buffer_finite(runtime, "direct-attention", layer_index, attention_output_buffer, 0, attention_input_size);
            termite_metal_debug_dump_buffer_finite(runtime, "direct-attn-added", layer_index, attn_added_buffer, 0, hidden_size);
            termite_metal_debug_dump_buffer_finite(runtime, "direct-ffn-input", layer_index, ffn_input_buffer, 0, hidden_size);
            termite_metal_debug_dump_buffer_finite(runtime, "direct-output", layer_index, output_buffer, output_offset, hidden_size);
        }
        return rc;
    }
}

int termite_metal_decode_runtime_apply_attention_gated_block_q_slot_device_kv_device(
    termite_metal_decode_runtime *runtime,
    uint32_t format,
    void *attention_input_handle,
    size_t attention_input_offset,
    size_t q_linear_slot,
    void *k_handle,
    size_t k_offset,
    void *v_handle,
    size_t v_offset,
    size_t kv_tokens,
    size_t num_heads,
    size_t num_kv_heads,
    size_t head_dim,
    size_t key_row_bytes,
    size_t v_row_stride,
    size_t base_key_row_bytes,
    size_t query_position,
    size_t kv_position_offset,
    size_t sliding_window,
    size_t attention_linear_slot,
    size_t attention_pre_linear_rms_norm_slot,
    size_t attention_post_linear_rms_norm_slot,
    void *residual_handle,
    size_t residual_offset,
    size_t hidden_size,
    size_t attention_input_size,
    float eps,
    size_t ffn_layer_norm_slot,
    size_t ffn_rms_norm_slot,
    size_t ffn_post_gate_rms_norm_slot,
    size_t gate_ffn_linear_slot,
    size_t up_ffn_linear_slot,
    size_t down_ffn_linear_slot,
    size_t intermediate_size,
    uint32_t activation_kind,
    size_t layer_index,
    void *output_handle,
    size_t output_offset,
    termite_metal_attention_gated_block_timing *timing
) {
    if (runtime == NULL || attention_input_handle == NULL || k_handle == NULL || v_handle == NULL || residual_handle == NULL || output_handle == NULL) return -1;
    if (layer_index >= TERMITE_METAL_ATTENTION_SPAN_SLOT_CAPACITY) return -2;
    if (timing != NULL) memset(timing, 0, sizeof(*timing));
    if (q_linear_slot >= TERMITE_METAL_LINEAR_SLOT_CAPACITY) return -2;
    if (runtime->linear_pipeline == nil || runtime->linear_reduce_pipeline == nil) return -3;
    if (runtime->linear_slot_prepared[q_linear_slot] == 0) return -4;
    if (runtime->linear_in_dims[q_linear_slot] != hidden_size || runtime->linear_out_dims[q_linear_slot] != attention_input_size) return -5;
    if (hidden_size > UINT32_MAX || attention_input_size > UINT32_MAX) return -6;
    @autoreleasepool {
        const size_t q_bytes = attention_input_size * sizeof(float);
        const size_t hidden_bytes = hidden_size * sizeof(float);
        if (termite_metal_decode_runtime_ensure_direct_block_hidden_buffers(runtime, hidden_bytes) != 0) return -7;
        if (termite_metal_decode_runtime_ensure_direct_block_q_buffer(runtime, q_bytes) != 0) return -8;
        id<MTLBuffer> input_buffer = (__bridge id<MTLBuffer>)attention_input_handle;
        id<MTLBuffer> residual_buffer = (__bridge id<MTLBuffer>)residual_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        id<MTLBuffer> q_buffer = runtime->direct_block_q_buffer;
        id<MTLBuffer> attention_output_buffer = runtime->direct_block_hidden_buffers[0];
        id<MTLBuffer> attn_added_buffer = runtime->direct_block_hidden_buffers[1];
        id<MTLBuffer> ffn_input_buffer = runtime->direct_block_hidden_buffers[2];
        if (input_buffer == nil || residual_buffer == nil || output_buffer == nil || q_buffer == nil || attention_output_buffer == nil || attn_added_buffer == nil || ffn_input_buffer == nil) return -9;

        termite_metal_apply_linear_params q_params = {
            .in_dim = (uint32_t)hidden_size,
            .out_dim = (uint32_t)attention_input_size,
        };

        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -10;

        uint64_t stage_started_at = termite_metal_clock_monotonic_nanos();
        id<MTLComputeCommandEncoder> q_encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (q_encoder == nil) return -11;
        const BOOL use_q_reduce = attention_input_size >= 128;
        [q_encoder setComputePipelineState:(use_q_reduce ? runtime->linear_reduce_pipeline : runtime->linear_pipeline)];
        [q_encoder setBuffer:input_buffer offset:attention_input_offset atIndex:0];
        [q_encoder setBuffer:runtime->linear_weight_buffers[q_linear_slot] offset:0 atIndex:1];
        [q_encoder setBuffer:runtime->linear_bias_buffers[q_linear_slot] offset:0 atIndex:2];
        [q_encoder setBuffer:q_buffer offset:0 atIndex:3];
        [q_encoder setBytes:&q_params length:sizeof(q_params) atIndex:4];
        if (use_q_reduce) {
            [q_encoder dispatchThreads:MTLSizeMake(attention_input_size * 32u, 1, 1) threadsPerThreadgroup:MTLSizeMake(32, 1, 1)];
        } else {
            NSUInteger q_thread_width = runtime->linear_pipeline.maxTotalThreadsPerThreadgroup;
            if (q_thread_width == 0) q_thread_width = 64;
            if (q_thread_width > attention_input_size && attention_input_size > 0) q_thread_width = attention_input_size;
            [q_encoder dispatchThreads:MTLSizeMake(attention_input_size, 1, 1) threadsPerThreadgroup:MTLSizeMake(q_thread_width, 1, 1)];
        }
        [q_encoder endEncoding];
        if (timing != NULL) timing->attention_span_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;

        stage_started_at = termite_metal_clock_monotonic_nanos();
        int rc = termite_metal_decode_runtime_encode_replace_attention_span_from_f32_key_device_slot(
            runtime,
            command_buffer,
            layer_index,
            format,
            k_handle,
            k_offset,
            v_handle,
            v_offset,
            kv_tokens,
            num_kv_heads,
            head_dim,
            key_row_bytes,
            base_key_row_bytes,
            v_row_stride,
            kv_position_offset
        );
        if (rc != 0 && timing != NULL) {
            timing->failure_stage = 1;
            timing->failure_code = rc;
        }
        if (timing != NULL) timing->replace_span_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;
        if (rc == 0) {
            stage_started_at = termite_metal_clock_monotonic_nanos();
            rc = termite_metal_decode_runtime_encode_attention_span_slot(
                runtime,
                command_buffer,
                layer_index,
                format,
                q_buffer,
                0,
                kv_tokens,
                num_heads,
                num_kv_heads,
                head_dim,
                key_row_bytes,
                base_key_row_bytes,
                query_position,
                kv_position_offset,
                sliding_window,
                attention_output_buffer,
                0
            );
            if (rc != 0 && timing != NULL) {
                timing->failure_stage = 2;
                timing->failure_code = rc;
            }
            if (timing != NULL) timing->attention_span_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;
        }
        if (rc == 0) {
            stage_started_at = termite_metal_clock_monotonic_nanos();
            rc = termite_metal_decode_runtime_encode_attention_block_prefix(
                runtime,
                command_buffer,
                attention_output_buffer,
                0,
                attention_pre_linear_rms_norm_slot,
                attention_linear_slot,
                attention_post_linear_rms_norm_slot,
                residual_buffer,
                residual_offset,
                attention_input_size,
                hidden_size,
                eps,
                ffn_layer_norm_slot,
                ffn_rms_norm_slot,
                attn_added_buffer,
                0,
                ffn_input_buffer,
                0
            );
            if (rc != 0 && timing != NULL) {
                timing->failure_stage = 3;
                timing->failure_code = rc;
            }
            if (timing != NULL) timing->attention_prefix_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;
        }
        if (rc == 0) {
            stage_started_at = termite_metal_clock_monotonic_nanos();
            rc = termite_metal_decode_runtime_encode_gated_ffn_residual(
                runtime,
                command_buffer,
                gate_ffn_linear_slot,
                up_ffn_linear_slot,
                down_ffn_linear_slot,
                ffn_post_gate_rms_norm_slot,
                ffn_input_buffer,
                0,
                attn_added_buffer,
                0,
                hidden_size,
                intermediate_size,
                activation_kind,
                output_buffer,
                output_offset
            );
            if (rc != 0 && timing != NULL) {
                timing->failure_stage = 4;
                timing->failure_code = rc;
            }
            if (timing != NULL) timing->gated_ffn_residual_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;
        }
        if (rc == 0) {
            stage_started_at = termite_metal_clock_monotonic_nanos();
            [command_buffer commit];
            [command_buffer waitUntilCompleted];
            if (timing != NULL) timing->command_wait_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;
            rc = command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -12;
        }
        if (rc == 0 && termite_metal_debug_direct_block_finite(layer_index)) {
            termite_metal_debug_dump_buffer_finite(runtime, "direct-q", layer_index, q_buffer, 0, attention_input_size);
            termite_metal_debug_dump_buffer_finite(runtime, "direct-attention", layer_index, attention_output_buffer, 0, attention_input_size);
            termite_metal_debug_dump_buffer_finite(runtime, "direct-attn-added", layer_index, attn_added_buffer, 0, hidden_size);
            termite_metal_debug_dump_buffer_finite(runtime, "direct-ffn-input", layer_index, ffn_input_buffer, 0, hidden_size);
            termite_metal_debug_dump_buffer_finite(runtime, "direct-output", layer_index, output_buffer, output_offset, hidden_size);
        }
        return rc;
    }
}

int termite_metal_decode_runtime_apply_attention_gated_block_q8_0_device_kv_device(
    termite_metal_decode_runtime *runtime,
    uint32_t format,
    void *q_handle,
    size_t q_offset,
    void *k_handle,
    size_t k_offset,
    void *v_handle,
    size_t v_offset,
    size_t kv_tokens,
    size_t num_heads,
    size_t num_kv_heads,
    size_t head_dim,
    size_t key_row_bytes,
    size_t v_row_stride,
    size_t base_key_row_bytes,
    size_t query_position,
    size_t kv_position_offset,
    size_t sliding_window,
    size_t attention_linear_slot,
    size_t attention_pre_linear_rms_norm_slot,
    size_t attention_post_linear_rms_norm_slot,
    void *residual_handle,
    size_t residual_offset,
    size_t attention_input_size,
    size_t hidden_size,
    float eps,
    size_t ffn_layer_norm_slot,
    size_t ffn_rms_norm_slot,
    size_t ffn_post_gate_rms_norm_slot,
    size_t ffn_post_down_rms_norm_slot,
    size_t gate_ffn_linear_slot,
    size_t up_ffn_linear_slot,
    size_t down_ffn_linear_slot,
    size_t intermediate_size,
    uint32_t activation_kind,
    size_t layer_index,
    void *output_handle,
    size_t output_offset,
    termite_metal_attention_gated_block_timing *timing
) {
    if (runtime == NULL || q_handle == NULL || k_handle == NULL || v_handle == NULL || residual_handle == NULL || output_handle == NULL) return -1;
    if (layer_index >= TERMITE_METAL_ATTENTION_SPAN_SLOT_CAPACITY) return -2;
    if (timing != NULL) memset(timing, 0, sizeof(*timing));
    @autoreleasepool {
        const size_t hidden_bytes = hidden_size * sizeof(float);
        const size_t attention_input_bytes = attention_input_size * sizeof(float);
        const size_t scratch_bytes = attention_input_bytes > hidden_bytes ? attention_input_bytes : hidden_bytes;
        if (termite_metal_decode_runtime_ensure_direct_block_hidden_buffers(runtime, scratch_bytes) != 0) return -3;
        id<MTLBuffer> q_buffer = (__bridge id<MTLBuffer>)q_handle;
        id<MTLBuffer> residual_buffer = (__bridge id<MTLBuffer>)residual_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        id<MTLBuffer> attention_output_buffer = runtime->direct_block_hidden_buffers[0];
        id<MTLBuffer> attn_added_buffer = runtime->direct_block_hidden_buffers[1];
        id<MTLBuffer> ffn_input_buffer = runtime->direct_block_hidden_buffers[2];
        if (q_buffer == nil || residual_buffer == nil || output_buffer == nil || attention_output_buffer == nil || attn_added_buffer == nil || ffn_input_buffer == nil) return -4;

        const bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLCommandBuffer> command_buffer = frame_owned
            ? termite_metal_new_command_buffer(runtime->queue, __func__)
            : runtime->active_frame_cb;
        if (command_buffer == nil) return -5;

        uint64_t stage_started_at = termite_metal_clock_monotonic_nanos();
        int rc = termite_metal_decode_runtime_encode_replace_attention_span_from_f32_key_device_slot(
            runtime,
            command_buffer,
            layer_index,
            format,
            k_handle,
            k_offset,
            v_handle,
            v_offset,
            kv_tokens,
            num_kv_heads,
            head_dim,
            key_row_bytes,
            base_key_row_bytes,
            v_row_stride,
            kv_position_offset
        );
        if (rc != 0 && timing != NULL) {
            timing->failure_stage = 1;
            timing->failure_code = rc;
        }
        if (timing != NULL) timing->replace_span_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;

        if (rc == 0) {
            stage_started_at = termite_metal_clock_monotonic_nanos();
            rc = termite_metal_decode_runtime_encode_attention_span_slot(
                runtime,
                command_buffer,
                layer_index,
                format,
                q_buffer,
                q_offset,
                kv_tokens,
                num_heads,
                num_kv_heads,
                head_dim,
                key_row_bytes,
                base_key_row_bytes,
                query_position,
                kv_position_offset,
                sliding_window,
                attention_output_buffer,
                0
            );
            if (rc != 0 && timing != NULL) {
                timing->failure_stage = 2;
                timing->failure_code = rc;
            }
            if (timing != NULL) timing->attention_span_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;
        }

        if (rc == 0) {
            stage_started_at = termite_metal_clock_monotonic_nanos();
            rc = termite_metal_decode_runtime_encode_attention_quantized_residual_q8_0_slot(
                runtime,
                command_buffer,
                attention_output_buffer,
                attention_pre_linear_rms_norm_slot,
                attention_linear_slot,
                attention_post_linear_rms_norm_slot,
                residual_buffer,
                residual_offset,
                1,
                attention_input_size,
                hidden_size,
                eps,
                attn_added_buffer
            );
            if (rc != 0 && timing != NULL) {
                timing->failure_stage = 3;
                timing->failure_code = rc;
            }
            if (timing != NULL) timing->attention_prefix_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;
        }

        id<MTLBuffer> ffn_gate_input_buffer = attn_added_buffer;
        size_t ffn_gate_input_offset = 0;
        size_t ffn_pre_gate_rms_norm_slot = SIZE_MAX;
        if (rc == 0) {
            if (ffn_layer_norm_slot != SIZE_MAX) {
                termite_metal_apply_layer_norm_params params = { .hidden_size = (uint32_t)hidden_size, .eps = eps };
                id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_FFN);
                if (encoder == nil) return -6;
                [encoder setComputePipelineState:runtime->layer_norm_pipeline];
                [encoder setBuffer:attn_added_buffer offset:0 atIndex:0];
                [encoder setBuffer:runtime->layer_norm_weight_buffers[ffn_layer_norm_slot] offset:0 atIndex:1];
                [encoder setBuffer:runtime->layer_norm_bias_buffers[ffn_layer_norm_slot] offset:0 atIndex:2];
                [encoder setBuffer:ffn_input_buffer offset:0 atIndex:3];
                [encoder setBytes:&params length:sizeof(params) atIndex:4];
                [encoder dispatchThreads:MTLSizeMake(1, 1, 1) threadsPerThreadgroup:MTLSizeMake(1, 1, 1)];
                [encoder endEncoding];
                ffn_gate_input_buffer = ffn_input_buffer;
            } else if (ffn_rms_norm_slot != SIZE_MAX) {
                ffn_pre_gate_rms_norm_slot = ffn_rms_norm_slot;
            }
        }

        if (rc == 0) {
            stage_started_at = termite_metal_clock_monotonic_nanos();
            id<MTLCommandBuffer> previous_frame = runtime->active_frame_cb;
            runtime->active_frame_cb = command_buffer;
            rc = termite_metal_decode_runtime_apply_gated_ffn_residual_q8_0_slots_device_impl(
                runtime,
                (__bridge void *)ffn_gate_input_buffer,
                ffn_gate_input_offset,
                (__bridge void *)attn_added_buffer,
                0,
                1,
                hidden_size,
                intermediate_size,
                activation_kind,
                gate_ffn_linear_slot,
                up_ffn_linear_slot,
                ffn_pre_gate_rms_norm_slot,
                ffn_post_gate_rms_norm_slot,
                ffn_post_down_rms_norm_slot,
                eps,
                down_ffn_linear_slot,
                output_handle,
                output_offset,
                (termite_metal_planned_layer_contract){0}
            );
            runtime->active_frame_cb = previous_frame;
            if (rc != 0 && timing != NULL) {
                timing->failure_stage = 4;
                timing->failure_code = rc;
            }
            if (timing != NULL) timing->gated_ffn_residual_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;
        }

        if (rc == 0 && frame_owned) {
            stage_started_at = termite_metal_clock_monotonic_nanos();
            [command_buffer commit];
            [command_buffer waitUntilCompleted];
            if (timing != NULL) timing->command_wait_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;
            rc = command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -9;
        }
        if (rc == 0 && frame_owned && termite_metal_debug_q80_block_finite(layer_index)) {
            termite_metal_debug_dump_buffer_finite(runtime, "attention", layer_index, attention_output_buffer, 0, attention_input_size);
            termite_metal_debug_dump_buffer_finite(runtime, "attn-added", layer_index, attn_added_buffer, 0, hidden_size);
            termite_metal_debug_dump_buffer_finite(runtime, "ffn-input", layer_index, ffn_input_buffer, 0, hidden_size);
            termite_metal_debug_dump_buffer_finite(runtime, "ffn-gate", layer_index, runtime->hot_intermediate_buffers[0], 0, intermediate_size);
            termite_metal_debug_dump_buffer_finite(runtime, "ffn-up", layer_index, runtime->hot_intermediate_buffers[1], 0, intermediate_size);
            termite_metal_debug_dump_buffer_finite(runtime, "ffn-activated", layer_index, runtime->hot_intermediate_buffers[2], 0, intermediate_size);
            termite_metal_debug_dump_buffer_finite(runtime, "ffn-gated", layer_index, runtime->hot_intermediate_buffers[3], 0, intermediate_size);
            if (ffn_post_gate_rms_norm_slot != SIZE_MAX) {
                termite_metal_debug_dump_buffer_finite(runtime, "ffn-post-gate", layer_index, runtime->hot_intermediate_buffers[4], 0, intermediate_size);
            }
            termite_metal_debug_dump_buffer_finite(runtime, "ffn-projected", layer_index, runtime->hot_hidden_buffers[0], 0, hidden_size);
            if (ffn_post_down_rms_norm_slot != SIZE_MAX) {
                termite_metal_debug_dump_buffer_finite(runtime, "ffn-post-down", layer_index, runtime->hot_hidden_buffers[1], 0, hidden_size);
            }
            termite_metal_debug_dump_buffer_finite(runtime, "output", layer_index, output_buffer, output_offset, hidden_size);
        }
        return rc;
    }
}

int termite_metal_decode_runtime_apply_attention_f32_gated_block_q8_0_device_kv_device(
    termite_metal_decode_runtime *runtime,
    void *q_handle,
    size_t q_offset,
    void *k_handle,
    size_t k_offset,
    void *v_handle,
    size_t v_offset,
    size_t q_len,
    size_t kv_tokens,
    size_t num_heads,
    size_t num_kv_heads,
    size_t head_dim,
    size_t query_position_offset,
    size_t kv_position_offset,
    size_t sliding_window,
    size_t total_sequence_len,
    size_t attention_linear_slot,
    size_t attention_pre_linear_rms_norm_slot,
    size_t attention_post_linear_rms_norm_slot,
    void *residual_handle,
    size_t residual_offset,
    size_t attention_input_size,
    size_t hidden_size,
    float eps,
    size_t ffn_layer_norm_slot,
    size_t ffn_rms_norm_slot,
    size_t ffn_post_gate_rms_norm_slot,
    size_t ffn_post_down_rms_norm_slot,
    size_t gate_ffn_linear_slot,
    size_t up_ffn_linear_slot,
    size_t down_ffn_linear_slot,
    size_t intermediate_size,
    uint32_t activation_kind,
    size_t layer_index,
    void *ple_handle,
    size_t ple_offset,
    size_t ple_gate_linear_slot,
    size_t ple_proj_linear_slot,
    size_t ple_post_norm_slot,
    size_t ple_hidden_size,
    int output_scale_present,
    float output_scale,
    void *output_handle,
    size_t output_offset,
    termite_metal_planned_layer_contract planned_contract,
    termite_metal_attention_gated_block_timing *timing,
    size_t paged_slot,
    uint32_t paged_format,
    const uint32_t *block_table,
    size_t block_count,
    size_t page_size,
    size_t paged_key_row_bytes,
    size_t paged_base_key_row_bytes,
    size_t paged_v_row_stride,
    size_t suffix_tokens
) {
    const bool use_paged_kv = (block_table != NULL && block_count != 0 && page_size != 0 && paged_key_row_bytes != 0 && paged_base_key_row_bytes != 0 && paged_v_row_stride != 0);
    if (runtime == NULL || q_handle == NULL || residual_handle == NULL || output_handle == NULL) return -1;
    if (!use_paged_kv && (k_handle == NULL || v_handle == NULL)) return -1;
    if (use_paged_kv && suffix_tokens != 0 && (k_handle == NULL || v_handle == NULL)) return -1;
    if (timing != NULL) memset(timing, 0, sizeof(*timing));
    if (runtime->attention_f32_pipeline == nil) return -2;
    if (q_len == 0 || kv_tokens == 0 || num_heads == 0 || num_kv_heads == 0 || head_dim == 0) return -3;
    if (num_heads % num_kv_heads != 0) return -4;
    if (q_len > UINT32_MAX || kv_tokens > UINT32_MAX || num_heads > UINT32_MAX || num_kv_heads > UINT32_MAX || head_dim > UINT32_MAX || query_position_offset > UINT32_MAX || kv_position_offset > UINT32_MAX || sliding_window > UINT32_MAX || total_sequence_len > UINT32_MAX) return -5;
    if (q_len != 1 && ffn_layer_norm_slot != SIZE_MAX) return -18;
    @autoreleasepool {
        if (q_len > SIZE_MAX / hidden_size || q_len > SIZE_MAX / attention_input_size) return -6;
        const size_t hidden_bytes = q_len * hidden_size * sizeof(float);
        const size_t attention_input_bytes = q_len * attention_input_size * sizeof(float);
        const size_t scratch_bytes = attention_input_bytes > hidden_bytes ? attention_input_bytes : hidden_bytes;
        if (termite_metal_decode_runtime_ensure_direct_block_hidden_buffers(runtime, scratch_bytes) != 0) return -6;

        id<MTLBuffer> q_buffer = (__bridge id<MTLBuffer>)q_handle;
        id<MTLBuffer> k_buffer = (__bridge id<MTLBuffer>)k_handle;
        id<MTLBuffer> v_buffer = (__bridge id<MTLBuffer>)v_handle;
        id<MTLBuffer> residual_buffer = (__bridge id<MTLBuffer>)residual_handle;
        id<MTLBuffer> output_buffer = (__bridge id<MTLBuffer>)output_handle;
        id<MTLBuffer> attention_output_buffer = runtime->direct_block_hidden_buffers[0];
        id<MTLBuffer> attn_added_buffer = runtime->direct_block_hidden_buffers[1];
        id<MTLBuffer> ffn_input_buffer = runtime->direct_block_hidden_buffers[2];
        const bool has_ple = (ple_handle != NULL && ple_gate_linear_slot != SIZE_MAX && ple_proj_linear_slot != SIZE_MAX && ple_post_norm_slot != SIZE_MAX && ple_hidden_size != 0);
        const bool has_output_scale = (output_scale_present != 0);
        id<MTLBuffer> block_output_buffer = has_ple ? runtime->direct_block_hidden_buffers[3] : output_buffer;
        size_t block_output_offset = has_ple ? 0 : output_offset;
        if (q_buffer == nil || residual_buffer == nil || output_buffer == nil || attention_output_buffer == nil || attn_added_buffer == nil || ffn_input_buffer == nil || block_output_buffer == nil) return -7;
        if ((!use_paged_kv || suffix_tokens != 0) && (k_buffer == nil || v_buffer == nil)) return -7;
        if (runtime->active_frame_cb != nil) {
            if (termite_metal_decode_runtime_retain_frame_resource(runtime, q_buffer) != 0) return -19;
            if (k_buffer != nil && termite_metal_decode_runtime_retain_frame_resource(runtime, k_buffer) != 0) return -19;
            if (v_buffer != nil && termite_metal_decode_runtime_retain_frame_resource(runtime, v_buffer) != 0) return -19;
            if (termite_metal_decode_runtime_retain_frame_resource(runtime, residual_buffer) != 0) return -19;
            if (termite_metal_decode_runtime_retain_frame_resource(runtime, output_buffer) != 0) return -19;
        }
        if (has_ple) {
            id<MTLBuffer> ple_buffer = (__bridge id<MTLBuffer>)ple_handle;
            if (q_len > SIZE_MAX / ple_hidden_size) return -17;
            const size_t ple_bytes = q_len * ple_hidden_size * sizeof(float);
            if (ple_buffer == nil || ple_offset + ple_bytes > ple_buffer.length) return -17;
            if (runtime->active_frame_cb != nil && termite_metal_decode_runtime_retain_frame_resource(runtime, ple_buffer) != 0) return -19;
        }

        const size_t q_bytes = q_len * num_heads * head_dim * sizeof(float);
        const size_t kv_source_tokens = use_paged_kv ? suffix_tokens : kv_tokens;
        const size_t kv_bytes = kv_source_tokens * num_kv_heads * head_dim * sizeof(float);
        if (q_offset + q_bytes > q_buffer.length ||
            residual_offset + hidden_bytes > residual_buffer.length ||
            output_offset + hidden_bytes > output_buffer.length) return -8;
        if (kv_source_tokens != 0 &&
            (k_offset + kv_bytes > k_buffer.length || v_offset + kv_bytes > v_buffer.length)) return -8;

        const bool frame_owned = (runtime->active_frame_cb == nil);
        id<MTLCommandBuffer> command_buffer = frame_owned
            ? termite_metal_new_command_buffer(runtime->queue, __func__)
            : runtime->active_frame_cb;
        if (command_buffer == nil) return -9;

        int rc = 0;
        bool attention_residual_done = false;
        bool ffn_ple_done = false;
        uint64_t stage_started_at = termite_metal_clock_monotonic_nanos();
        if (q_len == 1 &&
            rc == 0 &&
            !attention_residual_done &&
            !ffn_ple_done &&
            has_ple &&
            attention_pre_linear_rms_norm_slot == SIZE_MAX &&
            attention_post_linear_rms_norm_slot != SIZE_MAX &&
            ffn_layer_norm_slot == SIZE_MAX &&
            ffn_rms_norm_slot != SIZE_MAX &&
            ffn_post_gate_rms_norm_slot == SIZE_MAX &&
            ffn_post_down_rms_norm_slot != SIZE_MAX &&
            runtime->active_frame_cb == command_buffer) {
            const int planned_status = termite_metal_encode_attention_ffn_ple_q8_0_planned_1x(
                runtime,
                command_buffer,
                q_buffer,
                q_offset,
                k_buffer,
                k_offset,
                v_buffer,
                v_offset,
                attention_output_buffer,
                kv_tokens,
                num_heads,
                num_kv_heads,
                head_dim,
                query_position_offset,
                kv_position_offset,
                sliding_window,
                total_sequence_len,
                attention_linear_slot,
                attention_post_linear_rms_norm_slot,
                residual_buffer,
                residual_offset,
                attn_added_buffer,
                block_output_buffer,
                block_output_offset,
                ffn_rms_norm_slot,
                gate_ffn_linear_slot,
                up_ffn_linear_slot,
                down_ffn_linear_slot,
                ffn_post_down_rms_norm_slot,
                (__bridge id<MTLBuffer>)ple_handle,
                ple_offset,
                ple_gate_linear_slot,
                ple_proj_linear_slot,
                ple_post_norm_slot,
                output_buffer,
                output_offset,
                hidden_size,
                attention_input_size,
                intermediate_size,
                ple_hidden_size,
                activation_kind,
                eps,
                has_output_scale ? 1 : 0,
                output_scale,
                planned_contract,
                use_paged_kv,
                paged_slot,
                paged_format,
                block_table,
                block_count,
                page_size,
                paged_key_row_bytes,
                paged_base_key_row_bytes,
                paged_v_row_stride,
                suffix_tokens,
                -24);
            if (planned_status == 0) {
                attention_residual_done = true;
                ffn_ple_done = true;
                if (timing != NULL) {
                    if (!use_paged_kv) timing->attention_f32_kernels += 1;
                    timing->q8_0_linear_kernels += 4;
                    timing->q8_0_attention_linear_kernels += 1;
                    timing->q8_0_ffn_down_linear_kernels += 1;
                    timing->q8_0_ple_linear_kernels += 2;
                    timing->q8_0_pair_activation_kernels += 1;
                    timing->rms_norm_add_kernels += 3;
                    timing->gated_ffn_residual_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;
                }
            } else {
                rc = planned_status;
                if (timing != NULL) {
                    timing->failure_stage = 4;
                    timing->failure_code = rc;
                }
            }
        }
        if (q_len == 1 &&
            rc == 0 &&
            !attention_residual_done &&
            attention_pre_linear_rms_norm_slot == SIZE_MAX &&
            attention_post_linear_rms_norm_slot != SIZE_MAX &&
            !use_paged_kv &&
            runtime->active_frame_cb == command_buffer) {
            termite_metal_quant_linear_slot_view attention_view;
            if (termite_metal_decode_runtime_require_quant_linear_slot(runtime, TERMITE_METAL_QUANT_FORMAT_Q8_0, attention_linear_slot, attention_input_size, hidden_size, &attention_view) == 0) {
                const int planned_status = termite_metal_encode_attention_q8_0_linear_rms_add_planned_1x(
                    runtime,
                    command_buffer,
                    q_buffer,
                    q_offset,
                    k_buffer,
                    k_offset,
                    v_buffer,
                    v_offset,
                    attention_output_buffer,
                    0,
                    kv_tokens,
                    num_heads,
                    num_kv_heads,
                    head_dim,
                    query_position_offset,
                    kv_position_offset,
                    sliding_window,
                    total_sequence_len,
                    attention_view.weight_buffer,
                    runtime->hot_hidden_buffers[3],
                    0,
                    attention_input_size,
                    hidden_size,
                    attention_post_linear_rms_norm_slot,
                    residual_buffer,
                    residual_offset,
                    attn_added_buffer,
                    0,
                    eps,
                    -10);
                if (planned_status == 0) {
                    attention_residual_done = true;
                    if (timing != NULL) {
                        timing->attention_f32_kernels += 1;
                        timing->q8_0_linear_kernels += 1;
                        timing->q8_0_attention_linear_kernels += 1;
                        timing->rms_norm_add_kernels += 1;
                        timing->attention_span_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;
                    }
                } else {
                    rc = planned_status;
                    if (timing != NULL) {
                        timing->failure_stage = 2;
                        timing->failure_code = rc;
                    }
                }
            }
        }
        if (rc == 0 && !attention_residual_done && use_paged_kv) {
            termite_metal_decode_runtime_record_logical_command_op(
                runtime,
                TERMITE_METAL_PLAN_OP_DECODE_ATTENTION,
                TERMITE_METAL_OPERATOR_ATTENTION_PAGED,
                TERMITE_METAL_QUANT_MATMUL_DISPATCH_UNKNOWN);
            const size_t previous_region = termite_metal_enter_compute_region(runtime, TERMITE_METAL_COMPUTE_REGION_ATTENTION);
            if (suffix_tokens != 0) {
                id<MTLComputeCommandEncoder> paged_encoder = runtime->active_planned_compute_encoder;
                if (paged_encoder != nil && paged_format == 2u) {
                    rc = termite_metal_encode_paged_f32_kv_seed_on_encoder(
                        runtime,
                        paged_encoder,
                        paged_slot,
                        k_buffer,
                        k_offset,
                        v_buffer,
                        v_offset,
                        kv_tokens,
                        suffix_tokens,
                        num_kv_heads,
                        head_dim,
                        paged_key_row_bytes,
                        paged_v_row_stride,
                        kv_position_offset,
                        block_table,
	                        block_count,
	                        page_size,
	                        -31);
	                } else if (paged_encoder != nil && (paged_format == 0u || paged_format == 1u || paged_format == 3u || paged_format == 4u)) {
                    rc = termite_metal_encode_paged_quantized_kv_seed_on_encoder(
                        runtime,
                        paged_encoder,
                        paged_slot,
                        paged_format,
                        k_buffer,
                        k_offset,
                        v_buffer,
                        v_offset,
                        kv_tokens,
                        suffix_tokens,
                        num_kv_heads,
                        head_dim,
                        paged_key_row_bytes,
                        paged_base_key_row_bytes,
                        paged_v_row_stride,
                        kv_position_offset,
                        block_table,
	                        block_count,
	                        page_size,
	                        -31);
	                } else {
                    rc = termite_metal_decode_runtime_encode_attention_paged_update_from_f32_key_device_slot(
                        runtime,
                        command_buffer,
                        paged_slot,
                        paged_format,
                        k_handle,
                        k_offset,
                        v_handle,
                        v_offset,
                        kv_tokens,
                        suffix_tokens,
                        num_kv_heads,
                        head_dim,
                        paged_key_row_bytes,
                        paged_base_key_row_bytes,
                        paged_v_row_stride,
                        kv_position_offset,
                        block_table,
                        block_count,
                        page_size
                    );
                }
            }
            if (rc == 0) {
                rc = termite_metal_decode_runtime_encode_paged_attention_slot(
                    runtime,
                    command_buffer,
                    paged_slot,
                    paged_format,
                    q_buffer,
                    q_offset,
                    block_table,
                    block_count,
                    page_size,
                    q_len,
                    kv_tokens,
                    num_heads,
                    num_kv_heads,
                    head_dim,
                    paged_key_row_bytes,
                    paged_base_key_row_bytes,
                    query_position_offset,
                    kv_position_offset,
                    sliding_window,
                    0.0f,
                    NULL,
                    0,
                    attention_output_buffer,
                    0
                );
            }
            termite_metal_restore_compute_region(runtime, previous_region);
            if (rc != 0 && timing != NULL) {
                timing->failure_stage = 2;
                timing->failure_code = rc;
            }
        }
        if (rc == 0 && !attention_residual_done && !use_paged_kv) {
            termite_metal_decode_runtime_record_logical_command_op(
                runtime,
                TERMITE_METAL_PLAN_OP_DECODE_ATTENTION,
                TERMITE_METAL_OPERATOR_ATTENTION_FLASH,
                TERMITE_METAL_QUANT_MATMUL_DISPATCH_UNKNOWN);
            const size_t previous_region = termite_metal_enter_compute_region(runtime, TERMITE_METAL_COMPUTE_REGION_ATTENTION);
            BOOL encoder_owned = YES;
            id<MTLComputeCommandEncoder> encoder = termite_metal_scoped_compute_encoder_for(runtime, command_buffer, TERMITE_METAL_COMPUTE_SOURCE_ATTENTION, &encoder_owned);
            if (encoder == nil) {
                termite_metal_restore_compute_region(runtime, previous_region);
                return -10;
            }
            rc = termite_metal_encode_attention_f32_on_encoder(
                runtime,
                encoder,
                q_buffer,
                q_offset,
                k_buffer,
                k_offset,
                v_buffer,
                v_offset,
                attention_output_buffer,
                0,
                q_len,
                kv_tokens,
                num_heads,
                num_kv_heads,
                head_dim,
                query_position_offset,
                kv_position_offset,
                sliding_window,
                total_sequence_len,
                -10);
            termite_metal_end_scoped_compute_encoder(encoder, encoder_owned);
            termite_metal_restore_compute_region(runtime, previous_region);
            if (rc != 0 && timing != NULL) {
                timing->failure_stage = 2;
                timing->failure_code = rc;
            }
            if (rc == 0 && timing != NULL) timing->attention_f32_kernels += 1;
        }
        if (!attention_residual_done && timing != NULL) timing->attention_span_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;

        if (rc == 0 && !attention_residual_done) {
            stage_started_at = termite_metal_clock_monotonic_nanos();
            const size_t previous_region = termite_metal_enter_compute_region(runtime, TERMITE_METAL_COMPUTE_REGION_ATTENTION_PROJECT);
            rc = termite_metal_decode_runtime_encode_attention_quantized_residual_q8_0_slot(
                runtime,
                command_buffer,
                attention_output_buffer,
                attention_pre_linear_rms_norm_slot,
                attention_linear_slot,
                attention_post_linear_rms_norm_slot,
                residual_buffer,
                residual_offset,
                q_len,
                attention_input_size,
                hidden_size,
                eps,
                attn_added_buffer
            );
            termite_metal_restore_compute_region(runtime, previous_region);
            if (rc != 0 && timing != NULL) {
                timing->failure_stage = 3;
                timing->failure_code = rc;
            }
            if (rc == 0 && timing != NULL) {
                if (attention_pre_linear_rms_norm_slot != SIZE_MAX) timing->rms_norm_kernels += 1;
                timing->q8_0_linear_kernels += 1;
                timing->q8_0_attention_linear_kernels += 1;
                if (attention_post_linear_rms_norm_slot != SIZE_MAX) {
                    timing->rms_norm_add_kernels += 1;
                } else {
                    timing->add_kernels += 1;
                }
            }
            if (timing != NULL) timing->attention_prefix_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;
        }

        id<MTLBuffer> ffn_gate_input_buffer = attn_added_buffer;
        size_t ffn_gate_input_offset = 0;
        size_t ffn_pre_gate_rms_norm_slot = SIZE_MAX;
        if (rc == 0) {
            const size_t previous_region = termite_metal_enter_compute_region(runtime, TERMITE_METAL_COMPUTE_REGION_FFN_NORM);
            if (ffn_layer_norm_slot != SIZE_MAX) {
                termite_metal_apply_layer_norm_params params = { .hidden_size = (uint32_t)hidden_size, .eps = eps };
                id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
                if (encoder == nil) {
                    termite_metal_restore_compute_region(runtime, previous_region);
                    return -13;
                }
                [encoder setComputePipelineState:runtime->layer_norm_pipeline];
                [encoder setBuffer:attn_added_buffer offset:0 atIndex:0];
                [encoder setBuffer:runtime->layer_norm_weight_buffers[ffn_layer_norm_slot] offset:0 atIndex:1];
                [encoder setBuffer:runtime->layer_norm_bias_buffers[ffn_layer_norm_slot] offset:0 atIndex:2];
                [encoder setBuffer:ffn_input_buffer offset:0 atIndex:3];
                [encoder setBytes:&params length:sizeof(params) atIndex:4];
                [encoder dispatchThreads:MTLSizeMake(1, 1, 1) threadsPerThreadgroup:MTLSizeMake(1, 1, 1)];
                [encoder endEncoding];
                ffn_gate_input_buffer = ffn_input_buffer;
                if (timing != NULL) timing->layer_norm_kernels += 1;
            } else if (ffn_rms_norm_slot != SIZE_MAX) {
                ffn_pre_gate_rms_norm_slot = ffn_rms_norm_slot;
            }
            termite_metal_restore_compute_region(runtime, previous_region);
        }

        if (rc == 0 &&
            q_len == 1 &&
            !ffn_ple_done &&
            has_ple &&
            ffn_layer_norm_slot == SIZE_MAX &&
            ffn_pre_gate_rms_norm_slot != SIZE_MAX &&
            ffn_post_gate_rms_norm_slot == SIZE_MAX &&
            ffn_post_down_rms_norm_slot != SIZE_MAX &&
            runtime->active_frame_cb == command_buffer) {
            stage_started_at = termite_metal_clock_monotonic_nanos();
            rc = termite_metal_encode_q8_0_ffn_pregate_ple_planned_1x(
                runtime,
                command_buffer,
                ffn_gate_input_buffer,
                ffn_gate_input_offset,
                ffn_pre_gate_rms_norm_slot,
                gate_ffn_linear_slot,
                up_ffn_linear_slot,
                down_ffn_linear_slot,
                ffn_post_down_rms_norm_slot,
                attn_added_buffer,
                0,
                block_output_buffer,
                block_output_offset,
                (__bridge id<MTLBuffer>)ple_handle,
                ple_offset,
                ple_gate_linear_slot,
                ple_proj_linear_slot,
                ple_post_norm_slot,
                output_buffer,
                output_offset,
                hidden_size,
                intermediate_size,
                ple_hidden_size,
                activation_kind,
                eps,
                has_output_scale ? 1 : 0,
                output_scale,
                -22);
            if (rc == 0) {
                ffn_ple_done = true;
                if (timing != NULL) {
                    timing->q8_0_pair_activation_kernels += 1;
                    timing->q8_0_linear_kernels += 3;
                    timing->q8_0_ffn_down_linear_kernels += 1;
                    timing->q8_0_ple_linear_kernels += 2;
                    timing->rms_norm_add_kernels += 2;
                    timing->gated_ffn_residual_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;
                }
            } else if (timing != NULL) {
                timing->failure_stage = 4;
                timing->failure_code = rc;
            }
        }

        if (rc == 0 && !ffn_ple_done) {
            stage_started_at = termite_metal_clock_monotonic_nanos();
            const size_t previous_region = termite_metal_enter_compute_region(runtime, TERMITE_METAL_COMPUTE_REGION_FFN);
            id<MTLCommandBuffer> previous_frame = runtime->active_frame_cb;
            runtime->active_frame_cb = command_buffer;
            rc = termite_metal_decode_runtime_apply_gated_ffn_residual_q8_0_slots_device_impl(
                runtime,
                (__bridge void *)ffn_gate_input_buffer,
                ffn_gate_input_offset,
                (__bridge void *)attn_added_buffer,
                0,
                q_len,
                hidden_size,
                intermediate_size,
                activation_kind,
                gate_ffn_linear_slot,
                up_ffn_linear_slot,
                ffn_pre_gate_rms_norm_slot,
                ffn_post_gate_rms_norm_slot,
                ffn_post_down_rms_norm_slot,
                eps,
                down_ffn_linear_slot,
                (__bridge void *)block_output_buffer,
                block_output_offset,
                planned_contract
            );
            runtime->active_frame_cb = previous_frame;
            termite_metal_restore_compute_region(runtime, previous_region);
            if (rc != 0 && timing != NULL) {
                timing->failure_stage = 4;
                timing->failure_code = rc;
            }
            if (rc == 0 && timing != NULL) {
                timing->q8_0_pair_activation_kernels += 1;
                if (ffn_post_gate_rms_norm_slot != SIZE_MAX) timing->rms_norm_kernels += 1;
                timing->q8_0_linear_kernels += 1;
                timing->q8_0_ffn_down_linear_kernels += 1;
                if (ffn_post_down_rms_norm_slot != SIZE_MAX) {
                    timing->rms_norm_add_kernels += 1;
                } else {
                    timing->add_kernels += 1;
                }
            }
            if (timing != NULL) timing->gated_ffn_residual_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;
        }

        if (rc == 0 && has_ple && !ffn_ple_done) {
            stage_started_at = termite_metal_clock_monotonic_nanos();
            const size_t previous_region = termite_metal_enter_compute_region(runtime, TERMITE_METAL_COMPUTE_REGION_PLE);
            rc = termite_metal_encode_ple_residual_q8_0_device(
                runtime,
                command_buffer,
                (__bridge void *)block_output_buffer,
                block_output_offset,
                ple_handle,
                ple_offset,
                ple_gate_linear_slot,
                ple_proj_linear_slot,
                ple_post_norm_slot,
                q_len,
                hidden_size,
                ple_hidden_size,
                eps,
                activation_kind,
                has_output_scale ? 1 : 0,
                output_scale,
                output_handle,
                output_offset
            );
            termite_metal_restore_compute_region(runtime, previous_region);
            if (rc != 0 && timing != NULL) {
                timing->failure_stage = 5;
                timing->failure_code = rc;
            }
            if (rc == 0 && timing != NULL) {
                timing->q8_0_linear_kernels += 2;
                timing->q8_0_ple_linear_kernels += 2;
                timing->rms_norm_add_kernels += 1;
            }
        }

        if (rc == 0 && has_output_scale && !has_ple) {
            const size_t previous_region = termite_metal_enter_compute_region(runtime, TERMITE_METAL_COMPUTE_REGION_TAIL);
            id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
            if (encoder == nil) {
                termite_metal_restore_compute_region(runtime, previous_region);
                return -21;
            }
            [encoder setComputePipelineState:runtime->scale_pipeline];
            [encoder setBuffer:output_buffer offset:output_offset atIndex:0];
            [encoder setBuffer:output_buffer offset:output_offset atIndex:1];
            [encoder setBytes:&output_scale length:sizeof(output_scale) atIndex:2];
            const size_t elems = q_len * hidden_size;
            [encoder dispatchThreads:MTLSizeMake(elems, 1, 1) threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->scale_pipeline, elems), 1, 1)];
            [encoder endEncoding];
            termite_metal_restore_compute_region(runtime, previous_region);
        }

        if (rc == 0 && frame_owned) {
            stage_started_at = termite_metal_clock_monotonic_nanos();
            [command_buffer commit];
            [command_buffer waitUntilCompleted];
            if (timing != NULL) timing->command_wait_nanos += termite_metal_clock_monotonic_nanos() - stage_started_at;
            if (timing != NULL) timing->gpu_nanos += termite_metal_command_buffer_gpu_elapsed_nanos(command_buffer);
            rc = command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -16;
        }
        if (rc == 0 && frame_owned && termite_metal_debug_q80_block_finite(layer_index)) {
            termite_metal_debug_dump_buffer_finite(runtime, "f32-attention", layer_index, attention_output_buffer, 0, attention_input_size);
            termite_metal_debug_dump_buffer_finite(runtime, "f32-attn-added", layer_index, attn_added_buffer, 0, hidden_size);
            termite_metal_debug_dump_buffer_finite(runtime, "f32-ffn-input", layer_index, ffn_input_buffer, 0, hidden_size);
            termite_metal_debug_dump_buffer_finite(runtime, "f32-output", layer_index, output_buffer, output_offset, hidden_size);
        }
        return rc;
    }
}

int termite_metal_decode_runtime_apply_prefill_gated_frame_layer_q8_0_device(
    termite_metal_decode_runtime *runtime,
    int shares_kv,
    uint32_t q_format,
    uint32_t k_format,
    uint32_t v_format,
    size_t q_slot,
    size_t k_slot,
    size_t v_slot,
    void *input_handle,
    size_t input_offset,
    size_t rows,
    size_t hidden_size,
    size_t attention_input_size,
    size_t kv_dim,
    size_t q_norm_slot,
    size_t k_norm_slot,
    int value_norm_present,
    void *value_norm_weight_handle,
    size_t value_norm_weight_offset,
    size_t num_heads,
    size_t num_kv_heads,
    size_t head_dim,
    size_t rope_dim,
    size_t position,
    float theta,
    float freq_scale,
    float eps,
    float query_value_scale,
    uint32_t consecutive_pairs,
    termite_metal_planned_layer_contract setup_contract,
    size_t kv_tokens,
    size_t query_position_offset,
    size_t kv_position_offset,
    size_t sliding_window,
    size_t total_sequence_len,
    size_t attention_linear_slot,
    size_t attention_pre_linear_rms_norm_slot,
    size_t attention_post_linear_rms_norm_slot,
    void *residual_handle,
    size_t residual_offset,
    size_t ffn_layer_norm_slot,
    size_t ffn_rms_norm_slot,
    size_t ffn_post_gate_rms_norm_slot,
    size_t ffn_post_down_rms_norm_slot,
    size_t gate_ffn_linear_slot,
    size_t up_ffn_linear_slot,
    size_t down_ffn_linear_slot,
    size_t intermediate_size,
    uint32_t activation_kind,
    size_t layer_index,
    void *ple_handle,
    size_t ple_offset,
    size_t ple_gate_linear_slot,
    size_t ple_proj_linear_slot,
    size_t ple_post_norm_slot,
    size_t ple_hidden_size,
    int output_scale_present,
    float output_scale,
    void *output_handle,
    size_t output_offset,
    termite_metal_planned_layer_contract block_contract,
    termite_metal_attention_gated_block_timing *timing,
    size_t paged_slot,
    uint32_t paged_format,
    const uint32_t *block_table,
    size_t block_count,
    size_t page_size,
    size_t paged_key_row_bytes,
    size_t paged_base_key_row_bytes,
    size_t paged_v_row_stride,
    size_t suffix_tokens
) {
    if (runtime == NULL || input_handle == NULL || residual_handle == NULL || output_handle == NULL) return -1;
    if (rows == 0 || hidden_size == 0 || attention_input_size == 0 || kv_tokens == 0) return -2;
    if (runtime->active_frame_cb == nil) return -3;
    if (q_format != TERMITE_METAL_QUANT_FORMAT_Q8_0) return -4;
    if (!shares_kv && (k_format != TERMITE_METAL_QUANT_FORMAT_Q8_0 || v_format != TERMITE_METAL_QUANT_FORMAT_Q8_0)) return -5;
    if (suffix_tokens != 0 && suffix_tokens != rows) return -6;

    void *q_ready_handle = NULL;
    void *k_ready_handle = NULL;
    void *v_ready_handle = NULL;
    int rc = termite_metal_decode_runtime_apply_prefill_quantized_setup_device(
        runtime,
        shares_kv,
        q_format,
        k_format,
        v_format,
        q_slot,
        k_slot,
        v_slot,
        input_handle,
        input_offset,
        rows,
        hidden_size,
        attention_input_size,
        shares_kv ? 0 : kv_dim,
        q_norm_slot,
        shares_kv ? 0 : k_norm_slot,
        value_norm_present,
        value_norm_weight_handle,
        value_norm_weight_offset,
        num_heads,
        num_kv_heads,
        head_dim,
        rope_dim,
        position,
        theta,
        freq_scale,
        eps,
        query_value_scale,
        consecutive_pairs,
        setup_contract,
        1,
        &q_ready_handle,
        &k_ready_handle,
        &v_ready_handle
    );
    if (rc != 0) {
        if (timing != NULL) {
            timing->failure_stage = 1;
            timing->failure_code = rc;
        }
        return rc;
    }
    if (q_ready_handle == NULL) {
        if (runtime->active_planned_compute_encoder != nil) (void)termite_metal_decode_runtime_end_planned_compute_scope(runtime);
        if (timing != NULL) {
            timing->failure_stage = 1;
            timing->failure_code = -7;
        }
        return -7;
    }
    if (!shares_kv && (k_ready_handle == NULL || v_ready_handle == NULL)) {
        if (runtime->active_planned_compute_encoder != nil) (void)termite_metal_decode_runtime_end_planned_compute_scope(runtime);
        if (timing != NULL) {
            timing->failure_stage = 1;
            timing->failure_code = -8;
        }
        return -8;
    }

    rc = termite_metal_decode_runtime_apply_attention_f32_gated_block_q8_0_device_kv_device(
        runtime,
        q_ready_handle,
        0,
        shares_kv ? NULL : k_ready_handle,
        0,
        shares_kv ? NULL : v_ready_handle,
        0,
        rows,
        kv_tokens,
        num_heads,
        num_kv_heads,
        head_dim,
        query_position_offset,
        kv_position_offset,
        sliding_window,
        total_sequence_len,
        attention_linear_slot,
        attention_pre_linear_rms_norm_slot,
        attention_post_linear_rms_norm_slot,
        residual_handle,
        residual_offset,
        attention_input_size,
        hidden_size,
        eps,
        ffn_layer_norm_slot,
        ffn_rms_norm_slot,
        ffn_post_gate_rms_norm_slot,
        ffn_post_down_rms_norm_slot,
        gate_ffn_linear_slot,
        up_ffn_linear_slot,
        down_ffn_linear_slot,
        intermediate_size,
        activation_kind,
        layer_index,
        ple_handle,
        ple_offset,
        ple_gate_linear_slot,
        ple_proj_linear_slot,
        ple_post_norm_slot,
        ple_hidden_size,
        output_scale_present,
        output_scale,
        output_handle,
        output_offset,
        block_contract,
        timing,
        paged_slot,
        paged_format,
        block_table,
        block_count,
        page_size,
        paged_key_row_bytes,
        paged_base_key_row_bytes,
        paged_v_row_stride,
        suffix_tokens
    );
    if (rc != 0 && runtime->active_planned_compute_encoder != nil) {
        (void)termite_metal_decode_runtime_end_planned_compute_scope(runtime);
    }
    return rc;
}

int termite_metal_decode_runtime_ready(termite_metal_decode_runtime *runtime) {
    if (runtime == NULL) return 0;
    return (runtime->device != nil && runtime->queue != nil && runtime->library != nil) ? 1 : 0;
}

int termite_metal_decode_runtime_reset_state(termite_metal_decode_runtime *runtime) {
    if (runtime == NULL) return -1;
    runtime->attention_span_tokens = 0;
    runtime->attention_span_key_row_bytes = 0;
    runtime->attention_span_v_row_stride = 0;
    runtime->attention_span_position_offset = 0;
    // Attention-span slots back MetalKvStorage's persistent per-sequence KV
    // cache. Runtime resets clear transient graph/frame state only; sequence
    // release owns persistent KV slot invalidation.
    for (size_t slot = 0; slot < TERMITE_METAL_SCRATCH_POOL_CAPACITY; ++slot) {
        runtime->scratch_pool_in_use[slot] = 0;
        runtime->scratch_pool_pending_frame_release[slot] = 0;
    }
    for (size_t slot = 0; slot < TERMITE_METAL_GRAPH_PLAN_SLOT_CAPACITY; ++slot) {
        runtime->graph_plan_requested_bytes[slot] = 0;
    }
    runtime->graph_plan_active = 0;
    // Phase 4 — abandon any in-flight frame. If a frame was active (not
    // yet submitted) its encoded work is simply dropped. If it was
    // submitted but not waited on, we still release our reference;
    // Metal will complete it in the background.
    runtime->active_frame_cb = nil;
    runtime->submitted_frame_cb = nil;
    runtime->active_planned_compute_encoder = nil;
    termite_metal_decode_runtime_reset_planned_compute_ranges(runtime);
    runtime->active_frame_compute_encoder_count = 0;
    runtime->active_frame_blit_encoder_count = 0;
    runtime->active_frame_planned_compute_scope_count = 0;
    runtime->active_frame_planned_barrier_count = 0;
    runtime->submitted_frame_compute_encoder_count = 0;
    runtime->submitted_frame_blit_encoder_count = 0;
    runtime->submitted_frame_planned_compute_scope_count = 0;
    runtime->submitted_frame_planned_barrier_count = 0;
    runtime->active_planned_compute_source = TERMITE_METAL_COMPUTE_SOURCE_OTHER;
    runtime->active_planned_compute_region = TERMITE_METAL_COMPUTE_REGION_OTHER;
    memset(runtime->active_frame_compute_source_counts, 0, sizeof(runtime->active_frame_compute_source_counts));
    memset(runtime->submitted_frame_compute_source_counts, 0, sizeof(runtime->submitted_frame_compute_source_counts));
    memset(runtime->active_frame_compute_region_counts, 0, sizeof(runtime->active_frame_compute_region_counts));
    memset(runtime->submitted_frame_compute_region_counts, 0, sizeof(runtime->submitted_frame_compute_region_counts));
    memset(runtime->active_frame_compute_region_source_counts, 0, sizeof(runtime->active_frame_compute_region_source_counts));
    memset(runtime->submitted_frame_compute_region_source_counts, 0, sizeof(runtime->submitted_frame_compute_region_source_counts));
    memset(runtime->active_frame_planned_command_op_kind_counts, 0, sizeof(runtime->active_frame_planned_command_op_kind_counts));
    memset(runtime->submitted_frame_planned_command_op_kind_counts, 0, sizeof(runtime->submitted_frame_planned_command_op_kind_counts));
    memset(runtime->active_frame_planned_command_operator_counts, 0, sizeof(runtime->active_frame_planned_command_operator_counts));
    memset(runtime->submitted_frame_planned_command_operator_counts, 0, sizeof(runtime->submitted_frame_planned_command_operator_counts));
    memset(runtime->active_frame_planned_command_quant_dispatch_counts, 0, sizeof(runtime->active_frame_planned_command_quant_dispatch_counts));
    memset(runtime->submitted_frame_planned_command_quant_dispatch_counts, 0, sizeof(runtime->submitted_frame_planned_command_quant_dispatch_counts));
    memset(runtime->active_frame_blit_source_counts, 0, sizeof(runtime->active_frame_blit_source_counts));
    memset(runtime->submitted_frame_blit_source_counts, 0, sizeof(runtime->submitted_frame_blit_source_counts));
    if (termite_metal_active_encoder_runtime == runtime) {
        termite_metal_active_encoder_runtime = NULL;
    }
    termite_metal_decode_runtime_clear_frame_resources(&runtime->active_frame_retained_resources);
    termite_metal_decode_runtime_clear_frame_resources(&runtime->submitted_frame_retained_resources);
    runtime->frame_cb_count = 0;
    return 0;
}

// Phase 4 — frame API. See struct declaration for semantics. When a
// frame is active, internal `encode_*` helpers and public
// `apply_*_device` entrypoints that opt in will encode into the active
// CB and skip the per-call commit/wait. This collapses the ~96
// command-buffer submissions per Gemma token (3 CBs × 32 layers) down
// to a single CB per layer (first pass) or per token (final pass).
static void termite_metal_decode_runtime_clear_frame_resources(CFMutableArrayRef *resources) {
    if (resources == NULL || *resources == NULL) return;
    CFRelease(*resources);
    *resources = NULL;
}

static void termite_metal_decode_runtime_finish_frame_scratch_releases(termite_metal_decode_runtime *runtime) {
    if (runtime == NULL) return;
    for (size_t slot = 0; slot < TERMITE_METAL_SCRATCH_POOL_CAPACITY; ++slot) {
        if (runtime->scratch_pool_pending_frame_release[slot] == 0) continue;
        runtime->scratch_pool_pending_frame_release[slot] = 0;
        runtime->scratch_pool_in_use[slot] = 0;
    }
}

static bool termite_metal_decode_runtime_active_frame_empty(termite_metal_decode_runtime *runtime) {
    if (runtime == NULL || runtime->active_frame_cb == nil) return false;
    return runtime->active_frame_compute_encoder_count == 0 &&
        runtime->active_frame_blit_encoder_count == 0 &&
        runtime->active_frame_planned_compute_scope_count == 0 &&
        runtime->active_frame_planned_barrier_count == 0 &&
        runtime->active_frame_planned_command_op_count == 0 &&
        runtime->active_planned_compute_encoder == nil;
}

static int termite_metal_decode_runtime_retain_frame_resource(termite_metal_decode_runtime *runtime, id resource) {
    if (runtime == NULL || resource == nil) return -1;
    CFMutableArrayRef *resources = NULL;
    if (runtime->active_frame_cb != nil) {
        resources = &runtime->active_frame_retained_resources;
    } else if (runtime->submitted_frame_cb != nil) {
        resources = &runtime->submitted_frame_retained_resources;
    } else {
        return 0;
    }
    if (*resources == NULL) {
        *resources = CFArrayCreateMutable(kCFAllocatorDefault, 0, &kCFTypeArrayCallBacks);
        if (*resources == NULL) return -2;
    }
    CFArrayAppendValue(*resources, (__bridge const void *)resource);
    return 0;
}

int termite_metal_decode_runtime_retain_active_frame_buffer(termite_metal_decode_runtime *runtime, void *handle) {
    if (runtime == NULL || handle == NULL) return -1;
    if (runtime->active_frame_cb == nil) return 0;
    id<MTLBuffer> buffer = (__bridge id<MTLBuffer>)handle;
    if (buffer == nil) return -2;
    return termite_metal_decode_runtime_retain_frame_resource(runtime, buffer);
}

int termite_metal_decode_runtime_begin_frame(termite_metal_decode_runtime *runtime) {
    if (runtime == NULL) return -1;
    if (runtime->queue == nil) return -2;
    if (runtime->active_frame_cb != nil) return -3;
    if (runtime->submitted_frame_cb != nil) return -4;
    termite_metal_decode_runtime_finish_frame_scratch_releases(runtime);
    @autoreleasepool {
        id<MTLCommandBuffer> cb = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (cb == nil) return -5;
        cb.label = @"termite_metal_decode_runtime_frame";
        runtime->active_frame_cb = cb;
        runtime->active_frame_compute_encoder_count = 0;
        runtime->active_frame_blit_encoder_count = 0;
        runtime->active_frame_planned_compute_scope_count = 0;
        runtime->active_frame_planned_barrier_count = 0;
        runtime->active_frame_planned_command_op_count = 0;
        runtime->active_frame_host_staging_offset = 0;
        runtime->active_frame_host_staging_retained_buffer = nil;
        runtime->active_planned_compute_encoder = nil;
        termite_metal_decode_runtime_reset_planned_compute_ranges(runtime);
        runtime->active_planned_compute_scope_closed_for_encoder_transition = false;
        runtime->planned_compute_barrier_suppression_depth = 0;
        runtime->active_planned_compute_source = TERMITE_METAL_COMPUTE_SOURCE_OTHER;
        runtime->active_planned_compute_region = TERMITE_METAL_COMPUTE_REGION_OTHER;
        runtime->active_compute_region = TERMITE_METAL_COMPUTE_REGION_OTHER;
        memset(runtime->active_frame_compute_source_counts, 0, sizeof(runtime->active_frame_compute_source_counts));
        memset(runtime->active_frame_compute_region_counts, 0, sizeof(runtime->active_frame_compute_region_counts));
        memset(runtime->active_frame_compute_region_source_counts, 0, sizeof(runtime->active_frame_compute_region_source_counts));
        memset(runtime->active_frame_planned_command_op_kind_counts, 0, sizeof(runtime->active_frame_planned_command_op_kind_counts));
        memset(runtime->active_frame_planned_command_operator_counts, 0, sizeof(runtime->active_frame_planned_command_operator_counts));
        memset(runtime->active_frame_planned_command_quant_dispatch_counts, 0, sizeof(runtime->active_frame_planned_command_quant_dispatch_counts));
        memset(runtime->active_frame_blit_source_counts, 0, sizeof(runtime->active_frame_blit_source_counts));
        termite_metal_active_encoder_runtime = runtime;
        if (termite_metal_trace_frame_lifecycle_enabled()) {
            fprintf(stderr, "metal_frame_lifecycle: begin\n");
        }
        return 0;
    }
}

int termite_metal_decode_runtime_submit_frame(termite_metal_decode_runtime *runtime) {
    if (runtime == NULL) return -1;
    if (runtime->active_frame_cb == nil) return -2;
    if (runtime->submitted_frame_cb != nil) return -3;
    id<MTLCommandBuffer> cb = runtime->active_frame_cb;
    if (runtime->active_planned_compute_encoder != nil) {
        termite_metal_decode_runtime_close_planned_compute_encoder_for_transition(runtime);
    }
    if (termite_metal_trace_frame_lifecycle_enabled()) {
        fprintf(
            stderr,
            "metal_frame_lifecycle: submit compute=%llu blit=%llu planned_scopes=%llu planned_barriers=%llu\n",
            (unsigned long long)runtime->active_frame_compute_encoder_count,
            (unsigned long long)runtime->active_frame_blit_encoder_count,
            (unsigned long long)runtime->active_frame_planned_compute_scope_count,
            (unsigned long long)runtime->active_frame_planned_barrier_count
        );
    }
    runtime->last_frame_gpu_nanos = 0;
    [cb commit];
    runtime->submitted_frame_cb = cb;
    runtime->active_frame_cb = nil;
    runtime->submitted_frame_compute_encoder_count = runtime->active_frame_compute_encoder_count;
    runtime->submitted_frame_blit_encoder_count = runtime->active_frame_blit_encoder_count;
    runtime->submitted_frame_planned_compute_scope_count = runtime->active_frame_planned_compute_scope_count;
    runtime->submitted_frame_planned_barrier_count = runtime->active_frame_planned_barrier_count;
    runtime->submitted_frame_mps_dense_linear_count = runtime->active_frame_mps_dense_linear_count;
    runtime->submitted_frame_planned_command_op_count = runtime->active_frame_planned_command_op_count;
    memcpy(runtime->submitted_frame_compute_source_counts, runtime->active_frame_compute_source_counts, sizeof(runtime->submitted_frame_compute_source_counts));
    memcpy(runtime->submitted_frame_compute_region_counts, runtime->active_frame_compute_region_counts, sizeof(runtime->submitted_frame_compute_region_counts));
    memcpy(runtime->submitted_frame_compute_region_source_counts, runtime->active_frame_compute_region_source_counts, sizeof(runtime->submitted_frame_compute_region_source_counts));
    memcpy(runtime->submitted_frame_planned_command_op_kind_counts, runtime->active_frame_planned_command_op_kind_counts, sizeof(runtime->submitted_frame_planned_command_op_kind_counts));
    memcpy(runtime->submitted_frame_planned_command_operator_counts, runtime->active_frame_planned_command_operator_counts, sizeof(runtime->submitted_frame_planned_command_operator_counts));
    memcpy(runtime->submitted_frame_planned_command_quant_dispatch_counts, runtime->active_frame_planned_command_quant_dispatch_counts, sizeof(runtime->submitted_frame_planned_command_quant_dispatch_counts));
    memcpy(runtime->submitted_frame_blit_source_counts, runtime->active_frame_blit_source_counts, sizeof(runtime->submitted_frame_blit_source_counts));
    runtime->active_frame_compute_encoder_count = 0;
    runtime->active_frame_blit_encoder_count = 0;
    runtime->active_frame_planned_compute_scope_count = 0;
    runtime->active_frame_planned_barrier_count = 0;
    runtime->active_frame_mps_dense_linear_count = 0;
    runtime->active_frame_planned_command_op_count = 0;
    runtime->active_planned_compute_encoder = nil;
    termite_metal_decode_runtime_reset_planned_compute_ranges(runtime);
    runtime->active_planned_compute_scope_closed_for_encoder_transition = false;
    runtime->planned_compute_barrier_suppression_depth = 0;
    runtime->active_planned_compute_source = TERMITE_METAL_COMPUTE_SOURCE_OTHER;
    runtime->active_planned_compute_region = TERMITE_METAL_COMPUTE_REGION_OTHER;
    runtime->active_compute_region = TERMITE_METAL_COMPUTE_REGION_OTHER;
    memset(runtime->active_frame_compute_source_counts, 0, sizeof(runtime->active_frame_compute_source_counts));
    memset(runtime->active_frame_compute_region_counts, 0, sizeof(runtime->active_frame_compute_region_counts));
    memset(runtime->active_frame_compute_region_source_counts, 0, sizeof(runtime->active_frame_compute_region_source_counts));
    memset(runtime->active_frame_planned_command_op_kind_counts, 0, sizeof(runtime->active_frame_planned_command_op_kind_counts));
    memset(runtime->active_frame_planned_command_operator_counts, 0, sizeof(runtime->active_frame_planned_command_operator_counts));
    memset(runtime->active_frame_planned_command_quant_dispatch_counts, 0, sizeof(runtime->active_frame_planned_command_quant_dispatch_counts));
    memset(runtime->active_frame_blit_source_counts, 0, sizeof(runtime->active_frame_blit_source_counts));
    if (termite_metal_active_encoder_runtime == runtime) {
        termite_metal_active_encoder_runtime = NULL;
    }
    runtime->submitted_frame_retained_resources = runtime->active_frame_retained_resources;
    runtime->active_frame_retained_resources = NULL;
    runtime->frame_cb_count += 1;
    return 0;
}

int termite_metal_decode_runtime_cancel_frame(termite_metal_decode_runtime *runtime) {
    if (runtime == NULL) return -1;
    if (runtime->active_frame_cb == nil) return -2;
    if (termite_metal_trace_frame_lifecycle_enabled()) {
        fprintf(
            stderr,
            "metal_frame_lifecycle: cancel compute=%llu blit=%llu planned_scopes=%llu planned_barriers=%llu\n",
            (unsigned long long)runtime->active_frame_compute_encoder_count,
            (unsigned long long)runtime->active_frame_blit_encoder_count,
            (unsigned long long)runtime->active_frame_planned_compute_scope_count,
            (unsigned long long)runtime->active_frame_planned_barrier_count
        );
    }
    if (runtime->active_planned_compute_encoder != nil) {
        [runtime->active_planned_compute_encoder endEncoding];
        runtime->active_planned_compute_encoder = nil;
    }
    termite_metal_decode_runtime_reset_planned_compute_ranges(runtime);
    runtime->active_planned_compute_scope_closed_for_encoder_transition = false;
    runtime->planned_compute_barrier_suppression_depth = 0;
    runtime->active_frame_cb = nil;
    runtime->active_frame_compute_encoder_count = 0;
    runtime->active_frame_blit_encoder_count = 0;
    runtime->active_frame_planned_compute_scope_count = 0;
    runtime->active_frame_planned_barrier_count = 0;
    runtime->active_frame_mps_dense_linear_count = 0;
    runtime->active_frame_planned_command_op_count = 0;
    runtime->active_frame_host_staging_offset = 0;
    runtime->active_frame_host_staging_retained_buffer = nil;
    runtime->active_planned_compute_source = TERMITE_METAL_COMPUTE_SOURCE_OTHER;
    runtime->active_planned_compute_region = TERMITE_METAL_COMPUTE_REGION_OTHER;
    runtime->active_compute_region = TERMITE_METAL_COMPUTE_REGION_OTHER;
    memset(runtime->active_frame_compute_source_counts, 0, sizeof(runtime->active_frame_compute_source_counts));
    memset(runtime->active_frame_compute_region_counts, 0, sizeof(runtime->active_frame_compute_region_counts));
    memset(runtime->active_frame_compute_region_source_counts, 0, sizeof(runtime->active_frame_compute_region_source_counts));
    memset(runtime->active_frame_planned_command_op_kind_counts, 0, sizeof(runtime->active_frame_planned_command_op_kind_counts));
    memset(runtime->active_frame_planned_command_operator_counts, 0, sizeof(runtime->active_frame_planned_command_operator_counts));
    memset(runtime->active_frame_planned_command_quant_dispatch_counts, 0, sizeof(runtime->active_frame_planned_command_quant_dispatch_counts));
    memset(runtime->active_frame_blit_source_counts, 0, sizeof(runtime->active_frame_blit_source_counts));
    if (termite_metal_active_encoder_runtime == runtime) {
        termite_metal_active_encoder_runtime = NULL;
    }
    termite_metal_decode_runtime_clear_frame_resources(&runtime->active_frame_retained_resources);
    termite_metal_decode_runtime_finish_frame_scratch_releases(runtime);
    return 0;
}

int termite_metal_decode_runtime_wait_frame(termite_metal_decode_runtime *runtime) {
    if (runtime == NULL) return -1;
    id<MTLCommandBuffer> cb = runtime->submitted_frame_cb;
    if (cb == nil) return -2;
    [cb waitUntilCompleted];
    int status_code = (cb.status == MTLCommandBufferStatusCompleted) ? 0 : -3;
    runtime->last_frame_gpu_nanos = status_code == 0 ? termite_metal_command_buffer_gpu_elapsed_nanos(cb) : 0;
    runtime->last_frame_compute_encoder_count = runtime->submitted_frame_compute_encoder_count;
    runtime->last_frame_blit_encoder_count = runtime->submitted_frame_blit_encoder_count;
    runtime->last_frame_planned_compute_scope_count = runtime->submitted_frame_planned_compute_scope_count;
    runtime->last_frame_planned_barrier_count = runtime->submitted_frame_planned_barrier_count;
    runtime->last_frame_mps_dense_linear_count = runtime->submitted_frame_mps_dense_linear_count;
    runtime->last_frame_planned_command_op_count = runtime->submitted_frame_planned_command_op_count;
    memcpy(runtime->last_frame_compute_source_counts, runtime->submitted_frame_compute_source_counts, sizeof(runtime->last_frame_compute_source_counts));
    memcpy(runtime->last_frame_compute_region_counts, runtime->submitted_frame_compute_region_counts, sizeof(runtime->last_frame_compute_region_counts));
    memcpy(runtime->last_frame_compute_region_source_counts, runtime->submitted_frame_compute_region_source_counts, sizeof(runtime->last_frame_compute_region_source_counts));
    memcpy(runtime->last_frame_planned_command_op_kind_counts, runtime->submitted_frame_planned_command_op_kind_counts, sizeof(runtime->last_frame_planned_command_op_kind_counts));
    memcpy(runtime->last_frame_planned_command_operator_counts, runtime->submitted_frame_planned_command_operator_counts, sizeof(runtime->last_frame_planned_command_operator_counts));
    memcpy(runtime->last_frame_planned_command_quant_dispatch_counts, runtime->submitted_frame_planned_command_quant_dispatch_counts, sizeof(runtime->last_frame_planned_command_quant_dispatch_counts));
    memcpy(runtime->last_frame_blit_source_counts, runtime->submitted_frame_blit_source_counts, sizeof(runtime->last_frame_blit_source_counts));
    termite_metal_maybe_trace_last_frame(runtime);
    runtime->submitted_frame_compute_encoder_count = 0;
    runtime->submitted_frame_blit_encoder_count = 0;
    runtime->submitted_frame_planned_compute_scope_count = 0;
    runtime->submitted_frame_planned_barrier_count = 0;
    runtime->submitted_frame_mps_dense_linear_count = 0;
    runtime->submitted_frame_planned_command_op_count = 0;
    memset(runtime->submitted_frame_compute_source_counts, 0, sizeof(runtime->submitted_frame_compute_source_counts));
    memset(runtime->submitted_frame_compute_region_counts, 0, sizeof(runtime->submitted_frame_compute_region_counts));
    memset(runtime->submitted_frame_compute_region_source_counts, 0, sizeof(runtime->submitted_frame_compute_region_source_counts));
    memset(runtime->submitted_frame_planned_command_op_kind_counts, 0, sizeof(runtime->submitted_frame_planned_command_op_kind_counts));
    memset(runtime->submitted_frame_planned_command_operator_counts, 0, sizeof(runtime->submitted_frame_planned_command_operator_counts));
    memset(runtime->submitted_frame_planned_command_quant_dispatch_counts, 0, sizeof(runtime->submitted_frame_planned_command_quant_dispatch_counts));
    memset(runtime->submitted_frame_blit_source_counts, 0, sizeof(runtime->submitted_frame_blit_source_counts));
    if (status_code != 0) {
        termite_metal_log_command_buffer_failure(cb, __func__);
    }
    runtime->submitted_frame_cb = nil;
    termite_metal_decode_runtime_clear_frame_resources(&runtime->submitted_frame_retained_resources);
    runtime->active_frame_host_staging_offset = 0;
    runtime->active_frame_host_staging_retained_buffer = nil;
    termite_metal_decode_runtime_finish_frame_scratch_releases(runtime);
    return status_code;
}

int termite_metal_decode_runtime_flush_active_frame(termite_metal_decode_runtime *runtime) {
    if (runtime == NULL) return -1;
    const bool had_active_frame = (runtime->active_frame_cb != nil);
    if (termite_metal_trace_frame_lifecycle_enabled()) {
        fprintf(
            stderr,
            "metal_frame_lifecycle: flush had_active=%d submitted=%d compute=%llu blit=%llu planned_scopes=%llu planned_barriers=%llu\n",
            had_active_frame ? 1 : 0,
            runtime->submitted_frame_cb != nil ? 1 : 0,
            (unsigned long long)runtime->active_frame_compute_encoder_count,
            (unsigned long long)runtime->active_frame_blit_encoder_count,
            (unsigned long long)runtime->active_frame_planned_compute_scope_count,
            (unsigned long long)runtime->active_frame_planned_barrier_count
        );
    }
    if (runtime->submitted_frame_cb != nil) {
        int wait_rc = termite_metal_decode_runtime_wait_frame(runtime);
        if (wait_rc != 0) return wait_rc;
    }
    if (!had_active_frame) return 0;
    if (termite_metal_decode_runtime_active_frame_empty(runtime)) {
        int cancel_rc = termite_metal_decode_runtime_cancel_frame(runtime);
        if (cancel_rc != 0) return cancel_rc;
        return termite_metal_decode_runtime_begin_frame(runtime);
    }
    int submit_rc = termite_metal_decode_runtime_submit_frame(runtime);
    if (submit_rc != 0) return submit_rc;
    int wait_rc = termite_metal_decode_runtime_wait_frame(runtime);
    if (wait_rc != 0) return wait_rc;
    return termite_metal_decode_runtime_begin_frame(runtime);
}

int termite_metal_decode_runtime_active_frame_has_work(termite_metal_decode_runtime *runtime) {
    if (runtime == NULL || runtime->active_frame_cb == nil) return 0;
    return termite_metal_decode_runtime_active_frame_empty(runtime) ? 0 : 1;
}

int termite_metal_decode_runtime_has_active_frame(termite_metal_decode_runtime *runtime) {
    if (runtime == NULL) return 0;
    return runtime->active_frame_cb != nil ? 1 : 0;
}

int termite_metal_decode_runtime_push_compute_region(termite_metal_decode_runtime *runtime, size_t region, size_t *previous_out) {
    if (runtime == NULL || previous_out == NULL) return -1;
    if (region >= TERMITE_METAL_COMPUTE_REGION_COUNTER_COUNT) return -2;
    *previous_out = termite_metal_enter_compute_region(runtime, region);
    return 0;
}

int termite_metal_decode_runtime_pop_compute_region(termite_metal_decode_runtime *runtime, size_t previous) {
    if (runtime == NULL) return -1;
    termite_metal_restore_compute_region(runtime, previous);
    return 0;
}

uint64_t termite_metal_decode_runtime_frame_cb_count(termite_metal_decode_runtime *runtime) {
    if (runtime == NULL) return 0;
    return runtime->frame_cb_count;
}

uint64_t termite_metal_decode_runtime_last_frame_gpu_nanos(termite_metal_decode_runtime *runtime) {
    if (runtime == NULL) return 0;
    return runtime->last_frame_gpu_nanos;
}

int termite_metal_decode_runtime_memory_snapshot(
    termite_metal_decode_runtime *runtime,
    termite_metal_decode_runtime_memory_stats *snapshot
) {
    if (snapshot == NULL) return -1;
    memset(snapshot, 0, sizeof(*snapshot));
    if (runtime == NULL) return -2;

    termite_metal_decode_runtime_memory_add_buffer(snapshot, runtime->token_embedding_table_buffer, &snapshot->embedding_bytes);
    termite_metal_decode_runtime_memory_add_buffer(snapshot, runtime->position_embedding_table_buffer, &snapshot->embedding_bytes);
    for (size_t slot = 0; slot < TERMITE_METAL_GENERIC_EMBEDDING_CACHE_CAPACITY; ++slot) {
        termite_metal_decode_runtime_memory_add_buffer(snapshot, runtime->generic_embedding_cache[slot].buffer, &snapshot->embedding_bytes);
    }

    for (size_t slot = 0; slot < TERMITE_METAL_LAYER_NORM_SLOT_CAPACITY; ++slot) {
        termite_metal_decode_runtime_memory_add_buffer(snapshot, runtime->layer_norm_weight_buffers[slot], &snapshot->norm_bytes);
        termite_metal_decode_runtime_memory_add_buffer(snapshot, runtime->layer_norm_bias_buffers[slot], &snapshot->norm_bytes);
    }
    for (size_t slot = 0; slot < TERMITE_METAL_RMS_NORM_SLOT_CAPACITY; ++slot) {
        termite_metal_decode_runtime_memory_add_buffer(snapshot, runtime->rms_norm_weight_buffers[slot], &snapshot->norm_bytes);
    }

    for (size_t slot = 0; slot < TERMITE_METAL_LINEAR_SLOT_CAPACITY; ++slot) {
        termite_metal_decode_runtime_memory_add_buffer(snapshot, runtime->linear_weight_buffers[slot], &snapshot->dense_linear_bytes);
        termite_metal_decode_runtime_memory_add_buffer(snapshot, runtime->linear_bias_buffers[slot], &snapshot->dense_linear_bytes);
        if (runtime->linear_weight_buffers[slot] != nil) {
            snapshot->dense_linear_buffer_count += 1;
            const uint64_t bytes = (uint64_t)runtime->linear_weight_buffers[slot].length;
            snapshot->dense_linear_weight_bytes += bytes;
            if (runtime->linear_weight_dtypes[slot] == TERMITE_METAL_DENSE_LINEAR_DTYPE_BF16) {
                snapshot->dense_linear_bf16_weight_bytes += bytes;
                snapshot->dense_linear_bf16_slots += 1;
            } else {
                snapshot->dense_linear_f32_weight_bytes += bytes;
                snapshot->dense_linear_f32_slots += 1;
            }
            if (bytes > snapshot->dense_linear_largest_bytes) {
                snapshot->dense_linear_largest_slot = (uint64_t)slot;
                snapshot->dense_linear_largest_bytes = bytes;
                snapshot->dense_linear_largest_in_dim = (uint64_t)runtime->linear_in_dims[slot];
                snapshot->dense_linear_largest_out_dim = (uint64_t)runtime->linear_out_dims[slot];
            }
        }
        if (runtime->linear_bias_buffers[slot] != nil) {
            snapshot->dense_linear_buffer_count += 1;
        }
        termite_metal_decode_runtime_memory_add_buffer(snapshot, runtime->dense_qkv_packed_weight_buffers[slot], &snapshot->dense_linear_bytes);
        if (runtime->dense_qkv_packed_weight_buffers[slot] != nil) {
            snapshot->dense_linear_buffer_count += 1;
        }
        termite_metal_decode_runtime_memory_add_buffer(snapshot, runtime->q8_qkv_packed_weight_buffers[slot], &snapshot->quant_linear_bytes);
        termite_metal_decode_runtime_memory_add_buffer(snapshot, runtime->dense_pair_packed_weight_buffers[slot], &snapshot->dense_linear_bytes);
        if (runtime->dense_pair_packed_weight_buffers[slot] != nil) {
            snapshot->dense_linear_buffer_count += 1;
        }
        termite_metal_decode_runtime_memory_add_buffer(snapshot, runtime->quant_linear_weight_buffers[slot], &snapshot->quant_linear_bytes);
    }

    if (!termite_metal_decode_runtime_graph_alias(runtime, runtime->scratch_buffer)) {
        termite_metal_decode_runtime_memory_add_buffer(snapshot, runtime->scratch_buffer, &snapshot->scratch_bytes);
    }
    termite_metal_decode_runtime_memory_add_buffer(snapshot, runtime->host_staging_buffer, &snapshot->scratch_bytes);
    termite_metal_decode_runtime_memory_add_buffer(snapshot, runtime->active_frame_host_staging_buffer, &snapshot->scratch_bytes);
    termite_metal_decode_runtime_memory_add_buffer(snapshot, runtime->token_buffer, &snapshot->scratch_bytes);
    if (!termite_metal_decode_runtime_graph_alias(runtime, runtime->sample_logits_buffer)) {
        termite_metal_decode_runtime_memory_add_buffer(snapshot, runtime->sample_logits_buffer, &snapshot->scratch_bytes);
    }
    if (!termite_metal_decode_runtime_graph_alias(runtime, runtime->sample_topk_values_buffer)) {
        termite_metal_decode_runtime_memory_add_buffer(snapshot, runtime->sample_topk_values_buffer, &snapshot->scratch_bytes);
    }
    if (!termite_metal_decode_runtime_graph_alias(runtime, runtime->sample_topk_ids_buffer)) {
        termite_metal_decode_runtime_memory_add_buffer(snapshot, runtime->sample_topk_ids_buffer, &snapshot->scratch_bytes);
    }
    for (size_t i = 0; i < 4; ++i) {
        if (!termite_metal_decode_runtime_graph_alias(runtime, runtime->hot_hidden_buffers[i])) {
            termite_metal_decode_runtime_memory_add_buffer(snapshot, runtime->hot_hidden_buffers[i], &snapshot->scratch_bytes);
        }
    }
    for (size_t i = 0; i < 5; ++i) {
        if (!termite_metal_decode_runtime_graph_alias(runtime, runtime->hot_intermediate_buffers[i])) {
            termite_metal_decode_runtime_memory_add_buffer(snapshot, runtime->hot_intermediate_buffers[i], &snapshot->scratch_bytes);
        }
    }
    for (size_t i = 0; i < TERMITE_METAL_DIRECT_BLOCK_HIDDEN_BUFFER_COUNT; ++i) {
        if (!termite_metal_decode_runtime_graph_alias(runtime, runtime->direct_block_hidden_buffers[i])) {
            termite_metal_decode_runtime_memory_add_buffer(snapshot, runtime->direct_block_hidden_buffers[i], &snapshot->scratch_bytes);
        }
    }
    for (size_t i = 0; i < TERMITE_METAL_DIRECT_BLOCK_SHARED_BUFFER_COUNT; ++i) {
        termite_metal_decode_runtime_memory_add_buffer(snapshot, runtime->direct_block_shared_buffers[i], &snapshot->scratch_bytes);
    }
    if (!termite_metal_decode_runtime_graph_alias(runtime, runtime->direct_block_q_buffer)) {
        termite_metal_decode_runtime_memory_add_buffer(snapshot, runtime->direct_block_q_buffer, &snapshot->scratch_bytes);
    }
    for (size_t i = 0; i < 6; ++i) {
        if (!termite_metal_decode_runtime_graph_alias(runtime, runtime->active_layer_projection_buffers[i])) {
            termite_metal_decode_runtime_memory_add_buffer(snapshot, runtime->active_layer_projection_buffers[i], &snapshot->scratch_bytes);
        }
    }
    termite_metal_decode_runtime_memory_add_buffer(snapshot, runtime->i2s_qinput_buffer, &snapshot->scratch_bytes);
    termite_metal_decode_runtime_memory_add_buffer(snapshot, runtime->i2s_scale_buffer, &snapshot->scratch_bytes);
    for (size_t slot = 0; slot < TERMITE_METAL_GRAPH_PLAN_SLOT_CAPACITY; ++slot) {
        termite_metal_decode_runtime_memory_add_buffer(snapshot, runtime->graph_plan_buffers[slot], &snapshot->scratch_bytes);
        if (runtime->graph_plan_buffers[slot] != nil) {
            snapshot->graph_plan_slots += 1;
            snapshot->graph_plan_bytes += (uint64_t)runtime->graph_plan_buffers[slot].length;
        }
    }
    for (size_t slot = 0; slot < TERMITE_METAL_SCRATCH_POOL_CAPACITY; ++slot) {
        termite_metal_decode_runtime_memory_add_buffer(snapshot, runtime->scratch_pool_buffers[slot], &snapshot->scratch_bytes);
        if (runtime->scratch_pool_buffers[slot] != nil) {
            snapshot->scratch_pool_slots += 1;
            snapshot->scratch_pool_bytes += (uint64_t)runtime->scratch_pool_buffers[slot].length;
        }
        if (runtime->scratch_pool_in_use[slot] != 0) snapshot->scratch_pool_in_use_slots += 1;
        if (runtime->scratch_pool_pending_frame_release[slot] != 0) snapshot->scratch_pool_pending_slots += 1;
    }

    if (!termite_metal_decode_runtime_graph_alias(runtime, runtime->attention_span_encoded_key_buffer)) {
        termite_metal_decode_runtime_memory_add_buffer(snapshot, runtime->attention_span_encoded_key_buffer, &snapshot->attention_span_bytes);
    }
    if (!termite_metal_decode_runtime_graph_alias(runtime, runtime->attention_span_v_buffer)) {
        termite_metal_decode_runtime_memory_add_buffer(snapshot, runtime->attention_span_v_buffer, &snapshot->attention_span_bytes);
    }
    for (size_t slot = 0; slot < TERMITE_METAL_ATTENTION_SPAN_SLOT_CAPACITY; ++slot) {
        if (!termite_metal_decode_runtime_graph_alias(runtime, runtime->attention_span_encoded_key_buffers[slot])) {
            termite_metal_decode_runtime_memory_add_buffer(snapshot, runtime->attention_span_encoded_key_buffers[slot], &snapshot->attention_span_bytes);
        }
        if (!termite_metal_decode_runtime_graph_alias(runtime, runtime->attention_span_v_buffers[slot])) {
            termite_metal_decode_runtime_memory_add_buffer(snapshot, runtime->attention_span_v_buffers[slot], &snapshot->attention_span_bytes);
        }
    }

    termite_metal_decode_runtime_memory_add_buffer(snapshot, runtime->hidden_state_front, &snapshot->hidden_state_bytes);
    termite_metal_decode_runtime_memory_add_buffer(snapshot, runtime->hidden_state_back, &snapshot->hidden_state_bytes);
    termite_metal_decode_runtime_memory_add_resources(snapshot, runtime->active_frame_retained_resources, &snapshot->frame_retained_bytes);
    termite_metal_decode_runtime_memory_add_resources(snapshot, runtime->submitted_frame_retained_resources, &snapshot->frame_retained_bytes);
    snapshot->graph_plan_active = runtime->graph_plan_active;
    snapshot->graph_plan_count = runtime->graph_plan_count;
    snapshot->graph_plan_allocations = runtime->graph_plan_allocations;
    snapshot->graph_plan_reuses = runtime->graph_plan_reuses;
    snapshot->mps_dense_linear_standalone_calls = runtime->mps_dense_linear_standalone_calls;
    snapshot->mps_dense_linear_active_frame_calls = runtime->mps_dense_linear_active_frame_calls;
    snapshot->mps_dense_linear_standalone_wait_nanos = runtime->mps_dense_linear_standalone_wait_nanos;
    snapshot->mps_dense_linear_standalone_gpu_nanos = runtime->mps_dense_linear_standalone_gpu_nanos;
    snapshot->last_frame_mps_dense_linear_count = runtime->last_frame_mps_dense_linear_count;
    snapshot->dense_qkv_packed_calls = runtime->dense_qkv_packed_calls;
    snapshot->dense_qkv_packed_fallbacks = runtime->dense_qkv_packed_fallbacks;
    snapshot->dense_pair_packed_calls = runtime->dense_pair_packed_calls;
    snapshot->dense_pair_packed_fallbacks = runtime->dense_pair_packed_fallbacks;
    snapshot->deberta_ffn_fused_calls = runtime->deberta_ffn_fused_calls;
    snapshot->deberta_ffn_fused_mps_matmuls = runtime->deberta_ffn_fused_mps_matmuls;
    snapshot->deberta_ffn_fused_fallbacks = runtime->deberta_ffn_fused_fallbacks;
    snapshot->deberta_attention_flash_calls = runtime->deberta_attention_flash_calls;
    snapshot->deberta_attention_legacy_calls = runtime->deberta_attention_legacy_calls;
    snapshot->deberta_attention_gemm_calls = runtime->deberta_attention_gemm_calls;
    snapshot->deberta_attention_gemm_fallbacks = runtime->deberta_attention_gemm_fallbacks;
    snapshot->mpsgraph_ffn_calls = runtime->mpsgraph_ffn_calls;
    snapshot->mpsgraph_ffn_fallbacks = runtime->mpsgraph_ffn_fallbacks;
    snapshot->mpsgraph_ffn_compiles = runtime->mpsgraph_ffn_compiles;
    snapshot->mpsgraph_ffn_cache_hits = runtime->mpsgraph_ffn_cache_hits;
    snapshot->compute_encoder_count = runtime->compute_encoder_count;
    snapshot->blit_encoder_count = runtime->blit_encoder_count;
    snapshot->last_frame_compute_encoder_count = runtime->last_frame_compute_encoder_count;
    snapshot->last_frame_blit_encoder_count = runtime->last_frame_blit_encoder_count;
    snapshot->last_frame_planned_compute_scope_count = runtime->last_frame_planned_compute_scope_count;
    snapshot->last_frame_planned_barrier_count = runtime->last_frame_planned_barrier_count;
    snapshot->last_frame_compute_quant_linear_count = runtime->last_frame_compute_source_counts[TERMITE_METAL_COMPUTE_SOURCE_QUANT_LINEAR];
    snapshot->last_frame_compute_quant_qkv_count = runtime->last_frame_compute_source_counts[TERMITE_METAL_COMPUTE_SOURCE_QUANT_QKV];
    snapshot->last_frame_compute_quant_pair_act_count = runtime->last_frame_compute_source_counts[TERMITE_METAL_COMPUTE_SOURCE_QUANT_PAIR_ACT];
    snapshot->last_frame_compute_attention_count = runtime->last_frame_compute_source_counts[TERMITE_METAL_COMPUTE_SOURCE_ATTENTION];
    snapshot->last_frame_compute_rms_norm_count = runtime->last_frame_compute_source_counts[TERMITE_METAL_COMPUTE_SOURCE_RMS_NORM];
    snapshot->last_frame_compute_head_rope_count = runtime->last_frame_compute_source_counts[TERMITE_METAL_COMPUTE_SOURCE_HEAD_ROPE];
    snapshot->last_frame_compute_ffn_count = runtime->last_frame_compute_source_counts[TERMITE_METAL_COMPUTE_SOURCE_FFN];
    snapshot->last_frame_compute_ple_count = runtime->last_frame_compute_source_counts[TERMITE_METAL_COMPUTE_SOURCE_PLE];
    snapshot->last_frame_compute_tail_count = runtime->last_frame_compute_source_counts[TERMITE_METAL_COMPUTE_SOURCE_TAIL];
    snapshot->last_frame_compute_embedding_count = runtime->last_frame_compute_source_counts[TERMITE_METAL_COMPUTE_SOURCE_EMBEDDING];
    snapshot->last_frame_compute_dense_linear_count = runtime->last_frame_compute_source_counts[TERMITE_METAL_COMPUTE_SOURCE_DENSE_LINEAR];
    snapshot->last_frame_compute_layer_count = runtime->last_frame_compute_source_counts[TERMITE_METAL_COMPUTE_SOURCE_LAYER];
    snapshot->last_frame_compute_other_count = runtime->last_frame_compute_source_counts[TERMITE_METAL_COMPUTE_SOURCE_OTHER];
    snapshot->last_frame_compute_region_attention_count = runtime->last_frame_compute_region_counts[TERMITE_METAL_COMPUTE_REGION_ATTENTION];
    snapshot->last_frame_compute_region_attention_project_count = runtime->last_frame_compute_region_counts[TERMITE_METAL_COMPUTE_REGION_ATTENTION_PROJECT];
    snapshot->last_frame_compute_region_ffn_norm_count = runtime->last_frame_compute_region_counts[TERMITE_METAL_COMPUTE_REGION_FFN_NORM];
    snapshot->last_frame_compute_region_ffn_count = runtime->last_frame_compute_region_counts[TERMITE_METAL_COMPUTE_REGION_FFN];
    snapshot->last_frame_compute_region_ple_count = runtime->last_frame_compute_region_counts[TERMITE_METAL_COMPUTE_REGION_PLE];
    snapshot->last_frame_compute_region_tail_count = runtime->last_frame_compute_region_counts[TERMITE_METAL_COMPUTE_REGION_TAIL];
    snapshot->last_frame_compute_region_embedding_count = runtime->last_frame_compute_region_counts[TERMITE_METAL_COMPUTE_REGION_EMBEDDING];
    snapshot->last_frame_compute_region_layer_count = runtime->last_frame_compute_region_counts[TERMITE_METAL_COMPUTE_REGION_LAYER];
    snapshot->last_frame_compute_region_other_count = runtime->last_frame_compute_region_counts[TERMITE_METAL_COMPUTE_REGION_OTHER];
    snapshot->last_frame_planned_command_op_count = runtime->last_frame_planned_command_op_count;
    memcpy(
        snapshot->last_frame_planned_command_op_kind_counts,
        runtime->last_frame_planned_command_op_kind_counts,
        sizeof(snapshot->last_frame_planned_command_op_kind_counts));
    memcpy(
        snapshot->last_frame_planned_command_operator_counts,
        runtime->last_frame_planned_command_operator_counts,
        sizeof(snapshot->last_frame_planned_command_operator_counts));
    memcpy(
        snapshot->last_frame_planned_command_quant_dispatch_counts,
        runtime->last_frame_planned_command_quant_dispatch_counts,
        sizeof(snapshot->last_frame_planned_command_quant_dispatch_counts));
    snapshot->last_frame_blit_buffer_upload_count = runtime->last_frame_blit_source_counts[TERMITE_METAL_BLIT_SOURCE_BUFFER_UPLOAD];
    snapshot->last_frame_blit_buffer_copy_count = runtime->last_frame_blit_source_counts[TERMITE_METAL_BLIT_SOURCE_BUFFER_COPY];
    snapshot->last_frame_blit_buffer_slice_count = runtime->last_frame_blit_source_counts[TERMITE_METAL_BLIT_SOURCE_BUFFER_SLICE];
    snapshot->last_frame_blit_attention_span_count = runtime->last_frame_blit_source_counts[TERMITE_METAL_BLIT_SOURCE_ATTENTION_SPAN];
    snapshot->last_frame_blit_ffn_copy_count = runtime->last_frame_blit_source_counts[TERMITE_METAL_BLIT_SOURCE_FFN_COPY];
    snapshot->last_frame_blit_embedding_count = runtime->last_frame_blit_source_counts[TERMITE_METAL_BLIT_SOURCE_EMBEDDING];
    snapshot->last_frame_blit_other_count = runtime->last_frame_blit_source_counts[TERMITE_METAL_BLIT_SOURCE_OTHER];
    snapshot->q8_0_linear_dispatch_scalar = runtime->q8_0_linear_dispatch_scalar;
    snapshot->q8_0_linear_dispatch_mmv = runtime->q8_0_linear_dispatch_mmv;
    snapshot->q8_0_linear_dispatch_small_batch = runtime->q8_0_linear_dispatch_small_batch;
    snapshot->q8_0_linear_dispatch_mm = runtime->q8_0_linear_dispatch_mm;
    snapshot->q8_0_linear_rows_1 = runtime->q8_0_linear_rows_1;
    snapshot->q8_0_linear_rows_2_8 = runtime->q8_0_linear_rows_2_8;
    snapshot->q8_0_linear_rows_9_64 = runtime->q8_0_linear_rows_9_64;
    snapshot->q8_0_linear_rows_65_plus = runtime->q8_0_linear_rows_65_plus;
    snapshot->q8_0_pair_activation_mm_f16_output = runtime->q8_0_pair_activation_mm_f16_output;
    snapshot->q8_0_linear_mm_f16_input = runtime->q8_0_linear_mm_f16_input;
    snapshot->q8_0_pair_activation_rms_scale_mmv_f16_output = runtime->q8_0_pair_activation_rms_scale_mmv_f16_output;
    snapshot->q8_0_linear_mmv_f16_input = runtime->q8_0_linear_mmv_f16_input;
    memcpy(
        snapshot->q8_0_linear_family_dispatch_counts,
        runtime->q8_0_linear_family_dispatch_counts,
        sizeof(snapshot->q8_0_linear_family_dispatch_counts));
    return 0;
}

int termite_metal_decode_runtime_reserve(termite_metal_decode_runtime *runtime, size_t scratch_bytes, size_t token_bytes) {
    if (runtime == NULL) return -1;
    if (termite_metal_decode_runtime_ready(runtime) == 0) return -2;
    @autoreleasepool {
        if (scratch_bytes > runtime->scratch_capacity) {
            id<MTLBuffer> scratch = [runtime->device newBufferWithLength:scratch_bytes options:MTLResourceStorageModePrivate];
            if (scratch == nil) return -3;
            runtime->scratch_buffer = scratch;
            runtime->scratch_capacity = scratch_bytes;
        }
        if (token_bytes > runtime->token_capacity) {
            id<MTLBuffer> token = [runtime->device newBufferWithLength:token_bytes options:MTLResourceStorageModeShared];
            if (token == nil) return -4;
            runtime->token_buffer = token;
            runtime->token_capacity = token_bytes;
        }
        return 0;
    }
}

void *termite_metal_buffer_alloc(termite_metal_decode_runtime *runtime, size_t length, int storage_mode) {
    if (runtime == NULL || runtime->device == nil || length == 0) return NULL;
    @autoreleasepool {
        MTLResourceOptions options = (storage_mode == 1)
            ? MTLResourceStorageModePrivate
            : MTLResourceStorageModeShared;
        id<MTLBuffer> buffer = [runtime->device newBufferWithLength:length options:options];
        if (buffer == nil) return NULL;
        return (__bridge_retained void *)buffer;
    }
}

void termite_metal_buffer_release(void *handle) {
    if (handle == NULL) return;
    id<MTLBuffer> buffer = (__bridge_transfer id<MTLBuffer>)handle;
    (void)buffer;
}

void termite_metal_decode_runtime_release_buffer(termite_metal_decode_runtime *runtime, void *handle) {
    if (handle == NULL) return;
    @autoreleasepool {
        id<MTLBuffer> buffer = (__bridge_transfer id<MTLBuffer>)handle;
        if (runtime != NULL && (runtime->active_frame_cb != nil || runtime->submitted_frame_cb != nil)) {
            (void)termite_metal_decode_runtime_retain_frame_resource(runtime, buffer);
        }
    }
}

void *termite_metal_buffer_contents(void *handle) {
    if (handle == NULL) return NULL;
    id<MTLBuffer> buffer = (__bridge id<MTLBuffer>)handle;
    if (buffer.storageMode != MTLStorageModeShared) return NULL;
    return buffer.contents;
}

int termite_metal_buffer_upload(
    termite_metal_decode_runtime *runtime,
    void *handle,
    size_t offset,
    const void *src,
    size_t length
) {
    if (handle == NULL || src == NULL || length == 0) return -1;
    @autoreleasepool {
        id<MTLBuffer> dst = (__bridge id<MTLBuffer>)handle;
        if (offset + length > dst.length) return -2;
        if (dst.storageMode == MTLStorageModeShared) {
            memcpy((uint8_t *)dst.contents + offset, src, length);
            return 0;
        }
        if (runtime == NULL || runtime->device == nil || runtime->queue == nil) return -3;
        id<MTLBuffer> staging = [runtime->device newBufferWithBytes:src length:length options:MTLResourceStorageModeShared];
        if (staging == nil) return -4;
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -5;
        if (!frame_owned && termite_metal_decode_runtime_retain_frame_resource(runtime, staging) != 0) return -4;
        termite_metal_record_active_frame_blit_source(command_buffer, TERMITE_METAL_BLIT_SOURCE_BUFFER_UPLOAD);
        id<MTLBlitCommandEncoder> blit = termite_metal_tracked_blit_command_encoder(command_buffer);
        if (blit == nil) return -6;
        if ([blit respondsToSelector:@selector(setLabel:)]) {
            blit.label = @"buffer-upload";
        }
        [blit copyFromBuffer:staging sourceOffset:0 toBuffer:dst destinationOffset:offset size:length];
        [blit endEncoding];
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -7);
    }
}

int termite_metal_buffer_download(
    termite_metal_decode_runtime *runtime,
    void *handle,
    size_t offset,
    void *dst,
    size_t length
) {
    if (handle == NULL || dst == NULL || length == 0) return -1;
    @autoreleasepool {
        id<MTLBuffer> src = (__bridge id<MTLBuffer>)handle;
        if (offset + length > src.length) return -2;
        if (src.storageMode == MTLStorageModeShared) {
            if (termite_metal_trace_frame_lifecycle_enabled()) {
                fprintf(stderr, "metal_frame_lifecycle: buffer_download storage=shared length=%zu offset=%zu\n", length, offset);
            }
            if (runtime != NULL && termite_metal_decode_runtime_flush_active_frame(runtime) != 0) return -8;
            memcpy(dst, (const uint8_t *)src.contents + offset, length);
            return 0;
        }
        if (runtime == NULL || runtime->device == nil || runtime->queue == nil) return -3;
        if (termite_metal_trace_frame_lifecycle_enabled()) {
            fprintf(stderr, "metal_frame_lifecycle: buffer_download storage=private length=%zu offset=%zu\n", length, offset);
        }
        if (termite_metal_decode_runtime_flush_active_frame(runtime) != 0) return -8;
        id<MTLBuffer> staging = [runtime->device newBufferWithLength:length options:MTLResourceStorageModeShared];
        if (staging == nil) return -4;
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(runtime->queue, __func__);
        if (command_buffer == nil) return -5;
        id<MTLBlitCommandEncoder> blit = termite_metal_tracked_blit_command_encoder(command_buffer);
        if (blit == nil) return -6;
        if ([blit respondsToSelector:@selector(setLabel:)]) {
            blit.label = @"buffer-download";
        }
        [blit copyFromBuffer:src sourceOffset:offset toBuffer:staging destinationOffset:0 size:length];
        [blit endEncoding];
        if (termite_metal_commit_and_wait(command_buffer, __func__, -7) != 0) return -7;
        memcpy(dst, staging.contents, length);
        return 0;
    }
}

int termite_metal_buffer_copy(
    termite_metal_decode_runtime *runtime,
    void *src_handle,
    size_t src_offset,
    void *dst_handle,
    size_t dst_offset,
    size_t length
) {
    if (src_handle == NULL || dst_handle == NULL || length == 0) return -1;
    @autoreleasepool {
        id<MTLBuffer> src = (__bridge id<MTLBuffer>)src_handle;
        id<MTLBuffer> dst = (__bridge id<MTLBuffer>)dst_handle;
        if (src_offset + length > src.length || dst_offset + length > dst.length) return -2;
        if (src.storageMode == MTLStorageModeShared && dst.storageMode == MTLStorageModeShared) {
            memcpy((uint8_t *)dst.contents + dst_offset, (const uint8_t *)src.contents + src_offset, length);
            return 0;
        }
        if (runtime == NULL || runtime->device == nil || runtime->queue == nil) return -3;
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -4;
        termite_metal_record_active_frame_blit_source(command_buffer, TERMITE_METAL_BLIT_SOURCE_BUFFER_COPY);
        if (!frame_owned && termite_metal_trace_frame_blits_enabled()) {
            fprintf(stderr, "metal_frame_blit: kind=copy length=%zu src_offset=%zu dst_offset=%zu\n", length, src_offset, dst_offset);
        }
        id<MTLBlitCommandEncoder> blit = termite_metal_tracked_blit_command_encoder(command_buffer);
        if (blit == nil) return -5;
        if ([blit respondsToSelector:@selector(setLabel:)]) {
            blit.label = @"buffer-copy";
        }
        [blit copyFromBuffer:src sourceOffset:src_offset toBuffer:dst destinationOffset:dst_offset size:length];
        [blit endEncoding];
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -6);
    }
}

int termite_metal_buffer_copy_pair(
    termite_metal_decode_runtime *runtime,
    void *src_a_handle,
    size_t src_a_offset,
    void *dst_a_handle,
    size_t dst_a_offset,
    size_t length_a,
    void *src_b_handle,
    size_t src_b_offset,
    void *dst_b_handle,
    size_t dst_b_offset,
    size_t length_b
) {
    if (src_a_handle == NULL || dst_a_handle == NULL || src_b_handle == NULL || dst_b_handle == NULL) return -1;
    if (length_a == 0 || length_b == 0) return -2;
    @autoreleasepool {
        id<MTLBuffer> src_a = (__bridge id<MTLBuffer>)src_a_handle;
        id<MTLBuffer> dst_a = (__bridge id<MTLBuffer>)dst_a_handle;
        id<MTLBuffer> src_b = (__bridge id<MTLBuffer>)src_b_handle;
        id<MTLBuffer> dst_b = (__bridge id<MTLBuffer>)dst_b_handle;
        if (src_a_offset + length_a > src_a.length || dst_a_offset + length_a > dst_a.length) return -3;
        if (src_b_offset + length_b > src_b.length || dst_b_offset + length_b > dst_b.length) return -4;
        if (src_a.storageMode == MTLStorageModeShared &&
            dst_a.storageMode == MTLStorageModeShared &&
            src_b.storageMode == MTLStorageModeShared &&
            dst_b.storageMode == MTLStorageModeShared) {
            memcpy((uint8_t *)dst_a.contents + dst_a_offset, (const uint8_t *)src_a.contents + src_a_offset, length_a);
            memcpy((uint8_t *)dst_b.contents + dst_b_offset, (const uint8_t *)src_b.contents + src_b_offset, length_b);
            return 0;
        }
        if (runtime == NULL || runtime->device == nil || runtime->queue == nil) return -5;
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -6;
        termite_metal_record_active_frame_blit_source(command_buffer, TERMITE_METAL_BLIT_SOURCE_BUFFER_COPY);
        if (!frame_owned && termite_metal_trace_frame_blits_enabled()) {
            fprintf(stderr, "metal_frame_blit: kind=copy_pair length_a=%zu length_b=%zu src_a_offset=%zu dst_a_offset=%zu src_b_offset=%zu dst_b_offset=%zu\n", length_a, length_b, src_a_offset, dst_a_offset, src_b_offset, dst_b_offset);
        }
        id<MTLBlitCommandEncoder> blit = termite_metal_tracked_blit_command_encoder(command_buffer);
        if (blit == nil) return -7;
        if ([blit respondsToSelector:@selector(setLabel:)]) {
            blit.label = @"buffer-copy-pair";
        }
        [blit copyFromBuffer:src_a sourceOffset:src_a_offset toBuffer:dst_a destinationOffset:dst_a_offset size:length_a];
        [blit copyFromBuffer:src_b sourceOffset:src_b_offset toBuffer:dst_b destinationOffset:dst_b_offset size:length_b];
        [blit endEncoding];
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -8);
    }
}

int termite_metal_buffer_slice_last_dim_2d(
    termite_metal_decode_runtime *runtime,
    void *src_handle,
    size_t src_offset,
    size_t rows,
    size_t cols,
    size_t start,
    size_t out_cols,
    void *dst_handle,
    size_t dst_offset
) {
    if (runtime == NULL || runtime->queue == nil || src_handle == NULL || dst_handle == NULL) return -1;
    if (rows == 0 || cols == 0 || out_cols == 0 || start > cols || out_cols > cols - start) return -2;
    if (rows > UINT32_MAX || cols > UINT32_MAX || start > UINT32_MAX || out_cols > UINT32_MAX) return -2;
    @autoreleasepool {
        id<MTLBuffer> src = (__bridge id<MTLBuffer>)src_handle;
        id<MTLBuffer> dst = (__bridge id<MTLBuffer>)dst_handle;
        const size_t elem_size = sizeof(float);
        const size_t row_bytes = out_cols * elem_size;
        const size_t src_required = src_offset + ((rows - 1) * cols + start + out_cols) * elem_size;
        const size_t dst_required = dst_offset + rows * out_cols * elem_size;
        if (src_required > src.length || dst_required > dst.length) return -3;
        if (runtime->slice_last_dim_f32_2d_pipeline != nil) {
            termite_metal_slice_last_dim_f32_2d_params params = {
                .rows = (uint32_t)rows,
                .cols = (uint32_t)cols,
                .start = (uint32_t)start,
                .out_cols = (uint32_t)out_cols,
            };
            const size_t total = rows * out_cols;
            id<MTLComputeCommandEncoder> planned_encoder = runtime->active_planned_compute_encoder;
            if (planned_encoder != nil) {
                if (src_required < src_offset || dst_required < dst_offset) return -3;
                if (termite_metal_decode_runtime_prepare_planned_compute_unary_accesses(
                        runtime,
                        src,
                        src_offset,
                        src_required - src_offset,
                        dst,
                        dst_offset,
                        dst_required - dst_offset,
                        -3) != 0)
                {
                    return -3;
                }
                [planned_encoder setComputePipelineState:runtime->slice_last_dim_f32_2d_pipeline];
                [planned_encoder setBuffer:src offset:src_offset atIndex:0];
                [planned_encoder setBuffer:dst offset:dst_offset atIndex:1];
                [planned_encoder setBytes:&params length:sizeof(params) atIndex:2];
                [planned_encoder dispatchThreads:MTLSizeMake(total, 1, 1)
                           threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->slice_last_dim_f32_2d_pipeline, total), 1, 1)];
                return 0;
            }
            bool frame_owned = true;
            id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
            if (command_buffer == nil) return -4;
            id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder_for(command_buffer, TERMITE_METAL_COMPUTE_SOURCE_OTHER);
            if (encoder == nil) return -5;
            [encoder setComputePipelineState:runtime->slice_last_dim_f32_2d_pipeline];
            [encoder setBuffer:src offset:src_offset atIndex:0];
            [encoder setBuffer:dst offset:dst_offset atIndex:1];
            [encoder setBytes:&params length:sizeof(params) atIndex:2];
            [encoder dispatchThreads:MTLSizeMake(total, 1, 1)
               threadsPerThreadgroup:MTLSizeMake(termite_metal_thread_width(runtime->slice_last_dim_f32_2d_pipeline, total), 1, 1)];
            [encoder endEncoding];
            return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -6);
        }
        bool frame_owned = true;
        id<MTLCommandBuffer> command_buffer = termite_metal_decode_runtime_command_buffer(runtime, __func__, &frame_owned);
        if (command_buffer == nil) return -4;
        termite_metal_record_active_frame_blit_source(command_buffer, TERMITE_METAL_BLIT_SOURCE_BUFFER_SLICE);
        if (!frame_owned && termite_metal_trace_frame_blits_enabled()) {
            fprintf(stderr, "metal_frame_blit: kind=slice_last_dim rows=%zu cols=%zu start=%zu out_cols=%zu row_bytes=%zu\n", rows, cols, start, out_cols, row_bytes);
        }
        id<MTLBlitCommandEncoder> blit = termite_metal_tracked_blit_command_encoder(command_buffer);
        if (blit == nil) return -5;
        if ([blit respondsToSelector:@selector(setLabel:)]) {
            blit.label = @"buffer-slice-last-dim-2d";
        }
        for (size_t row = 0; row < rows; ++row) {
            const size_t src_row_offset = src_offset + (row * cols + start) * elem_size;
            const size_t dst_row_offset = dst_offset + row * out_cols * elem_size;
            [blit copyFromBuffer:src sourceOffset:src_row_offset toBuffer:dst destinationOffset:dst_row_offset size:row_bytes];
        }
        [blit endEncoding];
        return termite_metal_decode_runtime_finish_command_buffer(command_buffer, frame_owned, -6);
    }
}

int termite_metal_decode_runtime_reserve_hidden_state(
    termite_metal_decode_runtime *runtime,
    size_t max_prefill_rows,
    size_t hidden_size
) {
    if (runtime == NULL || runtime->device == nil) return -1;
    if (max_prefill_rows == 0 || hidden_size == 0) return -2;
    const size_t required_bytes = max_prefill_rows * hidden_size * sizeof(float);
    @autoreleasepool {
        if (required_bytes > runtime->hidden_state_capacity) {
            id<MTLBuffer> front = [runtime->device newBufferWithLength:required_bytes options:MTLResourceStorageModePrivate];
            if (front == nil) return -3;
            id<MTLBuffer> back = [runtime->device newBufferWithLength:required_bytes options:MTLResourceStorageModePrivate];
            if (back == nil) return -4;
            runtime->hidden_state_front = front;
            runtime->hidden_state_back = back;
            runtime->hidden_state_capacity = required_bytes;
        }
        runtime->hidden_state_max_rows = max_prefill_rows;
        runtime->hidden_state_hidden_size = hidden_size;
        return 0;
    }
}

void *termite_metal_decode_runtime_hidden_state_buffer(
    termite_metal_decode_runtime *runtime,
    int which
) {
    if (runtime == NULL) return NULL;
    id<MTLBuffer> buffer = (which == 0) ? runtime->hidden_state_front : runtime->hidden_state_back;
    if (buffer == nil) return NULL;
    return (__bridge_retained void *)buffer;
}

size_t termite_metal_decode_runtime_hidden_state_capacity(
    termite_metal_decode_runtime *runtime
) {
    if (runtime == NULL) return 0;
    return runtime->hidden_state_capacity;
}

size_t termite_metal_decode_runtime_hidden_state_max_rows(
    termite_metal_decode_runtime *runtime
) {
    if (runtime == NULL) return 0;
    return runtime->hidden_state_max_rows;
}

void *termite_metal_decode_runtime_acquire_scratch(
    termite_metal_decode_runtime *runtime,
    size_t bytes
) {
    if (runtime == NULL || runtime->device == nil || bytes == 0) return NULL;
    @autoreleasepool {
        // Prefer an idle slot that already has enough capacity — no allocation.
        for (size_t slot = 0; slot < TERMITE_METAL_SCRATCH_POOL_CAPACITY; ++slot) {
            if (runtime->scratch_pool_in_use[slot] == 0 &&
                runtime->scratch_pool_pending_frame_release[slot] == 0 &&
                runtime->scratch_pool_buffers[slot] != nil &&
                runtime->scratch_pool_capacities[slot] >= bytes) {
                runtime->scratch_pool_in_use[slot] = 1;
                return (__bridge_retained void *)runtime->scratch_pool_buffers[slot];
            }
        }
        // Fallback: find the first idle slot and (re)allocate it to fit.
        for (size_t slot = 0; slot < TERMITE_METAL_SCRATCH_POOL_CAPACITY; ++slot) {
            if (runtime->scratch_pool_in_use[slot] == 0 && runtime->scratch_pool_pending_frame_release[slot] == 0) {
                id<MTLBuffer> buffer = [runtime->device newBufferWithLength:bytes options:MTLResourceStorageModePrivate];
                if (buffer == nil) return NULL;
                runtime->scratch_pool_buffers[slot] = buffer;
                runtime->scratch_pool_capacities[slot] = bytes;
                runtime->scratch_pool_in_use[slot] = 1;
                return (__bridge_retained void *)buffer;
            }
        }
        // Pool exhausted — caller can fall back to `termite_metal_buffer_alloc`.
        return NULL;
    }
}

void termite_metal_decode_runtime_release_scratch(
    termite_metal_decode_runtime *runtime,
    void *handle
) {
    if (handle == NULL) return;
    // Consume the caller's +1 retain from acquire; the slot's own strong
    // reference in `scratch_pool_buffers[slot]` keeps the buffer alive across
    // frames so the next acquire can reuse the allocation.
    id<MTLBuffer> buffer = (__bridge_transfer id<MTLBuffer>)handle;
    if (runtime == NULL) {
        (void)buffer;
        return;
    }
    for (size_t slot = 0; slot < TERMITE_METAL_SCRATCH_POOL_CAPACITY; ++slot) {
        if (runtime->scratch_pool_buffers[slot] == buffer) {
            if (runtime->active_frame_cb != nil || runtime->submitted_frame_cb != nil) {
                runtime->scratch_pool_pending_frame_release[slot] = 1;
                runtime->scratch_pool_in_use[slot] = 1;
            } else {
                runtime->scratch_pool_pending_frame_release[slot] = 0;
                runtime->scratch_pool_in_use[slot] = 0;
            }
            break;
        }
    }
    (void)buffer;
}

int termite_metal_decode_runtime_prepare_decoder_only_greedy(
    termite_metal_decode_runtime *runtime,
    size_t hidden_size,
    size_t intermediate_size,
    size_t num_layers,
    size_t num_heads,
    size_t num_kv_heads,
    size_t head_dim,
    size_t vocab_size,
    size_t kv_tokens
) {
    if (runtime == NULL) return -1;
    if (hidden_size == 0 || intermediate_size == 0 || num_layers == 0 || num_heads == 0 || num_kv_heads == 0 || head_dim == 0 || vocab_size == 0) return -2;

    const size_t hidden_bytes = hidden_size * sizeof(float);
    const size_t q_bytes = num_heads * head_dim * sizeof(float);
    const size_t kv_bytes = num_kv_heads * head_dim * sizeof(float);
    const size_t attn_bytes = num_heads * head_dim * sizeof(float);
    const size_t mlp_bytes = intermediate_size * sizeof(float) * 2u;
    const size_t layer_scratch = hidden_bytes * 2u + q_bytes + kv_bytes * 2u + attn_bytes + mlp_bytes;
    const size_t kv_span_meta = kv_tokens * sizeof(uint32_t);
    const size_t scratch_bytes = layer_scratch + hidden_bytes + kv_span_meta + 4096u;

    return termite_metal_decode_runtime_reserve(runtime, scratch_bytes, sizeof(uint32_t));
}

static int termite_metal_dispatch(termite_metal_provider *provider, id<MTLComputePipelineState> pipeline, const float *input, size_t rows, size_t in_dim, size_t row_blocks, const uint8_t *weight_raw, size_t weight_bytes, size_t out_dim, float *output) {
    if (provider == NULL || pipeline == nil || input == NULL || weight_raw == NULL || output == NULL) return -1;
    @autoreleasepool {
        const size_t input_bytes = rows * in_dim * sizeof(float);
        const size_t output_bytes = rows * out_dim * sizeof(float);
        id<MTLBuffer> input_buffer = [provider->device newBufferWithBytesNoCopy:(void *)input length:input_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [provider->device newBufferWithBytes:input length:input_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> weight_buffer = [provider->device newBufferWithBytesNoCopy:(void *)weight_raw length:weight_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (weight_buffer == nil) weight_buffer = [provider->device newBufferWithBytes:weight_raw length:weight_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> output_buffer = [provider->device newBufferWithBytesNoCopy:output length:output_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) output_buffer = [provider->device newBufferWithBytes:output length:output_bytes options:MTLResourceStorageModeShared];
        if (input_buffer == nil || weight_buffer == nil || output_buffer == nil) return -3;
        termite_metal_linear_params params = { .rows = (uint32_t)rows, .in_dim = (uint32_t)in_dim, .out_dim = (uint32_t)out_dim, .row_blocks = (uint32_t)row_blocks };
        id<MTLBuffer> params_buffer = [provider->device newBufferWithBytes:&params length:sizeof(params) options:MTLResourceStorageModeShared];
        if (params_buffer == nil) return -4;
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(provider->queue, __func__);
        if (command_buffer == nil) return -5;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (encoder == nil) return -6;
        [encoder setComputePipelineState:pipeline];
        [encoder setBuffer:input_buffer offset:0 atIndex:0];
        [encoder setBuffer:weight_buffer offset:0 atIndex:1];
        [encoder setBuffer:output_buffer offset:0 atIndex:2];
        [encoder setBuffer:params_buffer offset:0 atIndex:3];
        const NSUInteger total = rows * out_dim;
        MTLSize grid_size = MTLSizeMake(total, 1, 1);
        NSUInteger thread_width = pipeline.maxTotalThreadsPerThreadgroup;
        if (thread_width == 0) thread_width = 64;
        if (thread_width > total && total > 0) thread_width = total;
        MTLSize group_size = MTLSizeMake(thread_width, 1, 1);
        [encoder dispatchThreads:grid_size threadsPerThreadgroup:group_size];
        [encoder endEncoding];
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -7;
    }
}

static int termite_metal_dispatch_pair(termite_metal_provider *provider, id<MTLComputePipelineState> pipeline, const float *input, size_t rows, size_t in_dim, size_t row_blocks, const uint8_t *weight_a_raw, size_t weight_a_bytes, const uint8_t *weight_b_raw, size_t weight_b_bytes, size_t out_dim, float *output_a, float *output_b) {
    if (provider == NULL || pipeline == nil || input == NULL || weight_a_raw == NULL || weight_b_raw == NULL || output_a == NULL || output_b == NULL) return -1;
    @autoreleasepool {
        const size_t input_bytes = rows * in_dim * sizeof(float);
        const size_t output_bytes = rows * out_dim * sizeof(float);
        id<MTLBuffer> input_buffer = [provider->device newBufferWithBytesNoCopy:(void *)input length:input_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [provider->device newBufferWithBytes:input length:input_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> weight_a_buffer = [provider->device newBufferWithBytesNoCopy:(void *)weight_a_raw length:weight_a_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (weight_a_buffer == nil) weight_a_buffer = [provider->device newBufferWithBytes:weight_a_raw length:weight_a_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> weight_b_buffer = [provider->device newBufferWithBytesNoCopy:(void *)weight_b_raw length:weight_b_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (weight_b_buffer == nil) weight_b_buffer = [provider->device newBufferWithBytes:weight_b_raw length:weight_b_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> output_a_buffer = [provider->device newBufferWithBytesNoCopy:output_a length:output_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_a_buffer == nil) output_a_buffer = [provider->device newBufferWithBytes:output_a length:output_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> output_b_buffer = [provider->device newBufferWithBytesNoCopy:output_b length:output_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_b_buffer == nil) output_b_buffer = [provider->device newBufferWithBytes:output_b length:output_bytes options:MTLResourceStorageModeShared];
        if (input_buffer == nil || weight_a_buffer == nil || weight_b_buffer == nil || output_a_buffer == nil || output_b_buffer == nil) return -3;
        termite_metal_linear_params params = {
            .rows = (uint32_t)rows,
            .in_dim = (uint32_t)in_dim,
            .out_dim = (uint32_t)out_dim,
            .row_blocks = (uint32_t)row_blocks,
        };
        id<MTLBuffer> params_buffer = [provider->device newBufferWithBytes:&params length:sizeof(params) options:MTLResourceStorageModeShared];
        if (params_buffer == nil) return -4;
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(provider->queue, __func__);
        if (command_buffer == nil) return -5;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (encoder == nil) return -6;
        [encoder setComputePipelineState:pipeline];
        [encoder setBuffer:input_buffer offset:0 atIndex:0];
        [encoder setBuffer:weight_a_buffer offset:0 atIndex:1];
        [encoder setBuffer:weight_b_buffer offset:0 atIndex:2];
        [encoder setBuffer:output_a_buffer offset:0 atIndex:3];
        [encoder setBuffer:output_b_buffer offset:0 atIndex:4];
        [encoder setBuffer:params_buffer offset:0 atIndex:5];
        MTLSize grid_size = MTLSizeMake(out_dim, rows, 1);
        NSUInteger thread_width = pipeline.maxTotalThreadsPerThreadgroup;
        if (thread_width == 0) thread_width = 32;
        if (thread_width > out_dim && out_dim > 0) thread_width = out_dim;
        MTLSize group_size = MTLSizeMake(thread_width, 1, 1);
        [encoder dispatchThreads:grid_size threadsPerThreadgroup:group_size];
        [encoder endEncoding];
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -7;
    }
}

static int termite_metal_dispatch_key_scores(termite_metal_provider *provider, id<MTLComputePipelineState> pipeline, const float *q, size_t q_len, const uint8_t *encoded_key, size_t encoded_key_bytes, size_t block_tokens, size_t num_heads, size_t num_kv_heads, size_t head_dim, size_t key_row_bytes, float *output) {
    if (provider == NULL || pipeline == nil || q == NULL || encoded_key == NULL || output == NULL) return -1;
    @autoreleasepool {
        const size_t q_bytes = q_len * num_heads * head_dim * sizeof(float);
        const size_t output_len = q_len * num_heads * block_tokens;
        const size_t output_bytes = output_len * sizeof(float);
        id<MTLBuffer> q_buffer = [provider->device newBufferWithBytesNoCopy:(void *)q length:q_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (q_buffer == nil) q_buffer = [provider->device newBufferWithBytes:q length:q_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> key_buffer = [provider->device newBufferWithBytesNoCopy:(void *)encoded_key length:encoded_key_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (key_buffer == nil) key_buffer = [provider->device newBufferWithBytes:encoded_key length:encoded_key_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> output_buffer = [provider->device newBufferWithBytesNoCopy:output length:output_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) output_buffer = [provider->device newBufferWithBytes:output length:output_bytes options:MTLResourceStorageModeShared];
        if (q_buffer == nil || key_buffer == nil || output_buffer == nil) return -3;
        termite_metal_key_scores_params params = {
            .q_len = (uint32_t)q_len,
            .block_tokens = (uint32_t)block_tokens,
            .num_heads = (uint32_t)num_heads,
            .num_kv_heads = (uint32_t)num_kv_heads,
            .head_dim = (uint32_t)head_dim,
            .key_row_bytes = (uint32_t)key_row_bytes,
        };
        id<MTLBuffer> params_buffer = [provider->device newBufferWithBytes:&params length:sizeof(params) options:MTLResourceStorageModeShared];
        if (params_buffer == nil) return -4;
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(provider->queue, __func__);
        if (command_buffer == nil) return -5;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (encoder == nil) return -6;
        [encoder setComputePipelineState:pipeline];
        [encoder setBuffer:q_buffer offset:0 atIndex:0];
        [encoder setBuffer:key_buffer offset:0 atIndex:1];
        [encoder setBuffer:output_buffer offset:0 atIndex:2];
        [encoder setBuffer:params_buffer offset:0 atIndex:3];
        MTLSize grid_size = MTLSizeMake(output_len, 1, 1);
        NSUInteger thread_width = pipeline.maxTotalThreadsPerThreadgroup;
        if (thread_width == 0) thread_width = 64;
        if (thread_width > output_len && output_len > 0) thread_width = output_len;
        MTLSize group_size = MTLSizeMake(thread_width, 1, 1);
        [encoder dispatchThreads:grid_size threadsPerThreadgroup:group_size];
        [encoder endEncoding];
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -7;
    }
}

static int termite_metal_dispatch_tl1(termite_metal_provider *provider, const float *input, size_t rows, size_t in_dim, const uint8_t *weight_raw, size_t weight_bytes, size_t out_dim, uint32_t packed_len, uint32_t bm, uint32_t cfg_by, uint32_t bmm, float *output) {
    if (provider == NULL || provider->tl1_pipeline == nil || input == NULL || weight_raw == NULL || output == NULL) return -1;
    @autoreleasepool {
        const size_t input_bytes = rows * in_dim * sizeof(float);
        const size_t output_bytes = rows * out_dim * sizeof(float);
        id<MTLBuffer> input_buffer = [provider->device newBufferWithBytesNoCopy:(void *)input length:input_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [provider->device newBufferWithBytes:input length:input_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> weight_buffer = [provider->device newBufferWithBytesNoCopy:(void *)weight_raw length:weight_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (weight_buffer == nil) weight_buffer = [provider->device newBufferWithBytes:weight_raw length:weight_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> output_buffer = [provider->device newBufferWithBytesNoCopy:output length:output_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) output_buffer = [provider->device newBufferWithBytes:output length:output_bytes options:MTLResourceStorageModeShared];
        if (input_buffer == nil || weight_buffer == nil || output_buffer == nil) return -3;
        termite_metal_tl1_params params = { .rows = (uint32_t)rows, .in_dim = (uint32_t)in_dim, .out_dim = (uint32_t)out_dim, .packed_len = packed_len, .bm = bm, .cfg_by = cfg_by, .bmm = bmm };
        id<MTLBuffer> params_buffer = [provider->device newBufferWithBytes:&params length:sizeof(params) options:MTLResourceStorageModeShared];
        if (params_buffer == nil) return -4;
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(provider->queue, __func__);
        if (command_buffer == nil) return -5;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (encoder == nil) return -6;
        [encoder setComputePipelineState:provider->tl1_pipeline];
        [encoder setBuffer:input_buffer offset:0 atIndex:0];
        [encoder setBuffer:weight_buffer offset:0 atIndex:1];
        [encoder setBuffer:output_buffer offset:0 atIndex:2];
        [encoder setBuffer:params_buffer offset:0 atIndex:3];
        MTLSize grid_size = MTLSizeMake(out_dim, rows, 1);
        NSUInteger thread_width = provider->tl1_pipeline.maxTotalThreadsPerThreadgroup;
        if (thread_width == 0) thread_width = 32;
        if (thread_width > out_dim && out_dim > 0) thread_width = out_dim;
        MTLSize group_size = MTLSizeMake(thread_width, 1, 1);
        [encoder dispatchThreads:grid_size threadsPerThreadgroup:group_size];
        [encoder endEncoding];
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -7;
    }
}

static int termite_metal_dispatch_tl2(termite_metal_provider *provider, const float *input, size_t rows, size_t in_dim, const uint8_t *weight_raw, size_t weight_bytes, size_t out_dim, uint32_t scale_off, uint32_t three_value_len, uint32_t three_sign_len, uint32_t bm, uint32_t cfg_by, uint32_t bmm, uint32_t three_cols, uint32_t two_cols, float *output) {
    if (provider == NULL || provider->tl2_pipeline == nil || input == NULL || weight_raw == NULL || output == NULL) return -1;
    @autoreleasepool {
        const size_t input_bytes = rows * in_dim * sizeof(float);
        const size_t output_bytes = rows * out_dim * sizeof(float);
        id<MTLBuffer> input_buffer = [provider->device newBufferWithBytesNoCopy:(void *)input length:input_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [provider->device newBufferWithBytes:input length:input_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> weight_buffer = [provider->device newBufferWithBytesNoCopy:(void *)weight_raw length:weight_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (weight_buffer == nil) weight_buffer = [provider->device newBufferWithBytes:weight_raw length:weight_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> output_buffer = [provider->device newBufferWithBytesNoCopy:output length:output_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) output_buffer = [provider->device newBufferWithBytes:output length:output_bytes options:MTLResourceStorageModeShared];
        if (input_buffer == nil || weight_buffer == nil || output_buffer == nil) return -3;
        termite_metal_tl2_params params = { .rows = (uint32_t)rows, .in_dim = (uint32_t)in_dim, .out_dim = (uint32_t)out_dim, .scale_off = scale_off, .three_value_len = three_value_len, .three_sign_len = three_sign_len, .bm = bm, .cfg_by = cfg_by, .bmm = bmm, .three_cols = three_cols, .two_cols = two_cols };
        id<MTLBuffer> params_buffer = [provider->device newBufferWithBytes:&params length:sizeof(params) options:MTLResourceStorageModeShared];
        if (params_buffer == nil) return -4;
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(provider->queue, __func__);
        if (command_buffer == nil) return -5;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (encoder == nil) return -6;
        [encoder setComputePipelineState:provider->tl2_pipeline];
        [encoder setBuffer:input_buffer offset:0 atIndex:0];
        [encoder setBuffer:weight_buffer offset:0 atIndex:1];
        [encoder setBuffer:output_buffer offset:0 atIndex:2];
        [encoder setBuffer:params_buffer offset:0 atIndex:3];
        MTLSize grid_size = MTLSizeMake(out_dim, rows, 1);
        NSUInteger thread_width = provider->tl2_pipeline.maxTotalThreadsPerThreadgroup;
        if (thread_width == 0) thread_width = 32;
        if (thread_width > out_dim && out_dim > 0) thread_width = out_dim;
        MTLSize group_size = MTLSizeMake(thread_width, 1, 1);
        [encoder dispatchThreads:grid_size threadsPerThreadgroup:group_size];
        [encoder endEncoding];
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -7;
    }
}

int termite_metal_provider_linear_q4_0(termite_metal_provider *provider, const float *input, size_t rows, size_t in_dim, const uint8_t *weight_raw, size_t out_dim, float *output) {
    if (in_dim % 32 != 0) return -2;
    return termite_metal_dispatch(provider, provider->q4_0_pipeline, input, rows, in_dim, in_dim / 32, weight_raw, out_dim * (in_dim / 32) * 18u, out_dim, output);
}

int termite_metal_provider_linear_q4_1(termite_metal_provider *provider, const float *input, size_t rows, size_t in_dim, const uint8_t *weight_raw, size_t out_dim, float *output) {
    if (in_dim % 32 != 0) return -2;
    return termite_metal_dispatch(provider, provider->q4_1_pipeline, input, rows, in_dim, in_dim / 32, weight_raw, out_dim * (in_dim / 32) * 20u, out_dim, output);
}

int termite_metal_provider_linear_q5_0(termite_metal_provider *provider, const float *input, size_t rows, size_t in_dim, const uint8_t *weight_raw, size_t out_dim, float *output) {
    if (in_dim % 32 != 0) return -2;
    return termite_metal_dispatch(provider, provider->q5_0_pipeline, input, rows, in_dim, in_dim / 32, weight_raw, out_dim * (in_dim / 32) * 22u, out_dim, output);
}

int termite_metal_provider_linear_q5_1(termite_metal_provider *provider, const float *input, size_t rows, size_t in_dim, const uint8_t *weight_raw, size_t out_dim, float *output) {
    if (in_dim % 32 != 0) return -2;
    return termite_metal_dispatch(provider, provider->q5_1_pipeline, input, rows, in_dim, in_dim / 32, weight_raw, out_dim * (in_dim / 32) * 24u, out_dim, output);
}

int termite_metal_provider_linear_q8_0(termite_metal_provider *provider, const float *input, size_t rows, size_t in_dim, const uint8_t *weight_raw, size_t out_dim, float *output) {
    if (in_dim % 32 != 0) return -2;
    return termite_metal_dispatch(provider, provider->q8_0_pipeline, input, rows, in_dim, in_dim / 32, weight_raw, out_dim * (in_dim / 32) * 34u, out_dim, output);
}

int termite_metal_provider_linear_q8_1(termite_metal_provider *provider, const float *input, size_t rows, size_t in_dim, const uint8_t *weight_raw, size_t out_dim, float *output) {
    if (in_dim % 32 != 0) return -2;
    return termite_metal_dispatch(provider, provider->q8_1_pipeline, input, rows, in_dim, in_dim / 32, weight_raw, out_dim * (in_dim / 32) * 36u, out_dim, output);
}

int termite_metal_provider_linear_q8_0_planned(termite_metal_provider *provider, const float *input, size_t rows, size_t in_dim, const uint8_t *weight_raw, size_t out_dim, uint8_t planned_dispatch, float *output) {
    if (provider == NULL || input == NULL || weight_raw == NULL || output == NULL) return -1;
    if (in_dim % 32 != 0) return -2;
    @autoreleasepool {
        const size_t input_bytes = rows * in_dim * sizeof(float);
        const size_t weight_bytes = out_dim * (in_dim / 32u) * 34u;
        const size_t output_bytes = rows * out_dim * sizeof(float);
        id<MTLBuffer> input_buffer = [provider->device newBufferWithBytesNoCopy:(void *)input length:input_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (input_buffer == nil) input_buffer = [provider->device newBufferWithBytes:input length:input_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> weight_buffer = [provider->device newBufferWithBytesNoCopy:(void *)weight_raw length:weight_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (weight_buffer == nil) weight_buffer = [provider->device newBufferWithBytes:weight_raw length:weight_bytes options:MTLResourceStorageModeShared];
        id<MTLBuffer> output_buffer = [provider->device newBufferWithBytesNoCopy:output length:output_bytes options:MTLResourceStorageModeShared deallocator:nil];
        if (output_buffer == nil) output_buffer = [provider->device newBufferWithBytes:output length:output_bytes options:MTLResourceStorageModeShared];
        if (input_buffer == nil || weight_buffer == nil || output_buffer == nil) return -3;
        id<MTLCommandBuffer> command_buffer = termite_metal_new_command_buffer(provider->queue, __func__);
        if (command_buffer == nil) return -4;
        id<MTLComputeCommandEncoder> encoder = termite_metal_tracked_compute_command_encoder(command_buffer);
        if (encoder == nil) return -5;
        termite_metal_decode_runtime runtime = {0};
        runtime.q8_0_pipeline = provider->q8_0_pipeline;
        runtime.q8_0_mmv_pipeline = provider->q8_0_mmv_pipeline;
        runtime.q8_0_small_batch_r2_pipeline = provider->q8_0_small_batch_r2_pipeline;
        runtime.q8_0_small_batch_r3_pipeline = provider->q8_0_small_batch_r3_pipeline;
        runtime.q8_0_small_batch_r4_pipeline = provider->q8_0_small_batch_r4_pipeline;
        runtime.q8_0_small_batch_pipeline = provider->q8_0_small_batch_pipeline;
        runtime.q8_0_mm_pipeline = getenv("TERMITE_METAL_DISABLE_Q8_MM") != NULL ? nil : provider->q8_0_mm_pipeline;
        const int rc = termite_metal_encode_q8_0_linear_raw_on_encoder_planned(
            &runtime,
            encoder,
            input_buffer,
            0,
            weight_buffer,
            output_buffer,
            0,
            rows,
            in_dim,
            out_dim,
            planned_dispatch,
            -6);
        if (rc != 0) {
            [encoder endEncoding];
            return rc;
        }
        [encoder endEncoding];
        [command_buffer commit];
        [command_buffer waitUntilCompleted];
        return command_buffer.status == MTLCommandBufferStatusCompleted ? 0 : -7;
    }
}

int termite_metal_provider_linear_q2_k(termite_metal_provider *provider, const float *input, size_t rows, size_t in_dim, const uint8_t *weight_raw, size_t out_dim, float *output) {
    if (in_dim % 256 != 0) return -2;
    return termite_metal_dispatch(provider, provider->q2_k_pipeline, input, rows, in_dim, in_dim / 256, weight_raw, out_dim * (in_dim / 256) * 84u, out_dim, output);
}

int termite_metal_provider_linear_q3_k(termite_metal_provider *provider, const float *input, size_t rows, size_t in_dim, const uint8_t *weight_raw, size_t out_dim, float *output) {
    if (in_dim % 256 != 0) return -2;
    return termite_metal_dispatch(provider, provider->q3_k_pipeline, input, rows, in_dim, in_dim / 256, weight_raw, out_dim * (in_dim / 256) * 110u, out_dim, output);
}

int termite_metal_provider_linear_q4_k(termite_metal_provider *provider, const float *input, size_t rows, size_t in_dim, const uint8_t *weight_raw, size_t out_dim, float *output) {
    if (in_dim % 256 != 0) return -2;
    return termite_metal_dispatch(provider, provider->q4_k_pipeline, input, rows, in_dim, in_dim / 256, weight_raw, out_dim * (in_dim / 256) * 144u, out_dim, output);
}

int termite_metal_provider_linear_q5_k(termite_metal_provider *provider, const float *input, size_t rows, size_t in_dim, const uint8_t *weight_raw, size_t out_dim, float *output) {
    if (in_dim % 256 != 0) return -2;
    return termite_metal_dispatch(provider, provider->q5_k_pipeline, input, rows, in_dim, in_dim / 256, weight_raw, out_dim * (in_dim / 256) * 176u, out_dim, output);
}

int termite_metal_provider_linear_q6_k(termite_metal_provider *provider, const float *input, size_t rows, size_t in_dim, const uint8_t *weight_raw, size_t out_dim, float *output) {
    if (in_dim % 256 != 0) return -2;
    return termite_metal_dispatch(provider, provider->q6_k_pipeline, input, rows, in_dim, in_dim / 256, weight_raw, out_dim * (in_dim / 256) * 210u, out_dim, output);
}

int termite_metal_provider_linear_iq4_xs(termite_metal_provider *provider, const float *input, size_t rows, size_t in_dim, const uint8_t *weight_raw, size_t out_dim, float *output) {
    if (in_dim % 256 != 0) return -2;
    return termite_metal_dispatch(provider, provider->iq4_xs_pipeline, input, rows, in_dim, in_dim / 256, weight_raw, out_dim * (in_dim / 256) * 136u, out_dim, output);
}

int termite_metal_provider_linear_iq4_nl(termite_metal_provider *provider, const float *input, size_t rows, size_t in_dim, const uint8_t *weight_raw, size_t out_dim, float *output) {
    if (in_dim % 32 != 0) return -2;
    return termite_metal_dispatch(provider, provider->iq4_nl_pipeline, input, rows, in_dim, in_dim / 32, weight_raw, out_dim * (in_dim / 32) * 18u, out_dim, output);
}

int termite_metal_provider_linear_mxfp4(termite_metal_provider *provider, const float *input, size_t rows, size_t in_dim, const uint8_t *weight_raw, size_t out_dim, float *output) {
    if (in_dim % 32 != 0) return -2;
    return termite_metal_dispatch(provider, provider->mxfp4_pipeline, input, rows, in_dim, in_dim / 32, weight_raw, out_dim * (in_dim / 32) * 17u, out_dim, output);
}

int termite_metal_provider_linear_q8_k(termite_metal_provider *provider, const float *input, size_t rows, size_t in_dim, const uint8_t *weight_raw, size_t out_dim, float *output) {
    if (in_dim % 256 != 0) return -2;
    return termite_metal_dispatch(provider, provider->q8_k_pipeline, input, rows, in_dim, in_dim / 256, weight_raw, out_dim * (in_dim / 256) * 292u, out_dim, output);
}

int termite_metal_provider_linear_tl1(termite_metal_provider *provider, const float *input, size_t rows, size_t in_dim, const uint8_t *weight_raw, size_t weight_bytes, size_t out_dim, uint32_t packed_len, uint32_t bm, uint32_t cfg_by, uint32_t bmm, float *output) {
    if (packed_len + 4u > weight_bytes) return -2;
    return termite_metal_dispatch_tl1(provider, input, rows, in_dim, weight_raw, weight_bytes, out_dim, packed_len, bm, cfg_by, bmm, output);
}

int termite_metal_provider_linear_tl2(termite_metal_provider *provider, const float *input, size_t rows, size_t in_dim, const uint8_t *weight_raw, size_t weight_bytes, size_t out_dim, uint32_t scale_off, uint32_t three_value_len, uint32_t three_sign_len, uint32_t bm, uint32_t cfg_by, uint32_t bmm, uint32_t three_cols, uint32_t two_cols, float *output) {
    if (scale_off + 4u > weight_bytes) return -2;
    return termite_metal_dispatch_tl2(provider, input, rows, in_dim, weight_raw, weight_bytes, out_dim, scale_off, three_value_len, three_sign_len, bm, cfg_by, bmm, three_cols, two_cols, output);
}

int termite_metal_provider_compressed_key_scores_polar4(termite_metal_provider *provider, const float *q, size_t q_len, const uint8_t *encoded_key, size_t block_tokens, size_t num_heads, size_t num_kv_heads, size_t head_dim, size_t key_row_bytes, float *output) {
    if (q == NULL || encoded_key == NULL || output == NULL || q_len == 0 || block_tokens == 0 || num_heads == 0 || num_kv_heads == 0 || head_dim == 0) return -2;
    return termite_metal_dispatch_key_scores(provider, provider->polar4_key_scores_pipeline, q, q_len, encoded_key, block_tokens * key_row_bytes, block_tokens, num_heads, num_kv_heads, head_dim, key_row_bytes, output);
}

int termite_metal_provider_compressed_key_scores_turbo3(termite_metal_provider *provider, const float *q, size_t q_len, const uint8_t *encoded_key, size_t block_tokens, size_t num_heads, size_t num_kv_heads, size_t head_dim, size_t key_row_bytes, float *output) {
    if (q == NULL || encoded_key == NULL || output == NULL || q_len == 0 || block_tokens == 0 || num_heads == 0 || num_kv_heads == 0 || head_dim == 0) return -2;
    return termite_metal_dispatch_key_scores(provider, provider->turbo3_key_scores_pipeline, q, q_len, encoded_key, block_tokens * key_row_bytes, block_tokens, num_heads, num_kv_heads, head_dim, key_row_bytes, output);
}
