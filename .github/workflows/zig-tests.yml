name: Zig Tests

on:
  workflow_call:
    inputs:
      pr_number:
        type: string
        default: ''
      head_sha:
        type: string
        default: ''
      base_sha:
        type: string
        default: ''
      check_id:
        type: string
        default: ''
      comment_id:
        type: string
        default: ''
      validation_scope:
        type: string
        default: all
  push:
    branches: ["main"]
  merge_group:
    types: [checks_requested]
  schedule:
    - cron: "0 8 * * *"
    - cron: "0 3 * * 0"
  workflow_dispatch:
    inputs:
      validation_scope:
        description: "Full validation scope"
        required: true
        default: all
        type: choice
        options:
          - all
          - inference-e2e

permissions:
  contents: read
  checks: read
  pull-requests: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  ZIG_LOCAL_CACHE_DIR: ${{ github.workspace }}/zig/.zig-cache
  ZIG_GLOBAL_CACHE_DIR: ${{ github.workspace }}/zig/.zig-global-cache

concurrency:
  group: zig-${{ inputs.pr_number || github.ref }}
  cancel-in-progress: true

jobs:
  admission:
    uses: ./.github/workflows/pr-ci-admission.yml
    with:
      pr_number: ${{ inputs.pr_number }}
      head_sha: ${{ inputs.head_sha }}
      base_sha: ${{ inputs.base_sha }}
      check_id: ${{ inputs.check_id }}
      comment_id: ${{ inputs.comment_id }}
      suite: zig

  changes:
    if: >-
      ${{ !cancelled() &&
      success() &&
      needs.admission.result == 'success' &&
      (inputs.pr_number == '' ||
      github.run_attempt == 1) }}
    needs: admission
    runs-on: arc-antfly-standard
    outputs:
      zig_tests: ${{ steps.detect.outputs.zig_tests }}
      gemma4_training: ${{ steps.detect.outputs.gemma4_training }}
      e2e: ${{ steps.detect.outputs.e2e }}
      model_check: ${{ steps.detect.outputs.model_check }}
      trace_validate: ${{ steps.detect.outputs.trace_validate }}
      backup_s3: ${{ steps.detect.outputs.backup_s3 }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.head_sha || github.sha }}
          persist-credentials: false
          fetch-depth: 0

      - name: Detect relevant changes
        id: detect
        shell: bash
        run: |
          set -euo pipefail

          if [[ "${{ inputs.pr_number != '' && 'pull_request' || github.event_name }}" == "workflow_dispatch" ]]; then
            if [[ "${{ inputs.validation_scope }}" == "inference-e2e" ]]; then
              {
                echo "zig_tests=false"
                echo "e2e=true"
                echo "model_check=false"
                echo "trace_validate=false"
                echo "backup_s3=false"
              } >> "$GITHUB_OUTPUT"
              exit 0
            fi
            {
              echo "zig_tests=true"
              echo "gemma4_training=true"
              echo "e2e=true"
              echo "model_check=true"
              echo "trace_validate=true"
              echo "backup_s3=true"
            } >> "$GITHUB_OUTPUT"
            exit 0
          fi

          if [[ "${{ inputs.pr_number != '' && 'pull_request' || github.event_name }}" == "schedule" ]]; then
            echo "zig_tests=false" >> "$GITHUB_OUTPUT"
            if [[ "${{ github.event.schedule }}" == "0 3 * * 0" ]]; then
              {
                echo "gemma4_training=true"
                echo "e2e=false"
                echo "model_check=true"
                echo "trace_validate=true"
                echo "backup_s3=false"
              } >> "$GITHUB_OUTPUT"
            else
              {
                echo "gemma4_training=false"
                echo "e2e=true"
                echo "model_check=false"
                echo "trace_validate=false"
                echo "backup_s3=true"
              } >> "$GITHUB_OUTPUT"
            fi
            exit 0
          fi

          if [[ "${{ inputs.pr_number != '' && 'pull_request' || github.event_name }}" == "pull_request" ]]; then
            base="${{ inputs.base_sha }}"
          elif [[ "${{ github.event_name }}" == "merge_group" && -n "${{ github.event.merge_group.base_sha }}" ]]; then
            base="${{ github.event.merge_group.base_sha }}"
          elif [[ "${{ inputs.pr_number != '' && 'pull_request' || github.event_name }}" == "push" && -n "${{ github.event.before }}" && "${{ github.event.before }}" != "0000000000000000000000000000000000000000" ]]; then
            base="${{ github.event.before }}"
          else
            {
              echo "zig_tests=true"
              echo "gemma4_training=true"
              echo "e2e=true"
              echo "model_check=true"
              echo "trace_validate=true"
              echo "backup_s3=true"
            } >> "$GITHUB_OUTPUT"
            exit 0
          fi

          if git diff --quiet "$base" "${{ inputs.head_sha || github.sha }}" -- \
            ':(glob)zig/**' \
            ':(exclude,glob)zig/pkg/antfly/antfarm/**' \
            ':(glob)scripts/ci/zig-*.sh' \
            scripts/ci/check_zig_checksum_usage.py \
            scripts/ci/test_check_zig_checksum_usage.py \
            scripts/ci/toolchain-policy.json \
            .github/actions/load-toolchain-policy/action.yml \
            scripts/test-backup-s3-integration.sh \
            scripts/run_db_query_matrix.py \
            scripts/test_run_db_query_matrix.py \
            scripts/test_conformance_fixtures.py \
            ':(glob)specs/openapi/**' \
            openapi.yaml \
            scripts/join_openapi.py \
            scripts/join_public_openapi.py \
            scripts/openapi_joiner.py \
            scripts/openapi_inputs.py \
            scripts/public_openapi_overlays.py \
            scripts/yaml_to_json.py \
            scripts/generate_graph_identifier_policy.py \
            scripts/generate_mcp_schema_fragments.py \
            scripts/pyproject.toml \
            scripts/uv.lock \
            scripts/ci/test_zig_validation_scope.py \
            ':(glob)scripts/bench/pdf/**' \
            Makefile \
            .github/workflows/zig-tests.yml \
            .github/workflows/antfly-container.yml
          then
            echo "zig_tests=false" >> "$GITHUB_OUTPUT"
          else
            echo "zig_tests=true" >> "$GITHUB_OUTPUT"
          fi

          if git diff --quiet "$base" "${{ inputs.head_sha || github.sha }}" -- \
            ':(glob)zig/lib/**' \
            zig/build.zig \
            zig/build.zig.zon \
            ':(glob)zig/build/**' \
            ':(glob)zig/pkg/antfly/build/**' \
            ':(glob)zig/pkg/antfly/src/inference_runtime/**' \
            zig/pkg/inference/build.zig \
            zig/pkg/inference/build.zig.zon \
            ':(glob)zig/pkg/inference/build/**' \
            zig/pkg/inference/src/architectures/gemma_graph.zig \
            zig/pkg/inference/src/architectures/session_factory.zig \
            ':(glob)zig/pkg/inference/src/backends/metal_*' \
            ':(glob)zig/pkg/inference/src/backends/decoder_*' \
            ':(glob)zig/pkg/inference/src/runtime/**' \
            ':(glob)zig/pkg/inference/src/ops/metal/**' \
            zig/pkg/inference/src/finetune/artifact_publication.zig \
            zig/pkg/inference/src/finetune/chat_template.zig \
            ':(glob)zig/pkg/inference/src/finetune/cli/**' \
            ':(glob)zig/pkg/inference/src/finetune/gemma4*' \
            zig/pkg/inference/src/finetune/grpo.zig \
            ':(glob)zig/pkg/inference/src/finetune/lora*' \
            zig/pkg/inference/src/finetune/path_isolation.zig \
            ':(glob)zig/pkg/inference/src/finetune/preference*' \
            ':(glob)zig/pkg/inference/src/finetune/run_gemma4*' \
            ':(glob)zig/pkg/inference/src/finetune/test/test_gemma4*' \
            ':(glob)zig/pkg/inference/src/finetune/tools/*gemma4*' \
            ':(glob)zig/pkg/inference/src/finetune/train/*gemma4*' \
            zig/pkg/inference/src/finetune/real_autodiff_trainer.zig \
            ':(glob)zig/pkg/inference/src/finetune/recipe*.zig' \
            zig/pkg/inference/src/finetune/training_environment.zig \
            ':(glob)zig/pkg/inference/src/graph/**' \
            ':(glob)zig/pkg/inference/src/gemma*_test_root.zig' \
            ':(glob)zig/pkg/inference/scripts/*gemma4*' \
            ':(glob)zig/pkg/inference/scripts/gemma4/**' \
            ':(glob)zig/pkg/inference/testdata/*dpo*' \
            ':(glob)zig/pkg/inference/testdata/*grpo*' \
            ':(glob)zig/pkg/inference/testdata/*gemma4*' \
            ':(glob)zig/pkg/inference/testdata/gemma4_oracle/**' \
            zig/pkg/inference/src/inference_internal.zig \
            zig/pkg/inference/src/ops/metal_compute.zig \
            zig/pkg/inference/src/ops/native_compute.zig \
            scripts/ci/audit_gemma4_test_selection.py \
            scripts/ci/test_audit_gemma4_test_selection.py \
            scripts/ci/test_zig_validation_scope.py \
            scripts/ci/toolchain-policy.json \
            .github/actions/load-toolchain-policy/action.yml \
            .github/workflows/zig-tests.yml \
            ':(exclude,glob)**/*.md'
          then
            echo "gemma4_training=false" >> "$GITHUB_OUTPUT"
          else
            echo "gemma4_training=true" >> "$GITHUB_OUTPUT"
          fi

          if git diff --quiet "$base" "${{ inputs.head_sha || github.sha }}" -- \
            ':(glob)zig/**' \
            ':(exclude,glob)zig/pkg/antfly/antfarm/**' \
            ':(glob)scripts/ci/zig-*.sh' \
            scripts/ci/toolchain-policy.json \
            ':(glob)go/pkg/operator/**' \
            ':(glob)go/pkg/sdk/admin/**' \
            go/pkg/sdk/go.mod \
            go/pkg/sdk/go.sum \
            ':(glob)specs/openapi/**' \
            .github/actions/load-toolchain-policy/action.yml \
            scripts/test-backup-s3-integration.sh \
            Makefile \
            .github/workflows/zig-tests.yml \
            .github/workflows/antfly-container.yml
          then
            echo "e2e=false" >> "$GITHUB_OUTPUT"
          else
            echo "e2e=true" >> "$GITHUB_OUTPUT"
          fi

          if git diff --quiet "$base" "${{ inputs.head_sha || github.sha }}" -- \
            scripts/test-backup-s3-integration.sh \
            zig/build.zig \
            ':(glob)zig/pkg/antfly/build/**' \
            ':(glob)zig/pkg/inference/build/**' \
            ':(glob)zig/lib/**/build_support.zig' \
            ':(glob)zig/lib/**/build.zig' \
            zig/tools/build_support.zig \
            zig/lib/httpx \
            zig/lib/objectstore \
            zig/pkg/antfly/src/api/backups.zig \
            zig/pkg/antfly/src/serverless/remote_uri.zig \
            .github/workflows/zig-tests.yml
          then
            echo "backup_s3=false" >> "$GITHUB_OUTPUT"
          else
            echo "backup_s3=true" >> "$GITHUB_OUTPUT"
          fi

          if git diff --quiet "$base" "${{ inputs.head_sha || github.sha }}" -- \
            ':(glob)zig/specs/tla/**' \
            ':(glob)scripts/tla-*.sh' \
            ':(glob)scripts/tla-*.py' \
            Makefile \
            .github/workflows/zig-tests.yml
          then
            echo "model_check=false" >> "$GITHUB_OUTPUT"
          else
            echo "model_check=true" >> "$GITHUB_OUTPUT"
          fi

          if git diff --quiet "$base" "${{ inputs.head_sha || github.sha }}" -- \
            ':(glob)zig/specs/tla/**' \
            ':(glob)scripts/tla-*.sh' \
            ':(glob)scripts/tla-*.py' \
            ':(glob)zig/pkg/antfly/src/tracing/**' \
            ':(glob)zig/pkg/antfly/src/raft/**' \
            ':(glob)zig/pkg/antfly/src/storage/db/**' \
            ':(glob)zig/lib/raft/**' \
            Makefile \
            zig/build.zig \
            ':(glob)zig/pkg/antfly/build/**' \
            ':(glob)zig/pkg/inference/build/**' \
            ':(glob)zig/lib/**/build_support.zig' \
            ':(glob)zig/lib/**/build.zig' \
            zig/tools/build_support.zig \
            .github/workflows/zig-tests.yml
          then
            echo "trace_validate=false" >> "$GITHUB_OUTPUT"
          else
            echo "trace_validate=true" >> "$GITHUB_OUTPUT"
          fi

  backup-s3-integration:
    if: >-
      ${{ !cancelled() &&
      success() &&
      needs.admission.result == 'success' &&
      (inputs.pr_number == '' ||
      github.run_attempt == 1) &&
      (needs.changes.outputs.backup_s3 == 'true' &&
      vars.ENABLE_BACKUP_S3_INTEGRATION == 'true') }}
    name: backup-s3-integration
    needs: [admission, changes]
    # Opt in explicitly to avoid GitHub-hosted runner charges while this
    # service-backed integration is temporarily disabled.
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.head_sha || github.sha }}
          persist-credentials: false
          submodules: true

      - name: Load pinned toolchain policy
        id: toolchain
        uses: ./.github/actions/load-toolchain-policy

      - name: Install build tools
        run: sudo apt-get update && sudo apt-get install -y build-essential pkg-config ca-certificates git curl xz-utils

      - name: Set up Zig
        uses: mlugg/setup-zig@d1434d08867e3ee9daa34448df10607b98908d29 # v2
        with:
          version: ${{ steps.toolchain.outputs.zig_version }}
          use-cache: false

      - name: Run backup listing against MinIO
        run: scripts/test-backup-s3-integration.sh

  arm64-codec:
    if: >-
      ${{ !cancelled() &&
      success() &&
      needs.admission.result == 'success' &&
      (inputs.pr_number == '' ||
      github.run_attempt == 1) &&
      (( (inputs.pr_number != '' ||
      github.event.schedule == '0 3 * * 0') &&
      ( needs.changes.outputs.zig_tests == 'true' ||
      needs.changes.outputs.model_check == 'true' ||
      needs.changes.outputs.trace_validate == 'true' ) ) ||
      github.event_name == 'merge_group' ||
      (github.event_name == 'push' &&
      github.ref == 'refs/heads/main') ||
      ((inputs.pr_number == '' &&
      github.event_name == 'workflow_dispatch') &&
      inputs.validation_scope == 'all') ||
      github.event.schedule == '0 8 * * *') }}
    name: ${{ (inputs.pr_number != '' || github.event.schedule == '0 3 * * 0') && 'zig-base / arm64-codec' || 'zig-full / arm64-codec' }}
    needs: [admission, changes]
    runs-on: arc-antfly-publish-arm64
    timeout-minutes: 20
    env:
      ZIG_LOCAL_CACHE_DIR: ${{ github.workspace }}/.zig-cache-arm64-codec
      ZIG_GLOBAL_CACHE_DIR: ${{ github.workspace }}/.zig-global-cache-arm64-codec
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.head_sha || github.sha }}
          persist-credentials: false

      - name: Load pinned toolchain policy
        id: toolchain
        uses: ./.github/actions/load-toolchain-policy

      - name: Verify native Linux ARM64 runner
        run: |
          set -euo pipefail
          uname -srm
          test "$(uname -s)" = "Linux"
          test "$(uname -m)" = "aarch64"

      - name: Install Zig
        env:
          ZIG_VERSION: ${{ steps.toolchain.outputs.zig_version }}
          ZIG_AARCH64_LINUX_SHA256: ${{ steps.toolchain.outputs.zig_aarch64_linux_sha256 }}
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y ca-certificates curl xz-utils python3 make
          curl -fsSL \
            "https://ziglang.org/download/${ZIG_VERSION}/zig-aarch64-linux-${ZIG_VERSION}.tar.xz" \
            -o "$RUNNER_TEMP/zig.tar.xz"
          echo "$ZIG_AARCH64_LINUX_SHA256  $RUNNER_TEMP/zig.tar.xz" | sha256sum --check --strict
          mkdir -p "$RUNNER_TEMP/zig"
          tar -xJf "$RUNNER_TEMP/zig.tar.xz" \
            -C "$RUNNER_TEMP/zig" \
            --strip-components=1
          echo "$RUNNER_TEMP/zig" >> "$GITHUB_PATH"

      - name: Verify Zig version
        env:
          ZIG_VERSION: ${{ steps.toolchain.outputs.zig_version }}
        run: test "$(zig version)" = "$ZIG_VERSION"

      - name: Run StreamVByte codec tests
        run: |
          set -euo pipefail
          zig test zig/pkg/antfly/src/encoding/streamvbyte.zig
          zig test -O ReleaseFast zig/pkg/antfly/src/encoding/streamvbyte.zig
          zig test zig/pkg/antfly/src/encoding/chunked_coder.zig
          zig test -O ReleaseFast zig/pkg/antfly/src/encoding/chunked_coder.zig

      - name: Check shared checksum usage
        run: make zig-checksums-check

      - name: Test baseline ARM64 checksum runtime dispatch
        run: |
          zig test zig/lib/hash/src/mod.zig -O ReleaseSafe -mcpu=baseline
          zig test zig/lib/hash/src/mod.zig -O ReleaseSafe -mcpu=baseline -lc

  zig-base-tests:
    if: >-
      ${{ !cancelled() &&
      success() &&
      needs.admission.result == 'success' &&
      (inputs.pr_number == '' ||
      github.run_attempt == 1) &&
      ((inputs.pr_number != '' ||
      github.event.schedule == '0 3 * * 0') &&
      (needs.changes.outputs.zig_tests == 'true' ||
      needs.changes.outputs.model_check == 'true' ||
      needs.changes.outputs.trace_validate == 'true')) }}
    name: zig-base / x86_64
    needs: [admission, changes]
    runs-on: arc-antfly-heavy
    # Keep the outer job timeout above the in-step hard watchdog so true hangs
    # produce the watchdog stack dump instead of an opaque GitHub cancellation.
    timeout-minutes: 120
    env:
      # ARC heavy pods have a 24 GiB limit but inject a 20 GiB default.
      # Admit the 22 GiB storage compile while leaving 2 GiB for the runner
      # and build driver. Keep this scoped to the heavy build jobs.
      ANTFLY_ZIG_MAX_RSS: "23622320128"
      ZIG_LOCAL_CACHE_DIR: /mnt/cache/antfly/zig/${{ github.job }}/${{ github.run_id }}-${{ github.run_attempt }}/zig-local
      ZIG_GLOBAL_CACHE_DIR: /mnt/cache/antfly/zig/zig-global
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.head_sha || github.sha }}
          persist-credentials: false
          fetch-depth: 0
          submodules: true

      - name: Load pinned toolchain policy
        id: toolchain
        uses: ./.github/actions/load-toolchain-policy

      - name: Install build tools
        run: sudo apt-get update && sudo apt-get install -y build-essential pkg-config ca-certificates git curl xz-utils gdb file

      - name: Install uv
        uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1

      - name: Set up Zig
        uses: mlugg/setup-zig@d1434d08867e3ee9daa34448df10607b98908d29 # v2
        with:
          version: ${{ steps.toolchain.outputs.zig_version }}
          use-cache: false

      - name: Restore ARC Zig cache dirs
        run: |
          local_cache="/mnt/cache/antfly/zig/${{ github.job }}/${{ github.run_id }}-${{ github.run_attempt }}/zig-local"
          global_cache="/mnt/cache/antfly/zig/zig-global"
          echo "ZIG_LOCAL_CACHE_DIR=$local_cache" >> "$GITHUB_ENV"
          echo "ZIG_GLOBAL_CACHE_DIR=$global_cache" >> "$GITHUB_ENV"
          mkdir -p "$local_cache" "$global_cache"

      - name: Verify Zig version
        run: zig version

      - name: Check shared checksum usage
        run: make zig-checksums-check

      - name: Test baseline checksum runtime dispatch
        working-directory: zig
        run: |
          zig test lib/hash/src/mod.zig -O Debug -mcpu=baseline -fno-llvm
          zig build lib-hash-test -Dcpu=baseline -Doptimize=ReleaseSafe

      - name: Test native static-musl process execution
        working-directory: zig
        run: |
          set -euo pipefail
          zig build lib-platform-test -Dtarget=x86_64-linux-musl --summary all 2>&1 | tee "$RUNNER_TEMP/platform-musl.log"
          # A successful aggregate could hide a skipped process check.
          grep -q 'run python3 success' "$RUNNER_TEMP/platform-musl.log"

      - name: Check generated sources
        run: make zig-generated-check

      - name: Compile consolidated storage and graph benchmark contracts
        if: needs.changes.outputs.zig_tests == 'true'
        working-directory: zig
        run: zig build antfly-storage-bench antfly-graph-bench -Doptimize=Debug

      # Keep inexpensive tooling checks beside the normal unit gate. The
      # isolated compilation/cache matrix runs in zig-full / build-cache.
      - name: Test build tooling
        working-directory: zig
        run: >-
          python3 -m unittest
          tools/test_patch_zig_0_16_build_runner_maxrss.py
          tools/test_run_bounded_zig_build.py
          tools/test_analyze_zig_import_graph.py
          tools/test_audit_storage_test_shards.py
          tools/test_audit_test_selection.py
          tools/test_check_storage_compilation.py
          tools/test_run_test_partitions.py
          tools/test_run_recall_checks.py
          tools/test_test_runner.py
          tools/test_sync_generated.py

      - name: Test benchmark orchestration and fixture setup
        run: |
          python3 -m unittest discover -s scripts -p test_run_db_query_matrix.py
          python3 -m unittest discover -s scripts -p test_conformance_fixtures.py
          python3 -m unittest discover -s scripts/bench/pdf -p 'test_*.py'

      - name: Check inference kernels through the root build graph
        working-directory: zig
        run: >-
          zig build inference-test -Dmetal=false -Dcuda=false --
          --test-filter "quant kernel compiler"
          --test-filter "metal renderer"

      - name: Run inference benchmark and release contract tests
        working-directory: zig/pkg/inference
        run: |
          python3 -m unittest \
            scripts.gemma4.test_benchmark_gemma4_long_e2e_server \
            scripts.gemma4.test_benchmark_gemma4_cuda_batching \
            scripts.gemma4.test_benchmark_gemma4_cuda_matrix \
            scripts.gemma4.test_benchmark_metal_gemma4_ab \
            scripts.gemma4.test_benchmark_metal_gemma4_long_output \
            scripts.test_content_security_contract \
            scripts.gemma4.test_gemma4_cuda_l4_release_gate \
            scripts.gemma4.test_gemma4_cuda_production_gate \
            scripts.gemma4.test_gemma4_qat_cuda_tuning \
            scripts.gemma4.test_gemma4_qat_llamacpp_pair_benchmark \
            scripts.test_paired_benchmark \
            scripts.test_regen_cuda_artifacts \
            scripts.test_summarize_cuda_flash_prefill_v2 \
            scripts.gemma4.test_validate_gemma4_cuda_candidate \
            scripts.gemma4.test_validate_gemma4_cuda_quality_candidate

      - name: Run Qwen model parity and promotion contract tests
        working-directory: zig/pkg/inference
        run: >-
          uv run --with numpy==2.4.2 python
          scripts/run_model_contract_tests.py
          qwen3_embedding qwen3vl

      - name: Test reproducible release archive packaging
        run: python3 scripts/packaging/test_reproducible_tar.py

      # ARC may expose the shared node's memory total when the runner cgroup is
      # unlimited. Cap the aggregate scheduler budget at 22 GiB so concurrent
      # test runtimes cannot overcommit the runner while still admitting the
      # largest individual compile step, which reserves 22 GiB. The explicit
      # heavy-job budget above leaves 2 GiB of pod headroom. The patched 0.16
      # runner correctly reserves each ready step's max_rss claim as it wakes it.
      - name: Configure bounded parallel unit tests
        if: needs.changes.outputs.zig_tests == 'true'
        id: cpus
        run: |
          lscpu
          jobs=$(nproc)
          if [ "$jobs" -gt 8 ]; then jobs=8; fi
          echo "list=0-$((jobs - 1))" >> "$GITHUB_OUTPUT"
          echo "max_rss=$(python3 zig/tools/run_bounded_zig_build.py --print-max-rss --max-rss-cap 23622320128)" >> "$GITHUB_OUTPUT"
          zig_lib_dir="$(zig env | sed -n 's/^[[:space:]]*\.lib_dir = "\(.*\)",$/\1/p')"
          patched_runner="${RUNNER_TEMP}/zig-build-runner-maxrss.zig"
          python3 zig/tools/patch_zig_0_16_build_runner_maxrss.py \
            "$zig_lib_dir/compiler/build_runner.zig" \
            "$patched_runner"
          echo "build_runner=$patched_runner" >> "$GITHUB_OUTPUT"

      - name: Run hermetic Antfly unit tests
        if: needs.changes.outputs.zig_tests == 'true'
        env:
          ANTFLY_TEST_CLEANUP_TRACE: "1"
        run: |
          set -euo pipefail

          collect_descendants() {
            local roots="$*"
            local all=" ${roots} "
            local changed=1
            while [ "$changed" -eq 1 ]; do
              changed=0
              while read -r pid ppid; do
                if [[ " $all " == *" $ppid "* && " $all " != *" $pid "* ]]; then
                  all="${all}${pid} "
                  changed=1
                fi
              done < <(ps -eo pid=,ppid=)
            done
            echo "$all"
          }

          dump_stacks() {
            local root_pid="$1"
            echo "::group::antfly unit watchdog ptrace settings"
            if [ -r /proc/sys/kernel/yama/ptrace_scope ]; then
              cat /proc/sys/kernel/yama/ptrace_scope || true
            fi
            sysctl kernel.yama.ptrace_scope 2>/dev/null || true
            echo "::endgroup::"

            echo "::group::antfly unit watchdog process tree"
            ps -eo pid,ppid,stat,etime,cmd | awk 'NR == 1 || /zig build|zig-local|\/test|make zig-unit-test|make -C \.\/zig unit-test/' || true
            echo "::endgroup::"

            local pids
            pids="$(collect_descendants "$root_pid")"
            for pid in $pids; do
              if ! kill -0 "$pid" 2>/dev/null; then
                continue
              fi
              local cmdline
              cmdline="$(tr '\0' ' ' < "/proc/$pid/cmdline" 2>/dev/null || true)"
              case "$cmdline" in
                *"/test "*|*"/test"*|*"zig build"*|*" make "*)
                  echo "::group::proc snapshot pid=$pid cmd=$cmdline"
                  if [ -r "/proc/$pid/status" ]; then
                    grep -E "^(Name|State|Pid|PPid|Threads|VmRSS|VmHWM|voluntary_ctxt_switches|nonvoluntary_ctxt_switches):" "/proc/$pid/status" || true
                  fi
                  if [ -r "/proc/$pid/wchan" ]; then
                    printf "wchan="
                    cat "/proc/$pid/wchan" || true
                    printf "\n"
                  fi
                  if [ -r "/proc/$pid/stack" ]; then
                    cat "/proc/$pid/stack" || true
                  fi
                  echo "::endgroup::"

                  echo "::group::gdb thread dump pid=$pid cmd=$cmdline"
                  timeout 45s gdb -batch -p "$pid" \
                    -ex "set pagination off" \
                    -ex "thread apply all bt" \
                    -ex "detach" || \
                    timeout 45s sudo gdb -batch -p "$pid" \
                      -ex "set pagination off" \
                      -ex "thread apply all bt" \
                      -ex "detach" || true
                  echo "::endgroup::"
                  ;;
              esac
            done
          }

          terminate_tree() {
            local root_pid="$1"
            local pids
            pids="$(collect_descendants "$root_pid")"
            for pid in $pids; do
              if [ "$pid" != "$$" ]; then
                kill -TERM "$pid" 2>/dev/null || true
              fi
            done
            sleep 5
            for pid in $pids; do
              if [ "$pid" != "$$" ]; then
                kill -KILL "$pid" 2>/dev/null || true
              fi
            done
          }

          if [ -r /proc/sys/kernel/yama/ptrace_scope ]; then
            echo "::group::configure ptrace for unit watchdog"
            echo "ptrace_scope before: $(cat /proc/sys/kernel/yama/ptrace_scope || true)"
            sudo sysctl -w kernel.yama.ptrace_scope=0 || true
            echo "ptrace_scope after: $(cat /proc/sys/kernel/yama/ptrace_scope || true)"
            echo "::endgroup::"
          fi

          unit_log="${RUNNER_TEMP:-/tmp}/antfly-zig-unit.log"
          : > "$unit_log"

          (
            set +e
            taskset -c "${{ steps.cpus.outputs.list }}" make zig-unit-test ZIG_BUILD_FLAGS="--build-runner ${{ steps.cpus.outputs.build_runner }} --maxrss ${{ steps.cpus.outputs.max_rss }} --summary all" 2>&1 | tee -a "$unit_log"
            exit "${PIPESTATUS[0]}"
          ) &
          unit_pid="$!"
          (
            # A cold Zig compile can legitimately remain silent for more than
            # 15 minutes on the x86_64 ARC runner. Keep the idle watchdog below
            # the independent hard limit without killing an active cold build.
            idle_limit_secs="${ANTFLY_UNIT_WATCHDOG_IDLE_SECS:-1800}"
            # PR #716's DB-core execution started 52 minutes into the combined
            # build/test step and was still progressing at the old 60m limit.
            # Allow 90m total, retaining the 30m idle and 120m outer job limits.
            hard_limit_secs="${ANTFLY_UNIT_WATCHDOG_HARD_SECS:-5400}"
            start_epoch="$(date +%s)"
            last_progress_epoch="$start_epoch"
            last_mtime=0
            while kill -0 "$unit_pid" 2>/dev/null; do
              sleep 15
              now_epoch="$(date +%s)"
              if [ -e "$unit_log" ]; then
                current_mtime="$(stat -c %Y "$unit_log" 2>/dev/null || echo 0)"
                if [ "$current_mtime" -gt "$last_mtime" ]; then
                  last_mtime="$current_mtime"
                  last_progress_epoch="$now_epoch"
                fi
              fi

              elapsed_secs=$((now_epoch - start_epoch))
              idle_secs=$((now_epoch - last_progress_epoch))
              if [ "$hard_limit_secs" -gt 0 ] && [ "$elapsed_secs" -ge "$hard_limit_secs" ]; then
                echo "::error::zig-unit-test hard watchdog fired after ${elapsed_secs}s; dumping stacks before terminating live test processes"
                tail -n 200 "$unit_log" || true
                dump_stacks "$unit_pid"
                terminate_tree "$unit_pid"
                exit 0
              fi
              if [ "$idle_limit_secs" -gt 0 ] && [ "$idle_secs" -ge "$idle_limit_secs" ]; then
                echo "::error::zig-unit-test idle watchdog fired after ${idle_secs}s without unit log progress; dumping stacks before terminating live test processes"
                tail -n 200 "$unit_log" || true
                dump_stacks "$unit_pid"
                terminate_tree "$unit_pid"
                exit 0
              fi
            done
          ) &
          watchdog_pid="$!"

          status=0
          wait "$unit_pid" || status="$?"
          kill "$watchdog_pid" 2>/dev/null || true
          wait "$watchdog_pid" 2>/dev/null || true
          if [ "$status" -ne 0 ] && [ -d /sys/fs/cgroup ]; then
            echo "::group::unit failure cgroup memory"
            for metric in memory.current memory.peak memory.max memory.events; do
              if [ -r "/sys/fs/cgroup/$metric" ]; then
                echo "[$metric]"
                cat "/sys/fs/cgroup/$metric"
              fi
            done
            echo "::endgroup::"
          fi
          exit "$status"

      - name: Set up Java for TLA+
        if: needs.changes.outputs.model_check == 'true' || needs.changes.outputs.trace_validate == 'true'
        uses: actions/setup-java@dd06d9cba3e5552c54d9f8ea23572deb30010f7c # v6.0.0
        with:
          distribution: "temurin"
          java-version: "21"

      - name: Run TLA verification
        if: needs.changes.outputs.model_check == 'true' || needs.changes.outputs.trace_validate == 'true'
        env:
          ANTFLY_TLA_MODEL_CHECK: ${{ needs.changes.outputs.model_check }}
          ANTFLY_TLA_TRACE_VALIDATE: ${{ needs.changes.outputs.trace_validate }}
        run: scripts/ci/zig-tla-verify.sh

  gemma4-metal-training-tests:
    name: gemma4-metal-training / macos-arm64-gpu
    needs: [admission, changes]
    if: >-
      ${{ !cancelled() && needs.admission.result == 'success' &&
      (inputs.pr_number == '' || github.run_attempt == 1) &&
      needs.changes.outputs.gemma4_training == 'true' }}
    runs-on: macos-15-xlarge
    timeout-minutes: 120
    env:
      ZIG_LOCAL_CACHE_DIR: ${{ runner.temp }}/antfly-gemma4-metal-zig-local
      ZIG_GLOBAL_CACHE_DIR: ${{ runner.temp }}/antfly-gemma4-metal-zig-global
      TERMITE_REQUIRE_METAL_TESTS: "1"
      ANTFLY_BENCHMARK_SOURCE_REVISION: ${{ inputs.head_sha || github.sha }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.head_sha || github.sha }}
          persist-credentials: false
          submodules: true
          fetch-depth: 0

      - name: Load pinned toolchain policy
        id: toolchain
        uses: ./.github/actions/load-toolchain-policy

      - name: Set up pinned Gemma4 benchmark Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Set up Zig
        uses: mlugg/setup-zig@d1434d08867e3ee9daa34448df10607b98908d29 # v2
        with:
          version: ${{ steps.toolchain.outputs.zig_version }}
          use-cache: false

      - name: Install Gemma4 Zig-oracle packaging dependencies
        run: python3 -m pip install --disable-pip-version-check -r zig/pkg/inference/scripts/gemma4/requirements-gemma4-zig-oracle.txt

      - name: Verify Metal device
        run: system_profiler SPDisplaysDataType

      - name: Validate Gemma4 Python contracts
        working-directory: zig/pkg/inference
        run: |
          set -euo pipefail
          python3 ../../../scripts/ci/test_zig_validation_scope.py
          python3 ../../../scripts/ci/test_audit_gemma4_test_selection.py
          python3 scripts/gemma4/compare_gemma4_lora_hf_zig.py validate-lock
          python3 -m unittest discover -v -s scripts/gemma4 -p 'test_*.py'
          fixture_dir="$(mktemp -d "${RUNNER_TEMP}/gemma4-oracle-contract.XXXXXX")"
          trap 'rm -rf "$fixture_dir"' EXIT
          python3 scripts/gemma4/generate_gemma4_oracle_fixture.py \
            --output "$fixture_dir/fixture.jsonl" \
            --manifest "$fixture_dir/fixture.manifest.json" \
            --train-count 8 --eval-count 4 --test-count 4
          python3 scripts/gemma4/generate_gemma4_oracle_fixture.py \
            --output "$fixture_dir/fixture.jsonl" \
            --manifest "$fixture_dir/fixture.manifest.json" \
            --train-count 8 --eval-count 4 --test-count 4 --check

      - name: Run Gemma4 Metal Debug gates
        working-directory: zig/pkg/inference
        run: |
          set -euo pipefail
          zig build test-gemma4-finetune -j1 --maxrss 10737418240 -Dmetal=true -Dcuda=false -Donnx=false -Dpjrt=false -Doptimize=Debug -Dbenchmark-source-revision="$ANTFLY_BENCHMARK_SOURCE_REVISION" --summary all 2>&1 | tee "$RUNNER_TEMP/gemma4-focused.log"
          zig build test-gemma4-serving -j1 -Dmetal=true -Dcuda=false -Donnx=false -Dpjrt=false -Doptimize=Debug --summary all
          zig build test-gemma-graph -j1 -Dmetal=true -Dcuda=false -Donnx=false -Dpjrt=false -Doptimize=Debug --summary all
          zig build test -j1 -Dmetal=true -Dcuda=false -Donnx=false -Dpjrt=false -Doptimize=Debug -- \
            --test-filter "backend deinit destroys only explicitly owned wrappers" \
            --test-filter "lora linear replaces an existing internal value without leaking" \
            --test-filter "owning tensor host mirror" \
            --test-filter "Gemma4 PEFT export parser requires the named production contract" \
            --test-filter "finetune contextual help covers the production Gemma4 path" \
            --test-filter "gemma4 prepare canonical flags normalize to the legacy adapter boundary" \
            --test-filter "gemma4 public prepare rejects media projection before command dispatch" \
            --test-filter "server generate forwards only explicit thinking mode" 2>&1 | tee "$RUNNER_TEMP/gemma4-cli.log"
          python3 ../../../scripts/ci/audit_gemma4_test_selection.py \
            --base "${{ inputs.base_sha || github.event.merge_group.base_sha || github.event.before }}" \
            "$RUNNER_TEMP/gemma4-focused.log" "$RUNNER_TEMP/gemma4-cli.log"

      - name: Run ML graph unit tests
        working-directory: zig/lib/ml
        run: zig build test -Doptimize=Debug --summary all

      - name: Run Gemma4 Metal ReleaseSafe gates
        working-directory: zig/pkg/inference
        run: |
          zig build test-gemma4-finetune -j1 --maxrss 10737418240 -Dmetal=true -Dcuda=false -Donnx=false -Dpjrt=false -Doptimize=ReleaseSafe -Dbenchmark-source-revision="$ANTFLY_BENCHMARK_SOURCE_REVISION" --summary all
          zig build test-gemma-graph -j1 -Dmetal=true -Dcuda=false -Donnx=false -Dpjrt=false -Doptimize=ReleaseSafe --summary all

      - name: Run packaged-optimization Metal lifecycle smoke
        working-directory: zig/pkg/inference
        run: |
          zig build test-gemma4-finetune -j1 --maxrss 10737418240 -Dmetal=true -Dcuda=false -Donnx=false -Dpjrt=false -Doptimize=ReleaseFast -Dbenchmark-source-revision="$ANTFLY_BENCHMARK_SOURCE_REVISION" --summary all

      - name: Build public CLI for PEFT interchange check
        working-directory: zig
        run: zig build antfly -j1 -Dmetal=true -Dcuda=false -Donnx=false -Dpjrt=false -Doptimize=ReleaseFast -Dbenchmark-source-revision="$ANTFLY_BENCHMARK_SOURCE_REVISION" --summary all

      - name: Load exported adapter with stock PEFT on CPU
        working-directory: zig/pkg/inference
        run: |
          set -euo pipefail
          python3 -m venv "$RUNNER_TEMP/gemma4-peft-venv"
          "$RUNNER_TEMP/gemma4-peft-venv/bin/python" -m pip install --disable-pip-version-check -r scripts/gemma4/requirements-gemma4-oracle.txt
          "$RUNNER_TEMP/gemma4-peft-venv/bin/python" scripts/gemma4/verify_gemma4_peft_export_roundtrip.py \
            --antfly ../../zig-out/bin/antfly \
            --work-dir "$RUNNER_TEMP/gemma4-peft-roundtrip"

  zig-full-tests:
    if: >-
      ${{ !cancelled() &&
      success() &&
      needs.admission.result == 'success' &&
      (inputs.pr_number == '' ||
      github.run_attempt == 1) &&
      (github.event_name == 'merge_group' ||
      (github.event_name == 'push' &&
      github.ref == 'refs/heads/main') ||
      ((inputs.pr_number == '' &&
      github.event_name == 'workflow_dispatch') &&
      inputs.validation_scope == 'all') ||
      github.event.schedule == '0 8 * * *') }}
    name: zig-full / x86_64
    needs: [admission, changes]
    runs-on: arc-antfly-heavy
    timeout-minutes: 120
    env:
      # Same 22 GiB build budget / 2 GiB runner headroom as zig-base-tests.
      ANTFLY_ZIG_MAX_RSS: "23622320128"
      ZIG_LOCAL_CACHE_DIR: /mnt/cache/antfly/zig/${{ github.job }}/${{ github.run_id }}-${{ github.run_attempt }}/zig-local
      ZIG_GLOBAL_CACHE_DIR: /mnt/cache/antfly/zig/zig-global
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.head_sha || github.sha }}
          persist-credentials: false
          submodules: true

      - name: Load pinned toolchain policy
        id: toolchain
        uses: ./.github/actions/load-toolchain-policy

      - name: Install build tools
        run: sudo apt-get update && sudo apt-get install -y build-essential pkg-config ca-certificates git curl xz-utils gdb

      - name: Install uv
        uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1

      - name: Set up Zig
        uses: mlugg/setup-zig@d1434d08867e3ee9daa34448df10607b98908d29 # v2
        with:
          version: ${{ steps.toolchain.outputs.zig_version }}
          use-cache: false

      - name: Restore ARC Zig cache dirs
        run: |
          local_cache="/mnt/cache/antfly/zig/${{ github.job }}/${{ github.run_id }}-${{ github.run_attempt }}/zig-local"
          global_cache="/mnt/cache/antfly/zig/zig-global"
          echo "ZIG_LOCAL_CACHE_DIR=$local_cache" >> "$GITHUB_ENV"
          echo "ZIG_GLOBAL_CACHE_DIR=$global_cache" >> "$GITHUB_ENV"
          mkdir -p "$local_cache" "$global_cache"

      - name: Verify Zig version
        run: zig version

      - name: Check generated sources
        run: make zig-generated-check

      # Keep inexpensive tooling checks beside the normal unit gate. The
      # isolated compilation/cache matrix runs in zig-full / build-cache.
      - name: Test build tooling
        working-directory: zig
        run: >-
          python3 -m unittest
          tools/test_patch_zig_0_16_build_runner_maxrss.py
          tools/test_run_bounded_zig_build.py
          tools/test_analyze_zig_import_graph.py
          tools/test_audit_storage_test_shards.py
          tools/test_audit_test_selection.py
          tools/test_check_storage_compilation.py
          tools/test_run_test_partitions.py
          tools/test_run_recall_checks.py
          tools/test_test_runner.py
          tools/test_sync_generated.py

      - name: Test benchmark orchestration and fixture setup
        run: |
          python3 -m unittest discover -s scripts -p test_run_db_query_matrix.py
          python3 -m unittest discover -s scripts -p test_conformance_fixtures.py
          python3 -m unittest discover -s scripts/bench/pdf -p 'test_*.py'

      # Keep normal build-step parallelism while bounding aggregate compiler
      # memory to the pod's cgroup budget and the 22 GiB largest-step claim.
      - name: Configure bounded parallel heavy tests
        id: cpus
        run: |
          jobs=$(nproc)
          if [ "$jobs" -gt 8 ]; then jobs=8; fi
          echo "list=0-$((jobs - 1))" >> "$GITHUB_OUTPUT"
          echo "max_rss=$(python3 zig/tools/run_bounded_zig_build.py --print-max-rss --max-rss-cap 23622320128)" >> "$GITHUB_OUTPUT"
          zig_lib_dir="$(zig env | sed -n 's/^[[:space:]]*\.lib_dir = "\(.*\)",$/\1/p')"
          patched_runner="${RUNNER_TEMP}/zig-build-runner-maxrss.zig"
          python3 zig/tools/patch_zig_0_16_build_runner_maxrss.py \
            "$zig_lib_dir/compiler/build_runner.zig" \
            "$patched_runner"
          echo "build_runner=$patched_runner" >> "$GITHUB_OUTPUT"

      - name: Run library, Antfly, and inference unit tests
        run: |
          set +e
          taskset -c ${{ steps.cpus.outputs.list }} make zig-unit-test ZIG_BUILD_FLAGS="--build-runner ${{ steps.cpus.outputs.build_runner }} --maxrss ${{ steps.cpus.outputs.max_rss }} --summary all"
          status="$?"
          if [ "$status" -ne 0 ] && [ -d /sys/fs/cgroup ]; then
            echo "::group::unit failure cgroup memory"
            for metric in memory.current memory.peak memory.max memory.events; do
              if [ -r "/sys/fs/cgroup/$metric" ]; then
                echo "[$metric]"
                cat "/sys/fs/cgroup/$metric"
              fi
            done
            echo "::endgroup::"
          fi
          exit "$status"

      - name: Run corpus-scale release regressions
        working-directory: zig
        run: >-
          taskset -c ${{ steps.cpus.outputs.list }}
          python3 tools/run_bounded_zig_build.py --zig zig -- build
          release-scale-test
          recall-harness
          --build-runner ${{ steps.cpus.outputs.build_runner }}
          --maxrss ${{ steps.cpus.outputs.max_rss }}

      - name: Set up Java for TLA+
        uses: actions/setup-java@dd06d9cba3e5552c54d9f8ea23572deb30010f7c # v6.0.0
        with:
          distribution: "temurin"
          java-version: "21"

      - name: Run TLA verification
        env:
          ANTFLY_TLA_MODEL_CHECK: "true"
          ANTFLY_TLA_TRACE_VALIDATE: "true"
        run: scripts/ci/zig-tla-verify.sh

      # Includes index-maintenance and index-ownership replay campaigns. The
      # fixed maintenance seed is already part of the zig-base unit aggregate.
      - name: Run Zig VOPR tests
        working-directory: zig
        run: >-
          taskset -c ${{ steps.cpus.outputs.list }}
          python3 tools/run_bounded_zig_build.py --zig zig -- build
          vopr-test
          --build-runner ${{ steps.cpus.outputs.build_runner }}
          --maxrss ${{ steps.cpus.outputs.max_rss }}

      # Includes complete graph owner coverage and the native maintenance fixture;
      # no graph-specific CLI or promotion matrix is built here.
      - name: Run Zig integration tests
        working-directory: zig
        run: >-
          taskset -c ${{ steps.cpus.outputs.list }}
          python3 tools/run_bounded_zig_build.py --zig zig -- build
          antfly-integration-test
          --build-runner ${{ steps.cpus.outputs.build_runner }}
          --maxrss ${{ steps.cpus.outputs.max_rss }}

      - name: Run Zig recall checks
        working-directory: zig
        run: >-
          taskset -c ${{ steps.cpus.outputs.list }}
          python3 tools/run_bounded_zig_build.py --zig zig -- build
          antfly-recall-test
          --build-runner ${{ steps.cpus.outputs.build_runner }}
          --maxrss ${{ steps.cpus.outputs.max_rss }}

      - name: Run Zig chaos tests
        working-directory: zig
        run: >-
          taskset -c ${{ steps.cpus.outputs.list }}
          python3 tools/run_bounded_zig_build.py --zig zig -- build
          antfly-chaos-test
          --build-runner ${{ steps.cpus.outputs.build_runner }}
          --maxrss ${{ steps.cpus.outputs.max_rss }}

  zig-build-cache-tests:
    if: >-
      ${{ !cancelled() &&
      success() &&
      needs.admission.result == 'success' &&
      (inputs.pr_number == '' ||
      github.run_attempt == 1) &&
      (github.event_name == 'merge_group' ||
      (github.event_name == 'push' &&
      github.ref == 'refs/heads/main') ||
      ((inputs.pr_number == '' &&
      github.event_name == 'workflow_dispatch') &&
      inputs.validation_scope == 'all') ||
      github.event.schedule == '0 8 * * *') }}
    name: zig-full / build-cache
    needs: [admission, changes]
    runs-on: arc-antfly-heavy
    timeout-minutes: 60
    env:
      ANTFLY_ZIG_MAX_RSS: "23622320128"
      ZIG_LOCAL_CACHE_DIR: /mnt/cache/antfly/zig/${{ github.job }}/${{ github.run_id }}-${{ github.run_attempt }}/zig-local
      ZIG_GLOBAL_CACHE_DIR: /mnt/cache/antfly/zig/zig-global
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.head_sha || github.sha }}
          persist-credentials: false
          submodules: true

      - name: Load pinned toolchain policy
        id: toolchain
        uses: ./.github/actions/load-toolchain-policy

      - name: Install build tools
        run: sudo apt-get update && sudo apt-get install -y build-essential pkg-config ca-certificates git curl xz-utils

      - name: Install uv
        uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1

      - name: Set up Zig
        uses: mlugg/setup-zig@d1434d08867e3ee9daa34448df10607b98908d29 # v2
        with:
          version: ${{ steps.toolchain.outputs.zig_version }}
          use-cache: false

      - name: Prepare ARC Zig cache dirs
        run: mkdir -p "$ZIG_LOCAL_CACHE_DIR" "$ZIG_GLOBAL_CACHE_DIR"

      # Each test owns its temporary source overlay and local cache. Keep
      # those isolated; only the normal global dependency cache is shared.
      # Verbose output identifies the individual checks in the CI log.
      - name: Test runtime compilation and cache contracts
        working-directory: zig
        run: python3 -u -m unittest -v tools/test_runtime_cache.py tools/test_linked_tests.py

      - name: Verify physical storage compilation ownership
        working-directory: zig
        run: python3 -u tools/check_storage_compilation.py --report storage-compilation.json

      - name: Upload storage compilation measurements
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: storage-compilation
          path: zig/storage-compilation.json
          if-no-files-found: ignore

  zig-base:
    if: >-
      ${{ !cancelled() &&
      needs.admission.result == 'success' &&
      (inputs.pr_number == '' ||
      github.run_attempt == 1) &&
      (always() &&
      (inputs.pr_number != '' ||
      github.event.schedule == '0 3 * * 0') &&
      ( needs.changes.result != 'success' ||
      needs.changes.outputs.zig_tests == 'true' ||
      needs.changes.outputs.model_check == 'true' ||
      needs.changes.outputs.trace_validate == 'true' )) }}
    name: zig-base
    needs: [admission, changes, zig-base-tests, arm64-codec, gemma4-metal-training-tests]
    runs-on: arc-antfly-standard
    steps:
      - name: Verify zig-base shards
        env:
          GEMMA4_REQUIRED: ${{ needs.changes.outputs.gemma4_training }}
          GEMMA4_RESULT: ${{ needs.gemma4-metal-training-tests.result }}
          CHANGES_RESULT: ${{ needs.changes.result }}
          X86_64_RESULT: ${{ needs.zig-base-tests.result }}
          ARM64_CODEC_RESULT: ${{ needs.arm64-codec.result }}
        run: |
          set -euo pipefail
          test "$CHANGES_RESULT" = "success"
          test "$X86_64_RESULT" = "success"
          test "$ARM64_CODEC_RESULT" = "success"

          if [ "$GEMMA4_REQUIRED" = "true" ]; then
            test "$GEMMA4_RESULT" = "success"
          fi

  zig-full:
    if: >-
      ${{ !cancelled() &&
      needs.admission.result == 'success' &&
      (inputs.pr_number == '' ||
      github.run_attempt == 1) &&
      (always() &&
      ( github.event_name == 'merge_group' ||
      (github.event_name == 'push' &&
      github.ref == 'refs/heads/main') ||
      ((inputs.pr_number == '' &&
      github.event_name == 'workflow_dispatch') &&
      inputs.validation_scope == 'all') ||
      github.event.schedule == '0 8 * * *' )) }}
    name: zig-full
    needs: [admission, changes, zig-full-tests, zig-build-cache-tests, arm64-codec, gemma4-metal-training-tests]
    runs-on: arc-antfly-standard
    steps:
      - name: Verify zig-full shards
        env:
          GEMMA4_REQUIRED: ${{ needs.changes.outputs.gemma4_training }}
          GEMMA4_RESULT: ${{ needs.gemma4-metal-training-tests.result }}
          CHANGES_RESULT: ${{ needs.changes.result }}
          X86_64_RESULT: ${{ needs.zig-full-tests.result }}
          BUILD_CACHE_RESULT: ${{ needs.zig-build-cache-tests.result }}
          ARM64_CODEC_RESULT: ${{ needs.arm64-codec.result }}
        run: |
          set -euo pipefail
          test "$CHANGES_RESULT" = "success"
          test "$X86_64_RESULT" = "success"
          test "$BUILD_CACHE_RESULT" = "success"
          test "$ARM64_CODEC_RESULT" = "success"

          if [ "$GEMMA4_REQUIRED" = "true" ]; then
            test "$GEMMA4_RESULT" = "success"
          fi

  e2e-base-build:
    if: >-
      ${{ !cancelled() &&
      success() &&
      needs.admission.result == 'success' &&
      (inputs.pr_number == '' ||
      github.run_attempt == 1) &&
      (inputs.pr_number != '' &&
      needs.changes.outputs.e2e == 'true') }}
    name: e2e-base / build
    needs: [admission, changes]
    runs-on: arc-antfly-heavy
    timeout-minutes: 90
    env:
      # Override ARC's 20 GiB default: storage reserves 22 GiB. The 24 GiB
      # pod still has 2 GiB outside the aggregate compiler reservation.
      ANTFLY_ZIG_MAX_RSS: "23622320128"
      ZIG_LOCAL_CACHE_DIR: /mnt/cache/antfly/zig/${{ github.job }}/${{ github.run_id }}-${{ github.run_attempt }}/zig-local
      ZIG_GLOBAL_CACHE_DIR: /mnt/cache/antfly/zig/zig-global
      ANTFLY_CI_TRACE: "1"
      # The PR E2E artifact is also the release-packaging smoke artifact. Build
      # the partitioned graph once, test that exact binary, and reuse its
      # distributed PIC archive for both public C API packaging shapes.
      ANTFLY_CI_ZIG_OPTIMIZE: ReleaseFast
      ANTFLY_CI_ZIG_STRIP: "true"
      ANTFLY_CI_BUILD_CAPI: "true"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.head_sha || github.sha }}
          persist-credentials: false
          submodules: true

      - name: Load pinned toolchain policy
        id: toolchain
        uses: ./.github/actions/load-toolchain-policy

      - name: Install build tools
        run: sudo apt-get update && sudo apt-get install -y build-essential pkg-config ca-certificates git curl xz-utils

      - name: Set up Zig
        uses: mlugg/setup-zig@d1434d08867e3ee9daa34448df10607b98908d29 # v2
        with:
          version: ${{ steps.toolchain.outputs.zig_version }}
          use-cache: false

      - name: Restore ARC Zig cache dirs
        run: |
          local_cache="/mnt/cache/antfly/zig/${{ github.job }}/${{ github.run_id }}-${{ github.run_attempt }}/zig-local"
          global_cache="/mnt/cache/antfly/zig/zig-global"
          echo "ZIG_LOCAL_CACHE_DIR=$local_cache" >> "$GITHUB_ENV"
          echo "ZIG_GLOBAL_CACHE_DIR=$global_cache" >> "$GITHUB_ENV"
          mkdir -p "$local_cache" "$global_cache"

      - name: Build E2E executable once
        run: scripts/ci/zig-build-e2e-binaries.sh

      - name: Package E2E executable
        run: tar -C zig -czf "${RUNNER_TEMP}/antfly-e2e-base.tar.gz" zig-out/bin/antfly

      - name: Upload E2E executable
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: antfly-e2e-base-${{ github.run_id }}
          path: ${{ runner.temp }}/antfly-e2e-base.tar.gz
          compression-level: 0
          overwrite: true
          retention-days: 1

  e2e-base-tests:
    if: >-
      ${{ !cancelled() &&
      needs.admission.result == 'success' &&
      (inputs.pr_number == '' ||
      github.run_attempt == 1) &&
      (always() &&
      inputs.pr_number != '' &&
      needs.changes.outputs.e2e == 'true' &&
      needs.e2e-base-build.result == 'success') }}
    name: e2e-base / ${{ matrix.suite }}
    needs: [admission, changes, e2e-base-build]
    runs-on: arc-antfly-heavy
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        suite: [antfly, inference]
    env:
      ZIG_LOCAL_CACHE_DIR: /mnt/cache/antfly/zig/${{ github.job }}/${{ matrix.suite }}/${{ github.run_id }}-${{ github.run_attempt }}/zig-local
      ZIG_GLOBAL_CACHE_DIR: /mnt/cache/antfly/zig/zig-global
      UV_CACHE_DIR: /mnt/cache/antfly/zig/uv
      HF_HOME: /mnt/cache/antfly/zig/hf
      ANTFLY_E2E_DURATION_FILE: /mnt/cache/antfly/zig/e2e-durations/antfly-v1.json
      ANTFLY_CI_TRACE: "1"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.head_sha || github.sha }}
          persist-credentials: false
          submodules: true

      - name: Load pinned toolchain policy
        id: toolchain
        uses: ./.github/actions/load-toolchain-policy

      - name: Install build tools
        run: sudo apt-get update && sudo apt-get install -y build-essential pkg-config ca-certificates git curl xz-utils gdb

      - name: Set up Zig
        uses: mlugg/setup-zig@d1434d08867e3ee9daa34448df10607b98908d29 # v2
        with:
          version: ${{ steps.toolchain.outputs.zig_version }}
          use-cache: false

      - name: Restore ARC cache dirs
        run: |
          local_cache="/mnt/cache/antfly/zig/${{ github.job }}/${{ matrix.suite }}/${{ github.run_id }}-${{ github.run_attempt }}/zig-local"
          global_cache="/mnt/cache/antfly/zig/zig-global"
          test_tmp="/mnt/cache/antfly/zig/e2e-tmp/${{ github.job }}/${{ matrix.suite }}/${{ github.run_id }}-${{ github.run_attempt }}"
          {
            echo "ZIG_LOCAL_CACHE_DIR=$local_cache"
            echo "ZIG_GLOBAL_CACHE_DIR=$global_cache"
            echo "TMPDIR=$test_tmp"
          } >> "$GITHUB_ENV"
          mkdir -p "$local_cache" "$global_cache" "$test_tmp" "$UV_CACHE_DIR" "$HF_HOME" "$(dirname "$ANTFLY_E2E_DURATION_FILE")"
          mkdir -p "/mnt/cache/antfly/zig/home-cache-${{ matrix.suite }}" "/mnt/cache/antfly/zig/home-antfly-${{ matrix.suite }}"
          rm -rf ~/.cache ~/.antfly
          ln -s "/mnt/cache/antfly/zig/home-cache-${{ matrix.suite }}" ~/.cache
          ln -s "/mnt/cache/antfly/zig/home-antfly-${{ matrix.suite }}" ~/.antfly

      - name: Install uv
        uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1

      - name: Compute CPU bound for E2E
        id: e2e_cpus
        run: |
          jobs=$(nproc)
          if [ "$jobs" -gt 8 ]; then jobs=8; fi
          echo "list=0-$((jobs - 1))" >> "$GITHUB_OUTPUT"

      - name: Download E2E executable
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: antfly-e2e-base-${{ github.run_id }}
          path: ${{ runner.temp }}/antfly-e2e-base

      - name: Install E2E executable
        run: tar -xzf "${RUNNER_TEMP}/antfly-e2e-base/antfly-e2e-base.tar.gz" -C zig

      - name: Set up Go for operator runtime contract
        if: matrix.suite == 'inference'
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: ${{ steps.toolchain.outputs.go_version }}
          cache: false

      # Execute the SAME reconciler-generated contract as the operator job's
      # pinned v0.2.1 baseline, now against this PR's newly built runtime.
      - name: Test generated inference command against candidate runtime
        if: matrix.suite == 'inference'
        working-directory: go/pkg/operator
        env:
          ANTFLY_RUNTIME_BIN: ${{ github.workspace }}/zig/zig-out/bin/antfly
          GOWORK: 'off'
          GOEXPERIMENT: simd
        run: >-
          taskset -c ${{ steps.e2e_cpus.outputs.list }} go test
          -tags runtimeintegration ./controllers/inference
          -run '^TestInferenceRuntimeContract$' -count=1 -v -timeout=20m

      - name: Run base E2E
        env:
          SKIP_BUILD: "1"
          ANTFLY_E2E_SUITE: ${{ matrix.suite }}
        run: taskset -c ${{ steps.e2e_cpus.outputs.list }} scripts/ci/zig-e2e-base-linux.sh

      - name: Run inference config contract with a real CPU model
        if: matrix.suite == 'inference'
        working-directory: zig
        env:
          ANTFLY_BIN: ./zig-out/bin/antfly
          ANTFLY_INFERENCE_MODELS_DIR: ${{ runner.temp }}/inference-config-models
          ANTFLY_INFERENCE_DOWNLOAD: "1"
          UV_PROJECT_ENVIRONMENT: /tmp/antfly-ci-venv/inference
        run: >-
          taskset -c ${{ steps.e2e_cpus.outputs.list }} uv run --project e2e/inference pytest -q
          e2e/inference/test_run_config.py

      - name: Run low-FD HTTP worker ratchet regression
        if: matrix.suite == 'antfly'
        working-directory: zig
        env:
          ANTFLY_REQUIRE_LOW_FD_HTTP_RATCHET: "1"
        run: |
          ulimit -n 256
          taskset -c ${{ steps.e2e_cpus.outputs.list }} zig build http-low-fd-ratchet-test

      - name: Run low-FD Autograph regression
        if: matrix.suite == 'antfly'
        env:
          SKIP_BUILD: "1"
          ANTFLY_E2E_NOFILE_LIMIT: "256"
          ANTFLY_E2E_REGRESSION_REPEATS: "3"
        run: >-
          taskset -c ${{ steps.e2e_cpus.outputs.list }}
          scripts/ci/zig-e2e-regression-loop.sh
          e2e/antfly/test_resolution.py::test_multinode_autograph_resolves_promotes_and_hydrates_entities

  e2e-base:
    if: >-
      ${{ !cancelled() &&
      needs.admission.result == 'success' &&
      (inputs.pr_number == '' ||
      github.run_attempt == 1) &&
      (always() &&
      inputs.pr_number != '' &&
      needs.changes.outputs.e2e == 'true') }}
    name: e2e-base
    needs: [admission, changes, e2e-base-build, e2e-base-tests]
    runs-on: arc-antfly-standard
    steps:
      - name: Verify base E2E shards
        env:
          BUILD_RESULT: ${{ needs.e2e-base-build.result }}
          TEST_RESULT: ${{ needs.e2e-base-tests.result }}
        run: |
          set -euo pipefail
          test "$BUILD_RESULT" = "success"
          test "$TEST_RESULT" = "success"

  e2e-full-build:
    if: >-
      ${{ !cancelled() &&
      success() &&
      needs.admission.result == 'success' &&
      (inputs.pr_number == '' ||
      github.run_attempt == 1) &&
      (github.event_name == 'merge_group' ||
      (github.event_name == 'push' &&
      github.ref == 'refs/heads/main') ||
      (inputs.pr_number == '' &&
      github.event_name == 'workflow_dispatch') ||
      github.event.schedule == '0 8 * * *') }}
    name: e2e-full / build
    needs: [admission, changes]
    runs-on: arc-antfly-heavy
    timeout-minutes: 90
    env:
      ANTFLY_ZIG_MAX_RSS: "23622320128"
      ZIG_LOCAL_CACHE_DIR: /mnt/cache/antfly/zig/${{ github.job }}/${{ github.run_id }}-${{ github.run_attempt }}/zig-local
      ZIG_GLOBAL_CACHE_DIR: /mnt/cache/antfly/zig/zig-global
      # Keep production optimization while preserving runtime-safety checks and
      # std.debug.assert invariants exercised by the full E2E suite.
      ANTFLY_CI_ZIG_OPTIMIZE: ReleaseSafe
      # Match release artifact symbol handling without disabling ReleaseSafe
      # checks or std.debug.assert invariants.
      ANTFLY_CI_ZIG_STRIP: "true"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.head_sha || github.sha }}
          persist-credentials: false
          submodules: true

      - name: Load pinned toolchain policy
        id: toolchain
        uses: ./.github/actions/load-toolchain-policy

      - name: Install build tools
        run: sudo apt-get update && sudo apt-get install -y build-essential pkg-config ca-certificates git curl xz-utils gdb

      - name: Set up Zig
        uses: mlugg/setup-zig@d1434d08867e3ee9daa34448df10607b98908d29 # v2
        with:
          version: ${{ steps.toolchain.outputs.zig_version }}
          use-cache: false

      - name: Restore ARC cache dirs
        run: |
          local_cache="/mnt/cache/antfly/zig/${{ github.job }}/${{ github.run_id }}-${{ github.run_attempt }}/zig-local"
          global_cache="/mnt/cache/antfly/zig/zig-global"
          echo "ZIG_LOCAL_CACHE_DIR=$local_cache" >> "$GITHUB_ENV"
          echo "ZIG_GLOBAL_CACHE_DIR=$global_cache" >> "$GITHUB_ENV"
          mkdir -p "$local_cache" "$global_cache"

      - name: Build E2E executable once
        run: scripts/ci/zig-build-e2e-binaries.sh

      - name: Package E2E executable
        run: tar -C zig -czf "${RUNNER_TEMP}/antfly-e2e-full.tar.gz" zig-out/bin/antfly

      - name: Upload E2E executable
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: antfly-e2e-full-${{ github.run_id }}
          path: ${{ runner.temp }}/antfly-e2e-full.tar.gz
          compression-level: 0
          overwrite: true
          retention-days: 1

  e2e-full-tests:
    if: >-
      ${{ !cancelled() &&
      needs.admission.result == 'success' &&
      (inputs.pr_number == '' ||
      github.run_attempt == 1) &&
      (always() &&
      needs.e2e-full-build.result == 'success' &&
      (github.event_name == 'merge_group' ||
      (github.event_name == 'push' &&
      github.ref == 'refs/heads/main') ||
      (inputs.pr_number == '' &&
      github.event_name == 'workflow_dispatch') ||
      github.event.schedule == '0 8 * * *')) }}
    name: e2e-full / ${{ matrix.suite }}
    needs: [admission, changes, e2e-full-build]
    runs-on: arc-antfly-heavy
    timeout-minutes: 150
    strategy:
      fail-fast: false
      matrix:
        suite: ${{ fromJSON((inputs.pr_number == '' && github.event_name == 'workflow_dispatch') && inputs.validation_scope == 'inference-e2e' && '["inference"]' || '["inference","antfly"]') }}
    env:
      UV_CACHE_DIR: /mnt/cache/antfly/zig/uv
      HF_HOME: /mnt/cache/antfly/zig/hf
      ANTFLY_E2E_DURATION_FILE: /mnt/cache/antfly/zig/e2e-durations/antfly-v1.json
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.head_sha || github.sha }}
          persist-credentials: false
          submodules: true

      - name: Load pinned toolchain policy
        id: toolchain
        uses: ./.github/actions/load-toolchain-policy

      - name: Install build tools
        run: sudo apt-get update && sudo apt-get install -y build-essential pkg-config ca-certificates git curl xz-utils gdb

      - name: Prepare ARC test cache dirs
        run: |
          test_tmp="/mnt/cache/antfly/zig/e2e-tmp/${{ github.job }}/${{ matrix.suite }}/${{ github.run_id }}-${{ github.run_attempt }}"
          echo "TMPDIR=$test_tmp" >> "$GITHUB_ENV"
          mkdir -p "$test_tmp" "$UV_CACHE_DIR" "$HF_HOME" "$(dirname "$ANTFLY_E2E_DURATION_FILE")"
          mkdir -p "/mnt/cache/antfly/zig/home-cache-full-${{ matrix.suite }}" "/mnt/cache/antfly/zig/home-antfly-full-${{ matrix.suite }}"
          rm -rf ~/.cache ~/.antfly
          ln -s "/mnt/cache/antfly/zig/home-cache-full-${{ matrix.suite }}" ~/.cache
          ln -s "/mnt/cache/antfly/zig/home-antfly-full-${{ matrix.suite }}" ~/.antfly

      - name: Install uv
        uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1

      - name: Compute CPU bound for E2E
        id: e2e_cpus
        run: |
          jobs=$(nproc)
          if [ "$jobs" -gt 8 ]; then jobs=8; fi
          echo "list=0-$((jobs - 1))" >> "$GITHUB_OUTPUT"

      - name: Set up Rust WASI target
        if: matrix.suite == 'antfly'
        uses: dtolnay/rust-toolchain@4360b52568e2003a75bf9bc1d59f33a8e3fc893c # stable
        with:
          toolchain: ${{ steps.toolchain.outputs.rust_version }}
          targets: wasm32-wasip2

      - name: Build MemoryAF extension package
        if: matrix.suite == 'antfly'
        run: cargo build --manifest-path extensions/memoryaf/Cargo.toml --release --target wasm32-wasip2

      - name: Download E2E executable
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: antfly-e2e-full-${{ github.run_id }}
          path: ${{ runner.temp }}/antfly-e2e-full

      - name: Install E2E executable
        run: tar -xzf "${RUNNER_TEMP}/antfly-e2e-full/antfly-e2e-full.tar.gz" -C zig

      - name: Report inference memory baseline
        if: matrix.suite == 'inference'
        run: |
          echo "memory.current=$(cat /sys/fs/cgroup/memory.current 2>/dev/null || echo unavailable)"
          echo "memory.max=$(cat /sys/fs/cgroup/memory.max 2>/dev/null || echo unavailable)"
          if [ -r /sys/fs/cgroup/memory.stat ]; then
            awk '$1 == "anon" || $1 == "file" || $1 == "inactive_file" { print "memory.stat." $1 "=" $2 }' /sys/fs/cgroup/memory.stat
          fi

      - name: Install Wasmtime C API
        if: matrix.suite == 'antfly'
        env:
          WASMTIME_VERSION: v45.0.2
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) arch=x86_64 ;;
            aarch64|arm64) arch=aarch64 ;;
            *) echo "unsupported architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          archive="wasmtime-${WASMTIME_VERSION}-${arch}-linux-c-api.tar.xz"
          root="/tmp/wasmtime-c-api"
          rm -rf "$root"
          mkdir -p "$root"
          curl -L "https://github.com/bytecodealliance/wasmtime/releases/download/${WASMTIME_VERSION}/${archive}" -o "/tmp/${archive}"
          tar -xJf "/tmp/${archive}" -C "$root" --strip-components=1
          lib="$(find "$root/lib" -maxdepth 1 -name 'libwasmtime.so*' | sort | head -n 1)"
          test -n "$lib"
          echo "ANTFLY_WASMTIME_LIB=$lib" >> "$GITHUB_ENV"
          echo "LD_LIBRARY_PATH=$root/lib:${LD_LIBRARY_PATH:-}" >> "$GITHUB_ENV"

      - name: Run Antfly inference full E2E
        if: matrix.suite == 'inference'
        working-directory: zig
        env:
          ANTFLY_BIN: ./zig-out/bin/antfly
          ANTFLY_INFERENCE_DOWNLOAD: "1"
          # Gemma 4 covers ordinary, tool-calling, and multimodal generation;
          # keep one decoder artifact across every generation endpoint.
          ANTFLY_INFERENCE_DEFAULT_GENERATOR_MODEL: ggml-org/gemma-4-e2b-it-gguf
          ANTFLY_INFERENCE_FLORENCE_MODEL: antflydb/florence-2-base
          ANTFLY_INFERENCE_REQUEST_TIMEOUT: "300"
          # Cold native model and projector initialization can exceed the
          # steady request deadline on CPU-only release runners. Only tests
          # explicitly covering first-use paths opt into this hard bound.
          ANTFLY_INFERENCE_FIRST_USE_REQUEST_TIMEOUT: "1800"
          ANTFLY_INFERENCE_MAX_LOADED_MODELS: "1"
          # This Burstable pod has a 15000Mi request but no finite memory.max.
          # Bound leaf-cgroup working set at 14000Mi so admission retains its
          # emergency reserve and leaves another 1000Mi of pod-request margin.
          ANTFLY_INFERENCE_PROCESS_MEMORY_BUDGET_MB: "14000"
          # Bound the native hot set while allowing the measured 11173MiB 2B
          # decoder working set plus its scoped projector to complete. Keep
          # 2000MiB inside the process envelope for harness and process overhead.
          ANTFLY_INFERENCE_HOST_BUDGET_MB: "12000"
          # This is a release contract, not an optional capability probe. Keep
          # it targeted so unrelated large-model suites do not inflate this job.
          RUN_MULTIMODAL_GENERATOR_TESTS: "1"
          RUN_CLIPCLAP_CONTRACT_TESTS: "1"
        run: |
          chmod +x ./zig-out/bin/antfly
          taskset -c ${{ steps.e2e_cpus.outputs.list }} uv run --project e2e/inference pytest -q --continue-on-collection-errors e2e/inference

      - name: Validate standalone inference resource composition
        if: matrix.suite == 'inference'
        working-directory: zig
        env:
          ANTFLY_BIN: ./zig-out/bin/antfly
          ANTFLY_INFERENCE_STANDALONE_TESTS: "1"
          ANTFLY_INFERENCE_STANDALONE_MODELS_DIR: ~/.antfly/inference/models
          ANTFLY_INFERENCE_STANDALONE_MODEL_NAME: ggml-org/gemma-4-e2b-it-gguf
          # Match the cold-start contract used by the full inference suite.
          # Standalone exercises the same CPU-only model initialization path.
          ANTFLY_INFERENCE_FIRST_USE_REQUEST_TIMEOUT: "1800"
          ANTFLY_INFERENCE_STANDALONE_PROCESS_MEMORY_BUDGET_MB: "14000"
          ANTFLY_INFERENCE_STANDALONE_HOST_BUDGET_MB: "12000"
          ANTFLY_INFERENCE_STANDALONE_COMBINED_BUDGET_MB: "12000"
        run: |
          taskset -c ${{ steps.e2e_cpus.outputs.list }} uv run --project e2e/antfly pytest -q \
            e2e/antfly/test_standalone.py::test_standalone_inference_process_envelope_composition

      - name: Run Antfly full E2E
        if: matrix.suite == 'antfly'
        working-directory: zig
        env:
          ANTFLY_BIN: ./zig-out/bin/antfly
        run: |
          chmod +x ./zig-out/bin/antfly
          taskset -c ${{ steps.e2e_cpus.outputs.list }} ../scripts/ci/zig-antfly-e2e-pytest.sh e2e/antfly

  e2e-full:
    if: >-
      ${{ !cancelled() &&
      needs.admission.result == 'success' &&
      (inputs.pr_number == '' ||
      github.run_attempt == 1) &&
      (always() &&
      (github.event_name == 'merge_group' ||
      (github.event_name == 'push' &&
      github.ref == 'refs/heads/main') ||
      (inputs.pr_number == '' &&
      github.event_name == 'workflow_dispatch') ||
      github.event.schedule == '0 8 * * *')) }}
    name: e2e-full
    needs: [admission, changes, e2e-full-build, e2e-full-tests]
    runs-on: arc-antfly-standard
    steps:
      - name: Verify full E2E suites
        env:
          BUILD_RESULT: ${{ needs.e2e-full-build.result }}
          TEST_RESULT: ${{ needs.e2e-full-tests.result }}
        run: |
          test "$BUILD_RESULT" = "success"
          test "$TEST_RESULT" = "success"
