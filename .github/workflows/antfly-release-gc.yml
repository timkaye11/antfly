name: Release object retention

# Scheduled runs produce an auditable plan. Applying a plan is an explicit,
# protected operation until real production plans have established confidence
# in the retention policy.
on:
  schedule:
    - cron: "17 8 * * 1"
  workflow_dispatch:
    inputs:
      apply:
        description: "Delete only when a fresh plan exactly matches the reviewed plan"
        required: true
        default: false
        type: boolean

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  plan:
    name: Plan release object retention
    runs-on: arc-antfly-standard
    permissions:
      actions: read
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: ./.github/actions/setup-github-cli
      - name: Load pinned toolchain policy
        id: toolchain
        uses: ./.github/actions/load-toolchain-policy
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: ${{ steps.toolchain.outputs.python_build }}
      - name: Require the default-branch retention controller
        env:
          DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}
        run: test "$GITHUB_REF" = "refs/heads/$DEFAULT_BRANCH"
      - name: Verify the protected release environment contract
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: >-
          python scripts/release/github_environment.py check
          --repository "$GITHUB_REPOSITORY"
      - name: Install hash-locked object storage client
        run: |
          python -m pip install --require-hashes -r scripts/release/requirements.lock
          python -m pip check
      - name: Compute the journal-aware retention plan
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_ENDPOINT_URL: ${{ secrets.AWS_ENDPOINT_URL }}
        run: |
          python scripts/release/release_gc.py \
            --endpoint "$AWS_ENDPOINT_URL" \
            --plan-out dist/release-gc/plan.json
          approval_sha256="$(python -c "import json,sys; print(json.load(open(sys.argv[1]))['approval_sha256'])" dist/release-gc/plan.json)"
          {
            echo "### Release GC approval contract"
            echo
            echo "\`${approval_sha256}\`"
            echo
            echo "Review the uploaded \`release-gc-plan\` artifact before approval."
          } >> "$GITHUB_STEP_SUMMARY"
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-gc-plan
          path: dist/release-gc/plan.json
          if-no-files-found: error

  approve:
    name: Approve release object deletion
    if: ${{ github.event_name == 'workflow_dispatch' && inputs.apply }}
    needs: plan
    runs-on: ubuntu-latest
    environment: release-promotion
    steps:
      - run: true

  apply:
    name: Apply the exact approved release retention plan
    needs: approve
    runs-on: arc-antfly-standard
    concurrency:
      group: antfly-release-storage
      cancel-in-progress: false
    permissions:
      contents: read
      id-token: write
      packages: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: ./.github/actions/setup-github-cli
      - name: Load pinned toolchain policy
        id: toolchain
        uses: ./.github/actions/load-toolchain-policy
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: ${{ steps.toolchain.outputs.python_build }}
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-gc-plan
          path: dist/release-gc/approved
      - name: Install hash-locked object storage client
        run: |
          python -m pip install --require-hashes -r scripts/release/requirements.lock
          python -m pip check
      - name: Authenticate to Google Cloud for container retention
        id: auth
        uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093 # v3
        with:
          workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}
          token_format: access_token
      - uses: google-github-actions/setup-gcloud@aa5489c8933f4cc7a4f7d45035b3b1440c9c10db # v3
      - name: Install crane
        uses: imjasonh/setup-crane@feee3b6bb0d4c68370f256a4502498c9227e5c6b # v0.7
      - name: Authenticate container registries
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "${{ steps.auth.outputs.access_token }}" | crane auth login us-central1-docker.pkg.dev -u oauth2accesstoken --password-stdin
          echo "$GH_TOKEN" | crane auth login ghcr.io -u "${{ github.actor }}" --password-stdin
      - name: Recompute and match the approved retention plan inside the release lock
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_ENDPOINT_URL: ${{ secrets.AWS_ENDPOINT_URL }}
        run: |
          python scripts/release/release_gc.py \
            --endpoint "$AWS_ENDPOINT_URL" \
            --approved-plan dist/release-gc/approved/plan.json \
            --plan-out dist/release-gc/apply-plan.json
      - name: Delete expired container images
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          python scripts/release/release_registry_gc.py \
            --plan dist/release-gc/apply-plan.json \
            --gar-image us-central1-docker.pkg.dev/antfly-image-artifacts/containers/antfly \
            --ghcr-image antflydb/antfly \
            --apply
      - name: Delete expired R2 releases after container cleanup
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_ENDPOINT_URL: ${{ secrets.AWS_ENDPOINT_URL }}
        run: |
          python scripts/release/release_gc.py \
            --endpoint "$AWS_ENDPOINT_URL" \
            --apply-plan dist/release-gc/apply-plan.json \
            --containers-cleaned \
            --plan-out dist/release-gc/applied-plan.json
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-gc-applied-plan
          path: dist/release-gc/applied-plan.json
          if-no-files-found: error
