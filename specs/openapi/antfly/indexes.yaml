openapi: 3.0.3
info:
  title: Index Configuration
  description: "An OpenAPI specification for configuring different indexes in AntflyDB."
  version: "0.0.1"
components:
  schemas:
    SortDirection:
      $ref: "sort.yaml#/components/schemas/SortDirection"
    SortField:
      $ref: "sort.yaml#/components/schemas/SortField"
    IndexType:
      # FIXME (ajr) Looks like a bug in oapi-codegen where it doesn't respect
      # the x-go-type-skip-optional-pointer when the schema is referenced from
      # another schema in the MarshalJSON.
      #
      # x-go-type-skip-optional-pointer: true
      type: string
      enum:
        - full_text
        - embeddings
        - graph
        - algebraic
      description: "The type of the index."
    IndexConfig:
      type: object
      description: "Configuration for an index"
      oneOf:
        - $ref: "#/components/schemas/FullTextIndexConfig"
        - $ref: "#/components/schemas/EmbeddingsIndexConfig"
        - $ref: "#/components/schemas/GraphIndexConfig"
        - $ref: "#/components/schemas/AlgebraicIndexConfig"
      required:
        - name
        - type
      properties:
        name:
          type: string
          description: "Name of the index"
        description:
          type: string
          description: "Optional description of the index and its purpose"
        type:
          $ref: "#/components/schemas/IndexType"
        version:
          x-go-type-skip-optional-pointer: true
          type: integer
          default: 0
          description: "Version of the index implementation. Defaults to 0."
        enrichments:
          type: array
          items:
            $ref: "#/components/schemas/EnrichmentConfig"
          x-go-type-skip-optional-pointer: true
          description: "Inline managed enrichment definitions required by this index. Enrichments are table-level generated artifacts such as chunks, asset-derived document units, or embeddings over an artifact stream."
          example:
            - name: document_chunks_v1
              kind: chunk
              field: text
              source_artifact_name: document_units_v1
              chunk_size: 512
            - name: document_chunk_dense_v1
              kind: embedding
              field: text
              source_artifact_name: document_chunks_v1
              expected_dims: 768
    CreateIndexCommon:
      type: object
      description: "Fields shared by every create-index variant. The index name is owned by the request path."
      properties:
        description:
          type: string
          description: "Optional description of the index and its purpose"
        version:
          x-go-type-skip-optional-pointer: true
          type: integer
          default: 0
          description: "Version of the index implementation. Defaults to 0."
        enrichments:
          type: array
          items:
            $ref: "#/components/schemas/EnrichmentConfig"
          x-go-type-skip-optional-pointer: true
          description: "Inline managed enrichment definitions required by this index."
    ArtifactIndexSource:
      type: object
      additionalProperties: false
      required: [artifact]
      description: "Named generated artifact stream consumed by an index. Producer inputs belong on the matching enrichment."
      properties:
        artifact:
          type: string
          minLength: 1
          description: "Stable name of a generated artifact stream."
    FullTextArtifactIndexSource:
      type: object
      additionalProperties: false
      required: [artifact]
      description: "Textual artifact stream consumed by a full-text index, with an optional source-local projection."
      properties:
        artifact:
          type: string
          minLength: 1
          description: "Stable name of a chunk or textual asset artifact stream. Artifact names must be unique within the index."
        field:
          type: string
          minLength: 1
          description: "Optional field selected from this artifact's records. When omitted, the index-level field is inherited; when both are omitted, Antfly indexes the default text projection."
    CreateFullTextIndexRequest:
      description: "Create a full-text index."
      allOf:
        - $ref: "#/components/schemas/CreateIndexCommon"
        - $ref: "#/components/schemas/FullTextIndexConfig"
        - type: object
          required: [type]
          properties:
            type:
              type: string
              enum: [full_text]
    CreateEmbeddingsIndexRequest:
      description: "Create a dense or sparse embeddings index."
      allOf:
        - $ref: "#/components/schemas/CreateIndexCommon"
        - $ref: "#/components/schemas/EmbeddingsIndexConfig"
        - type: object
          required: [type]
          properties:
            type:
              type: string
              enum: [embeddings]
    CreateGraphIndexRequest:
      description: "Create a graph index."
      allOf:
        - $ref: "#/components/schemas/CreateIndexCommon"
        - $ref: "#/components/schemas/GraphIndexConfig"
        - type: object
          required: [type]
          properties:
            type:
              type: string
              enum: [graph]
    GraphArtifactSourceConfig:
      type: object
      additionalProperties: false
      required: [artifact]
      description: "Artifact stream materialized into graph edges. Each source artifact is limited to 16 MiB and 1,000,000 relation items so live apply, repair, split, and restore share one bounded admission contract. Artifact-backed graph sources require index_capabilities.artifact_sources=true and are rejected by serverless deployments."
      properties:
        artifact: { type: string, minLength: 1 }
        path:
          type: string
          pattern: '^(\$|\$\.[A-Za-z0-9_]+(\.[A-Za-z0-9_]+)*(\[\*\])?)?$'
          description: "Optional root path selecting the graph payload. Supports `$`, dot-separated ASCII field names such as `$.relations`, and an optional terminal `[*]` such as `$.relations[*]`."
        format:
          type: string
          enum: [extraction_relation, extraction_graph]
          default: extraction_relation
        mention_edge_type:
          $ref: '#/components/schemas/GraphEdgeType'
        nodes: { $ref: "#/components/schemas/GraphArtifactNodeMappingConfig" }
        edge: { $ref: "#/components/schemas/GraphArtifactEdgeMappingConfig" }
        context: { $ref: "#/components/schemas/GraphArtifactContextConfig" }
    GraphArtifactProducerSourceConfig:
      type: object
      additionalProperties: false
      required: [type, value]
      description: "Document input used by an artifact producer. Field sources read one document field; template sources render a Handlebars template."
      properties:
        type:
          type: string
          enum: [field, template]
        value:
          type: string
          minLength: 1
    GraphArtifactProducerConfig:
      type: object
      additionalProperties: false
      required: [name, kind, source]
      description: "Asset producer used by an artifact-backed graph index."
      properties:
        name: { type: string, minLength: 1 }
        kind:
          type: string
          enum: [asset]
        source: { $ref: "#/components/schemas/GraphArtifactProducerSourceConfig" }
        content_type: { type: string }
        execution: { $ref: "#/components/schemas/ExecutionPolicy" }
        producer_json:
          type: object
          additionalProperties: true
          writeOnly: true
          description: "Write-only producer configuration; it may contain credentials and is never returned."
    GraphTemplateValue:
      description: "A literal string or finite numeric value, or a Handlebars template evaluated for each materialized graph item."
      anyOf:
        - type: string
        - type: number
          format: double
    GraphArtifactNodeMappingConfig:
      type: object
      additionalProperties: false
      description: "Maps each artifact item to graph node identifiers."
      properties:
        model:
          type: string
          enum: [document, external]
          default: document
        target: { $ref: "#/components/schemas/GraphTemplateValue" }
    GraphArtifactEdgeMappingConfig:
      type: object
      additionalProperties: false
      description: "Maps each artifact item to an edge type, weight, and public metadata."
      properties:
        type: { $ref: "#/components/schemas/GraphTemplateValue" }
        weight: { $ref: "#/components/schemas/GraphTemplateValue" }
        metadata:
          type: object
          additionalProperties: true
          description: "JSON metadata template copied onto each materialized edge. Sensitive keys are omitted from create responses."
    GraphArtifactContextConfig:
      type: object
      additionalProperties: false
      description: "Document fields made available to graph mapping templates through `_doc.value`."
      properties:
        doc_fields:
          type: array
          items: { type: string, minLength: 1 }
    GraphBoundedTraversalConfig:
      type: object
      additionalProperties: false
      required: [law]
      description: "Algebraic law used to combine bounded graph traversal provenance."
      properties:
        law:
          type: string
          enum: [provenance_semiring]
    GraphAlgebraicPlanningConfig:
      type: object
      additionalProperties: false
      description: "Optional algebraic planning features for graph traversal."
      properties:
        bounded_traversal:
          $ref: "#/components/schemas/GraphBoundedTraversalConfig"
    CreatedGraphArtifactProducerConfig:
      type: object
      additionalProperties: false
      required: [name, kind, source]
      description: "Credential-free graph artifact producer configuration returned after creation."
      properties:
        name: { type: string, minLength: 1 }
        kind:
          type: string
          enum: [asset]
        source: { $ref: "#/components/schemas/GraphArtifactProducerSourceConfig" }
        content_type: { type: string }
        execution: { $ref: "#/components/schemas/ExecutionPolicy" }
    CreatedGraphArtifactSourceConfig:
      type: object
      additionalProperties: false
      required: [artifact]
      description: "Canonical artifact stream materialized into graph edges."
      properties:
        artifact: { type: string, minLength: 1 }
        path: { type: string }
        format:
          type: string
          enum: [extraction_relation, extraction_graph]
          default: extraction_relation
        mention_edge_type: { type: string }
        nodes: { $ref: "#/components/schemas/GraphArtifactNodeMappingConfig" }
        edge: { $ref: "#/components/schemas/GraphArtifactEdgeMappingConfig" }
        context: { $ref: "#/components/schemas/GraphArtifactContextConfig" }
    GraphResolverConfig:
      type: object
      additionalProperties: false
      required: [name, table, source_artifact, resolution_artifact, key_template]
      description: "Versioned entity resolver attached to an artifact-backed graph index."
      properties:
        name: { type: string }
        table: { type: string }
        source_artifact: { type: string }
        source_artifact_kind:
          type: string
          enum: [asset, chunk, any]
          default: asset
        resolution_artifact: { type: string }
        key_template: { type: string }
        type_must_match: { type: boolean, default: true }
        scorer_json: { type: string }
        candidate_search:
          type: string
          enum: ["", exact_key, prefix, ann]
        candidate_ann_index: { type: string }
        candidate_limit: { type: integer, format: uint, minimum: 0 }
        name_embedding: { type: string }
        name_embedding_dims: { type: integer, format: uint, minimum: 0 }
        fusion_combine:
          type: string
          enum: ["", noisy_or, max, mean]
        fusion_trust: { type: number, format: double, minimum: 0, maximum: 1 }
        fusion_prior: { type: number, format: double, minimum: 0, maximum: 1 }
        fusion_prior_weight: { type: number, format: double, minimum: 0 }
        config_generation: { type: integer, format: uint64, minimum: 0 }
    CreateAlgebraicIndexRequest:
      description: "Create a schema-derived algebraic index."
      allOf:
        - $ref: "#/components/schemas/CreateIndexCommon"
        - $ref: "#/components/schemas/AlgebraicIndexConfig"
        - type: object
          required: [type]
          properties:
            type:
              type: string
              enum: [algebraic]
    CreateIndexRequest:
      description: "Type-safe configuration for a new index. The index name is owned by the request path."
      x-antfly-sdk-alternative:
        description: "SDK builders also accept the named IndexConfig form and convert it to this path-identified request. This is an alternative construction API, not a deprecated wire request."
        schema:
          $ref: "#/components/schemas/IndexConfig"
      oneOf:
        - $ref: "#/components/schemas/CreateFullTextIndexRequest"
        - $ref: "#/components/schemas/CreateEmbeddingsIndexRequest"
        - $ref: "#/components/schemas/CreateGraphIndexRequest"
        - $ref: "#/components/schemas/CreateAlgebraicIndexRequest"
      discriminator:
        propertyName: type
        mapping:
          full_text: "#/components/schemas/CreateFullTextIndexRequest"
          embeddings: "#/components/schemas/CreateEmbeddingsIndexRequest"
          graph: "#/components/schemas/CreateGraphIndexRequest"
          algebraic: "#/components/schemas/CreateAlgebraicIndexRequest"
    CreatedIndexCommon:
      type: object
      description: "Fields returned for every newly created index. Provider credentials are write-only and are never returned."
      required:
        - name
      properties:
        name:
          type: string
          description: "Name of the created index"
        description:
          type: string
          description: "Optional description of the index and its purpose"
        version:
          x-go-type-skip-optional-pointer: true
          type: integer
          default: 0
          description: "Version of the index implementation. Defaults to 0."
        enrichments:
          type: array
          items:
            $ref: "#/components/schemas/CreatedEnrichmentConfig"
          x-go-type-skip-optional-pointer: true
          description: "Normalized inline managed enrichment definitions required by this index."
    CreatedProviderConfig:
      type: object
      additionalProperties: false
      required: [provider]
      description: "Credential-free provider configuration returned after index creation. Only non-secret provider settings are represented."
      properties:
        provider:
          type: string
          description: "Configured provider discriminator."
        model:
          type: string
          description: "Configured provider model when applicable."
        models:
          type: array
          items: { type: string }
        project_id: { type: string }
        location: { type: string }
        region: { type: string }
        url: { type: string, format: uri }
        api_url: { type: string, format: uri }
        dimension: { type: integer, minimum: 1 }
        dimensions: { type: integer, minimum: 1 }
        input_type: { type: string }
        truncate: { type: string }
        strip_new_lines: { type: boolean }
        batch_size: { type: integer, minimum: 1 }
        temperature: { type: number, format: float }
        max_tokens: { type: integer, minimum: 1 }
        top_p: { type: number, format: float }
        top_k: { type: integer }
        frequency_penalty: { type: number, format: float }
        presence_penalty: { type: number, format: float }
        timeout: { type: integer, minimum: 1 }
    CreatedEnrichmentConfig:
      type: object
      additionalProperties: false
      required: [name, kind]
      description: "Credential-free normalized enrichment configuration returned after index creation."
      properties:
        name: { type: string }
        kind: { $ref: "#/components/schemas/EnrichmentKind" }
        field: { type: string }
        template: { type: string }
        source_artifact_name: { type: string }
        expected_dims: { type: integer, minimum: 0 }
        vector_space:
          type: string
          minLength: 1
          description: "Optional stable model/token-space identifier asserted for this embedding artifact."
        chunk_size: { type: integer, minimum: 0 }
        chunk_overlap: { type: integer, minimum: 0 }
        chunker_json: { type: string }
        full_text_index: { type: boolean, default: false }
        content_type: { type: string }
        execution: { $ref: "#/components/schemas/ExecutionPolicy" }
    CreatedEmbeddingsIndexConfig:
      type: object
      description: "Credential-free normalized embeddings configuration returned after creation. Single-source v0.2 fields are preserved alongside canonical sources for read compatibility."
      properties:
        publication_policy: { $ref: "#/components/schemas/IndexPublicationPolicy" }
        coverage_policy: { $ref: "#/components/schemas/DerivedCoveragePolicy" }
        external: { type: boolean, default: false }
        sparse: { type: boolean, default: false }
        dimension: { type: integer, minimum: 1 }
        field: { type: string }
        sources:
          type: array
          minItems: 1
          maxItems: 64
          uniqueItems: true
          items: { $ref: "#/components/schemas/ArtifactIndexSource" }
          description: "Embedding artifact streams indexed together as independent vector members."
        embedding_name:
          type: string
          readOnly: true
          description: "Released v0.2 single-source read field, preserved when that request form created the index. Canonical source identity is also returned through sources."
        source_artifact_name:
          type: string
          readOnly: true
          deprecated: true
          x-deprecated-reason: "Use the matching embedding enrichment's source_artifact_name; it is authoritative."
          description: "Deprecated v0.2 descriptive source read field, preserved when supplied with embedding_name. The matching enrichment is authoritative."
        template: { type: string }
        distance_metric: { $ref: "#/components/schemas/DistanceMetric" }
        mem_only: { type: boolean }
        embedder: { $ref: "#/components/schemas/CreatedProviderConfig" }
        summarizer: { $ref: "#/components/schemas/CreatedProviderConfig" }
        chunker: { $ref: "chunking.yaml#/components/schemas/ChunkerConfig" }
        top_k: { type: integer, minimum: 1, default: 10 }
        min_weight: { type: number, format: float, default: 0.0 }
        chunk_size: { type: integer, minimum: 64, default: 1024 }
        execution: { $ref: "#/components/schemas/IndexExecutionConfig" }
    CreatedFullTextIndexConfig:
      type: object
      description: "Canonical full-text configuration returned after creation. Single-source alternative request forms are represented through sources."
      properties:
        sources:
          type: array
          minItems: 1
          maxItems: 64
          uniqueItems: true
          items: { $ref: "#/components/schemas/FullTextArtifactIndexSource" }
        mem_only: { type: boolean }
        field: { type: string }
    CreatedGraphIndexConfig:
      type: object
      description: "Credential-free normalized graph configuration returned after creation."
      properties:
        summarizer: { $ref: "#/components/schemas/CreatedProviderConfig" }
        template: { type: string }
        edge_types:
          type: array
          items: { $ref: "#/components/schemas/EdgeTypeConfig" }
        max_edges_per_document:
          type: integer
          minimum: 0
          maximum: 1000000
          description: "Maximum number of distinct visible edges materialized per document after source precedence and identity deduplication. Zero uses the server safety limit (currently 1,000,000). Independent aggregate reconciliation budgets bound work across overlapping source manifests."
        sources:
          type: array
          minItems: 1
          maxItems: 64
          uniqueItems: true
          items: { $ref: "#/components/schemas/CreatedGraphArtifactSourceConfig" }
        artifact: { $ref: "#/components/schemas/CreatedGraphArtifactProducerConfig" }
        algebraic_planning: { $ref: "#/components/schemas/GraphAlgebraicPlanningConfig" }
        resolvers:
          type: array
          items: { $ref: "#/components/schemas/GraphResolverConfig" }
    CreatedFullTextIndex:
      description: "Normalized effective full-text index configuration returned after creation."
      allOf:
        - $ref: "#/components/schemas/CreatedIndexCommon"
        - $ref: "#/components/schemas/CreatedFullTextIndexConfig"
        - type: object
          required: [type]
          properties:
            type:
              type: string
              enum: [full_text]
    CreatedEmbeddingsIndex:
      description: "Normalized effective dense or sparse embeddings index configuration returned after creation."
      allOf:
        - $ref: "#/components/schemas/CreatedIndexCommon"
        - $ref: "#/components/schemas/CreatedEmbeddingsIndexConfig"
        - type: object
          required: [type]
          properties:
            type:
              type: string
              enum: [embeddings]
    CreatedGraphIndex:
      description: "Normalized effective graph index configuration returned after creation."
      allOf:
        - $ref: "#/components/schemas/CreatedIndexCommon"
        - $ref: "#/components/schemas/CreatedGraphIndexConfig"
        - type: object
          required: [type]
          properties:
            type:
              type: string
              enum: [graph]
    CreatedAlgebraicIndex:
      description: "Normalized effective schema-derived algebraic index configuration returned after creation."
      allOf:
        - $ref: "#/components/schemas/CreatedIndexCommon"
        - $ref: "#/components/schemas/AlgebraicIndexConfig"
        - type: object
          required: [type]
          properties:
            type:
              type: string
              enum: [algebraic]
    CreatedIndex:
      description: "Discriminated normalized configuration returned after an index is created."
      oneOf:
        - $ref: "#/components/schemas/CreatedFullTextIndex"
        - $ref: "#/components/schemas/CreatedEmbeddingsIndex"
        - $ref: "#/components/schemas/CreatedGraphIndex"
        - $ref: "#/components/schemas/CreatedAlgebraicIndex"
      discriminator:
        propertyName: type
        mapping:
          full_text: "#/components/schemas/CreatedFullTextIndex"
          embeddings: "#/components/schemas/CreatedEmbeddingsIndex"
          graph: "#/components/schemas/CreatedGraphIndex"
          algebraic: "#/components/schemas/CreatedAlgebraicIndex"
    EnrichmentKind:
      type: string
      enum:
        - chunk
        - asset
        - embedding
      description: "Managed generated artifact kind."
    ExecutionPolicy:
      type: object
      additionalProperties: false
      description: "Non-semantic execution policy for one producer or index maintenance operation. These fields tune how work is batched and do not change generated artifact identity."
      properties:
        batch_items:
          x-go-type-skip-optional-pointer: true
          type: integer
          minimum: 1
          description: "Maximum items to process in one batch for this operation."
        batch_bytes:
          x-go-type-skip-optional-pointer: true
          type: integer
          minimum: 1
          format: uint64
          description: "Approximate maximum source bytes to process in one batch for this operation."
    IndexExecutionConfig:
      type: object
      additionalProperties: false
      description: "Namespaced execution policy for managed index shorthand. Only namespaces with runtime effects are accepted."
      properties:
        chunking:
          $ref: "#/components/schemas/ExecutionPolicy"
          description: "Chunk producer batching for shorthand-created chunk enrichments."
        embedding:
          $ref: "#/components/schemas/ExecutionPolicy"
          description: "Embedding producer batching for shorthand-created embedding enrichments."
    EnrichmentConfig:
      type: object
      description: "Inline managed enrichment definition. Enrichments materialize generated artifacts before indexing and may target source rows or previously generated artifact streams."
      required:
        - name
        - kind
      properties:
        name:
          type: string
          description: "Stable generated artifact name."
        kind:
          $ref: "#/components/schemas/EnrichmentKind"
        field:
          x-go-type-skip-optional-pointer: true
          type: string
          description: "Source field to read from the source document or source artifact payload."
        template:
          x-go-type-skip-optional-pointer: true
          type: string
          description: "Optional template for generated text input."
        source_artifact_name:
          x-go-type-skip-optional-pointer: true
          type: string
          description: "Existing artifact stream this enrichment consumes. Chunk enrichments may consume asset artifacts; embedding enrichments may consume chunk artifacts."
        expected_dims:
          x-go-type-skip-optional-pointer: true
          type: integer
          minimum: 0
          description: "Expected embedding dimension for embedding enrichments."
        vector_space:
          x-go-type-skip-optional-pointer: true
          type: string
          minLength: 1
          description: "Optional stable model/token-space identifier for embedding artifacts. When omitted on every source, Antfly requires the effective producers to be semantically equivalent. To combine intentionally compatible but distinct producers, set the same identifier on every source. Explicit and implicit modes cannot be mixed; dimensions are always validated independently."
        chunk_size:
          x-go-type-skip-optional-pointer: true
          type: integer
          minimum: 0
          description: "Chunk size for chunk enrichments."
        chunk_overlap:
          x-go-type-skip-optional-pointer: true
          type: integer
          minimum: 0
          description: "Chunk overlap for chunk enrichments."
        chunker_json:
          x-go-type-skip-optional-pointer: true
          type: string
          description: "Serialized chunker configuration for chunk enrichments."
        full_text_index:
          x-go-type-skip-optional-pointer: true
          type: boolean
          default: false
          description: "When true on a chunk or asset enrichment, route generated text into the table's default full-text index."
        content_type:
          x-go-type-skip-optional-pointer: true
          type: string
          description: "Produced asset content type for asset enrichments."
        producer_json:
          x-go-type-skip-optional-pointer: true
          type: string
          writeOnly: true
          description: "Write-only serialized producer configuration. For managed embedding enrichments Antfly stores a canonical semantic producer identity here; credentials and execution policy are excluded."
        execution:
          $ref: "#/components/schemas/ExecutionPolicy"
          description: "Non-semantic execution policy for this enrichment producer. This does not participate in generated artifact identity."
    FullTextIndexConfig:
      type: object
      x-antfly-mutually-exclusive:
        - [sources, artifact_name]
      properties:
        sources:
          type: array
          minItems: 1
          maxItems: 64
          uniqueItems: true
          items:
            $ref: "#/components/schemas/FullTextArtifactIndexSource"
          x-go-type-skip-optional-pointer: true
          description: "Chunk or textual asset streams indexed together; every artifact record is an independent full-text member. A source-local field overrides the shared index-level field for that stream. Artifact names must be unique. Requires index_capabilities.artifact_sources=true and is rejected by serverless deployments."
        mem_only:
          x-go-type-skip-optional-pointer: true
          type: boolean
          description: "Whether to use memory-only storage"
        field:
          x-go-type-skip-optional-pointer: true
          type: string
          minLength: 1
          description: "Content field indexed as text. With an artifact source, this selects the field within each artifact record; without one, it selects a document field. String values and arrays of strings are indexed; missing, null, and non-text values produce no posting. Omit to index the default text projection."
        artifact_name:
          x-go-type-skip-optional-pointer: true
          type: string
          minLength: 1
          description: "Single-source convenience form. Mutually exclusive with sources; normalized responses use sources. Requires index_capabilities.artifact_sources=true and is rejected by serverless deployments."
    AlgebraicIndexConfig:
      type: object
      description: "Schema-derived algebraic sidecar configuration. Public requests may opt into schema derivation, while materializations remain engine-owned."
      properties:
        derive_from_schema:
          x-go-type-skip-optional-pointer: true
          type: boolean
          description: "When true, derive the algebraic capability sidecar from the table schema. Internal fields and materialization definitions are not public API."
    IndexReadinessState:
      type: string
      enum:
        - pending
        - queryable_partial
        - ready
        - failed
      description: "Lifecycle state for the desired index incarnation. A failed desired repair may coexist with queryable=true when a separately proven serving incarnation remains available; clients must use the explicit milestone booleans."
    IndexReadinessReason:
      type: string
      enum:
        - load_failure
        - enrichment_failure
        - runtime_unavailable
        - shard_observation_incomplete
        - incarnation_pending
        - source_publication
        - repair
        - backfill
        - coverage
        - replay
        - publication
      description: "Stable machine-readable reason why an index is pending, partial, or failed."
    IndexSourceReadinessReason:
      type: string
      enum:
        - index_failed
        - enrichment_failure
        - repair
        - runtime_unavailable
        - shard_observation_incomplete
        - source_observation_incomplete
        - publication
      description: "Stable machine-readable reason why an artifact source is pending or failed."
    IndexReadinessStatus:
      type: object
      additionalProperties: false
      required:
        - state
        - queryable
        - complete
        - pending_reasons
      properties:
        state:
          $ref: '#/components/schemas/IndexReadinessState'
        queryable:
          type: boolean
          description: "Whether the published generation can safely answer queries."
        complete:
          type: boolean
          description: "Whether the desired incarnation has complete coverage and publication according to its configured policies."
        incarnation:
          type: string
          description: "Opaque identity for the desired index incarnation. Clients may compare it for equality but must not interpret its contents."
        target_revision:
          type: integer
          format: uint64
          description: "Highest captured source/replay revision required by this readiness observation."
        published_revision:
          type: integer
          format: uint64
          description: "Highest revision published to the query-visible index represented by this observation."
        pending_reasons:
          type: array
          items:
            $ref: '#/components/schemas/IndexReadinessReason'
          description: "Stable, machine-readable blockers or failure reasons. Empty when state is ready."
        sources:
          type: array
          description: "Operational readiness for each configured artifact stream. Present only for artifact-backed indexes, in configuration order."
          items:
            $ref: '#/components/schemas/IndexSourceReadinessStatus'
    IndexSourceReadinessStatus:
      type: object
      additionalProperties: false
      required:
        - artifact
        - state
        - complete
        - pending_reasons
      properties:
        artifact:
          type: string
          description: "Configured artifact stream identity."
        state:
          type: string
          enum:
            - pending
            - ready
            - failed
        complete:
          type: boolean
          description: "Whether this source is fully observed and published on every expected shard."
        pending_reasons:
          type: array
          items:
            $ref: '#/components/schemas/IndexSourceReadinessReason'
          description: "Stable, machine-readable blockers or failure reasons for this source. Empty when state is ready."
    IndexRepairStatus:
      type: object
      description: Compact user-facing state for an automatic index repair. Detailed diagnostics are available from the admin API and metrics.
      required:
        - state
        - action_required
        - blocks_queryable
        - blocks_complete
      properties:
        state:
          type: string
          enum:
            - rebuilding
            - waiting
            - paused
            - failed
          description: Stable repair state. Internal state-machine phases are intentionally not exposed here.
        action_required:
          type: boolean
          description: Whether an operator must resume, retry, reconfigure, or drop the affected index.
        blocks_queryable:
          type: boolean
          description: Whether this repair currently prevents the proven serving incarnation from answering queries.
        blocks_complete:
          type: boolean
          description: Whether this repair prevents the desired incarnation from satisfying the complete milestone.
        reason:
          type: string
          description: Diagnostic reason automation stopped. Present only when action_required is true.
    FullTextIndexStats:
      type: object
      required:
        - index_type
      properties:
        index_type:
          x-go-type-skip-optional-pointer: true
          type: string
          enum:
            - full_text
          description: "Discriminator for the index stats variant."
        readiness:
          $ref: '#/components/schemas/IndexReadinessStatus'
        error:
          x-go-type-skip-optional-pointer: true
          type: string
          description: "Error message if stats could not be retrieved"
        total_indexed:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Number of documents in the index"
        disk_usage:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Size of the index in bytes"
        rebuilding:
          x-go-type-skip-optional-pointer: true
          type: boolean
          description: "Whether the index is currently rebuilding"
        repair:
          $ref: '#/components/schemas/IndexRepairStatus'
        backfill_active:
          x-go-type-skip-optional-pointer: true
          type: boolean
          description: "Whether the index is actively rebuilding, replaying, or catching up."
        backfill_progress:
          x-go-type-skip-optional-pointer: true
          type: number
          format: double
          description: "Progress of ongoing rebuild as fraction [0.0, 1.0]"
        backfill_items_processed:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Number of documents indexed during current rebuild"
        backfill_state:
          x-go-type-skip-optional-pointer: true
          type: string
          description: "Operational readiness state such as ready, running, retrying, degraded, or failed."
        doc_count:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Number of documents visible to the index."
        term_count:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Number of indexed terms when available."
        replay_applied_sequence:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Highest replay sequence applied to the index runtime."
        replay_target_sequence:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Replay sequence the index runtime must reach to be current."
        replay_catch_up_required:
          x-go-type-skip-optional-pointer: true
          type: boolean
          description: "Whether replay must catch up before the index is fully current."
        runtime_present:
          x-go-type-skip-optional-pointer: true
          type: boolean
        runtime_fresh:
          x-go-type-skip-optional-pointer: true
          type: boolean
        runtime_source:
          x-go-type-skip-optional-pointer: true
          type: string
        runtime_freshness:
          x-go-type-skip-optional-pointer: true
          type: string
        catch_up_active:
          x-go-type-skip-optional-pointer: true
          type: boolean
        catch_up_phase:
          x-go-type-skip-optional-pointer: true
          type: string
        catch_up_applied_sequence:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        catch_up_target_sequence:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        text_merge:
          x-go-type-skip-optional-pointer: true
          type: object
          additionalProperties: true
          description: "Full-text merge runtime diagnostics."
        async_indexing:
          x-go-type-skip-optional-pointer: true
          type: object
          additionalProperties: true
          description: "Asynchronous indexer runtime diagnostics."
        projection_checkpoint_status:
          x-go-type-skip-optional-pointer: true
          type: string
          description: "Durable projection checkpoint status: clean, rebuilding, degraded, or repair_required."
        projection_checkpoint_applied_sequence:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Highest derived-log sequence covered by the durable projection checkpoint."
        projection_checkpoint_generation:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Projection generation associated with the durable checkpoint."
        projection_checkpoint_config_fingerprint:
          x-go-type-skip-optional-pointer: true
          type: string
          pattern: "^[0-9a-f]{16}$"
          description: "Projection configuration identity associated with the durable checkpoint."
        checkpoint_replay_tail_sequence_count:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Number of derived-log sequences after the durable checkpoint that still need replay."
        repair_scan_issue_count:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Repair issues found by explicit repair-scan accounting for this projection."
        edge_count:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        node_count:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        repair_degraded:
          x-go-type-skip-optional-pointer: true
          type: boolean
        repair_issue_count:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        repair_summary_ready:
          x-go-type-skip-optional-pointer: true
          type: boolean
        repair_issue_count_estimated:
          x-go-type-skip-optional-pointer: true
          type: boolean
        expected_groups:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        reported_groups:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        fresh_groups:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        stale_groups:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        missing_groups:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        unknown_remote_groups:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        resolution:
          x-go-type-skip-optional-pointer: true
          type: object
          additionalProperties: true
          description: "Artifact resolution replay diagnostics."
        promotion:
          x-go-type-skip-optional-pointer: true
          type: object
          additionalProperties: true
          description: "Artifact promotion replay diagnostics."
    IndexPublicationPolicy:
      type: string
      enum:
        - progressive
        - atomic
      default: progressive
      description: >-
        Publication behavior for a managed embeddings index. `progressive`
        makes a safely checkpointed active generation queryable before initial
        source coverage is complete. `atomic` keeps a new generation
        unavailable until complete validation and activation.
    EmbeddingsIndexConfig:
      type: object
      description: >-
        Unified configuration for embeddings indexes.
        When sparse is true, creates a sparse vector index (SPLADE inverted index).
        When sparse is false (default), creates a dense vector index (HNSW).
        For dense indexes, dimension can be omitted if an embedder is configured — it will be auto-detected.
      x-antfly-mutually-exclusive:
        - [sources, external]
        - [sources, field]
        - [sources, template]
        - [sources, chunker]
        - [sources, embedding_name]
        - [sources, source_artifact_name]
      # OpenAPI 3.0 has no dependentRequired keyword. Repository-owned server
      # and SDK request validators mirror this extension while keeping generated
      # models usable instead of forcing the configuration into an opaque union.
      x-antfly-dependent-required:
        source_artifact_name: [embedding_name]
      properties:
        publication_policy:
          $ref: "#/components/schemas/IndexPublicationPolicy"
        coverage_policy:
          $ref: "#/components/schemas/DerivedCoveragePolicy"
          description: >-
            Source-unit completeness policy for managed embeddings. `strict`
            requires one produced outcome per source document; `partial` permits
            intentional skips; `best_effort` also treats terminal failures as
            complete while reporting the index unhealthy. External indexes use
            `external: true` and must not set this field.
        external:
          x-go-type-skip-optional-pointer: true
          type: boolean
          default: false
          description: "When true, embeddings are supplied externally via _embeddings and the index does not derive prompts from a field or template."
        sparse:
          x-go-type-skip-optional-pointer: true
          type: boolean
          default: false
          description: "When true, creates a sparse (SPLADE) inverted index. When false (default), creates a dense (HNSW) vector index."
        dimension:
          x-go-type-skip-optional-pointer: true
          type: integer
          minimum: 1
          description: "Vector dimension for dense indexes. Required for external dense indexes. Can be omitted for managed dense indexes when an embedder is configured (auto-detected via probe). Ignored for sparse indexes."
        field:
          x-go-type-skip-optional-pointer: true
          type: string
          description: "Field to extract embeddings from (managed indexes only; not allowed when external=true)"
        sources:
          type: array
          minItems: 1
          maxItems: 64
          uniqueItems: true
          items:
            $ref: "#/components/schemas/ArtifactIndexSource"
          x-go-type-skip-optional-pointer: true
          description: >-
            Embedding artifact streams indexed together. Each artifact record is an independent
            vector member identified by (artifact name, source key). All sources must use the same
            dense vector space or sparse token space. Not allowed with external, field, template,
            chunker, embedding_name, or source_artifact_name. Requires
            index_capabilities.artifact_sources=true and is rejected by serverless deployments.
        embedding_name:
          x-go-type-skip-optional-pointer: true
          type: string
          minLength: 1
          description: "Released v0.2 single-source alternative request form. Mutually exclusive with sources. Required when source_artifact_name is set. Responses also expose canonical sources while preserving these fields. Requires index_capabilities.artifact_sources=true and is rejected by serverless deployments."
        source_artifact_name:
          x-go-type-skip-optional-pointer: true
          type: string
          minLength: 1
          deprecated: true
          x-deprecated-reason: "Set source_artifact_name only on the matching embedding enrichment."
          description: "Deprecated v0.2 descriptive field. When supplied for compatibility, embedding_name is required and this value must exactly match the source_artifact_name on the authoritative embedding enrichment. New clients should declare the relationship only on that enrichment."
        template:
          x-go-type-skip-optional-pointer: true
          type: string
          description: >-
            Handlebars template for generating prompts (managed indexes only; not allowed when external=true).
            See https://handlebarsjs.com/guide/ for more information.
          example: 'Hello, {{#if (eq Name "John")}}Johnathan{{else}}{{Name}}{{/if}}! You are {{Age}} years old.'
        distance_metric:
          $ref: "#/components/schemas/DistanceMetric"
        mem_only:
          x-go-type-skip-optional-pointer: true
          type: boolean
          description: "Whether to use in-memory only storage (dense only)"
        embedder:
          description: "Configuration for the embeddings plugin (managed indexes only; not allowed when external=true)"
          $ref: "embeddings.yaml#/components/schemas/EmbedderConfig"
        summarizer:
          description: "Configuration for the summarizer plugin (dense managed indexes only)"
          $ref: "../shared/generating.yaml#/components/schemas/GeneratorConfig"
        chunker:
          description: "Configuration for the chunking plugin. When specified, documents are automatically chunked at write time before indexing. (dense managed indexes only)"
          $ref: "chunking.yaml#/components/schemas/ChunkerConfig"
        top_k:
          x-go-type-skip-optional-pointer: true
          type: integer
          minimum: 1
          default: 10
          description: "Default number of results to return from search (sparse only)"
        min_weight:
          x-go-type-skip-optional-pointer: true
          type: number
          format: float
          default: 0.0
          description: "Minimum weight threshold for sparse vector entries (sparse only)"
        chunk_size:
          x-go-type-skip-optional-pointer: true
          type: integer
          minimum: 64
          default: 1024
          description: "Number of documents per posting list chunk (sparse only)"
        execution:
          $ref: "#/components/schemas/IndexExecutionConfig"
          description: "Non-semantic execution policy for shorthand-created chunking or embedding producers."
    DerivedCoveragePolicy:
      type: string
      enum:
        - strict
        - partial
        - best_effort
      default: strict
      description: "How generation-scoped source outcomes determine derived-index completeness."
    DerivedCoverageStatusPolicy:
      type: string
      enum:
        - strict
        - partial
        - best_effort
        - external
    DerivedCoverageObservationIncompleteReason:
      type: string
      enum:
        - runtime_unavailable
        - missing_group
        - unknown_group
        - remote_unknown_group
        - stale_group
        - summary_unavailable
        - config_mismatch
        - counter_mismatch
      description: "A structured reason why the coverage projection cannot be treated as globally complete."
    DerivedCoverageStatus:
      type: object
      additionalProperties: false
      required:
        - policy
        - observation_complete
        - observation_incomplete_reasons
        - config_fingerprint
        - summary_ready
        - config_mismatch_group_count
        - source_total
        - produced
        - skipped
        - terminal_failed
        - covered
        - settled
        - uncovered
        - pending
        - complete
        - healthy
        - degraded
      properties:
        policy:
          $ref: "#/components/schemas/DerivedCoverageStatusPolicy"
        observation_complete:
          type: boolean
          description: "Whether every expected shard contributed a fresh, configuration-compatible observation with valid outcome cardinality."
        observation_incomplete_reasons:
          type: array
          items:
            $ref: "#/components/schemas/DerivedCoverageObservationIncompleteReason"
          description: "Empty when observation_complete is true; otherwise identifies every known reason the projection is incomplete."
        config_fingerprint:
          type: string
          pattern: "^[0-9a-f]{16}$"
          description: "Versioned semantic configuration fingerprint encoded as fixed-width hexadecimal. Non-semantic execution tuning does not affect it."
        summary_ready:
          type: boolean
          description: "Whether all observed shard-local coverage summaries were read atomically and completely."
        config_mismatch_group_count:
          type: integer
          format: uint64
          description: "Freshly observed shard groups reporting a different semantic configuration fingerprint."
        source_total:
          type: integer
          format: uint64
          description: >-
            Source documents observed across fresh shard reports. This is the
            exact table total only when observation_complete is true; otherwise
            it is a lower bound and all outcome counts are partial observations.
        produced:
          type: integer
          format: uint64
          description: "Source documents with a durable produced outcome for this index generation."
        skipped:
          type: integer
          format: uint64
          description: "Source documents intentionally producing no indexable output."
        terminal_failed:
          type: integer
          format: uint64
          description: "Source documents whose generation failed non-retryably."
        covered:
          type: integer
          format: uint64
          description: "Raw terminal source outcomes counted by the configured policy. This may exceed source_total only while observation_complete is false with counter_mismatch."
        settled:
          type: integer
          format: uint64
          description: "Source documents with any durable terminal outcome: produced, intentionally skipped, or terminally failed."
        uncovered:
          x-go-type-skip-optional-pointer: false
          type: integer
          format: uint64
          nullable: true
          description: "Source documents without an outcome accepted by the configured coverage policy. Null when observations are incomplete."
        pending:
          x-go-type-skip-optional-pointer: false
          type: integer
          format: uint64
          nullable: true
          description: "Source documents that have not reached any terminal outcome and may still be processing. Null when observations are incomplete."
        complete:
          type: boolean
          description: "Whether observations are complete, replay has reached its target, and every observed source has an outcome accepted by the policy."
        healthy:
          type: boolean
          description: "Whether coverage is complete without terminal failures."
        degraded:
          type: boolean
          description: "Whether all sources are settled but coverage remains unhealthy under the configured policy, including terminal failures or policy-rejected skips."
    EnrichmentRuntimeStatus:
      type: object
      additionalProperties: false
      description: "Runtime state for the durable embeddings enrichment worker."
      required:
        - enabled
        - target_sequence
        - applied_sequence
        - pending_sequence_count
        - projection_checkpoint_status
        - projection_checkpoint_applied_sequence
        - projection_checkpoint_generation
        - projection_checkpoint_config_fingerprint
        - projection_checkpoint_identity_consistent
        - checkpoint_replay_tail_sequence_count
        - processed_requests
        - error_count
        - retryable_error_count
        - fatal_error_count
        - consecutive_retry_count
        - next_retry_at_ms
        - retrying
        - worker_failed
        - worker_started
        - stalled
        - skip_by_hash_count
        - skipped_source_count
        - codec_decode_failures
        - embed_batches_started
        - embed_batches_completed
        - embed_items_started
        - embed_items_completed
        - active_embed_batch_items
        - active_embed_batch_bytes
        - active_embed_batch_max_bytes
        - active_embed_batch_started_ms
        - last_embed_batch_items
        - last_embed_batch_bytes
        - last_embed_batch_max_bytes
        - last_embed_batch_completed_ms
        - last_embed_batch_ns
        - total_embed_ns
      properties:
        enabled:
          type: boolean
        target_sequence:
          type: integer
          format: uint64
        applied_sequence:
          type: integer
          format: uint64
        pending_sequence_count:
          type: integer
          format: uint64
        projection_checkpoint_status:
          type: string
        projection_checkpoint_applied_sequence:
          type: integer
          format: uint64
        projection_checkpoint_generation:
          type: integer
          format: uint64
        projection_checkpoint_config_fingerprint:
          type: string
          pattern: "^[0-9a-f]{16}$"
        projection_checkpoint_identity_consistent:
          type: boolean
          description: "Whether every shard contributing to this status reports the same checkpoint generation and configuration identity."
        checkpoint_replay_tail_sequence_count:
          type: integer
          format: uint64
        processed_requests:
          type: integer
          format: uint64
        error_count:
          type: integer
          format: uint64
        retryable_error_count:
          type: integer
          format: uint64
        fatal_error_count:
          type: integer
          format: uint64
        consecutive_retry_count:
          type: integer
          format: uint32
          description: "Consecutive durable worker retries for the current failed request window."
        next_retry_at_ms:
          type: integer
          format: uint64
          description: "Unix epoch time in milliseconds when the current durable retry becomes eligible. Zero when not retrying."
        retrying:
          type: boolean
        worker_failed:
          type: boolean
        worker_started:
          type: boolean
          description: "Whether the background enrichment worker is currently running."
        stalled:
          type: boolean
          description: "Whether work is pending with no running worker, retry, or terminal failure explaining the backlog."
        skip_by_hash_count:
          type: integer
          format: uint64
        skipped_source_count:
          type: integer
          format: uint64
        codec_decode_failures:
          type: integer
          format: uint64
        embed_batches_started:
          type: integer
          format: uint64
        embed_batches_completed:
          type: integer
          format: uint64
        embed_items_started:
          type: integer
          format: uint64
        embed_items_completed:
          type: integer
          format: uint64
        active_embed_batch_items:
          type: integer
          format: uint64
        active_embed_batch_bytes:
          type: integer
          format: uint64
        active_embed_batch_max_bytes:
          type: integer
          format: uint64
        active_embed_batch_started_ms:
          type: integer
          format: uint64
        last_embed_batch_items:
          type: integer
          format: uint64
        last_embed_batch_bytes:
          type: integer
          format: uint64
        last_embed_batch_max_bytes:
          type: integer
          format: uint64
        last_embed_batch_completed_ms:
          type: integer
          format: uint64
          description: "Wall-clock completion time in Unix milliseconds for the most recently completed embedding batch."
        last_embed_batch_ns:
          type: integer
          format: uint64
          description: "Elapsed duration in nanoseconds for the most recently completed embedding batch."
        total_embed_ns:
          type: integer
          format: uint64
    EmbeddingsIndexStats:
      type: object
      description: "Statistics for an embeddings index (dense or sparse)"
      required:
        - index_type
      properties:
        index_type:
          x-go-type-skip-optional-pointer: true
          type: string
          enum:
            - embeddings
          description: "Discriminator for the index stats variant."
        readiness:
          $ref: '#/components/schemas/IndexReadinessStatus'
        error:
          x-go-type-skip-optional-pointer: true
          type: string
          description: "Error message if stats could not be retrieved"
        total_indexed:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Number of vectors/documents in the index"
        disk_usage:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Size of the index in bytes"
        total_nodes:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Total number of nodes in the index (dense only)"
        total_terms:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Number of unique terms in the inverted index (sparse only)"
        rebuilding:
          x-go-type-skip-optional-pointer: true
          type: boolean
          description: "Whether enrichment, publication, or replay work is still pending. Documents that do not contain the indexed field are terminal skipped outcomes and do not keep this true."
        repair:
          $ref: '#/components/schemas/IndexRepairStatus'
        wal_backlog:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Number of documents pending enrichment in the WAL"
        backfill_active:
          x-go-type-skip-optional-pointer: true
          type: boolean
          description: "Whether the index is actively rebuilding, replaying, enriching, or catching up."
        backfill_progress:
          x-go-type-skip-optional-pointer: true
          type: number
          format: double
          description: "Fraction of source documents with a terminal materialization outcome, including produced embeddings and intentionally skipped documents. Reaches 1.0 when no source work is pending and replay is current."
        backfill_items_processed:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Total items processed during backfill"
        backfill_state:
          x-go-type-skip-optional-pointer: true
          type: string
          description: "Operational readiness state. Clients should use ready (or rebuilding=false) for query readiness; replay watermarks diagnose replay progress but do not replace this signal."
        doc_count:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Number of physical vectors or sparse entries visible to the index; chunked indexes may contain multiple entries per source document."
        coverage:
          $ref: "#/components/schemas/DerivedCoverageStatus"
          description: "Generation-scoped source-document coverage, separate from physical index cardinality."
        query_visible_doc_count:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Documents currently visible to queries."
        published_doc_count:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        published_node_count:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        root_node:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        published_root_node:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        dense_replay_applied_sequence:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        dense_replay_target_sequence:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        dense_publish_pending:
          x-go-type-skip-optional-pointer: true
          type: boolean
          description: "Whether dense/vector artifacts still need publication before queries see the latest data."
        replay_applied_sequence:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        replay_target_sequence:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        replay_catch_up_required:
          x-go-type-skip-optional-pointer: true
          type: boolean
        runtime_present:
          x-go-type-skip-optional-pointer: true
          type: boolean
        runtime_fresh:
          x-go-type-skip-optional-pointer: true
          type: boolean
        runtime_source:
          x-go-type-skip-optional-pointer: true
          type: string
        runtime_freshness:
          x-go-type-skip-optional-pointer: true
          type: string
        catch_up_active:
          x-go-type-skip-optional-pointer: true
          type: boolean
        catch_up_phase:
          x-go-type-skip-optional-pointer: true
          type: string
        catch_up_applied_sequence:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        catch_up_target_sequence:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        enrichment_runtime:
          $ref: "#/components/schemas/EnrichmentRuntimeStatus"
        hbc_cache:
          x-go-type-skip-optional-pointer: true
          type: object
          additionalProperties: true
        hbc_posting:
          x-go-type-skip-optional-pointer: true
          type: object
          additionalProperties: true
        async_indexing:
          x-go-type-skip-optional-pointer: true
          type: object
          additionalProperties: true
        projection_checkpoint_status:
          x-go-type-skip-optional-pointer: true
          type: string
          description: "Durable projection checkpoint status: clean, rebuilding, degraded, or repair_required."
        projection_checkpoint_applied_sequence:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Highest derived-log sequence covered by the durable projection checkpoint."
        projection_checkpoint_generation:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Projection generation associated with the durable checkpoint."
        projection_checkpoint_config_fingerprint:
          x-go-type-skip-optional-pointer: true
          type: string
          pattern: "^[0-9a-f]{16}$"
          description: "Projection configuration identity associated with the durable checkpoint."
        checkpoint_replay_tail_sequence_count:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Number of derived-log sequences after the durable checkpoint that still need replay."
        repair_scan_issue_count:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Repair issues found by explicit repair-scan accounting for this projection."
        term_count:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        edge_count:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        node_count:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        repair_degraded:
          x-go-type-skip-optional-pointer: true
          type: boolean
        repair_issue_count:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        repair_summary_ready:
          x-go-type-skip-optional-pointer: true
          type: boolean
        repair_issue_count_estimated:
          x-go-type-skip-optional-pointer: true
          type: boolean
        expected_groups:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        reported_groups:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        fresh_groups:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        stale_groups:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        missing_groups:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        unknown_remote_groups:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        resolution:
          x-go-type-skip-optional-pointer: true
          type: object
          additionalProperties: true
          description: "Artifact resolution replay diagnostics."
        promotion:
          x-go-type-skip-optional-pointer: true
          type: object
          additionalProperties: true
          description: "Artifact promotion replay diagnostics."
    IndexStats:
      type: object
      description: "Statistics for an index"
      discriminator:
        propertyName: index_type
        mapping:
          full_text: "indexes.yaml#/components/schemas/FullTextIndexStats"
          embeddings: "indexes.yaml#/components/schemas/EmbeddingsIndexStats"
          graph: "indexes.yaml#/components/schemas/GraphIndexStats"
          algebraic: "indexes.yaml#/components/schemas/AlgebraicIndexStats"
      oneOf:
        - $ref: "#/components/schemas/FullTextIndexStats"
        - $ref: "#/components/schemas/EmbeddingsIndexStats"
        - $ref: "#/components/schemas/GraphIndexStats"
        - $ref: "#/components/schemas/AlgebraicIndexStats"
    AlgebraicIndexStats:
      type: object
      description: "Compact public statistics for an algebraic sidecar index. Detailed runtime, adaptive, and materialization records remain internal diagnostics."
      required:
        - index_type
      properties:
        index_type:
          x-go-type-skip-optional-pointer: true
          type: string
          enum:
            - algebraic
          description: "Discriminator for the index stats variant."
        readiness:
          $ref: '#/components/schemas/IndexReadinessStatus'
        error:
          x-go-type-skip-optional-pointer: true
          type: string
          description: "Error message if stats could not be retrieved"
        total_indexed:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Number of documents reflected in the algebraic sidecar"
        disk_usage:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Size of the index in bytes"
        rebuilding:
          x-go-type-skip-optional-pointer: true
          type: boolean
          description: "Whether the sidecar is currently rebuilding"
        repair:
          $ref: '#/components/schemas/IndexRepairStatus'
        backfill_active:
          x-go-type-skip-optional-pointer: true
          type: boolean
          description: "Whether the sidecar is actively rebuilding, replaying, or catching up."
        backfill_progress:
          x-go-type-skip-optional-pointer: true
          type: number
          format: double
          description: "Backfill progress as a ratio from 0.0 to 1.0"
        backfill_items_processed:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Number of documents processed during current backfill"
        backfill_state:
          x-go-type-skip-optional-pointer: true
          type: string
          description: "Operational readiness state such as ready, running, retrying, degraded, or failed."
        doc_count:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Number of documents visible to the sidecar."
        term_count:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        replay_applied_sequence:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        replay_target_sequence:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        replay_catch_up_required:
          x-go-type-skip-optional-pointer: true
          type: boolean
        runtime_present:
          x-go-type-skip-optional-pointer: true
          type: boolean
        runtime_fresh:
          x-go-type-skip-optional-pointer: true
          type: boolean
        runtime_source:
          x-go-type-skip-optional-pointer: true
          type: string
        runtime_freshness:
          x-go-type-skip-optional-pointer: true
          type: string
        catch_up_active:
          x-go-type-skip-optional-pointer: true
          type: boolean
        catch_up_phase:
          x-go-type-skip-optional-pointer: true
          type: string
        catch_up_applied_sequence:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        catch_up_target_sequence:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        async_indexing:
          x-go-type-skip-optional-pointer: true
          type: object
          additionalProperties: true
        healthy:
          x-go-type-skip-optional-pointer: true
          type: boolean
        parse_error_count:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        schema_version:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        capability_lifecycle_status:
          x-go-type-skip-optional-pointer: true
          type: string
          description: "Schema-derived algebraic capability lifecycle, for example current, stale, or rebuild_required."
        planner_selected:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        planner_fallback_count:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        planner_last_decision:
          x-go-type-skip-optional-pointer: true
          type: string
          enum:
            - selected
            - fallback
        planner_last_fallback_reason:
          x-go-type-skip-optional-pointer: true
          type: string
        planner_last_estimated_scan_rows:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Latest algebraic planner scan-row estimate for the last selected or fallback decision."
        planner_last_estimated_result_buckets:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Latest algebraic planner result-bucket estimate for the last selected or fallback decision."
        planner_lifecycle_ready:
          x-go-type-skip-optional-pointer: true
          type: boolean
        planner_lifecycle_blocking_reason:
          x-go-type-skip-optional-pointer: true
          type: string
        adaptive_progress_count:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        recommendation_count:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Number of currently recommended algebraic shapes."
        adaptive_backfilling_count:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        adaptive_ready_count:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        adaptive_stale_count:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        adaptive_cleanup_recommended_count:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        last_error_reason:
          x-go-type-skip-optional-pointer: true
          type: string
        active_progress_lifecycle:
          x-go-type-skip-optional-pointer: true
          type: string
        active_progress_rows_processed:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        active_progress_target_rows:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        projection_checkpoint_status:
          x-go-type-skip-optional-pointer: true
          type: string
          description: "Durable projection checkpoint status: clean, rebuilding, degraded, or repair_required."
        projection_checkpoint_applied_sequence:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Highest derived-log sequence covered by the durable projection checkpoint."
        projection_checkpoint_generation:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Projection generation associated with the durable checkpoint."
        projection_checkpoint_config_fingerprint:
          x-go-type-skip-optional-pointer: true
          type: string
          pattern: "^[0-9a-f]{16}$"
          description: "Projection configuration identity associated with the durable checkpoint."
        checkpoint_replay_tail_sequence_count:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Number of derived-log sequences after the durable checkpoint that still need replay."
        repair_scan_issue_count:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Repair issues found by explicit repair-scan accounting for this projection."
        edge_count:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        node_count:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        repair_degraded:
          x-go-type-skip-optional-pointer: true
          type: boolean
        repair_issue_count:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        repair_summary_ready:
          x-go-type-skip-optional-pointer: true
          type: boolean
        repair_issue_count_estimated:
          x-go-type-skip-optional-pointer: true
          type: boolean
        expected_groups:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        reported_groups:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        fresh_groups:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        stale_groups:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        missing_groups:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        unknown_remote_groups:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        source_artifact:
          x-go-type-skip-optional-pointer: true
          type: object
          additionalProperties: true
          description: "Source artifact stream used to materialize graph edges."
        resolver_replay:
          x-go-type-skip-optional-pointer: true
          type: object
          additionalProperties: true
          description: "Graph resolver replay diagnostics."
        resolution:
          x-go-type-skip-optional-pointer: true
          type: object
          additionalProperties: true
          description: "Artifact resolution replay diagnostics."
        promotion:
          x-go-type-skip-optional-pointer: true
          type: object
          additionalProperties: true
          description: "Artifact promotion replay diagnostics."
    MergeStrategy:
      type: string
      enum:
        - rrf
        - rsf
        - failover
      default: rrf
      description: |
        Merge strategy for combining results from the semantic_search and full_text_search.
        rrf: Reciprocal Rank Fusion - combines scores using reciprocal rank formula
        rsf: Relative Score Fusion - normalizes scores by min/max within a window and combines weighted scores
        failover: Use full_text_search if embedding generation fails
    MergeConfig:
      type: object
      description: Configuration for result fusion when combining multiple search indexes.
      properties:
        strategy:
          $ref: "#/components/schemas/MergeStrategy"
        weights:
          type: object
          additionalProperties:
            type: number
            format: double
          description: |
            Named weights keyed by index name. `full_text` for the full-text search index;
            embedding index names for vector indexes.
            Unspecified indexes default to 1.0. Applied in both RRF and RSF.
          example:
            full_text: 0.3
            title_embedding: 1.0
        window_size:
          x-go-type-skip-optional-pointer: true
          type: integer
          minimum: 1
          description: RSF normalization window size. Defaults to `limit`.
        rank_constant:
          x-go-type-skip-optional-pointer: true
          type: number
          format: double
          description: RRF k constant (1/(k+rank)). Defaults to 60.0.
    Pruner:
      type: object
      description: |
        Configuration for pruning search results based on score quality.
        Helps filter out low-relevance results in RAG pipelines by detecting
        score gaps or deviations from top results.
      properties:
        min_score_ratio:
          x-go-type-skip-optional-pointer: true
          type: number
          format: double
          description: |
            Keep only results with score >= max_score * min_score_ratio.
            For example, 0.5 keeps results scoring at least half of the top result.
            Applied after fusion scoring.
          example: 0.5
        max_score_gap_percent:
          x-go-type-skip-optional-pointer: true
          type: number
          format: double
          description: |
            Stop returning results when the gap between consecutive scores
            exceeds this percentage of the total score range (max - min).
            Detects "elbows" in score distributions regardless of score scale.
            For example, 30.0 stops when a gap spans 30% of the score range.
          example: 30.0
        min_absolute_score:
          x-go-type-skip-optional-pointer: true
          type: number
          format: double
          description: |
            Hard minimum score threshold. Results with scores below this value
            are excluded regardless of other pruning settings.
          example: 0.01
        require_multi_index:
          x-go-type-skip-optional-pointer: true
          type: boolean
          description: |
            Only keep results that appear in multiple indexes (both full-text
            and vector search). Useful for increasing precision by requiring
            agreement between different retrieval methods.
          default: false
        std_dev_threshold:
          x-go-type-skip-optional-pointer: true
          type: number
          format: double
          description: |
            Keep results within N standard deviations below the mean score.
            For example, 1.0 keeps results with score >= mean - 1*stddev.
            Useful for statistical outlier detection in result sets.
          example: 1.5
    DistanceMetric:
      x-go-type-skip-optional-pointer: true
      type: string
      enum: [l2_squared, inner_product, cosine]
      default: l2_squared
      description: >-
        Distance metric for the vector index (dense only).
        Use "cosine" for models trained with cosine similarity (e.g. CLIP, OpenAI).
        Use "inner_product" for models trained with dot product similarity.
        Use "l2_squared" (default) for models trained with Euclidean distance.
    AntflyType:
      type: string
      enum:
        - search_as_you_type
        - keyword
        - text
        - html
        - numeric
        - datetime
        - boolean
        - link
        - geopoint
        - geoshape
        - embedding
        - blob
    GraphIndexConfig:
      type: object
      description: "Configuration for graph index type"
      x-antfly-mutually-exclusive:
        - [sources, source]
      properties:
        sources:
          type: array
          minItems: 1
          maxItems: 64
          uniqueItems: true
          items:
            $ref: "#/components/schemas/GraphArtifactSourceConfig"
          x-go-type-skip-optional-pointer: true
          description: "Ordered chunk or JSON asset streams whose edge-like values are unioned into this graph index. Artifact names must be unique within the array because the artifact name is the source identity. Earlier sources win when multiple sources materialize the same edge identity. Requires index_capabilities.artifact_sources=true and is rejected by serverless deployments."
        summarizer:
          description: "Configuration for generating node summaries (enables tree navigation in Retrieval Agent)"
          $ref: "../shared/generating.yaml#/components/schemas/GeneratorConfig"
        template:
          x-go-type-skip-optional-pointer: true
          type: string
          description: |
            Handlebars template for generating summarizer input text.
            Uses document fields as template variables.
            Same pattern as EmbeddingsConfig template.
          example: "{{title}}\n{{content}}"
        edge_types:
          type: array
          description: "List of edge types with their configurations"
          items:
            $ref: "#/components/schemas/EdgeTypeConfig"
        max_edges_per_document:
          x-go-type-skip-optional-pointer: true
          type: integer
          minimum: 0
          maximum: 1000000
          description: "Maximum number of distinct visible edges materialized per document after source precedence and identity deduplication. Zero uses the server safety limit (currently 1,000,000). Independent aggregate reconciliation budgets bound work across overlapping source manifests."
        source:
          description: "Single-source convenience form. Mutually exclusive with sources; normalized responses use sources. Requires index_capabilities.artifact_sources=true and is rejected by serverless deployments."
          $ref: "#/components/schemas/GraphArtifactSourceConfig"
        artifact:
          $ref: "#/components/schemas/GraphArtifactProducerConfig"
          description: "Single asset-producer shorthand. It must be paired with exactly one source selecting the same artifact name."
        algebraic_planning:
          $ref: "#/components/schemas/GraphAlgebraicPlanningConfig"
        resolvers:
          type: array
          items:
            $ref: "#/components/schemas/GraphResolverConfig"
    GraphEdgeType:
      type: string
      minLength: 1
      maxLength: 65536
      x-antfly-max-utf8-bytes: 65536
      description: >-
        Durable graph edge type. Values must be valid UTF-8 and encode to at
        most 64 KiB; `maxLength` is the standard-schema code-point ceiling and
        `x-antfly-max-utf8-bytes` carries the exact wire-byte limit.
    EdgeTypeConfig:
      type: object
      description: "Configuration for a specific edge type"
      properties:
        name:
          $ref: '#/components/schemas/GraphEdgeType'
        field:
          x-go-type-skip-optional-pointer: true
          type: string
          description: |
            Document field containing target node key(s) for automatic edge creation.
            Supports string (single target) or array of strings (multiple targets).
            When omitted, edges must be provided explicitly via _edges.
        topology:
          x-go-type-skip-optional-pointer: true
          type: string
          enum:
            - tree
            - graph
          default: graph
          description: |
            Topology constraint for this edge type:
            - tree: Single parent per node, no cycles
            - graph: No constraints (default)
        max_weight:
          x-go-type-skip-optional-pointer: true
          type: number
          format: double
          default: 1.0
          description: "Maximum allowed edge weight"
        min_weight:
          x-go-type-skip-optional-pointer: true
          type: number
          format: double
          default: 0.0
          description: "Minimum allowed edge weight"
        allow_self_loops:
          x-go-type-skip-optional-pointer: true
          type: boolean
          default: true
          description: "Whether to allow edges from a node to itself"
        required_metadata:
          type: array
          description: "Required metadata fields for this edge type"
          items:
            type: string
      required:
        - name
    GraphIndexStats:
      type: object
      description: "Statistics for graph index"
      required:
        - index_type
      properties:
        index_type:
          x-go-type-skip-optional-pointer: true
          type: string
          enum:
            - graph
          description: "Discriminator for the index stats variant."
        readiness:
          $ref: '#/components/schemas/IndexReadinessStatus'
        error:
          x-go-type-skip-optional-pointer: true
          type: string
          description: "Error message if stats could not be retrieved"
        total_edges:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Total number of edges in the graph"
        edge_types:
          type: object
          additionalProperties:
            type: integer
            format: uint64
          description: "Count of edges per edge type"
        rebuilding:
          x-go-type-skip-optional-pointer: true
          type: boolean
          description: "Whether the index is currently rebuilding"
        repair:
          $ref: '#/components/schemas/IndexRepairStatus'
        backfill_active:
          x-go-type-skip-optional-pointer: true
          type: boolean
          description: "Whether the index is actively rebuilding, materializing, or catching up."
        backfill_progress:
          x-go-type-skip-optional-pointer: true
          type: number
          format: double
          description: "Rebuild progress as a ratio from 0.0 to 1.0"
        backfill_items_processed:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Number of edges indexed during current rebuild"
        backfill_state:
          x-go-type-skip-optional-pointer: true
          type: string
          description: "Operational readiness state such as ready, running, retrying, degraded, or failed."
        doc_count:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Number of documents covered by the graph index."
        edge_count:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Number of graph edges currently indexed."
        node_count:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Number of graph nodes currently indexed."
        replay_applied_sequence:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        replay_target_sequence:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        replay_catch_up_required:
          x-go-type-skip-optional-pointer: true
          type: boolean
        runtime_present:
          x-go-type-skip-optional-pointer: true
          type: boolean
        runtime_fresh:
          x-go-type-skip-optional-pointer: true
          type: boolean
        runtime_source:
          x-go-type-skip-optional-pointer: true
          type: string
        runtime_freshness:
          x-go-type-skip-optional-pointer: true
          type: string
        catch_up_active:
          x-go-type-skip-optional-pointer: true
          type: boolean
        catch_up_phase:
          x-go-type-skip-optional-pointer: true
          type: string
        catch_up_applied_sequence:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        catch_up_target_sequence:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        source_artifact:
          x-go-type-skip-optional-pointer: true
          type: object
          additionalProperties: true
          description: "Graph source artifact materialization status."
        resolver_replay:
          x-go-type-skip-optional-pointer: true
          type: object
          additionalProperties: true
          description: "Resolver replay diagnostics for graph materialization."
        async_indexing:
          x-go-type-skip-optional-pointer: true
          type: object
          additionalProperties: true
        projection_checkpoint_status:
          x-go-type-skip-optional-pointer: true
          type: string
          description: "Durable projection checkpoint status: clean, rebuilding, degraded, or repair_required."
        projection_checkpoint_applied_sequence:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Highest derived-log sequence covered by the durable projection checkpoint."
        projection_checkpoint_generation:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Projection generation associated with the durable checkpoint."
        projection_checkpoint_config_fingerprint:
          x-go-type-skip-optional-pointer: true
          type: string
          pattern: "^[0-9a-f]{16}$"
          description: "Projection configuration identity associated with the durable checkpoint."
        checkpoint_replay_tail_sequence_count:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Number of derived-log sequences after the durable checkpoint that still need replay."
        repair_scan_issue_count:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
          description: "Repair issues found by explicit repair-scan accounting for this projection."
        term_count:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        repair_degraded:
          x-go-type-skip-optional-pointer: true
          type: boolean
        repair_issue_count:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        repair_summary_ready:
          x-go-type-skip-optional-pointer: true
          type: boolean
        repair_issue_count_estimated:
          x-go-type-skip-optional-pointer: true
          type: boolean
        expected_groups:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        reported_groups:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        fresh_groups:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        stale_groups:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        missing_groups:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        unknown_remote_groups:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: uint64
        resolution:
          x-go-type-skip-optional-pointer: true
          type: object
          additionalProperties: true
          description: "Artifact resolution replay diagnostics."
        promotion:
          x-go-type-skip-optional-pointer: true
          type: object
          additionalProperties: true
          description: "Artifact promotion replay diagnostics."
        algebraic_graph:
          x-go-type-skip-optional-pointer: true
          type: object
          description: "Algebraic graph execution health for bounded semiring traversal."
          properties:
            traversal:
              type: object
              properties:
                attempted:
                  type: integer
                  format: uint64
                proven:
                  type: integer
                  format: uint64
                rejected:
                  type: integer
                  format: uint64
                fallback:
                  type: integer
                  format: uint64
                result_nodes:
                  type: integer
                  format: uint64
    Edge:
      x-go-type-skip-optional-pointer: true
      type: object
      description: "A typed, weighted connection between documents"
      required:
        - source
        - target
        - type
        - weight
      properties:
        source:
          x-go-type-skip-optional-pointer: true
          type: string
          format: byte
          description: Base64-encoded source document key
        target:
          x-go-type-skip-optional-pointer: true
          type: string
          format: byte
          description: Base64-encoded target document key
        type:
          x-go-type-skip-optional-pointer: true
          $ref: '#/components/schemas/GraphEdgeType'
        weight:
          x-go-type-skip-optional-pointer: true
          type: number
          format: double
          minimum: 0
          description: Finite non-negative edge cost or confidence. The max_weight_product path objective additionally requires values in [0,1].
        created_at:
          x-go-type-skip-optional-pointer: true
          type: string
          format: date-time
          description: When the edge was created
        updated_at:
          x-go-type-skip-optional-pointer: true
          type: string
          format: date-time
          description: When the edge was last updated
        metadata:
          x-go-type-skip-optional-pointer: true
          type: object
          additionalProperties: true
          description: Optional edge metadata
    EdgeDirection:
      x-go-type-skip-optional-pointer: true
      type: string
      enum:
        - out
        - in
        - both
      default: out
      description: |
        Direction of edges to query:
        - out: Outgoing edges from the node
        - in: Incoming edges to the node
        - both: Both outgoing and incoming edges
    GraphPathEdgeDirection:
      type: string
      enum: [out, in]
      description: >-
        Physical stored-edge orientation relative to this path edge's `from`
        endpoint. `out` means the stored relationship points from `from` to
        `to`; `in` means the path traversed a relationship stored from `to` to
        `from`. This keeps paths lossless when a `both` query encounters
        reciprocal relationships.
    PathWeightMode:
      x-go-type-skip-optional-pointer: true
      type: string
      enum:
        - min_hops
        - min_weight
        - max_weight
      description: |
        Path weighting algorithm for pathfinding:
        - min_hops: Minimize number of edges
        - min_weight: Minimize sum of finite non-negative edge weights
        - max_weight: Maximize product of finite edge weights in [0,1]
    TraversalRules:
      x-go-type-skip-optional-pointer: true
      type: object
      description: "Rules for graph traversal"
      properties:
        edge_types:
          x-go-type-skip-optional-pointer: true
          type: array
          items:
            type: string
          description: Filter edges by type (empty = all types)
        min_weight:
          x-go-type-skip-optional-pointer: true
          type: number
          format: double
          default: 0.0
          description: Minimum edge weight filter
        max_weight:
          x-go-type-skip-optional-pointer: true
          type: number
          format: double
          default: 1.0
          description: Maximum edge weight filter
        direction:
          $ref: "#/components/schemas/EdgeDirection"
        max_depth:
          x-go-type-skip-optional-pointer: true
          type: integer
          default: 3
          description: Maximum traversal depth (0 = unlimited)
        max_results:
          x-go-type-skip-optional-pointer: true
          type: integer
          default: 100
          description: Maximum results to return (0 = unlimited)
        include_paths:
          x-go-type-skip-optional-pointer: true
          type: boolean
          default: false
          description: Include path information in results
        deduplicate_nodes:
          x-go-type-skip-optional-pointer: true
          type: boolean
          default: true
          description: Visit each node only once
    TraversalResult:
      x-go-type-skip-optional-pointer: true
      type: object
      description: "A single result from graph traversal"
      required:
        - key
        - depth
      properties:
        key:
          x-go-type-skip-optional-pointer: true
          type: string
          format: byte
          description: Base64-encoded document key
        document:
          x-go-type-skip-optional-pointer: true
          type: object
          additionalProperties: true
          description: Document data (if loaded)
        depth:
          x-go-type-skip-optional-pointer: true
          type: integer
          description: Distance from start node (0 = start node)
        path:
          x-go-type-skip-optional-pointer: true
          type: array
          items:
            type: string
            format: byte
          description: Sequence of keys from start to this node (if include_paths=true)
        path_edges:
          x-go-type-skip-optional-pointer: true
          type: array
          items:
            $ref: "#/components/schemas/Edge"
          description: Sequence of edges from start to this node (if include_paths=true)
        total_weight:
          x-go-type-skip-optional-pointer: true
          type: number
          format: double
          description: Sum of raw edge weights along the path. Path ordering still follows the selected weight mode.
    PathFindWeightMode:
      x-go-type-skip-optional-pointer: true
      type: string
      enum:
        - min_hops
        - max_weight
        - min_weight
      default: min_hops
      description: |-
        Algorithm for path finding:
        - min_hops: Shortest path by hop count (breadth-first search, ignores weights)
        - max_weight: Path with maximum product of edge weights (strongest connection chain)
        - min_weight: Path with minimum sum of edge weights (lowest cost route)
    PathFindRequest:
      x-go-type-skip-optional-pointer: true
      type: object
      required:
        - source
        - target
      properties:
        source:
          x-go-type-skip-optional-pointer: true
          type: string
          description: Source node key (base64-encoded)
        target:
          x-go-type-skip-optional-pointer: true
          type: string
          description: Target node key (base64-encoded)
        edge_types:
          x-go-type-skip-optional-pointer: true
          type: array
          items:
            type: string
          description: Filter by specific edge types
        max_depth:
          x-go-type-skip-optional-pointer: true
          type: integer
          default: 10
          minimum: 1
          maximum: 50
        weight_mode:
          $ref: "#/components/schemas/PathFindWeightMode"
        k:
          x-go-type-skip-optional-pointer: true
          type: integer
          default: 1
          minimum: 1
          maximum: 100
        min_weight:
          x-go-type-skip-optional-pointer: true
          type: number
          format: double
        max_weight:
          x-go-type-skip-optional-pointer: true
          type: number
          format: double
        direction:
          $ref: "#/components/schemas/EdgeDirection"
    PathFindResult:
      x-go-type-skip-optional-pointer: true
      type: object
      properties:
        paths:
          x-go-type-skip-optional-pointer: true
          type: array
          items:
            $ref: "#/components/schemas/Path"
        source:
          x-go-type-skip-optional-pointer: true
          type: string
        target:
          x-go-type-skip-optional-pointer: true
          type: string
        weight_mode:
          $ref: "#/components/schemas/PathFindWeightMode"
        paths_found:
          x-go-type-skip-optional-pointer: true
          type: integer
        search_time_ms:
          x-go-type-skip-optional-pointer: true
          type: number
          format: double
    Path:
      x-go-type-skip-optional-pointer: true
      type: object
      properties:
        nodes:
          x-go-type-skip-optional-pointer: true
          type: array
          items:
            type: string
          description: Ordered list of node keys (base64-encoded)
        edges:
          x-go-type-skip-optional-pointer: true
          type: array
          items:
            $ref: "#/components/schemas/PathEdge"
        total_weight:
          x-go-type-skip-optional-pointer: true
          type: number
          format: double
          description: Sum of raw edge weights along the path. Path ordering still follows the selected weight mode.
        length:
          x-go-type-skip-optional-pointer: true
          type: integer
    PathEdge:
      x-go-type-skip-optional-pointer: true
      type: object
      properties:
        source:
          x-go-type-skip-optional-pointer: true
          type: string
        target:
          x-go-type-skip-optional-pointer: true
          type: string
        type:
          x-go-type-skip-optional-pointer: true
          type: string
        weight:
          x-go-type-skip-optional-pointer: true
          type: number
          format: double
        metadata:
          x-go-type-skip-optional-pointer: true
          type: object
          additionalProperties: true
    GraphPathEdge:
      type: object
      additionalProperties: false
      required: [from, to, direction, type, weight]
      description: >-
        One edge in a canonical path. `from` and `to` are the exact ordered
        traversal endpoints, not unqualified physical edge keys, so identity
        remains unambiguous across tables and for equal keys in different
        tables.
      properties:
        from: { $ref: '#/components/schemas/GraphPathEndpoint' }
        to: { $ref: '#/components/schemas/GraphPathEndpoint' }
        direction: { $ref: '#/components/schemas/GraphPathEdgeDirection' }
        type:
          $ref: '#/components/schemas/GraphEdgeType'
        weight:
          type: number
          format: double
          minimum: 0
          description: Finite durable edge weight. max_weight_product paths further require values in [0,1].
        metadata:
          type: object
          additionalProperties: true
    NodeFilter:
      x-go-type-skip-optional-pointer: true
      type: object
      description: "Filter nodes during graph traversal using existing query primitives"
      properties:
        filter_query:
          x-go-type-skip-optional-pointer: true
          $ref: "#/components/schemas/LegacyGraphDocumentQuery"
          description: "Antfly query to filter nodes (same syntax as search filter_query)"
        filter_prefix:
          x-go-type-skip-optional-pointer: true
          type: string
          description: "Filter by key prefix"
    LegacyGraphNodeSelector:
      x-go-type-skip-optional-pointer: true
      type: object
      additionalProperties: false
      deprecated: true
      description: "Deprecated v0.2 graph selector. Unqualified target keys match any reachable table."
      properties:
        keys:
          x-go-type-skip-optional-pointer: true
          type: array
          items:
            type: string
          description: "Legacy list of node keys. Target keys match any reachable table."
        identities:
          x-go-type-skip-optional-pointer: true
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/GraphPathEndpoint'
          description: "Exact node identities. Omitted table means the query table."
        result_ref:
          x-go-type-skip-optional-pointer: true
          type: string
          description: |
            Reference to search results to use as nodes:
            - "$full_text_results" - use full-text search results
            - "$embeddings_results" - use merged vector search results
            - "$fused_results" - use fused retrieval results
            - "$graph_results.<query-name>" - use a prior graph query result
        limit:
          x-go-type-skip-optional-pointer: true
          type: integer
          description: "Maximum number of nodes to select from result_ref; invalid with keys or identities."
        node_filter:
          $ref: "#/components/schemas/NodeFilter"
          description: "Filter which nodes to use as start/target"
    GraphKeyNodeSelector:
      type: object
      additionalProperties: false
      required: [keys]
      properties:
        keys:
          type: array
          minItems: 1
          maxItems: 10000
          uniqueItems: true
          items: { type: string, minLength: 1 }
          description: Exact keys in the table being queried.
    GraphIdentityNodeSelector:
      type: object
      additionalProperties: false
      required: [identities]
      properties:
        identities:
          type: array
          minItems: 1
          maxItems: 10000
          uniqueItems: true
          items: { $ref: '#/components/schemas/GraphPathEndpoint' }
          description: Exact node identities. Omitted table means the query table.
    GraphResultRefNodeSelector:
      description: >-
        Select nodes from a prior graph result. `binding` is valid only when
        `result_ref` names a prior MATCH result; traversal and path results
        select their returned or endpoint nodes and prohibit `binding`.
      type: object
      additionalProperties: false
      required: [result_ref]
      properties:
        result_ref:
          type: string
          x-antfly-graph-result-name-schema:
            $ref: 'generated/graph_identifier.yaml#/components/schemas/GraphIdentifier'
          description: >-
            `$query_results` selects the final ranked query results.
            `$graph_results.<query-name>` selects a prior graph query result.
            Prior MATCH results require `binding`; traversal and path results
            prohibit it. A path result selects the endpoint node of each
            returned path.
        binding:
          $ref: 'generated/graph_identifier.yaml#/components/schemas/GraphIdentifier'
        limit:
          type: integer
          minimum: 1
          maximum: 10000
          description: Maximum referenced results to use. Omit only when the referenced result is complete.
    GraphNodeSelector:
      oneOf:
        - $ref: '#/components/schemas/GraphKeyNodeSelector'
        - $ref: '#/components/schemas/GraphIdentityNodeSelector'
        - $ref: '#/components/schemas/GraphResultRefNodeSelector'
      description: Select graph nodes using exactly one explicit, exact selector form.
    GraphQueryType:
      x-go-type-skip-optional-pointer: true
      type: string
      enum: [traverse, neighbors, shortest_path, k_shortest_paths, pattern]
      description: Deprecated discriminator used by LegacyGraphQuery.
    GraphQueryParams:
      x-go-type-skip-optional-pointer: true
      type: object
      deprecated: true
      description: Deprecated graph_searches traversal and path parameters.
      properties:
        edge_types:
          x-go-type-skip-optional-pointer: true
          type: array
          maxItems: 64
          uniqueItems: true
          items: { $ref: '#/components/schemas/GraphEdgeType' }
          description: At most 64 unique edge types totaling at most 64 KiB.
        direction: { $ref: "#/components/schemas/EdgeDirection" }
        max_depth:
          x-go-type-skip-optional-pointer: true
          type: integer
          minimum: 1
          maximum: 64
        min_weight:
          x-go-type-skip-optional-pointer: true
          type: number
          format: double
        max_weight:
          x-go-type-skip-optional-pointer: true
          type: number
          format: double
        max_results:
          x-go-type-skip-optional-pointer: true
          type: integer
          minimum: 1
          maximum: 10000
        deduplicate_nodes:
          x-go-type-skip-optional-pointer: true
          type: boolean
        include_paths:
          x-go-type-skip-optional-pointer: true
          type: boolean
        weight_mode: { $ref: "#/components/schemas/PathWeightMode" }
        k:
          x-go-type-skip-optional-pointer: true
          type: integer
          minimum: 1
          maximum: 100
        node_filter: { $ref: "#/components/schemas/NodeFilter" }
        algorithm:
          x-go-type-skip-optional-pointer: true
          type: string
        algorithm_params:
          x-go-type-skip-optional-pointer: true
          type: object
          additionalProperties: true
    PatternEdgeStep:
      x-go-type-skip-optional-pointer: true
      type: object
      deprecated: true
      description: Deprecated linear graph_searches pattern edge.
      properties:
        types:
          x-go-type-skip-optional-pointer: true
          type: array
          maxItems: 64
          uniqueItems: true
          items: { $ref: '#/components/schemas/GraphEdgeType' }
          description: Empty or omitted matches every edge type; otherwise at most 64 unique types totaling at most 64 KiB.
        direction: { $ref: "#/components/schemas/EdgeDirection" }
        min_hops:
          x-go-type-skip-optional-pointer: true
          type: integer
          default: 1
          minimum: 1
        max_hops:
          x-go-type-skip-optional-pointer: true
          type: integer
          default: 1
          minimum: 1
        min_weight:
          x-go-type-skip-optional-pointer: true
          type: number
          format: double
        max_weight:
          x-go-type-skip-optional-pointer: true
          type: number
          format: double
    PatternStep:
      x-go-type-skip-optional-pointer: true
      type: object
      deprecated: true
      description: Deprecated linear graph_searches pattern step.
      properties:
        alias:
          x-go-type-skip-optional-pointer: true
          type: string
        node_filter: { $ref: "#/components/schemas/NodeFilter" }
        edge: { $ref: "#/components/schemas/PatternEdgeStep" }
    LegacyGraphQuery:
      x-go-type-skip-optional-pointer: true
      type: object
      deprecated: true
      description: Deprecated graph_searches request. Use the operation-keyed GraphQuery DSL.
      required: [type, index_name]
      properties:
        type: { $ref: "#/components/schemas/GraphQueryType" }
        index_name:
          x-go-type-skip-optional-pointer: true
          type: string
        start_nodes: { $ref: '#/components/schemas/LegacyGraphNodeSelector' }
        target_nodes: { $ref: '#/components/schemas/LegacyGraphNodeSelector' }
        params: { $ref: '#/components/schemas/GraphQueryParams' }
        pattern:
          x-go-type-skip-optional-pointer: true
          type: array
          items: { $ref: '#/components/schemas/PatternStep' }
        return_aliases:
          x-go-type-skip-optional-pointer: true
          type: array
          items: { type: string }
        include_documents:
          x-go-type-skip-optional-pointer: true
          type: boolean
        include_edges:
          x-go-type-skip-optional-pointer: true
          type: boolean
        fields:
          x-go-type-skip-optional-pointer: true
          type: array
          items: { type: string }
    LegacyGraphDocumentQuery:
      type: object
      additionalProperties: true
      description: >-
        Deprecated free-form graph filter accepted by the v0.2 compatibility contract.
    GraphDocumentTermFilter:
      type: object
      additionalProperties: false
      required: [term, path]
      properties:
        term: { type: string }
        path:
          type: string
          minLength: 1
          pattern: '^/'
          description: RFC 6901 JSON Pointer to the stored-document value.
    GraphDocumentFuzzyFilter:
      type: object
      additionalProperties: false
      required: [term, path, fuzziness]
      properties:
        term: { type: string }
        path:
          type: string
          minLength: 1
          pattern: '^/'
          description: RFC 6901 JSON Pointer to the stored-document value.
        fuzziness:
          $ref: 'query.yaml#/components/schemas/Fuzziness'
          description: Required so fuzzy and exact term predicates remain structurally distinct.
        prefix_length:
          type: integer
          format: int32
          minimum: 0
          maximum: 255
    GraphDocumentPrefixFilter:
      type: object
      additionalProperties: false
      required: [prefix, path]
      properties:
        prefix: { type: string }
        path:
          type: string
          minLength: 1
          pattern: '^/'
          description: RFC 6901 JSON Pointer to the stored-document value.
    GraphDocumentRegexpFilter:
      type: object
      additionalProperties: false
      required: [regexp, path]
      properties:
        regexp: { type: string }
        path:
          type: string
          minLength: 1
          pattern: '^/'
          description: RFC 6901 JSON Pointer to the stored-document value.
    GraphDocumentWildcardFilter:
      type: object
      additionalProperties: false
      required: [wildcard, path]
      properties:
        wildcard: { type: string }
        path:
          type: string
          minLength: 1
          pattern: '^/'
          description: RFC 6901 JSON Pointer to the stored-document value.
    GraphDocumentNumericRangeBody:
      type: object
      additionalProperties: false
      required: [path]
      # OpenAPI 3.0 code generators commonly turn a validation-only anyOf into
      # an unusable model union. This equivalent boolean schema keeps the
      # object model stable while requiring min or max for standard validators.
      not:
        allOf:
          - not: { required: [min] }
          - not: { required: [max] }
      description: >-
        At least one of min or max is required and enforced by every Antfly
        execution boundary. When both are present, min must not exceed max.
      properties:
        path:
          type: string
          minLength: 1
          pattern: '^/'
          description: RFC 6901 JSON Pointer to the stored-document value.
        min:
          type: number
          format: double
          x-go-type-skip-optional-pointer: false
        max:
          type: number
          format: double
          x-go-type-skip-optional-pointer: false
        inclusive_min:
          type: boolean
          default: true
          x-go-type-skip-optional-pointer: false
        inclusive_max:
          type: boolean
          default: false
          x-go-type-skip-optional-pointer: false
    GraphDocumentNumericRangeFilter:
      type: object
      additionalProperties: false
      required: [numeric_range]
      properties:
        numeric_range: { $ref: '#/components/schemas/GraphDocumentNumericRangeBody' }
    GraphDocumentTermRangeBody:
      type: object
      additionalProperties: false
      required: [path]
      not:
        allOf:
          - not: { required: [min] }
          - not: { required: [max] }
      description: At least one of min or max is required and enforced by every Antfly execution boundary.
      properties:
        path:
          type: string
          minLength: 1
          pattern: '^/'
          description: RFC 6901 JSON Pointer to the stored-document value.
        min:
          type: string
          x-go-type-skip-optional-pointer: false
        max:
          type: string
          x-go-type-skip-optional-pointer: false
        inclusive_min:
          type: boolean
          default: true
          x-go-type-skip-optional-pointer: false
        inclusive_max:
          type: boolean
          default: false
          x-go-type-skip-optional-pointer: false
    GraphDocumentTermRangeFilter:
      type: object
      additionalProperties: false
      required: [term_range]
      properties:
        term_range: { $ref: '#/components/schemas/GraphDocumentTermRangeBody' }
    GraphDocumentDateRangeBody:
      type: object
      additionalProperties: false
      required: [path]
      not:
        allOf:
          - not: { required: [start] }
          - not: { required: [end] }
      description: >-
        At least one of start or end is required and enforced by every Antfly
        execution boundary. Bounds are RFC 3339 instants in Antfly's unsigned
        Unix-nanosecond domain, from 1970-01-01T00:00:00Z through
        2554-07-21T23:34:33.709551615Z inclusive. When both are present,
        start must not exceed end after offset normalization.
      properties:
        path:
          type: string
          minLength: 1
          pattern: '^/'
          description: RFC 6901 JSON Pointer to the stored-document value.
        start:
          type: string
          format: date-time
          x-go-type-skip-optional-pointer: false
        end:
          type: string
          format: date-time
          x-go-type-skip-optional-pointer: false
        inclusive_start:
          type: boolean
          default: true
          x-go-type-skip-optional-pointer: false
        inclusive_end:
          type: boolean
          default: false
          x-go-type-skip-optional-pointer: false
    GraphDocumentDateRangeFilter:
      type: object
      additionalProperties: false
      required: [date_range]
      properties:
        date_range: { $ref: '#/components/schemas/GraphDocumentDateRangeBody' }
    GraphDocumentMatchAllFilter:
      type: object
      additionalProperties: false
      required: [match_all]
      properties:
        match_all: { type: object, additionalProperties: false }
    GraphDocumentMatchNoneFilter:
      type: object
      additionalProperties: false
      required: [match_none]
      properties:
        match_none: { type: object, additionalProperties: false }
    GraphDocumentIdsFilter:
      type: object
      additionalProperties: false
      required: [ids]
      properties:
        ids:
          type: array
          minItems: 1
          maxItems: 10000
          uniqueItems: true
          items: { type: string }
    GraphDocumentBoolFieldBody:
      type: object
      additionalProperties: false
      required: [path, value]
      properties:
        path:
          type: string
          minLength: 1
          pattern: '^/'
          description: RFC 6901 JSON Pointer to the stored-document value.
        value: { type: boolean }
    GraphDocumentBoolFieldFilter:
      type: object
      additionalProperties: false
      required: [bool_field]
      properties:
        bool_field: { $ref: '#/components/schemas/GraphDocumentBoolFieldBody' }
    GraphDocumentFilterBoolean:
      type: object
      additionalProperties: false
      minProperties: 1
      description: >-
        Stored-document boolean predicate. `must` and `filter` are required
        clauses; `should` uses its disjunction threshold; and `must_not`
        negates its thresholded disjunction as one group. Thus a
        `must_not.min` of N excludes a document only when at least N of that
        group's clauses match.
      properties:
        must:
          $ref: '#/components/schemas/GraphDocumentFilterConjunction'
        should:
          $ref: '#/components/schemas/GraphDocumentFilterDisjunction'
        must_not:
          $ref: '#/components/schemas/GraphDocumentFilterDisjunction'
        filter:
          $ref: '#/components/schemas/GraphDocumentFilter'
    GraphDocumentFilterConjunction:
      type: object
      additionalProperties: false
      required: [conjuncts]
      properties:
        conjuncts:
          type: array
          minItems: 1
          maxItems: 64
          items: { $ref: '#/components/schemas/GraphDocumentFilter' }
    GraphDocumentFilterDisjunction:
      type: object
      additionalProperties: false
      required: [disjuncts]
      properties:
        disjuncts:
          type: array
          minItems: 1
          maxItems: 64
          items: { $ref: '#/components/schemas/GraphDocumentFilter' }
        min:
          type: integer
          format: uint32
          minimum: 0
          maximum: 64
          x-go-type-skip-optional-pointer: false
          description: >-
            Minimum number of disjuncts that must match. Omit for conventional
            context-sensitive disjunction semantics; set to 0 to impose no
            matching-clause requirement. Under `must_not`, the complete
            thresholded disjunction is negated as one group.
    GraphDocumentFilter:
      oneOf:
        - $ref: '#/components/schemas/GraphDocumentFuzzyFilter'
        - $ref: '#/components/schemas/GraphDocumentTermFilter'
        - $ref: '#/components/schemas/GraphDocumentPrefixFilter'
        - $ref: '#/components/schemas/GraphDocumentRegexpFilter'
        - $ref: '#/components/schemas/GraphDocumentWildcardFilter'
        - $ref: '#/components/schemas/GraphDocumentNumericRangeFilter'
        - $ref: '#/components/schemas/GraphDocumentTermRangeFilter'
        - $ref: '#/components/schemas/GraphDocumentDateRangeFilter'
        - $ref: '#/components/schemas/GraphDocumentMatchAllFilter'
        - $ref: '#/components/schemas/GraphDocumentMatchNoneFilter'
        - $ref: '#/components/schemas/GraphDocumentIdsFilter'
        - $ref: '#/components/schemas/GraphDocumentBoolFieldFilter'
        - $ref: '#/components/schemas/GraphDocumentFilterBoolean'
        - $ref: '#/components/schemas/GraphDocumentFilterConjunction'
        - $ref: '#/components/schemas/GraphDocumentFilterDisjunction'
      description: >-
        A non-scoring stored-document predicate embedded at a graph node. It
        uses structurally distinct stored-field predicates and deliberately
        excludes analyzer-backed full-text clauses such as match, phrase,
        multi_match, and query_string. Fuzzy predicates require an explicit
        fuzziness. Range predicates use numeric_range, term_range, or date_range
        wrappers,
        and every stored value is addressed by an RFC 6901 JSON Pointer in `path`.
        Alias-to-alias predicates belong in GraphMatch.where.
    GraphPathObjective:
      type: string
      enum: [min_hops, min_weight_sum, max_weight_product]
      description: |
        Objective used to rank graph paths:
        - min_hops: Minimize the number of edges.
        - min_weight_sum: Minimize the sum of finite non-negative edge weights.
        - max_weight_product: Maximize the product of edge weights, requiring every traversed weight to be in [0,1].
    GraphEdgeWeightRange:
      type: object
      additionalProperties: false
      minProperties: 1
      description: >-
        Inclusive per-edge weight filter. At least one bound is required. Bounds
        must be finite and non-negative; when both are present, min must not
        exceed max. This filters individual stored edges and does not constrain
        the aggregate path objective.
      properties:
        min: { type: number, format: double, minimum: 0, x-go-type-skip-optional-pointer: false }
        max: { type: number, format: double, minimum: 0, x-go-type-skip-optional-pointer: false }
    GraphTraversal:
      type: object
      additionalProperties: false
      required: [start]
      description: >-
        Breadth-first traversal with request-wide deduplication by exact
        table-qualified node identity. Direction defaults to `out`; use `both`
        to traverse a relationship as undirected without storing a reciprocal
        edge.
      properties:
        start:
          $ref: '#/components/schemas/GraphNodeSelector'
        direction:
          $ref: '#/components/schemas/EdgeDirection'
          description: Stored-edge direction relative to each expanded node; defaults to `out`.
        edge_types:
          type: array
          maxItems: 64
          uniqueItems: true
          items: { $ref: '#/components/schemas/GraphEdgeType' }
          description: At most 64 unique edge types totaling at most 64 KiB.
        max_depth:
          type: integer
          minimum: 1
          maximum: 64
          default: 1
          description: Maximum traversal depth. Defaults to one hop to keep fan-out explicit.
        edge_weight:
          x-go-type-skip-optional-pointer: false
          $ref: '#/components/schemas/GraphEdgeWeightRange'
        limit:
          type: integer
          minimum: 1
          maximum: 10000
          default: 100
        include_paths: { type: boolean, default: false }
        include_documents:
          type: boolean
          default: false
          description: >-
            Include each result node's stored document when it exists at the
            pinned snapshot. A dangling graph identity omits document. When
            false, document is always omitted.
        fields:
          type: array
          items: { type: string }
          description: Requires include_documents=true. Omit to include all document fields.
        filter:
          $ref: '#/components/schemas/GraphDocumentFilter'
          description: Non-scoring structured stored-document predicate for reached nodes.
    GraphPathEndpoint:
      type: object
      additionalProperties: false
      required: [key]
      properties:
        key: { type: string, minLength: 1 }
        table:
          x-go-type-skip-optional-pointer: false
          type: string
          minLength: 1
          pattern: '[^ \t\r\n]'
          description: Optional table qualifier for an exact cross-table node identity. Omit for the query table.
    GraphPath:
      type: object
      additionalProperties: false
      required: [nodes, edges, length, objective, weight_sum, objective_value]
      description: An ordered canonical graph path with table-qualified node identities and a self-describing ranking score.
      properties:
        nodes:
          type: array
          minItems: 1
          maxItems: 65
          items: { $ref: '#/components/schemas/GraphPathEndpoint' }
          description: Ordered node identities. Table is omitted for nodes in the query table.
        edges:
          type: array
          maxItems: 64
          items: { $ref: '#/components/schemas/GraphPathEdge' }
          description: Ordered edges; edges[i] traverses from nodes[i] to nodes[i + 1].
        objective:
          $ref: '#/components/schemas/GraphPathObjective'
        weight_sum:
          type: number
          format: double
          minimum: 0
          description: Sum of raw edge weights along the path, independent of the selected ranking objective.
        objective_value:
          type: number
          format: double
          minimum: 0
          description: The user-facing value optimized by objective; edge count for min_hops, weight_sum for min_weight_sum, and the raw edge-weight product for max_weight_product.
        length:
          type: integer
          minimum: 0
          maximum: 64
    GraphShortestPath:
      type: object
      additionalProperties: false
      required: [from, to]
      description: Find the best path from `from` to `to` in the requested stored-edge direction.
      properties:
        from: { $ref: '#/components/schemas/GraphPathEndpoint' }
        to: { $ref: '#/components/schemas/GraphPathEndpoint' }
        direction:
          $ref: '#/components/schemas/EdgeDirection'
          description: Stored-edge direction relative to each expanded path node; defaults to `out`.
        edge_types:
          type: array
          maxItems: 64
          uniqueItems: true
          items: { $ref: '#/components/schemas/GraphEdgeType' }
          description: At most 64 unique edge types totaling at most 64 KiB.
        max_depth:
          type: integer
          minimum: 1
          maximum: 64
          default: 10
        edge_weight:
          x-go-type-skip-optional-pointer: false
          $ref: '#/components/schemas/GraphEdgeWeightRange'
        objective:
          $ref: '#/components/schemas/GraphPathObjective'
          default: min_hops
        filter:
          $ref: '#/components/schemas/GraphDocumentFilter'
          description: Non-scoring structured stored-document predicate for path nodes.
        include_documents:
          type: boolean
          default: false
          description: >-
            Attach the terminal stored document as paths[].document when it
            exists at the pinned snapshot. A dangling terminal identity omits
            document. When false, document is always omitted.
        fields:
          type: array
          items: { type: string }
          description: Requires include_documents=true. Omit to include all document fields.
    GraphKShortestPaths:
      type: object
      additionalProperties: false
      required: [from, to, k]
      description: >-
        Find up to `k` loopless paths from `from` to `to` in the requested
        stored-edge direction. Results are unique by ordered table-qualified
        node identities plus stored-edge direction and type, and are ordered
        best-first by the selected objective.
      properties:
        from: { $ref: '#/components/schemas/GraphPathEndpoint' }
        to: { $ref: '#/components/schemas/GraphPathEndpoint' }
        direction:
          $ref: '#/components/schemas/EdgeDirection'
          description: Stored-edge direction relative to each expanded path node; defaults to `out`.
        k:
          type: integer
          minimum: 1
          maximum: 100
        edge_types:
          type: array
          maxItems: 64
          uniqueItems: true
          items: { $ref: '#/components/schemas/GraphEdgeType' }
          description: At most 64 unique edge types totaling at most 64 KiB.
        max_depth:
          type: integer
          minimum: 1
          maximum: 64
          default: 10
        edge_weight:
          x-go-type-skip-optional-pointer: false
          $ref: '#/components/schemas/GraphEdgeWeightRange'
        objective:
          $ref: '#/components/schemas/GraphPathObjective'
          default: min_hops
        filter:
          $ref: '#/components/schemas/GraphDocumentFilter'
          description: Non-scoring structured stored-document predicate for path nodes.
        include_documents:
          type: boolean
          default: false
          description: >-
            Attach each terminal stored document as paths[].document when it
            exists at the pinned snapshot. A dangling terminal identity omits
            document. When false, document is always omitted.
        fields:
          type: array
          items: { type: string }
          description: Requires include_documents=true. Omit to include all document fields.
    GraphMatchNode:
      type: object
      additionalProperties: false
      description: >-
        Declared under an alias of at most 128 Unicode code points. Omit table
        for the queried table. Declare it for a cross-table alias that may be
        used as the source of a relationship, including planner-selected
        reverse expansion of a branched pattern.
      properties:
        table:
          type: string
          minLength: 1
          pattern: '[^ \t\r\n]'
          description: Owning table for this alias. Omit for the queried table.
        filter:
          $ref: '#/components/schemas/GraphDocumentFilter'
          description: >-
            Non-scoring structured stored-document predicate evaluated for this
            alias. Serverless execution rejects document filters on aliases
            qualified with a different table because its published snapshot
            contains only the queried table. Explicitly qualifying an alias
            with the queried table is equivalent to omitting `table`.
    GraphMatchEdge:
      type: object
      additionalProperties: false
      required: [from, to]
      description: >-
        Structural edge expansion from the `from` alias to the `to` alias.
        Direction defaults to `out`; use `in` to reverse the stored edge or
        `both` to match an undirected relationship without duplicating stored
        edges. A fixed
        single-hop relationship preserves physical self-loops and may bind two
        distinct aliases to the same node identity. Variable-length expansion
        uses node-simple paths: a (table, key) identity is visited at most once
        within one expanded edge path, except when closing onto an already bound
        target alias for an explicit cycle. Exact distributed and serverless
        execution rejects planner-required reverse variable expansion when the
        source tables of unnamed intermediate nodes cannot be proven. Express
        cross-table multi-hop patterns as explicit single-hop edges with a
        table-qualified alias at each table boundary.
      properties:
        from: { $ref: 'generated/graph_identifier.yaml#/components/schemas/GraphIdentifier' }
        to: { $ref: 'generated/graph_identifier.yaml#/components/schemas/GraphIdentifier' }
        direction:
          $ref: '#/components/schemas/EdgeDirection'
          description: Stored-edge direction relative to `from`; defaults to `out`.
        types:
          type: array
          maxItems: 64
          uniqueItems: true
          items: { $ref: '#/components/schemas/GraphEdgeType' }
          description: Empty or omitted matches every edge type; otherwise at most 64 unique types totaling at most 64 KiB.
        min_hops:
          type: integer
          minimum: 1
          maximum: 64
          default: 1
        max_hops:
          type: integer
          minimum: 1
          maximum: 64
          default: 1
        edge_weight:
          x-go-type-skip-optional-pointer: false
          $ref: '#/components/schemas/GraphEdgeWeightRange'
    GraphAliasOperand:
      type: object
      additionalProperties: false
      required: [alias]
      properties:
        alias: { $ref: 'generated/graph_identifier.yaml#/components/schemas/GraphIdentifier' }
    GraphNotEqualPredicate:
      type: object
      additionalProperties: false
      required: [left, right]
      properties:
        left: { $ref: '#/components/schemas/GraphAliasOperand' }
        right: { $ref: '#/components/schemas/GraphAliasOperand' }
    GraphNotExistsPattern:
      type: object
      additionalProperties: false
      required: [edges]
      properties:
        edges:
          type: array
          minItems: 1
          maxItems: 64
          items: { $ref: '#/components/schemas/GraphMatchEdge' }
      description: >-
        Correlated negative-edge predicate over aliases already visible at this
        point in the MATCH. It does not introduce new aliases.
    GraphWhereAnd:
      type: object
      additionalProperties: false
      required: [and]
      properties:
        and:
          type: array
          minItems: 1
          maxItems: 64
          items: { $ref: '#/components/schemas/GraphWhereExpression' }
    GraphWhereNotEqual:
      type: object
      additionalProperties: false
      required: [not_equal]
      properties:
        not_equal: { $ref: '#/components/schemas/GraphNotEqualPredicate' }
    GraphWhereNotExists:
      type: object
      additionalProperties: false
      required: [not_exists]
      properties:
        not_exists: { $ref: '#/components/schemas/GraphNotExistsPattern' }
    GraphWhereExpression:
      oneOf:
        - $ref: '#/components/schemas/GraphWhereAnd'
        - $ref: '#/components/schemas/GraphWhereNotEqual'
        - $ref: '#/components/schemas/GraphWhereNotExists'
    GraphOptionalMatch:
      type: object
      additionalProperties: false
      required: [edges]
      description: >-
        One correlated left-outer graph pattern. Optional groups are evaluated
        in array order and must connect to an alias visible from the required
        MATCH or an earlier optional group. Each input binding is extended by
        every matching optional binding; when none match, exactly one binding
        is retained with every alias introduced by this group set to null.
      properties:
        nodes:
          type: object
          maxProperties: 64
          additionalProperties: { $ref: '#/components/schemas/GraphMatchNode' }
          x-antfly-property-name-schema: { $ref: 'generated/graph_identifier.yaml#/components/schemas/GraphIdentifier' }
          description: Keys are GraphIdentifiers naming aliases introduced by this optional match.
        edges:
          type: array
          minItems: 1
          maxItems: 64
          items: { $ref: '#/components/schemas/GraphMatchEdge' }
        where: { $ref: '#/components/schemas/GraphWhereExpression' }
    GraphMatch:
      type: object
      additionalProperties: false
      required: [anchor, nodes, edges]
      description: >-
        `anchor` names the alias enumerated from the query table as the source
        relation. Every other alias is reached through graph edges and may
        resolve to a table-qualified target identity. An `ids` filter, or a
        disjunction made only of `ids` filters, uses the table's primary
        identity access path and needs no secondary index. Stored-field
        predicates and row-level authorization filters on the anchor must have
        native index coverage so Antfly can enumerate the complete relation in
        `_id` order; otherwise the request fails with
        `graph_anchor_filter_requires_index`.
      properties:
        anchor:
          $ref: 'generated/graph_identifier.yaml#/components/schemas/GraphIdentifier'
        nodes:
          type: object
          minProperties: 1
          maxProperties: 64
          additionalProperties: { $ref: '#/components/schemas/GraphMatchNode' }
          x-antfly-property-name-schema: { $ref: 'generated/graph_identifier.yaml#/components/schemas/GraphIdentifier' }
          description: Keys are GraphIdentifiers naming aliases in the required match.
        edges:
          type: array
          maxItems: 64
          items: { $ref: '#/components/schemas/GraphMatchEdge' }
        where: { $ref: '#/components/schemas/GraphWhereExpression' }
        optional:
          type: array
          maxItems: 64
          items: { $ref: '#/components/schemas/GraphOptionalMatch' }
          description: >-
            Ordered correlated left-outer patterns. Aliases introduced by an
            earlier item are visible to later items, including as null.
    GraphRowCountTarget:
      type: string
      enum: ['*']
      description: >-
        Count every complete graph binding, including a binding retained by an
        unmatched optional group through null extension.
    GraphRowCountAggregate:
      type: object
      additionalProperties: false
      required: [count]
      properties:
        count:
          $ref: '#/components/schemas/GraphRowCountTarget'
    GraphAliasCountAggregate:
      type: object
      additionalProperties: false
      required: [count]
      properties:
        count:
          $ref: 'generated/graph_identifier.yaml#/components/schemas/GraphIdentifier'
          description: >-
            Count bindings in which this alias is non-null. An unmatched
            optional alias does not increment the count.
        distinct:
          type: boolean
          default: false
          description: >-
            Count exact table-qualified identities. Exact distinct sets share a
            request resource budget and fail with
            `graph_distinct_budget_exceeded` instead of returning a partial count.
    GraphCountAggregate:
      oneOf:
        - $ref: '#/components/schemas/GraphRowCountAggregate'
        - $ref: '#/components/schemas/GraphAliasCountAggregate'
      description: >-
        Exact count(*) or count(alias). The distinct option exists only for
        alias counts, making distinct count(*) invalid by schema and at runtime.
        count(*) includes null-extended optional rows; count(alias) counts only
        bindings where that alias is non-null.
    GraphBindingsReturn:
      type: object
      additionalProperties: false
      required: [bindings]
      properties:
        bindings:
          type: array
          minItems: 1
          maxItems: 64
          uniqueItems: true
          items: { $ref: 'generated/graph_identifier.yaml#/components/schemas/GraphIdentifier' }
        limit:
          type: integer
          minimum: 1
          maximum: 10000
          default: 100
        include_documents:
          type: boolean
          default: false
          description: >-
            Hydrate documents for projected non-null bindings when they exist at
            the pinned snapshot. A dangling graph identity omits document. When
            false, document is always omitted. The product of `limit` and the
            number of projected bindings may not exceed 10,000. Table-qualified
            bindings are hydrated by coordinator-backed deployments; runtimes
            with only a source-table snapshot reject such requests instead of
            silently omitting an available document.
        fields:
          type: array
          items: { type: string }
          description: Document fields to hydrate. Requires include_documents=true; omit to include all fields.
    GraphAggregatesReturn:
      type: object
      additionalProperties: false
      required: [aggregates]
      properties:
        aggregates:
          type: object
          minProperties: 1
          maxProperties: 64
          additionalProperties: { $ref: '#/components/schemas/GraphCountAggregate' }
          x-antfly-property-name-schema: { $ref: 'generated/graph_identifier.yaml#/components/schemas/GraphIdentifier' }
          description: Keys are GraphIdentifiers naming aggregate results.
    GraphReturn:
      oneOf:
        - $ref: '#/components/schemas/GraphBindingsReturn'
        - $ref: '#/components/schemas/GraphAggregatesReturn'
      description: Return bindings or exact aggregates. Bindings and aggregates are mutually exclusive.
    GraphMatchQuery:
      type: object
      additionalProperties: false
      required: [index, match, return]
      description: >-
        Conjunctive graph match over the complete authorized source universe.
        Top-level retrieval queries and filters do not scope that universe; put
        source constraints on the node named by match.anchor.
        Results are exact or the request fails; execution never labels a partial
        aggregate exact. Source anchors are streamed in stable snapshot-pinned
        pages and charged to the request-wide `scanned_anchors` work dimension;
        transient expansion state remains bounded, and execution observes request
        deadlines, cancellation, and server resource admission. Exact
        distinct identity sets are also bounded and fail closed when their
        request-scoped memory budget is exhausted.
      properties:
        index: { type: string, minLength: 1, pattern: '.*\S.*' }
        match: { $ref: '#/components/schemas/GraphMatch' }
        return: { $ref: '#/components/schemas/GraphReturn' }
    GraphTraverseQuery:
      type: object
      additionalProperties: false
      required: [index, traverse]
      properties:
        index: { type: string, minLength: 1, pattern: '.*\S.*' }
        traverse: { $ref: '#/components/schemas/GraphTraversal' }
    GraphShortestPathQuery:
      type: object
      additionalProperties: false
      required: [index, shortest_path]
      properties:
        index: { type: string, minLength: 1, pattern: '.*\S.*' }
        shortest_path: { $ref: '#/components/schemas/GraphShortestPath' }
    GraphKShortestPathsQuery:
      type: object
      additionalProperties: false
      required: [index, k_shortest_paths]
      properties:
        index: { type: string, minLength: 1, pattern: '.*\S.*' }
        k_shortest_paths: { $ref: '#/components/schemas/GraphKShortestPaths' }
    GraphQuery:
      oneOf:
        - $ref: '#/components/schemas/GraphMatchQuery'
        - $ref: '#/components/schemas/GraphTraverseQuery'
        - $ref: '#/components/schemas/GraphShortestPathQuery'
        - $ref: '#/components/schemas/GraphKShortestPathsQuery'
    GraphQueries:
      type: object
      minProperties: 1
      maxProperties: 64
      additionalProperties:
        $ref: '#/components/schemas/GraphQuery'
      x-antfly-property-name-schema:
        $ref: 'generated/graph_identifier.yaml#/components/schemas/GraphIdentifier'
      description: >-
        Named canonical graph operations. When graph_queries is present it must
        contain at least one operation. A request may contain at most 64
        operations, of which at most eight may be MATCH operations. Keys use
        the versioned GraphIdentifier policy.
    GraphResultNode:
      x-go-type-skip-optional-pointer: true
      type: object
      additionalProperties: false
      description: >-
        A traversal result node. Traversal paths, when requested, are carried by
        path and path_edges. Pathfinding results use GraphPathResult instead.
      required:
        - key
        - depth
      properties:
        key:
          x-go-type-skip-optional-pointer: true
          type: string
          minLength: 1
          description: "Document key"
        table:
          x-go-type-skip-optional-pointer: false
          type: string
          minLength: 1
          pattern: '[^ \t\r\n]'
          description: "Owning table for a cross-table node; omitted for nodes in the queried table"
        depth:
          x-go-type-skip-optional-pointer: true
          type: integer
          minimum: 0
          maximum: 64
          description: "Hop count from the start node; when path is present this equals path length minus one"
        document:
          x-go-type-skip-optional-pointer: true
          type: object
          additionalProperties: true
          description: >-
            Stored document when include_documents=true and the identity exists
            at the pinned snapshot; otherwise omitted.
        path:
          x-go-type-skip-optional-pointer: true
          type: array
          minItems: 1
          maxItems: 65
          items:
            $ref: '#/components/schemas/GraphPathEndpoint'
          description: >-
            Exact ordered traversal identities from the start node, terminating
            at this node's fully qualified identity. Present only for traversal
            queries with include_paths=true.
        path_edges:
          x-go-type-skip-optional-pointer: true
          type: array
          maxItems: 64
          items:
            $ref: '#/components/schemas/GraphPathEdge'
          description: >-
            Ordered typed traversal edges from the start node. Present only with
            path for traversal queries.
        provenance:
          x-go-type-skip-optional-pointer: true
          type: array
          items:
            type: string
          description: "Algebraic provenance labels folded into this result, when requested by an algebraic graph executor"
        evidence:
          x-go-type-skip-optional-pointer: true
          type: object
          additionalProperties: true
          description: "Parsed evidence envelope for provenance labels and edge metadata"
    LegacyGraphResultNode:
      x-go-type-skip-optional-pointer: true
      type: object
      deprecated: true
      description: Deprecated graph_searches node response with an unqualified string path.
      required:
        - key
      properties:
        key:
          x-go-type-skip-optional-pointer: true
          type: string
          description: "Document key"
        table:
          x-go-type-skip-optional-pointer: true
          type: string
          description: "Owning table for a cross-table node; omitted for nodes in the queried table"
        depth:
          x-go-type-skip-optional-pointer: true
          type: integer
          description: "Distance from start node"
        distance:
          x-go-type-skip-optional-pointer: true
          type: number
          format: double
          description: "Weighted distance"
        document:
          x-go-type-skip-optional-pointer: true
          type: object
          additionalProperties: true
          description: "Full document (if include_documents=true)"
        path:
          x-go-type-skip-optional-pointer: true
          type: array
          items:
            type: string
          description: "Deprecated unqualified keys in the path from the start node to this node"
        path_edges:
          x-go-type-skip-optional-pointer: true
          type: array
          items:
            $ref: '#/components/schemas/PathEdge'
          description: "Edges in path from start to this node"
        provenance:
          x-go-type-skip-optional-pointer: true
          type: array
          items:
            type: string
          description: "Algebraic provenance labels folded into this result, when requested by an algebraic graph executor"
        evidence:
          x-go-type-skip-optional-pointer: true
          type: object
          additionalProperties: true
          description: "Parsed evidence envelope for provenance labels and edge metadata"
        edges:
          x-go-type-skip-optional-pointer: true
          type: array
          items:
            $ref: '#/components/schemas/Edge'
          description: "Connected edges when supplied by the graph executor."
    GraphAggregateValue:
      type: object
      additionalProperties: false
      required: [value, exact]
      properties:
        value:
          type: string
          pattern: '^[0-9]+$'
          description: Decimal string so counts remain lossless in JavaScript.
        exact:
          type: boolean
          enum: [true]
          description: Always true. Exact aggregate execution fails instead of returning a partial value.
    PatternMatch:
      x-go-type-skip-optional-pointer: true
      type: object
      deprecated: true
      description: Deprecated graph_searches pattern response row.
      properties:
        bindings:
          x-go-type-skip-optional-pointer: true
          type: object
          additionalProperties:
            $ref: '#/components/schemas/LegacyGraphResultNode'
        path:
          x-go-type-skip-optional-pointer: true
          type: array
          items:
            $ref: '#/components/schemas/PathEdge'
    GraphResultStats:
      type: object
      additionalProperties: false
      required: [returned_items, truncated]
      description: Completion statistics for a bounded graph result.
      properties:
        returned_items:
          type: integer
          format: uint64
          minimum: 0
          maximum: 10000
          description: Number of primary result items returned (nodes or rows).
        truncated:
          type: boolean
          description: True when bounded enumeration stopped before exhaustive completion.
    GraphExactResultStats:
      type: object
      additionalProperties: false
      required: [returned_items]
      description: Completion statistics for a graph result that is exact or fails without producing a result.
      properties:
        returned_items:
          type: integer
          format: uint64
          minimum: 0
          maximum: 10000
          description: Number of primary result items returned (paths or aggregates).
    GraphBindingsResult:
      x-go-type-skip-optional-pointer: true
      type: object
      additionalProperties: false
      required: [kind, rows, stats]
      description: A deterministic bounded prefix of projected bindings from a canonical graph MATCH query. Inspect stats.truncated to determine whether enumeration was exhaustive.
      properties:
        kind:
          type: string
          enum: [bindings]
          description: Stable discriminator for the graph result shape.
        rows:
          type: array
          maxItems: 10000
          items: { $ref: '#/components/schemas/GraphResultRow' }
        stats: { $ref: '#/components/schemas/GraphResultStats' }
    GraphAggregatesResult:
      x-go-type-skip-optional-pointer: true
      type: object
      additionalProperties: false
      required: [kind, aggregates, stats]
      description: Complete exact aggregates from a canonical graph MATCH query.
      properties:
        kind:
          type: string
          enum: [aggregates]
          description: Stable discriminator for the graph result shape.
        aggregates:
          type: object
          minProperties: 1
          maxProperties: 64
          additionalProperties:
            $ref: '#/components/schemas/GraphAggregateValue'
          x-antfly-property-name-schema:
            $ref: 'generated/graph_identifier.yaml#/components/schemas/GraphIdentifier'
          description: Keys are the GraphIdentifiers selected by the corresponding aggregate return projection.
        stats: { $ref: '#/components/schemas/GraphExactResultStats' }
    GraphNodesResult:
      x-go-type-skip-optional-pointer: true
      type: object
      additionalProperties: false
      required: [kind, nodes, stats]
      description: Composable results from a canonical traversal query.
      properties:
        kind:
          type: string
          enum: [nodes]
          description: Stable discriminator for the graph result shape.
        nodes:
          type: array
          maxItems: 10000
          items: { $ref: '#/components/schemas/GraphResultNode' }
          description: Traversal result nodes; requested paths are stored on each node.
        stats: { $ref: '#/components/schemas/GraphResultStats' }
    GraphPathResult:
      x-go-type-skip-optional-pointer: true
      type: object
      additionalProperties: false
      required: [path]
      description: >-
        One authoritative pathfinding result. The terminal identity is the last
        element of path.nodes, so it is never duplicated in a parallel node array.
      properties:
        path: { $ref: '#/components/schemas/GraphPath' }
        document:
          x-go-type-skip-optional-pointer: true
          type: object
          additionalProperties: true
          description: >-
            Stored terminal document when include_documents=true and the terminal
            identity exists at the pinned snapshot; otherwise omitted.
    GraphPathsResult:
      x-go-type-skip-optional-pointer: true
      type: object
      additionalProperties: false
      required: [kind, paths, stats]
      description: Composable results from canonical shortest_path or k_shortest_paths queries.
      properties:
        kind:
          type: string
          enum: [paths]
          description: Stable discriminator for the graph result shape.
        paths:
          type: array
          maxItems: 100
          items: { $ref: '#/components/schemas/GraphPathResult' }
        stats: { $ref: '#/components/schemas/GraphExactResultStats' }
    LegacyGraphSearchResult:
      x-go-type-skip-optional-pointer: true
      type: object
      additionalProperties: false
      deprecated: true
      required: [type, total]
      description: Deprecated graph_searches response envelope.
      properties:
        kind:
          type: string
          enum: [legacy]
          description: >-
            Optional transition discriminator accepted by current SDKs. Servers
            omit it for graph_searches during the v0.2 compatibility release so
            strict previously generated clients continue to decode the original
            response shape.
        type:
          deprecated: true
          $ref: "#/components/schemas/GraphQueryType"
        nodes:
          x-go-type-skip-optional-pointer: true
          type: array
          items: { $ref: '#/components/schemas/LegacyGraphResultNode' }
          description: Result nodes. Optional for compatibility with v0.2 responses.
        paths:
          x-go-type-skip-optional-pointer: true
          type: array
          items: { $ref: '#/components/schemas/Path' }
          description: Result paths. Optional for compatibility with v0.2 responses.
        matches:
          deprecated: true
          x-go-type-skip-optional-pointer: true
          type: array
          items:
            $ref: '#/components/schemas/PatternMatch'
          description: Deprecated graph_searches pattern results; use rows for graph_queries.
        total:
          deprecated: true
          x-go-type-skip-optional-pointer: true
          type: integer
          description: Deprecated graph_searches result count; use stats or a named count aggregate.
        took:
          x-go-type-skip-optional-pointer: true
          type: integer
          format: int64
          deprecated: true
          description: "Whole-query execution time in milliseconds; optional for compatibility with v0.2 responses. Use the parent query result's took field."
    GraphResult:
      description: >-
        A canonical result produced by graph_queries. Bindings, exact
        aggregates, and node/path results use required stable discriminators.
      oneOf:
        - $ref: '#/components/schemas/GraphBindingsResult'
        - $ref: '#/components/schemas/GraphAggregatesResult'
        - $ref: '#/components/schemas/GraphNodesResult'
        - $ref: '#/components/schemas/GraphPathsResult'
      discriminator:
        propertyName: kind
        mapping:
          bindings: '#/components/schemas/GraphBindingsResult'
          aggregates: '#/components/schemas/GraphAggregatesResult'
          nodes: '#/components/schemas/GraphNodesResult'
          paths: '#/components/schemas/GraphPathsResult'
    StatefulGraphResult:
      description: >-
        Graph result emitted by the stateful compatibility transport. Canonical
        graph_queries produce GraphResult; deprecated graph_searches may produce
        LegacyGraphSearchResult during the compatibility window.
      oneOf:
        - $ref: '#/components/schemas/GraphResult'
        - $ref: '#/components/schemas/LegacyGraphSearchResult'
    GraphQueryResults:
      type: object
      minProperties: 1
      maxProperties: 64
      additionalProperties:
        $ref: '#/components/schemas/GraphResult'
      x-antfly-property-name-schema:
        $ref: 'generated/graph_identifier.yaml#/components/schemas/GraphIdentifier'
      description: >-
        Non-empty canonical graph results keyed exactly by graph_queries
        operation name. Keys use the versioned GraphIdentifier policy.
    StatefulGraphQueryResults:
      type: object
      minProperties: 1
      maxProperties: 64
      additionalProperties:
        $ref: '#/components/schemas/StatefulGraphResult'
      description: >-
        Stateful graph results keyed by operation name. Legacy values are
        possible only when the corresponding request used graph_searches.
    GraphBindingNode:
      type: object
      additionalProperties: false
      required: [key]
      description: >-
        One exact node identity projected from a MATCH binding. Conjunctive
        bindings deliberately do not expose traversal depth, distance, or path:
        those values are not uniquely defined for branched patterns and may
        depend on execution order.
      properties:
        key:
          type: string
          minLength: 1
          description: Exact document key.
        table:
          x-go-type-skip-optional-pointer: false
          type: string
          minLength: 1
          pattern: '[^ \t\r\n]'
          description: Owning table for a cross-table binding; omitted for the queried table.
        document:
          type: object
          additionalProperties: true
          description: >-
            Stored document when include_documents=true and the identity exists
            at the pinned snapshot; otherwise omitted.
    GraphResultBinding:
      x-go-type: '*GraphBindingNode'
      oneOf:
        - $ref: '#/components/schemas/GraphBindingNode'
        - enum: [null]
    GraphResultRow:
      type: object
      minProperties: 1
      maxProperties: 64
      additionalProperties: { $ref: '#/components/schemas/GraphResultBinding' }
      x-antfly-property-name-schema:
        $ref: 'generated/graph_identifier.yaml#/components/schemas/GraphIdentifier'
